# ops/init/projects

This folder stores default scaffolds for new projects.
Templates are README-first; no project payloads live here.

- `default/README.md` is the minimal scaffold used by `ops/bin/project init`.
