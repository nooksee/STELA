---
trace_id: stela-20260301T182329Z-3c29505d
decision_id: RoR-2026-03-01-003
packet_id: DP-OPS-0140
decision_type: cbc
created_at: 2026-03-01T18:23:29Z
authorized_by: Operator
---

## Context

CbC Design Discipline Preflight record for packet DP-OPS-0140.
Tool or binary under review: tools/verify.sh

## CbC Preflight Questions

**Q1: Is structural prevention viable instead of detection?**
No. verify.sh asserts yes/no facts about observable filesystem state (directory existence, factory head reachability, storage subdirectory presence, and Filing Doctrine compliance). No structural mechanism in a permissive git repository can prevent filesystem layout violations at creation time; custom git hooks and filesystem ACLs were identified as alternatives and evaluated as disproportionate.

**Q2: Does the enforcement complexity exceed 100 lines without justification?**
No. 287 lines. The line count is explained by the breadth of distinct filesystem assertions (multiple directory targets, multiple factory head files, multiple storage subdirectory checks). The logic per check is flat and straightforward.

**Q3: Was structural prevention identified as viable but deferred?**
No. Git hooks and filesystem ACLs were identified as alternatives and evaluated as disproportionate. No structural fix was deferred.

**Q4: Complexity score (1–5) and rationale:**
Score: 2
Flat filesystem-assertion logic. Each check is a yes/no predicate against the filesystem with no complex parsing. The 287-line count reflects check breadth, not design complexity.

**Q5: Does a SoP note record any deferred structural prevention decision?**
No. Both alternatives were rejected as disproportionate; no ongoing deferral is recorded.

## Decision

C — Keep. Without verify.sh, Filing Doctrine violations surface as silent downstream failures rather than clean early errors. No redesign recommended.

## Consequence

Allowlist enforcement requires this leaf to be present and allowlisted before certify runs
when the CbC preflight is applicable. Integrity lint will fail if a cbc leaf is absent from
the allowlist when the TASK.md CbC preflight slot does not begin with "Not applicable".

## Pointer

- Authorized by: Operator

## Status

accepted
