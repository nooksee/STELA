---
trace_id: stela-20260316T161414Z-4553755a
decision_id: RoR-2026-03-16-002
packet_id: DP-OPS-0202
decision_type: cbc
created_at: 2026-03-16T16:14:14Z
authorized_by: Operator
---

## Context

CbC Design Discipline Preflight record for packet DP-OPS-0202.
Tool or binary under review: tools/test/bundle.sh

## CbC Preflight Questions

**Q1: Is structural prevention viable instead of detection?**
No. Bundle transport correctness depends on runtime surfaces (`PLAN.md`, `TOPIC.md`, current `RESULTS`, current `CLOSING`, current packet source, package members, and dump file blocks). Those invariants require a live smoke path.

**Q2: Does the enforcement complexity exceed 100 lines without justification?**
No. The smoke matrix is large, but it covers the canonical transport contract and catches regressions earlier than audit or certify would.

**Q3: Was structural prevention identified as viable but deferred?**
Yes. Operator-facing churn was structural and is now addressed by quarantine under `storage/_smoke/`. The smoke itself remains necessary.

**Q4: Complexity score (1–5) and rationale:**
Score: 4
Keep. `tools/test/bundle.sh` remains inside certify replay because bundle and audit-delivery correctness are closeout-critical. The packet-local work in DP-OPS-0202 narrows where those artifacts land, but it does not reduce the need for a real transport smoke gate.

**Q5: Does a SoP note record any deferred structural prevention decision?**
Yes. DP-OPS-0202 closeout records that smoke outputs are quarantined under `storage/_smoke/` for tests while operator-facing bundle outputs remain under `storage/handoff/` and `storage/dumps/`.

## Decision

Keep `tools/test/bundle.sh` in certify-critical replay as a closeout-critical lane. Quarantine its explicit outputs under `storage/_smoke/`, but preserve real bundle, dump, manifest, and package behavior.

## Consequence

Bundle regressions that would invalidate audit delivery still fail during certify. The operator no longer has to watch transient smoke artifacts overwrite the same roots used for final handoff output.

## Pointer

- Authorized by: Operator
- Lane: bundle-smoke
- Reason class: closeout-critical
- Policy surface: ops/lib/manifests/VERIFY.md

## Status

accepted
