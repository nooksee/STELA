---
trace_id: stela-20260312T140000Z-0184cbc1
decision_id: RoR-2026-03-12-001
packet_id: DP-OPS-0184
decision_type: cbc
created_at: 2026-03-12T14:00:00Z
authorized_by: Operator
---

## Context

CbC Design Discipline Preflight record for packet DP-OPS-0184.
Tool or binary under review: `ops/src/stances/analyst.md.tpl`, `ops/src/stances/architect.md.tpl`, `ops/src/stances/auditor.md.tpl`, `ops/src/stances/foreman.md.tpl`, `tools/lint/style.sh`, `docs/ops/README.md`, `docs/ops/registry/templates.md`, `ops/src/docs/runbook.md.tpl`, `ops/src/runbook/*.yaml`, `docs/ops/specs/runbook/*.md`, and runtime plan wording for runbook activation state plus legacy `auditor` alias retirement.

## CbC Preflight Questions

**Q1: Is structural prevention viable instead of detection?**
Yes, partially. The structural machine-ingest floor already exists in shared fence rules, first-line markers, role-boundary rules, and addendum/audit split contracts. The targeted symptom-suppressor prose is not structural and can be removed without weakening the real intake contract. For external model instructions, structural prevention means codifying them as explicit runbook assets with proof rules instead of leaving them as invisible browser-state folklore, and avoiding undeployed instruction text that falsely implies activation.

**Q2: Does the enforcement complexity exceed 100 lines without justification?**
No. The packet removes narrow wording locks, retires one mirrored style convergence rule, deletes an obsolete generic template, adds four self-contained runbook YAML assets with matching specs, and narrows their proof model by removing a dead self-disclosure path. The complexity risk is coordination across mirrored surfaces, not algorithmic complexity.

**Q3: Was structural prevention identified as viable but deferred?**
Yes. The system-wide CbC review identified this cluster as a B-score redesign candidate: retain shared structural rules, remove symptom-patch prose, stop requiring exact wording whose only purpose is to preserve that prose, represent external adapter deployment as concrete runbook assets rather than implicit operator memory, and keep undeployed adapters textless until activation is explicit.

**Q4: Complexity score (1–5) and rationale:**
Score: 3
The cleanup is bounded and mechanically simple, but it spans multiple mirrored stance surfaces, one style convergence rule, a template retirement, new runbook source/spec surfaces, and proof-model cleanup. Moderate complexity is justified by the need to avoid leaving stale wording locks, misleading undeployed text, or orphaned abstractions behind.

**Q5: Does a SoP note record any deferred structural prevention decision?**
No current SoP note records this specific deferral. This leaf is the explicit CbC record for the redesign.

## Decision

B — Keep the structural stance contract, remove symptom-patch prose plus any exact-text style convergence that exists only to preserve that prose, formalize external adapter deployment as explicit runbook assets, and keep undeployed adapters textless until activation is explicit.

## Consequence

Allowlist enforcement requires this leaf to be present and allowlisted before certify runs
when the CbC preflight is applicable. Integrity lint will fail if a cbc leaf is absent from
the allowlist when the TASK.md CbC preflight slot does not begin with "Not applicable".

## Pointer

- Authorized by: Operator

## Status

accepted
