---
trace_id: stela-20260301T182329Z-81bcc3d5
decision_id: RoR-2026-03-01-009
packet_id: DP-OPS-0140
decision_type: cbc
created_at: 2026-03-01T18:23:29Z
authorized_by: Operator
---

## Context

CbC Design Discipline Preflight record for packet DP-OPS-0140.
Tool or binary under review: tools/lint/integrity.sh

## CbC Preflight Questions

**Q1: Is structural prevention viable instead of detection?**
No. The allowlist contract is a structural mechanism at the DP layer, but detecting violations of that contract requires scanning the working tree at execution time. There is no mechanism to prevent unauthorized file creation during DP execution without an active detection step.

**Q2: Does the enforcement complexity exceed 100 lines without justification?**
No. 183 lines. The check is a three-step process (read allowlist pointer from TASK.md, resolve the allowlist file, compare against changed and untracked paths) with distinct git porcelain interactions at each step.

**Q3: Was structural prevention identified as viable but deferred?**
No. The allowlist contract itself is the structural prevention layer; integrity.sh is the detection component that enforces it. No structural alternative beyond detection has been identified.

**Q4: Complexity score (1–5) and rationale:**
Score: 2
Three clearly separated operations (pointer resolution, allowlist read, path comparison) with no complex parsing. Moderate line count for the precision required across git working-tree queries.

**Q5: Does a SoP note record any deferred structural prevention decision?**
No.

## Decision

C — Keep. Reference implementation of a CbC-justified linter. Without integrity.sh, any file modification during a DP execution passes gates regardless of declared scope. The allowlist contract is the structural layer; integrity.sh is the detection enforcement.

## Consequence

Allowlist enforcement requires this leaf to be present and allowlisted before certify runs
when the CbC preflight is applicable. Integrity lint will fail if a cbc leaf is absent from
the allowlist when the TASK.md CbC preflight slot does not begin with "Not applicable".

## Pointer

- Authorized by: Operator

## Status

accepted
