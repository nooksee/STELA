---
trace_id: stela-20260402T035518Z-2e3992f9
decision_id: RoR-2026-04-02-001
packet_id: DP-OPS-0242
decision_type: op
created_at: 2026-04-02T03:55:18Z
authorized_by: Operator
---

## Context

Operator authorization record for packet DP-OPS-0242.

The first `DP-OPS-0242` attempt targeted direct contractor stance/spec removal. Execution showed that the real blocker was not contractor routing but the deletion lifecycle across allowlist gates:

- `tools/lint/dp.sh` already tolerated deleted tracked files remaining in the allowlist while a delete diff was still in-flight
- `tools/lint/integrity.sh` still required current allowlist coverage for those same deleted paths
- `DP-OPS-0241` had already shown the clean-tree aftermath of that mismatch when stale allowlist entries failed later in CI

Operator authorized rewinding the failed delete attempt, rewriting the roadmap and intake around the prerequisite gate fix, and recording that decision explicitly before continuing the decoupling spine.

## Decision

Scope boundary: replace direct contractor removal as the immediate packet with the narrower deletion-semantics gate fix; update only `tools/lint/integrity.sh`, `tools/lint/dp.sh`, their coupled specs, and the RoR pointer path for this decision.

Authorization: Operator authorizes the prerequisite `DP-OPS-0242` gate-fix packet, including creation of this op decision leaf and recertification of the packet so the receipt and audit chain reflect the corrected decision path. Contractor stance/spec removal is deferred to the next dependent packet after this gate fix lands.

## Consequence

This record provides the authorization trail for any operator-approved scope expansion,
exception, or out-of-band action taken during DP-OPS-0242 execution.

## Pointer

- Authorized by: Operator

## Status

accepted
