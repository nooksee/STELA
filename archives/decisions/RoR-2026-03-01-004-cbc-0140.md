---
trace_id: stela-20260301T182329Z-dc7a3796
decision_id: RoR-2026-03-01-004
packet_id: DP-OPS-0140
decision_type: cbc
created_at: 2026-03-01T18:23:29Z
authorized_by: Operator
---

## Context

CbC Design Discipline Preflight record for packet DP-OPS-0140.
Tool or binary under review: tools/lint/dp.sh

## CbC Preflight Questions

**Q1: Is structural prevention viable instead of detection?**
Partial. The template hash mechanism is prevention-adjacent: ops/bin/draft generates the DP structure and dp.sh confirms it was not tampered with. AI-authored content fields (narrative body, receipt commands, closing block) cannot be structurally prevented from containing invalid state; detection is required for content-layer violations.

**Q2: Does the enforcement complexity exceed 100 lines without justification?**
Yes — 1,127 lines exceeds 100. Justified by the embedded --test mode fixture harness (approximately 200 lines), which accounts for the overage. The core linter covers allowlist pointer integrity, placeholder detection, closing block field validation, and canonical template hash verification — each a distinct validation concern.

**Q3: Was structural prevention identified as viable but deferred?**
The test fixture extraction to tools/test/dp.sh is identified as a viable future refactor that would reduce the core linter to approximately 900 lines. This is queued as a low-priority improvement and is not a blocking structural prevention decision.

**Q4: Complexity score (1–5) and rationale:**
Score: 4
High line count with an embedded test fixture and multiple distinct validation concerns (template hash, structure hash, allowlist pointer, placeholder detection, closing block). The test fixture is the primary maintenance complexity driver; the core linter is well-structured.

**Q5: Does a SoP note record any deferred structural prevention decision?**
No. The test fixture extraction is queued as a code organization improvement, not a structural prevention deferral.

## Decision

C — Keep. Without dp.sh, hand-authored DPs with arbitrary structure pass all gates and the allowlist pointer check vanishes. Refactor candidate noted: extract test fixture to tools/test/dp.sh as a future work item. Not urgent.

## Consequence

Allowlist enforcement requires this leaf to be present and allowlisted before certify runs
when the CbC preflight is applicable. Integrity lint will fail if a cbc leaf is absent from
the allowlist when the TASK.md CbC preflight slot does not begin with "Not applicable".

## Pointer

- Authorized by: Operator

## Status

accepted
