---
trace_id: stela-20260402T021729Z-211946df
packet_id: DP-OPS-0241
created_at: 2026-04-02T02:21:30Z
previous: archives/surfaces/TASK-DP-OPS-0240-786f20a2.md
---
# STELA TASK DASHBOARD
Status: ACTIVE
Owner: Integrator
Last Updated: 2026-02-14

## 1. Session State
Pointer: Session context output (generated by ops/bin/open)
Context Manifest: ops/lib/manifests/CONTEXT.md (checked by tools/lint/context.sh)

Session State contract:
- Session State is pointer-first. Do not write inline branch or hash state here.
- OPEN (and OPEN-PORCELAIN when dirty) is the session state snapshot.
- RESULTS is the audit record for the DP.
- TASK is the stable routing surface.

## 2. Logic Pointers
Primary Constraint: PoT.md (Policy of Truth).
Surface Contract Spec: docs/ops/specs/surfaces/task.md.

### 2.1 Governance Pointers
Jurisdiction: PoT.md Section 3.
Git Authority: PoT.md Section 4.1.
Behavioral Standard: PoT.md Section 4.2.
Staffing Protocol: PoT.md Section 4.1.

### 2.2 Enforcement and Gates
Style: tools/lint/style.sh
Context: tools/lint/context.sh
Truth: tools/lint/truth.sh
DP lint: tools/lint/dp.sh (enforces DP transaction rules in Section 3; does not enforce TASK container schema)
TASK lint: tools/lint/task.sh (sole TASK surface/container schema enforcer)
Repo verify: tools/verify.sh

## 3. Current Dispatch Packet (DP)

Section-by-section authoring protocol:
- Generate DP structure via `./ops/bin/bundle --profile=draft --out=auto` (standard operator path: Architect completes the embedded DP AUTHORING SCAFFOLD; save the returned DP body to `storage/dp/intake/DP.md`) or locally via `ops/bin/draft` (template render only).
- Do not hand-edit DP structural boilerplate in `TASK.md` after generation.
- Manual edits are limited to slot content only (objective, scope, safety, plan body, receipt body).
- Freeze each completed subsection in order (3.1 -> 3.2 -> 3.3 -> 3.4.x).
- Do not revise earlier sections to justify later work. If scope changes, end the thread and draft a new DP.

Freeze Rule:
- If a completed DP subsection changes later, append an AMENDMENT block in RESULTS; otherwise DP is FAIL.

AMENDMENT: minimum fields
- Reopened section:
- Reason:
- Impact:
- Allowlist change (none or exact paths):

Input discipline (hard rule):
- Worker receives only the DP text. Worker does not require OPEN or DUMP attachments.
- DP writer must not attach or cite disposable artifacts (rough notes, chat logs, scratch drafts, throwaway excerpts).
- DP writer must not include pasted bundles (OPEN, DUMP payloads, manifests) inside the DP body.
- Canon pointers only. Evidence belongs in storage artifacts and RESULTS.


### DP-OPS-0241: Remove Conform Lane from Active Shipping Surfaces

## 3.1 Freshness Gate (Must Pass Before Work)
Base Branch: main
Work Branch: work/dp-ops-0241-2026-04-01
Base HEAD: a9a952e7
Freshness Stamp: 2026-04-01
Note: FRESHNESS_STAMP must be YYYY-MM-DD format only. No trace tokens, no timestamps, no other text. Certify rejects all other forms. dp.sh enforces at lint time.

Required local re-check (worker runs before any edits; paste verbatim outputs under `## Contractor Execution Narrative` -> `### Preflight State` in RESULTS):
- git rev-parse --abbrev-ref HEAD
- git rev-parse --short HEAD
- git status --porcelain

These three commands are freshness proof only. Do not treat §3.4.5 receipt replay as a substitute for their pre-edit outputs.

Preconditions:
- No commits on main.
- Working tree must be clean before execution begins.
- If Base HEAD changes, regenerate session artifacts from canonical tools before proceeding.

STOP conditions:
- STOP if any mismatch (branch, Base HEAD, missing work branch).
- STOP if working tree is dirty before execution begins.
- STOP if told to create or switch branches.

## 3.1.1 DP Preflight Gate (Run Before Any Edits)
Purpose:
- Catch malformed DP or TASK schema before work begins.

Worker runs (paste outcome in RESULTS):
- bash tools/lint/dp.sh --test
- bash tools/lint/dp.sh TASK.md
- bash tools/lint/task.sh

STOP if any preflight check fails.

### CbC Design Discipline Preflight (TASK.md §3.1.1)
Required when the DP objective adds, modifies, or replaces a linter, script, guard, or validation binary.
For non-tooling DPs: state "Not applicable" with a one-line justification.
Tooling-applicable. This packet removes a live secondary-lane profile/stance path and updates the coupled bundle, manifest, lint, test, and operator-facing docs surfaces that still advertise or validate that lane.

## 3.2 Required Context Load (Read Before Doing Anything)

### 3.2.1 Canon load order (always)
Worker must confirm loaded before edits begin:
1. PoT.md
2. SoP.md
3. PoW.md
4. TASK.md
5. docs/MAP.md
6. docs/MANUAL.md
7. ops/lib/manifests/CONTEXT.md

Notes:
- Worker does not read OPEN. OPEN is for Integrator state refresh and for receipts.
- Disposable artifacts must not be referenced or included.
- Worker input is the DP text only.
- DP writer must not attach or cite disposable artifacts.
- DP writer must not embed pasted bundles.

## Rules

1 Temp path rule uses `var/tmp` only
2 RESULTS generation rule uses `ops/bin/certify` only
3 Receipt rule runs every DP mandated command and treats any skip as STOP
4 Disposable artifact rule treats OPEN dump bundles and manifests as non canonical session artifacts
5 Use repo-relative paths only in all output. Never print absolute filesystem paths. Do not emit clickable file links.

### 3.2.2 DP-scoped load order (per DP)
- ops/lib/manifests/BUNDLE.md
- ops/lib/scripts/bundle.sh
- ops/src/stances/conformist.md.tpl
- docs/ops/specs/stances/conformist.md
- ops/bin/manifest
- ops/bin/template
- tools/lint/response.sh
- docs/ops/specs/tools/lint/response.md
- tools/lint/style.sh
- docs/ops/specs/tools/lint/style.md
- tools/test/bundle.sh
- docs/ops/specs/tools/test/bundle.md
- ops/etc/verification.manifest
- docs/MANUAL.md
- docs/MAP.md
- ops/bin/help
- ops/bin/open
- docs/ops/specs/binaries/bundle.md
- docs/ops/specs/binaries/manifest.md
- docs/ops/specs/binaries/template.md
- docs/ops/registry/binaries.md
- docs/ops/registry/lint.md
- docs/ops/registry/test.md

## 3.3 Scope and Safety
Objective:
- Remove the active `conform` / `conformist` secondary lane and the legacy `hygiene` profile alias from shipping/profile/route/stance/mode surfaces.
- Keep the standalone `ops/bin/hygiene` maintenance tool intact.
- Preserve execution-decision as a disposable/manual side lane and do not invent new bindings for the remaining conformist-bound factory agents.

In scope:
- `ops/src/stances/conformist.md.tpl`
  - Delete the retired conformist stance template.
- `docs/ops/specs/stances/conformist.md`
  - Delete the retired conformist stance spec.
- `ops/lib/manifests/BUNDLE.md`
  - Remove `conform` from supported profiles, transport contracts, and shipping-spine prose.
  - Remove the legacy `hygiene` alias routing and sunset-note keys.
- `ops/lib/scripts/bundle.sh`
  - Remove `conform` and `hygiene` from profile parsing, routing, and operator-facing spine/secondary-lane output.
- `ops/bin/manifest`
  - Remove `stance-conformist` render support.
- `ops/bin/template`
  - Remove `stance-conformist` and legacy conform/hygiene render routing from the compatibility shim.
- `tools/lint/response.sh`
  - Remove `conformist` mode and its fixtures.
  - Preserve `execution-decision` mode.
- `docs/ops/specs/tools/lint/response.md`
  - Mirror the removal of conformist mode exactly.
- `tools/lint/style.sh`
  - Remove conformist-mode contract freezes and retired hygiene-alias bundle-note freezes that become false once the lane is removed.
  - Preserve unrelated OPEN, audit, and planning guards.
- `docs/ops/specs/tools/lint/style.md`
  - Mirror the updated style-guard contract exactly.
- `tools/test/bundle.sh`
  - Remove conform-profile route/package expectations from the bundle smoke tests.
- `docs/ops/specs/tools/test/bundle.md`
  - Mirror the updated bundle test contract exactly.
- `ops/etc/verification.manifest`
  - Remove conformist-lane references from the affected bundle-route verification slice.
- `docs/MANUAL.md`
  - Remove `conform/conformist` from secondary-lane and attachment-contract prose.
  - Preserve `execution-decision`.
- `docs/MAP.md`
  - Remove `conform/conformist` from the secondary-lane description.
  - Preserve `execution-decision`.
- `ops/bin/help`
  - Remove `conform` from operator-facing delivery-profile/help text only.
- `ops/bin/open`
  - Remove `conform/conformist` from the secondary-lane summary only.
- `docs/ops/specs/binaries/bundle.md`
  - Mirror the removal of conform-profile routing exactly.
- `docs/ops/specs/binaries/manifest.md`
  - Mirror the removal of `stance-conformist` render support exactly.
- `docs/ops/specs/binaries/template.md`
  - Mirror the removal of `stance-conformist` compatibility routing exactly.
- `docs/ops/registry/binaries.md`
  - Remove conform-profile / hygiene-alias claims from the bundle row.
- `docs/ops/registry/lint.md`
  - Remove conformist-mode claims from the response-lint row.
- `docs/ops/registry/test.md`
  - Remove conform-profile claims from the bundle-smoke-test row.
- `storage/dp/active/allowlist.txt`
  - Widen only if direct execution proves a real coverage gap for the touched/deleted paths.

Out of scope:
- `ops/bin/hygiene`
- `docs/ops/specs/binaries/hygiene.md`
- `docs/ops/registry/agents.md`
- `docs/ops/registry/factory.md`
- `docs/ops/specs/definitions/agents.md`
- `ops/src/definitions/agent.md.tpl`
- `tools/lint/agent.sh`
- `tools/lint/factory.sh`
- `docs/ops/specs/tools/lint/agent.md`
- `docs/ops/specs/tools/lint/factory.md`
- `docs/ops/specs/scripts/factory.md`
- `ops/etc/persistence.manifest`
- `ops/src/runbook/*.yaml`
- `docs/ops/specs/runbook/*.md`
- `storage/handoff/PLAN-RUNBOOK.md`

Safety and invariants:
- Do not delete or rewrite `ops/bin/hygiene` or `docs/ops/specs/binaries/hygiene.md`; generic English uses of the word `hygiene` remain valid.
- Do not touch the remaining conformist-bound factory agents:
  - `opt/_factory/agents/r-agent-03.md`
  - `opt/_factory/agents/r-agent-05.md`
  - `opt/_factory/agents/r-agent-07.md`
  - `opt/_factory/agents/r-agent-09.md`
- Do not invent a replacement binding for the remaining conformist-bound factory-agent identity surfaces.
- Preserve `execution-decision` as a disposable/manual side lane.
- Keep bundle/operator/help/open changes limited to removal of the conform lane and hygiene alias only; do not widen into unrelated shipping-spine or runbook redesign.
- Keep response/style/test changes structural and coupled to the lane removal; do not widen into unrelated planner or audit behavior.

Worker Constraints (SSOT injected):
## Section 1: Universal Template Rules
- Template source uses `tpl` files with optional YAML frontmatter
- Canon frontmatter keys: `template_type` `template_id` `template_version` `requires_slots` `includes`
- Renderer strips frontmatter before output write
- Slot token form `\{\{TOKEN\}\}` with uppercase alphanumeric underscore
- Include forms `\{\{@include:path\}\}` and `\{\{@include:path#section\}\}`
- Include resolution is strict: missing file fail missing section fail circular graph fail
- Strict mode default: every required slot value present and no unresolved token
- Non strict mode allowed only for lint and normalization workflows
- Worker facing generated surfaces remain pointer first and exclude disposable artifacts

Target Files allowlist (hard gate):
- storage/dp/active/allowlist.txt

## 3.4 Execution Plan (A-E)

### 3.4.1 State
- The live bundle shipping surfaces still advertise `conform` as an active secondary lane and still accept legacy `hygiene` as a compatibility alias in `ops/lib/manifests/BUNDLE.md`, `ops/lib/scripts/bundle.sh`, and the coupled bundle binary/spec/registry surfaces.
- The conformist stance/spec pair still exists and is still exposed through `ops/bin/manifest` and `ops/bin/template`.
- Response lint still exposes a `conformist` mode, and style lint still freezes a conformist-mode contract.
- Bundle smoke tests and the verification manifest still account for a live conform route.
- Operator-facing docs (`docs/MANUAL.md`, `docs/MAP.md`, `ops/bin/help`, `ops/bin/open`) still present the lane as active.
- The standalone `ops/bin/hygiene` binary and its spec still exist and are explicitly out of scope for deletion.
- The remaining conformist-bound factory agents still exist and must not be rebound by invention in this packet.

### 3.4.2 Request
- Remove the active conform/conformist lane and the legacy hygiene alias from shipping/profile/route/stance/mode surfaces only.
- Delete the conformist stance/spec pair.
- Update the coupled lint/test/spec/registry/operator surfaces so no active shipping surface still claims the lane exists.
- Keep `ops/bin/hygiene`, its spec, `execution-decision`, and the remaining conformist-bound factory-agent identity surfaces intact.
- Widen the allowlist only if direct execution proves a real coverage gap.

### 3.4.3 Changelog
- DELETE `ops/src/stances/conformist.md.tpl`
- DELETE `docs/ops/specs/stances/conformist.md`
- UPDATE `ops/lib/manifests/BUNDLE.md`
- UPDATE `ops/lib/scripts/bundle.sh`
- UPDATE `ops/bin/manifest`
- UPDATE `ops/bin/template`
- UPDATE `tools/lint/response.sh`
- UPDATE `docs/ops/specs/tools/lint/response.md`
- UPDATE `tools/lint/style.sh`
- UPDATE `docs/ops/specs/tools/lint/style.md`
- UPDATE `tools/test/bundle.sh`
- UPDATE `docs/ops/specs/tools/test/bundle.md`
- UPDATE `ops/etc/verification.manifest`
- UPDATE `docs/MANUAL.md`
- UPDATE `docs/MAP.md`
- UPDATE `ops/bin/help`
- UPDATE `ops/bin/open`
- UPDATE `docs/ops/specs/binaries/bundle.md`
- UPDATE `docs/ops/specs/binaries/manifest.md`
- UPDATE `docs/ops/specs/binaries/template.md`
- UPDATE `docs/ops/registry/binaries.md`
- UPDATE `docs/ops/registry/lint.md`
- UPDATE `docs/ops/registry/test.md`
- UPDATE `storage/dp/active/allowlist.txt` only if execution proves it is required

### 3.4.4 Patch / Diff
1. Remove conform/hygiene profile routing and shipping-spine claims from `ops/lib/manifests/BUNDLE.md` and `ops/lib/scripts/bundle.sh`.
2. Delete the conformist stance template/spec pair and remove `stance-conformist` render support from `ops/bin/manifest` and `ops/bin/template`.
3. Remove conformist mode from `tools/lint/response.sh` and mirror the change exactly in `docs/ops/specs/tools/lint/response.md`.
4. Remove conformist-lane style freezes that become false after lane deletion, while preserving unrelated planning/audit/OPEN/style guards.
5. Update bundle smoke tests/specs and the verification manifest so route/package contracts no longer expect a conform profile.
6. Remove conform/conformist from operator-facing docs and binary help/open summaries, while preserving `execution-decision`.
7. Update the coupled bundle/lint/test registry/spec surfaces so no active shipping surface still presents conform as a live lane.
8. Verify allowlist coverage for all touched/deleted paths and widen `storage/dp/active/allowlist.txt` only if direct execution proves a real gap.

### 3.4.5 Receipt (Proofs to collect) - MUST RUN

**Mandatory receipt commands (always run; do not omit):**
- bash tools/lint/dp.sh TASK.md
- bash tools/lint/task.sh
- bash tools/lint/integrity.sh
- bash tools/lint/style.sh
- ./ops/bin/llms
- bash tools/lint/integrity.sh
- git diff --name-only
- git diff --stat
- comm -23 <(git diff --name-only | sort) <(sort storage/dp/active/allowlist.txt) || true
- comm -23 <(git ls-files --others --exclude-standard | sort) <(sort storage/dp/active/allowlist.txt) || true
- ./ops/bin/open

**DP-specific receipt commands (scope-specific; author below):**
Note: Command substitution forms (e.g., $(pwd), $(git ...)) are rejected by certify replay. Use literal values only. Repo-local binaries must be authored as literal `./ops/bin/...` or `ops/bin/...` commands. Unsupported receipt-command forms are rejected by dp.sh and certify.
- bash tools/lint/response.sh --test
- bash tools/test/bundle.sh --slice=route-contract
- bash tools/test/bundle.sh --slice=package-contract
- bash tools/test/bundle.sh --slice=fail-closed
- bash tools/lint/truth.sh

## 3.5 Closeout (Mandatory Routing)
- Execute docs/MANUAL.md Closeout Cycle in order (Verify, Harvest, Refresh, Log, Prune).
- Update SoP.md and PoW.md with DP entries, including objective summary and current proof / receipt summary.
- Protocol order for closeout: Verify -> Generate Results -> COMMIT (Operator Only) -> Prune.
- Run prune hygiene: ./ops/bin/prune --scrub.
- Refresh llms artifacts: `./ops/bin/llms`
- Regenerate session artifacts: `./ops/bin/open --out=auto`
- Ensure the next session begins with refreshed session artifacts and matching receipts.
- Note: audit dump generation is owned by `./ops/bin/bundle --profile=audit --out=auto` and is separate from operator session refresh. Do not stamp an additional `ops/bin/dump --scope=core` into closeout unless the DP explicitly authorizes a standalone state capture.
- Refresh side-effect: `ops/bin/llms` regenerates `ops/lib/manifests/OPS.md` as a compile event; if `OPS.md` is not in the allowlist, restore it before running `integrity.sh`. See `docs/MANUAL.md` Refresh side-effect notice for the full procedure.

### 3.5.1 Mandatory Closing Sidecar
Closing sidecar content is generated and maintained at `storage/handoff/CLOSING.md` and is validated by `ops/bin/certify` as a hard gate at certification time. Do not author, predict, populate, or approximate any sidecar-derived closeout output outside this sidecar.
Certify separately collects contractor-authored narrative for the RESULTS Contractor Execution Narrative section at certify time via interactive editor prompt; this narrative input is independent from closing sidecar content.

Before running certify, confirm `storage/handoff/CLOSING.md` has been maintained throughout execution and reflects observable reality only.

Closing sidecar fields required at certify time:
- Commit Message
- Create Pull Request (Title)
- Create Pull Request (Description)
- Confirm Merge (Commit Message)
- Confirm Merge (Extended Description)
- Confirm Merge (Add a Comment)
