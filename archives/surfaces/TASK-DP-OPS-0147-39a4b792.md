---
trace_id: stela-20260304T023655Z-2fe61681
packet_id: DP-OPS-0147
created_at: 2026-03-04T02:37:30Z
previous: (none)
---
# STELA TASK DASHBOARD
Status: ACTIVE
Owner: Integrator
Last Updated: 2026-03-02

## 1. Session State
Pointer: Session context output (generated by ops/bin/open)
Context Manifest: ops/lib/manifests/CONTEXT.md (checked by tools/lint/context.sh)

Session State contract:
- Session State is pointer-first. Do not write inline branch or hash state here.
- OPEN and OPEN-PORCELAIN are the session state snapshot.
- OPEN-PORCELAIN is emitted on every OPEN run. Clean sessions produce a zero-line artifact.
- RESULTS is the audit record for the DP.
- TASK is the stable routing surface.
- Operator note (2026-02-17): Do not edit `SoP.md` or `PoW.md` unless explicitly instructed.

## 2. Logic Pointers
Primary Constraint: PoT.md (Policy of Truth).
Surface Contract Spec: docs/ops/specs/surfaces/task.md.

### 2.1 Governance Pointers
Jurisdiction: PoT.md Section 3.
Git Authority: PoT.md Section 4.1.
Behavioral Standard: PoT.md Section 4.2.
Staffing Protocol: PoT.md Section 4.1.

### 2.2 Enforcement and Gates
Style: tools/lint/style.sh
Context: tools/lint/context.sh
Truth: tools/lint/truth.sh
DP lint: tools/lint/dp.sh (enforces DP transaction rules in Section 3; does not enforce TASK container schema)
TASK lint: tools/lint/task.sh (sole TASK surface/container schema enforcer)
Repo verify: tools/verify.sh
llms parity: tools/lint/llms.sh

## 3. Current Dispatch Packet (DP)

Section-by-section authoring protocol:
- Generate DP structure from `ops/src/surfaces/dp.md.tpl` using `ops/bin/draft`.
- Do not hand-edit DP structural boilerplate in `TASK.md` after generation.
- Manual edits are limited to slot content only (objective, scope, safety, plan body, receipt body).
- Freeze each completed subsection in order (3.1 -> 3.2 -> 3.3 -> 3.4.x).
- Do not revise earlier sections to justify later work. If scope changes, end the thread and draft a new DP.

Freeze Rule:
- If a completed DP subsection changes later, append an AMENDMENT block in RESULTS; otherwise DP is FAIL.

AMENDMENT: minimum fields
- Reopened section:
- Reason:
- Impact:
- Allowlist change (none or exact paths):

Input discipline (hard rule):
- Worker receives only the DP text. Worker does not require OPEN or DUMP attachments.
- DP writer must not attach or cite disposable artifacts (rough notes, chat logs, scratch drafts, throwaway excerpts).
- DP writer must not include pasted bundles (OPEN, DUMP payloads, manifests) inside the DP body.
- Canon pointers only. Evidence belongs in storage artifacts and RESULTS.


### DP-OPS-0147: Bundle Hardening Pass 2: Smoke Gate, Attachment Contract Tables, and Audit-Auditor Guardrails

## 3.1 Freshness Gate (Must Pass Before Work)
Base Branch: main
Required Work Branch: work/dp-ops-0147-bundle-hardening-pass2-2026-03-03
Base HEAD: 39a4b792
Freshness Stamp: 2026-03-03
Note: FRESHNESS_STAMP must be YYYY-MM-DD format only. No trace tokens, no timestamps, no other text. Certify rejects all other forms. dp.sh enforces at lint time.

Required local re-check (worker runs; paste outputs in RESULTS):
- git rev-parse --abbrev-ref HEAD
- git rev-parse --short HEAD
- git status --porcelain

Preconditions:
- No commits on main.
- Working tree must be clean before execution begins.
- If Base HEAD changes, regenerate session artifacts from canonical tools before proceeding.

STOP conditions:
- STOP if any mismatch (branch, Base HEAD, missing work branch).
- STOP if working tree is dirty before execution begins.
- STOP if told to create or switch branches.

## 3.1.1 DP Preflight Gate (Run Before Any Edits)
Purpose:
- Catch malformed DP or TASK schema before work begins.

Worker runs (paste outcome in RESULTS):
- bash tools/lint/dp.sh --test
- bash tools/lint/dp.sh TASK.md
- bash tools/lint/task.sh

STOP if any preflight check fails.

### CbC Design Discipline Preflight (TASK.md §3.1.1)
Required when the DP objective adds, modifies, or replaces a linter, script, guard, or validation binary.
For non-tooling DPs: state "Not applicable" with a one-line justification.
Applicable. This packet adds `tools/test/bundle.sh` (new test script) and extends `tools/lint/style.sh` and `tools/lint/dp.sh` with new deterministic checks.

`tools/test/bundle.sh` is a test harness surface and is out of CbC enforcement scope per `docs/DESIGN.md`.

Style-lint extension rationale:
- Structural prevention is not viable for hand-authored prompt prose surfaces.
- Deterministic line-presence checks are proportionate and bounded.

DP-lint extension rationale:
- Template hash validation gates structure but does not prevent foreign citation artifacts in DP body content.
- Deterministic contamination scan is proportionate and bounded.

## 3.2 Required Context Load (Read Before Doing Anything)

### 3.2.1 Canon load order (always)
Worker must confirm loaded before edits begin:
1. PoT.md
2. SoP.md
3. PoW.md
4. TASK.md
5. docs/MAP.md
6. docs/MANUAL.md
7. ops/lib/manifests/CONTEXT.md

Notes:
- Worker does not read OPEN. OPEN is for Integrator state refresh and for receipts.
- Disposable artifacts must not be referenced or included.
- Worker input is the DP text only.
- DP writer must not attach or cite disposable artifacts.
- DP writer must not embed pasted bundles.

<!-- CCD: ff_target="machine-dense" ff_band="30-40" -->
# Contractor Brief

1 Temp path rule uses `var/tmp` only
2 RESULTS generation rule uses `ops/bin/certify` only
3 Receipt rule runs every DP mandated command and treats any skip as STOP
4 Disposable artifact rule treats OPEN dump bundles and manifests as non canonical session artifacts
5 Use repo-relative paths only in all output. Never print absolute filesystem paths. Do not emit clickable file links.

### 3.2.2 DP-scoped load order (per DP)
1. PoT.md
2. SoP.md
3. PoW.md
4. TASK.md
5. docs/MAP.md
6. docs/MANUAL.md
7. ops/lib/manifests/CONTEXT.md
8. ops/lib/scripts/bundle.sh
9. ops/bin/bundle
10. tools/verify.sh
11. tools/lint/style.sh
12. tools/lint/dp.sh
13. docs/ops/prompts/README.md
14. docs/ops/prompts/e-prompt-01.md
15. docs/ops/prompts/e-prompt-05.md
16. docs/ops/registry/test.md
17. docs/ops/specs/tools/test/agent.md
18. docs/ops/specs/tools/test/bundle.md
19. docs/ops/specs/tools/lint/style.md
20. docs/ops/specs/tools/lint/dp.md
21. docs/ops/registry/lint.md

## 3.3 Scope and Safety
Objective:
Convert bundle reliability policy into enforceable checks. Add a deterministic bundle smoke gate covering profile routing and artifact contract validation. Canonicalize attachment contract tables in `docs/MANUAL.md` and `docs/ops/prompts/README.md`. Add deterministic lint guardrails for audit-versus-auditor mode-split boundaries. Add a contamination guard in `tools/lint/dp.sh` to reject foreign citation tokens in DP draft text.

In scope:
- tools/verify.sh (UPDATE)
- tools/lint/style.sh (UPDATE)
- tools/lint/dp.sh (UPDATE)
- docs/MANUAL.md (UPDATE)
- docs/ops/prompts/README.md (UPDATE)
- docs/ops/registry/lint.md (UPDATE)
- docs/ops/registry/test.md (UPDATE)
- docs/ops/specs/tools/lint/style.md (UPDATE)
- docs/ops/specs/tools/lint/dp.md (UPDATE)
- tools/test/bundle.sh (NEW)
- docs/ops/specs/tools/test/bundle.md (NEW)
- storage/dp/active/allowlist.txt (UPDATE)
- TASK.md (UPDATE)
- SoP.md (UPDATE)
- PoW.md (UPDATE)
- llms.txt (UPDATE)
- llms-core.txt (UPDATE)
- llms-full.txt (UPDATE)

Out of scope:
- `ops/bin/certify --narrative-file` and any certify UX or CLI changes.
- Smart prune policy engine for `storage/handoff/`.
- Structural changes to DP or TASK templates unrelated to this scope.
- Any modification to `ops/bin/bundle` or `ops/lib/scripts/bundle.sh` behavior.
- Any change to bundle profile routing rules beyond what the smoke test exercises.

Safety and invariants:
- No manual edits to generated artifacts in `storage/handoff/`, `storage/dumps/`, or RESULTS outputs.
- Keep runtime behavior deterministic; no profile alias relabeling or ad hoc output naming.
- Do not broaden scope into certify CLI changes or prune implementation in this packet.
- Maintain allowlist coverage for every tracked file touched by this packet.
- Keep receipt commands certify-compatible: literal values only, no forbidden glob or brace patterns in Section 3.4.5.
- Bundle smoke test must exercise the public binary contract (`ops/bin/bundle`) only; do not call `ops/lib/scripts/bundle.sh` directly.
- Bundle smoke test must not force bundle artifacts into `var/tmp/`; canonical bundle output remains under `storage/handoff/`.

Worker Constraints (SSOT injected):
## Section 1: Universal Template Rules
- Template source uses `tpl` files with optional YAML frontmatter
- Canon frontmatter keys: `template_type` `template_id` `template_version` `requires_slots` `includes`
- Renderer strips frontmatter before output write
- Slot token form `\{\{TOKEN\}\}` with uppercase alphanumeric underscore
- Include forms `\{\{@include:path\}\}` and `\{\{@include:path#section\}\}`
- Include resolution is strict: missing file fail missing section fail circular graph fail
- Strict mode default: every required slot value present and no unresolved token
- Non strict mode allowed only for lint and normalization workflows
- Worker facing generated surfaces remain pointer first and exclude disposable artifacts

Target Files allowlist (hard gate):
- storage/dp/active/allowlist.txt

## 3.4 Execution Plan (A-E)

### 3.4.1 State
- Bundle v2 is live and generates `BUNDLE-*` artifacts under `storage/handoff/` with `bundle_version: "2"` manifests and tar sidecars.
- No smoke test exists for bundle profile routing or artifact contract validation; `tools/verify.sh` does not invoke any bundle-specific test.
- `tools/lint/style.sh` has no mode-split invariant checks; audit-versus-auditor prompt boundary enforcement relies on prose documentation only.
- `tools/lint/dp.sh` has no contamination guard for foreign citation tokens.
- `docs/MANUAL.md` and `docs/ops/prompts/README.md` carry no canonical attachment contract tables.
- `docs/ops/specs/tools/test/bundle.md` does not exist.

### 3.4.2 Request
Implement PLAN-selected path: Selected Option B, Slice Mode multi, Selected Slices S1,S2,S3, Execution Order S1 -> S2 -> S3.

S1 — Bundle Smoke Gate:
- Add `tools/test/bundle.sh` with deterministic checks covering valid profiles (`analyst`, `architect`, `audit`, `hygiene`, `auto`), auditor invalid paths (missing `--intent`, malformed `--intent`), and auditor valid path with a repository decision ID sourced from `archives/decisions/`.
- Test assertions: native `BUNDLE-*` naming convention, `bundle_version: "2"` in manifest, expected resolved profile fields.
- Use `ops/bin/bundle --out=auto` for all test invocations; parse emitted artifact and manifest paths from command output.
- Validate artifact path prefix is `storage/handoff/BUNDLE-`.
- Clean up only test-generated bundle artifacts after assertions complete; do not remove pre-existing artifacts.
- Add `docs/ops/specs/tools/test/bundle.md` as the technical spec for the new test.
- Register the new test as TEST-02 in `docs/ops/registry/test.md`.
- Add `bash tools/test/bundle.sh` invocation to `tools/verify.sh`.

S2 — Attachment Contract Tables:
- Update `docs/MANUAL.md` with a compact profile-to-attach table covering analyst, architect, audit, auditor, project, and hygiene profiles under a heading `Attachment Contract Table`.
- Update `docs/ops/prompts/README.md` with a matching attachment contract table and explicit mode-split statement.
- Add an explicit fallback rule in both locations: when tar ingestion is unreliable in a web model context, attach the dump payload and dump manifest files directly.

S3 — Audit-Auditor Guardrail Lint and DP Contamination Guard:
- Extend `tools/lint/style.sh` with deterministic checks for mode-split invariants.
- Extend `tools/lint/dp.sh` with deterministic rejection scan for foreign citation artifacts in DP body text.
- Update `docs/ops/specs/tools/lint/style.md` and `docs/ops/specs/tools/lint/dp.md`.
- Update `docs/ops/registry/lint.md` notes for style and dp lint entries.

### 3.4.3 Changelog
UPDATE:
- tools/verify.sh
- tools/lint/style.sh
- tools/lint/dp.sh
- docs/MANUAL.md
- docs/ops/prompts/README.md
- docs/ops/registry/lint.md
- docs/ops/registry/test.md
- docs/ops/specs/tools/lint/style.md
- docs/ops/specs/tools/lint/dp.md
- storage/dp/active/allowlist.txt
- TASK.md
- SoP.md
- PoW.md
- llms.txt
- llms-core.txt
- llms-full.txt

NEW:
- tools/test/bundle.sh
- docs/ops/specs/tools/test/bundle.md

### 3.4.4 Patch / Diff
1. Update `storage/dp/active/allowlist.txt` with the complete tracked file set for this DP before any other edits.
2. Add `tools/test/bundle.sh`:
   - Source `ops/lib/scripts/common.sh` and emit telemetry leaves.
   - Implement deterministic checks for valid and invalid bundle profiles.
   - Parse emitted artifact and manifest paths from command output.
   - Assert artifact prefix `storage/handoff/BUNDLE-` and manifest field invariants.
   - Cleanup only exact paths captured during the run.
3. Add `docs/ops/specs/tools/test/bundle.md` with Purpose, Mechanics, and failure behavior.
4. Update `docs/ops/registry/test.md` with TEST-02 entry.
5. Update `tools/verify.sh` to call `bash tools/test/bundle.sh`.
6. Update `docs/MANUAL.md` with `Attachment Contract Table` and fallback note.
7. Update `docs/ops/prompts/README.md` with matching table and mode-split lines.
8. Extend `tools/lint/style.sh` with deterministic mode-split guard checks.
9. Extend `tools/lint/dp.sh` with foreign citation contamination guard and clear fail output.
10. Update `docs/ops/specs/tools/lint/style.md` and `docs/ops/specs/tools/lint/dp.md` for the new checks.
11. Update `docs/ops/registry/lint.md` notes for style and dp lint hardening.
12. Run receipt commands in Section 3.4.5 and complete closeout via certify.

### 3.4.5 Receipt (Proofs to collect) - MUST RUN

**Mandatory receipt commands (always run; do not omit):**
- bash tools/lint/dp.sh TASK.md
- bash tools/lint/task.sh
- bash tools/lint/integrity.sh
- bash tools/lint/style.sh
- git diff --name-only
- git diff --stat
- comm -23 <(git diff --name-only | sort) <(sort storage/dp/active/allowlist.txt) || true
- comm -23 <(git ls-files --others --exclude-standard | sort) <(sort storage/dp/active/allowlist.txt) || true
- ./ops/bin/open

**DP-specific receipt commands (scope-specific; author below):**
Note: Command substitution forms (e.g., $(pwd), $(git ...)) are rejected by certify replay. Use literal values only. dp.sh enforces at lint time.
- bash tools/lint/truth.sh
- bash tools/lint/ff.sh
- bash tools/lint/context.sh
- bash tools/lint/agent.sh
- bash tools/lint/factory.sh
- bash tools/lint/plan.sh storage/handoff/PLAN.md
- bash -n tools/test/bundle.sh
- bash tools/test/bundle.sh
- bash tools/verify.sh
- grep -n "tools/test/bundle.sh" tools/verify.sh
- grep -n "TEST-02" docs/ops/registry/test.md
- grep -n "Attachment Contract Table" docs/MANUAL.md
- grep -n "Attachment Contract Table" docs/ops/prompts/README.md
- grep -n "profile is for PASS/FAIL audit verdicts only." docs/ops/prompts/README.md
- grep -n "profile is for addendum authorization only." docs/ops/prompts/README.md

## 3.5 Closeout (Mandatory Routing)
- Execute docs/MANUAL.md Closeout Cycle in order (Verify, Harvest, Refresh, Log, Prune).
- Update SoP.md and PoW.md with DP entries, including objective summary and verification commands run.
- Protocol order for closeout: Verify -> Generate Results -> COMMIT (Operator Only) -> Prune.
- Run prune hygiene: ./ops/bin/prune --scrub.
- Regenerate session artifacts: `./ops/bin/open --out=auto`
- Capture updated Ops state: `./ops/bin/dump --scope=core --format=chatgpt --out=auto`
- Ensure the next session begins with refreshed session artifacts and matching receipts.
- Refresh side-effect: `ops/bin/llms` regenerates `ops/lib/manifests/OPS.md` as a compile event; if `OPS.md` is not in the allowlist, restore it before running `integrity.sh`. See `docs/MANUAL.md` Refresh side-effect notice for the full procedure.

### 3.5.1 Mandatory Closing Sidecar
Closing sidecar content is generated and maintained at `storage/handoff/CLOSING-DP-OPS-0147.md` and is validated by `ops/bin/certify` as a hard gate at certification time. Do not author, predict, populate, or approximate any sidecar-derived closeout output outside this sidecar.
Certify separately collects contractor-authored narrative for the RESULTS Contractor Execution Narrative section at certify time via interactive editor prompt; this narrative input is independent from closing sidecar content.

Before running certify, confirm `storage/handoff/CLOSING-DP-OPS-0147.md` has been maintained throughout execution and reflects observable reality only.

Closing sidecar fields required at certify time:
- Commit Message
- Create Pull Request (Title)
- Create Pull Request (Description)
- Confirm Merge (Commit Message)
- Confirm Merge (Extended Description)
- Confirm Merge (Add a Comment)
