---
trace_id: stela-20260228T170701Z-32c4234f
packet_id: DP-OPS-0135
created_at: 2026-02-28T17:11:02Z
previous: (none)
---
## 2026-02-28 16:24:37 UTC - DP-OPS-0135 core and factory traversal scopes
Objective summary:
Implemented `core` and `factory` traversal scope values structurally across the dump toolchain:
- Extended `ops/lib/scripts/traverse.sh` usage string, argument parser, and `include_by_scope()` function to accept and enforce `core` (excludes `projects/` and `opt/_factory/`) and `factory` (includes only `opt/_factory/`).
- Extended `ops/bin/dump` usage string, argument parser, and manifest header scope-exclusion reporting block to accept and correctly label `core` and `factory`.
- Updated `docs/ops/specs/binaries/dump.md` to remove the "Not implemented" annotation from the `factory` scope definition, replacing it with an implemented reference to the implementing scripts.
- Updated `storage/dp/active/allowlist.txt` for packet files and certify closeout artifacts.
- Added `.claude/` to `.gitignore` to resolve integrity gate failure from untracked tool-local settings file (anomaly correction; consistent with existing `.vscode/` and `.idea/` entries).

Existing `platform`, `full`, and `project` scope behavior is unchanged.

Verification commands:
- git rev-parse --abbrev-ref HEAD
- git rev-parse --short HEAD
- git status --porcelain
- bash tools/lint/dp.sh --test
- bash tools/lint/dp.sh TASK.md
- bash tools/lint/task.sh
- bash tools/lint/integrity.sh
- bash tools/lint/style.sh
- git diff --name-only
- git diff --stat
- comm -23 <(git diff --name-only | sort) <(sort storage/dp/active/allowlist.txt) || true
- comm -23 <(git ls-files --others --exclude-standard | sort) <(sort storage/dp/active/allowlist.txt) || true
- ./ops/bin/open
- grep -n "scope=core" ops/lib/scripts/traverse.sh
- grep -n "scope=factory" ops/lib/scripts/traverse.sh
- grep -n "scope=core" ops/bin/dump
- grep -n "scope=factory" ops/bin/dump
- grep -n "core)" ops/lib/scripts/traverse.sh
- grep -n "factory)" ops/lib/scripts/traverse.sh
- grep -n "core\|factory" docs/ops/specs/binaries/dump.md
- grep -c "Not implemented" docs/ops/specs/binaries/dump.md
- bash tools/lint/truth.sh
- bash tools/lint/ff.sh
- bash tools/lint/context.sh
- bash tools/lint/agent.sh
- bash tools/lint/dp.sh TASK.md
- bash tools/lint/factory.sh
- ./ops/lib/scripts/agent.sh harvest-check
- ./ops/bin/map
- ./tools/verify.sh

Functional receipt summary:
All required freshness, preflight, verify, mandatory receipt, and DP-specific checks executed. The `core` and `factory` scope values are now structurally encoded in traverse.sh and dump. The `factory` scope definition in dump.md no longer carries a "Not implemented" annotation.
