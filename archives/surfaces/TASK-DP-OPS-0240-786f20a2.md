---
trace_id: stela-20260401T144808Z-1edc6124
packet_id: DP-OPS-0240
created_at: 2026-04-01T14:49:04Z
previous: archives/surfaces/TASK-DP-OPS-0239-f91a8311.md
---
# STELA TASK DASHBOARD
Status: ACTIVE
Owner: Integrator
Last Updated: 2026-02-14

## 1. Session State
Pointer: Session context output (generated by ops/bin/open)
Context Manifest: ops/lib/manifests/CONTEXT.md (checked by tools/lint/context.sh)

Session State contract:
- Session State is pointer-first. Do not write inline branch or hash state here.
- OPEN (and OPEN-PORCELAIN when dirty) is the session state snapshot.
- RESULTS is the audit record for the DP.
- TASK is the stable routing surface.

## 2. Logic Pointers
Primary Constraint: PoT.md (Policy of Truth).
Surface Contract Spec: docs/ops/specs/surfaces/task.md.

### 2.1 Governance Pointers
Jurisdiction: PoT.md Section 3.
Git Authority: PoT.md Section 4.1.
Behavioral Standard: PoT.md Section 4.2.
Staffing Protocol: PoT.md Section 4.1.

### 2.2 Enforcement and Gates
Style: tools/lint/style.sh
Context: tools/lint/context.sh
Truth: tools/lint/truth.sh
DP lint: tools/lint/dp.sh (enforces DP transaction rules in Section 3; does not enforce TASK container schema)
TASK lint: tools/lint/task.sh (sole TASK surface/container schema enforcer)
Repo verify: tools/verify.sh

## 3. Current Dispatch Packet (DP)

Section-by-section authoring protocol:
- Generate DP structure via `./ops/bin/bundle --profile=draft --out=auto` (standard operator path: Architect completes the embedded DP AUTHORING SCAFFOLD; save the returned DP body to `storage/dp/intake/DP.md`) or locally via `ops/bin/draft` (template render only).
- Do not hand-edit DP structural boilerplate in `TASK.md` after generation.
- Manual edits are limited to slot content only (objective, scope, safety, plan body, receipt body).
- Freeze each completed subsection in order (3.1 -> 3.2 -> 3.3 -> 3.4.x).
- Do not revise earlier sections to justify later work. If scope changes, end the thread and draft a new DP.

Freeze Rule:
- If a completed DP subsection changes later, append an AMENDMENT block in RESULTS; otherwise DP is FAIL.

AMENDMENT: minimum fields
- Reopened section:
- Reason:
- Impact:
- Allowlist change (none or exact paths):

Input discipline (hard rule):
- Worker receives only the DP text. Worker does not require OPEN or DUMP attachments.
- DP writer must not attach or cite disposable artifacts (rough notes, chat logs, scratch drafts, throwaway excerpts).
- DP writer must not include pasted bundles (OPEN, DUMP payloads, manifests) inside the DP body.
- Canon pointers only. Evidence belongs in storage artifacts and RESULTS.


### DP-OPS-0240: Thin Planning Core and Relax PLAN Floor Enforcement

## 3.1 Freshness Gate (Must Pass Before Work)
Base Branch: main
Work Branch: work/dp-ops-0240-2026-04-01
Base HEAD: 786f20a2
Freshness Stamp: 2026-04-01
Note: FRESHNESS_STAMP must be YYYY-MM-DD format only. No trace tokens, no timestamps, no other text. Certify rejects all other forms. dp.sh enforces at lint time.

Required local re-check (worker runs before any edits; paste verbatim outputs under `## Contractor Execution Narrative` -> `### Preflight State` in RESULTS):
- git rev-parse --abbrev-ref HEAD
- git rev-parse --short HEAD
- git status --porcelain

These three commands are freshness proof only. Do not treat §3.4.5 receipt replay as a substitute for their pre-edit outputs.

Preconditions:
- No commits on main.
- Working tree must be clean before execution begins.
- If Base HEAD changes, regenerate session artifacts from canonical tools before proceeding.

STOP conditions:
- STOP if any mismatch (branch, Base HEAD, missing work branch).
- STOP if working tree is dirty before execution begins.
- STOP if told to create or switch branches.

## 3.1.1 DP Preflight Gate (Run Before Any Edits)
Purpose:
- Catch malformed DP or TASK schema before work begins.

Worker runs (paste outcome in RESULTS):
- bash tools/lint/dp.sh --test
- bash tools/lint/dp.sh TASK.md
- bash tools/lint/task.sh

STOP if any preflight check fails.

### CbC Design Discipline Preflight (TASK.md §3.1.1)
Required when the DP objective adds, modifies, or replaces a linter, script, guard, or validation binary.
For non-tooling DPs: state "Not applicable" with a one-line justification.
Tooling-applicable. This packet changes the planning stance contract and the coupled plan/style lint surfaces that freeze and validate planner output behavior.

## 3.2 Required Context Load (Read Before Doing Anything)

### 3.2.1 Canon load order (always)
Worker must confirm loaded before edits begin:
1. PoT.md
2. SoP.md
3. PoW.md
4. TASK.md
5. docs/MAP.md
6. docs/MANUAL.md
7. ops/lib/manifests/CONTEXT.md

Notes:
- Worker does not read OPEN. OPEN is for Integrator state refresh and for receipts.
- Disposable artifacts must not be referenced or included.
- Worker input is the DP text only.
- DP writer must not attach or cite disposable artifacts.
- DP writer must not embed pasted bundles.

## Rules

1 Temp path rule uses `var/tmp` only
2 RESULTS generation rule uses `ops/bin/certify` only
3 Receipt rule runs every DP mandated command and treats any skip as STOP
4 Disposable artifact rule treats OPEN dump bundles and manifests as non canonical session artifacts
5 Use repo-relative paths only in all output. Never print absolute filesystem paths. Do not emit clickable file links.

### 3.2.2 DP-scoped load order (per DP)
- ops/src/stances/planning.md.tpl
- docs/ops/specs/stances/planning.md
- docs/ops/specs/stances/plan.md
- tools/lint/plan.sh
- docs/ops/specs/tools/lint/plan.md
- tools/lint/style.sh
- docs/ops/specs/tools/lint/style.md
- docs/ops/registry/lint.md

## 3.3 Scope and Safety
Objective:
- Thin the universal planning stance so it governs packet-boundary truth, question-vs-plan decisions, and the minimum final-plan floor, while popup/widget instructions live as an additive host overlay instead of bloating the core contract.
- Keep the popup-biased Analyst `A./B./C.` transport and fixed redirect fallback, but stop treating presentation choreography as the whole planner contract.
- Align the PLAN surface, lint, and registry language with the actual floor-not-cage rule: required headings remain mandatory, and extra peer sections are allowed when they keep the handoff truthful and narrow.

In scope:
- `ops/src/stances/planning.md.tpl`
  - Thin the core planning contract so it centers on authoritative input, when to ask, when to stop asking, planning-only boundary, and minimum final-plan structure.
  - Preserve the DP-OPS-0238 multi-family question-first packet-selection rules and the DP-OPS-0239 portable Analyst `A./B./C.` fallback transport.
  - Move host-specific widget/tool invocation guidance into an additive overlay section instead of blending it into the universal core rules.
  - Keep final-plan mode as fenced output only, with required core headings still mandatory and extra peer sections explicitly allowed when needed.
- `docs/ops/specs/stances/planning.md`
  - Mirror the thinner core planning contract, the portable popup-biased fallback, and the additive host-overlay split exactly.
- `docs/ops/specs/stances/plan.md`
  - Clarify that `Summary`, `Key Changes`, `Test Plan`, and `Assumptions` are required core headings, not an exclusive ontology.
  - State that additional headings must stand as proper peer sections when needed instead of being buried under required headings.
- `tools/lint/plan.sh`
  - Keep the required core-heading checks.
  - Add explicit self-test proof that plans with additional peer sections still pass.
  - Keep PLAN lint as a structural floor rather than widening into prose/style judgment.
- `docs/ops/specs/tools/lint/plan.md`
  - Mirror the updated PLAN lint behavior and self-test expectations exactly.
- `tools/lint/style.sh`
  - Replace the over-scripted planning guard lines with checks that freeze the thinner core contract, the portable fallback transport, the additive overlay split, and the floor-not-cage final-plan rule.
- `docs/ops/specs/tools/lint/style.md`
  - Mirror the updated planning/style guard contract exactly.
- `docs/ops/registry/lint.md`
  - Update the PLAN lint registry note so it no longer falsely says the linter enforces the canonical heading set only.

Out of scope:
- `ops/src/stances/plan.md.tpl`
  - Direct repo truth shows this template is a minimum four-section example and the repo exposes no visible optional peer-section slot mechanic; do not widen this packet into template-renderer work that is not required to land the enforceable floor contract.
- `tools/lint/response.sh`
- `docs/ops/specs/tools/lint/response.md`
- bundle mechanics, draft bundle mechanics, and DP scaffold mechanics
- host/UI code or any promise of guaranteed popup/widget rendering
- DP-OPS-0238 first-packet question-first selection rules
- DP-OPS-0239 popup fallback wording and redirect label
- `storage/dp/active/allowlist.txt` edits unless direct execution proves a real coverage gap

Safety and invariants:
- Preserve the DP-OPS-0238 rule that multi-family topics default to question mode unless the immediate packet is explicitly operator-stated or directly required by attached evidence.
- Preserve the DP-OPS-0239 portable Analyst fallback transport: short question sentence, `A.` substantive option, `B.` substantive option, `C. Tell Analyst to do something else instead.`
- Do not represent popup or widget behavior as deterministic repo-owned behavior; host-specific tool guidance must remain an additive overlay only.
- Required PLAN headings remain mandatory, but extra headings must be allowed as proper peer sections when they keep the handoff truthful, narrow, and naturally structured.
- Do not widen this packet into response-parser changes, bundle changes, or template-renderer expansion.
- Keep plan lint and style lint structural; do not add subjective prose-rubric checks or broad new bumper logic.

Worker Constraints (SSOT injected):
## Section 1: Universal Template Rules
- Template source uses `tpl` files with optional YAML frontmatter
- Canon frontmatter keys: `template_type` `template_id` `template_version` `requires_slots` `includes`
- Renderer strips frontmatter before output write
- Slot token form `\{\{TOKEN\}\}` with uppercase alphanumeric underscore
- Include forms `\{\{@include:path\}\}` and `\{\{@include:path#section\}\}`
- Include resolution is strict: missing file fail missing section fail circular graph fail
- Strict mode default: every required slot value present and no unresolved token
- Non strict mode allowed only for lint and normalization workflows
- Worker facing generated surfaces remain pointer first and exclude disposable artifacts

Target Files allowlist (hard gate):
- storage/dp/active/allowlist.txt

## 3.4 Execution Plan (A-E)

### 3.4.1 State
- The live planning stance still mixes three different concerns in one over-scripted body:
  - core packet-selection and final-plan rules
  - portable popup-biased `A./B./C.` clarification transport
  - host-specific widget/tool hints
- The live planning spec mirrors that same blended contract instead of distinguishing core canon from host overlay.
- `tools/lint/plan.sh` already behaves more like a floor than a cage: it requires the four core headings but does not reject additional headings. Its current fixtures do not positively prove that extra peer sections pass.
- `docs/ops/specs/tools/lint/plan.md` and `docs/ops/registry/lint.md` still describe PLAN lint too narrowly, as if it enforced the heading set only.
- `docs/ops/specs/stances/plan.md` already allows additional bounded sections, but the planning contract and adjacent docs still do not clearly state that extra headings should stand as peer sections rather than being sandwiched under a required heading.
- `ops/src/stances/plan.md.tpl` is a minimum four-section template example and the repo shows no visible optional peer-section slot mechanism. That is not the enforceable seam for this packet.

### 3.4.2 Request
- Refactor the planning stance/spec so the universal core contract is thinner:
  - authoritative input (`TOPIC.md`)
  - attached evidence first
  - planning-only boundary
  - multi-family question-first packet selection
  - when to ask additional bounded follow-up questions
  - final-plan fenced-output boundary
  - required final-plan floor with extra peer sections allowed
- Preserve the popup-biased Analyst `A./B./C.` clarification transport as the portable fallback base.
- Preserve host-specific widget/tool guidance only as an additive overlay, not as the universal core planning contract.
- Update the PLAN surface spec plus PLAN lint/spec/registry wording so they all tell the same truth:
  - `Summary`, `Key Changes`, `Test Plan`, and `Assumptions` remain mandatory
  - extra headings are allowed when needed
  - those extra headings should stand as proper peer sections
  - PLAN lint is a structural floor and must prove that additional peer sections can pass
- Update the style guard/spec pair so they enforce the thinner planning core, the additive overlay split, and the floor-not-cage PLAN rule without freezing the old blended choreography.
- Do not touch response parsing, bundle mechanics, draft mechanics, or template-renderer behavior.

### 3.4.3 Changelog
- UPDATE `ops/src/stances/planning.md.tpl`
- UPDATE `docs/ops/specs/stances/planning.md`
- UPDATE `docs/ops/specs/stances/plan.md`
- UPDATE `tools/lint/plan.sh`
- UPDATE `docs/ops/specs/tools/lint/plan.md`
- UPDATE `tools/lint/style.sh`
- UPDATE `docs/ops/specs/tools/lint/style.md`
- UPDATE `docs/ops/registry/lint.md`

### 3.4.4 Patch / Diff
1. Update `ops/src/stances/planning.md.tpl` so the universal planning contract is organized around core planning truth and handoff rules first, while the popup-biased Analyst fallback and any host-specific widget/tool instructions are clearly separated as transport/overlay guidance rather than blended into the core.
2. Mirror that thinner contract exactly in `docs/ops/specs/stances/planning.md`, including the explicit split between portable fallback transport and additive host overlay.
3. Update `docs/ops/specs/stances/plan.md` so the required four headings are described as a mandatory floor, and additional headings are explicitly allowed as proper peer sections when needed for truthful handoff.
4. Update `tools/lint/plan.sh` so its fixtures prove the floor-not-cage behavior directly: plans missing required headings still fail, and plans with additional peer sections still pass.
5. Update `docs/ops/specs/tools/lint/plan.md` and `docs/ops/registry/lint.md` to match the actual PLAN lint behavior exactly.
6. Update `tools/lint/style.sh` and `docs/ops/specs/tools/lint/style.md` together so the planning/style guards freeze the thinner core contract, the additive overlay split, and the floor-not-cage PLAN rule rather than the older monolithic choreography.
7. Verify allowlist coverage for all touched files and widen allowlist only if direct execution proves a real gap.

### 3.4.5 Receipt (Proofs to collect) - MUST RUN

**Mandatory receipt commands (always run; do not omit):**
- bash tools/lint/dp.sh TASK.md
- bash tools/lint/task.sh
- bash tools/lint/integrity.sh
- bash tools/lint/style.sh
- ./ops/bin/llms
- bash tools/lint/integrity.sh
- git diff --name-only
- git diff --stat
- comm -23 <(git diff --name-only | sort) <(sort storage/dp/active/allowlist.txt) || true
- comm -23 <(git ls-files --others --exclude-standard | sort) <(sort storage/dp/active/allowlist.txt) || true
- ./ops/bin/open

**DP-specific receipt commands (scope-specific; author below):**
Note: Command substitution forms (e.g., $(pwd), $(git ...)) are rejected by certify replay. Use literal values only. Repo-local binaries must be authored as literal `./ops/bin/...` or `ops/bin/...` commands. Unsupported receipt-command forms are rejected by dp.sh and certify.
- bash tools/lint/plan.sh --test
- bash tools/lint/truth.sh
- ./ops/bin/manifest render stance-planning --out=-

## 3.5 Closeout (Mandatory Routing)
- Execute docs/MANUAL.md Closeout Cycle in order (Verify, Harvest, Refresh, Log, Prune).
- Update SoP.md and PoW.md with DP entries, including objective summary and current proof / receipt summary.
- Protocol order for closeout: Verify -> Generate Results -> COMMIT (Operator Only) -> Prune.
- Run prune hygiene: ./ops/bin/prune --scrub.
- Refresh llms artifacts: `./ops/bin/llms`
- Regenerate session artifacts: `./ops/bin/open --out=auto`
- Ensure the next session begins with refreshed session artifacts and matching receipts.
- Note: audit dump generation is owned by `./ops/bin/bundle --profile=audit --out=auto` and is separate from operator session refresh. Do not stamp an additional `ops/bin/dump --scope=core` into closeout unless the DP explicitly authorizes a standalone state capture.
- Refresh side-effect: `ops/bin/llms` regenerates `ops/lib/manifests/OPS.md` as a compile event; if `OPS.md` is not in the allowlist, restore it before running `integrity.sh`. See `docs/MANUAL.md` Refresh side-effect notice for the full procedure.

### 3.5.1 Mandatory Closing Sidecar
Closing sidecar content is generated and maintained at `storage/handoff/CLOSING.md` and is validated by `ops/bin/certify` as a hard gate at certification time. Do not author, predict, populate, or approximate any sidecar-derived closeout output outside this sidecar.
Certify separately collects contractor-authored narrative for the RESULTS Contractor Execution Narrative section at certify time via interactive editor prompt; this narrative input is independent from closing sidecar content.

Before running certify, confirm `storage/handoff/CLOSING.md` has been maintained throughout execution and reflects observable reality only.

Closing sidecar fields required at certify time:
- Commit Message
- Create Pull Request (Title)
- Create Pull Request (Description)
- Confirm Merge (Commit Message)
- Confirm Merge (Extended Description)
- Confirm Merge (Add a Comment)
