---
trace_id: stela-20260317T065939Z-00eb1001
packet_id: DP-OPS-0205
created_at: 2026-03-17T07:19:55Z
previous: archives/surfaces/SoP-2026-03-16-a9d63903.md
---
## 2026-03-17 00:00:00 UTC — DP-OPS-0205 gate audit rerun identity on explicit intent

Objective: make `AUDIT-R*` emission depend on explicit `--rerun` operator intent rather than on the mere existence of prior local `AUDIT-*` artifacts.

Outcome: `bundle_resolve_audit_submission_path` now short-circuits to initial identity when `--rerun` is absent; the prior-artifact directory scan only runs on explicit `--rerun`. The `--rerun` flag is validated to the audit profile only. Bundle smoke updated to prove (1) initial delivery stays `AUDIT-*` even with stale local artifacts present and (2) rerun identity appears only on explicit `--rerun`. Docs and manifest updated to reflect the intention-driven trigger rule.
