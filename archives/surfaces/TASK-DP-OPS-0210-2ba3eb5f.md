---
trace_id: stela-20260318T044240Z-53d468bf
packet_id: DP-OPS-0210
created_at: 2026-03-18T04:46:57Z
previous: (none)
---
# STELA TASK DASHBOARD
Status: ACTIVE
Owner: Integrator
Last Updated: 2026-03-18

## 1. Session State
Pointer: Session context output (generated by ops/bin/open)
Context Manifest: ops/lib/manifests/CONTEXT.md (checked by tools/lint/context.sh)

Session State contract:
- Session State is pointer-first. Do not write inline branch or hash state here.
- OPEN is the session state snapshot. OPEN-PORCELAIN is conditional dirty-state detail.
- RESULTS is the audit record for the DP.
- TASK is the stable routing surface.

## 2. Logic Pointers
Primary Constraint: PoT.md (Policy of Truth).
Surface Contract Spec: docs/ops/specs/surfaces/task.md.

### 2.1 Governance Pointers
Jurisdiction: PoT.md Section 3.
Git Authority: PoT.md Section 4.1.
Behavioral Standard: PoT.md Section 4.2.
Staffing Protocol: PoT.md Section 4.1.

### 2.2 Enforcement and Gates
Style: tools/lint/style.sh
Context: tools/lint/context.sh
Truth: tools/lint/truth.sh
DP lint: tools/lint/dp.sh (enforces DP transaction rules in Section 3; does not enforce TASK container schema)
TASK lint: tools/lint/task.sh (sole TASK surface/container schema enforcer)
Repo verify: tools/verify.sh
llms parity: tools/lint/llms.sh

## 3. Current Dispatch Packet (DP)

Section-by-section authoring protocol:
- Generate DP structure from `ops/src/surfaces/dp.md.tpl` using `ops/bin/draft`.
- Do not hand-edit DP structural boilerplate in `TASK.md` after generation.
- Manual edits are limited to slot content only (objective, scope, safety, plan body, receipt body).
- Freeze each completed subsection in order (3.1 -> 3.2 -> 3.3 -> 3.4.x).
- Do not revise earlier sections to justify later work. If scope changes, end the thread and draft a new DP.

Freeze Rule:
- If a completed DP subsection changes later, append an AMENDMENT block in RESULTS; otherwise DP is FAIL.

AMENDMENT: minimum fields
- Reopened section:
- Reason:
- Impact:
- Allowlist change (none or exact paths):

Input discipline (hard rule):
- Worker receives only the DP text. Worker does not require OPEN or DUMP attachments.
- DP writer must not attach or cite disposable artifacts (rough notes, chat logs, scratch drafts, throwaway excerpts).
- DP writer must not include pasted bundles (OPEN, DUMP payloads, manifests) inside the DP body.
- Canon pointers only. Evidence belongs in storage artifacts and RESULTS.

### DP-OPS-0210: Shipping spine surface contract lock

## 3.1 Freshness Gate (Must Pass Before Work)
Base Branch: main
Required Work Branch: work/dp-ops-0210-shipping-spine-surface-contract-lock-2026-03-17
Base HEAD: c6ac87bc93
Freshness Stamp: 2026-03-17
Note: FRESHNESS_STAMP must be YYYY-MM-DD format only. No trace tokens, no timestamps, no other text. Certify rejects all other forms. dp.sh enforces at lint time.

Required local re-check (worker runs; paste outputs in RESULTS):
- git rev-parse --abbrev-ref HEAD
- git rev-parse --short HEAD
- git status --porcelain

Preconditions:
- No commits on main.
- Working tree must be clean before execution begins.
- If Base HEAD changes, regenerate session artifacts from canonical tools before proceeding.

STOP conditions:
- STOP if any mismatch (branch, Base HEAD, missing work branch).
- STOP if working tree is dirty before execution begins.
- STOP if told to create or switch branches.

## 3.1.1 DP Preflight Gate (Run Before Any Edits)
Purpose:
- Catch malformed DP or TASK schema before work begins.

Worker runs (paste outcome in RESULTS):
- bash tools/lint/dp.sh --test
- bash tools/lint/dp.sh TASK.md
- bash tools/lint/task.sh

STOP if any preflight check fails.

### CbC Design Discipline Preflight (TASK.md §3.1.1)
Required when the DP objective adds, modifies, or replaces a linter, script, guard, or validation binary.
For non-tooling DPs: state "Not applicable" with a one-line justification.
Applicable. This packet tightens the operator-visible shipping contract across bundle, open, help, stance/spec surfaces, and route-contract proof.

Invariants:
1. The operator-visible spine is one explainable chain: `TOPIC.md` -> `PLAN.md` -> `storage/dp/intake/DP.md` -> execution against the active packet identity -> `RESULTS` + `CLOSING` -> `AUDIT` -> merge.
2. Each active stage has one obvious latest-wins surface or one obvious emitted artifact set; duplicate live names with the same active meaning are defects.
3. `foreman` / `addendum`, `conform` / `conformist`, and `execution-decision` are bounded secondary paths and must not replace `RESULTS`, `CLOSING`, or audit truth.
4. Bundle, open, help, MANUAL, specs, and route-contract proof must agree on the same shipping surface map.
5. Missing required surfaces fail closed rather than silently rerouting the shipping chain.
6. Simple latest-wins active filenames do not erase packet identity; the process still speaks in `DP-OPS-XXXX`, addendum letters, RESULTS packet ids, and audit lineage where truth matters.

## 3.2 Required Context Load (Read Before Doing Anything)

### 3.2.1 Canon load order (always)
Worker must confirm loaded before edits begin:
1. PoT.md
2. SoP.md
3. PoW.md
4. TASK.md
5. docs/MAP.md
6. docs/MANUAL.md
7. ops/lib/manifests/CONTEXT.md

Notes:
- Worker does not read OPEN. OPEN is for Integrator state refresh and for receipts.
- Disposable artifacts must not be referenced or included.
- Worker input is the DP text only.
- DP writer must not attach or cite disposable artifacts.
- DP writer must not embed pasted bundles.

## Rules

1 Temp path rule uses `var/tmp` only
2 RESULTS generation rule uses `ops/bin/certify` only
3 Receipt rule runs every DP mandated command and treats any skip as STOP
4 Disposable artifact rule treats OPEN dump bundles and manifests as non canonical session artifacts
5 Use repo-relative paths only in all output. Never print absolute filesystem paths. Do not emit clickable file links.

### 3.2.2 DP-scoped load order (per DP)
- storage/dp/processed/DP-OPS-0208.md
- storage/dp/processed/DP-OPS-0209.md
- docs/MANUAL.md
- docs/MAP.md
- ops/bin/help
- ops/bin/open
- ops/lib/scripts/bundle.sh
- ops/lib/manifests/BUNDLE.md
- docs/ops/specs/binaries/bundle.md
- docs/ops/specs/binaries/open.md
- docs/ops/specs/binaries/addendum.md
- docs/ops/specs/runbook/execution-decision.md
- docs/ops/specs/tools/lint/response.md
- docs/ops/specs/stances/architect.md
- docs/ops/specs/stances/foreman.md
- docs/ops/specs/stances/conformist.md
- ops/src/runbook/execution-decision.yaml
- tools/test/bundle.sh
- storage/dp/active/allowlist.txt

## 3.3 Scope and Safety
Objective:
- Lock the operator-visible DP shipping spine so the current system can be explained and run as one bounded chain: `TOPIC.md` -> `PLAN.md` -> `DP.md` active draft -> execution -> `RESULTS` + `CLOSING` -> audit bundle / rerun -> merge. Make `foreman` / `addendum`, `conform` / `conformist`, and `execution-decision` explicit secondary lanes instead of semi-detached folklore.
- Preserve the real role, task, and surface contracts that later factory migration must capture in definitions, including `Worker` as the execution role seeded from current system reality rather than added later as a guess.

In scope:
- UPDATE docs/MANUAL.md: define the full shipping spine, including merge guidance, in one explainable operator flow.
- UPDATE docs/MAP.md: expose the shipping spine in continuity language instead of only broad persistence/wayfinding language.
- UPDATE ops/bin/help: current top-command guidance and role summary must match the live shipping spine exactly.
- UPDATE ops/bin/open: emitted session guidance must describe the same spine and sidepaths without duplicate active-state naming.
- UPDATE ops/src/surfaces/dp.md.tpl: remove the unconditional closeout `core` dump stamp from the default DP closeout route and distinguish operator refresh from audit/bundle dump generation.
- UPDATE ops/bin/certify, ops/bin/open, and docs/ops/specs/binaries/open.md: keep `OPEN` only if the trace dependency is actually required, and make `OPEN-PORCELAIN` explicitly conditional rather than folklore-required.
- UPDATE the live surface contract so the active DP draft surface is `storage/dp/intake/DP.md` and the same simple latest-wins naming rule is explicit for other disposable active surfaces where applicable.
- UPDATE ops/lib/scripts/bundle.sh: route-contract text and request metadata must describe the full spine and keep secondary lanes bounded.
- UPDATE ops/lib/manifests/BUNDLE.md: explicit shipping-surface contract for analyst, architect, audit, foreman, conform, and rerun semantics.
- UPDATE docs/ops/specs/binaries/bundle.md, docs/ops/specs/binaries/open.md, and docs/ops/specs/binaries/dump.md: document the same latest-wins surfaces, emitted artifact identities, and the distinction between closeout refresh and audit dump generation.
- UPDATE docs/ops/specs/binaries/addendum.md: make addendum authority positioning explicit relative to the main shipping spine.
- UPDATE docs/MANUAL.md, docs/ops/specs/binaries/bundle.md, docs/ops/specs/binaries/addendum.md, ops/lib/manifests/BUNDLE.md, and ops/lib/scripts/bundle.sh: define the full foreman/addendum intervention chain, including a foreman wake-up bundle and an addendum bundle/transport surface for authorized addendum delivery.
- UPDATE docs/ops/specs/runbook/execution-decision.md and ops/src/runbook/execution-decision.yaml: replace the current execution-decision prompt with the fresher operator-tested wording and define execution-decision as disposable/manual placement for now.
- UPDATE docs/MANUAL.md and docs/MAP.md: state the interim execution-decision handling clearly:
  - received fenced markdown from auditor/analyst/architect/other secondary lanes may be placed at `storage/handoff/EXECUTION-DECISION.md`
  - architect-generated intake variant may be placed at `storage/dp/intake/EXECUTION-DECISION.md`
  - no execution-decision bundle/profile exists yet
- UPDATE docs/ops/specs/tools/lint/response.md: ensure analyst, architect, foreman, and conformist ingress rules align with the declared shipping role boundaries.
- UPDATE docs/ops/specs/stances/architect.md, docs/ops/specs/stances/foreman.md, docs/ops/specs/stances/conformist.md: align stance contracts to the same spine map.
- UPDATE tools/test/bundle.sh: strengthen route-contract proof so the spine is checked as a whole instead of by accident.
- UPDATE storage/dp/active/allowlist.txt for all tracked files touched by this packet and the active closing-sidecar path for DP-OPS-0210.
- MAINTAIN storage/handoff/CLOSING-DP-OPS-0210.md and generated TASK leaf refresh for this packet.

Out of scope:
- New ingest binaries or stdin-paste infrastructure.
- Factory transition work or definition-driven runtime migration.
- Renaming or promoting factory definitions in this packet; this packet only hardens the current shipping data those later definitions must inherit.
- Retuning certify replay lanes or persistence traversal.
- Replacing addendum issuance authority; operator/foreman authorization remains required.
- Broad stabilization/freeze work beyond what the shipping spine needs directly.
- CI workflow redesign not required to keep the operator-visible spine coherent.

Safety and invariants:
- The shipping spine must reduce explanation cost, not just move text around.
- Latest-wins active surfaces remain exact-file contracts only.
- Simple active filenames are for operator-facing live surfaces only; packet identity remains internal, receipt-grade, and archive-grade.
- Audit rerun identity remains explicit and transport-driven.
- Default closeout routing must not stamp an extra `./ops/bin/dump --scope=core --format=chatgpt --out=auto` into every packet unless that dump is truly part of the declared spine contract.
- Audit bundle dump generation remains owned by `bundle`; closeout refresh guidance must not pretend it is the same thing.
- `OPEN` remains only if its trace dependency is still justified by the actual certify/audit path; `OPEN-PORCELAIN` is conditional dirty-state evidence, not a universal shipping requirement.
- `execution-decision` is disposable/manual-placement evidence for now: useful and explicit, but still subordinate to `RESULTS`, `CLOSING`, and audit truth.
- Workers and auditors may trigger a foreman intervention request, but they do not self-authorize addendum scope. Addendum authority still flows through operator/foreman issuance.
- The shipping spine must expose both the foreman wake-up bundle and the authorized addendum delivery surface explicitly.
- `foreman` / `addendum`, `conform` / `conformist`, and `execution-decision` remain secondary to `RESULTS`, `CLOSING`, and audit packet truth.
- Missing required shipping surfaces remain fail-closed.

Worker Constraints (SSOT injected):
## Section 1: Universal Template Rules
- Template source uses `tpl` files with optional YAML frontmatter
- Canon frontmatter keys: `template_type` `template_id` `template_version` `requires_slots` `includes`
- Renderer strips frontmatter before output write
- Slot token form `\{\{TOKEN\}\}` with uppercase alphanumeric underscore
- Include forms `\{\{@include:path\}\}` and `\{\{@include:path#section\}\}`
- Include resolution is strict: missing file fail missing section fail circular graph fail
- Strict mode default: every required slot value present and no unresolved token
- Non strict mode allowed only for lint and normalization workflows
- Worker facing generated surfaces remain pointer first and exclude disposable artifacts

Target Files allowlist (hard gate):
- storage/dp/active/allowlist.txt

## 3.4 Execution Plan (A-E)

### 3.4.1 State
- Analyst, architect, audit, foreman, and conform all exist, but the repo does not yet present them as one easy-to-explain shipping system.
- DP-OPS-0208 hardened the architect path and DP-OPS-0209 added local hook guardrails, but the broader spine is still spread across MANUAL, bundle/open/help output, stance specs, and smoke fixtures.
- The active DP draft surface is still more packet-named than operator-simple; that keeps the truth model intact but leaves unnecessary explanation cost at the live disposable layer.
- `execution-decision` exists as a runbook lane and operator-tested prompt, but the fresher prompt wording and the intended disposable/manual placement contract are not yet reflected in canon.
- `foreman` exists as an authorization-intake bundle and `ops/bin/addendum` exists as an issuance tool, but the repo does not yet present the full intervention chain as one explainable transport path during execution or audit.
- `OPEN` and `OPEN-PORCELAIN` still read more like inherited habit than a sharply justified shipping contract; `OPEN-PORCELAIN` in particular is dirty-state detail rather than a universal requirement.
- The main risk is not lack of tooling. It is operator explanation debt, duplicate active-surface language, and sidepaths that can feel more official than they are.

### 3.4.2 Request
- Define the active spine end to end in one repo-consistent contract.
- Keep the main chain explicit: topic -> plan -> DP draft -> execution -> results/closing -> audit -> merge.
- Make the active DP draft surface simple-name (`DP.md`) without losing process identity (`DP-OPS-XXXX`).
- Name secondary lanes explicitly: foreman/addendum, conform/conformist, execution-decision.
- Leave the repo with enough accurate role/task/surface data that later factory definitions for `Worker`, `Analyst`, `Architect`, `Auditor`, `Foreman`, and `Conformist` can be seeded from repo reality instead of operator memory.
- Separate the default closeout refresh route from audit bundle dump generation so the shipping spine does not imply two different dump obligations by accident.
- Decide whether `OPEN` is truly spine-grade or only a trace-recovery convenience, and stop implying `OPEN-PORCELAIN` is universally required when it is only dirty-state detail.
- Replace the stale execution-decision prompt text in canon with the fresher operator-tested wording and lock the interim handling as disposable/manual placement rather than bundle transport.
- Define the intervention path so a contractor or auditor can call for foreman help, the operator can wake foreman via bundle, and an authorized addendum can then be delivered as an explicit bundle/transport surface rather than repo folklore.
- Prove the contract through route-contract smoke rather than relying on operator memory.
- Minimize wording sprawl and duplicate surface names while preserving fail-closed behavior.

### 3.4.3 Changelog
UPDATE:
- docs/MANUAL.md
- docs/MAP.md
- ops/bin/help
- ops/bin/open
- ops/bin/certify
- ops/src/surfaces/dp.md.tpl
- ops/lib/scripts/bundle.sh
- ops/lib/manifests/BUNDLE.md
- docs/ops/specs/binaries/bundle.md
- docs/ops/specs/binaries/open.md
- docs/ops/specs/binaries/dump.md
- docs/ops/specs/binaries/addendum.md
- docs/ops/specs/binaries/draft.md
- docs/ops/specs/runbook/execution-decision.md
- ops/src/runbook/execution-decision.yaml
- docs/ops/specs/tools/lint/response.md
- docs/ops/specs/stances/architect.md
- docs/ops/specs/stances/foreman.md
- docs/ops/specs/stances/conformist.md
- tools/lint/integrity.sh
- ops/bin/draft
- tools/test/bundle.sh
- storage/dp/active/allowlist.txt
- storage/dp/intake/DP.md
- storage/handoff/CLOSING-DP-OPS-0210.md

### 3.4.4 Patch / Diff
1. Update storage/dp/active/allowlist.txt first with every tracked file touched by this packet and the active closing-sidecar path for DP-OPS-0210.
2. Inventory the live shipping chain across analyst, architect, audit, foreman, conform, execution-decision, closeout, and merge guidance.
3. Pivot the live DP draft contract to `storage/dp/intake/DP.md`, document that packet identity remains internal/process-grade as `DP-OPS-XXXX`, and keep the packet-scoped intake mirror for certify/processed continuity.
4. In ops/bin/draft and docs/ops/specs/binaries/draft.md, refresh the active latest-wins draft surface at `storage/dp/intake/DP.md` whenever a packet-scoped intake draft is generated.
5. In docs/MANUAL.md, docs/MAP.md, ops/bin/help, and ops/bin/open, rewrite the operator-facing narrative so the same end-to-end spine appears everywhere with one obvious active surface per stage.
6. In ops/src/surfaces/dp.md.tpl, stop stamping the unconditional closeout `core` dump into every packet and rewrite the default closeout route so operator refresh and audit dump generation are explicitly separate.
7. In ops/bin/certify, ops/bin/open, and open-related docs, decide whether `OPEN` remains a justified trace dependency and make `OPEN-PORCELAIN` explicitly conditional dirty-state evidence instead of implied always-on shipping output.
8. In docs/ops/specs/runbook/execution-decision.md and ops/src/runbook/execution-decision.yaml, replace the stale prompt text with the fresher operator-tested execution-decision prompt wording and align the schema/labels to that wording.
9. In docs/MANUAL.md, docs/MAP.md, and the runbook/spec surfaces, define the interim execution-decision placement contract explicitly: `storage/handoff/EXECUTION-DECISION.md` for received fenced markdown, `storage/dp/intake/EXECUTION-DECISION.md` for architect intake, and no bundle profile yet.
10. In bundle/addendum surfaces, define the full intervention chain explicitly:
   - contractor or auditor may report `ADDENDUM REQUIRED`
   - operator may run the foreman wake-up bundle
   - authorized addendum may then be emitted/delivered through an explicit addendum bundle/transport surface
   - contractor resumes only against the authorized addendum
11. In the affected specs and stance documents, align analyst/architect/foreman/conformist/execution-decision boundaries to the same spine map and remove wording that implies parallel authority.
12. In tools/test/bundle.sh, strengthen route-contract proof so the declared spine, including simple active naming versus packet identity, closeout-vs-audit dump separation, conditional `OPEN-PORCELAIN`, disposable/manual execution-decision handling, and explicit foreman/addendum intervention transport, is validated deliberately.
13. Keep the packet bounded to shipping-spine clarity and contract proof; do not drift into factory transition, ingest tooling, or broad stabilization.

### 3.4.5 Receipt (Proofs to collect) - MUST RUN

**Mandatory receipt commands (always run; do not omit):**
- bash tools/lint/dp.sh TASK.md
- bash tools/lint/task.sh
- bash tools/lint/integrity.sh
- bash tools/lint/style.sh
- ./ops/bin/llms
- bash tools/lint/integrity.sh
- git diff --name-only
- git diff --stat
- comm -23 <(git diff --name-only | sort) <(sort storage/dp/active/allowlist.txt) || true
- comm -23 <(git ls-files --others --exclude-standard | sort) <(sort storage/dp/active/allowlist.txt) || true
- ./ops/bin/open

**DP-specific receipt commands (scope-specific; author below):**
Note: Command substitution forms (e.g., $(pwd), $(git ...)) are rejected by certify replay. Use literal values only. dp.sh enforces at lint time.
- bash tools/test/bundle.sh --slice=route-contract
- bash tools/verify.sh --mode=certify-critical
- grep -n "TOPIC.md\|PLAN.md\|DP.md\|DP-OPS-\|RESULTS\|CLOSING-\|foreman\|conform\|conformist\|addendum\|execution-decision\|merge\|dump --scope=core\|Capture updated Ops state\|OPEN-PORCELAIN\|STELA_TRACE_ID\|REQUEST FOR EXECUTION-DECISION LOG" docs/MANUAL.md docs/MAP.md ops/bin/help ops/bin/open ops/bin/certify ops/src/surfaces/dp.md.tpl ops/lib/scripts/bundle.sh ops/lib/manifests/BUNDLE.md docs/ops/specs/binaries/bundle.md docs/ops/specs/binaries/open.md docs/ops/specs/binaries/dump.md docs/ops/specs/binaries/addendum.md docs/ops/specs/runbook/execution-decision.md docs/ops/specs/tools/lint/response.md docs/ops/specs/stances/architect.md docs/ops/specs/stances/foreman.md docs/ops/specs/stances/conformist.md ops/src/runbook/execution-decision.yaml tools/test/bundle.sh

## 3.5 Closeout (Mandatory Routing)
- Execute docs/MANUAL.md Closeout Cycle in order (Verify, Harvest, Refresh, Log, Prune).
- Update SoP.md and PoW.md with DP entries, including objective summary and verification commands run.
- Protocol order for closeout: Verify -> Generate Results -> COMMIT (Operator Only) -> Prune.
- Run prune hygiene: ./ops/bin/prune --scrub.
- Refresh llms artifacts: `./ops/bin/llms`
- Regenerate session artifacts: `./ops/bin/open --out=auto`
- Ensure the next session begins with refreshed session artifacts and matching receipts.
- Note: audit dump generation is owned by `./ops/bin/bundle --profile=audit --out=auto` and is separate from operator session refresh. Do not stamp an additional `ops/bin/dump --scope=core` into closeout unless the DP explicitly authorizes a standalone state capture.
- Refresh side-effect: `ops/bin/llms` regenerates `ops/lib/manifests/OPS.md` as a compile event; if `OPS.md` is not in the allowlist, restore it before running `integrity.sh`. See `docs/MANUAL.md` Refresh side-effect notice for the full procedure.

### 3.5.1 Mandatory Closing Sidecar
Closing sidecar content is generated and maintained at `storage/handoff/CLOSING-DP-OPS-0210.md` and is validated by `ops/bin/certify` as a hard gate at certification time. Do not author, predict, populate, or approximate any sidecar-derived closeout output outside this sidecar.
Certify separately collects contractor-authored narrative for the RESULTS Contractor Execution Narrative section at certify time via interactive editor prompt; this narrative input is independent from closing sidecar content.

Before running certify, confirm `storage/handoff/CLOSING-DP-OPS-0210.md` has been maintained throughout execution and reflects observable reality only.

Closing sidecar fields required at certify time:
- Commit Message
- Create Pull Request (Title)
- Create Pull Request (Description)
- Confirm Merge (Commit Message)
- Confirm Merge (Extended Description)
- Confirm Merge (Add a Comment)
