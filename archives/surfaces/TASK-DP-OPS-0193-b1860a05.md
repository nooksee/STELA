---
trace_id: stela-20260315T014409Z-07cd28e5
packet_id: DP-OPS-0193
created_at: 2026-03-15T01:47:08Z
previous: archives/surfaces/TASK-DP-OPS-0192-6e3eb072.md
---
# STELA TASK DASHBOARD
Status: ACTIVE
Owner: Integrator
Last Updated: 2026-03-02

## 1. Session State
Pointer: Session context output (generated by ops/bin/open)
Context Manifest: ops/lib/manifests/CONTEXT.md (checked by tools/lint/context.sh)

Session State contract:
- Session State is pointer-first. Do not write inline branch or hash state here.
- OPEN and OPEN-PORCELAIN are the session state snapshot.
- OPEN-PORCELAIN is emitted on every OPEN run. Clean sessions produce a zero-line artifact.
- RESULTS is the audit record for the DP.
- TASK is the stable routing surface.
- Operator note (2026-02-17): Do not edit `SoP.md` or `PoW.md` unless explicitly instructed.

## 2. Logic Pointers
Primary Constraint: PoT.md (Policy of Truth).
Surface Contract Spec: docs/ops/specs/surfaces/task.md.

### 2.1 Governance Pointers
Jurisdiction: PoT.md Section 3.
Git Authority: PoT.md Section 4.1.
Behavioral Standard: PoT.md Section 4.2.
Staffing Protocol: PoT.md Section 4.1.

### 2.2 Enforcement and Gates
Style: tools/lint/style.sh
Context: tools/lint/context.sh
Truth: tools/lint/truth.sh
DP lint: tools/lint/dp.sh (enforces DP transaction rules in Section 3; does not enforce TASK container schema)
TASK lint: tools/lint/task.sh (sole TASK surface/container schema enforcer)
Repo verify: tools/verify.sh
llms parity: tools/lint/llms.sh

## 3. Current Dispatch Packet (DP)

Section-by-section authoring protocol:
- Generate DP structure from `ops/src/surfaces/dp.md.tpl` using `ops/bin/draft`.
- Do not hand-edit DP structural boilerplate in `TASK.md` after generation.
- Manual edits are limited to slot content only (objective, scope, safety, plan body, receipt body).
- Freeze each completed subsection in order (3.1 -> 3.2 -> 3.3 -> 3.4.x).
- Do not revise earlier sections to justify later work. If scope changes, end the thread and draft a new DP.

Freeze Rule:
- If a completed DP subsection changes later, append an AMENDMENT block in RESULTS; otherwise DP is FAIL.

AMENDMENT: minimum fields
- Reopened section:
- Reason:
- Impact:
- Allowlist change (none or exact paths):

Input discipline (hard rule):
- Worker receives only the DP text. Worker does not require OPEN or DUMP attachments.
- DP writer must not attach or cite disposable artifacts (rough notes, chat logs, scratch drafts, throwaway excerpts).
- DP writer must not include pasted bundles (OPEN, DUMP payloads, manifests) inside the DP body.
- Canon pointers only. Evidence belongs in storage artifacts and RESULTS.


### DP-OPS-0190: Architect Packet Fidelity and Canonical Conformance

## 3.1 Freshness Gate (Must Pass Before Work)
Base Branch: main
Required Work Branch: PROPOSED/work/dp-ops-0190-t1-1-architect-packet-fidelity-canonical-conformance-2026-03-14
Base HEAD: 02ed6bba
Freshness Stamp: 2026-03-14
Note: FRESHNESS_STAMP must be YYYY-MM-DD format only. No trace tokens, no timestamps, no other text. Certify rejects all other forms. dp.sh enforces at lint time.

Required local re-check (worker runs; paste outputs in RESULTS):
- git rev-parse --abbrev-ref HEAD
- git rev-parse --short HEAD
- git status --porcelain

Preconditions:
- No commits on main.
- Working tree must be clean before execution begins.
- If Base HEAD changes, regenerate session artifacts from canonical tools before proceeding.

STOP conditions:
- STOP if any mismatch (branch, Base HEAD, missing work branch).
- STOP if working tree is dirty before execution begins.
- STOP if told to create or switch branches.

## 3.1.1 DP Preflight Gate (Run Before Any Edits)
Purpose:
- Catch malformed DP or TASK schema before work begins.

Worker runs (paste outcome in RESULTS):
- bash tools/lint/dp.sh --test
- bash tools/lint/dp.sh TASK.md
- bash tools/lint/task.sh

STOP if any preflight check fails.

### CbC Design Discipline Preflight (TASK.md §3.1.1)
Required when the DP objective adds, modifies, or replaces a linter, script, guard, or validation binary.
For non-tooling DPs: state "Not applicable" with a one-line justification.
Applicable. This packet modifies enforcement and intake-contract tooling for architect DP generation: `tools/lint/dp.sh`, `ops/src/stances/architect.md.tpl`, and directly coupled architect transport tests. Structural prevention already exists in machine-carried architect request metadata (`packet_id`, `closing_sidecar`, `title_suffix`) and in the canonical DP template, but detection remains necessary because raw model output can still drift on packet identity usage, truncate canonical tail sections, or insert illegal receipt proofs. The intended checks are deterministic string or structure assertions over visible packet text and transport fixtures, with low false-positive risk and bounded implementation cost.

## 3.2 Required Context Load (Read Before Doing Anything)

### 3.2.1 Canon load order (always)
Worker must confirm loaded before edits begin:
1. PoT.md
2. SoP.md
3. PoW.md
4. TASK.md
5. docs/MAP.md
6. docs/MANUAL.md
7. ops/lib/manifests/CONTEXT.md

Notes:
- Worker does not read OPEN. OPEN is for Integrator state refresh and for receipts.
- Disposable artifacts must not be referenced or included.
- Worker input is the DP text only.
- DP writer must not attach or cite disposable artifacts.
- DP writer must not embed pasted bundles.

## Rules

1 Temp path rule uses `var/tmp` only
2 RESULTS generation rule uses `ops/bin/certify` only
3 Receipt rule runs every DP mandated command and treats any skip as STOP
4 Disposable artifact rule treats OPEN dump bundles and manifests as non canonical session artifacts
5 Use repo-relative paths only in all output. Never print absolute filesystem paths. Do not emit clickable file links.

### 3.2.2 DP-scoped load order (per DP)
- storage/handoff/PLAN.md
- PoT.md
- ops/src/stances/architect.md.tpl
- docs/ops/specs/stances/architect.md
- ops/src/surfaces/dp.md.tpl
- tools/lint/dp.sh
- docs/ops/specs/tools/lint/dp.md
- tools/test/bundle.sh
- docs/ops/specs/tools/test/bundle.md
- docs/ops/specs/binaries/bundle.md
- storage/dp/active/allowlist.txt

## 3.3 Scope and Safety
Objective:
- Eliminate the remaining architect packet-fidelity drift by binding drafted architect packets to transport-carried packet identity, full canonical tail completion, and legal receipt proof shapes.
- Add deterministic lint and directly coupled test coverage so architect packet identity and receipt legality fail closed.

In scope:
- UPDATE `ops/src/stances/architect.md.tpl` to require use of transport-carried `request.packet_id` and `request.closing_sidecar` only, require full canonical `3.5` and `3.5.1` tail completion, and encode the T1.1 receipt contract without illegal proof shapes.
- UPDATE `docs/ops/specs/stances/architect.md` to document the transport-defined identity contract, canonical-tail completion requirement, and architect receipt restrictions.
- UPDATE `ops/src/surfaces/dp.md.tpl` so the work-branch field reads as a proposal slot to the DP writer by template shape, using a proposal-specific slot name such as `{{PROPOSED_WORK_BRANCH}}` rather than an opaque branch slot.
- UPDATE `tools/lint/dp.sh` to reject architect-packet drift that is visible in packet text: truncated or placeholder-tainted canonical tail sections and forbidden receipt-command patterns that touch dump artifacts or archive-history-derived packet identity proofs.
- UPDATE `docs/ops/specs/tools/lint/dp.md` to document the new architect-facing packet-fidelity and receipt-legality checks.
- UPDATE `docs/MANUAL.md` so its pre-dispatch finalize language for provisional values matches PoT and `tools/lint/dp.sh` without stage-confusing restatement.
- UPDATE `tools/test/bundle.sh` and `docs/ops/specs/tools/test/bundle.md` only as needed for direct architect transport fixtures that prove the T1.1 packet identity metadata is carried machine-first for the active slice.
- UPDATE `storage/dp/active/allowlist.txt` with every tracked file touched by this packet.

Out of scope:
- New architect slice transport or bundle-routing behavior from T1.
- Include-boundary hygiene work from T1.2.
- Edits to generated artifacts under `storage/handoff/` or `storage/dumps/`.
- Structural edits to `TASK.md` or archived TASK leaves.
- Any packet id, closing sidecar, or title derivation from archive names, dump paths, or TASK history.
- Any new options, phases, slices, or handoff-surface rewrites outside active slice T1.1.

Safety and invariants:
- No manual generated output edits: do not edit bundle artifacts, dump artifacts, RESULTS, or archive leaves as part of this packet.
- No structural TASK edits: keep `TASK.md` container structure unchanged in this slice.
- Treat transport metadata as authoritative: architect packet identity must come from machine-carried request fields, never from archive inference.
- Keep provisional-value semantics aligned with PoT by slot shape and finalize-stage wording; do not add new downstream authority narration.
- Keep receipt proofs within the active slice contract: allow packet identity checks against transport metadata, `tools/lint/dp.sh`, directly coupled packet-structure tests, and exact canonical-tail checks only.
- Reject forbidden proof shapes in authored receipts, especially `ops/bin/dump`, `storage/dumps/`, dump artifact filenames, and archive or TASK-history-derived packet-identity proofs.
- Allowlist hard gate remains authoritative for every touched path.

Worker Constraints (SSOT injected):
## Section 1: Universal Template Rules
- Template source uses `tpl` files with optional YAML frontmatter
- Canon frontmatter keys: `template_type` `template_id` `template_version` `requires_slots` `includes`
- Renderer strips frontmatter before output write
- Slot token form `\{\{TOKEN\}\}` with uppercase alphanumeric underscore
- Include forms `\{\{@include:path\}\}` and `\{\{@include:path#section\}\}`
- Include resolution is strict: missing file fail missing section fail circular graph fail
- Strict mode default: every required slot value present and no unresolved token
- Non strict mode allowed only for lint and normalization workflows
- Worker facing generated surfaces remain pointer first and exclude disposable artifacts

Target Files allowlist (hard gate):
- storage/dp/active/allowlist.txt

## 3.4 Execution Plan (A-E)

### 3.4.1 State
- Bundle state already resolves profile `architect` with validated active slice `T1.1`.
- Active repo state from bundle metadata is `main` at `02ed6bba`.
- Architect transport already emits machine-carried request metadata for the active slice, including `packet_id: DP-OPS-0190`, `closing_sidecar: storage/handoff/CLOSING-DP-OPS-0190.md`, and title suffix `Architect Packet Fidelity and Canonical Conformance`.
- Current drift is no longer slice selection. Current drift is architect packet fidelity: identity usage, branch-slot semantics, canonical tail completion, and illegal receipt proof shapes.
- `PLAN.md` selects corridor option A with multi-slice order `T1 -> T1.1 -> T1.2`; this packet is restricted to T1.1 only.

### 3.4.2 Request
- Implement slice T1.1 only.
- Make architect drafting consume the machine-carried packet identity fields already present in architect request metadata rather than any archive, dump, or TASK-history inference path.
- Make the DP template express the work-branch field as a proposal slot to the DP writer rather than a free-form branch field, without restating PoT authority inside downstream prose.
- Align PoT, the DP template, `tools/lint/dp.sh`, and `docs/MANUAL.md` on one consistent provisional-value stage model for drafting, pre-dispatch finalization, and worker execution.
- Tighten the architect stance contract so the drafted packet always completes canonical sections `3.5` and `3.5.1` without truncation, placeholder misuse, or sidecar-field population.
- Add deterministic packet-fidelity enforcement in `tools/lint/dp.sh` and direct architect transport fixtures so illegal receipt-command shapes and packet-tail drift fail closed.
- Keep T1 transport work and T1.2 include-boundary hygiene untouched unless a directly coupled test proves a strict dependency, in which case STOP and escalate rather than widening scope silently.

### 3.4.3 Changelog
UPDATE:
- ops/src/stances/architect.md.tpl
- docs/ops/specs/stances/architect.md
- ops/src/surfaces/dp.md.tpl
- tools/lint/dp.sh
- docs/ops/specs/tools/lint/dp.md
- docs/MANUAL.md
- tools/test/bundle.sh
- docs/ops/specs/tools/test/bundle.md
- storage/dp/active/allowlist.txt

### 3.4.4 Patch / Diff
1. Update `storage/dp/active/allowlist.txt` first with every tracked file in this packet and the active closing-sidecar path for `DP-OPS-0190`.
2. Tighten `ops/src/stances/architect.md.tpl` so architect drafting:
   - uses transport-carried `request.packet_id` and `request.closing_sidecar` as the only packet-identity source,
   - forbids archive, dump, or TASK-history inference for packet identity,
   - requires full canonical completion of `3.5` and `3.5.1`,
   - keeps `3.5.1` as sidecar-maintenance guidance only, with no populated sidecar outputs in the DP body,
   - encodes the T1.1 receipt contract so only allowed proof shapes are requested and forbidden dump-touch or archive-derived proofs are disallowed.
3. Update `docs/ops/specs/stances/architect.md` to mirror the tightened transport-identity contract, canonical-tail completion rule, and architect receipt restrictions.
4. Update `ops/src/surfaces/dp.md.tpl` so the work-branch field uses a proposal-specific slot name such as `{{PROPOSED_WORK_BRANCH}}` (or an equivalent proposal-slot form) that signals draft-stage proposal semantics without adding downstream authority narration.
5. Extend `tools/lint/dp.sh` with architect-packet fidelity checks that fail closed on:
   - missing, truncated, or placeholder-tainted `3.5` / `3.5.1` canonical tail sections,
   - forbidden receipt-command patterns under `3.4.5` that reference `ops/bin/dump`, `storage/dumps/`, dump artifact filenames, or archive / TASK-history packet-identity proof paths,
   - any architect-facing packet text pattern that contradicts the mandatory sidecar-maintenance-only contract.
6. Update `docs/ops/specs/tools/lint/dp.md` to document the new architect packet-fidelity and receipt-legality checks, including the allowed and forbidden T1.1 proof shapes.
7. Update `docs/MANUAL.md` so the pre-dispatch DP finalize gate language for provisional values matches PoT and `tools/lint/dp.sh` without adding conflicting stage commentary.
8. Extend `tools/test/bundle.sh` and `docs/ops/specs/tools/test/bundle.md` only as needed to cover a validated architect multi-slice fixture where the active slice exposes transport-carried `packet_id`, `closing_sidecar`, and `title_suffix` for T1.1 without archive inference.
9. Run the targeted lint and test stack, confirm no scope leak beyond T1.1, and collect exact receipts.

### 3.4.5 Receipt (Proofs to collect) - MUST RUN

**Mandatory receipt commands (always run; do not omit):**
- bash tools/lint/dp.sh TASK.md
- bash tools/lint/task.sh
- bash tools/lint/integrity.sh
- bash tools/lint/style.sh
- ./ops/bin/llms
- bash tools/lint/integrity.sh
- git diff --name-only
- git diff --stat
- comm -23 <(git diff --name-only | sort) <(sort storage/dp/active/allowlist.txt) || true
- comm -23 <(git ls-files --others --exclude-standard | sort) <(sort storage/dp/active/allowlist.txt) || true
- ./ops/bin/open

**DP-specific receipt commands (scope-specific; author below):**
Note: Command substitution forms (e.g., $(pwd), $(git ...)) are rejected by certify replay. Use literal values only. dp.sh enforces at lint time.
- bash tools/lint/dp.sh --test
- bash tools/test/bundle.sh
- bash tools/verify.sh
- bash tools/lint/truth.sh
- bash tools/lint/context.sh
- grep -n "canonical proposal-form branch value only" ops/src/stances/architect.md.tpl
- grep -n "Do not author or populate any §3.5.1 Mandatory Closing Sidecar field at draft time" ops/src/stances/architect.md.tpl
- grep -n "PROPOSED_WORK_BRANCH" ops/src/surfaces/dp.md.tpl
- grep -n "^## 3.5 Closeout (Mandatory Routing)$" ops/src/surfaces/dp.md.tpl
- grep -n "^### 3.5.1 Mandatory Closing Sidecar$" ops/src/surfaces/dp.md.tpl
- grep -n "disallowed PROPOSED provisional-marker form found in DP payload" tools/lint/dp.sh
- grep -n "PROPOSED" docs/ops/specs/tools/lint/dp.md
- grep -n "provisional-value usage matches the canonical draft-time DP contract" docs/MANUAL.md
- grep -n "canonical proposal form" docs/ops/specs/stances/architect.md
- grep -n "closing-sidecar coherence" docs/ops/specs/stances/architect.md

## 3.5 Closeout (Mandatory Routing)
- Execute docs/MANUAL.md Closeout Cycle in order (Verify, Harvest, Refresh, Log, Prune).
- Update SoP.md and PoW.md with DP entries, including objective summary and verification commands run.
- Protocol order for closeout: Verify -> Generate Results -> COMMIT (Operator Only) -> Prune.
- Run prune hygiene: ./ops/bin/prune --scrub.
- Refresh llms artifacts: `./ops/bin/llms`
- Regenerate session artifacts: `./ops/bin/open --out=auto`
- Capture updated Ops state: `./ops/bin/dump --scope=core --format=chatgpt --out=auto`
- Ensure the next session begins with refreshed session artifacts and matching receipts.
- Refresh side-effect: `ops/bin/llms` regenerates `ops/lib/manifests/OPS.md` as a compile event; if `OPS.md` is not in the allowlist, restore it before running `integrity.sh`. See `docs/MANUAL.md` Refresh side-effect notice for the full procedure.

### 3.5.1 Mandatory Closing Sidecar
Closing sidecar content is generated and maintained at `storage/handoff/CLOSING-DP-OPS-0190.md` and is validated by `ops/bin/certify` as a hard gate at certification time. Do not author, predict, populate, or approximate any sidecar-derived closeout output outside this sidecar.
Certify separately collects contractor-authored narrative for the RESULTS Contractor Execution Narrative section at certify time via interactive editor prompt; this narrative input is independent from closing sidecar content.

Before running certify, confirm `storage/handoff/CLOSING-DP-OPS-0190.md` has been maintained throughout execution and reflects observable reality only.

Closing sidecar fields required at certify time:
- Commit Message
- Create Pull Request (Title)
- Create Pull Request (Description)
- Confirm Merge (Commit Message)
- Confirm Merge (Extended Description)
- Confirm Merge (Add a Comment)
