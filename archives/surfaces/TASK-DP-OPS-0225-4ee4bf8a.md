---
trace_id: stela-20260323T204155Z-1fec0576
packet_id: DP-OPS-0225
created_at: 2026-03-23T21:48:54Z
previous: (none)
---
# STELA TASK DASHBOARD
Status: ACTIVE
Owner: Integrator
Last Updated: 2026-03-19

## 1. Session State
Pointer: Session context output (generated by ops/bin/open)
Context Manifest: ops/lib/manifests/CONTEXT.md (checked by tools/lint/context.sh)

Session State contract:
- Session State is pointer-first. Do not write inline branch or hash state here.
- OPEN is the session state snapshot. OPEN-PORCELAIN is conditional dirty-state detail.
- RESULTS is the audit record for the DP.
- TASK is the stable routing surface.

## 2. Logic Pointers
Primary Constraint: PoT.md (Policy of Truth).
Surface Contract Spec: docs/ops/specs/surfaces/task.md.

### 2.1 Governance Pointers
Jurisdiction: PoT.md Section 3.
Git Authority: PoT.md Section 4.1.
Behavioral Standard: PoT.md Section 4.2.
Staffing Protocol: PoT.md Section 4.1.

### 2.2 Enforcement and Gates
Style: tools/lint/style.sh
Context: tools/lint/context.sh
Truth: tools/lint/truth.sh
DP lint: tools/lint/dp.sh (enforces DP transaction rules in Section 3; does not enforce TASK container schema)
TASK lint: tools/lint/task.sh (sole TASK surface/container schema enforcer)
Repo verify: tools/verify.sh
llms parity: tools/lint/llms.sh

## 3. Current Dispatch Packet (DP)

Section-by-section authoring protocol:
- Generate DP structure from `ops/src/surfaces/dp.md.tpl` using `ops/bin/draft`.
- Do not hand-edit DP structural boilerplate in `TASK.md` after generation.
- Manual edits are limited to slot content only (objective, scope, safety, plan body, receipt body).
- Freeze each completed subsection in order (3.1 -> 3.2 -> 3.3 -> 3.4.x).
- Do not revise earlier sections to justify later work. If scope changes, end the thread and draft a new DP.

Freeze Rule:
- If a completed DP subsection changes later, append an AMENDMENT block in RESULTS; otherwise DP is FAIL.

AMENDMENT: minimum fields
- Reopened section:
- Reason:
- Impact:
- Allowlist change (none or exact paths):

Input discipline (hard rule):
- Worker receives only the DP text. Worker does not require OPEN or DUMP attachments.
- DP writer must not attach or cite disposable artifacts (rough notes, chat logs, scratch drafts, throwaway excerpts).
- DP writer must not include pasted bundles (OPEN, DUMP payloads, manifests) inside the DP body.
- Canon pointers only. Evidence belongs in storage artifacts and RESULTS.



### DP-OPS-0225: Architect to Draft rename

## 3.1 Freshness Gate (Must Pass Before Work)
Base Branch: main
Required Work Branch: work/dp-ops-0225-architect-draft-upgrade-2026-03-23
Base HEAD: 4ee4bf8a
Freshness Stamp: 2026-03-23
Note: FRESHNESS_STAMP must be YYYY-MM-DD format only. No trace tokens, no timestamps, no other text. Certify rejects all other forms. dp.sh enforces at lint time.

Required local re-check (worker runs; paste outputs in RESULTS):
- git rev-parse --abbrev-ref HEAD
- git rev-parse --short HEAD
- git status --porcelain

Preconditions:
- No commits on main.
- Working tree must be clean before execution begins.
- If Base HEAD changes, regenerate session artifacts from canonical tools before proceeding.

STOP conditions:
- STOP if any mismatch (branch, Base HEAD, missing work branch).
- STOP if working tree is dirty before execution begins.
- STOP if told to create or switch branches.

## 3.1.1 DP Preflight Gate (Run Before Any Edits)
Purpose:
- Catch malformed DP or TASK schema before work begins.

Worker runs (paste outcome in RESULTS):
- bash tools/lint/dp.sh --test
- bash tools/lint/dp.sh TASK.md
- bash tools/lint/task.sh

STOP if any preflight check fails.

### CbC Design Discipline Preflight (TASK.md §3.1.1)
Required when the DP objective adds, modifies, or replaces a linter, script, guard, or validation binary.
For non-tooling DPs: state "Not applicable" with a one-line justification.
Not applicable. This DP renames identifiers — no linter, script, guard, or validation binary is added, replaced, or modified in functional behavior. tools/lint/style.sh and tools/lint/response.sh are updated only to rename internal identifiers (variable names, function names, mode strings) to stay consistent with the renamed stance file.

## 3.2 Required Context Load (Read Before Doing Anything)

### 3.2.1 Canon load order (always)
Worker must confirm loaded before edits begin:
1. PoT.md
2. SoP.md
3. PoW.md
4. TASK.md
5. docs/MAP.md
6. docs/MANUAL.md
7. ops/lib/manifests/CONTEXT.md

Notes:
- Worker does not read OPEN. OPEN is for Integrator state refresh and for receipts.
- Disposable artifacts must not be referenced or included.
- Worker input is the DP text only.
- DP writer must not attach or cite disposable artifacts.
- DP writer must not embed pasted bundles.

## Rules

1 Temp path rule uses `var/tmp` only
2 RESULTS generation rule uses `ops/bin/certify` only
3 Receipt rule runs every DP mandated command and treats any skip as STOP
4 Disposable artifact rule treats OPEN dump bundles and manifests as non canonical session artifacts
5 Use repo-relative paths only in all output. Never print absolute filesystem paths. Do not emit clickable file links.

### 3.2.2 DP-scoped load order (per DP)
Read before edits:
- storage/dp/active/allowlist.txt
- storage/handoff/PLAN-FACTORY.md (Architect to Draft section)
- ops/src/stances/architect.md.tpl (full)
- docs/ops/specs/stances/architect.md (full)
- ops/lib/manifests/BUNDLE.md
- ops/lib/scripts/bundle.sh (architect references)
- tools/lint/response.sh (architect references)
- tools/lint/style.sh (architect references)
- ops/bin/manifest
- ops/bin/template
- tools/test/bundle.sh (architect references)
- ops/etc/verification.manifest

## 3.3 Scope and Safety
Objective:
Rename all system artifacts that identify the architect task to "draft". The Architect role name is a staffing identity and is not changed. Every bundle profile, stance file name, artifact prefix, mode string, manifest key, variable name, function name, lint identifier, test fixture, and prose reference that names the architect task becomes "draft" or "DRAFT". After this DP, --profile=architect is not a valid profile and ops/src/stances/architect.md.tpl does not exist.

In scope:
File renames:
- ops/src/stances/architect.md.tpl → ops/src/stances/draft.md.tpl
- docs/ops/specs/stances/architect.md → docs/ops/specs/stances/draft.md

ops/src/stances/draft.md.tpl (renamed from architect.md.tpl):
- template_id: architect → template_id: draft
- resolved_profile=architect → resolved_profile=draft
- "correct architect bundle" → "correct draft bundle"
- "in architect mode" → "in draft mode" (two occurrences; style.sh required_scope_only must match in lockstep)

ops/lib/manifests/BUNDLE.md:
- supported_profiles: architect → draft
- auto_plan_profile=architect → auto_plan_profile=draft
- stance_template_architect=stance-architect → stance_template_draft=stance-draft
- artifact_prefix_architect=ARCHITECT → artifact_prefix_draft=DRAFT
- dump_scope_architect=full → dump_scope_draft=full
- All prose: architect → draft, ARCHITECT → DRAFT

ops/lib/scripts/bundle.sh:
- Usage line: architect → draft
- Function bundle_resolve_architect_packet_id → bundle_resolve_draft_packet_id (definition and all call sites)
- architect) case → draft)
- All "architect" string comparisons → "draft"
- ARCHITECT prefix strings → DRAFT

tools/lint/response.sh:
- Usage line and mode matrix comment: architect → draft
- check_architect_body_scope → check_draft_body_scope (function def and call)
- All "architect body" error strings → "draft body"
- response_mode="architect" → response_mode="draft" in test block
- All response_architect_* var names → response_draft_*
- EOF_ARCH_AUDIT → EOF_DRAFT_AUDIT, EOF_ARCH_NARRATIVE → EOF_DRAFT_NARRATIVE (and any other EOF_ARCH_* tags)
- Mode validation list: architect → draft

tools/lint/style.sh:
- check_architect_mode_contract → check_draft_mode_contract (function def and call)
- local stance_architect=.../architect.md.tpl → local stance_draft=.../draft.md.tpl
- All $stance_architect → $stance_draft
- All "architect.md.tpl missing..." error strings → "draft.md.tpl missing..."
- required_scope_only: "in architect mode" → "in draft mode" (must match updated draft.md.tpl text exactly; tpl and style.sh change in lockstep)

ops/bin/manifest:
- stance-architect → stance-draft (valid-key regex, usage, case branch)
- [stance-architect]="ops/src/stances/architect.md.tpl" → [stance-draft]="ops/src/stances/draft.md.tpl"

ops/bin/template:
- stance-architect → stance-draft (valid-key list, case branch)

ops/bin/open:
- NEXT OPERATOR MOVES example: --profile=architect --out=auto → --profile=draft --out=auto

ops/bin/help:
- Command Index bundle description profile list: architect → draft

tools/test/bundle.sh:
- --profile=architect → --profile=draft
- All resolved_profile='architect' assertions → 'draft'
- All ARCHITECT prefix checks → DRAFT

ops/etc/verification.manifest:
- bundle-route-contract lane match field: ops/src/stances/architect.md.tpl → ops/src/stances/draft.md.tpl

storage/dp/active/allowlist.txt:
- Append: ops/src/stances/draft.md.tpl
- Append: docs/ops/specs/stances/draft.md

Docs prose sweep (architect → draft where naming task/profile; preserve "Architect" as role name):
- docs/MANUAL.md
- docs/MAP.md
- docs/ops/specs/binaries/bundle.md
- docs/ops/specs/stances/plan.md
- docs/ops/registry/agents.md (task/profile refs only; role name preserved)
- docs/ops/registry/binaries.md
- docs/ops/registry/tasks.md
- docs/ops/registry/lint.md
- Other docs/ops/specs/* files with architect task references

Out of scope:
- Auditor → Audit rename (item 2f, separate DP)
- Foreman → Addenda rename (item 2g, separate DP)
- Conformist/Conform removal (item 2h, separate DP)
- Contractor removal (item 2i, separate DP)
- H1: llms hook inline (standalone DP)
- Stance-Family Normalization (item 3, post-decoupling)
- Factory Canon Reset (item 4, post-decoupling)
- Changes to ops/bin/certify, ops/bin/bundle, ops/bin/draft, or any binary not listed in scope
- Changes to ops/src/stances/auditor.md.tpl, foreman.md.tpl, conformist.md.tpl, or planning.md.tpl

Safety and invariants:
- bash tools/lint/response.sh --test must pass
- bash tools/lint/style.sh must pass (no failed assertions)
- ops/bin/bundle --profile=draft must resolve without error
- ops/bin/manifest stance-draft must return ops/src/stances/draft.md.tpl
- --profile=architect must NOT be a valid profile after this DP (bundle must reject it)
- ops/src/stances/architect.md.tpl must not exist after the file rename
- bash tools/test/bundle.sh --mode=certify-critical must pass
- bash tools/test/bundle.sh --slice=route-contract must pass
- No changes to certify, bundle binary, open binary, or any binary outside listed scope
- style.sh required_scope_only must match the updated "in draft mode" line in draft.md.tpl exactly

Worker Constraints (SSOT injected):
## Section 1: Universal Template Rules
- Template source uses `tpl` files with optional YAML frontmatter
- Canon frontmatter keys: `template_type` `template_id` `template_version` `requires_slots` `includes`
- Renderer strips frontmatter before output write
- Slot token form `\{\{TOKEN\}\}` with uppercase alphanumeric underscore
- Include forms `\{\{@include:path\}\}` and `\{\{@include:path#section\}\}`
- Include resolution is strict: missing file fail missing section fail circular graph fail
- Strict mode default: every required slot value present and no unresolved token
- Non strict mode allowed only for lint and normalization workflows
- Worker facing generated surfaces remain pointer first and exclude disposable artifacts

Target Files allowlist (hard gate):
- storage/dp/active/allowlist.txt

## 3.4 Execution Plan (A-E)

### 3.4.1 State
Completed: DP-OPS-0220 (process integrity), DP-OPS-0221 (execution-decision reset), DP-OPS-0222 (foreman evidence-first reset), DP-OPS-0223 (label-anchor hardening / --intake-only / --rerun guard), DP-OPS-0224 (analyst→planning rescue closeout; item 2d complete).
Active: DP-OPS-0225 (this packet) — item 2e, Architect → Draft.
Remaining after this DP: 2f (Auditor → Audit), 2g (Foreman → Addenda), 2h (Conformist removal), 2i (Contractor removal), H1 (llms hook inline), Item 3 (Stance-Family Normalization), Item 4 (Factory Canon Reset).

### 3.4.2 Request
Execute item 2e: rename all system artifacts that identify the architect task to "draft". One DP per the established one-per-role decoupling pattern. Full change inventory in storage/handoff/PLAN-FACTORY.md Architect to Draft section.

### 3.4.3 Changelog
After certification, update storage/handoff/PLAN.md:
- Add to Completed section: "Item 2e: Architect → Draft — landed in DP-OPS-0225 (AUDIT PASS)"
- Remove item 2e block from Remaining section

After certification, update storage/handoff/PLAN-FACTORY.md:
- Architect row Status: "Researched — ready" → "Complete — DP-OPS-0225"

### 3.4.4 Patch / Diff
storage/handoff/PLAN.md — Completed section, add line:
- Item 2e: Architect → Draft — landed in DP-OPS-0225 (AUDIT PASS)

storage/handoff/PLAN.md — Remaining section, remove:
#### 2e. Architect → Draft
DP: TBD
Goal: rename all system artifacts that identify the architect task to "draft"
Acceptance: no architect identifier remains in profiles, stance files, prefixes, lint modes, or function names

storage/handoff/PLAN-FACTORY.md — mapping table Architect row:
Status: "Researched — ready" → "Complete — DP-OPS-0225"

### 3.4.5 Receipt (Proofs to collect) - MUST RUN

**Mandatory receipt commands (always run; do not omit):**
- bash tools/lint/dp.sh TASK.md
- bash tools/lint/task.sh
- bash tools/lint/integrity.sh
- bash tools/lint/style.sh
- ./ops/bin/llms
- bash tools/lint/integrity.sh
- git diff --name-only
- git diff --stat
- comm -23 <(git diff --name-only | sort) <(sort storage/dp/active/allowlist.txt) || true
- comm -23 <(git ls-files --others --exclude-standard | sort) <(sort storage/dp/active/allowlist.txt) || true
- ./ops/bin/open

**DP-specific receipt commands (scope-specific; author below):**
Note: Command substitution forms (e.g., $(pwd), $(git ...)) are rejected by certify replay. Use literal values only. dp.sh enforces at lint time.
- ./ops/bin/manifest stance-draft --out=-
- rg -n -- '--profile=architect|stance-architect|ops/src/stances/architect\.md\.tpl|docs/ops/specs/stances/architect\.md' ops docs tools || true
- bash tools/test/bundle.sh --slice=route-contract

## 3.5 Closeout (Mandatory Routing)
- Execute docs/MANUAL.md Closeout Cycle in order (Verify, Harvest, Refresh, Log, Prune).
- Update SoP.md and PoW.md with DP entries, including objective summary and current proof / receipt summary.
- Protocol order for closeout: Verify -> Generate Results -> COMMIT (Operator Only) -> Prune.
- Run prune hygiene: ./ops/bin/prune --scrub.
- Refresh llms artifacts: `./ops/bin/llms`
- Regenerate session artifacts: `./ops/bin/open --out=auto`
- Ensure the next session begins with refreshed session artifacts and matching receipts.
- Note: audit dump generation is owned by `./ops/bin/bundle --profile=audit --out=auto` and is separate from operator session refresh. Do not stamp an additional `ops/bin/dump --scope=core` into closeout unless the DP explicitly authorizes a standalone state capture.
- Refresh side-effect: `ops/bin/llms` regenerates `ops/lib/manifests/OPS.md` as a compile event; if `OPS.md` is not in the allowlist, restore it before running `integrity.sh`. See `docs/MANUAL.md` Refresh side-effect notice for the full procedure.

### 3.5.1 Mandatory Closing Sidecar
Closing sidecar content is generated and maintained at `storage/handoff/CLOSING.md` and is validated by `ops/bin/certify` as a hard gate at certification time. Do not author, predict, populate, or approximate any sidecar-derived closeout output outside this sidecar.
Certify separately collects contractor-authored narrative for the RESULTS Contractor Execution Narrative section at certify time via interactive editor prompt; this narrative input is independent from closing sidecar content.

Before running certify, confirm `storage/handoff/CLOSING.md` has been maintained throughout execution and reflects observable reality only.

Closing sidecar fields required at certify time:
- Commit Message
- Create Pull Request (Title)
- Create Pull Request (Description)
- Confirm Merge (Commit Message)
- Confirm Merge (Extended Description)
- Confirm Merge (Add a Comment)
