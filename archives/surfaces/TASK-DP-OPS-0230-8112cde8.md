---
trace_id: stela-20260324T214110Z-172d4ac5
packet_id: DP-OPS-0230
created_at: 2026-03-24T21:43:45Z
previous: archives/surfaces/TASK-DP-OPS-0229-4cf4a7ab.md
---
# STELA TASK DASHBOARD
Status: ACTIVE
Owner: Integrator
Last Updated: 2026-03-19

## 1. Session State
Pointer: Session context output (generated by ops/bin/open)
Context Manifest: ops/lib/manifests/CONTEXT.md (checked by tools/lint/context.sh)

Session State contract:
- Session State is pointer-first. Do not write inline branch or hash state here.
- OPEN is the session state snapshot. OPEN-PORCELAIN is conditional dirty-state detail.
- RESULTS is the audit record for the DP.
- TASK is the stable routing surface.

## 2. Logic Pointers
Primary Constraint: PoT.md (Policy of Truth).
Surface Contract Spec: docs/ops/specs/surfaces/task.md.

### 2.1 Governance Pointers
Jurisdiction: PoT.md Section 3.
Git Authority: PoT.md Section 4.1.
Behavioral Standard: PoT.md Section 4.2.
Staffing Protocol: PoT.md Section 4.1.

### 2.2 Enforcement and Gates
Style: tools/lint/style.sh
Context: tools/lint/context.sh
Truth: tools/lint/truth.sh
DP lint: tools/lint/dp.sh (enforces DP transaction rules in Section 3; does not enforce TASK container schema)
TASK lint: tools/lint/task.sh (sole TASK surface/container schema enforcer)
Repo verify: tools/verify.sh
llms parity: tools/lint/llms.sh

## 3. Current Dispatch Packet (DP)

Section-by-section authoring protocol:
- Generate DP structure from `ops/src/surfaces/dp.md.tpl` using `ops/bin/draft`.
- Do not hand-edit DP structural boilerplate in `TASK.md` after generation.
- Manual edits are limited to slot content only (objective, scope, safety, plan body, receipt body).
- Freeze each completed subsection in order (3.1 -> 3.2 -> 3.3 -> 3.4.x).
- Do not revise earlier sections to justify later work. If scope changes, end the thread and draft a new DP.

Freeze Rule:
- If a completed DP subsection changes later, append an AMENDMENT block in RESULTS; otherwise DP is FAIL.

AMENDMENT: minimum fields
- Reopened section:
- Reason:
- Impact:
- Allowlist change (none or exact paths):

Input discipline (hard rule):
- Worker receives only the DP text. Worker does not require OPEN or DUMP attachments.
- DP writer must not attach or cite disposable artifacts (rough notes, chat logs, scratch drafts, throwaway excerpts).
- DP writer must not include pasted bundles (OPEN, DUMP payloads, manifests) inside the DP body.
- Canon pointers only. Evidence belongs in storage artifacts and RESULTS.



### DP-OPS-0230: Role-vs-Lane Vocabulary Cleanup

## 3.1 Freshness Gate (Must Pass Before Work)
Base Branch: main
Required Work Branch: work/dp-ops-0230-role-vs-lane-cleanup-2026-03-24
Base HEAD: 8112cde8
Freshness Stamp: 2026-03-24
Note: FRESHNESS_STAMP must be YYYY-MM-DD format only. No trace tokens, no timestamps, no other text. Certify rejects all other forms. dp.sh enforces at lint time.

Required local re-check (worker runs before any edits; paste verbatim outputs under `## Contractor Execution Narrative` -> `### Preflight State` in RESULTS):
- git rev-parse --abbrev-ref HEAD
- git rev-parse --short HEAD
- git status --porcelain

These three commands are freshness proof only. Do not treat §3.4.5 receipt replay as a substitute for their pre-edit outputs.

Preconditions:
- No commits on main.
- Working tree must be clean before execution begins.
- If Base HEAD changes, regenerate session artifacts from canonical tools before proceeding.

STOP conditions:
- STOP if any mismatch (branch, Base HEAD, missing work branch).
- STOP if working tree is dirty before execution begins.
- STOP if told to create or switch branches.

## 3.1.1 DP Preflight Gate (Run Before Any Edits)
Purpose:
- Catch malformed DP or TASK schema before work begins.

Worker runs (paste outcome in RESULTS):
- bash tools/lint/dp.sh --test
- bash tools/lint/dp.sh TASK.md
- bash tools/lint/task.sh

STOP if any preflight check fails.

### CbC Design Discipline Preflight (TASK.md §3.1.1)
Required when the DP objective adds, modifies, or replaces a linter, script, guard, or validation binary.
For non-tooling DPs: state "Not applicable" with a one-line justification.
Conditionally applicable. This DP may touch `tools/lint/factory.sh` only if the targeted search in §3.4.4 Step 1 confirms a runtime-role text correction is needed. If no `tools/lint/factory.sh` edit is made, state "Not applicable" because the packet remains prose-only plus leaf/registry corrections. If a text-only correction is made in `tools/lint/factory.sh`, the change must preserve the validated runtime-role and stance-id allow-set values and remain limited to role/lane wording only; do not introduce behavioral change.

## 3.2 Required Context Load (Read Before Doing Anything)

### 3.2.1 Canon load order (always)
Worker must confirm loaded before edits begin:
1. PoT.md
2. SoP.md
3. PoW.md
4. TASK.md
5. docs/MAP.md
6. docs/MANUAL.md
7. ops/lib/manifests/CONTEXT.md

Notes:
- Worker does not read OPEN. OPEN is for Integrator state refresh and for receipts.
- Disposable artifacts must not be referenced or included.
- Worker input is the DP text only.
- DP writer must not attach or cite disposable artifacts.
- DP writer must not embed pasted bundles.

## Rules

1 Temp path rule uses `var/tmp` only
2 RESULTS generation rule uses `ops/bin/certify` only
3 Receipt rule runs every DP mandated command and treats any skip as STOP
4 Disposable artifact rule treats OPEN dump bundles and manifests as non canonical session artifacts
5 Use repo-relative paths only in all output. Never print absolute filesystem paths. Do not emit clickable file links.

### 3.2.2 DP-scoped load order (per DP)
1. `docs/ops/specs/definitions/agents.md` — authoritative role-boundary split: `runtime_role` keeps actor noun (`auditor`); `stance_id` carries task noun (`audit`).
2. `docs/ops/registry/agents.md` — agent registry table; Runtime Role column is the canonical actor-noun surface.
3. `docs/ops/registry/factory.md` — factory census registry; Runtime Role Naming Map.
4. `opt/_factory/INDEX.md` — factory navigation index; Runtime Role Naming Map.
5. `opt/_factory/agents/r-agent-02.md` — code-reviewer agent leaf; Identity Contract block.
6. `opt/_factory/agents/r-agent-06.md` — security-reviewer agent leaf; Identity Contract block.
7. `docs/ops/specs/tools/lint/factory.md` — factory lint spec; allowed runtime-role and stance-id value sets.
8. `tools/lint/factory.sh` — factory lint runtime; runtime-role and stance-id guard patterns.
9. `docs/MANUAL.md` — operator mechanics; Shipping Spine Active Surfaces table and Analyst Workflow steps.
10. `ops/lib/manifests/BUNDLE.md` — bundle policy manifest; Planning Transport Contract.

## 3.3 Scope and Safety
Objective:
Finish role-vs-lane vocabulary cleanup now that draft-path and DP-lint equilibrium are restored. Correct any remaining explicit `audit` usages in runtime-role–bearing positions across the 8 files identified in the PLAN, and fix the confirmed actor/lane prose blends in `docs/MANUAL.md` and `ops/lib/manifests/BUNDLE.md`.

In scope:
- Targeted search-and-correct for `audit` in explicit runtime-role contexts (runtime_role fields, Runtime Role Naming Map entries, role-map prose) across: `docs/ops/registry/factory.md`, `opt/_factory/INDEX.md`, `docs/ops/registry/agents.md`, `opt/_factory/agents/r-agent-02.md`, `opt/_factory/agents/r-agent-06.md`, `docs/ops/specs/definitions/agents.md`, `docs/ops/specs/tools/lint/factory.md`, `tools/lint/factory.sh`.
- Confirmed prose fixes in `docs/MANUAL.md`: replace two "analyst model" actor/lane blends with role noun "Analyst".
- Confirmed prose fix in `ops/lib/manifests/BUNDLE.md`: replace "written by the model" in Planning Transport Contract with "written by the Analyst".
- Internal identifiers (`stance_id: audit`, profile keys `audit`, stance template key `stance-audit`, render keys) are explicitly out of scope and must not be touched.

Out of scope:
- `stance_id` values in agent leaves and registries — these correctly carry the task noun `audit` and must not be changed.
- `ops/src/stances/draft.md.tpl` legacy compatibility note — addressed in the separate Narrow Prompt/Prose Cleanup packet per PLAN ordering.
- `storage/handoff/PLAN-MANUAL.md` and `storage/handoff/PLAN-POT.md` follow-on tracks — separate packets.
- Any factory extraction, task renaming (2g–2i), or stance normalization work (Item 3).
- No structural changes to TASK.md or DP schema.

Safety and invariants:
- No manual-generated output; all edits are exact targeted text replacements only.
- No structural changes to TASK.md or DP sections.
- Allowlist hard gate: every edited file must appear in `storage/dp/active/allowlist.txt` before edits begin.
- Do not touch `stance_id`, profile keys, stance template keys, or any internal/render identifier that correctly carries a task noun.
- If targeted search finds no mismatch in a file, do not edit that file.

Worker Constraints (SSOT injected):
## Section 1: Universal Template Rules
- Template source uses `tpl` files with optional YAML frontmatter
- Canon frontmatter keys: `template_type` `template_id` `template_version` `requires_slots` `includes`
- Renderer strips frontmatter before output write
- Slot token form `\{\{TOKEN\}\}` with uppercase alphanumeric underscore
- Include forms `\{\{@include:path\}\}` and `\{\{@include:path#section\}\}`
- Include resolution is strict: missing file fail missing section fail circular graph fail
- Strict mode default: every required slot value present and no unresolved token
- Non strict mode allowed only for lint and normalization workflows
- Worker facing generated surfaces remain pointer first and exclude disposable artifacts

Target Files allowlist (hard gate):
- storage/dp/active/allowlist.txt

## 3.4 Execution Plan (A-E)

### 3.4.1 State
Branch: main. HEAD: 8112cde8. Working tree: clean. Last commit: "Main history now aligns DP lint with PoT branch form and schema-aware marker detection."

PLAN governs this packet and identifies eight files with confirmed `audit` hard mismatches in runtime-role–bearing positions. Direct inspection of the attached dump at 8112cde8 shows those files currently using `auditor` in the visible runtime_role fields and Runtime Role Naming Map entries. This is a discrepancy between the PLAN assertion and dump evidence: either the mismatches were partially resolved by the 0226–0229 series and the PLAN list is partially stale, or the mismatches exist in positions not surfaced in the dump view. The worker must resolve this with an explicit targeted search (Step 1 below) before deciding on edits. Do not skip the search and do not assume the files are clean based on this note alone.

Confirmed from dump inspection at 8112cde8:
- `docs/MANUAL.md`: two actor/lane blends present — "analyst model writes this" (Active Surfaces table) and "to the analyst model" (Analyst Workflow step 3).
- `ops/lib/manifests/BUNDLE.md`: one actor/lane blend present — "written by the model after each planning run" in Planning Transport Contract.

### 3.4.2 Request
1. Run a targeted search across the 8 listed files for `audit` used as a runtime-role actor noun (not as a stance-id or profile key). Specifically: any `runtime_role` field value equal to `audit`; any Runtime Role Naming Map or role-map entry where the key is `audit` (not `auditor`); any actor-bearing prose phrase using "audit" as a role noun where `auditor` is the correct actor noun. STOP and record findings before proceeding.
2. For each confirmed mismatch found: replace `audit` with `auditor` in that position only. Do not touch adjacent `stance_id` values, profile keys, or internal identifiers.
3. In `docs/MANUAL.md`: apply the two confirmed soft blend corrections (see §3.4.4).
4. In `ops/lib/manifests/BUNDLE.md`: apply the one confirmed soft blend correction (see §3.4.4).
5. Do not edit any file where the search finds no mismatch.

### 3.4.3 Changelog
Files that are candidates for UPDATE based on PLAN scope and dump findings. Edit only files where a correction is confirmed by the Step 1 search or by the confirmed dump findings below.

- UPDATE `docs/MANUAL.md` — fix two confirmed actor/lane soft blends
- UPDATE `ops/lib/manifests/BUNDLE.md` — fix one confirmed actor/lane soft blend
- UPDATE `docs/ops/registry/factory.md` — correct `runtime_role` or role-map entry if Step 1 search confirms mismatch
- UPDATE `opt/_factory/INDEX.md` — correct role-map entry if Step 1 search confirms mismatch
- UPDATE `docs/ops/registry/agents.md` — correct Runtime Role column values if Step 1 search confirms mismatch
- UPDATE `opt/_factory/agents/r-agent-02.md` — correct `runtime_role` field if Step 1 search confirms mismatch
- UPDATE `opt/_factory/agents/r-agent-06.md` — correct `runtime_role` field if Step 1 search confirms mismatch
- UPDATE `docs/ops/specs/definitions/agents.md` — correct runtime-role allowed-set prose if Step 1 search confirms mismatch
- UPDATE `docs/ops/specs/tools/lint/factory.md` — correct runtime-role allowed-set spec text if Step 1 search confirms mismatch
- UPDATE `tools/lint/factory.sh` — correct runtime-role guard error-message text if Step 1 search confirms mismatch (no behavioral change to the allow-set constants)

### 3.4.4 Patch / Diff

1. **Verify allowlist coverage.** Confirm every file in §3.4.3 Changelog appears in `storage/dp/active/allowlist.txt`. STOP if any target file is absent from the allowlist.

2. **Step 1 — Targeted search across the 8 hard-mismatch candidate files.**
   Run the following commands and paste verbatim output under Preflight State in RESULTS:
```
   grep -n "runtime_role" docs/ops/registry/factory.md opt/_factory/INDEX.md docs/ops/registry/agents.md opt/_factory/agents/r-agent-02.md opt/_factory/agents/r-agent-06.md docs/ops/specs/definitions/agents.md docs/ops/specs/tools/lint/factory.md tools/lint/factory.sh
   grep -n "role.*map\|Role.*Map\|role.*naming\|Role.*Naming" docs/ops/registry/factory.md opt/_factory/INDEX.md docs/ops/registry/agents.md docs/ops/specs/definitions/agents.md
   grep -n "\baudit\b" docs/ops/registry/factory.md opt/_factory/INDEX.md docs/ops/registry/agents.md opt/_factory/agents/r-agent-02.md opt/_factory/agents/r-agent-06.md docs/ops/specs/definitions/agents.md docs/ops/specs/tools/lint/factory.md tools/lint/factory.sh
```
   For each hit containing `audit` (without `auditor`) in a runtime-role bearing position: confirm whether it is a role noun that should be `auditor` or a legitimate task/profile/stance identifier that must remain `audit`. STOP if ambiguous and request operator clarification.

3. **Step 2 — Apply confirmed hard-mismatch corrections (only for files where Step 1 confirms a defect).**
   For each file where Step 1 confirms `audit` is incorrectly used as a runtime-role actor noun:
   - Replace the specific token: `audit` → `auditor` in that position only.
   - Do not change `stance_id: audit`, `stance-audit`, `audit_profile`, `profile=audit`, or any other identifier that correctly carries the task noun.
   - Record each edit in the execution narrative with the before/after string and file:line reference.

4. **Step 3 — Fix confirmed soft blend in `docs/MANUAL.md` (unconditional).**
   In the **Shipping Spine Active Surfaces table** (near top of file), find:
```
   (latest-wins; analyst model writes this)
```
   Replace with:
```
   (latest-wins; Analyst writes this)
```

5. **Step 4 — Fix confirmed soft blend in `docs/MANUAL.md` (unconditional).**
   In the **Analyst Workflow** section, find:
```
   Deliver the bundle artifact (`PLANNING-*.txt` or `PLANNING-*.tar`) to the analyst model.
```
   Replace with:
```
   Deliver the bundle artifact (`PLANNING-*.txt` or `PLANNING-*.tar`) to the Analyst.
```

6. **Step 5 — Fix confirmed soft blend in `ops/lib/manifests/BUNDLE.md` (unconditional).**
   In the **Planning Transport Contract** section, find:
```
   `storage/handoff/PLAN.md` is the latest-wins output surface written by the model after each planning run.
```
   Replace with:
```
   `storage/handoff/PLAN.md` is the latest-wins output surface written by the Analyst after each planning run.
```

7. **Step 6 — Verify no unintended changes.** After all edits, run:
```
   git diff
```
   Confirm no `stance_id`, profile key, stance template key, or internal render identifier was altered. STOP if any such change is found and revert it.

### 3.4.5 Receipt (Proofs to collect) - MUST RUN

**Mandatory receipt commands (always run; do not omit):**
- bash tools/lint/dp.sh TASK.md
- bash tools/lint/task.sh
- bash tools/lint/integrity.sh
- bash tools/lint/style.sh
- ./ops/bin/llms
- bash tools/lint/integrity.sh
- git diff --name-only
- git diff --stat
- comm -23 <(git diff --name-only | sort) <(sort storage/dp/active/allowlist.txt) || true
- comm -23 <(git ls-files --others --exclude-standard | sort) <(sort storage/dp/active/allowlist.txt) || true
- ./ops/bin/open

**DP-specific receipt commands (scope-specific; author below):**
Note: Command substitution forms (e.g., $(pwd), $(git ...)) are rejected by certify replay. Use literal values only. Repo-local binaries must be authored as literal `./ops/bin/...` or `ops/bin/...` commands. Unsupported receipt-command forms are rejected by dp.sh and certify.
- bash tools/lint/factory.sh
- bash tools/lint/agent.sh opt/_factory/agents/r-agent-02.md
- bash tools/lint/agent.sh opt/_factory/agents/r-agent-06.md
- grep -n "runtime_role" opt/_factory/agents/r-agent-02.md opt/_factory/agents/r-agent-06.md docs/ops/registry/agents.md
- bash -lc '! grep -n "analyst model\\|drafting model\\|written by the model" docs/MANUAL.md ops/lib/manifests/BUNDLE.md'

## 3.5 Closeout (Mandatory Routing)
- Execute docs/MANUAL.md Closeout Cycle in order (Verify, Harvest, Refresh, Log, Prune).
- Update SoP.md and PoW.md with DP entries, including objective summary and current proof / receipt summary.
- Protocol order for closeout: Verify -> Generate Results -> COMMIT (Operator Only) -> Prune.
- Run prune hygiene: ./ops/bin/prune --scrub.
- Refresh llms artifacts: `./ops/bin/llms`
- Regenerate session artifacts: `./ops/bin/open --out=auto`
- Ensure the next session begins with refreshed session artifacts and matching receipts.
- Note: audit dump generation is owned by `./ops/bin/bundle --profile=audit --out=auto` and is separate from operator session refresh. Do not stamp an additional `ops/bin/dump --scope=core` into closeout unless the DP explicitly authorizes a standalone state capture.
- Refresh side-effect: `ops/bin/llms` regenerates `ops/lib/manifests/OPS.md` as a compile event; if `OPS.md` is not in the allowlist, restore it before running `integrity.sh`. See `docs/MANUAL.md` Refresh side-effect notice for the full procedure.

### 3.5.1 Mandatory Closing Sidecar
Closing sidecar content is generated and maintained at `storage/handoff/CLOSING.md` and is validated by `ops/bin/certify` as a hard gate at certification time. Do not author, predict, populate, or approximate any sidecar-derived closeout output outside this sidecar.
Certify separately collects contractor-authored narrative for the RESULTS Contractor Execution Narrative section at certify time via interactive editor prompt; this narrative input is independent from closing sidecar content.

Before running certify, confirm `storage/handoff/CLOSING.md` has been maintained throughout execution and reflects observable reality only.

Closing sidecar fields required at certify time:
- Commit Message
- Create Pull Request (Title)
- Create Pull Request (Description)
- Confirm Merge (Commit Message)
- Confirm Merge (Extended Description)
- Confirm Merge (Add a Comment)
