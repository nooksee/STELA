---
trace_id: stela-20260226T031149Z-5f691c04
packet_id: DP-OPS-0117
created_at: 2026-02-26T03:16:27Z
previous: (none)
---
# STELA TASK DASHBOARD
Status: ACTIVE
Owner: Integrator
Last Updated: 2026-02-18

## 1. Session State
Pointer: Session context output (generated by ops/bin/open)
Context Manifest: ops/lib/manifests/CONTEXT.md (checked by tools/lint/context.sh)

Session State contract:
- Session State is pointer-first. Do not write inline branch or hash state here.
- OPEN and OPEN-PORCELAIN are the session state snapshot.
- OPEN-PORCELAIN is emitted on every OPEN run. Clean sessions produce a zero-line artifact.
- RESULTS is the audit record for the DP.
- TASK is the stable routing surface.
- Operator note (2026-02-17): Do not edit `SoP.md` or `PoW.md` unless explicitly instructed.

## 2. Logic Pointers
Primary Constraint: PoT.md (Policy of Truth).
Surface Contract Spec: docs/ops/specs/surfaces/task.md.

### 2.1 Governance Pointers
Jurisdiction: PoT.md Section 3.
Git Authority: PoT.md Section 4.1.
Behavioral Standard: PoT.md Section 4.2.
Staffing Protocol: PoT.md Section 4.1.

### 2.2 Enforcement and Gates
Style: tools/lint/style.sh
Context: tools/lint/context.sh
Truth: tools/lint/truth.sh
DP lint: tools/lint/dp.sh (enforces DP transaction rules in Section 3; does not enforce TASK container schema)
TASK lint: tools/lint/task.sh (sole TASK surface/container schema enforcer)
Repo verify: tools/verify.sh
llms parity: tools/lint/llms.sh

## 3. Current Dispatch Packet (DP)

Section-by-section authoring protocol:
- Generate DP structure from `ops/src/surfaces/dp.md.tpl` using `ops/bin/draft`.
- Do not hand-edit DP structural boilerplate in `TASK.md` after generation.
- Manual edits are limited to slot content only (objective, scope, safety, plan body, receipt body).
- Freeze each completed subsection in order (3.1 -> 3.2 -> 3.3 -> 3.4.x).
- Do not revise earlier sections to justify later work. If scope changes, end the thread and draft a new DP.

Freeze Rule:
- If a completed DP subsection changes later, append an AMENDMENT block in RESULTS; otherwise DP is FAIL.

AMENDMENT: minimum fields
- Reopened section:
- Reason:
- Impact:
- Allowlist change (none or exact paths):

Input discipline (hard rule):
- Worker receives only the DP text. Worker does not require OPEN or DUMP attachments.
- DP writer must not attach or cite disposable artifacts (rough notes, chat logs, scratch drafts, throwaway excerpts).
- DP writer must not include pasted bundles (OPEN, DUMP payloads, manifests) inside the DP body.
- Canon pointers only. Evidence belongs in storage artifacts and RESULTS.

### DP-OPS-0117: `CONTRACTOR-NOTES.md` Surface Formalization

## 3.1 Freshness Gate (Must Pass Before Work)
Base Branch: main
Required Work Branch: work/DP-OPS-0117-contractor-notes-surface-2026-02-26
Base HEAD: cca423d7b
Freshness Stamp: 2026-02-26
Note: FRESHNESS_STAMP must be YYYY-MM-DD format only. No trace tokens, no timestamps, no other text. Certify rejects all other forms. dp.sh enforces at lint time.

Required local re-check (worker runs; paste outputs in RESULTS):
- git rev-parse --abbrev-ref HEAD
- git rev-parse --short HEAD
- git status --porcelain

Preconditions:
- No commits on main.
- Working tree must be clean before execution begins.
- If Base HEAD changes, regenerate session artifacts from canonical tools before proceeding.

STOP conditions:
- STOP if any mismatch (branch, Base HEAD, missing work branch).
- STOP if working tree is dirty before execution begins.
- STOP if told to create or switch branches.

## 3.1.1 DP Preflight Gate (Run Before Any Edits)
Purpose:
- Catch malformed DP or TASK schema before work begins.

Worker runs (paste outcome in RESULTS):
- bash tools/lint/dp.sh --test
- bash tools/lint/dp.sh TASK.md
- bash tools/lint/task.sh

STOP if any preflight check fails.

### CbC Design Discipline Preflight (TASK.md §3.1.1)
Required when the DP objective adds, modifies, or replaces a linter, script, guard, or validation binary.
For non-tooling DPs: state "Not applicable" with a one-line justification.
Not applicable. This DP creates a surface template (`ops/src/surfaces/notes.md.tpl`) and updates closeout documentation and manifests; it does not add, modify, or replace any linter, script, guard, or validation binary. The `tools/lint/dp.sh` change in this packet is a SHA256 constant update only — a mechanical hash synchronization required whenever `ops/src/surfaces/dp.md.tpl` changes — and introduces no new check logic, function, or behavioral path.

## 3.2 Required Context Load (Read Before Doing Anything)

### 3.2.1 Canon load order (always)
Worker must confirm loaded before edits begin:
1. PoT.md
2. SoP.md
3. PoW.md
4. TASK.md
5. docs/MAP.md
6. docs/MANUAL.md
7. ops/lib/manifests/CONTEXT.md

Notes:
- Worker does not read OPEN. OPEN is for Integrator state refresh and for receipts.
- Disposable artifacts must not be referenced or included.
- Worker input is the DP text only.
- DP writer must not attach or cite disposable artifacts.
- DP writer must not embed pasted bundles.

<!-- CCD: ff_target="machine-dense" ff_band="10-20" -->
# Contractor Brief

1 Temp path rule uses `var/tmp` only
2 RESULTS generation rule uses `ops/bin/certify` only
3 Receipt rule runs every DP mandated command and treats any skip as STOP
4 Disposable artifact rule treats OPEN dump bundles and manifests as non canonical session artifacts
5 Path rule uses existing repo paths or explicit `NEW` changelog markers and forbids globs and brace expansion

### 3.2.2 DP-scoped load order (per DP)
- `ops/src/surfaces/notes.md.tpl` — does not exist at BASE HEAD; worker confirms absence before creating. No read required.
- `ops/src/manifests/context.md.tpl` — source template for `ops/lib/manifests/CONTEXT.md`; worker reads the full file before editing to identify the last entry in the Members list.
- `ops/lib/manifests/CONTEXT.md` — generated output; worker reads the full file to confirm the current member set before regeneration. Do not manually edit this file.
- `ops/src/surfaces/dp.md.tpl` — DP structure template; worker reads the full file, specifically the `## 3.5 Closeout (Mandatory Routing)` section, to identify the exact insertion point for the CONTRACTOR-NOTES.md production requirement.
- `tools/lint/dp.sh` — worker reads the line containing `CANONICAL_DP_TEMPLATE_SHA256` to record the existing value before update.
- `docs/MANUAL.md` — operator manual; worker reads the full Closeout Cycle section to identify the exact location for the CONTRACTOR-NOTES.md surface documentation, specifically the area following the `2.5 Post-Work Audit` subsection.

## 3.3 Scope and Safety
Objective:
Formalize `CONTRACTOR-NOTES.md` as a defined closing surface by: creating `ops/src/surfaces/notes.md.tpl` with a four-field required schema; registering `storage/dp/active/notes.md` in `ops/src/manifests/context.md.tpl` and regenerating `ops/lib/manifests/CONTEXT.md`; extending `ops/src/surfaces/dp.md.tpl` §3.5 to require CONTRACTOR-NOTES.md production before operator pre-audit review and updating `tools/lint/dp.sh` with the new template SHA256; and documenting authoring and routing expectations in `docs/MANUAL.md`.

In scope:
- `ops/src/surfaces/notes.md.tpl` (NEW) — define the four-field schema: `Scope Confirmation:`, `Anomalies Encountered:`, `Open Items / Residue:`, and `Closing Schema Baseline:`. The `Closing Schema Baseline:` field uses current-schema-only wording (current six-label schema, post-0116+A baseline). The template includes a clarifying note that historical packet references may appear in narrative, but the field describes the active packet's current-schema assumptions unless the packet explicitly tests historical artifacts. Template must include YAML frontmatter with `template_type: surface`, `template_id: notes`, `template_version: 1`.
- `ops/src/manifests/context.md.tpl` — append `- \`storage/dp/active/notes.md\`` to the Members list as the last entry.
- `ops/lib/manifests/CONTEXT.md` — regenerated from `ops/src/manifests/context.md.tpl` by running `./ops/bin/compile` after the template edit. No direct manual edits permitted.
- `ops/src/surfaces/dp.md.tpl` — in `## 3.5 Closeout (Mandatory Routing)`, insert one new bullet after the line `- Protocol order for closeout: Verify -> Generate Results -> COMMIT (Operator Only) -> Prune.` requiring the contractor to produce `storage/dp/active/notes.md` using `ops/src/surfaces/notes.md.tpl` before operator pre-audit review.
- `tools/lint/dp.sh` — update the value of `CANONICAL_DP_TEMPLATE_SHA256` to the SHA256 of the modified `ops/src/surfaces/dp.md.tpl`. This is a single-constant update; no check logic, function, or behavioral path changes.
- `docs/MANUAL.md` — add a `### Contractor Notes Surface` subsection immediately following the `2.5 Post-Work Audit` subsection in the Closeout Cycle section, documenting: where the template lives, the four required fields and their intended content, the `Closing Schema Baseline:` current-schema convention, and that prior CONTRACTOR-NOTES.md files are not retroactively reformatted.
- `storage/dp/active/allowlist.txt` — include all changed persistent repo-state files plus certify-generated ledger and TASK archive surfaces and compile archive wildcard.

Out of scope:
- A linter for `CONTRACTOR-NOTES.md` — explicitly out of scope. This packet is template and documentation only. No new validator, lint gate, or schema enforcement binary for the notes surface is introduced.
- `ops/bin/certify` — no changes. Certify fallback logic is unchanged.
- Prior `CONTRACTOR-NOTES.md` files in `storage/handoff/` — not retroactively reformatted to the new schema.
- Any other section of `tools/lint/dp.sh` beyond the `CANONICAL_DP_TEMPLATE_SHA256` constant update.
- `docs/ops/specs/tools/lint/dp.md` — no changes. The dp.sh change is a hash constant update only and does not alter the check list documented in that spec.
- Any change to `ops/bin/compile` behavior.

Safety and invariants:
- No manual edits to generated outputs. `ops/lib/manifests/CONTEXT.md` carries `<!-- GENERATED FILE. DO NOT EDIT. -->`. Edit only the source template `ops/src/manifests/context.md.tpl`, then regenerate via `./ops/bin/compile`.
- No structural edits to TASK or the DP boilerplate.
- The allowlist hard gate enforces the exact file set; no unlisted file may be modified.
- `ops/src/surfaces/dp.md.tpl` edits are limited to inserting one new bullet in `## 3.5`; no other section of the template may be modified.
- `tools/lint/dp.sh` edits are limited to replacing the `CANONICAL_DP_TEMPLATE_SHA256` string value; no other line in dp.sh may be modified.
- All existing lint gates must continue to pass after edits.

Worker Constraints (SSOT injected):
## Section 1: Universal Template Rules
- Template source uses `tpl` files with optional YAML frontmatter
- Canon frontmatter keys: `template_type` `template_id` `template_version` `requires_slots` `includes`
- Renderer strips frontmatter before output write
- Slot token form `\{\{TOKEN\}\}` with uppercase alphanumeric underscore
- Include forms `\{\{@include:path\}\}` and `\{\{@include:path#section\}\}`
- Include resolution is strict: missing file fail missing section fail circular graph fail
- Strict mode default: every required slot value present and no unresolved token
- Non strict mode allowed only for lint and normalization workflows
- Worker facing generated surfaces remain pointer first and exclude disposable artifacts

Target Files allowlist (hard gate):
- storage/dp/active/allowlist.txt

## 3.4 Execution Plan (A-E)

### 3.4.1 State
Branch `main` is at HEAD `cca423d7b`, the merge commit for DP-OPS-0114 (`ops/bin/llms` Refresh Side-Effect Documentation). Working tree is clean with zero porcelain entries. `TASK.md` is a single-line pointer to `archives/surfaces/TASK-DP-OPS-0114-10e5cc3a2.md`. `ops/src/surfaces/notes.md.tpl` does not exist in the repository. `storage/dp/active/notes.md` is not a member of `ops/src/manifests/context.md.tpl` or the generated `ops/lib/manifests/CONTEXT.md`. `ops/src/surfaces/dp.md.tpl` §3.5 does not reference CONTRACTOR-NOTES.md. `docs/MANUAL.md` does not document `CONTRACTOR-NOTES.md` as a required handoff surface. `CANONICAL_DP_TEMPLATE_SHA256` in `tools/lint/dp.sh` is currently `35c078b9236f5bb18d4dd471f2f7efc36734585f955a938e6992e8cf47588f44`.

### 3.4.2 Request
Create `ops/src/surfaces/notes.md.tpl` with YAML frontmatter (`template_type: surface`, `template_id: notes`, `template_version: 1`) and four labeled fields — `Scope Confirmation:`, `Anomalies Encountered:`, `Open Items / Residue:`, and `Closing Schema Baseline:` — each with one-sentence authoring guidance. The `Closing Schema Baseline:` field guidance must state that the current six-label schema (post-0116+A baseline) is assumed and that an explicit exception is recorded here only when the packet intentionally tests historical artifacts.

Append `- \`storage/dp/active/notes.md\`` to the Members list in `ops/src/manifests/context.md.tpl`, then run `./ops/bin/compile` to regenerate `ops/lib/manifests/CONTEXT.md`.

In `ops/src/surfaces/dp.md.tpl`, in `## 3.5 Closeout (Mandatory Routing)`, insert the following bullet immediately after the line `- Protocol order for closeout: Verify -> Generate Results -> COMMIT (Operator Only) -> Prune.`:
```
- Before handing work for operator pre-audit review, produce `storage/dp/active/notes.md` using `ops/src/surfaces/notes.md.tpl` as the schema. See `docs/MANUAL.md` Contractor Notes Surface for field definitions and routing.
```

Compute `sha256sum ops/src/surfaces/dp.md.tpl` and replace the value of `CANONICAL_DP_TEMPLATE_SHA256` in `tools/lint/dp.sh` with the 64-character hex digest from that output.

In `docs/MANUAL.md`, in the Closeout Cycle section, insert a `### Contractor Notes Surface` subsection immediately after the `2.5 Post-Work Audit (Integrator; mandatory before COMMIT)` subsection.

Update `storage/dp/active/allowlist.txt` to cover all changed files, the certify-generated surfaces, and the compile archive wildcard. Before running `ops/bin/certify`, update the three `archives/surfaces/` entries to the short SHA of the tip commit on the work branch.

### 3.4.3 Changelog
- NEW: `ops/src/surfaces/notes.md.tpl` — four-field CONTRACTOR-NOTES.md schema template.
- UPDATE: `ops/src/manifests/context.md.tpl` — append `storage/dp/active/notes.md` to Members list.
- UPDATE: `ops/lib/manifests/CONTEXT.md` — regenerated from updated source template via `./ops/bin/compile`; no direct manual edits.
- UPDATE: `ops/src/surfaces/dp.md.tpl` — insert CONTRACTOR-NOTES.md production requirement bullet in `## 3.5 Closeout (Mandatory Routing)`.
- UPDATE: `tools/lint/dp.sh` — replace `CANONICAL_DP_TEMPLATE_SHA256` value with SHA256 of modified `ops/src/surfaces/dp.md.tpl`.
- UPDATE: `docs/MANUAL.md` — add `### Contractor Notes Surface` subsection to Closeout Cycle section.
- UPDATE: `storage/dp/active/allowlist.txt` — cover all changed files plus certify-generated surfaces.

### 3.4.4 Patch / Diff

**Step 1.** Confirm `ops/src/surfaces/notes.md.tpl` does not exist:
```
ls ops/src/surfaces/notes.md.tpl
```
Expected: `No such file or directory`. If the file exists, STOP and report to the Integrator.

**Step 2.** Create `ops/src/surfaces/notes.md.tpl` with the following exact content:
```
---
template_type: surface
template_id: notes
template_version: 1
ff_target: operator-technical
ff_band: "25-40"
---
# Contractor Notes — DP-OPS-0117

## Scope Confirmation
State what was executed versus what was scoped. Note any items deliberately not
performed and state the reason for each omission.

## Anomalies Encountered
Describe friction items, unexpected behaviors, and workaround decisions encountered
during execution. State "None." if no anomalies occurred.

## Open Items / Residue
List anything unresolved, non-blocking residue, or audit hazards remaining after
execution. State "None." if all items are resolved.

## Closing Schema Baseline
State which closing schema was assumed for this packet. Default: current six-label
schema (post-0116+A baseline). Historical packet references may appear in narrative
text elsewhere, but this field describes only the active packet's schema assumptions.
Record an explicit exception here only when this packet intentionally tests or touches
historical artifacts or compatibility paths.
```

**Step 3.** Read `ops/src/manifests/context.md.tpl` in full. Confirm the last entry in the Members list is `- \`tools/lint/truth.sh\``.

**Step 4.** In `ops/src/manifests/context.md.tpl`, append the following line as the new last entry in the Members list, immediately after the line `- \`tools/lint/truth.sh\``:
```
- `storage/dp/active/notes.md`
```

**Step 5.** Run `./ops/bin/compile`. Confirm the command exits zero. Confirm `ops/lib/manifests/CONTEXT.md` now contains `storage/dp/active/notes.md` in its Members list. Do not edit `ops/lib/manifests/CONTEXT.md` directly.

**Step 6.** Read `ops/src/surfaces/dp.md.tpl` in full. Confirm the `## 3.5 Closeout (Mandatory Routing)` section contains the line:
```
- Protocol order for closeout: Verify -> Generate Results -> COMMIT (Operator Only) -> Prune.
```

**Step 7.** In `ops/src/surfaces/dp.md.tpl`, in `## 3.5 Closeout (Mandatory Routing)`, insert the following line immediately after the line confirmed in Step 6:
```
- Before handing work for operator pre-audit review, produce `storage/dp/active/notes.md` using `ops/src/surfaces/notes.md.tpl` as the schema. See `docs/MANUAL.md` Contractor Notes Surface for field definitions and routing.
```

**Step 8.** Compute the SHA256 of the modified `ops/src/surfaces/dp.md.tpl`:
```
sha256sum ops/src/surfaces/dp.md.tpl
```
Record the 64-character hex digest (first whitespace-delimited token in the output).

**Step 9.** In `tools/lint/dp.sh`, replace the value of `CANONICAL_DP_TEMPLATE_SHA256` with the digest recorded in Step 8. The replacement targets exactly this line:
```
CANONICAL_DP_TEMPLATE_SHA256="35c078b9236f5bb18d4dd471f2f7efc36734585f955a938e6992e8cf47588f44"
```
After the edit, the line must read:
```
CANONICAL_DP_TEMPLATE_SHA256="<64-character hex digest from Step 8>"
```
No other line in `tools/lint/dp.sh` may be modified.

**Step 10.** Read `docs/MANUAL.md`. Identify the `2.5 Post-Work Audit (Integrator; mandatory before COMMIT)` subsection within the Closeout Cycle section. Confirm its closing line ends with `If scope is clean: proceed to step 3.`

**Step 11.** In `docs/MANUAL.md`, immediately after the paragraph ending with `If scope is clean: proceed to step 3.` and before `3. Harvest`, insert the following subsection:
```markdown
### Contractor Notes Surface
The Contractor creates and populates `storage/dp/active/notes.md` using
`ops/src/surfaces/notes.md.tpl` as the schema before the operator's post-work audit
review (before step 2.5). This file is not Operator-authored and is not deferred to
a later session.

Required fields:
- `Scope Confirmation:` — what was executed versus what was scoped; note any conditional
  skips with reasons.
- `Anomalies Encountered:` — friction items, unexpected behaviors, workaround decisions.
- `Open Items / Residue:` — anything unresolved, non-blocking residue, or audit hazards.
- `Closing Schema Baseline:` — explicit declaration that the current six-label schema was
  assumed (post-0116+A baseline), or an explicit exception note if the packet
  intentionally touches historical artifacts or compatibility paths. Historical packet
  references may appear in narrative text; this field records only the active packet's
  schema assumptions.

Prior `CONTRACTOR-NOTES.md` files in `storage/handoff/` are not retroactively reformatted
to this schema.

Routing: `storage/dp/active/notes.md` is registered as a handoff surface member
in `ops/lib/manifests/CONTEXT.md`.
```

**Step 12.** Update `storage/dp/active/allowlist.txt` to contain exactly the following entries:
```
TASK.md
PoW.md
SoP.md
ops/src/surfaces/notes.md.tpl
ops/src/manifests/context.md.tpl
ops/lib/manifests/CONTEXT.md
ops/src/surfaces/dp.md.tpl
tools/lint/dp.sh
docs/MANUAL.md
storage/dp/active/allowlist.txt
storage/handoff/CLOSING-DP-OPS-0117.md
llms.txt
llms-core.txt
llms-full.txt
archives/manifests/compile-*.md
archives/surfaces/SoP-2026-02-26-cca423d7b.md
archives/surfaces/PoW-2026-02-26-cca423d7b.md
archives/surfaces/TASK-DP-OPS-0117-cca423d7b.md
```

**Step 13.** Before running `ops/bin/certify`, update the three `archives/surfaces/` entries in `storage/dp/active/allowlist.txt` to use the short SHA of the tip commit on the work branch at the time of certification. Run `git rev-parse --short HEAD` on the work branch to obtain the value, then replace `cca423d7b` in the three archive surface paths with that value.

### 3.4.5 Receipt (Proofs to collect) - MUST RUN

**Mandatory receipt commands (always run; do not omit):**
- bash tools/lint/dp.sh TASK.md
- bash tools/lint/task.sh
- bash tools/lint/integrity.sh
- bash tools/lint/style.sh
- git diff --name-only
- git diff --stat
- comm -23 <(git diff --name-only | sort) <(sort storage/dp/active/allowlist.txt) || true
- comm -23 <(git ls-files --others --exclude-standard | sort) <(sort storage/dp/active/allowlist.txt) || true
- ./ops/bin/open

**DP-specific receipt commands (scope-specific; author below):**
Note: Command substitution forms (e.g., $(pwd), $(git ...)) are rejected by certify replay. Use literal values only. dp.sh enforces at lint time.
- bash tools/lint/dp.sh --test
- sha256sum ops/src/surfaces/dp.md.tpl
- grep -n "CANONICAL_DP_TEMPLATE_SHA256" tools/lint/dp.sh
- grep -n "CONTRACTOR-NOTES" ops/src/surfaces/dp.md.tpl
- grep -n "CONTRACTOR-NOTES" ops/src/manifests/context.md.tpl
- grep -n "CONTRACTOR-NOTES" ops/lib/manifests/CONTEXT.md
- grep -n "CONTRACTOR-NOTES" docs/MANUAL.md
- grep -n "notes.md.tpl" docs/MANUAL.md
- wc -l ops/src/surfaces/notes.md.tpl

## 3.5 Closeout (Mandatory Routing)
- Execute docs/MANUAL.md Closeout Cycle in order (Verify, Harvest, Refresh, Log, Prune).
- Update SoP.md and PoW.md with DP entries, including objective summary and verification commands run.
- Protocol order for closeout: Verify -> Generate Results -> COMMIT (Operator Only) -> Prune.
- Before handing work for operator pre-audit review, produce `storage/dp/active/notes.md` using `ops/src/surfaces/notes.md.tpl` as the schema. See `docs/MANUAL.md` Contractor Notes Surface for field definitions and routing.
- Run prune hygiene: ./ops/bin/prune --scrub.
- Regenerate session artifacts: `./ops/bin/open --out=auto`
- Capture updated platform state: `./ops/bin/dump --scope=platform --format=chatgpt --out=auto`
- Ensure the next session begins with refreshed session artifacts and matching receipts.
- Refresh side-effect: `ops/bin/llms` regenerates `ops/lib/manifests/OPS.md` as a compile event; if `OPS.md` is not in the allowlist, restore it before running `integrity.sh`. See `docs/MANUAL.md` Refresh side-effect notice for the full procedure.

### 3.5.1 Mandatory Closing Block
Closing block content is generated exclusively by `ops/bin/certify` from the closing sidecar at `storage/handoff/CLOSING-DP-OPS-0117.md` at certification time. Do not author, predict, populate, or approximate any closing block field at draft time or during execution. The closing sidecar is the only human-authored narrative input certify ingests. Certify generates all of the following from observable repository state: Commit Message, Create Pull Request (Title), Create Pull Request (Description), Confirm Merge (Commit Message), Confirm Merge (Extended Description), and Confirm Merge (Add a Comment).

Before running certify, confirm `storage/handoff/CLOSING-DP-OPS-0117.md` has been maintained throughout execution and reflects observable reality only.

Fields certify generates from the closing sidecar and repository state:
- Commit Message
- Create Pull Request (Title)
- Create Pull Request (Description)
- Confirm Merge (Commit Message)
- Confirm Merge (Extended Description)
- Confirm Merge (Add a Comment)
