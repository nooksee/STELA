---
trace_id: stela-20260324T155212Z-22583069
packet_id: DP-OPS-0227
created_at: 2026-03-24T16:06:20Z
previous: archives/surfaces/TASK-DP-OPS-0226-37e92ff4.md
---
# STELA TASK DASHBOARD
Status: ACTIVE
Owner: Integrator
Last Updated: 2026-03-19

## 1. Session State
Pointer: Session context output (generated by ops/bin/open)
Context Manifest: ops/lib/manifests/CONTEXT.md (checked by tools/lint/context.sh)

Session State contract:
- Session State is pointer-first. Do not write inline branch or hash state here.
- OPEN is the session state snapshot. OPEN-PORCELAIN is conditional dirty-state detail.
- RESULTS is the audit record for the DP.
- TASK is the stable routing surface.

## 2. Logic Pointers
Primary Constraint: PoT.md (Policy of Truth).
Surface Contract Spec: docs/ops/specs/surfaces/task.md.

### 2.1 Governance Pointers
Jurisdiction: PoT.md Section 3.
Git Authority: PoT.md Section 4.1.
Behavioral Standard: PoT.md Section 4.2.
Staffing Protocol: PoT.md Section 4.1.

### 2.2 Enforcement and Gates
Style: tools/lint/style.sh
Context: tools/lint/context.sh
Truth: tools/lint/truth.sh
DP lint: tools/lint/dp.sh (enforces DP transaction rules in Section 3; does not enforce TASK container schema)
TASK lint: tools/lint/task.sh (sole TASK surface/container schema enforcer)
Repo verify: tools/verify.sh
llms parity: tools/lint/llms.sh

## 3. Current Dispatch Packet (DP)

Section-by-section authoring protocol:
- Generate DP structure from `ops/src/surfaces/dp.md.tpl` using `ops/bin/draft`.
- Do not hand-edit DP structural boilerplate in `TASK.md` after generation.
- Manual edits are limited to slot content only (objective, scope, safety, plan body, receipt body).
- Freeze each completed subsection in order (3.1 -> 3.2 -> 3.3 -> 3.4.x).
- Do not revise earlier sections to justify later work. If scope changes, end the thread and draft a new DP.

Freeze Rule:
- If a completed DP subsection changes later, append an AMENDMENT block in RESULTS; otherwise DP is FAIL.

AMENDMENT: minimum fields
- Reopened section:
- Reason:
- Impact:
- Allowlist change (none or exact paths):

Input discipline (hard rule):
- Worker receives only the DP text. Worker does not require OPEN or DUMP attachments.
- DP writer must not attach or cite disposable artifacts (rough notes, chat logs, scratch drafts, throwaway excerpts).
- DP writer must not include pasted bundles (OPEN, DUMP payloads, manifests) inside the DP body.
- Canon pointers only. Evidence belongs in storage artifacts and RESULTS.



### DP-OPS-0227: Consolidate draft lane onto renderer-owned DP generation

## 3.1 Freshness Gate (Must Pass Before Work)
Base Branch: main
Required Work Branch: work/dp-ops-0227-draft-path-consolidation-2026-03-24
Base HEAD: 7d1aed3d
Freshness Stamp: 2026-03-24
Note: FRESHNESS_STAMP must be YYYY-MM-DD format only. No trace tokens, no timestamps, no other text. Certify rejects all other forms. dp.sh enforces at lint time.

Required local re-check (worker runs before any edits; paste verbatim outputs under `## Contractor Execution Narrative` -> `### Preflight State` in RESULTS):
- git rev-parse --abbrev-ref HEAD
- git rev-parse --short HEAD
- git status --porcelain

These three commands are freshness proof only. Do not treat §3.4.5 receipt replay as a substitute for their pre-edit outputs.

Preconditions:
- No commits on main.
- Working tree must be clean before execution begins.
- If Base HEAD changes, regenerate session artifacts from canonical tools before proceeding.

STOP conditions:
- STOP if any mismatch (branch, Base HEAD, missing work branch).
- STOP if working tree is dirty before execution begins.
- STOP if told to create or switch branches.

## 3.1.1 DP Preflight Gate (Run Before Any Edits)
Purpose:
- Catch malformed DP or TASK schema before work begins.

Worker runs (paste outcome in RESULTS):
- bash tools/lint/dp.sh --test
- bash tools/lint/dp.sh TASK.md
- bash tools/lint/task.sh

STOP if any preflight check fails.

### CbC Design Discipline Preflight (TASK.md §3.1.1)
Not applicable. This DP modifies stance templates, spec prose, and operator workflow documentation only. No linter, script, guard, or validation binary is added, modified, or replaced.

## 3.2 Required Context Load (Read Before Doing Anything)

### 3.2.1 Canon load order (always)
Worker must confirm loaded before edits begin:
1. PoT.md
2. SoP.md
3. PoW.md
4. TASK.md
5. docs/MAP.md
6. docs/MANUAL.md
7. ops/lib/manifests/CONTEXT.md

Notes:
- Worker does not read OPEN. OPEN is for Integrator state refresh and for receipts.
- Disposable artifacts must not be referenced or included.
- Worker input is the DP text only.
- DP writer must not attach or cite disposable artifacts.
- DP writer must not embed pasted bundles.

## Rules

1 Temp path rule uses `var/tmp` only
2 RESULTS generation rule uses `ops/bin/certify` only
3 Receipt rule runs every DP mandated command and treats any skip as STOP
4 Disposable artifact rule treats OPEN dump bundles and manifests as non canonical session artifacts
5 Use repo-relative paths only in all output. Never print absolute filesystem paths. Do not emit clickable file links.

### 3.2.2 DP-scoped load order (per DP)
- `ops/src/stances/draft.md.tpl`
- `docs/ops/specs/stances/draft.md`
- `docs/MANUAL.md` §Draft Workflow and §Pointer-First TASK Diagnostics
- `ops/lib/manifests/BUNDLE.md` §Draft Transport Contract and §Shipping Spine Contract
- `ops/src/surfaces/dp.md.tpl`
- `docs/ops/specs/binaries/draft.md`
- `tools/lint/dp.sh` (header constants only — template hash, required slot list)
- `ops/bin/draft` (scaffold and validate flag signatures only)

## 3.3 Scope and Safety
Objective:
Remove the prose full-body DP authoring contract from the draft stance and its coupled spec. Standardize the draft lane on slot-scaffold output validated and rendered through `ops/bin/draft` / `ops/bin/template`. Add a pre-dispatch `tools/lint/dp.sh` gate to the operator draft workflow in MANUAL.md. Fix confirmed actor/lane vocabulary drift in MANUAL.md and BUNDLE.md.

In scope:
- `ops/src/stances/draft.md.tpl`: replace the CONSTRUCT/CONTENT REQUIREMENTS/CONSTRAINTS freehand-authoring instruction block with slot-scaffold authoring instructions; Architect emits a populated slots scaffold, not a fenced DP body; operator runs `ops/bin/draft` to render and validates with `tools/lint/dp.sh`.
- `docs/ops/specs/stances/draft.md`: update Outputs, Invariants, and Shipping Spine Position sections to match the new slot-scaffold contract; remove the "Architect does not STOP for missing section heading labels" invariant that applies only to the old prose path; add the intake lint gate contract.
- `docs/MANUAL.md` §Draft Workflow: add step to run `bash tools/lint/dp.sh storage/dp/intake/DP.md` after `ops/bin/draft` and before dispatch; update step 3/4 narration to reflect slot-scaffold handoff. Fix two confirmed actor/lane prose loci: (1) "drafting model" → "Architect" where the actor is the Architect role; (2) "analyst/draft authoring" → correct actor-bearing phrase.
- `ops/lib/manifests/BUNDLE.md` §Draft Transport Contract: correct "Architect model output is a fenced DP draft block" → slot scaffold output rendered by operator via `ops/bin/draft`; fix "planning model; planning writes PLAN.md" actor/lane blend.
- `storage/dp/active/allowlist.txt`: add all modified files.

Out of scope:
- `ops/src/surfaces/dp.md.tpl`: template structure is renderer-owned and correct; no edits.
- `ops/bin/draft`, `ops/bin/template`: no binary changes; scaffold and validate paths already exist.
- `tools/lint/dp.sh`: no linter changes.
- Role-task decoupling items 2g–2i (Foreman→Addenda, Conformist removal, Contractor removal): separate DPs per plan ordering.
- Stance-family normalization (Item 3) and factory extraction (Item 4): downstream of this packet.
- H1 llms hook inline: remains next after this packet per plan ordering.
- Any change to `TOPIC.md`, `PLAN.md`, or `storage/dp/intake/DP.md` semantics.
- Any multi-version plan retention or multi-slice preservation beyond current `var/tmp/PLAN.md.prev` disposable backup.

Safety and invariants:
- No manually generated DP body output. Architect emits a slots scaffold only; `ops/bin/draft` owns rendering.
- No structural edits to TASK.md or DP surfaces.
- All changed files must appear in `storage/dp/active/allowlist.txt` before edits begin.
- Allowlist hard gate: `comm -23 <(git diff --name-only | sort) <(sort storage/dp/active/allowlist.txt) || true` must show no out-of-allowlist diffs at receipt time.
- Stance template slot-for-slot parity: the new stance must not remove any slot from the dp.md.tpl contract; it must instruct the Architect to populate all required slots in the scaffold.
- No changes to certify, bundle, audit, or foreman lane semantics.
- No edits to disposable or cold-archive artifacts.

Worker Constraints (SSOT injected):
## Section 1: Universal Template Rules
- Template source uses `tpl` files with optional YAML frontmatter
- Canon frontmatter keys: `template_type` `template_id` `template_version` `requires_slots` `includes`
- Renderer strips frontmatter before output write
- Slot token form `\{\{TOKEN\}\}` with uppercase alphanumeric underscore
- Include forms `\{\{@include:path\}\}` and `\{\{@include:path#section\}\}`
- Include resolution is strict: missing file fail missing section fail circular graph fail
- Strict mode default: every required slot value present and no unresolved token
- Non strict mode allowed only for lint and normalization workflows
- Worker facing generated surfaces remain pointer first and exclude disposable artifacts

Target Files allowlist (hard gate):
- storage/dp/active/allowlist.txt

## 3.4 Execution Plan (A-E)

### 3.4.1 State
- Base branch: main at 7d1aed3d (clean working tree, 0 porcelain entries)
- Active packet: DP-OPS-0227 (this packet; intake surface is `storage/dp/intake/DP.md`)
- Last certified packet: DP-OPS-0226 (Auditor→Audit rename; follow-up role/lane cleanup required)
- Draft stance template: `ops/src/stances/draft.md.tpl` — currently instructs Architect to freehand the full DP body using CONSTRUCT/CONTENT REQUIREMENTS/CONSTRAINTS block; include directives are embedded as literal text in the stance output, causing machine-generated DPs to carry stale template-owned include expansions
- Canonical render path exists: `ops/bin/draft --emit-dp-slots-scaffold` / `--validate-dp-slots-scaffold` and `ops/bin/template render dp` are implemented and passing
- `tools/lint/dp.sh` validates rendered DPs but is not yet in the operator draft workflow as a mandatory pre-dispatch step
- Confirmed actor/lane drift: MANUAL.md "drafting model" (2 instances) and "analyst/draft authoring"; BUNDLE.md Draft Transport Contract "Architect model output is a fenced DP draft block"; `docs/ops/specs/stances/draft.md` "written by the planning model"

### 3.4.2 Request
Consolidate the draft lane onto the renderer-owned DP generation path that already exists in the repo. The Architect's job in the draft lane becomes: read PLAN.md, populate a DP slots scaffold with packet-specific content for all required slots, and output the scaffold. The operator then runs `ops/bin/draft` to render the DP, and runs `bash tools/lint/dp.sh storage/dp/intake/DP.md` to confirm structural correctness before dispatch. The stance must not instruct the Architect to populate or resolve include directives; those are renderer-owned.

Required work:

1. **`ops/src/stances/draft.md.tpl`** — Replace the `CONSTRUCT` / `CONTENT REQUIREMENTS` / `CONSTRAINTS` freehand DP-body authoring block with a slot-scaffold authoring contract. New contract: Architect reads PLAN.md, uses Summary/Key Changes/Test Plan/Assumptions to populate a DP slots scaffold covering all required DP slots from `ops/src/surfaces/dp.md.tpl`, including the Required Work Branch field. Output contract changes from "exactly one fenced markdown code block starting with `### DP-`" to "exactly one fenced markdown code block containing the populated slots scaffold in `ops/bin/draft --emit-dp-slots-scaffold` format." Retain all precondition, plan-scope, and bundle-manifest validation rules. Retain hard truth and continuity rules via shared includes.

2. **`docs/ops/specs/stances/draft.md`** — Update Outputs section: remove "First non-empty line inside the fenced body must start with `### DP-`"; replace with slots scaffold output contract; add "Architect output is validated by `bash tools/lint/dp.sh storage/dp/intake/DP.md` after operator renders via `ops/bin/draft`." Update Invariants: remove "Architect does not STOP for missing section heading labels when plan intent and authority are visible" (this invariant guarded against stopping on the old prose path; the slot path does not need it). Update Shipping Spine Position: replace "Architect output is a fenced DP draft block; the operator saves it to the `dp_draft_path`" with the new two-step: Architect emits slots scaffold → operator runs `ops/bin/draft` to render → operator validates with `tools/lint/dp.sh` → dispatch. Fix actor/lane: "written by the planning model" → "written by the Analyst".

3. **`docs/MANUAL.md`** §Draft Workflow — Update steps 3–5: step 3 becomes "Deliver bundle to Architect; Architect populates and returns a DP slots scaffold"; step 4 becomes "Run `ops/bin/draft` to render the DP from the scaffold"; add new step 5 "Run `bash tools/lint/dp.sh storage/dp/intake/DP.md` and confirm PASS before dispatch"; renumber subsequent steps. Fix two actor/lane prose loci confirmed in plan: "drafting model" → "Architect" (2 instances), "analyst/draft authoring" → "Analyst/Architect draft authoring".

4. **`ops/lib/manifests/BUNDLE.md`** §Draft Transport Contract — Change "`storage/dp/intake/DP.md` is the deterministic active DP draft surface; Architect model output is a fenced DP draft block that the operator saves to this path for dispatch" → "`storage/dp/intake/DP.md` is the deterministic active DP draft surface; Architect emits a DP slots scaffold; operator renders to this path via `ops/bin/draft` and validates with `tools/lint/dp.sh` before dispatch." Fix §Shipping Spine Contract: "deliver context + PLAN.md to Architect model; Architect emits fenced DP draft block; operator saves to `storage/dp/intake/DP.md`" → "deliver context + PLAN.md to Architect; Architect emits populated slots scaffold; operator renders via `ops/bin/draft`, validates with `tools/lint/dp.sh`, and saves to `storage/dp/intake/DP.md`." Fix actor/lane blend "planning model; planning writes PLAN.md" → "Analyst; Analyst writes PLAN.md."

5. **`storage/dp/active/allowlist.txt`** — Add all four modified files: `ops/src/stances/draft.md.tpl`, `docs/ops/specs/stances/draft.md`, `docs/MANUAL.md`, `ops/lib/manifests/BUNDLE.md`.

### 3.4.3 Changelog
- UPDATE: `storage/dp/active/allowlist.txt` — add four target file paths
- UPDATE: `ops/src/stances/draft.md.tpl` — replace freehand DP-body authoring block with slot-scaffold output contract
- UPDATE: `docs/ops/specs/stances/draft.md` — update Outputs, Invariants, and Shipping Spine Position; fix actor/lane drift
- UPDATE: `docs/MANUAL.md` — update Draft Workflow steps 3–5; add dp.sh pre-dispatch gate; fix two actor/lane prose loci
- UPDATE: `ops/lib/manifests/BUNDLE.md` — update Draft Transport Contract and Shipping Spine Contract; fix actor/lane blend

### 3.4.4 Patch / Diff

Step 1 — Confirm allowlist gate before any edits:
- Read `storage/dp/active/allowlist.txt`
- Confirm `ops/src/stances/draft.md.tpl`, `docs/ops/specs/stances/draft.md`, `docs/MANUAL.md`, `ops/lib/manifests/BUNDLE.md` are present; if any are absent, add them to `storage/dp/active/allowlist.txt` first and STOP to confirm before continuing

Step 2 — Edit `ops/src/stances/draft.md.tpl`:
- Retain YAML frontmatter and shared include directives for `stance_shared_rules`, `stance_hard_truth_rules`, `stance_output_guidance_rules`, `stance_continuity_rules`
- Retain the plan-scope and bundle-manifest validation rules (resolved_profile=draft check, PLAN.md as governing scope, Summary/Key Changes/Test Plan/Assumptions orientation)
- Replace the entire `Steps:` block starting at `2. **CONSTRUCT** DP using canonical structure` through the end of `4. **CONSTRAINTS**` with a new slot-scaffold authoring contract:
  - Step 2: **POPULATE SLOTS SCAFFOLD** — emit a populated `ops/bin/draft` DP slots scaffold covering all required DP slots from `ops/src/surfaces/dp.md.tpl`, including the Required Work Branch field. Slot content must be packet-specific prose derived from the settled plan. Do not populate include directives; those are renderer-owned. Do not emit the full rendered DP body.
  - Retain constraints: stay in plan scope, no pattern-paths, exact file paths from dump or marked NEW, bare work branch name only.
- Update `Output only:` contract: remove "Full DP (starting at `### DP-...`) in one markdown code block." Replace with "Populated DP slots scaffold in one fenced markdown code block. Scaffold must contain all required slot sections with packet-specific content. Operator renders via `ops/bin/draft` and validates with `bash tools/lint/dp.sh storage/dp/intake/DP.md` before dispatch."
- Retain the `single_fence_contract_rules` include for single-fence/no-extra-text behavior. Remove only the explicit `First non-empty line inside the code block must start with \`### DP-\`.` rule and replace it with a scaffold-first-line rule appropriate to the emitted slots scaffold.
- Retain `non_audit_role_drift_rules` include

Step 3 — Edit `docs/ops/specs/stances/draft.md`:
- In **Outputs** section: remove line "First non-empty line inside the fenced body must start with `### DP-`." Replace "Architect reads the final plan body directly and uses `Summary`, `Key Changes`, `Test Plan`, and `Assumptions` when present to build the DP." with "Architect reads the final plan body directly and uses `Summary`, `Key Changes`, `Test Plan`, and `Assumptions` when present to populate the DP slots scaffold." Replace "Architect ingress lint delegates fenced DP bodies to `tools/lint/dp.sh`." with "Operator renders the slots scaffold to `storage/dp/intake/DP.md` via `ops/bin/draft` and validates the rendered DP with `bash tools/lint/dp.sh storage/dp/intake/DP.md` before dispatch." Change "Output contract requires exactly one fenced markdown code block." to "Output contract requires exactly one fenced markdown code block containing the populated slots scaffold." Remove the "Active DP draft surface: `storage/dp/intake/DP.md` is the latest-wins output target; operator saves the fenced DP draft block output there for dispatch." line and replace with "Active DP draft surface: `storage/dp/intake/DP.md` is rendered by the operator via `ops/bin/draft` from the scaffold, then validated with `tools/lint/dp.sh` before dispatch."
- In **Invariants and failure modes** section: remove "Architect does not STOP for missing section heading labels when plan intent and authority are visible." (applies to freehand prose path only; slot path is validated by scaffold structure, not heading labels)
- In **Shipping Spine Position**: replace "Architect output is a fenced DP draft block; the operator saves it to the `dp_draft_path` printed in bundle `[REQUEST]`." with "Architect output is a populated DP slots scaffold; the operator runs `ops/bin/draft` to render the DP to `storage/dp/intake/DP.md`, validates with `bash tools/lint/dp.sh storage/dp/intake/DP.md`, and dispatches the passing packet." Fix actor/lane: replace "written by the planning model" (if present in this file) with "written by the Analyst."

Step 4 — Edit `docs/MANUAL.md` §Draft Workflow:
- Locate the Draft Workflow canonical operator flow block (steps 1–5 under "Canonical operator flow for a draft session:")
- Current steps 3–5 read approximately: (3) "Deliver the bundle artifact to the drafting model." (4) "Save the fenced DP draft from the model output to the active intake path…" (5) "Dispatch the DP per Section 2 Dispatch Packet Mechanics."
- Replace with:
  - Step 3: "Deliver the bundle artifact (`DRAFT-*.txt` or `DRAFT-*.tar`) to the Architect."
  - Step 4: "Save the populated DP slots scaffold from the Architect output to `var/tmp/dp-slots-scaffold.md`."
  - Step 5: "Render the DP: `./ops/bin/draft --id=DP-OPS-XXXX --title=\"...\" --work-branch=work/... --base-head=<hash> --slots-file=var/tmp/dp-slots-scaffold.md`"
  - Step 6 (new): "Validate the rendered intake: `bash tools/lint/dp.sh storage/dp/intake/DP.md` — confirm PASS before dispatch."
  - Step 7: "Dispatch the DP per Section 2 Dispatch Packet Mechanics."
- Fix "drafting model" → "Architect" at both confirmed loci in MANUAL.md
- Fix "analyst/draft authoring" → "Analyst/Architect draft authoring"

Step 5 — Edit `ops/lib/manifests/BUNDLE.md` §Draft Transport Contract:
- Replace: "`storage/dp/intake/DP.md` is the deterministic active DP draft surface; Architect model output is a fenced DP draft block that the operator saves to this path for dispatch."
  With: "`storage/dp/intake/DP.md` is the deterministic active DP draft surface; Architect emits a populated DP slots scaffold; operator renders to this path via `ops/bin/draft` and validates with `bash tools/lint/dp.sh storage/dp/intake/DP.md` before dispatch."
- In §Shipping Spine Contract, replace: "deliver context + PLAN.md to Architect model; Architect emits fenced DP draft block; operator saves to `storage/dp/intake/DP.md` while packet identity remains `DP-OPS-XXXX`"
  With: "deliver context + PLAN.md to Architect; Architect emits populated DP slots scaffold; operator renders via `ops/bin/draft`, validates with `bash tools/lint/dp.sh storage/dp/intake/DP.md`, and saves to `storage/dp/intake/DP.md` while packet identity remains `DP-OPS-XXXX`"
- Fix actor/lane blend: replace "planning model; planning writes PLAN.md" with "Analyst; Analyst writes PLAN.md"

Step 6 — Run preflight and receipt commands per §3.4.5.

### 3.4.5 Receipt (Proofs to collect) - MUST RUN

**Mandatory receipt commands (always run; do not omit):**
- bash tools/lint/dp.sh TASK.md
- bash tools/lint/task.sh
- bash tools/lint/integrity.sh
- bash tools/lint/style.sh
- ./ops/bin/llms
- bash tools/lint/integrity.sh
- git diff --name-only
- git diff --stat
- comm -23 <(git diff --name-only | sort) <(sort storage/dp/active/allowlist.txt) || true
- comm -23 <(git ls-files --others --exclude-standard | sort) <(sort storage/dp/active/allowlist.txt) || true
- ./ops/bin/open

**DP-specific receipt commands (scope-specific; author below):**
Note: Command substitution forms (e.g., $(pwd), $(git ...)) are rejected by certify replay. Use literal values only. Repo-local binaries must be authored as literal `./ops/bin/...` or `ops/bin/...` commands. Unsupported receipt-command forms are rejected by dp.sh and certify.
- bash tools/lint/dp.sh storage/dp/intake/DP.md
- bash tools/lint/style.sh ops/src/stances/draft.md.tpl
- bash tools/lint/style.sh docs/ops/specs/stances/draft.md
- bash tools/lint/style.sh docs/MANUAL.md
- bash tools/lint/style.sh ops/lib/manifests/BUNDLE.md
- bash tools/test/bundle.sh

## 3.5 Closeout (Mandatory Routing)
- Execute docs/MANUAL.md Closeout Cycle in order (Verify, Harvest, Refresh, Log, Prune).
- Update SoP.md and PoW.md with DP entries, including objective summary and current proof / receipt summary.
- Protocol order for closeout: Verify -> Generate Results -> COMMIT (Operator Only) -> Prune.
- Run prune hygiene: ./ops/bin/prune --scrub.
- Refresh llms artifacts: `./ops/bin/llms`
- Regenerate session artifacts: `./ops/bin/open --out=auto`
- Ensure the next session begins with refreshed session artifacts and matching receipts.
- Note: audit dump generation is owned by `./ops/bin/bundle --profile=audit --out=auto` and is separate from operator session refresh. Do not stamp an additional `ops/bin/dump --scope=core` into closeout unless the DP explicitly authorizes a standalone state capture.
- Refresh side-effect: `ops/bin/llms` regenerates `ops/lib/manifests/OPS.md` as a compile event; if `OPS.md` is not in the allowlist, restore it before running `integrity.sh`. See `docs/MANUAL.md` Refresh side-effect notice for the full procedure.

### 3.5.1 Mandatory Closing Sidecar
Closing sidecar content is generated and maintained at `storage/handoff/CLOSING.md` and is validated by `ops/bin/certify` as a hard gate at certification time. Do not author, predict, populate, or approximate any sidecar-derived closeout output outside this sidecar.
Certify separately collects contractor-authored narrative for the RESULTS Contractor Execution Narrative section at certify time via interactive editor prompt; this narrative input is independent from closing sidecar content.

Before running certify, confirm `storage/handoff/CLOSING.md` has been maintained throughout execution and reflects observable reality only.

Closing sidecar fields required at certify time:
- Commit Message
- Create Pull Request (Title)
- Create Pull Request (Description)
- Confirm Merge (Commit Message)
- Confirm Merge (Extended Description)
- Confirm Merge (Add a Comment)
