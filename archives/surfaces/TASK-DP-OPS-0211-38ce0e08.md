---
trace_id: stela-20260318T204304Z-3bee3ead
packet_id: DP-OPS-0211
created_at: 2026-03-18T21:17:42Z
previous: archives/surfaces/TASK-DP-OPS-0210-2ba3eb5f.md
---
# STELA TASK DASHBOARD
Status: ACTIVE
Owner: Integrator
Last Updated: 2026-03-18

## 1. Session State
Pointer: Session context output (generated by ops/bin/open)
Context Manifest: ops/lib/manifests/CONTEXT.md (checked by tools/lint/context.sh)

Session State contract:
- Session State is pointer-first. Do not write inline branch or hash state here.
- OPEN is the session state snapshot. OPEN-PORCELAIN is conditional dirty-state detail.
- RESULTS is the audit record for the DP.
- TASK is the stable routing surface.

## 2. Logic Pointers
Primary Constraint: PoT.md (Policy of Truth).
Surface Contract Spec: docs/ops/specs/surfaces/task.md.

### 2.1 Governance Pointers
Jurisdiction: PoT.md Section 3.
Git Authority: PoT.md Section 4.1.
Behavioral Standard: PoT.md Section 4.2.
Staffing Protocol: PoT.md Section 4.1.

### 2.2 Enforcement and Gates
Style: tools/lint/style.sh
Context: tools/lint/context.sh
Truth: tools/lint/truth.sh
DP lint: tools/lint/dp.sh (enforces DP transaction rules in Section 3; does not enforce TASK container schema)
TASK lint: tools/lint/task.sh (sole TASK surface/container schema enforcer)
Repo verify: tools/verify.sh
llms parity: tools/lint/llms.sh

## 3. Current Dispatch Packet (DP)

Section-by-section authoring protocol:
- Generate DP structure from `ops/src/surfaces/dp.md.tpl` using `ops/bin/draft`.
- Do not hand-edit DP structural boilerplate in `TASK.md` after generation.
- Manual edits are limited to slot content only (objective, scope, safety, plan body, receipt body).
- Freeze each completed subsection in order (3.1 -> 3.2 -> 3.3 -> 3.4.x).
- Do not revise earlier sections to justify later work. If scope changes, end the thread and draft a new DP.

Freeze Rule:
- If a completed DP subsection changes later, append an AMENDMENT block in RESULTS; otherwise DP is FAIL.

AMENDMENT: minimum fields
- Reopened section:
- Reason:
- Impact:
- Allowlist change (none or exact paths):

Input discipline (hard rule):
- Worker receives only the DP text. Worker does not require OPEN or DUMP attachments.
- DP writer must not attach or cite disposable artifacts (rough notes, chat logs, scratch drafts, throwaway excerpts).
- DP writer must not include pasted bundles (OPEN, DUMP payloads, manifests) inside the DP body.
- Canon pointers only. Evidence belongs in storage artifacts and RESULTS.

### DP-OPS-0211: Latest-wins results closing addendum surfaces

## 3.1 Freshness Gate (Must Pass Before Work)
Base Branch: main
Required Work Branch: work/dp-ops-0211-latest-wins-results-closing-addendum-surfaces-2026-03-18
Base HEAD: 38ce0e08
Freshness Stamp: 2026-03-18
Note: FRESHNESS_STAMP must be YYYY-MM-DD format only. No trace tokens, no timestamps, no other text. Certify rejects all other forms. dp.sh enforces at lint time.

Required local re-check (worker runs; paste outputs in RESULTS):
- git rev-parse --abbrev-ref HEAD
- git rev-parse --short HEAD
- git status --porcelain

Preconditions:
- No commits on main.
- Working tree must be clean before execution begins.
- If Base HEAD changes, regenerate session artifacts from canonical tools before proceeding.

STOP conditions:
- STOP if any mismatch (branch, Base HEAD, missing work branch).
- STOP if working tree is dirty before execution begins.
- STOP if told to create or switch branches.

## 3.1.1 DP Preflight Gate (Run Before Any Edits)
Purpose:
- Catch malformed DP or TASK schema before work begins.

Worker runs (paste outcome in RESULTS):
- bash tools/lint/dp.sh --test
- bash tools/lint/dp.sh TASK.md
- bash tools/lint/task.sh

STOP if any preflight check fails.

### CbC Design Discipline Preflight (TASK.md §3.1.1)
Required when the DP objective adds, modifies, or replaces a linter, script, guard, or validation binary.
For non-tooling DPs: state "Not applicable" with a one-line justification.
Applicable. This packet changes certify, addendum, prune, bundle, persistence, retention, lint, and operator-facing surface contracts for current receipt and addendum routing.

Invariants:
1. Operator-facing latest-wins surfaces may be simple-name contracts, but packet truth and addendum lineage remain explicit as `DP-OPS-XXXX` and `DP-OPS-XXXX-ADDENDUM-A` where audit, telemetry, processed storage, and lineage require them.
2. `RESULTS.md`, `CLOSING.md`, and `ADDENDUM.md` must not erase current-packet identity, addendum letter identity, or audit bundle coherence.
3. Audit transport remains identity-bearing and current-packet-scoped even if operator-facing active surfaces become latest-wins.
4. Addendum authority remains operator/foreman controlled; this packet may simplify active surfaces but must not weaken addendum authorization boundaries.
5. Lint, certify, bundle, prune, persistence, retention, and operator docs must agree on the same current-surface versus identity-surface map.
6. `PLAN.md` remains latest-wins and today only has a disposable `var/tmp/PLAN.md.prev` backup; this packet must not imply stronger multi-slice retention than the repo actually provides.

## 3.2 Required Context Load (Read Before Doing Anything)

### 3.2.1 Canon load order (always)
Worker must confirm loaded before edits begin:
1. PoT.md
2. SoP.md
3. PoW.md
4. TASK.md
5. docs/MAP.md
6. docs/MANUAL.md
7. ops/lib/manifests/CONTEXT.md

Notes:
- Worker does not read OPEN. OPEN is for Integrator state refresh and for receipts.
- Disposable artifacts must not be referenced or included.
- Worker input is the DP text only.
- DP writer must not attach or cite disposable artifacts.
- DP writer must not embed pasted bundles.

## Rules

1 Temp path rule uses `var/tmp` only
2 RESULTS generation rule uses `ops/bin/certify` only
3 Receipt rule runs every DP mandated command and treats any skip as STOP
4 Disposable artifact rule treats OPEN dump bundles and manifests as non canonical session artifacts
5 Use repo-relative paths only in all output. Never print absolute filesystem paths. Do not emit clickable file links.

### 3.2.2 DP-scoped load order (per DP)
- storage/dp/processed/DP-OPS-0210.md
- docs/MANUAL.md
- docs/MAP.md
- ops/bin/help
- ops/bin/open
- ops/bin/certify
- ops/bin/prune
- ops/bin/draft
- ops/bin/addendum
- ops/lib/scripts/bundle.sh
- ops/lib/scripts/ledger.sh
- ops/lib/manifests/BUNDLE.md
- ops/etc/persistence.manifest
- ops/etc/retention.manifest
- docs/ops/specs/binaries/bundle.md
- docs/ops/specs/binaries/certify.md
- docs/ops/specs/binaries/prune.md
- docs/ops/specs/binaries/draft.md
- docs/ops/specs/binaries/addendum.md
- docs/ops/specs/surfaces/results.md
- docs/ops/specs/surfaces/task.md
- docs/ops/specs/surfaces/pow.md
- docs/ops/specs/tools/lint/results.md
- docs/ops/specs/tools/lint/dp.md
- docs/ops/specs/tools/lint/style.md
- docs/ops/specs/tools/verify.md
- docs/ops/specs/tools/test/bundle.md
- docs/ops/specs/scripts/ledger.md
- ops/src/surfaces/results.md.tpl
- ops/src/surfaces/task.md.tpl
- ops/src/surfaces/closing.md.tpl
- ops/src/stances/auditor.md.tpl
- tools/lint/results.sh
- tools/lint/dp.sh
- tools/lint/style.sh
- tools/lint/integrity.sh
- tools/lint/response.sh
- tools/test/bundle.sh
- tools/verify.sh
- storage/dp/active/allowlist.txt

## 3.3 Scope and Safety
Objective:
- Finish the latest-wins operator surface corridor by introducing active simple-name surfaces for `RESULTS.md`, `CLOSING.md`, and `ADDENDUM.md` while keeping current packet identity, addendum letter identity, audit bundle coherence, and processed-storage lineage explicit. Leave the repo with one explainable map for active receipt surfaces versus identity-bearing emitted artifacts.

In scope:
- UPDATE docs/MANUAL.md, docs/MAP.md, ops/bin/help, and ops/bin/open: define `RESULTS.md`, `CLOSING.md`, and `ADDENDUM.md` as operator-facing active surfaces if the runtime lands them, and keep packet/addendum identity separate where truth matters.
- UPDATE ops/bin/certify: define and enforce the active `RESULTS.md` and `CLOSING.md` surfaces alongside current-packet identity handling, addendum routing, and audit-facing receipt consistency.
- UPDATE ops/bin/addendum and ops/bin/draft where required: define an active latest-wins addendum surface and preserve packet-scoped addendum identity and processed continuity.
- UPDATE ops/bin/prune, ops/lib/scripts/ledger.sh, ops/etc/persistence.manifest, and ops/etc/retention.manifest: reflect the active latest-wins receipt surfaces without breaking packet-scoped retention or prune safety.
- UPDATE ops/lib/scripts/bundle.sh and ops/lib/manifests/BUNDLE.md: keep audit bundle intake coherent when active latest-wins surfaces exist, while preserving current-packet receipt identity in audit transport.
- UPDATE tools/lint/results.sh, tools/lint/dp.sh, tools/lint/style.sh, and tools/lint/integrity.sh: keep results and closing validation coherent under the new active-surface map.
- UPDATE tools/lint/results.sh, tools/lint/dp.sh, tools/lint/style.sh, tools/lint/integrity.sh, tools/lint/response.sh, and tools/verify.sh where required: keep current-surface validation, tracked-intake guards, and architect-response fixtures coherent under the new active-surface map.
- UPDATE tools/test/bundle.sh: prove closeout and audit coherence under the latest-wins receipt model and active addendum surface.
- UPDATE docs/ops/specs/binaries/bundle.md, docs/ops/specs/binaries/certify.md, docs/ops/specs/binaries/prune.md, docs/ops/specs/binaries/draft.md, docs/ops/specs/binaries/addendum.md, docs/ops/specs/surfaces/results.md, docs/ops/specs/surfaces/task.md, docs/ops/specs/surfaces/pow.md, docs/ops/specs/tools/lint/results.md, docs/ops/specs/tools/lint/dp.md, docs/ops/specs/tools/lint/style.md, docs/ops/specs/tools/verify.md, and docs/ops/specs/scripts/ledger.md to describe the same active-surface contract.
- UPDATE storage/dp/active/allowlist.txt for all tracked files touched by this packet. Active storage surfaces remain disposable and do not gain allowlist entries just because they are canonical.

Out of scope:
- Reworking audit bundle family naming (`AUDIT-*`, `AUDIT-R*`).
- Reworking `OPEN` / `OPEN-PORCELAIN` again.
- Changing `TOPIC.md`, `PLAN.md`, or `DP.md` semantics already locked by DP-OPS-0210.
- Introducing multi-version plan retention or multi-slice preservation beyond the current `var/tmp/PLAN.md.prev` disposable backup.
- Redesigning addendum authority or execution-decision placement.
- Factory migration work.

Safety and invariants:
- Active latest-wins receipt surfaces must remain exact-file contracts only.
- Current packet identity remains explicit in generated content, audit package members, telemetry, processed storage, and lineage-sensitive validations.
- `RESULTS.md` and `CLOSING.md` must not create ambiguity about which packet they represent at any given time.
- `ADDENDUM.md` must not create ambiguity about which base DP and addendum letter it represents at any given time.
- Audit bundle intake remains current-packet-coherent and fail-closed.
- Prune and ledger pointer checks remain safety-first and must not allow active receipt ambiguity.
- `PLAN.md` is still latest-wins and currently backed up only to `var/tmp/PLAN.md.prev`; no stronger retention or multi-slice safeguard may be implied without implementing it.

Worker Constraints (SSOT injected):
## Section 1: Universal Template Rules
- Template source uses `tpl` files with optional YAML frontmatter
- Canon frontmatter keys: `template_type` `template_id` `template_version` `requires_slots` `includes`
- Renderer strips frontmatter before output write
- Slot token form `\{\{TOKEN\}\}` with uppercase alphanumeric underscore
- Include forms `\{\{@include:path\}\}` and `\{\{@include:path#section\}\}`
- Include resolution is strict: missing file fail missing section fail circular graph fail
- Strict mode default: every required slot value present and no unresolved token
- Non strict mode allowed only for lint and normalization workflows
- Worker facing generated surfaces remain pointer first and exclude disposable artifacts

Target Files allowlist (hard gate):
- storage/dp/active/allowlist.txt

## 3.4 Execution Plan (A-E)

### 3.4.1 State
- DP-OPS-0210 locked the front-half latest-wins chain through `TOPIC.md`, `PLAN.md`, and `storage/dp/intake/DP.md`.
- The back half is now landing on active simple-name surfaces `storage/handoff/RESULTS.md`, `storage/handoff/CLOSING.md`, and `storage/dp/intake/ADDENDUM.md`, but docs, specs, tests, and residual guards still carry enough packet-scoped receipt language to create split-brain.
- Processed storage, audit transport, telemetry, and addendum lineage still retain packet identity where that truth matters.
- The remaining work in this packet is parity: make every runtime, guard, test, manifest, and doc agree on the same active-surface versus identity-surface split.
- `PLAN.md` already demonstrates the current safety model for latest-wins operator surfaces: it is overwritten on rerun and bundle writes only a disposable `var/tmp/PLAN.md.prev` backup. There is no multi-slice-aware preservation contract today.
- The main risk is not the rename itself. It is leaving active latest-wins surfaces, packet-scoped mirrors, prune safety, and audit transport only half-aligned.

### 3.4.2 Request
- Introduce one obvious active latest-wins operator surface for results, closing, and addendum.
- Keep packet identity and addendum lineage explicit where those are the truth-bearing surfaces.
- Make the operator-visible map honest: latest-wins for current active work, identity-bearing artifacts for lineage and audit.
- Keep audit bundle behavior coherent even if operator-facing active surfaces become simple-name files.
- Keep prune and ledger safety rules strict under the new surface model.
- Do not claim stronger retention or multi-slice plan protection than the repo actually implements.
- Prove the new map through closeout, audit-coherence, and route-contract tests.

### 3.4.3 Changelog
UPDATE:
- docs/MANUAL.md
- docs/MAP.md
- ops/bin/help
- ops/bin/open
- ops/bin/certify
- ops/bin/prune
- ops/bin/draft
- ops/bin/addendum
- ops/lib/scripts/bundle.sh
- ops/lib/scripts/ledger.sh
- ops/lib/manifests/BUNDLE.md
- ops/etc/persistence.manifest
- ops/etc/retention.manifest
- docs/ops/specs/binaries/bundle.md
- docs/ops/specs/binaries/certify.md
- docs/ops/specs/binaries/prune.md
- docs/ops/specs/binaries/draft.md
- docs/ops/specs/binaries/addendum.md
- docs/ops/specs/surfaces/results.md
- docs/ops/specs/surfaces/task.md
- docs/ops/specs/surfaces/pow.md
- docs/ops/specs/tools/lint/results.md
- docs/ops/specs/tools/lint/dp.md
- docs/ops/specs/tools/lint/style.md
- docs/ops/specs/tools/verify.md
- docs/ops/specs/scripts/ledger.md
- ops/src/surfaces/results.md.tpl
- ops/src/surfaces/task.md.tpl
- ops/src/surfaces/closing.md.tpl
- ops/src/stances/auditor.md.tpl
- tools/lint/results.sh
- tools/lint/dp.sh
- tools/lint/style.sh
- tools/lint/integrity.sh
- tools/lint/response.sh
- tools/test/bundle.sh
- tools/verify.sh
- storage/dp/active/allowlist.txt
- storage/handoff/CLOSING.md

### 3.4.4 Patch / Diff
1. Update storage/dp/active/allowlist.txt first with every tracked file touched by this packet. Do not add active storage surfaces just because they are canonical; storage remains disposable operational state.
2. Inventory every current packet-scoped consumer of RESULTS, CLOSING, and addendum intake names before changing any path contract.
3. Introduce one active latest-wins operator surface for results at `storage/handoff/RESULTS.md`, while preserving current-packet results identity where certify, audit, telemetry, or historical lineage require it.
4. Introduce one active latest-wins operator surface for closing at `storage/handoff/CLOSING.md`, while preserving current-packet closing identity where certify, audit, or historical lineage require it.
5. Introduce one active latest-wins operator surface for addendum at `storage/dp/intake/ADDENDUM.md`, while preserving `DP-OPS-XXXX-ADDENDUM-A` identity for packet-scoped issuance, processed storage, and addendum-specific certify paths.
6. Update certify, addendum, draft, prune, bundle, ledger, lint, persistence, and retention code so the active-surface model and identity-surface model are both explicit and coherent.
7. Update operator docs and specs so they explain the same split: active latest-wins surfaces for current work, packet-scoped identity artifacts for lineage-sensitive truth.
8. Keep audit bundle transport fail-closed and current-packet-coherent even if operator-facing active receipt surfaces become simple-name files.
9. Keep the packet bounded to surface contract completion; do not drift into authority redesign, execution-decision changes, or plan-retention redesign.

### 3.4.5 Receipt (Proofs to collect) - MUST RUN

**Mandatory receipt commands (always run; do not omit):**
- bash tools/lint/dp.sh TASK.md
- bash tools/lint/task.sh
- bash tools/lint/integrity.sh
- bash tools/lint/style.sh
- ./ops/bin/llms
- bash tools/lint/integrity.sh
- git diff --name-only
- git diff --stat
- comm -23 <(git diff --name-only | sort) <(sort storage/dp/active/allowlist.txt) || true
- comm -23 <(git ls-files --others --exclude-standard | sort) <(sort storage/dp/active/allowlist.txt) || true
- ./ops/bin/open

**DP-specific receipt commands (scope-specific; author below):**
Note: Command substitution forms (e.g., $(pwd), $(git ...)) are rejected by certify replay. Use literal values only. dp.sh enforces at lint time.
- bash tools/test/bundle.sh --slice=closeout-sanity
- bash tools/test/bundle.sh --slice=audit-coherence
- bash tools/test/bundle.sh --slice=route-contract
- bash tools/verify.sh --mode=certify-critical
- bash tools/lint/results.sh --all
- grep -n "RESULTS.md\|CLOSING.md\|ADDENDUM.md\|DP-OPS-.*-RESULTS\|CLOSING-DP-OPS-\|ADDENDUM-[A-Z]\|PLAN.md.prev\|storage/dp/intake/DP-\*\.md\|storage/dp/intake/ADDENDUM\.md" docs/MANUAL.md docs/MAP.md ops/bin/help ops/bin/open ops/bin/certify ops/bin/prune ops/bin/draft ops/bin/addendum ops/lib/scripts/bundle.sh ops/lib/scripts/ledger.sh ops/lib/manifests/BUNDLE.md ops/etc/persistence.manifest ops/etc/retention.manifest docs/ops/specs/binaries/bundle.md docs/ops/specs/binaries/certify.md docs/ops/specs/binaries/prune.md docs/ops/specs/binaries/draft.md docs/ops/specs/binaries/addendum.md docs/ops/specs/surfaces/results.md docs/ops/specs/surfaces/task.md docs/ops/specs/surfaces/pow.md docs/ops/specs/tools/lint/results.md docs/ops/specs/tools/lint/dp.md docs/ops/specs/tools/lint/style.md docs/ops/specs/tools/verify.md docs/ops/specs/scripts/ledger.md ops/src/surfaces/results.md.tpl ops/src/surfaces/task.md.tpl ops/src/surfaces/closing.md.tpl ops/src/stances/auditor.md.tpl tools/lint/results.sh tools/lint/dp.sh tools/lint/style.sh tools/lint/integrity.sh tools/lint/response.sh tools/test/bundle.sh tools/verify.sh

## 3.5 Closeout (Mandatory Routing)
- Execute docs/MANUAL.md Closeout Cycle in order (Verify, Harvest, Refresh, Log, Prune).
- Update SoP.md and PoW.md with DP entries, including objective summary and current proof / receipt summary.
- Protocol order for closeout: Verify -> Generate Results -> COMMIT (Operator Only) -> Prune.
- Run prune hygiene: ./ops/bin/prune --scrub.
- Refresh llms artifacts: `./ops/bin/llms`
- Regenerate session artifacts: `./ops/bin/open --out=auto`
- Ensure the next session begins with refreshed session artifacts and matching receipts.
- Note: audit dump generation is owned by `./ops/bin/bundle --profile=audit --out=auto` and is separate from operator session refresh. Do not stamp an additional `ops/bin/dump --scope=core` into closeout unless the DP explicitly authorizes a standalone state capture.
- Refresh side-effect: `ops/bin/llms` regenerates `ops/lib/manifests/OPS.md` as a compile event; if `OPS.md` is not in the allowlist, restore it before running `integrity.sh`. See `docs/MANUAL.md` Refresh side-effect notice for the full procedure.

### 3.5.1 Mandatory Closing Sidecar
Closing sidecar content is generated and maintained at `storage/handoff/CLOSING.md` and is validated by `ops/bin/certify` as a hard gate at certification time. Do not author, predict, populate, or approximate any sidecar-derived closeout output outside this sidecar.
Certify separately collects contractor-authored narrative for the RESULTS Contractor Execution Narrative section at certify time via interactive editor prompt; this narrative input is independent from closing sidecar content.

Before running certify, confirm `storage/handoff/CLOSING.md` has been maintained throughout execution and reflects observable reality only.

Closing sidecar fields required at certify time:
- Commit Message
- Create Pull Request (Title)
- Create Pull Request (Description)
- Confirm Merge (Commit Message)
- Confirm Merge (Extended Description)
- Confirm Merge (Add a Comment)
