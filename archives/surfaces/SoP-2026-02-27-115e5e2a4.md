---
trace_id: stela-20260228T032141Z-36f308cb
packet_id: DP-OPS-0131
created_at: 2026-02-28T03:33:49Z
previous: (none)
---
## 2026-02-28 03:17:34 UTC - DP-OPS-0131 conditional contractor notes decision pointer lint
Objective summary:
Updated `tools/lint/style.sh` to enforce Execution Decision Record validation for
`storage/dp/active/notes.md` with conditional behavior:
- `Decision Required` must be `Yes` or `No`.
- `Decision Pointer` remains permissive when `Decision Required: No`.
- `Decision Pointer` must be non-`None`, under `archives/decisions/`, end with
  `.md`, and resolve to an existing file when `Decision Required: Yes`.

Updated `docs/ops/specs/tools/lint/style.md` to document the new enforcement step
and the explicit scan of `storage/dp/active/notes.md`. Updated
`storage/dp/active/allowlist.txt`, maintained `storage/dp/active/notes.md` for
DP-OPS-0131, and maintained `storage/handoff/CLOSING-DP-OPS-0131.md` for certify.

Verification commands:
- git rev-parse --abbrev-ref HEAD
- git rev-parse --short HEAD
- git status --porcelain
- bash tools/lint/dp.sh --test
- bash tools/lint/dp.sh TASK.md
- bash tools/lint/task.sh
- bash tools/lint/dp.sh TASK.md
- bash tools/lint/task.sh
- bash tools/lint/integrity.sh
- bash tools/lint/style.sh
- git diff --name-only
- git diff --stat
- comm -23 <(git diff --name-only | sort) <(sort storage/dp/active/allowlist.txt) || true
- comm -23 <(git ls-files --others --exclude-standard | sort) <(sort storage/dp/active/allowlist.txt) || true
- ./ops/bin/open
- ./tools/verify.sh
- ./tools/lint/truth.sh
- ./tools/lint/context.sh
- ./tools/lint/ff.sh
- grep -n 'Execution Decision Record' ops/src/surfaces/notes.md.tpl
- grep -n '^Decision Required:' storage/dp/active/notes.md
- grep -n '^Decision Pointer:' storage/dp/active/notes.md
- grep -n 'Decision Required' tools/lint/style.sh
- ./ops/bin/certify --dp=DP-OPS-0131 --out=auto
- bash tools/lint/results.sh storage/handoff/DP-OPS-0131-RESULTS.md
- ./ops/bin/prune --scrub
- ./ops/bin/open --out=auto
- ./ops/bin/dump --scope=platform --format=chatgpt --out=auto

Functional receipt summary:
Freshness and preflight gates passed. Style lint now enforces conditional
contractor-notes decision pointer requirements while preserving the permissive
path when `Decision Required: No`.
