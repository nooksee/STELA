---
trace_id: stela-20260322T193937Z-745c0492
packet_id: DP-OPS-0220
created_at: 2026-03-22T19:44:06Z
previous: archives/surfaces/TASK-DP-OPS-0219-1649d4e0.md
---
# STELA TASK DASHBOARD
Status: ACTIVE
Owner: Integrator
Last Updated: 2026-03-19

## 1. Session State
Pointer: Session context output (generated by ops/bin/open)
Context Manifest: ops/lib/manifests/CONTEXT.md (checked by tools/lint/context.sh)

Session State contract:
- Session State is pointer-first. Do not write inline branch or hash state here.
- OPEN is the session state snapshot. OPEN-PORCELAIN is conditional dirty-state detail.
- RESULTS is the audit record for the DP.
- TASK is the stable routing surface.

## 2. Logic Pointers
Primary Constraint: PoT.md (Policy of Truth).
Surface Contract Spec: docs/ops/specs/surfaces/task.md.

### 2.1 Governance Pointers
Jurisdiction: PoT.md Section 3.
Git Authority: PoT.md Section 4.1.
Behavioral Standard: PoT.md Section 4.2.
Staffing Protocol: PoT.md Section 4.1.

### 2.2 Enforcement and Gates
Style: tools/lint/style.sh
Context: tools/lint/context.sh
Truth: tools/lint/truth.sh
DP lint: tools/lint/dp.sh (enforces DP transaction rules in Section 3; does not enforce TASK container schema)
TASK lint: tools/lint/task.sh (sole TASK surface/container schema enforcer)
Repo verify: tools/verify.sh
llms parity: tools/lint/llms.sh

## 3. Current Dispatch Packet (DP)

Section-by-section authoring protocol:
- Generate DP structure from `ops/src/surfaces/dp.md.tpl` using `ops/bin/draft`.
- Do not hand-edit DP structural boilerplate in `TASK.md` after generation.
- Manual edits are limited to slot content only (objective, scope, safety, plan body, receipt body).
- Freeze each completed subsection in order (3.1 -> 3.2 -> 3.3 -> 3.4.x).
- Do not revise earlier sections to justify later work. If scope changes, end the thread and draft a new DP.

Freeze Rule:
- If a completed DP subsection changes later, append an AMENDMENT block in RESULTS; otherwise DP is FAIL.

AMENDMENT: minimum fields
- Reopened section:
- Reason:
- Impact:
- Allowlist change (none or exact paths):

Input discipline (hard rule):
- Worker receives only the DP text. Worker does not require OPEN or DUMP attachments.
- DP writer must not attach or cite disposable artifacts (rough notes, chat logs, scratch drafts, throwaway excerpts).
- DP writer must not include pasted bundles (OPEN, DUMP payloads, manifests) inside the DP body.
- Canon pointers only. Evidence belongs in storage artifacts and RESULTS.



### DP-OPS-0220: DP Process Integrity Fixes

## 3.1 Freshness Gate (Must Pass Before Work)
Base Branch: main
Required Work Branch: work/dp-ops-0220-process-integrity-fixes-2026-03-22
Base HEAD: d7f51e6a
Freshness Stamp: 2026-03-22
Note: FRESHNESS_STAMP must be YYYY-MM-DD format only. No trace tokens, no timestamps, no other text. Certify rejects all other forms. dp.sh enforces at lint time.

Required local re-check (worker runs; paste outputs in RESULTS):
- git rev-parse --abbrev-ref HEAD
- git rev-parse --short HEAD
- git status --porcelain

Preconditions:
- No commits on main.
- Working tree must be clean before execution begins.
- If Base HEAD changes, regenerate session artifacts from canonical tools before proceeding.

STOP conditions:
- STOP if any mismatch (branch, Base HEAD, missing work branch).
- STOP if working tree is dirty before execution begins.
- STOP if told to create or switch branches.

## 3.1.1 DP Preflight Gate (Run Before Any Edits)
Purpose:
- Catch malformed DP or TASK schema before work begins.

Worker runs (paste outcome in RESULTS):
- bash tools/lint/dp.sh --test
- bash tools/lint/dp.sh TASK.md
- bash tools/lint/task.sh

STOP if any preflight check fails.

### CbC Design Discipline Preflight (TASK.md §3.1.1)
Required when the DP objective adds, modifies, or replaces a linter, script, guard, or validation binary.
For non-tooling DPs: state "Not applicable" with a one-line justification.
Applicable. This packet modifies tools/lint/dp.sh (a validation binary) and ops/bin/draft (a generation binary).

Invariants:
1. dp.sh provisional-marker scan must reject any rendered DP containing the literal drafting-marker word anywhere in the body.
2. ops/bin/draft must continue to emit a valid DP skeleton after removing the provisional-prefix/ prepend logic.
3. No existing passing dp.sh smoke fixture may begin failing after the scan addition.

## 3.2 Required Context Load (Read Before Doing Anything)

### 3.2.1 Canon load order (always)
Worker must confirm loaded before edits begin:
1. PoT.md
2. SoP.md
3. PoW.md
4. TASK.md
5. docs/MAP.md
6. docs/MANUAL.md
7. ops/lib/manifests/CONTEXT.md

Notes:
- Worker does not read OPEN. OPEN is for Integrator state refresh and for receipts.
- Disposable artifacts must not be referenced or included.
- Worker input is the DP text only.
- DP writer must not attach or cite disposable artifacts.
- DP writer must not embed pasted bundles.

## Rules

1 Temp path rule uses `var/tmp` only
2 RESULTS generation rule uses `ops/bin/certify` only
3 Receipt rule runs every DP mandated command and treats any skip as STOP
4 Disposable artifact rule treats OPEN dump bundles and manifests as non canonical session artifacts
5 Use repo-relative paths only in all output. Never print absolute filesystem paths. Do not emit clickable file links.

### 3.2.2 DP-scoped load order (per DP)
8. tools/lint/dp.sh (provisional-marker-scan insertion point and existing branch-coherence check around lines 1080-1100)
9. ops/bin/draft (lines 338-350: proposed_work_branch prepend block)
10. ops/src/stances/architect.md.tpl (line 47: provisional-prefix example to remove)
11. docs/ops/specs/tools/lint/dp.md (line 63: canonical proposal-slot rendering language)
12. tools/lint/response.sh (line 707: provisional-prefix fixture to fix)
13. docs/MANUAL.md (closeout cycle and §3.1 preflight proof sections)

## 3.3 Scope and Safety
Objective:
Close three recurring process friction points: (1) consolidate provisional-marker authority to PoT, slot name, and a single dp.sh scan — removing all value-level provisional-prefix/ wiring from draft, stances, specs, and fixtures; (2) add --rerun guidance to MANUAL.md closeout instructions so audit resubmission bundles carry the correct R-number without operator prompting; (3) clarify in MANUAL.md and the DP template that §3.1 pre-edit preflight outputs belong in the Contractor Execution Narrative, not the receipt command list.

In scope:
- tools/lint/dp.sh: add scan of rendered DP text for literal drafting-marker word; fail if found
- ops/bin/draft: remove lines that prepend provisional-prefix/ to the branch value
- ops/src/stances/architect.md.tpl: remove provisional-prefix/work/... example from line 47
- docs/ops/specs/tools/lint/dp.md: remove canonical proposal-slot rendering language referencing provisional-prefix/work/...
- tools/lint/response.sh: fix fixture at line 707 that uses provisional-prefix/work/...
- docs/MANUAL.md: add --rerun guidance to audit resubmission / closeout cycle section
- docs/MANUAL.md: clarify that §3.1 pre-edit preflight outputs belong in Contractor Execution Narrative, not receipt command list
- storage/dp/active/allowlist.txt: add TASK archive entry for this DP

Out of scope:
- PoT.md: no change; §6.3 provisional marking doctrine is correct as written
- ops/src/surfaces/dp.md.tpl: slot name WORK-BRANCH slot is correct; no rename
- ops/bin/certify: no change
- Any stance templates other than architect.md.tpl
- TASK.md, SoP.md, PoW.md: no structural edits

Safety and invariants:
- dp.sh --test must pass after adding the provisional-marker scan check
- The provisional-marker scan must not fire on the slot token form work/dp-ops-0220-process-integrity-fixes-2026-03-22 (template source only); it fires on rendered DP text where the drafting-marker word appears as a value
- Removing the draft prepend logic must not break ops/bin/draft --test or any draft smoke fixtures
- architect.md.tpl removal must not break tools/lint/style.sh architect freeze checks
- All existing dp.sh, response.sh, and style.sh tests must continue to pass

Worker Constraints (SSOT injected):
## Section 1: Universal Template Rules
- Template source uses `tpl` files with optional YAML frontmatter
- Canon frontmatter keys: `template_type` `template_id` `template_version` `requires_slots` `includes`
- Renderer strips frontmatter before output write
- Slot token form `\{\{TOKEN\}\}` with uppercase alphanumeric underscore
- Include forms `\{\{@include:path\}\}` and `\{\{@include:path#section\}\}`
- Include resolution is strict: missing file fail missing section fail circular graph fail
- Strict mode default: every required slot value present and no unresolved token
- Non strict mode allowed only for lint and normalization workflows
- Worker facing generated surfaces remain pointer first and exclude disposable artifacts

Target Files allowlist (hard gate):
- storage/dp/active/allowlist.txt

## 3.4 Execution Plan (A-E)

### 3.4.1 State
tools/lint/dp.sh: branch-coherence check around line 1087 references provisional-prefix/work/... as canonical; no scan exists for the drafting-marker word appearing in rendered DP text.
ops/bin/draft lines 342-344: if proposed_work_branch does not start with provisional-prefix/, draft prepends provisional-prefix/ — this is the root cause of value-level bleed.
ops/src/stances/architect.md.tpl line 47: explicitly instructs architect to emit provisional-prefix/work/... as the Required Work Branch value.
docs/ops/specs/tools/lint/dp.md line 63: describes provisional-prefix/work/... as canonical proposal-slot rendering, treating value-level bleed as intended behavior.
tools/lint/response.sh line 707: smoke fixture uses provisional-prefix/work/... in Required Work Branch field.
docs/MANUAL.md: no --rerun guidance exists for audit resubmission; no clarification that §3.1 outputs belong in narrative.

### 3.4.2 Request
1. In tools/lint/dp.sh, after the existing branch-coherence check (around line 1090), add a provisional-marker presence scan: grep the rendered DP text for the literal drafting-marker word (whole-word or substring, case-sensitive); if found, fail with "provisional-marker is a drafting marker and must not appear in a finalized DP — remove it before worker execution."

2. In ops/bin/draft, remove the block (lines 342-344) that conditionally prepends provisional-prefix/ to the proposed_work_branch value. The slot name WORK-BRANCH slot already signals provisional; the value must be the bare work/<topic>-YYYY-MM-DD branch name only.

3. In ops/src/stances/architect.md.tpl line 47, replace the instruction that says to populate Required Work Branch with provisional-prefix/work/... with an instruction that says to populate it with the bare work/<topic>-YYYY-MM-DD branch name only.

4. In docs/ops/specs/tools/lint/dp.md line 63, remove the phrase "canonical proposal-slot rendering in the branch field (provisional-prefix/work/...)" or rewrite to reflect that provisional-marker does not appear in a finalized DP value.

5. In tools/lint/response.sh line 707, replace the Required Work Branch fixture value provisional-prefix/work/dp-ops-9999-response-envelope-self-test-2026-03-04 with work/dp-ops-9999-response-envelope-self-test-2026-03-04.

6. In docs/MANUAL.md, locate the closeout cycle section. Add: when an audit bundle must be regenerated after an AUDIT FAIL verdict, run ops/bin/bundle --profile=audit --rerun --out=auto; the --rerun flag increments the submission R-number; omitting it produces a duplicate R0 bundle.

7. In docs/MANUAL.md, locate or add a §3.1 preflight proof note. Add: the three §3.1 freshness-gate commands (git rev-parse --abbrev-ref HEAD, git rev-parse --short HEAD, git status --porcelain) must be run before any file edits begin and their verbatim outputs pasted into the Contractor Execution Narrative under Preflight State; they must not be added to the receipt command list, where certify replays them at dirty-closeout time and the outputs are not proof of pre-edit state.

8. In storage/dp/active/allowlist.txt, append:
   archives/surfaces/TASK-DP-OPS-0220-*.md

9. Run receipt commands and paste outputs in RESULTS.

### 3.4.3 Changelog
UPDATE tools/lint/dp.sh — add provisional-marker presence scan; fail if literal word found in rendered DP text
UPDATE ops/bin/draft — remove provisional-prefix/ value-level prepend block
UPDATE ops/src/stances/architect.md.tpl — replace provisional-prefix/work/... example with bare work/... instruction
UPDATE docs/ops/specs/tools/lint/dp.md — remove canonical proposal-slot rendering language for provisional-prefix/work/...
UPDATE tools/lint/response.sh — fix provisional-prefix/work/... fixture to bare work/... value
UPDATE docs/MANUAL.md — add --rerun guidance; add §3.1 preflight proof placement note
UPDATE storage/dp/active/allowlist.txt — add TASK archive entry for DP-OPS-0220

### 3.4.4 Patch / Diff
A. Read tools/lint/dp.sh lines 1080-1100 in full.

B. Insert provisional-marker presence scan after the existing branch-coherence check as described in Request item 1. Run bash tools/lint/dp.sh --test. STOP if test fails.

C. Read ops/bin/draft lines 338-350 in full.

D. Remove the provisional-prefix/ prepend block as described in Request item 2.

E. Read ops/src/stances/architect.md.tpl line 47 in context.

F. Apply replacement described in Request item 3.

G. Read docs/ops/specs/tools/lint/dp.md line 63 in context.

H. Apply removal or rewrite described in Request item 4.

I. Read tools/lint/response.sh line 707 in context.

J. Apply fixture fix described in Request item 5. Run bash tools/lint/response.sh --test. STOP if test fails.

K. Read docs/MANUAL.md. Locate closeout cycle section and any preflight proof sections.

L. Apply MANUAL.md additions described in Request items 6 and 7.

M. In storage/dp/active/allowlist.txt, append archives/surfaces/TASK-DP-OPS-0220-*.md.

N. Run receipt commands and paste outputs in RESULTS.

### 3.4.5 Receipt (Proofs to collect) - MUST RUN

**Mandatory receipt commands (always run; do not omit):**
- bash tools/lint/dp.sh TASK.md
- bash tools/lint/task.sh
- bash tools/lint/integrity.sh
- bash tools/lint/style.sh
- ./ops/bin/llms
- bash tools/lint/integrity.sh
- git diff --name-only
- git diff --stat
- comm -23 <(git diff --name-only | sort) <(sort storage/dp/active/allowlist.txt) || true
- comm -23 <(git ls-files --others --exclude-standard | sort) <(sort storage/dp/active/allowlist.txt) || true
- ./ops/bin/open

**DP-specific receipt commands (scope-specific; author below):**
Note: Command substitution forms (e.g., $(pwd), $(git ...)) are rejected by certify replay. Use literal values only. dp.sh enforces at lint time.
- bash tools/lint/dp.sh --test
- bash tools/lint/response.sh --test

## 3.5 Closeout (Mandatory Routing)
- Execute docs/MANUAL.md Closeout Cycle in order (Verify, Harvest, Refresh, Log, Prune).
- Update SoP.md and PoW.md with DP entries, including objective summary and current proof / receipt summary.
- Protocol order for closeout: Verify -> Generate Results -> COMMIT (Operator Only) -> Prune.
- Run prune hygiene: ./ops/bin/prune --scrub.
- Refresh llms artifacts: `./ops/bin/llms`
- Regenerate session artifacts: `./ops/bin/open --out=auto`
- Ensure the next session begins with refreshed session artifacts and matching receipts.
- Note: audit dump generation is owned by `./ops/bin/bundle --profile=audit --out=auto` and is separate from operator session refresh. Do not stamp an additional `ops/bin/dump --scope=core` into closeout unless the DP explicitly authorizes a standalone state capture.
- Refresh side-effect: `ops/bin/llms` regenerates `ops/lib/manifests/OPS.md` as a compile event; if `OPS.md` is not in the allowlist, restore it before running `integrity.sh`. See `docs/MANUAL.md` Refresh side-effect notice for the full procedure.

### 3.5.1 Mandatory Closing Sidecar
Closing sidecar content is generated and maintained at `storage/handoff/CLOSING.md` and is validated by `ops/bin/certify` as a hard gate at certification time. Do not author, predict, populate, or approximate any sidecar-derived closeout output outside this sidecar.
Certify separately collects contractor-authored narrative for the RESULTS Contractor Execution Narrative section at certify time via interactive editor prompt; this narrative input is independent from closing sidecar content.

Before running certify, confirm `storage/handoff/CLOSING.md` has been maintained throughout execution and reflects observable reality only.

Closing sidecar fields required at certify time:
- Commit Message
- Create Pull Request (Title)
- Create Pull Request (Description)
- Confirm Merge (Commit Message)
- Confirm Merge (Extended Description)
- Confirm Merge (Add a Comment)
