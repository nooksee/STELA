---
trace_id: stela-20260226T135833Z-291651cb
packet_id: DP-OPS-0118
created_at: 2026-02-26T15:11:01Z
previous: (none)
---
# STELA TASK DASHBOARD
Status: ACTIVE
Owner: Integrator
Last Updated: 2026-02-18

## 1. Session State
Pointer: Session context output (generated by ops/bin/open)
Context Manifest: ops/lib/manifests/CONTEXT.md (checked by tools/lint/context.sh)

Session State contract:
- Session State is pointer-first. Do not write inline branch or hash state here.
- OPEN and OPEN-PORCELAIN are the session state snapshot.
- OPEN-PORCELAIN is emitted on every OPEN run. Clean sessions produce a zero-line artifact.
- RESULTS is the audit record for the DP.
- TASK is the stable routing surface.
- Operator note (2026-02-17): Do not edit `SoP.md` or `PoW.md` unless explicitly instructed.

## 2. Logic Pointers
Primary Constraint: PoT.md (Policy of Truth).
Surface Contract Spec: docs/ops/specs/surfaces/task.md.

### 2.1 Governance Pointers
Jurisdiction: PoT.md Section 3.
Git Authority: PoT.md Section 4.1.
Behavioral Standard: PoT.md Section 4.2.
Staffing Protocol: PoT.md Section 4.1.

### 2.2 Enforcement and Gates
Style: tools/lint/style.sh
Context: tools/lint/context.sh
Truth: tools/lint/truth.sh
DP lint: tools/lint/dp.sh (enforces DP transaction rules in Section 3; does not enforce TASK container schema)
TASK lint: tools/lint/task.sh (sole TASK surface/container schema enforcer)
Repo verify: tools/verify.sh
llms parity: tools/lint/llms.sh

## 3. Current Dispatch Packet (DP)

Section-by-section authoring protocol:
- Generate DP structure from `ops/src/surfaces/dp.md.tpl` using `ops/bin/draft`.
- Do not hand-edit DP structural boilerplate in `TASK.md` after generation.
- Manual edits are limited to slot content only (objective, scope, safety, plan body, receipt body).
- Freeze each completed subsection in order (3.1 -> 3.2 -> 3.3 -> 3.4.x).
- Do not revise earlier sections to justify later work. If scope changes, end the thread and draft a new DP.

Freeze Rule:
- If a completed DP subsection changes later, append an AMENDMENT block in RESULTS; otherwise DP is FAIL.

AMENDMENT: minimum fields
- Reopened section:
- Reason:
- Impact:
- Allowlist change (none or exact paths):

Input discipline (hard rule):
- Worker receives only the DP text. Worker does not require OPEN or DUMP attachments.
- DP writer must not attach or cite disposable artifacts (rough notes, chat logs, scratch drafts, throwaway excerpts).
- DP writer must not include pasted bundles (OPEN, DUMP payloads, manifests) inside the DP body.
- Canon pointers only. Evidence belongs in storage artifacts and RESULTS.


### DP-OPS-0118: `dp.md.tpl` Hash-Coupling Recovery Procedure Codification

## 3.1 Freshness Gate (Must Pass Before Work)
Base Branch: main
Required Work Branch: work/dp-ops-0118-2026-02-26
Base HEAD: 1d32e5522
Freshness Stamp: 2026-02-26
Note: FRESHNESS_STAMP must be YYYY-MM-DD format only. No trace tokens, no timestamps, no other text. Certify rejects all other forms. dp.sh enforces at lint time.

Required local re-check (worker runs; paste outputs in RESULTS):
- git rev-parse --abbrev-ref HEAD
- git rev-parse --short HEAD
- git status --porcelain

Preconditions:
- No commits on main.
- Working tree must be clean before execution begins.
- If Base HEAD changes, regenerate session artifacts from canonical tools before proceeding.

STOP conditions:
- STOP if any mismatch (branch, Base HEAD, missing work branch).
- STOP if working tree is dirty before execution begins.
- STOP if told to create or switch branches.

## 3.1.1 DP Preflight Gate (Run Before Any Edits)
Purpose:
- Catch malformed DP or TASK schema before work begins.

Worker runs (paste outcome in RESULTS):
- bash tools/lint/dp.sh --test
- bash tools/lint/dp.sh TASK.md
- bash tools/lint/task.sh

STOP if any preflight check fails.

### CbC Design Discipline Preflight (TASK.md §3.1.1)
Required when the DP objective adds, modifies, or replaces a linter, script, guard, or validation binary.
For non-tooling DPs: state "Not applicable" with a one-line justification.
Not applicable. This packet is docs-only; it adds a procedure subsection to `docs/MANUAL.md` and does not add, modify, or replace any linter, script, guard, or validation binary.

## 3.2 Required Context Load (Read Before Doing Anything)

### 3.2.1 Canon load order (always)
Worker must confirm loaded before edits begin:
1. PoT.md
2. SoP.md
3. PoW.md
4. TASK.md
5. docs/MAP.md
6. docs/MANUAL.md
7. ops/lib/manifests/CONTEXT.md

Notes:
- Worker does not read OPEN. OPEN is for Integrator state refresh and for receipts.
- Disposable artifacts must not be referenced or included.
- Worker input is the DP text only.
- DP writer must not attach or cite disposable artifacts.
- DP writer must not embed pasted bundles.

<!-- CCD: ff_target="machine-dense" ff_band="10-20" -->
# Contractor Brief

1 Temp path rule uses `var/tmp` only
2 RESULTS generation rule uses `ops/bin/certify` only
3 Receipt rule runs every DP mandated command and treats any skip as STOP
4 Disposable artifact rule treats OPEN dump bundles and manifests as non canonical session artifacts
5 Path rule uses existing repo paths or explicit `NEW` changelog markers and forbids globs and brace expansion

### 3.2.2 DP-scoped load order (per DP)
Worker must load the following in addition to the canon load order:
- docs/MANUAL.md — target file; worker must read current content in full before editing
- tools/lint/dp.sh — source of the hash-coupling mechanism being documented; worker must read the `CANONICAL_DP_TEMPLATE_SHA256` constant and `check_template_hash_preflight` function to verify accuracy of the documented recovery steps
- ops/src/surfaces/dp.md.tpl — the template whose edit triggers the coupling; worker must read to confirm the procedure text accurately describes the artifact

## 3.3 Scope and Safety
Objective:
Add a documented recovery procedure subsection for the `dp.md.tpl` hash-coupling failure mode to `docs/MANUAL.md`. The subsection encodes the exact authorized trigger condition (canonical template hash mismatch in `tools/lint/dp.sh` immediately after `ops/src/surfaces/dp.md.tpl` is edited in the current work branch), the linear recovery steps, the CONTRACTOR-NOTES.md documentation requirement when the recovery is invoked, and the addendum path for non-matching failure modes. No code, template, or linter changes are made.

In scope:
- `docs/MANUAL.md` — add one new subsection (`### dp.md.tpl Hash-Coupling Recovery`) within the existing Closeout Cycle section, positioned immediately after the `### Contractor Notes Surface` subsection and before the `3. Harvest` header.

Out of scope:
- `ops/src/surfaces/dp.md.tpl` — must not be edited while documenting recovery for that template.
- `tools/lint/dp.sh` — no code or constant changes; referenced for accuracy verification only.
- `ops/bin/draft` — no changes; referenced in recovery step text only.
- `ops/bin/certify` — no changes.
- Any other file not listed in the Changelog.

Safety and invariants:
- No manual edits to generated outputs.
- No structural edits to TASK.md or the DP block.
- Allowlist hard gate: only `docs/MANUAL.md` is a non-certify-managed change on this branch; PoW.md, SoP.md, and TASK.md must be in the allowlist to cover certify-managed pointer rewrites at closeout.
- This packet is docs-only. If any verification gate fails during execution, stop and report to the Integrator before proceeding.
- The recovery procedure text must describe the exact trigger condition from the authoritative source (`tools/lint/dp.sh` `check_template_hash_preflight` function and `CANONICAL_DP_TEMPLATE_SHA256` constant) and must not generalize to other `dp.sh` failure modes.

Worker Constraints (SSOT injected):
## Section 1: Universal Template Rules
- Template source uses `tpl` files with optional YAML frontmatter
- Canon frontmatter keys: `template_type` `template_id` `template_version` `requires_slots` `includes`
- Renderer strips frontmatter before output write
- Slot token form `\{\{TOKEN\}\}` with uppercase alphanumeric underscore
- Include forms `\{\{@include:path\}\}` and `\{\{@include:path#section\}\}`
- Include resolution is strict: missing file fail missing section fail circular graph fail
- Strict mode default: every required slot value present and no unresolved token
- Non strict mode allowed only for lint and normalization workflows
- Worker facing generated surfaces remain pointer first and exclude disposable artifacts

Target Files allowlist (hard gate):
- storage/dp/active/allowlist.txt

## 3.4 Execution Plan (A-E)

### 3.4.1 State
Repository is clean at main HEAD 1d32e5522. Working tree has zero porcelain entries. Last commit (1d32e5522) added the `CONTRACTOR-NOTES.md` closeout surface and document routing requirements (DP-OPS-0117, audited PASS). `docs/MANUAL.md` exists and contains the full Closeout Cycle section including the `### Contractor Notes Surface` subsection. `tools/lint/dp.sh` contains the `CANONICAL_DP_TEMPLATE_SHA256` constant and the `check_template_hash_preflight` function. `ops/src/surfaces/dp.md.tpl` exists. The hash-coupling recovery path has been exercised in DP-OPS-0114 and DP-OPS-0117 executions but has not yet been codified as a documented procedure in `docs/MANUAL.md`. OQ-7 in the active plan identifies `docs/MANUAL.md` as the recommended codification target.

### 3.4.2 Request
Add a new `### dp.md.tpl Hash-Coupling Recovery` subsection to `docs/MANUAL.md`. The subsection must satisfy all of the following requirements:

1. State the exact authorized trigger condition verbatim: `bash tools/lint/dp.sh TASK.md` fails with a canonical template hash mismatch immediately after `ops/src/surfaces/dp.md.tpl` was edited in the current work branch.
2. State explicitly that this recovery procedure is authorized for this trigger condition only and that any other `dp.sh` failure requires a stop and Integrator review.
3. Provide a linear, unconditional sequence of numbered recovery steps:
   a. Compute the new template hash using `sha256sum ops/src/surfaces/dp.md.tpl` (with a note to use `shasum -a 256` when `sha256sum` is unavailable).
   b. Update the `CANONICAL_DP_TEMPLATE_SHA256` constant in `tools/lint/dp.sh` to the computed value.
   c. Re-run `bash tools/lint/dp.sh --test` and confirm PASS.
   d. Re-run `bash tools/lint/dp.sh TASK.md`. If PASS, recovery is complete; if the lint fails on normalized-structure mismatch (not a hash mismatch), proceed to step (e).
   e. Rerender `TASK.md` mechanically using `./ops/bin/draft` with the same arguments as the original draft invocation. Do not hand-edit the structural boilerplate output.
   f. Re-run `bash tools/lint/dp.sh TASK.md` and confirm PASS.
4. State the CONTRACTOR-NOTES.md documentation requirement: when this recovery is invoked, the contractor must record it in the `Anomalies Encountered` field of `storage/handoff/CONTRACTOR-NOTES.md`, including the triggering lint failure output, the hash value before and after the update, whether a TASK rerender was required, and the final `bash tools/lint/dp.sh TASK.md` PASS confirmation.
5. State the addendum path: any `dp.sh` failure that does not match the authorized trigger condition must not use this recovery procedure; the contractor must stop, report to the Integrator, and await addendum authorization before proceeding.

### 3.4.3 Changelog
- UPDATE: docs/MANUAL.md

### 3.4.4 Patch / Diff
Step 1. Open `docs/MANUAL.md` and locate the `### Contractor Notes Surface` subsection. Confirm that it ends with the routing sentence: `Routing: storage/handoff/CONTRACTOR-NOTES.md is a closeout-time handoff surface under storage/handoff/. It is required by closeout procedure and DP routing, but it is not a global CONTEXT manifest dependency because it is produced late in the session.`

Step 2. Confirm that the line immediately following the Contractor Notes Surface content is `3. Harvest`.

Step 3. Insert the following new subsection into `docs/MANUAL.md` immediately after the final line of the `### Contractor Notes Surface` block and before the `3. Harvest` header. Do not alter any existing content above or below the insertion point.

Insert text (exact):

~~~
### dp.md.tpl Hash-Coupling Recovery

**Authorized trigger condition:** `bash tools/lint/dp.sh TASK.md` fails with a canonical
template hash mismatch immediately after `ops/src/surfaces/dp.md.tpl` was edited in the
current work branch.

This recovery procedure is authorized for this exact trigger condition only. Any other
`dp.sh` failure requires a stop and Integrator review, and may require an addendum before
work continues.

**Recovery steps (run in order; do not skip):**

1. Compute the new template hash:
   ~~~bash
   sha256sum ops/src/surfaces/dp.md.tpl
   ~~~
   If `sha256sum` is not available, use: `shasum -a 256 ops/src/surfaces/dp.md.tpl`

2. Update the `CANONICAL_DP_TEMPLATE_SHA256` constant in `tools/lint/dp.sh` to the
   computed hash value.

3. Re-run the template hash preflight and confirm PASS:
   ~~~bash
   bash tools/lint/dp.sh --test
   ~~~

4. Re-run the full DP lint:
   ~~~bash
   bash tools/lint/dp.sh TASK.md
   ~~~
   If PASS: recovery is complete. If the lint fails on normalized-structure mismatch
   (not a hash mismatch): proceed to step 5.

5. Rerender `TASK.md` mechanically using the canonical generator with the same arguments
   as the original draft invocation:
   ~~~bash
   ./ops/bin/draft --id=<DP_ID> ...
   ~~~
   Do not hand-edit structural boilerplate in the rendered output.

6. Re-run `bash tools/lint/dp.sh TASK.md` and confirm PASS.

**Documentation requirement:** When this recovery is used, record it in the
`Anomalies Encountered` field of `storage/handoff/CONTRACTOR-NOTES.md`. Include:
the triggering lint failure output, the `CANONICAL_DP_TEMPLATE_SHA256` value before
and after the update, whether a TASK rerender (step 5) was required, and the final
`bash tools/lint/dp.sh TASK.md` PASS confirmation.

**Addendum path:** If a `dp.sh` failure does not match the authorized trigger condition
above, do not apply this recovery. Stop, report to the Integrator, and await addendum
authorization before proceeding.
~~~

Step 4. Verify the insertion is syntactically correct: the subsection header `### dp.md.tpl Hash-Coupling Recovery` is at the correct heading level within the Closeout Cycle section, and the `3. Harvest` numbered header immediately follows the new subsection with no intervening blank-line violations.

Step 5. Confirm `docs/MANUAL.md` has no other changes outside the inserted subsection by reviewing the full diff before proceeding to receipt collection.

### 3.4.5 Receipt (Proofs to collect) - MUST RUN

**Mandatory receipt commands (always run; do not omit):**
- bash tools/lint/dp.sh TASK.md
- bash tools/lint/task.sh
- bash tools/lint/integrity.sh
- bash tools/lint/style.sh
- git diff --name-only
- git diff --stat
- comm -23 <(git diff --name-only | sort) <(sort storage/dp/active/allowlist.txt) || true
- comm -23 <(git ls-files --others --exclude-standard | sort) <(sort storage/dp/active/allowlist.txt) || true
- ./ops/bin/open

**DP-specific receipt commands (scope-specific; author below):**
Note: Command substitution forms (e.g., $(pwd), $(git ...)) are rejected by certify replay. Use literal values only. dp.sh enforces at lint time.
- bash tools/lint/truth.sh
- bash tools/lint/ff.sh
- bash tools/lint/context.sh
- grep -n "dp.md.tpl Hash-Coupling Recovery" docs/MANUAL.md
- rg -n "run_test|PROPOSED token|OK: --test passed" tools/lint/dp.sh
- sed -n '1238,1288p' tools/lint/dp.sh
- sed -n '1340,1378p' tools/lint/dp.sh
- ./ops/bin/llms --out-dir=/home/nos4r2/dev/nukece
- git restore --source=HEAD --staged --worktree -- ops/lib/manifests/OPS.md

## 3.5 Closeout (Mandatory Routing)
- Execute docs/MANUAL.md Closeout Cycle in order (Verify, Harvest, Refresh, Log, Prune).
- Update SoP.md and PoW.md with DP entries, including objective summary and verification commands run.
- Protocol order for closeout: Verify -> Generate Results -> COMMIT (Operator Only) -> Prune.
- Before handing work for operator pre-audit review, produce `storage/handoff/CONTRACTOR-NOTES.md` using `ops/src/surfaces/notes.md.tpl` as the schema. See `docs/MANUAL.md` Contractor Notes Surface for field definitions and routing.
- Run prune hygiene: ./ops/bin/prune --scrub.
- Regenerate session artifacts: `./ops/bin/open --out=auto`
- Capture updated platform state: `./ops/bin/dump --scope=platform --format=chatgpt --out=auto`
- Ensure the next session begins with refreshed session artifacts and matching receipts.
- Refresh side-effect: `ops/bin/llms` regenerates `ops/lib/manifests/OPS.md` as a compile event; if `OPS.md` is not in the allowlist, restore it before running `integrity.sh`. See `docs/MANUAL.md` Refresh side-effect notice for the full procedure.

### 3.5.1 Mandatory Closing Block
Closing block content is generated exclusively by `ops/bin/certify` from the closing sidecar at `storage/handoff/CLOSING-DP-OPS-0118.md` at certification time. Do not author, predict, populate, or approximate any closing block field at draft time or during execution. The closing sidecar is the only human-authored narrative input certify ingests. Certify generates all of the following from observable repository state: Primary Commit Header, Pull Request Title, Pull Request Description, Final Squash Stub, Extended Technical Manifest, and Review Conversation Starter.

Before running certify, confirm `storage/handoff/CLOSING-DP-OPS-0118.md` has been maintained throughout execution and reflects observable reality only.

Fields certify generates from the closing sidecar and repository state:
- Primary Commit Header
- Pull Request Title
- Pull Request Description
- Final Squash Stub
- Extended Technical Manifest
- Review Conversation Starter
