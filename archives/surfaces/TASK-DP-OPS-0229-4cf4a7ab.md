---
trace_id: stela-20260324T200931Z-1e9e23dd
packet_id: DP-OPS-0229
created_at: 2026-03-24T20:12:40Z
previous: archives/surfaces/TASK-DP-OPS-0228-b00ba25a.md
---
# STELA TASK DASHBOARD
Status: ACTIVE
Owner: Integrator
Last Updated: 2026-03-19

## 1. Session State
Pointer: Session context output (generated by ops/bin/open)
Context Manifest: ops/lib/manifests/CONTEXT.md (checked by tools/lint/context.sh)

Session State contract:
- Session State is pointer-first. Do not write inline branch or hash state here.
- OPEN is the session state snapshot. OPEN-PORCELAIN is conditional dirty-state detail.
- RESULTS is the audit record for the DP.
- TASK is the stable routing surface.

## 2. Logic Pointers
Primary Constraint: PoT.md (Policy of Truth).
Surface Contract Spec: docs/ops/specs/surfaces/task.md.

### 2.1 Governance Pointers
Jurisdiction: PoT.md Section 3.
Git Authority: PoT.md Section 4.1.
Behavioral Standard: PoT.md Section 4.2.
Staffing Protocol: PoT.md Section 4.1.

### 2.2 Enforcement and Gates
Style: tools/lint/style.sh
Context: tools/lint/context.sh
Truth: tools/lint/truth.sh
DP lint: tools/lint/dp.sh (enforces DP transaction rules in Section 3; does not enforce TASK container schema)
TASK lint: tools/lint/task.sh (sole TASK surface/container schema enforcer)
Repo verify: tools/verify.sh
llms parity: tools/lint/llms.sh

## 3. Current Dispatch Packet (DP)

Section-by-section authoring protocol:
- Generate DP structure from `ops/src/surfaces/dp.md.tpl` using `ops/bin/draft`.
- Do not hand-edit DP structural boilerplate in `TASK.md` after generation.
- Manual edits are limited to slot content only (objective, scope, safety, plan body, receipt body).
- Freeze each completed subsection in order (3.1 -> 3.2 -> 3.3 -> 3.4.x).
- Do not revise earlier sections to justify later work. If scope changes, end the thread and draft a new DP.

Freeze Rule:
- If a completed DP subsection changes later, append an AMENDMENT block in RESULTS; otherwise DP is FAIL.

AMENDMENT: minimum fields
- Reopened section:
- Reason:
- Impact:
- Allowlist change (none or exact paths):

Input discipline (hard rule):
- Worker receives only the DP text. Worker does not require OPEN or DUMP attachments.
- DP writer must not attach or cite disposable artifacts (rough notes, chat logs, scratch drafts, throwaway excerpts).
- DP writer must not include pasted bundles (OPEN, DUMP payloads, manifests) inside the DP body.
- Canon pointers only. Evidence belongs in storage artifacts and RESULTS.



### DP-OPS-0229: Remove DP Lint Branch-Name and Drafting-Marker Overreach

## 3.1 Freshness Gate (Must Pass Before Work)
Base Branch: main
Required Work Branch: work/dp-ops-0229-dp-lint-overreach-2026-03-24
Base HEAD: 4cf4a7ab
Freshness Stamp: 2026-03-24
Note: FRESHNESS_STAMP must be YYYY-MM-DD format only. No trace tokens, no timestamps, no other text. Certify rejects all other forms. dp.sh enforces at lint time.

Required local re-check (worker runs before any edits; paste verbatim outputs under `## Contractor Execution Narrative` -> `### Preflight State` in RESULTS):
- git rev-parse --abbrev-ref HEAD
- git rev-parse --short HEAD
- git status --porcelain

These three commands are freshness proof only. Do not treat §3.4.5 receipt replay as a substitute for their pre-edit outputs.

Preconditions:
- No commits on main.
- Working tree must be clean before execution begins.
- If Base HEAD changes, regenerate session artifacts from canonical tools before proceeding.

STOP conditions:
- STOP if any mismatch (branch, Base HEAD, missing work branch).
- STOP if working tree is dirty before execution begins.
- STOP if told to create or switch branches.

## 3.1.1 DP Preflight Gate (Run Before Any Edits)
Purpose:
- Catch malformed DP or TASK schema before work begins.

Worker runs (paste outcome in RESULTS):
- bash tools/lint/dp.sh --test
- bash tools/lint/dp.sh TASK.md
- bash tools/lint/task.sh

STOP if any preflight check fails.

### CbC Design Discipline Preflight (TASK.md §3.1.1)
Applicable. This packet modifies `tools/lint/dp.sh`, a linter and enforcement binary. Run `./ops/bin/decision create --dp=DP-OPS-0229 --type=cbc --status=accepted --out=auto` before closeout verification. The generated leaf is covered by the existing `archives/decisions/RoR-????-??-??-???-cbc-*.md` allowlist wildcard; no additional allowlist entry is required.

## 3.2 Required Context Load (Read Before Doing Anything)

### 3.2.1 Canon load order (always)
Worker must confirm loaded before edits begin:
1. PoT.md
2. SoP.md
3. PoW.md
4. TASK.md
5. docs/MAP.md
6. docs/MANUAL.md
7. ops/lib/manifests/CONTEXT.md

Notes:
- Worker does not read OPEN. OPEN is for Integrator state refresh and for receipts.
- Disposable artifacts must not be referenced or included.
- Worker input is the DP text only.
- DP writer must not attach or cite disposable artifacts.
- DP writer must not embed pasted bundles.

## Rules

1 Temp path rule uses `var/tmp` only
2 RESULTS generation rule uses `ops/bin/certify` only
3 Receipt rule runs every DP mandated command and treats any skip as STOP
4 Disposable artifact rule treats OPEN dump bundles and manifests as non canonical session artifacts
5 Use repo-relative paths only in all output. Never print absolute filesystem paths. Do not emit clickable file links.

### 3.2.2 DP-scoped load order (per DP)
- `tools/lint/dp.sh` — current lint implementation being corrected
- `docs/ops/specs/tools/lint/dp.md` — coupled spec being corrected
- `PoT.md` §6.2.3 — branch-naming authority cited in the corrected branch-form check
- `PoT.md` §6.3 — provisional-value marking authority cited in the corrected drafting-marker check

## 3.3 Scope and Safety
Objective:
Remove the packet-id-fragment requirement from `check_dp_packet_coherence()` in `tools/lint/dp.sh` and replace the raw substring drafting-marker scan in `check_proposed_token_scan()` with a pattern-aware check that does not false-positive on canonical schema identifiers. Align `docs/ops/specs/tools/lint/dp.md` to match both corrections.

In scope:
- `tools/lint/dp.sh`: `check_dp_packet_coherence()`, `check_proposed_token_scan()`, and `run_test()` fixture updates for both affected checks
- `docs/ops/specs/tools/lint/dp.md`: Mechanics step 3, Packet Coherence section, Drafting-Marker Scan Fixtures section, Closeout and Coherence Fixtures section

Out of scope:
- `tools/lint/dp.sh` hash constants (`CANONICAL_DP_TEMPLATE_SHA256`, `CANONICAL_ADDENDUM_TEMPLATE_SHA256`) and all other lint checks not listed above
- Any `docs/ops/specs/tools/lint/dp.md` section not directly coupled to the two corrected checks
- All other lint scripts, binaries, or specs not explicitly listed in scope
- `PoT.md`, `TASK.md`, `SoP.md`, `PoW.md` — no semantic changes to these surfaces

Safety and invariants:
- Do not change `CANONICAL_DP_TEMPLATE_SHA256` or `CANONICAL_ADDENDUM_TEMPLATE_SHA256` in `tools/lint/dp.sh`
- Do not change any lint check in `tools/lint/dp.sh` other than `check_dp_packet_coherence()` and `check_proposed_token_scan()` and their directly coupled test fixtures in `run_test()`
- The corrected drafting-marker scan must still fail on value-level usage of the drafting-marker keyword in prose, as a value prefix, and as a branch-style value; correction scope is false-positive suppression only
- The corrected branch check must still fail on work-branch values that do not follow `work/<topic>-YYYY-MM-DD` form, and must still reject missing or placeholder branch values
- Do not remove or weaken the closing-sidecar path check or sidecar id coherence check in `check_dp_packet_coherence()`
- Allowlist hard gate: `storage/dp/active/allowlist.txt`

Worker Constraints (SSOT injected):
## Section 1: Universal Template Rules
- Template source uses `tpl` files with optional YAML frontmatter
- Canon frontmatter keys: `template_type` `template_id` `template_version` `requires_slots` `includes`
- Renderer strips frontmatter before output write
- Slot token form `\{\{TOKEN\}\}` with uppercase alphanumeric underscore
- Include forms `\{\{@include:path\}\}` and `\{\{@include:path#section\}\}`
- Include resolution is strict: missing file fail missing section fail circular graph fail
- Strict mode default: every required slot value present and no unresolved token
- Non strict mode allowed only for lint and normalization workflows
- Worker facing generated surfaces remain pointer first and exclude disposable artifacts

Target Files allowlist (hard gate):
- storage/dp/active/allowlist.txt

## 3.4 Execution Plan (A-E)

### 3.4.1 State
- Branch: main, HEAD `4cf4a7ab`. Working tree is clean.
- `tools/lint/dp.sh` `check_dp_packet_coherence()` derives the lowercase form of the heading packet id and requires `Required Work Branch` to contain that fragment. PoT §6.2.3 authorizes `work/<topic>-YYYY-MM-DD` without a packet-id-fragment constraint; the fragment requirement is a local overreach.
- `tools/lint/dp.sh` `check_proposed_token_scan()` applies a raw substring match against every line of the DP payload. This match false-positives when the drafting-marker keyword appears as a prefix component of an uppercase underscore-connected schema identifier in the canonical DP template. That schema identifier is a legitimate slot name and must not trigger the scan.
- `docs/ops/specs/tools/lint/dp.md` describes both overreach behaviors as intended contract and is therefore out of sync with the corrected rules.
- `tools/lint/dp.sh` and `docs/ops/specs/tools/lint/dp.md` are both already present in `storage/dp/active/allowlist.txt`; no allowlist mutation is required for this packet.

### 3.4.2 Request
1. In `tools/lint/dp.sh` `check_dp_packet_coherence()`: remove the `expected_fragment` derivation and the conditional that tests whether `work_branch` contains the packet-id fragment. Replace with a regex form check: when `work_branch` is non-empty and `contains_placeholder "$work_branch"` returns false, require `work_branch` to match `^work/[^[:space:]]+-[0-9]{4}-[0-9]{2}-[0-9]{2}$`; emit a failure citing `PoT.md §6.2.3`. Preserve the closing-sidecar path check and sidecar id coherence check in `check_dp_packet_coherence()` unchanged.
2. In `tools/lint/dp.sh` `check_proposed_token_scan()`: introduce a `scrubbed` local variable. For each `line` read from the file, compute `scrubbed` by running the line through a sed substitution that strips occurrences of the drafting-marker keyword immediately followed by an underscore and one or more uppercase letters or underscores (the schema-identifier form). Test `scrubbed` against the drafting-marker keyword pattern instead of the raw `line`. On a match, the `echo` output must reference the original `line` content (not `scrubbed`) so the reported text is readable.
3. In `tools/lint/dp.sh` `run_test()` coherence fixture block: change the sed substitution from injecting `work/dp-ops-9999-2026-02-14` to injecting `work/no-date-fragment` (a branch that fails the new form check); update the associated `echo "FAIL: ..."` message from "packet-coherence work-branch mismatch failure" to "packet-coherence work-branch form failure".
4. In `tools/lint/dp.sh` `run_test()`: add a schema-identifier pass fixture immediately after the existing coherence fixture cleanup. Allocate `tmp_proposed_schema_ok="$(mktemp)"`, copy `tmp_valid` into it, append a line containing the schema-identifier form of the drafting-marker keyword (keyword immediately followed by `_WORK_BRANCH`) to the file, reset `failures=0`, call `check_proposed_token_scan "$tmp_proposed_schema_ok" >/dev/null 2>&1`, and if `failures` is non-zero print `FAIL: --test expected schema-identifier form to pass drafting-marker scan` to stderr and exit 1; then `rm -f "$tmp_proposed_schema_ok"`. Add `"$tmp_proposed_schema_ok"` to all existing `rm -f` cleanup lists in `run_test()` that include `"$tmp_proposed_prose_ok"`.
5. In `docs/ops/specs/tools/lint/dp.md` Mechanics step 3: replace the description stating the scan checks every line for "the literal drafting-marker word" with a description stating the scan is pattern-aware: before testing each line, the script strips schema-identifier occurrences (the drafting-marker keyword immediately followed by an underscore and one or more uppercase letters or underscores) using sed; a line where the keyword only appears within such schema identifiers passes; a line where the keyword remains after stripping fails; citation is `PoT.md §6.3`; no prose-only exception applies to value-level usage.
6. In `docs/ops/specs/tools/lint/dp.md` Packet Coherence section `check_dp_packet_coherence()` first sub-bullet: replace "requires `Required Work Branch` to include the lowercase packet-id fragment (for example `DP-OPS-0176` -> `dp-ops-0176`)" with "requires `Required Work Branch` to follow `work/<topic>-YYYY-MM-DD` form (PoT.md §6.2.3)". Remove all packet-id-fragment language from that bullet.
7. In `docs/ops/specs/tools/lint/dp.md` Drafting-Marker Scan Fixtures section: append a third fixture class after the existing two: "**3. Schema-identifier form (PASS):** a fixture line includes the drafting-marker keyword only as part of a canonical uppercase-underscore-connected schema identifier; the scan must pass."
8. In `docs/ops/specs/tools/lint/dp.md` Closeout and Coherence Fixtures section fixture 2: change the label from "Work-branch coherence mismatch (FAIL)" to "Invalid work-branch form (FAIL)" and replace the body from "heading id and `Required Work Branch` id fragment disagree and must fail" with "`Required Work Branch` does not follow `work/<topic>-YYYY-MM-DD` form (PoT.md §6.2.3) and must fail".
9. Verify that `tools/lint/dp.sh` and `docs/ops/specs/tools/lint/dp.md` are already present in `storage/dp/active/allowlist.txt` before running `bash tools/lint/integrity.sh`; no additions are needed.

### 3.4.3 Changelog
- UPDATE tools/lint/dp.sh
- UPDATE docs/ops/specs/tools/lint/dp.md

### 3.4.4 Patch / Diff
1. `tools/lint/dp.sh` — `check_dp_packet_coherence()`: remove the block that sets `expected_fragment` via `printf '%s' "$dp_id" | tr '[:upper:]' '[:lower:]'` and the `if [[ -n "$work_branch" && "$work_branch" != *"${expected_fragment}"* ]]; then fail ...; fi` block. In its place add: `if [[ -n "$work_branch" ]] && ! contains_placeholder "$work_branch"; then if [[ ! "$work_branch" =~ ^work/[^[:space:]]+-[0-9]{4}-[0-9]{2}-[0-9]{2}$ ]]; then fail "Required Work Branch must follow 'work/<topic>-YYYY-MM-DD' form (PoT.md §6.2.3): ${work_branch}"; fi; fi`. Leave all lines of `check_dp_packet_coherence()` not related to the fragment check unchanged.
2. `tools/lint/dp.sh` — `check_proposed_token_scan()`: after `local line=""` add `local scrubbed=""`. Inside the while loop body, before the pattern test, insert `scrubbed="$(printf '%s' "$line" | sed 's/<DRAFTING_MARKER_KEYWORD>_[A-Z][A-Z_]*/SCHEMA_ID/g')"` where `<DRAFTING_MARKER_KEYWORD>` is replaced with the actual keyword string appearing in the existing `*KEYWORD*` test. Change `if [[ "$line" == *<KEYWORD>* ]]; then` to `if [[ "$scrubbed" == *<KEYWORD>* ]]; then`. Keep `echo "... ${line} ..."` referencing `$line` (not `$scrubbed`) so reported output shows the original content.
3. `tools/lint/dp.sh` — `run_test()` coherence fixture: change `sed -i 's#^Required Work Branch: .*#Required Work Branch: work/dp-ops-9999-2026-02-14#' "$tmp_coherence_bad"` to `sed -i 's#^Required Work Branch: .*#Required Work Branch: work/no-date-fragment#' "$tmp_coherence_bad"`. Change `echo "FAIL: --test expected packet-coherence work-branch mismatch failure"` to `echo "FAIL: --test expected packet-coherence work-branch form failure"`.
4. `tools/lint/dp.sh` — `run_test()` new fixture (insert after `rm -f "$tmp_coherence_bad"`): allocate `tmp_proposed_schema_ok="$(mktemp)"`, copy `tmp_valid` into it via `cp "$tmp_valid" "$tmp_proposed_schema_ok"`, append the schema-identifier line via `printf '%s\n' '# The <KEYWORD>_WORK_BRANCH slot is a schema identifier.' >> "$tmp_proposed_schema_ok"` (using the actual keyword), reset `failures=0`, call `check_proposed_token_scan "$tmp_proposed_schema_ok" >/dev/null 2>&1`, if `(( failures ))` then cleanup and `echo "FAIL: --test expected schema-identifier form to pass drafting-marker scan" >&2; exit 1`, then `rm -f "$tmp_proposed_schema_ok"`. Add `"$tmp_proposed_schema_ok"` to each existing `rm -f` list in `run_test()` that contains `"$tmp_proposed_prose_ok"`.
5. `docs/ops/specs/tools/lint/dp.md` — Mechanics step 3: rewrite the drafting-marker scan sentence from "The scan checks every line for the literal drafting-marker word; if the word appears anywhere in the rendered DP text the scan reports each matching line number and full offending line to stderr and exits non-zero" to "The scan is pattern-aware (PoT.md §6.3): before testing each line for the drafting-marker keyword, occurrences of the schema-identifier form — the keyword immediately followed by an underscore and one or more uppercase letters or underscores — are stripped via sed; a line where the keyword only appears within such schema identifiers passes; a line where the keyword remains after stripping fails, with the line number and original content reported to stderr." Retain "There is no prose-only exception" for value-level usage.
6. `docs/ops/specs/tools/lint/dp.md` — Packet Coherence section, `check_dp_packet_coherence()` first sub-bullet: replace the text "requires `Required Work Branch` to include the lowercase packet-id fragment (for example `DP-OPS-0176` -> `dp-ops-0176`)" with "requires `Required Work Branch` to follow `work/<topic>-YYYY-MM-DD` form (PoT.md §6.2.3)".
7. `docs/ops/specs/tools/lint/dp.md` — Drafting-Marker Scan Fixtures: after the "2. **Drafting-marker present (FAIL):**" block, add: "3. **Schema-identifier form (PASS):** a fixture line includes the drafting-marker keyword only as part of a canonical uppercase-underscore-connected schema identifier; the scan must pass."
8. `docs/ops/specs/tools/lint/dp.md` — Closeout and Coherence Fixtures: change fixture 2 label and body: "2. **Invalid work-branch form (FAIL):** `Required Work Branch` does not follow `work/<topic>-YYYY-MM-DD` form (PoT.md §6.2.3) and must fail."
9. Verify allowlist coverage: confirm `tools/lint/dp.sh` and `docs/ops/specs/tools/lint/dp.md` are present as exact entries in `storage/dp/active/allowlist.txt`; make no changes to the allowlist.

### 3.4.5 Receipt (Proofs to collect) - MUST RUN

**Mandatory receipt commands (always run; do not omit):**
- bash tools/lint/dp.sh TASK.md
- bash tools/lint/task.sh
- bash tools/lint/integrity.sh
- bash tools/lint/style.sh
- ./ops/bin/llms
- bash tools/lint/integrity.sh
- git diff --name-only
- git diff --stat
- comm -23 <(git diff --name-only | sort) <(sort storage/dp/active/allowlist.txt) || true
- comm -23 <(git ls-files --others --exclude-standard | sort) <(sort storage/dp/active/allowlist.txt) || true
- ./ops/bin/open

**DP-specific receipt commands (scope-specific; author below):**
Note: Command substitution forms (e.g., $(pwd), $(git ...)) are rejected by certify replay. Use literal values only. Repo-local binaries must be authored as literal `./ops/bin/...` or `ops/bin/...` commands. Unsupported receipt-command forms are rejected by dp.sh and certify.
- bash tools/lint/dp.sh --test
- bash tools/lint/dp.sh storage/dp/intake/DP.md

## 3.5 Closeout (Mandatory Routing)
- Execute docs/MANUAL.md Closeout Cycle in order (Verify, Harvest, Refresh, Log, Prune).
- Update SoP.md and PoW.md with DP entries, including objective summary and current proof / receipt summary.
- Protocol order for closeout: Verify -> Generate Results -> COMMIT (Operator Only) -> Prune.
- Run prune hygiene: ./ops/bin/prune --scrub.
- Refresh llms artifacts: `./ops/bin/llms`
- Regenerate session artifacts: `./ops/bin/open --out=auto`
- Ensure the next session begins with refreshed session artifacts and matching receipts.
- Note: audit dump generation is owned by `./ops/bin/bundle --profile=audit --out=auto` and is separate from operator session refresh. Do not stamp an additional `ops/bin/dump --scope=core` into closeout unless the DP explicitly authorizes a standalone state capture.
- Refresh side-effect: `ops/bin/llms` regenerates `ops/lib/manifests/OPS.md` as a compile event; if `OPS.md` is not in the allowlist, restore it before running `integrity.sh`. See `docs/MANUAL.md` Refresh side-effect notice for the full procedure.

### 3.5.1 Mandatory Closing Sidecar
Closing sidecar content is generated and maintained at `storage/handoff/CLOSING.md` and is validated by `ops/bin/certify` as a hard gate at certification time. Do not author, predict, populate, or approximate any sidecar-derived closeout output outside this sidecar.
Certify separately collects contractor-authored narrative for the RESULTS Contractor Execution Narrative section at certify time via interactive editor prompt; this narrative input is independent from closing sidecar content.

Before running certify, confirm `storage/handoff/CLOSING.md` has been maintained throughout execution and reflects observable reality only.

Closing sidecar fields required at certify time:
- Commit Message
- Create Pull Request (Title)
- Create Pull Request (Description)
- Confirm Merge (Commit Message)
- Confirm Merge (Extended Description)
- Confirm Merge (Add a Comment)
