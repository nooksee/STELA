---
trace_id: stela-20260315T041841Z-511650a5
packet_id: DP-OPS-0194
created_at: 2026-03-15T04:19:08Z
previous: archives/surfaces/TASK-DP-OPS-0193-b1860a05.md
---
# STELA TASK DASHBOARD
Status: ACTIVE
Owner: Integrator
Last Updated: 2026-03-02

## 1. Session State
Pointer: Session context output (generated by ops/bin/open)
Context Manifest: ops/lib/manifests/CONTEXT.md (checked by tools/lint/context.sh)

Session State contract:
- Session State is pointer-first. Do not write inline branch or hash state here.
- OPEN and OPEN-PORCELAIN are the session state snapshot.
- OPEN-PORCELAIN is emitted on every OPEN run. Clean sessions produce a zero-line artifact.
- RESULTS is the audit record for the DP.
- TASK is the stable routing surface.
- Operator note (2026-02-17): Do not edit `SoP.md` or `PoW.md` unless explicitly instructed.

## 2. Logic Pointers
Primary Constraint: PoT.md (Policy of Truth).
Surface Contract Spec: docs/ops/specs/surfaces/task.md.

### 2.1 Governance Pointers
Jurisdiction: PoT.md Section 3.
Git Authority: PoT.md Section 4.1.
Behavioral Standard: PoT.md Section 4.2.
Staffing Protocol: PoT.md Section 4.1.

### 2.2 Enforcement and Gates
Style: tools/lint/style.sh
Context: tools/lint/context.sh
Truth: tools/lint/truth.sh
DP lint: tools/lint/dp.sh (enforces DP transaction rules in Section 3; does not enforce TASK container schema)
TASK lint: tools/lint/task.sh (sole TASK surface/container schema enforcer)
Repo verify: tools/verify.sh
llms parity: tools/lint/llms.sh

## 3. Current Dispatch Packet (DP)

Section-by-section authoring protocol:
- Generate DP structure from `ops/src/surfaces/dp.md.tpl` using `ops/bin/draft`.
- Do not hand-edit DP structural boilerplate in `TASK.md` after generation.
- Manual edits are limited to slot content only (objective, scope, safety, plan body, receipt body).
- Freeze each completed subsection in order (3.1 -> 3.2 -> 3.3 -> 3.4.x).
- Do not revise earlier sections to justify later work. If scope changes, end the thread and draft a new DP.

Freeze Rule:
- If a completed DP subsection changes later, append an AMENDMENT block in RESULTS; otherwise DP is FAIL.

AMENDMENT: minimum fields
- Reopened section:
- Reason:
- Impact:
- Allowlist change (none or exact paths):

Input discipline (hard rule):
- Worker receives only the DP text. Worker does not require OPEN or DUMP attachments.
- DP writer must not attach or cite disposable artifacts (rough notes, chat logs, scratch drafts, throwaway excerpts).
- DP writer must not include pasted bundles (OPEN, DUMP payloads, manifests) inside the DP body.
- Canon pointers only. Evidence belongs in storage artifacts and RESULTS.


### DP-OPS-0194: Profile Disposable Input Transport

## 3.1 Freshness Gate (Must Pass Before Work)
Base Branch: work/dp-ops-0193-analyst-dump-topic-inclusion-2026-03-14
Required Work Branch: work/dp-ops-0194-profile-disposable-input-transport-2026-03-14
Base HEAD: b1860a05
Freshness Stamp: 2026-03-14
Note: FRESHNESS_STAMP must be YYYY-MM-DD format only. No trace tokens, no timestamps, no other text. Certify rejects all other forms. dp.sh enforces at lint time.

Required local re-check (worker runs; paste outputs in RESULTS):
- git rev-parse --abbrev-ref HEAD
- git rev-parse --short HEAD
- git status --porcelain

Preconditions:
- No commits on main.
- Working tree must be clean before execution begins.
- If Base HEAD changes, regenerate session artifacts from canonical tools before proceeding.

STOP conditions:
- STOP if any mismatch (branch, Base HEAD, missing work branch).
- STOP if working tree is dirty before execution begins.
- STOP if told to create or switch branches.

## 3.1.1 DP Preflight Gate (Run Before Any Edits)
Purpose:
- Catch malformed DP or TASK schema before work begins.

Worker runs (paste outcome in RESULTS):
- bash tools/lint/dp.sh --test
- bash tools/lint/dp.sh TASK.md
- bash tools/lint/task.sh

STOP if any preflight check fails.

### CbC Design Discipline Preflight (TASK.md §3.1.1)
Required when the DP objective adds, modifies, or replaces a linter, script, guard, or validation binary.
For non-tooling DPs: state "Not applicable" with a one-line justification.
Applicable. This packet hardens disposable runtime transport. The operator should not have to remember separate disposable attachments when the repo already has a bundle binary and exact knowledge of the files a mode depends on. Structural prevention is to make bundle transport carry current disposable inputs through one profile-scoped exact-file mechanism: analyst topic, architect plan, and audit results/closing. Detection still matters because profile wiring, dump inclusion, and smoke proofs can drift unless they are checked together.

## 3.2 Required Context Load (Read Before Doing Anything)

### 3.2.1 Canon load order (always)
Worker must confirm loaded before edits begin:
1. PoT.md
2. SoP.md
3. PoW.md
4. TASK.md
5. docs/MAP.md
6. docs/MANUAL.md
7. ops/lib/manifests/CONTEXT.md

Notes:
- Worker does not read OPEN. OPEN is for Integrator state refresh and for receipts.
- Disposable artifacts must not be referenced or included.
- Worker input is the DP text only.
- DP writer must not attach or cite disposable artifacts.
- DP writer must not embed pasted bundles.

## Rules

1 Temp path rule uses `var/tmp` only
2 RESULTS generation rule uses `ops/bin/certify` only
3 Receipt rule runs every DP mandated command and treats any skip as STOP
4 Disposable artifact rule treats OPEN dump bundles and manifests as non canonical session artifacts
5 Use repo-relative paths only in all output. Never print absolute filesystem paths. Do not emit clickable file links.

### 3.2.2 DP-scoped load order (per DP)
- ops/lib/scripts/bundle.sh
- ops/lib/manifests/BUNDLE.md
- docs/ops/specs/binaries/bundle.md
- docs/ops/specs/scripts/bundle.md
- tools/test/bundle.sh
- docs/ops/specs/tools/test/bundle.md
- storage/dp/active/allowlist.txt

## 3.3 Scope and Safety
Objective:
- Replace ad hoc disposable attachment burden with one bundle-managed, profile-scoped exact-file transport mechanism for current disposable inputs.
- Make the current required disposable set self-carrying in dump payloads and package members: analyst `storage/handoff/TOPIC.md`, architect `storage/handoff/PLAN.md`, and audit `storage/handoff/<DP_ID>-RESULTS.md` plus `storage/handoff/CLOSING-<DP_ID>.md`.

In scope:
- UPDATE `ops/lib/scripts/bundle.sh` so profile-specific disposable input files are resolved through one exact-file transport path and are added to dump payloads and package members for the current live set.
- UPDATE `ops/lib/scripts/bundle.sh` so analyst continues to require `storage/handoff/TOPIC.md`, architect includes `storage/handoff/PLAN.md`, and audit resolves and includes the current DP `RESULTS` and `CLOSING` files from current session state.
- UPDATE `ops/lib/manifests/BUNDLE.md`, `docs/ops/specs/binaries/bundle.md`, and `docs/ops/specs/scripts/bundle.md` so bundle policy and transport docs describe the current disposable file set and the future extension rule: exact file additions only.
- UPDATE `tools/test/bundle.sh` and `docs/ops/specs/tools/test/bundle.md` so analyst, architect, and audit bundle smoke prove the expected disposable file blocks and package members are present for each profile.
- UPDATE `storage/dp/active/allowlist.txt` with every tracked file touched by this packet.

Out of scope:
- Generic inclusion of `storage/`, `storage/handoff/`, or directory-level disposable sweeps.
- Pattern/glob-based disposable transport.
- Changes to architect packet-id derivation, slice validation, title suffix derivation, or active-slice projection semantics.
- Changes to analyst stance or analyst output contract from DP-OPS-0191.
- Changes to `ops/bin/dump`; explicit include support already exists and is not being widened here.
- Changes to generated artifacts under `storage/handoff/`, `storage/dumps/`, or archives.

Safety and invariants:
- Disposable transport remains file-level and profile-scoped only.
- Analyst input remains `storage/handoff/TOPIC.md`; analyst output remains `storage/handoff/PLAN.md`.
- Architect routing remains driven by `storage/handoff/PLAN.md` and existing request metadata.
- Audit receipt authority remains `storage/handoff/<DP_ID>-RESULTS.md`; the closing sidecar remains `storage/handoff/CLOSING-<DP_ID>.md`.
- Execution-decision remains a separate workflow-specific diagnostic surface and is not part of this generic disposable input transport mechanism.
- Future disposable additions must enter through exact-file policy wiring plus test proof; no implicit widening.
- Non-required disposable files must not appear in a profile dump or package by accident.
- Allowlist hard gate remains authoritative for every touched path.

Worker Constraints (SSOT injected):
## Section 1: Universal Template Rules
- Template source uses `tpl` files with optional YAML frontmatter
- Canon frontmatter keys: `template_type` `template_id` `template_version` `requires_slots` `includes`
- Renderer strips frontmatter before output write
- Slot token form `\{\{TOKEN\}\}` with uppercase alphanumeric underscore
- Include forms `\{\{@include:path\}\}` and `\{\{@include:path#section\}\}`
- Include resolution is strict: missing file fail missing section fail circular graph fail
- Strict mode default: every required slot value present and no unresolved token
- Non strict mode allowed only for lint and normalization workflows
- Worker facing generated surfaces remain pointer first and exclude disposable artifacts

Target Files allowlist (hard gate):
- storage/dp/active/allowlist.txt

## 3.4 Execution Plan (A-E)

### 3.4.1 State
- Analyst now embeds `storage/handoff/TOPIC.md` in its full dump, but the bundle transport still treats disposable file carriage as one-off profile logic instead of a coherent exact-file mechanism.
- Architect still relies on separate `storage/handoff/PLAN.md` attachment in the normal non-tar workflow even though the bundle already knows that file is architect input.
- Audit still relies on separately surfaced disposable runtime files even though the current DP `RESULTS` receipt and `CLOSING` sidecar are exact inputs to the audit workflow.
- Bundle package membership is still partly generic (`TOPIC.md` / `PLAN.md` presence) instead of being driven by profile-specific disposable input rules.

### 3.4.2 Request
- Replace one-off disposable transport with one profile-scoped exact-file mechanism in bundle runtime.
- Carry the current required disposable set automatically:
  - analyst: `storage/handoff/TOPIC.md`
  - architect: `storage/handoff/PLAN.md`
  - audit: current `storage/handoff/<DP_ID>-RESULTS.md` and `storage/handoff/CLOSING-<DP_ID>.md`
- Keep the mechanism exact-file only so future disposable files can be added narrowly without widening transport behavior.
- If audit current-DP resolution becomes ambiguous or the change starts dragging unrelated disposable files into profile dumps/packages, STOP and tighten the resolution path before widening behavior.

### 3.4.3 Changelog
UPDATE:
- ops/lib/scripts/bundle.sh
- ops/lib/manifests/BUNDLE.md
- docs/ops/specs/binaries/bundle.md
- docs/ops/specs/scripts/bundle.md
- tools/test/bundle.sh
- docs/ops/specs/tools/test/bundle.md
- storage/dp/active/allowlist.txt

### 3.4.4 Patch / Diff
1. Update `storage/dp/active/allowlist.txt` first with every tracked file in this packet and the active closing-sidecar path for `DP-OPS-0194`.
2. Refactor `ops/lib/scripts/bundle.sh` so disposable input carriage is driven by one profile-scoped exact-file resolution path instead of scattered per-profile special cases and generic topic/plan packaging.
3. Wire the current live disposable set through that path:
   - analyst `storage/handoff/TOPIC.md`
   - architect `storage/handoff/PLAN.md`
   - audit current `storage/handoff/<DP_ID>-RESULTS.md` and `storage/handoff/CLOSING-<DP_ID>.md`
4. Preserve current analyst fail-closed behavior when `storage/handoff/TOPIC.md` is missing.
5. Preserve current architect request metadata and active-slice projection behavior exactly as-is.
6. Resolve the current audit DP identity from current session state deterministically enough to include the correct `RESULTS` and `CLOSING` files without operator attachment burden.
7. Update `ops/lib/manifests/BUNDLE.md`, `docs/ops/specs/binaries/bundle.md`, and `docs/ops/specs/scripts/bundle.md` so they describe the exact current disposable file set and the extension rule for future file-level additions.
8. Extend `tools/test/bundle.sh` and `docs/ops/specs/tools/test/bundle.md` so bundle smoke proves analyst, architect, and audit all carry the expected disposable inputs in dump payloads and package members.
9. Run the targeted lint and test stack, then collect receipts showing the current disposable files are transport-carried without separate operator attachment.

### 3.4.5 Receipt (Proofs to collect) - MUST RUN

**Mandatory receipt commands (always run; do not omit):**
- bash tools/lint/dp.sh TASK.md
- bash tools/lint/task.sh
- bash tools/lint/integrity.sh
- bash tools/lint/style.sh
- ./ops/bin/llms
- bash tools/lint/integrity.sh
- git diff --name-only
- git diff --stat
- comm -23 <(git diff --name-only | sort) <(sort storage/dp/active/allowlist.txt) || true
- comm -23 <(git ls-files --others --exclude-standard | sort) <(sort storage/dp/active/allowlist.txt) || true
- ./ops/bin/open

**DP-specific receipt commands (scope-specific; author below):**
Note: Command substitution forms (e.g., $(pwd), $(git ...)) are rejected by certify replay. Use literal values only. dp.sh enforces at lint time.
- bash tools/lint/dp.sh --test
- bash tools/test/bundle.sh
- bash tools/verify.sh --mode=certify-critical
- ./ops/bin/bundle --profile=analyst --out=auto
- ./ops/bin/bundle --profile=architect --out=auto
- grep -n "TOPIC.md\|PLAN.md\|RESULTS.md\|CLOSING-" ops/lib/scripts/bundle.sh ops/lib/manifests/BUNDLE.md docs/ops/specs/binaries/bundle.md docs/ops/specs/scripts/bundle.md
- grep -n "FILE BEGIN: storage/handoff/TOPIC.md\|FILE BEGIN: storage/handoff/PLAN.md\|RESULTS.md\|CLOSING-" tools/test/bundle.sh docs/ops/specs/tools/test/bundle.md

## 3.5 Closeout (Mandatory Routing)
- Execute docs/MANUAL.md Closeout Cycle in order (Verify, Harvest, Refresh, Log, Prune).
- Update SoP.md and PoW.md with DP entries, including objective summary and verification commands run.
- Protocol order for closeout: Verify -> Generate Results -> COMMIT (Operator Only) -> Prune.
- Run prune hygiene: ./ops/bin/prune --scrub.
- Refresh llms artifacts: `./ops/bin/llms`
- Regenerate session artifacts: `./ops/bin/open --out=auto`
- Capture updated Ops state: `./ops/bin/dump --scope=core --format=chatgpt --out=auto`
- Ensure the next session begins with refreshed session artifacts and matching receipts.
- Refresh side-effect: `ops/bin/llms` regenerates `ops/lib/manifests/OPS.md` as a compile event; if `OPS.md` is not in the allowlist, restore it before running `integrity.sh`. See `docs/MANUAL.md` Refresh side-effect notice for the full procedure.

### 3.5.1 Mandatory Closing Sidecar
Closing sidecar content is generated and maintained at `storage/handoff/CLOSING-DP-OPS-0194.md` and is validated by `ops/bin/certify` as a hard gate at certification time. Do not author, predict, populate, or approximate any sidecar-derived closeout output outside this sidecar.
Certify separately collects contractor-authored narrative for the RESULTS Contractor Execution Narrative section at certify time via interactive editor prompt; this narrative input is independent from closing sidecar content.

Before running certify, confirm `storage/handoff/CLOSING-DP-OPS-0194.md` has been maintained throughout execution and reflects observable reality only.

Closing sidecar fields required at certify time:
- Commit Message
- Create Pull Request (Title)
- Create Pull Request (Description)
- Confirm Merge (Commit Message)
- Confirm Merge (Extended Description)
- Confirm Merge (Add a Comment)
