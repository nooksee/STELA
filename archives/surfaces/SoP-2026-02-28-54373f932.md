---
trace_id: stela-20260228T135914Z-4cab79e6
packet_id: DP-OPS-0133
created_at: 2026-02-28T13:59:55Z
previous: archives/surfaces/SoP-2026-02-28-dcb90a88b.md
---
## 2026-02-28 13:12:54 UTC - DP-OPS-0132 trace health checks and optional lint health mode
Objective summary:
Implemented Workstream B Slice B3 observability hardening for telemetry gaps:
- Added `health` to `ops/bin/trace` with deterministic, read-only reporting for
  `unclosed-run`, `unresolved-head`, and `malformed-filename` conditions.
- Added optional `--health` mode to `tools/lint/leaf.sh` that preserves default
  token-presence lint behavior and exits non-zero only when health gaps are detected.
- Updated `docs/ops/specs/binaries/trace.md` and
  `docs/ops/specs/tools/lint/leaf.md` to define the new command/flag behavior and
  opt-in enforcement model.
- Updated `docs/MANUAL.md` Trace Cookbook with a `Trace Health Check` recipe for
  standalone reporting and CI/manual gating.
- Updated `storage/dp/active/allowlist.txt` and produced
  `storage/dp/active/notes.md` via the canonical notes schema.

Verification commands:
- git rev-parse --abbrev-ref HEAD
- git rev-parse --short HEAD
- git status --porcelain
- bash tools/lint/dp.sh --test
- bash tools/lint/dp.sh TASK.md
- bash tools/lint/task.sh
- bash tools/lint/integrity.sh
- bash tools/lint/style.sh
- git diff --name-only
- git diff --stat
- comm -23 <(git diff --name-only | sort) <(sort storage/dp/active/allowlist.txt) || true
- comm -23 <(git ls-files --others --exclude-standard | sort) <(sort storage/dp/active/allowlist.txt) || true
- ./ops/bin/open
- bash tools/lint/leaf.sh
- bash tools/lint/leaf.sh --health
- ./ops/bin/trace health
- ./ops/bin/trace heads
- ./ops/bin/trace recent --limit=5
- wc -l ops/bin/trace
- wc -l tools/lint/leaf.sh
- grep -c "health" ops/bin/trace
- grep -c "health" tools/lint/leaf.sh
- grep -n "trace health" docs/ops/specs/binaries/trace.md
- grep -n "health" docs/ops/specs/tools/lint/leaf.md
- grep -n "trace health\|leaf.sh --health" docs/MANUAL.md
- ./tools/verify.sh
- ./tools/lint/truth.sh
- ./tools/lint/style.sh
- ./tools/lint/ff.sh
- ./tools/lint/context.sh
- ./tools/lint/agent.sh
- bash tools/lint/factory.sh
- ./ops/bin/map
- ./ops/bin/llms --out-dir=.
- ./ops/bin/prune --scrub
- ./ops/bin/open --out=auto
- ./ops/bin/dump --scope=platform --format=chatgpt --out=auto

Functional receipt summary:
All required freshness, preflight, lint, and DP-specific health checks executed.
`trace health` reports clean state after the optional lint health mode closes the
current lint invocation before scanning, and `leaf.sh --health` now provides the
intended opt-in non-zero gate without changing default mandatory lint behavior.
