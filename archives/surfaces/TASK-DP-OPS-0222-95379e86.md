---
trace_id: stela-20260323T002934Z-6d605bb0
packet_id: DP-OPS-0222
created_at: 2026-03-23T00:39:52Z
previous: (none)
---
# STELA TASK DASHBOARD
Status: ACTIVE
Owner: Integrator
Last Updated: 2026-03-19

## 1. Session State
Pointer: Session context output (generated by ops/bin/open)
Context Manifest: ops/lib/manifests/CONTEXT.md (checked by tools/lint/context.sh)

Session State contract:
- Session State is pointer-first. Do not write inline branch or hash state here.
- OPEN is the session state snapshot. OPEN-PORCELAIN is conditional dirty-state detail.
- RESULTS is the audit record for the DP.
- TASK is the stable routing surface.

## 2. Logic Pointers
Primary Constraint: PoT.md (Policy of Truth).
Surface Contract Spec: docs/ops/specs/surfaces/task.md.

### 2.1 Governance Pointers
Jurisdiction: PoT.md Section 3.
Git Authority: PoT.md Section 4.1.
Behavioral Standard: PoT.md Section 4.2.
Staffing Protocol: PoT.md Section 4.1.

### 2.2 Enforcement and Gates
Style: tools/lint/style.sh
Context: tools/lint/context.sh
Truth: tools/lint/truth.sh
DP lint: tools/lint/dp.sh (enforces DP transaction rules in Section 3; does not enforce TASK container schema)
TASK lint: tools/lint/task.sh (sole TASK surface/container schema enforcer)
Repo verify: tools/verify.sh
llms parity: tools/lint/llms.sh

## 3. Current Dispatch Packet (DP)

Section-by-section authoring protocol:
- Generate DP structure from `ops/src/surfaces/dp.md.tpl` using `ops/bin/draft`.
- Do not hand-edit DP structural boilerplate in `TASK.md` after generation.
- Manual edits are limited to slot content only (objective, scope, safety, plan body, receipt body).
- Freeze each completed subsection in order (3.1 -> 3.2 -> 3.3 -> 3.4.x).
- Do not revise earlier sections to justify later work. If scope changes, end the thread and draft a new DP.

Freeze Rule:
- If a completed DP subsection changes later, append an AMENDMENT block in RESULTS; otherwise DP is FAIL.

AMENDMENT: minimum fields
- Reopened section:
- Reason:
- Impact:
- Allowlist change (none or exact paths):

Input discipline (hard rule):
- Worker receives only the DP text. Worker does not require OPEN or DUMP attachments.
- DP writer must not attach or cite disposable artifacts (rough notes, chat logs, scratch drafts, throwaway excerpts).
- DP writer must not include pasted bundles (OPEN, DUMP payloads, manifests) inside the DP body.
- Canon pointers only. Evidence belongs in storage artifacts and RESULTS.



### DP-OPS-0222: Foreman Lane Evidence-First Reset

## 3.1 Freshness Gate (Must Pass Before Work)
Base Branch: main
Required Work Branch: work/dp-ops-0222-foreman-evidence-first-reset-2026-03-22
Base HEAD: 95379e86
Freshness Stamp: 2026-03-23
Note: FRESHNESS_STAMP must be YYYY-MM-DD format only. No trace tokens, no timestamps, no other text. Certify rejects all other forms. dp.sh enforces at lint time.

Required local re-check (worker runs; paste outputs in RESULTS):
- git rev-parse --abbrev-ref HEAD
- git rev-parse --short HEAD
- git status --porcelain

Preconditions:
- No commits on main.
- Working tree must be clean before execution begins.
- If Base HEAD changes, regenerate session artifacts from canonical tools before proceeding.

STOP conditions:
- STOP if any mismatch (branch, Base HEAD, missing work branch).
- STOP if working tree is dirty before execution begins.
- STOP if told to create or switch branches.

## 3.1.1 DP Preflight Gate (Run Before Any Edits)
Purpose:
- Catch malformed DP or TASK schema before work begins.

Worker runs (paste outcome in RESULTS):
- bash tools/lint/dp.sh --test
- bash tools/lint/dp.sh TASK.md
- bash tools/lint/task.sh

STOP if any preflight check fails.

### CbC Design Discipline Preflight (TASK.md §3.1.1)
Required when the DP objective adds, modifies, or replaces a linter, script, guard, or validation binary.
For non-tooling DPs: state "Not applicable" with a one-line justification.
Applicable. This packet modifies ops/lib/scripts/bundle.sh (a tooling binary) and ops/src/stances/foreman.md.tpl (a stance template consumed by the bundle tooling).

Invariants:
1. bundle_parse_foreman_intent must accept `ADDENDUM REQUIRED: DP-OPS-XXXX - <blocker>` and reject forms where the first field is not a canonical DP-OPS-NNNN packet identifier.
2. Foreman bundle assembly must confirm the base DP packet ID appears in the dump payload; die with a clear message if not.
3. All other bundle profiles (analyst, architect, audit, project, conform, hygiene) must remain unaffected.
4. Foreman manifest JSON must emit base_dp_id and base_dp_in_dump fields (replacing decision_id and decision_leaf_present).
5. The rendered foreman stance must not reference a pre-existing decision leaf as a required precondition.

## 3.2 Required Context Load (Read Before Doing Anything)

### 3.2.1 Canon load order (always)
Worker must confirm loaded before edits begin:
1. PoT.md
2. SoP.md
3. PoW.md
4. TASK.md
5. docs/MAP.md
6. docs/MANUAL.md
7. ops/lib/manifests/CONTEXT.md

Notes:
- Worker does not read OPEN. OPEN is for Integrator state refresh and for receipts.
- Disposable artifacts must not be referenced or included.
- Worker input is the DP text only.
- DP writer must not attach or cite disposable artifacts.
- DP writer must not embed pasted bundles.

## Rules

1 Temp path rule uses `var/tmp` only
2 RESULTS generation rule uses `ops/bin/certify` only
3 Receipt rule runs every DP mandated command and treats any skip as STOP
4 Disposable artifact rule treats OPEN dump bundles and manifests as non canonical session artifacts
5 Use repo-relative paths only in all output. Never print absolute filesystem paths. Do not emit clickable file links.

### 3.2.2 DP-scoped load order (per DP)
8. ops/lib/manifests/BUNDLE.md (lines 14-17: foreman_profile and foreman_intent_form; line 127: profile description)
9. ops/lib/scripts/bundle.sh (function bundle_parse_foreman_intent lines 744-752; lines 1238-1303: foreman intent parse and decision-leaf-present enforcement; lines 1400-1403: foreman bundle text emission; lines 1611-1618: foreman manifest JSON emission)
10. ops/src/stances/foreman.md.tpl (full file)
11. docs/ops/specs/stances/foreman.md (lines 31-38: Shipping Spine Position steps)
12. docs/ops/specs/binaries/addendum.md (lines 50-58: Foreman Intervention Chain)
13. docs/MANUAL.md (lines 51-53: Secondary Lanes foreman description)

## 3.3 Scope and Safety
Objective:
Remove the hidden prerequisite from the foreman intervention lane. Currently the lane requires a pre-existing RoR-* decision leaf ID in the bundle intent and validates that the leaf is present in the dump payload; if no leaf has been authored yet, the bundle dies and the lane is inaccessible. The fix: change the intent form so the first field is the base DP ID (DP-OPS-NNNN) instead of a decision leaf ID, validate that the base DP appears in the dump (natural for core scope), and update the foreman stance to build the addendum case from visible evidence in the dump rather than from a pre-existing leaf. Align BUNDLE.md, bundle.sh, foreman.md.tpl, and the three documentation surfaces to describe the same evidence-first model.

In scope:
- ops/lib/manifests/BUNDLE.md: change foreman_intent_form from `ADDENDUM REQUIRED: <DECISION_ID> - <ONE-LINE BLOCKER>` to `ADDENDUM REQUIRED: <BASE_DP_ID> - <ONE-LINE BLOCKER>`; update line 127 profile description to match
- ops/lib/scripts/bundle.sh: update bundle_parse_foreman_intent regex to require DP-OPS-NNNN pattern for the first field; rename BUNDLE_FOREMAN_DECISION_ID to BUNDLE_FOREMAN_BASE_DP_ID; rename decision_leaf_present to foreman_base_dp_in_dump; update the presence check error message; update foreman text emission (lines 1400-1403) and manifest JSON emission (lines 1611-1618) to use new field names
- ops/src/stances/foreman.md.tpl: replace the decision-leaf requirement rule with an evidence-first rule; update Steps 0-2 to remove the pre-existing-leaf precondition and describe building the case from visible evidence (RESULTS, OPEN metadata, boundary condition in intent)
- docs/ops/specs/stances/foreman.md: update Shipping Spine Position steps to reflect the new intent form and evidence-first model
- docs/ops/specs/binaries/addendum.md: update Foreman Intervention Chain to reflect the new intent form
- docs/MANUAL.md: update Secondary Lanes foreman description to reflect the new intent form
- storage/dp/active/allowlist.txt: add TASK archive entry for this DP

Out of scope:
- ops/bin/addendum: no change; the addendum binary's slot validation and letter assignment are unaffected
- ops/src/stances/addendum.md.tpl: no change to addendum template structure
- tools/lint/response.sh: no change
- Any bundle profile other than foreman
- TASK.md, SoP.md, PoW.md: no structural edits

Safety and invariants:
- bundle_parse_foreman_intent must reject any intent where the first field is not DP-OPS-[0-9]{4,}
- The foreman presence check must still die when the base DP ID is absent from the dump payload
- All other bundle profiles must continue to work as before
- bash tools/lint/dp.sh --test must pass after changes
- Foreman stance must not reference RoR-* decision leaf as a required precondition

Worker Constraints (SSOT injected):
## Section 1: Universal Template Rules
- Template source uses `tpl` files with optional YAML frontmatter
- Canon frontmatter keys: `template_type` `template_id` `template_version` `requires_slots` `includes`
- Renderer strips frontmatter before output write
- Slot token form `\{\{TOKEN\}\}` with uppercase alphanumeric underscore
- Include forms `\{\{@include:path\}\}` and `\{\{@include:path#section\}\}`
- Include resolution is strict: missing file fail missing section fail circular graph fail
- Strict mode default: every required slot value present and no unresolved token
- Non strict mode allowed only for lint and normalization workflows
- Worker facing generated surfaces remain pointer first and exclude disposable artifacts

Target Files allowlist (hard gate):
- storage/dp/active/allowlist.txt

## 3.4 Execution Plan (A-E)

### 3.4.1 State
ops/lib/manifests/BUNDLE.md line 16: `foreman_intent_form=ADDENDUM REQUIRED: <DECISION_ID> - <ONE-LINE BLOCKER>` — implies a pre-existing RoR-* decision leaf as the first field. Line 127 repeats `<DECISION_ID>` form in the profile description.

ops/lib/scripts/bundle.sh bundle_parse_foreman_intent (lines 744-752): regex `^ADDENDUM[[:space:]]+REQUIRED:[[:space:]]+([^[:space:]]+)[[:space:]]+-[[:space:]]+(.+)$` accepts any non-space token as decision_id. Lines 1238-1303: parsed decision_id is used to grep the dump payload; if absent, die with "foreman decision leaf not present in dump payload". Variable names `BUNDLE_FOREMAN_DECISION_ID` and `decision_leaf_present` reflect the old decision-leaf model. Lines 1400-1403 and 1611-1618 emit `Addendum decision id`, `Decision leaf present in dump`, `decision_id`, and `decision_leaf_present` in bundle text and manifest JSON.

ops/src/stances/foreman.md.tpl lines 13-14: requires `ADDENDUM REQUIRED:` intent and a referenced decision leaf in dump payload; line 14 states the decision leaf is the addendum case. Step 0 precondition checks for decision leaf presence. Step 1 reads decision leaf ID from intent. Step 2 locates and reads the decision leaf in the dump.

docs/ops/specs/stances/foreman.md line 33: example intent form uses `<DECISION_ID>`.
docs/ops/specs/binaries/addendum.md line 53: Foreman Intervention Chain step 2 uses `<DECISION_ID>` in the intent form.
docs/MANUAL.md line 52: Secondary Lanes foreman description uses `<DECISION_ID>`.

### 3.4.2 Request
1. In ops/lib/manifests/BUNDLE.md:
   - Line 16: change `foreman_intent_form=ADDENDUM REQUIRED: <DECISION_ID> - <ONE-LINE BLOCKER>` to `foreman_intent_form=ADDENDUM REQUIRED: <BASE_DP_ID> - <ONE-LINE BLOCKER>`.
   - Line 127: change `intent form must be \`ADDENDUM REQUIRED: <DECISION_ID> - <BLOCKER>\`` to `intent form must be \`ADDENDUM REQUIRED: <BASE_DP_ID> - <BLOCKER>\``.

2. In ops/lib/scripts/bundle.sh:
   a. Update bundle_parse_foreman_intent regex: change `([^[:space:]]+)` to `(DP-OPS-[0-9]{4,})` so only canonical DP packet IDs are accepted as the first field.
   b. Rename `BUNDLE_FOREMAN_DECISION_ID` to `BUNDLE_FOREMAN_BASE_DP_ID` in the function and at the usage site (line 1246 assignment).
   c. Rename local variable `decision_id` to `foreman_base_dp_id` at line 1239 declaration and all uses in the foreman block (lines 1246, 1297-1303, 1401, 1613-1614).
   d. Rename local variable `decision_leaf_present` to `foreman_base_dp_in_dump` at line 1240 declaration and all uses (lines 1299, 1301, 1402, 1618).
   e. Update error message at line 1301: change "foreman decision leaf not present in dump payload:" to "foreman base DP not confirmed in dump payload:".
   f. Update foreman text emission at lines 1400-1403: replace `Addendum decision id` label with `Addendum base DP` and `Decision leaf present in dump` with `Base DP confirmed in dump`.
   g. Update manifest JSON emission at lines 1611-1618: replace field names `decision_id` with `base_dp_id` and `decision_leaf_present` with `base_dp_in_dump`.

3. In ops/src/stances/foreman.md.tpl:
   a. Replace lines 13-14 with: "Require `ADDENDUM REQUIRED:` intent in bundle OPEN metadata carrying a canonical base DP ID (DP-OPS-XXXX) and one-line blocker description." and "Build the addendum case from visible evidence in the bundle dump: RESULTS narrative, OPEN metadata, and the boundary condition stated in the intent. No pre-existing decision leaf is required."
   b. Update Step 0 PRECONDITIONS: confirm intent carries `ADDENDUM REQUIRED` with a parseable base DP ID and one-line blocker. If missing or malformed: STOP and request a correctly assembled foreman bundle.
   c. Update Step 1 EXTRACT: read base DP ID and one-line blocker from the intent line.
   d. Replace Step 2 REVIEW with: "**BUILD CASE**: From visible evidence in the dump (RESULTS narrative, OPEN metadata, boundary condition in intent), identify the context, the impacted scope, and the proposed resolution. Summarize the case in A.3 Addendum Objective."

4. In docs/ops/specs/stances/foreman.md:
   - Update the Shipping Spine Position step 2 bundle command to use `<BASE_DP_ID>` instead of `<DECISION_ID>` in the intent string.
   - Remove any reference to a pre-existing decision leaf as a required precondition; replace with a note that the foreman builds the case from visible evidence.

5. In docs/ops/specs/binaries/addendum.md:
   - In Foreman Intervention Chain step 2, change `<DECISION_ID>` to `<BASE_DP_ID>` in the intent form.

6. In docs/MANUAL.md:
   - In Secondary Lanes foreman description, change `<DECISION_ID>` to `<BASE_DP_ID>` in the foreman wake-up bundle command.

7. In storage/dp/active/allowlist.txt, append:
   archives/surfaces/TASK-DP-OPS-0222-*.md

8. Run receipt commands and paste outputs in RESULTS.

### 3.4.3 Changelog
UPDATE ops/lib/manifests/BUNDLE.md — change foreman_intent_form to BASE_DP_ID form; update line 127 profile description
UPDATE ops/lib/scripts/bundle.sh — update bundle_parse_foreman_intent regex; rename BUNDLE_FOREMAN_DECISION_ID and decision_leaf_present variables; update text and JSON emission
UPDATE ops/src/stances/foreman.md.tpl — replace decision-leaf prerequisite rule with evidence-first rule; update Steps 0-2
UPDATE docs/ops/specs/stances/foreman.md — update Shipping Spine Position to evidence-first model
UPDATE docs/ops/specs/binaries/addendum.md — update Foreman Intervention Chain intent form
UPDATE docs/MANUAL.md — update Secondary Lanes foreman description intent form
UPDATE storage/dp/active/allowlist.txt — add TASK archive entry for DP-OPS-0222

### 3.4.4 Patch / Diff
A. Read ops/lib/manifests/BUNDLE.md lines 14-18 and line 127 in full.

B. Apply BUNDLE.md changes as described in Request item 1.

C. Read ops/lib/scripts/bundle.sh function bundle_parse_foreman_intent (lines 744-752), lines 1238-1248 (foreman intent parse), lines 1297-1303 (decision-leaf-present check), lines 1400-1403 (foreman text emission), lines 1611-1618 (manifest JSON emission) in full.

D. Apply bundle.sh changes as described in Request item 2. Rename variables consistently across all six locations. STOP if any use of the old variable names remains in the foreman block.

E. Read ops/src/stances/foreman.md.tpl in full.

F. Apply foreman.md.tpl changes as described in Request item 3.

G. Read docs/ops/specs/stances/foreman.md lines 30-38 in full.

H. Apply foreman spec changes as described in Request item 4.

I. Read docs/ops/specs/binaries/addendum.md lines 50-58 in full.

J. Apply addendum spec changes as described in Request item 5.

K. Read docs/MANUAL.md lines 50-55 in full.

L. Apply MANUAL.md changes as described in Request item 6.

M. In storage/dp/active/allowlist.txt, append archives/surfaces/TASK-DP-OPS-0222-*.md.

N. Run receipt commands and paste outputs in RESULTS.

### 3.4.5 Receipt (Proofs to collect) - MUST RUN

**Mandatory receipt commands (always run; do not omit):**
- bash tools/lint/dp.sh TASK.md
- bash tools/lint/task.sh
- bash tools/lint/integrity.sh
- bash tools/lint/style.sh
- ./ops/bin/llms
- bash tools/lint/integrity.sh
- git diff --name-only
- git diff --stat
- comm -23 <(git diff --name-only | sort) <(sort storage/dp/active/allowlist.txt) || true
- comm -23 <(git ls-files --others --exclude-standard | sort) <(sort storage/dp/active/allowlist.txt) || true
- ./ops/bin/open

**DP-specific receipt commands (scope-specific; author below):**
Note: Command substitution forms (e.g., $(pwd), $(git ...)) are rejected by certify replay. Use literal values only. dp.sh enforces at lint time.
- bash tools/lint/dp.sh --test

## 3.5 Closeout (Mandatory Routing)
- Execute docs/MANUAL.md Closeout Cycle in order (Verify, Harvest, Refresh, Log, Prune).
- Update SoP.md and PoW.md with DP entries, including objective summary and current proof / receipt summary.
- Protocol order for closeout: Verify -> Generate Results -> COMMIT (Operator Only) -> Prune.
- Run prune hygiene: ./ops/bin/prune --scrub.
- Refresh llms artifacts: `./ops/bin/llms`
- Regenerate session artifacts: `./ops/bin/open --out=auto`
- Ensure the next session begins with refreshed session artifacts and matching receipts.
- Note: audit dump generation is owned by `./ops/bin/bundle --profile=audit --out=auto` and is separate from operator session refresh. Do not stamp an additional `ops/bin/dump --scope=core` into closeout unless the DP explicitly authorizes a standalone state capture.
- Refresh side-effect: `ops/bin/llms` regenerates `ops/lib/manifests/OPS.md` as a compile event; if `OPS.md` is not in the allowlist, restore it before running `integrity.sh`. See `docs/MANUAL.md` Refresh side-effect notice for the full procedure.

### 3.5.1 Mandatory Closing Sidecar
Closing sidecar content is generated and maintained at `storage/handoff/CLOSING.md` and is validated by `ops/bin/certify` as a hard gate at certification time. Do not author, predict, populate, or approximate any sidecar-derived closeout output outside this sidecar.
Certify separately collects contractor-authored narrative for the RESULTS Contractor Execution Narrative section at certify time via interactive editor prompt; this narrative input is independent from closing sidecar content.

Before running certify, confirm `storage/handoff/CLOSING.md` has been maintained throughout execution and reflects observable reality only.

Closing sidecar fields required at certify time:
- Commit Message
- Create Pull Request (Title)
- Create Pull Request (Description)
- Confirm Merge (Commit Message)
- Confirm Merge (Extended Description)
- Confirm Merge (Add a Comment)
