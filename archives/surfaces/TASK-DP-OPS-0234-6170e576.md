---
trace_id: stela-20260326T030458Z-68427890
packet_id: DP-OPS-0234
created_at: 2026-03-26T03:06:39Z
previous: archives/surfaces/TASK-DP-OPS-0233-cfa39a27.md
---
# STELA TASK DASHBOARD
Status: ACTIVE
Owner: Integrator
Last Updated: 2026-02-14

## 1. Session State
Pointer: Session context output (generated by ops/bin/open)
Context Manifest: ops/lib/manifests/CONTEXT.md (checked by tools/lint/context.sh)

Session State contract:
- Session State is pointer-first. Do not write inline branch or hash state here.
- OPEN (and OPEN-PORCELAIN when dirty) is the session state snapshot.
- RESULTS is the audit record for the DP.
- TASK is the stable routing surface.

## 2. Logic Pointers
Primary Constraint: PoT.md (Policy of Truth).
Surface Contract Spec: docs/ops/specs/surfaces/task.md.

### 2.1 Governance Pointers
Jurisdiction: PoT.md Section 3.
Git Authority: PoT.md Section 4.1.
Behavioral Standard: PoT.md Section 4.2.
Staffing Protocol: PoT.md Section 4.1.

### 2.2 Enforcement and Gates
Style: tools/lint/style.sh
Context: tools/lint/context.sh
Truth: tools/lint/truth.sh
DP lint: tools/lint/dp.sh (enforces DP transaction rules in Section 3; does not enforce TASK container schema)
TASK lint: tools/lint/task.sh (sole TASK surface/container schema enforcer)
Repo verify: tools/verify.sh

## 3. Current Dispatch Packet (DP)

Section-by-section authoring protocol:
- Generate DP structure via `./ops/bin/bundle --profile=draft --out=auto` (standard operator path: Architect completes the embedded DP AUTHORING SCAFFOLD; save the returned DP body to `storage/dp/intake/DP.md`) or locally via `ops/bin/draft` (template render only).
- Do not hand-edit DP structural boilerplate in `TASK.md` after generation.
- Manual edits are limited to slot content only (objective, scope, safety, plan body, receipt body).
- Freeze each completed subsection in order (3.1 -> 3.2 -> 3.3 -> 3.4.x).
- Do not revise earlier sections to justify later work. If scope changes, end the thread and draft a new DP.

Freeze Rule:
- If a completed DP subsection changes later, append an AMENDMENT block in RESULTS; otherwise DP is FAIL.

AMENDMENT: minimum fields
- Reopened section:
- Reason:
- Impact:
- Allowlist change (none or exact paths):

Input discipline (hard rule):
- Worker receives only the DP text. Worker does not require OPEN or DUMP attachments.
- DP writer must not attach or cite disposable artifacts (rough notes, chat logs, scratch drafts, throwaway excerpts).
- DP writer must not include pasted bundles (OPEN, DUMP payloads, manifests) inside the DP body.
- Canon pointers only. Evidence belongs in storage artifacts and RESULTS.

### DP-OPS-0234: Flip planning stance to final-plan-first; update coupled lint guards and spec

## 3.1 Freshness Gate (Must Pass Before Work)
Base Branch: main
Work Branch: work/dp-ops-0234-2026-03-25
Base HEAD: 6170e576
Freshness Stamp: 2026-03-25
Note: FRESHNESS_STAMP must be YYYY-MM-DD format only. No trace tokens, no timestamps, no other text. Certify rejects all other forms. dp.sh enforces at lint time.

Required local re-check (worker runs before any edits; paste verbatim outputs under `## Contractor Execution Narrative` -> `### Preflight State` in RESULTS):
- git rev-parse --abbrev-ref HEAD
- git rev-parse --short HEAD
- git status --porcelain

These three commands are freshness proof only. Do not treat §3.4.5 receipt replay as a substitute for their pre-edit outputs.

Preconditions:
- No commits on main.
- Working tree must be clean before execution begins.
- If Base HEAD changes, regenerate session artifacts from canonical tools before proceeding.

STOP conditions:
- STOP if any mismatch (branch, Base HEAD, missing work branch).
- STOP if working tree is dirty before execution begins.
- STOP if told to create or switch branches.

## 3.1.1 DP Preflight Gate (Run Before Any Edits)
Purpose:
- Catch malformed DP or TASK schema before work begins.

Worker runs (paste outcome in RESULTS):
- bash tools/lint/dp.sh --test
- bash tools/lint/dp.sh TASK.md
- bash tools/lint/task.sh

STOP if any preflight check fails.

### CbC Design Discipline Preflight (TASK.md §3.1.1)
Required when the DP objective adds, modifies, or replaces a linter, script, guard, or validation binary.
For non-tooling DPs: state "Not applicable" with a one-line justification.
This DP modifies `tools/lint/style.sh`, a validation script. CbC preflight is applicable. Before dispatch, confirm: (a) each new replacement guard in `check_planning_mode_contract()` would fail on at least one plausible regression of the intended planning contract, (b) no replacement guard relies on weak token-only markers such as `immediately`, `(Recommended)`, `Questions / Conversation:`, `plan.md.tpl`, or `storage/handoff/PLAN.md` as standalone proof of behavior, and (c) `bash tools/lint/style.sh` passes only after both the stance and guard changes are applied together. Do not apply either change in isolation as a partial state.

## 3.2 Required Context Load (Read Before Doing Anything)

### 3.2.1 Canon load order (always)
Worker must confirm loaded before edits begin:
1. PoT.md
2. SoP.md
3. PoW.md
4. TASK.md
5. docs/MAP.md
6. docs/MANUAL.md
7. ops/lib/manifests/CONTEXT.md

Notes:
- Worker does not read OPEN. OPEN is for Integrator state refresh and for receipts.
- Disposable artifacts must not be referenced or included.
- Worker input is the DP text only.
- DP writer must not attach or cite disposable artifacts.
- DP writer must not embed pasted bundles.

## Rules

1 Temp path rule uses `var/tmp` only
2 RESULTS generation rule uses `ops/bin/certify` only
3 Receipt rule runs every DP mandated command and treats any skip as STOP
4 Disposable artifact rule treats OPEN dump bundles and manifests as non canonical session artifacts
5 Use repo-relative paths only in all output. Never print absolute filesystem paths. Do not emit clickable file links.

### 3.2.2 DP-scoped load order (per DP)
- `ops/src/stances/planning.md.tpl` — primary stance surface being rewritten
- `docs/ops/specs/stances/planning.md` — coupled spec being updated
- `tools/lint/style.sh` — validation script whose planning mode guards are being replaced
- `docs/ops/specs/tools/lint/style.md` — spec for style.sh being updated to reflect new guards
- `tools/lint/response.sh` — read-only reference to confirm question-mode output-shape enforcement is independent of the stance template before removing any stance lines that govern that shape

## 3.3 Scope and Safety
Objective:
Flip the planning stance from conversational-first to final-plan-first; update the coupled spec; replace the stale planning lint guards in style.sh and its spec with a small set of core contract-line checks chosen for behavioral distinctness rather than weak token presence; and remove the hard-coded `Canonical question-mode format` code block once the coupled lint expectations move off the lines it supports.

In scope:
- `ops/src/stances/planning.md.tpl`: rewrite the default Rule line and the Steps block to final-plan-first; add clarification-as-exception, subordinate-choices-direct, and after-clarification-immediate Rule lines; remove the `Canonical question-mode format` fenced code block and its trailing `Lint patterns:` line; rename the four `For conversational planning mode:` machine-ingest lines to `For machine-ingest question mode:` with exception-mode framing; retain all `For machine-ingest planning mode:` and `For final plan mode:` lines and all shared include directives unchanged.
- `docs/ops/specs/stances/planning.md`: update the Outputs block to mirror the final-plan-first contract; remove conversational-default output statements; add exception-mode clarification contract statements.
- `tools/lint/style.sh` `check_planning_mode_contract()`: replace Guards 22/25/26/27/28 exact-string assertions with a small set of replacement checks that distinguish the new final-plan-first contract from plausible regressions; update `mark_failure` messages for the replaced guards; keep all other guard variables, assertion values, and failure messages unchanged.
- `docs/ops/specs/tools/lint/style.md` Planning Mode Contract Guardrails: update Guards 22/25/26/27/28 target assertions and failure messages to match the new style.sh checks exactly; keep Guards 21/23/24/29/30/31 unchanged.

Out of scope:
- `ops/src/stances/plan.md.tpl` — must not be touched
- `tools/lint/response.sh` — read-only reference only; do not edit; if inspecting it reveals that removing a stance line would break response.sh output-shape enforcement, STOP and report before proceeding
- Bundle mechanics, DP scaffold mechanics, factory, registry, or non-planning-contract surfaces
- `ops/src/stances/planning.md.tpl` YAML frontmatter — must not be changed

Safety and invariants:
- No manual-generated stance output; all planning stance output continues to route through `ops/bin/manifest render stance-planning`
- Allowlist hard gate: all four touched paths are already present in `storage/dp/active/allowlist.txt`; no new allowlist entries expected; confirm with the allowlist comm receipt
- `ops/src/stances/plan.md.tpl` must remain untouched
- Final plan mode output shape, the four canonical plan section headings, and the output surface `storage/handoff/PLAN.md` remain unchanged
- The Q1./Q2./Q3. with A./B./C. and one `(Recommended)` question format is preserved in the exception path Steps prose; only the freestanding code block, the conversational-default Rule line, and the conversational-first Steps shape are removed
- New style.sh guards must distinguish the intended planning behavior from plausible regressions; do not use weak token-only markers such as `immediately`, `(Recommended)`, `Questions / Conversation:`, `plan.md.tpl`, or `storage/handoff/PLAN.md` as standalone replacements
- `bash tools/lint/style.sh` must pass after both stance and guard changes are applied

Worker Constraints (SSOT injected):
## Section 1: Universal Template Rules
- Template source uses `tpl` files with optional YAML frontmatter
- Canon frontmatter keys: `template_type` `template_id` `template_version` `requires_slots` `includes`
- Renderer strips frontmatter before output write
- Slot token form `\{\{TOKEN\}\}` with uppercase alphanumeric underscore
- Include forms `\{\{@include:path\}\}` and `\{\{@include:path#section\}\}`
- Include resolution is strict: missing file fail missing section fail circular graph fail
- Strict mode default: every required slot value present and no unresolved token
- Non strict mode allowed only for lint and normalization workflows
- Worker facing generated surfaces remain pointer first and exclude disposable artifacts

Target Files allowlist (hard gate):
- storage/dp/active/allowlist.txt

## 3.4 Execution Plan (A-E)

### 3.4.1 State
Branch `main`, HEAD `6170e576`, working tree expected clean before execution begins. The attached PLAN.md declares `Planning Contract Cleanup` as the active next intake target. DP-OPS-0233 (foreman → addenda) is landed and confirmed AUDIT PASS R2. The current `ops/src/stances/planning.md.tpl` is conversational-first: the Rules block leads with `* Default planning behavior is conversational planning.`, the Steps block presents conversational mode as step 3 and final plan as step 4, and a freestanding `Canonical question-mode format` fenced code block with a `Lint patterns:` trailing line is embedded in the stance body. Style.sh `check_planning_mode_contract()` Guards 22/25/26/27/28 are exact-string assertions against these conversational-first lines. `tools/lint/response.sh --mode=planning` independently enforces the `1. Analysis and Discussion` first-line shape and `Questions / Conversation:` footer at output time; those checks live in response.sh, not in the stance template. All four source files in scope are confirmed present in `storage/dp/active/allowlist.txt`.

### 3.4.2 Request
- Rewrite `ops/src/stances/planning.md.tpl` to final-plan-first: remove `* Default planning behavior is conversational planning.`; add a new final-plan-first Rule line declaring the primary output is the final `storage/handoff/PLAN.md` emitted immediately when topic and evidence settle intent; add a clarification-as-exception Rule line (ask only when needed, prefer one real packet-boundary question, up to 3); add a subordinate-choices-direct Rule line; add an after-clarification-immediate Rule line; rewrite the Steps block as three steps — step 1 reads topic and evidence and emits the final plan immediately when intent is settled, step 2 covers the question-mode exception path (preserving Q1./Q2./Q3. with A./B./C. and one `(Recommended)`, the `Questions / Conversation:` footer, and the response-format hint, followed by immediate plan emission after clarification), step 3 covers final plan mode shape; delete the `Canonical question-mode format` fenced code block and its `Lint patterns:` trailing line; rename the four `For conversational planning mode:` machine-ingest lines to `For machine-ingest question mode:` and revise their bodies to exception-mode framing.
- Update `docs/ops/specs/stances/planning.md` Outputs block to mirror the final-plan-first contract as described in §3.4.4 step 5.
- Replace Guards 22/25/26/27/28 in `check_planning_mode_contract()` in `tools/lint/style.sh` with five new checks tied to the new planning-contract lines in the updated stance; each replacement must fail on at least one plausible regression of the intended contract. Do not use token-only markers or path references as standalone proof. Update each `mark_failure` message accordingly; do not alter any other guard.
- Update `docs/ops/specs/tools/lint/style.md` Guards 22/25/26/27/28 to exactly reflect the new style.sh checks.

### 3.4.3 Changelog
- UPDATE `ops/src/stances/planning.md.tpl`
- UPDATE `docs/ops/specs/stances/planning.md`
- UPDATE `tools/lint/style.sh`
- UPDATE `docs/ops/specs/tools/lint/style.md`

### 3.4.4 Patch / Diff
1. Edit `ops/src/stances/planning.md.tpl` Rules block:
   - Remove: `* Default planning behavior is conversational planning.`
   - Add in its place: `* Primary output is the final \`storage/handoff/PLAN.md\`; emit it immediately when the topic and attached evidence settle intent.`
   - After the existing `* When the topic and attached evidence settle intent enough for direct draft handoff, emit the final \`PLAN.md\` draft in one fenced markdown block.` line, insert:
     - `* Ask clarifying questions only when needed to write a truthful plan; prefer one real packet-boundary question; ask the minimum number needed, up to 3.`
     - `* Settle subordinate choices directly when attached evidence and the narrowness constraint already make them non-blocking.`
     - `* After clarification, emit the final plan immediately.`
2. Replace the current Steps block (steps 0–4) with a simplified three-step block:
   - Step 0 PRECONDITIONS: unchanged — if attached `storage/handoff/TOPIC.md` is missing, STOP and report.
   - Step 1: read the attached topic and all directly attached bundle evidence; if topic and evidence settle intent, emit the final `PLAN.md` draft immediately in a fenced markdown code block generated against `ops/src/stances/plan.md.tpl`; do not stop at analysis-only output; do not ask for clarification when intent is already settled.
   - Step 2 (question exception path, used only when clarification is necessary to write a truthful plan): ask the minimum number of questions needed, up to 3; prefer one real packet-boundary question over a broad questionnaire; settle all subordinate choices directly that do not change the packet boundary; output a fenced markdown block whose first non-empty line starts with `1. Analysis and Discussion`, followed by a `2. Decision Questions` section using Q1./Q2./Q3. with A./B./C. options and exactly one `(Recommended)` per question, ending with `Questions / Conversation:` and a concise response format hint such as `Q1:A, Q2:C` or `Use recommended options`; after operator clarification, emit the final plan immediately.
   - Step 3 (final plan mode): output only the complete `PLAN.md` draft in a fenced markdown code block; use only the plan sections `Summary`, `Key Changes`, `Test Plan`, and `Assumptions`; make the plan decision-complete for direct draft drafting.
3. Remove the `Canonical question-mode format` fenced code block and the `Lint patterns:` line that follows it from `ops/src/stances/planning.md.tpl`.
4. Rename and revise the four `For conversational planning mode:` machine-ingest lines in `ops/src/stances/planning.md.tpl`:
   - Remove: `For conversational planning mode: first non-empty line inside the fenced body must start with \`1. Analysis and Discussion\`.` — response.sh independently enforces this shape at output time; the stance does not need to carry a machine-ingest duplicate.
   - Replace `For conversational planning mode: when asking questions, use a \`2. Decision Questions\` section; allow at most 3 questions; each question must present exactly 3 meaningful options with one marked \`(Recommended)\`; end with \`Questions / Conversation:\` and a concise operator response format such as \`Q1:A, Q2:C\` or \`Use recommended options\`.` with: `For machine-ingest question mode: when clarification is needed, use a \`2. Decision Questions\` section; allow at most 3 questions; each question must present exactly 3 meaningful, mutually exclusive options with exactly one marked \`(Recommended)\`; end with \`Questions / Conversation:\` and a concise operator response format such as \`Q1:A, Q2:C\` or \`Use recommended options\`.`
   - Replace `For conversational planning mode: if topic text is present but weak or ambiguous, interpret conservatively, state assumptions, and ask concise follow-up questions instead of forcing a final plan.` with: `For machine-ingest question mode: if topic text is present but intent cannot be settled from attached evidence, ask the minimum clarifying questions needed rather than forcing a final plan.`
   - Replace `For conversational planning mode: if topic text is nonsensical or non-actionable, stop at the nearest truthful boundary and ask for clarification.` with: `For machine-ingest question mode: if topic text is nonsensical or non-actionable, stop at the nearest truthful boundary and ask for clarification.`
5. Update `docs/ops/specs/stances/planning.md` Outputs block:
   - Remove: `Planning remains conversational while material ambiguity is still open.`
   - Remove: `Conversational mode first non-empty line is \`1. Analysis and Discussion\`.`
   - Add: `Planning emits the final plan immediately when topic and evidence settle intent; clarification questions are the narrow exception, not the default mode.`
   - Add: `Subordinate choices that do not change the immediate packet boundary are settled directly in the plan rather than asked as questions.`
   - Add: `One real packet-boundary question is preferred over a broad questionnaire when a question is needed; up to 3 questions are permitted in a single run.`
   - Add: `After clarification, emit the final plan immediately.`
   - Retain the structured question mode description as the exception-path spec.
6. Update `check_planning_mode_contract()` in `tools/lint/style.sh` — replace only the five stale guard variables; keep all others unchanged:
   - Replace `required_discussion_default` value with the exact new final-plan-first Rule line: `* Primary output is the final \`storage/handoff/PLAN.md\`; emit it immediately when the topic and attached evidence settle intent.`; update `mark_failure` to `"planning.md.tpl missing planning final-plan-first default line"`.
   - Replace `required_default_first` variable name and value with `required_clarification_exception` set to the exact clarification-exception Rule line: `* Ask clarifying questions only when needed to write a truthful plan; prefer one real packet-boundary question; ask the minimum number needed, up to 3.`; update `mark_failure` to `"planning.md.tpl missing planning clarification-exception line"`.
   - Replace `required_questions` variable name and value with `required_subordinate_choices` set to the exact subordinate-choices Rule line: `* Settle subordinate choices directly when attached evidence and the narrowness constraint already make them non-blocking.`; update `mark_failure` to `"planning.md.tpl missing planning subordinate-choices direct line"`.
   - Replace `required_weak_topic` variable name and value with `required_after_clarification` set to the exact after-clarification Rule line: `* After clarification, emit the final plan immediately.`; update `mark_failure` to `"planning.md.tpl missing planning after-clarification-immediate line"`.
   - Replace `required_bad_topic` variable name and value with `required_nonsensical_topic` set to the exact `For machine-ingest question mode: if topic text is nonsensical or non-actionable, stop at the nearest truthful boundary and ask for clarification.` line; update `mark_failure` to `"planning.md.tpl missing planning nonsensical-topic line"`.
   - Before finalizing the replacements, verify each candidate guard would fail on at least one plausible regression: restored conversational-first default, clarification still treated as the main mode, omission of subordinate-choice direct settlement, omission of immediate post-clarification emission, or nonsense-topic forced-plan behavior. If a candidate would still pass those regressions, STOP and revise it instead of dropping to token-only markers.
   - Update each corresponding `if` block and `grep -Fq` call to use the renamed variable; keep all other variables, assertions, and `mark_failure` messages unchanged.
7. Update `docs/ops/specs/tools/lint/style.md` Planning Mode Contract Guardrails:
   - Replace Guard 22 with: Target file `ops/src/stances/planning.md.tpl`; Assertion: file must include `* Primary output is the final \`storage/handoff/PLAN.md\`; emit it immediately when the topic and attached evidence settle intent.`; Failure message: `planning.md.tpl missing planning final-plan-first default line`; Invariant: planning defaults to final-plan-first rather than conversational-first.
   - Replace Guard 25 with: Target file `ops/src/stances/planning.md.tpl`; Assertion: file must include `* Ask clarifying questions only when needed to write a truthful plan; prefer one real packet-boundary question; ask the minimum number needed, up to 3.`; Failure message: `planning.md.tpl missing planning clarification-exception line`; Invariant: clarification remains a narrow exception rather than the default mode.
   - Replace Guard 26 with: Target file `ops/src/stances/planning.md.tpl`; Assertion: file must include `* Settle subordinate choices directly when attached evidence and the narrowness constraint already make them non-blocking.`; Failure message: `planning.md.tpl missing planning subordinate-choices direct line`; Invariant: subordinate choices are settled directly instead of being expanded into unnecessary questions.
   - Replace Guard 27 with: Target file `ops/src/stances/planning.md.tpl`; Assertion: file must include `* After clarification, emit the final plan immediately.`; Failure message: `planning.md.tpl missing planning after-clarification-immediate line`; Invariant: the clarification path returns immediately to final-plan output.
   - Replace Guard 28 with: Target file `ops/src/stances/planning.md.tpl`; Assertion: file must include `For machine-ingest question mode: if topic text is nonsensical or non-actionable, stop at the nearest truthful boundary and ask for clarification.`; Failure message: `planning.md.tpl missing planning nonsensical-topic line`; Invariant: nonsense topics still stop truthfully rather than forcing fabricated plans.
   - Keep Guards 21/23/24/29/30/31 assertions, failure messages, and invariant prose exactly unchanged.

### 3.4.5 Receipt (Proofs to collect) - MUST RUN

**Mandatory receipt commands (always run; do not omit):**
- bash tools/lint/dp.sh TASK.md
- bash tools/lint/task.sh
- bash tools/lint/integrity.sh
- bash tools/lint/style.sh
- ./ops/bin/llms
- bash tools/lint/integrity.sh
- git diff --name-only
- git diff --stat
- comm -23 <(git diff --name-only | sort) <(sort storage/dp/active/allowlist.txt) || true
- comm -23 <(git ls-files --others --exclude-standard | sort) <(sort storage/dp/active/allowlist.txt) || true
- ./ops/bin/open

**DP-specific receipt commands (scope-specific; author below):**
Note: Command substitution forms (e.g., $(pwd), $(git ...)) are rejected by certify replay. Use literal values only. Repo-local binaries must be authored as literal `./ops/bin/...` or `ops/bin/...` commands. Unsupported receipt-command forms are rejected by dp.sh and certify.
- bash tools/lint/dp.sh storage/dp/intake/DP.md
- grep -n "Primary output is the final" ops/src/stances/planning.md.tpl
- grep -n "Ask clarifying questions only when needed" ops/src/stances/planning.md.tpl
- grep -n "Settle subordinate choices directly" ops/src/stances/planning.md.tpl
- grep -n "After clarification, emit the final plan immediately" ops/src/stances/planning.md.tpl
- grep -n "For machine-ingest question mode:" ops/src/stances/planning.md.tpl
- ! grep -n "Default planning behavior is conversational planning" ops/src/stances/planning.md.tpl
- ! grep -n "Canonical question-mode format" ops/src/stances/planning.md.tpl
- ! grep -n "For conversational planning mode:" ops/src/stances/planning.md.tpl
- grep -n "required_clarification_exception\|required_subordinate_choices\|required_after_clarification\|required_nonsensical_topic" tools/lint/style.sh

## 3.5 Closeout (Mandatory Routing)
- Execute docs/MANUAL.md Closeout Cycle in order (Verify, Harvest, Refresh, Log, Prune).
- Update SoP.md and PoW.md with DP entries, including objective summary and current proof / receipt summary.
- Protocol order for closeout: Verify -> Generate Results -> COMMIT (Operator Only) -> Prune.
- Run prune hygiene: ./ops/bin/prune --scrub.
- Refresh llms artifacts: `./ops/bin/llms`
- Regenerate session artifacts: `./ops/bin/open --out=auto`
- Ensure the next session begins with refreshed session artifacts and matching receipts.
- Note: audit dump generation is owned by `./ops/bin/bundle --profile=audit --out=auto` and is separate from operator session refresh. Do not stamp an additional `ops/bin/dump --scope=core` into closeout unless the DP explicitly authorizes a standalone state capture.
- Refresh side-effect: `ops/bin/llms` regenerates `ops/lib/manifests/OPS.md` as a compile event; if `OPS.md` is not in the allowlist, restore it before running `integrity.sh`. See `docs/MANUAL.md` Refresh side-effect notice for the full procedure.

### 3.5.1 Mandatory Closing Sidecar
Closing sidecar content is generated and maintained at `storage/handoff/CLOSING.md` and is validated by `ops/bin/certify` as a hard gate at certification time. Do not author, predict, populate, or approximate any sidecar-derived closeout output outside this sidecar.
Certify separately collects contractor-authored narrative for the RESULTS Contractor Execution Narrative section at certify time via interactive editor prompt; this narrative input is independent from closing sidecar content.

Before running certify, confirm `storage/handoff/CLOSING.md` has been maintained throughout execution and reflects observable reality only.

Closing sidecar fields required at certify time:
- Commit Message
- Create Pull Request (Title)
- Create Pull Request (Description)
- Confirm Merge (Commit Message)
- Confirm Merge (Extended Description)
- Confirm Merge (Add a Comment)
