---
trace_id: stela-20260315T051132Z-090931be
packet_id: DP-OPS-0195
created_at: 2026-03-15T05:11:57Z
previous: (none)
---
# STELA TASK DASHBOARD
Status: ACTIVE
Owner: Integrator
Last Updated: 2026-03-02

## 1. Session State
Pointer: Session context output (generated by ops/bin/open)
Context Manifest: ops/lib/manifests/CONTEXT.md (checked by tools/lint/context.sh)

Session State contract:
- Session State is pointer-first. Do not write inline branch or hash state here.
- OPEN and OPEN-PORCELAIN are the session state snapshot.
- OPEN-PORCELAIN is emitted on every OPEN run. Clean sessions produce a zero-line artifact.
- RESULTS is the audit record for the DP.
- TASK is the stable routing surface.
- Operator note (2026-02-17): Do not edit `SoP.md` or `PoW.md` unless explicitly instructed.

## 2. Logic Pointers
Primary Constraint: PoT.md (Policy of Truth).
Surface Contract Spec: docs/ops/specs/surfaces/task.md.

### 2.1 Governance Pointers
Jurisdiction: PoT.md Section 3.
Git Authority: PoT.md Section 4.1.
Behavioral Standard: PoT.md Section 4.2.
Staffing Protocol: PoT.md Section 4.1.

### 2.2 Enforcement and Gates
Style: tools/lint/style.sh
Context: tools/lint/context.sh
Truth: tools/lint/truth.sh
DP lint: tools/lint/dp.sh (enforces DP transaction rules in Section 3; does not enforce TASK container schema)
TASK lint: tools/lint/task.sh (sole TASK surface/container schema enforcer)
Repo verify: tools/verify.sh
llms parity: tools/lint/llms.sh

## 3. Current Dispatch Packet (DP)

Section-by-section authoring protocol:
- Generate DP structure from `ops/src/surfaces/dp.md.tpl` using `ops/bin/draft`.
- Do not hand-edit DP structural boilerplate in `TASK.md` after generation.
- Manual edits are limited to slot content only (objective, scope, safety, plan body, receipt body).
- Freeze each completed subsection in order (3.1 -> 3.2 -> 3.3 -> 3.4.x).
- Do not revise earlier sections to justify later work. If scope changes, end the thread and draft a new DP.

Freeze Rule:
- If a completed DP subsection changes later, append an AMENDMENT block in RESULTS; otherwise DP is FAIL.

AMENDMENT: minimum fields
- Reopened section:
- Reason:
- Impact:
- Allowlist change (none or exact paths):

Input discipline (hard rule):
- Worker receives only the DP text. Worker does not require OPEN or DUMP attachments.
- DP writer must not attach or cite disposable artifacts (rough notes, chat logs, scratch drafts, throwaway excerpts).
- DP writer must not include pasted bundles (OPEN, DUMP payloads, manifests) inside the DP body.
- Canon pointers only. Evidence belongs in storage artifacts and RESULTS.


### DP-OPS-0195: Analyst Topic-Bound Inference Discipline

## 3.1 Freshness Gate (Must Pass Before Work)
Base Branch: main
Required Work Branch: work/dp-ops-0195-analyst-topic-bound-inference-discipline-2026-03-15
Base HEAD: 94f8b173
Freshness Stamp: 2026-03-15
Note: FRESHNESS_STAMP must be YYYY-MM-DD format only. No trace tokens, no timestamps, no other text. Certify rejects all other forms. dp.sh enforces at lint time.

Required local re-check (worker runs; paste outputs in RESULTS):
- git rev-parse --abbrev-ref HEAD
- git rev-parse --short HEAD
- git status --porcelain

Preconditions:
- No commits on main.
- Working tree must be clean before execution begins.
- If Base HEAD changes, regenerate session artifacts from canonical tools before proceeding.

STOP conditions:
- STOP if any mismatch (branch, Base HEAD, missing work branch).
- STOP if working tree is dirty before execution begins.
- STOP if told to create or switch branches.

## 3.1.1 DP Preflight Gate (Run Before Any Edits)
Purpose:
- Catch malformed DP or TASK schema before work begins.

Worker runs (paste outcome in RESULTS):
- bash tools/lint/dp.sh --test
- bash tools/lint/dp.sh TASK.md
- bash tools/lint/task.sh

STOP if any preflight check fails.

### CbC Design Discipline Preflight (TASK.md §3.1.1)
Required when the DP objective adds, modifies, or replaces a linter, script, guard, or validation binary.
For non-tooling DPs: state "Not applicable" with a one-line justification.
Applicable. This packet hardens the analyst mode contract so broad topics do not silently expand into unsupported repo-operating claims. Structural prevention is to narrow the analyst stance itself: keep plans topic-bound, keep inference minimal, and forbid unsupported workflow examples or operator-command detail unless attached evidence names them. Detection remains necessary because a future stance edit can quietly re-open synthesis freedom unless exact contract lines are checked by style lint.

## 3.2 Required Context Load (Read Before Doing Anything)

### 3.2.1 Canon load order (always)
Worker must confirm loaded before edits begin:
1. PoT.md
2. SoP.md
3. PoW.md
4. TASK.md
5. docs/MAP.md
6. docs/MANUAL.md
7. ops/lib/manifests/CONTEXT.md

Notes:
- Worker does not read OPEN. OPEN is for Integrator state refresh and for receipts.
- Disposable artifacts must not be referenced or included.
- Worker input is the DP text only.
- DP writer must not attach or cite disposable artifacts.
- DP writer must not embed pasted bundles.

## Rules

1 Temp path rule uses `var/tmp` only
2 RESULTS generation rule uses `ops/bin/certify` only
3 Receipt rule runs every DP mandated command and treats any skip as STOP
4 Disposable artifact rule treats OPEN dump bundles and manifests as non canonical session artifacts
5 Use repo-relative paths only in all output. Never print absolute filesystem paths. Do not emit clickable file links.

### 3.2.2 DP-scoped load order (per DP)
- ops/src/stances/analyst.md.tpl
- docs/ops/specs/stances/analyst.md
- tools/lint/style.sh
- docs/ops/specs/tools/lint/style.md
- ops/src/stances/plan.md.tpl
- storage/dp/active/allowlist.txt

## 3.3 Scope and Safety
Objective:
- Tighten analyst machine-ingest behavior so broad topics produce topic-bound plans instead of unsupported repo-operating detail.
- Make the analyst stance explicitly forbid unsupported workflow examples, GitHub action lists, command-family claims, and other repo-operating specifics unless they are directly visible in attached artifacts.

In scope:
- UPDATE `ops/src/stances/analyst.md.tpl` so machine-ingest analyst mode states three new hard rules:
  - do not add repository-operating details, workflow examples, command families, or GitHub action lists unless directly visible in attached artifacts;
  - when the topic is broad, keep the plan generic and high-level rather than converting it into specific operating claims;
  - when required handoff fields force inference, make the smallest reasonable inference and avoid supporting detail that reads as established repo fact.
- UPDATE `docs/ops/specs/stances/analyst.md` so the analyst technical spec documents those new inference-discipline rules as part of the machine-ingest contract.
- UPDATE `tools/lint/style.sh` so analyst mode contract checks fail if the new exact analyst inference-discipline lines are missing from `ops/src/stances/analyst.md.tpl`.
- UPDATE `docs/ops/specs/tools/lint/style.md` so the style-lint technical spec records the new analyst contract checks and why they exist.
- UPDATE `tools/test/bundle.sh` so architect slice smoke always installs its own deterministic architect `PLAN.md` fixture instead of depending on whatever live handoff plan is present during certify-critical verify.
- UPDATE `docs/ops/specs/tools/test/bundle.md` so the bundle smoke spec records that architect slice smoke must be fixture-isolated from live handoff `PLAN.md`.
- UPDATE `storage/dp/active/allowlist.txt` with every tracked file touched by this packet.

Out of scope:
- Changes to analyst transport, bundle dump composition, or `TOPIC.md` carriage.
- Changes to `ops/src/stances/plan.md.tpl` section shape.
- Changes to architect, audit, foreman, or conformist stance contracts.
- Semantic response lint that attempts to prove plan evidence quality from output text alone.
- Changes to generated artifacts under `storage/handoff/`, `storage/dumps/`, or archives.

Safety and invariants:
- Analyst input remains `storage/handoff/TOPIC.md`; analyst output remains `storage/handoff/PLAN.md`.
- Analyst output shape remains `PLAN.md` only, in one fenced markdown block, with the existing simplified sections.
- This packet tightens evidence discipline only; it does not widen analyst source authority.
- New contract wording must remain generic and stale-proof; it must not name temporary local files or session-specific artifacts beyond the existing analyst input source.
- Style lint remains the enforcement floor for presence of required analyst contract lines.
- Bundle smoke fixtures must be self-contained and must not depend on mutable live handoff planning state during certify-critical verify.
- Allowlist hard gate remains authoritative for every touched path.

Worker Constraints (SSOT injected):
## Section 1: Universal Template Rules
- Template source uses `tpl` files with optional YAML frontmatter
- Canon frontmatter keys: `template_type` `template_id` `template_version` `requires_slots` `includes`
- Renderer strips frontmatter before output write
- Slot token form `\{\{TOKEN\}\}` with uppercase alphanumeric underscore
- Include forms `\{\{@include:path\}\}` and `\{\{@include:path#section\}\}`
- Include resolution is strict: missing file fail missing section fail circular graph fail
- Strict mode default: every required slot value present and no unresolved token
- Non strict mode allowed only for lint and normalization workflows
- Worker facing generated surfaces remain pointer first and exclude disposable artifacts

Target Files allowlist (hard gate):
- storage/dp/active/allowlist.txt

## 3.4 Execution Plan (A-E)

### 3.4.1 State
- Current analyst machine-ingest behavior is shape-correct but still inference-loose.
- The latest GPT analyst sample respected `PLAN.md` output shape and topic input, but it still expanded a broad topic into specific repo-operating claims that were not directly visible in the attached evidence.
- The current analyst stance forbids menus and fallback behavior, but it does not explicitly forbid unsupported operational examples or require broad topics to stay generic.
- `tools/lint/style.sh` currently checks only the analyst output/fence/topic lines, so these missing inference-discipline rules can drift silently.
- `tools/test/bundle.sh --mode=certify-critical` currently allows architect slice smoke to reuse a live `storage/handoff/PLAN.md` if it merely looks architect-shaped, which lets unrelated analyst plan experiments break closeout on non-architect packets.

### 3.4.2 Request
- Tighten the analyst stance so attached-topic planning stays topic-bound.
- Forbid unsupported repo-operating detail unless attached evidence directly names it.
- Keep broad-topic planning high-level and generic rather than operationally specific.
- When handoff fields require inference, require the smallest reasonable inference and prohibit supporting prose that masquerades as established repo fact.
- Lock those new rules in with exact-line style-lint contract checks so a future stance edit cannot silently remove them.
- Make architect slice smoke deterministic by forcing it to install its own fixture `PLAN.md` during bundle smoke instead of reusing mutable live handoff plan state.
- If the wording starts duplicating PoT authority or expanding analyst into repo-wide constitutional narration, STOP and tighten the language back to analyst-mode behavior only.

### 3.4.3 Changelog
UPDATE:
- ops/src/stances/analyst.md.tpl
- docs/ops/specs/stances/analyst.md
- tools/lint/style.sh
- docs/ops/specs/tools/lint/style.md
- tools/test/bundle.sh
- docs/ops/specs/tools/test/bundle.md
- storage/dp/active/allowlist.txt

### 3.4.4 Patch / Diff
1. Update `storage/dp/active/allowlist.txt` first with every tracked file in this packet and the active closing-sidecar path for `DP-OPS-0195`.
2. Tighten `ops/src/stances/analyst.md.tpl` with exact machine-ingest analyst rules that prohibit unsupported repo-operating detail, keep broad topics generic, and constrain inference to the smallest reasonable handoff choice.
3. Keep the existing analyst output shape untouched: one fenced `PLAN.md` block, same simplified sections, same `TOPIC.md` input contract.
4. Update `docs/ops/specs/stances/analyst.md` so the technical spec documents the new inference-discipline lines and their purpose.
5. Extend `tools/lint/style.sh` analyst-mode contract checks so the new exact analyst lines are required.
6. Update `docs/ops/specs/tools/lint/style.md` so style-lint documentation includes the new analyst-mode contract checks and the rationale for them.
7. Update `tools/test/bundle.sh` so architect slice smoke always installs and later restores a deterministic architect `PLAN.md` fixture regardless of what live handoff plan is present.
8. Update `docs/ops/specs/tools/test/bundle.md` so the certify-critical architect smoke contract explicitly requires fixture isolation from live handoff plan state.
9. Run the targeted lint stack and collect receipts proving the analyst contract lines and the fixture-isolated certify-critical path both hold.

### 3.4.5 Receipt (Proofs to collect) - MUST RUN

**Mandatory receipt commands (always run; do not omit):**
- bash tools/lint/dp.sh TASK.md
- bash tools/lint/task.sh
- bash tools/lint/integrity.sh
- bash tools/lint/style.sh
- ./ops/bin/llms
- bash tools/lint/integrity.sh
- git diff --name-only
- git diff --stat
- comm -23 <(git diff --name-only | sort) <(sort storage/dp/active/allowlist.txt) || true
- comm -23 <(git ls-files --others --exclude-standard | sort) <(sort storage/dp/active/allowlist.txt) || true
- ./ops/bin/open

**DP-specific receipt commands (scope-specific; author below):**
Note: Command substitution forms (e.g., $(pwd), $(git ...)) are rejected by certify replay. Use literal values only. dp.sh enforces at lint time.
- bash tools/lint/dp.sh --test
- bash tools/lint/style.sh
- bash tools/lint/response.sh --test
- bash tools/verify.sh --mode=certify-critical
- ./ops/bin/bundle --profile=analyst --out=auto
- grep -n "unsupported repo-operating detail\|specific operating claims\|smallest reasonable inference\|GitHub action lists\|workflow examples" ops/src/stances/analyst.md.tpl docs/ops/specs/stances/analyst.md tools/lint/style.sh docs/ops/specs/tools/lint/style.md
- grep -n "deterministic `storage/handoff/PLAN.md` fixture\|live handoff `PLAN.md`" tools/test/bundle.sh docs/ops/specs/tools/test/bundle.md

## 3.5 Closeout (Mandatory Routing)
- Execute docs/MANUAL.md Closeout Cycle in order (Verify, Harvest, Refresh, Log, Prune).
- Update SoP.md and PoW.md with DP entries, including objective summary and verification commands run.
- Protocol order for closeout: Verify -> Generate Results -> COMMIT (Operator Only) -> Prune.
- Run prune hygiene: ./ops/bin/prune --scrub.
- Refresh llms artifacts: `./ops/bin/llms`
- Regenerate session artifacts: `./ops/bin/open --out=auto`
- Capture updated Ops state: `./ops/bin/dump --scope=core --format=chatgpt --out=auto`
- Ensure the next session begins with refreshed session artifacts and matching receipts.
- Refresh side-effect: `ops/bin/llms` regenerates `ops/lib/manifests/OPS.md` as a compile event; if `OPS.md` is not in the allowlist, restore it before running `integrity.sh`. See `docs/MANUAL.md` Refresh side-effect notice for the full procedure.

### 3.5.1 Mandatory Closing Sidecar
Closing sidecar content is generated and maintained at `storage/handoff/CLOSING-DP-OPS-0195.md` and is validated by `ops/bin/certify` as a hard gate at certification time. Do not author, predict, populate, or approximate any sidecar-derived closeout output outside this sidecar.
Certify separately collects contractor-authored narrative for the RESULTS Contractor Execution Narrative section at certify time via interactive editor prompt; this narrative input is independent from closing sidecar content.

Before running certify, confirm `storage/handoff/CLOSING-DP-OPS-0195.md` has been maintained throughout execution and reflects observable reality only.

Closing sidecar fields required at certify time:
- Commit Message
- Create Pull Request (Title)
- Create Pull Request (Description)
- Confirm Merge (Commit Message)
- Confirm Merge (Extended Description)
- Confirm Merge (Add a Comment)
