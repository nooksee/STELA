---
trace_id: stela-20260320T031144Z-4bbf2e72
packet_id: DP-OPS-0216
created_at: 2026-03-20T03:12:39Z
previous: (none)
---
# STELA TASK DASHBOARD
Status: ACTIVE
Owner: Integrator
Last Updated: 2026-03-19

## 1. Session State
Pointer: Session context output (generated by ops/bin/open)
Context Manifest: ops/lib/manifests/CONTEXT.md (checked by tools/lint/context.sh)

Session State contract:
- Session State is pointer-first. Do not write inline branch or hash state here.
- OPEN is the session state snapshot. OPEN-PORCELAIN is conditional dirty-state detail.
- RESULTS is the audit record for the DP.
- TASK is the stable routing surface.

## 2. Logic Pointers
Primary Constraint: PoT.md (Policy of Truth).
Surface Contract Spec: docs/ops/specs/surfaces/task.md.

### 2.1 Governance Pointers
Jurisdiction: PoT.md Section 3.
Git Authority: PoT.md Section 4.1.
Behavioral Standard: PoT.md Section 4.2.
Staffing Protocol: PoT.md Section 4.1.

### 2.2 Enforcement and Gates
Style: tools/lint/style.sh
Context: tools/lint/context.sh
Truth: tools/lint/truth.sh
DP lint: tools/lint/dp.sh (enforces DP transaction rules in Section 3; does not enforce TASK container schema)
TASK lint: tools/lint/task.sh (sole TASK surface/container schema enforcer)
Repo verify: tools/verify.sh
llms parity: tools/lint/llms.sh

## 3. Current Dispatch Packet (DP)

Section-by-section authoring protocol:
- Generate DP structure from `ops/src/surfaces/dp.md.tpl` using `ops/bin/draft`.
- Do not hand-edit DP structural boilerplate in `TASK.md` after generation.
- Manual edits are limited to slot content only (objective, scope, safety, plan body, receipt body).
- Freeze each completed subsection in order (3.1 -> 3.2 -> 3.3 -> 3.4.x).
- Do not revise earlier sections to justify later work. If scope changes, end the thread and draft a new DP.

Freeze Rule:
- If a completed DP subsection changes later, append an AMENDMENT block in RESULTS; otherwise DP is FAIL.

AMENDMENT: minimum fields
- Reopened section:
- Reason:
- Impact:
- Allowlist change (none or exact paths):

Input discipline (hard rule):
- Worker receives only the DP text. Worker does not require OPEN or DUMP attachments.
- DP writer must not attach or cite disposable artifacts (rough notes, chat logs, scratch drafts, throwaway excerpts).
- DP writer must not include pasted bundles (OPEN, DUMP payloads, manifests) inside the DP body.
- Canon pointers only. Evidence belongs in storage artifacts and RESULTS.



### DP-OPS-0216: Generated planning reset and direct architect intake

## 3.1 Freshness Gate (Must Pass Before Work)
Base Branch: main
Required Work Branch: work/dp-ops-0216-generated-planning-reset-2026-03-19
Base HEAD: 18833d19
Freshness Stamp: 2026-03-19
Note: FRESHNESS_STAMP must be YYYY-MM-DD format only. No trace tokens, no timestamps, no other text. Certify rejects all other forms. dp.sh enforces at lint time.

Required local re-check (worker runs; paste outputs in RESULTS):
- git rev-parse --abbrev-ref HEAD
- git rev-parse --short HEAD
- git status --porcelain

Preconditions:
- No commits on main.
- Working tree must be clean before execution begins.
- If Base HEAD changes, regenerate session artifacts from canonical tools before proceeding.

STOP conditions:
- STOP if any mismatch (branch, Base HEAD, missing work branch).
- STOP if working tree is dirty before execution begins.
- STOP if told to create or switch branches.

## 3.1.1 DP Preflight Gate (Run Before Any Edits)
Purpose:
- Catch malformed DP or TASK schema before work begins.

Worker runs (paste outcome in RESULTS):
- bash tools/lint/dp.sh --test
- bash tools/lint/dp.sh TASK.md
- bash tools/lint/task.sh

STOP if any preflight check fails.

### CbC Design Discipline Preflight (TASK.md §3.1.1)
Required when the DP objective adds, modifies, or replaces a linter, script, guard, or validation binary.
For non-tooling DPs: state "Not applicable" with a one-line justification.
Applicable. This packet rewrites the generated planning contract across analyst, architect, bundle runtime, lint, editor scaffolds, tests, and synced canon.

Invariants:
1. The topic system stays. Analyst still reads `storage/handoff/TOPIC.md` and still emits exactly one fenced markdown block.
2. Generated planning no longer uses slice machinery. No generated `Architect Handoff`, `Selected Option`, `Slice Mode`, `Selected Slices`, `Execution Order`, or `Architect Constraints` fields remain in the formal path.
3. Analyst stays conversational when ambiguity matters and writes final `storage/handoff/PLAN.md` only when the plan is decision-complete.
4. Architect reads the final plan form directly from `Summary`, `Key Changes`, `Test Plan`, and `Assumptions` and may make bounded bridge decisions inside that settled scope.
5. `storage/current/` is removed from generated planning flow, and `storage/dp/processed/` is removed from the repo plus active runtime/doc/lint references.

Structural prevention assessment:
- Strong. The analyst stance, plan template, architect stance, bundle runtime, lint gates, editor scaffold, tests, and synced docs can all be made to encode the simpler planning path directly instead of describing one thing while enforcing another.

Complexity note:
- High but coherent. The change is broad, but it is one contract reset: topic in, analyst discussion first, final plan only when settled, architect reads plan directly, no generated slice machinery, no current-plan planning path, and no processed shelf.

Decision Record Trigger:
- If execution uncovers a real dependency that requires preserving generated slice machinery, preserving `storage/current/` in the planning path, or keeping `storage/dp/processed/` alive outside archives, set `Decision Required: Yes` and author a decision leaf under `archives/decisions/`.
- Otherwise set `Decision Required: No` and `Decision Leaf: None`.

## 3.2 Required Context Load (Read Before Doing Anything)

### 3.2.1 Canon load order (always)
Worker must confirm loaded before edits begin:
1. PoT.md
2. SoP.md
3. PoW.md
4. TASK.md
5. docs/MAP.md
6. docs/MANUAL.md
7. ops/lib/manifests/CONTEXT.md

Notes:
- Worker does not read OPEN. OPEN is for Integrator state refresh and for receipts.
- Disposable artifacts must not be referenced or included.
- Worker input is the DP text only.
- DP writer must not attach or cite disposable artifacts.
- DP writer must not embed pasted bundles.

## Rules

1 Temp path rule uses `var/tmp` only
2 RESULTS generation rule uses `ops/bin/certify` only
3 Receipt rule runs every DP mandated command and treats any skip as STOP
4 Disposable artifact rule treats OPEN dump bundles and manifests as non canonical session artifacts
5 Use repo-relative paths only in all output. Never print absolute filesystem paths. Do not emit clickable file links.

### 3.2.2 DP-scoped load order (per DP)
- storage/handoff/PLAN.md
- ops/src/stances/analyst.md.tpl
- ops/src/stances/plan.md.tpl
- ops/src/stances/architect.md.tpl
- ops/lib/manifests/BUNDLE.md
- ops/lib/scripts/bundle.sh
- ops/lib/scripts/editor.sh
- tools/lint/plan.sh
- tools/lint/response.sh
- tools/lint/style.sh
- tools/test/bundle.sh
- tools/test/editor.sh
- storage/.gitignore
- docs/MANUAL.md
- docs/MAP.md
- docs/ops/specs/stances/analyst.md
- docs/ops/specs/stances/plan.md
- docs/ops/specs/stances/architect.md
- docs/ops/specs/binaries/bundle.md
- docs/ops/specs/binaries/meta.md
- docs/ops/specs/tools/lint/plan.md
- docs/ops/specs/tools/lint/response.md
- docs/ops/specs/tools/lint/style.md
- docs/ops/registry/lint.md

## 3.3 Scope and Safety
Objective:
- Reset generated planning to a simpler path: analyst stays conversational in one fenced markdown block until intent is stable, then emits a final simplified `PLAN.md`; architect reads that plan directly and drafts a usable DP without generated slice machinery.
- Remove `storage/current/` from generated planning flow and remove `storage/dp/processed/` from the live repo surface.

In scope:
- Analyst stance and plan template changes needed for conversational-first planning and final simplified `PLAN.md` output.
- Architect stance and bundle/runtime changes needed to consume plan form directly.
- Plan/response/style lint updates for the new analyst and final-plan contracts.
- Editor scaffold updates for the simplified plan shape.
- Bundle/editor tests and synced docs/specs/registry entries that currently freeze the old generated plan shape.
- Removal of tracked `storage/current/.gitkeep` plus generated-planning references to `storage/current/`.
- Removal of tracked `storage/dp/processed/.gitkeep` plus ignore/runtime/doc/lint references to the processed shelf.

Out of scope:
- Topic surface redesign.
- Audit, foreman, addendum, or execution-decision redesign.
- TASK promotion or closeout-surface redesign.
- Proof-economy and smoke-isolation work beyond the route/editor coverage required by this contract reset.
- Archive history rewrites; archived leaves may still contain historical references.

Safety and invariants:
- No manual generated-output fabrication.
- Keep the single fenced markdown envelope for analyst and architect responses.
- Do not reintroduce generated slice machinery under new names.
- Do not keep `storage/current/` alive as a shadow planning path.
- Do not keep `storage/dp/processed/` alive via `.gitkeep`, ignore carve-outs, or active runtime references.
- Keep `storage/dp/intake/DP.md` as the active latest-wins draft surface.

Worker Constraints (SSOT injected):
## Section 1: Universal Template Rules
- Template source uses `tpl` files with optional YAML frontmatter
- Canon frontmatter keys: `template_type` `template_id` `template_version` `requires_slots` `includes`
- Renderer strips frontmatter before output write
- Slot token form `\{\{TOKEN\}\}` with uppercase alphanumeric underscore
- Include forms `\{\{@include:path\}\}` and `\{\{@include:path#section\}\}`
- Include resolution is strict: missing file fail missing section fail circular graph fail
- Strict mode default: every required slot value present and no unresolved token
- Non strict mode allowed only for lint and normalization workflows
- Worker facing generated surfaces remain pointer first and exclude disposable artifacts

Target Files allowlist (hard gate):
- storage/dp/active/allowlist.txt

## 3.4 Execution Plan (A-E)

### 3.4.1 State
- The live handoff plan now describes the intended simpler system directly: topic stays, analyst stays conversational first, final `PLAN.md` is simplified, architect reads plan form directly, and generated slice machinery is gone.
- The repo still freezes the old planning path in multiple places: analyst explicit plan-output mode, plan template, architect handoff validation, bundle `--slice` handling, route tests, plan lint fixtures, response/style lint, editor scaffold checks, docs, and spec text.
- `storage/current/` and `storage/dp/processed/` still exist through tracked placeholders, ignore carve-outs, and stale planning-path language.
- This packet exists to make the repo match the new planning reality everywhere at once.

### 3.4.2 Request
- Make analyst conversation-first even when the topic asks for a plan; keep one fenced markdown block and keep markdown output.
- Make final generated `PLAN.md` contain only `Summary`, `Key Changes`, `Test Plan`, and `Assumptions`.
- Make architect consume the final plan form directly instead of `Architect Handoff` fields or slice metadata.
- Remove generated slice machinery from runtime, lint, tests, docs, and specs.
- Remove `storage/current/` from generated planning flow and remove `storage/dp/processed/` from the live repo surface.
- Keep the change exhaustive and internally synced so the system can come back on clean `main` without manual repair steps or shadow exceptions.

### 3.4.3 Changelog
- UPDATE storage/dp/active/allowlist.txt
- UPDATE storage/.gitignore
- DELETE storage/current/.gitkeep
- DELETE storage/dp/processed/.gitkeep
- UPDATE ops/src/stances/analyst.md.tpl
- UPDATE ops/src/stances/plan.md.tpl
- UPDATE ops/src/stances/architect.md.tpl
- UPDATE ops/lib/manifests/BUNDLE.md
- UPDATE ops/lib/scripts/bundle.sh
- UPDATE ops/lib/scripts/editor.sh
- UPDATE tools/lint/plan.sh
- UPDATE tools/lint/response.sh
- UPDATE tools/lint/style.sh
- UPDATE tools/test/bundle.sh
- UPDATE tools/test/editor.sh
- UPDATE docs/MANUAL.md
- UPDATE docs/MAP.md
- UPDATE docs/ops/specs/stances/analyst.md
- UPDATE docs/ops/specs/stances/plan.md
- UPDATE docs/ops/specs/stances/architect.md
- UPDATE docs/ops/specs/binaries/bundle.md
- UPDATE docs/ops/specs/binaries/certify.md
- UPDATE docs/ops/specs/binaries/open.md
- UPDATE docs/ops/specs/scripts/bundle.md
- UPDATE docs/ops/specs/tools/lint/plan.md
- UPDATE docs/ops/specs/tools/lint/response.md
- UPDATE docs/ops/specs/tools/lint/style.md
- UPDATE docs/ops/specs/tools/verify.md
- UPDATE docs/ops/registry/binaries.md
- UPDATE docs/ops/registry/scripts.md
- UPDATE docs/ops/registry/lint.md
- UPDATE ops/bin/certify
- UPDATE ops/bin/open

### 3.4.4 Patch / Diff
1. Update `storage/dp/active/allowlist.txt` with the tracked doc/runtime edits plus the `storage/current/.gitkeep`, `storage/dp/processed/.gitkeep`, and `storage/.gitignore` shelf cleanup entries.
2. In `ops/src/stances/analyst.md.tpl` and `ops/src/stances/plan.md.tpl`, remove explicit plan-only slice/handoff output and replace it with the simpler final plan shape plus question-first analyst behavior.
3. In `ops/src/stances/architect.md.tpl`, remove handoff-field and slice dependence, keep the single fenced markdown contract, and make architect read plan form directly while preserving bounded continuity and truth discipline.
4. In `ops/lib/scripts/bundle.sh`, remove architect formal slice machinery and `storage/current/` planning-path use, and make architect routing consume the final simplified plan directly.
5. In `ops/lib/scripts/editor.sh`, `tools/lint/plan.sh`, `tools/lint/response.sh`, and `tools/lint/style.sh`, align scaffolding and validation to the new analyst/final-plan contracts.
6. In `tools/test/bundle.sh` and `tools/test/editor.sh`, replace old slice/handoff fixtures with conversational-analyst and simplified-plan fixtures, and make route/editor proof cover the new path.
7. Remove `storage/current/.gitkeep`, remove `storage/dp/processed/.gitkeep`, clean `storage/.gitignore`, and remove active non-archive references to both retired planning/runtime shelves from lint/runtime/docs/specs touched by this packet.
8. In `ops/bin/open`, remove stale architect `--slice` guidance from generated operator output, and in `ops/bin/certify`, replay and record negated Section 3.4.5 proof commands instead of silently skipping them.
9. In `ops/lib/manifests/BUNDLE.md`, `docs/MANUAL.md`, `docs/MAP.md`, `docs/ops/specs/stances/*.md`, `docs/ops/specs/binaries/*.md`, `docs/ops/specs/scripts/*.md`, `docs/ops/specs/tools/*.md`, and `docs/ops/registry/*.md`, sync canon to the simpler generated planning path with no slice prose and no `storage/current/` planning path.

### 3.4.5 Receipt (Proofs to collect) - MUST RUN

**Mandatory receipt commands (always run; do not omit):**
- bash tools/lint/dp.sh TASK.md
- bash tools/lint/task.sh
- bash tools/lint/integrity.sh
- bash tools/lint/style.sh
- ./ops/bin/llms
- bash tools/lint/integrity.sh
- git diff --name-only
- git diff --stat
- comm -23 <(git diff --name-only | sort) <(sort storage/dp/active/allowlist.txt) || true
- comm -23 <(git ls-files --others --exclude-standard | sort) <(sort storage/dp/active/allowlist.txt) || true
- ./ops/bin/open

**DP-specific receipt commands (scope-specific; author below):**
Note: Command substitution forms (e.g., $(pwd), $(git ...)) are rejected by certify replay. Use literal values only. dp.sh enforces at lint time.
- bash tools/lint/dp.sh --test
- bash tools/lint/plan.sh --test
- bash tools/lint/plan.sh storage/handoff/PLAN.md
- bash tools/lint/response.sh --test
- bash tools/lint/style.sh
- bash tools/lint/integrity.sh
- bash tools/test/bundle.sh --slice=route-contract
- bash tools/test/editor.sh
- ./ops/bin/bundle --profile=architect --out=auto
- ! rg -n "Architect Handoff|Selected Slices|Execution Order|current_plan_source|# DP Plan:" ops/src ops/lib docs/MANUAL.md docs/MAP.md docs/ops/specs docs/ops/registry
- ! test -e storage/current/.gitkeep
- ! test -e storage/dp/processed/.gitkeep
- ! rg -n "storage/dp/processed|dp/processed|DP_PROCESSED_DIR" storage/.gitignore ops/lib/scripts/bundle.sh tools/lint/dp.sh tools/lint/integrity.sh docs/MANUAL.md docs/MAP.md docs/ops/specs docs/ops/registry/lint.md

## 3.5 Closeout (Mandatory Routing)
- Execute docs/MANUAL.md Closeout Cycle in order (Verify, Harvest, Refresh, Log, Prune).
- Update SoP.md and PoW.md with DP entries, including objective summary and current proof / receipt summary.
- Protocol order for closeout: Verify -> Generate Results -> COMMIT (Operator Only) -> Prune.
- Run prune hygiene: ./ops/bin/prune --scrub.
- Refresh llms artifacts: `./ops/bin/llms`
- Regenerate session artifacts: `./ops/bin/open --out=auto`
- Ensure the next session begins with refreshed session artifacts and matching receipts.
- Note: audit dump generation is owned by `./ops/bin/bundle --profile=audit --out=auto` and is separate from operator session refresh. Do not stamp an additional `ops/bin/dump --scope=core` into closeout unless the DP explicitly authorizes a standalone state capture.
- Refresh side-effect: `ops/bin/llms` regenerates `ops/lib/manifests/OPS.md` as a compile event; if `OPS.md` is not in the allowlist, restore it before running `integrity.sh`. See `docs/MANUAL.md` Refresh side-effect notice for the full procedure.

### 3.5.1 Mandatory Closing Sidecar
Closing sidecar content is generated and maintained at `storage/handoff/CLOSING.md` and is validated by `ops/bin/certify` as a hard gate at certification time. Do not author, predict, populate, or approximate any sidecar-derived closeout output outside this sidecar.
Certify separately collects contractor-authored narrative for the RESULTS Contractor Execution Narrative section at certify time via interactive editor prompt; this narrative input is independent from closing sidecar content.

Before running certify, confirm `storage/handoff/CLOSING.md` has been maintained throughout execution and reflects observable reality only.

Closing sidecar fields required at certify time:
- Commit Message
- Create Pull Request (Title)
- Create Pull Request (Description)
- Confirm Merge (Commit Message)
- Confirm Merge (Extended Description)
- Confirm Merge (Add a Comment)
