---
trace_id: stela-20260322T025333Z-631973fc
packet_id: DP-OPS-0217
created_at: 2026-03-22T03:19:48Z
previous: (none)
---
# STELA TASK DASHBOARD
Status: ACTIVE
Owner: Integrator
Last Updated: 2026-03-19

## 1. Session State
Pointer: Session context output (generated by ops/bin/open)
Context Manifest: ops/lib/manifests/CONTEXT.md (checked by tools/lint/context.sh)

Session State contract:
- Session State is pointer-first. Do not write inline branch or hash state here.
- OPEN is the session state snapshot. OPEN-PORCELAIN is conditional dirty-state detail.
- RESULTS is the audit record for the DP.
- TASK is the stable routing surface.

## 2. Logic Pointers
Primary Constraint: PoT.md (Policy of Truth).
Surface Contract Spec: docs/ops/specs/surfaces/task.md.

### 2.1 Governance Pointers
Jurisdiction: PoT.md Section 3.
Git Authority: PoT.md Section 4.1.
Behavioral Standard: PoT.md Section 4.2.
Staffing Protocol: PoT.md Section 4.1.

### 2.2 Enforcement and Gates
Style: tools/lint/style.sh
Context: tools/lint/context.sh
Truth: tools/lint/truth.sh
DP lint: tools/lint/dp.sh (enforces DP transaction rules in Section 3; does not enforce TASK container schema)
TASK lint: tools/lint/task.sh (sole TASK surface/container schema enforcer)
Repo verify: tools/verify.sh
llms parity: tools/lint/llms.sh

## 3. Current Dispatch Packet (DP)

Section-by-section authoring protocol:
- Generate DP structure from `ops/src/surfaces/dp.md.tpl` using `ops/bin/draft`.
- Do not hand-edit DP structural boilerplate in `TASK.md` after generation.
- Manual edits are limited to slot content only (objective, scope, safety, plan body, receipt body).
- Freeze each completed subsection in order (3.1 -> 3.2 -> 3.3 -> 3.4.x).
- Do not revise earlier sections to justify later work. If scope changes, end the thread and draft a new DP.

Freeze Rule:
- If a completed DP subsection changes later, append an AMENDMENT block in RESULTS; otherwise DP is FAIL.

AMENDMENT: minimum fields
- Reopened section:
- Reason:
- Impact:
- Allowlist change (none or exact paths):

Input discipline (hard rule):
- Worker receives only the DP text. Worker does not require OPEN or DUMP attachments.
- DP writer must not attach or cite disposable artifacts (rough notes, chat logs, scratch drafts, throwaway excerpts).
- DP writer must not include pasted bundles (OPEN, DUMP payloads, manifests) inside the DP body.
- Canon pointers only. Evidence belongs in storage artifacts and RESULTS.



### DP-OPS-0217: Rewrite bundle smoke into a kernel-only transport test

## 3.1 Freshness Gate (Must Pass Before Work)
Base Branch: main
Required Work Branch: work/dp-ops-0217-bundle-smoke-kernel-rewrite-2026-03-21
Base HEAD: ee91d8a1
Freshness Stamp: 2026-03-21
Note: FRESHNESS_STAMP must be YYYY-MM-DD format only. No trace tokens, no timestamps, no other text. Certify rejects all other forms. dp.sh enforces at lint time.

Required local re-check (worker runs; paste outputs in RESULTS):
- git rev-parse --abbrev-ref HEAD
- git rev-parse --short HEAD
- git status --porcelain

Preconditions:
- No commits on main.
- Working tree must be clean before execution begins.
- If Base HEAD changes, regenerate session artifacts from canonical tools before proceeding.

STOP conditions:
- STOP if any mismatch (branch, Base HEAD, missing work branch).
- STOP if working tree is dirty before execution begins.
- STOP if told to create or switch branches.

## 3.1.1 DP Preflight Gate (Run Before Any Edits)
Purpose:
- Catch malformed DP or TASK schema before work begins.

Worker runs (paste outcome in RESULTS):
- bash tools/lint/dp.sh --test
- bash tools/lint/dp.sh TASK.md
- bash tools/lint/task.sh

STOP if any preflight check fails.

### CbC Design Discipline Preflight (TASK.md §3.1.1)
Required when the DP objective adds, modifies, or replaces a linter, script, guard, or validation binary.
For non-tooling DPs: state "Not applicable" with a one-line justification.
Applicable. This packet rewrites `tools/test/bundle.sh`, a critical validation binary, and adds one test-isolation env var to `ops/lib/scripts/bundle.sh`.

Invariants:
1. Public entrypoint stays `bash tools/test/bundle.sh`; no CLI-visible interface change.
2. The test never writes to live `TASK.md`, `storage/handoff/`, `storage/current/`, or `storage/dp/`; isolation is enforced through an ephemeral runtime root under `var/tmp/_smoke/`.
3. `--mode=certify-critical` skips when no watched bundle-kernel file appears in tracked modifications or untracked additions; runs the full minimal kernel suite when any watched file changed.
4. Explicit `--slice=...` always forces execution of that slice regardless of mode.
5. All temp outputs are confined to `var/tmp/_smoke/`; cleanup removes only the active run's ephemeral root.

Structural prevention assessment:
- Strong. Full live-surface isolation is achievable by routing all bundle invocations through an ephemeral root and adding one env var (`BUNDLE_TEST_HANDOFF_ROOT`) to `ops/lib/scripts/bundle.sh` that, when set, redirects handoff input surface reads (TOPIC.md, PLAN.md, RESULTS.md, CLOSING.md) and TASK resolution to the specified root. The `--mode=certify-critical` skip gate is preventable by inspecting the git change set against a declared watched-path set at test entry.

Complexity note:
- Moderate but contained. The test rewrite is bounded: one ephemeral root pattern, one watched-path gate, four narrow slices. The result is structurally simpler than the monolith it replaces.

Decision Record Trigger:
- If execution uncovers a real dependency requiring the test to read live `TASK.md` directly for packet-ID computation in a way that cannot be served through a synthetic surface, set `Decision Required: Yes` and author a decision leaf under `archives/decisions/`.
- Otherwise set `Decision Required: No` and `Decision Leaf: None`.

## 3.2 Required Context Load (Read Before Doing Anything)

### 3.2.1 Canon load order (always)
Worker must confirm loaded before edits begin:
1. PoT.md
2. SoP.md
3. PoW.md
4. TASK.md
5. docs/MAP.md
6. docs/MANUAL.md
7. ops/lib/manifests/CONTEXT.md

Notes:
- Worker does not read OPEN. OPEN is for Integrator state refresh and for receipts.
- Disposable artifacts must not be referenced or included.
- Worker input is the DP text only.
- DP writer must not attach or cite disposable artifacts.
- DP writer must not embed pasted bundles.

## Rules

1 Temp path rule uses `var/tmp` only
2 RESULTS generation rule uses `ops/bin/certify` only
3 Receipt rule runs every DP mandated command and treats any skip as STOP
4 Disposable artifact rule treats OPEN dump bundles and manifests as non canonical session artifacts
5 Use repo-relative paths only in all output. Never print absolute filesystem paths. Do not emit clickable file links.

### 3.2.2 DP-scoped load order (per DP)
- tools/test/bundle.sh
- ops/lib/scripts/bundle.sh
- ops/lib/manifests/BUNDLE.md
- docs/ops/specs/tools/test/bundle.md
- docs/ops/registry/test.md
- docs/ops/specs/binaries/bundle.md
- docs/ops/specs/scripts/bundle.md

## 3.3 Scope and Safety
Objective:
- Replace `tools/test/bundle.sh` with a narrow transport-kernel test that uses an isolated ephemeral runtime root under `var/tmp/_smoke/`, proves only bundle-kernel truth (profile routing, request assembly, package membership, dump/manifest/persistence coherence, fail-closed behavior, and audit rerun lineage identity), and gates `--mode=certify-critical` on watched bundle-kernel path changes rather than running on every unrelated packet closeout.

In scope:
- Full rewrite of `tools/test/bundle.sh`: thin runner plus one small helper library; combined implementation in the low hundreds of lines.
- Internal, test-only ephemeral runtime root: each test invocation generates a unique `var/tmp/_smoke/<run-id>/` root; all bundle invocations within the test run are routed through that root.
- One surgical addition to `ops/lib/scripts/bundle.sh`: `BUNDLE_TEST_HANDOFF_ROOT` env var; when set, bundle reads handoff input surfaces (TOPIC.md, PLAN.md, RESULTS.md, CLOSING.md) and resolves TASK from the specified root instead of `$REPO_ROOT`; output routing is unchanged; no profile, routing, or business-logic changes.
- New public slice surface: `route-contract`, `package-contract`, `fail-closed`, `rerun-lineage`.
- Removal of `closeout-sanity` and `audit-coherence` slices and all related backup/restore fixture machinery.
- `--mode=certify-critical` smart gate: inspects `git diff --name-only` and `git ls-files --others --exclude-standard` against the watched set at test entry; skips if no watched path changed; runs full minimal suite if any watched path changed.
- Watched paths: `ops/bin/bundle`, `ops/lib/scripts/bundle.sh`, `ops/lib/manifests/BUNDLE.md`, `tools/test/bundle.sh`.
- Update `docs/ops/specs/tools/test/bundle.md` to reflect kernel-only role, four new slices, ephemeral isolation, and `--mode=certify-critical` watched-path behavior.
- Update `docs/ops/registry/test.md` (`TEST-02` entry) to match new kernel-only scope and slice surface.
- Update `storage/dp/active/allowlist.txt` for all files touched by this packet.

Out of scope:
- Changes to bundle profiles, routing logic, or business rules in `ops/lib/scripts/bundle.sh` beyond the one test-isolation env var.
- Changes to `ops/lib/manifests/BUNDLE.md` policy entries.
- Changes to certify, audit, or any surface not listed in scope.
- New operator workflows or new bundle profiles.
- Archive history rewrites; archived leaves may still reference historical slice names.

Safety and invariants:
- No manual generated-output fabrication.
- Public command `bash tools/test/bundle.sh` is preserved without modification.
- Before/after `git status --porcelain` is unchanged by the test run.
- Test never writes to live `TASK.md`, `storage/handoff/`, `storage/current/`, or `storage/dp/`.
- All temp outputs confined to `var/tmp/_smoke/`; cleanup removes only the active run's ephemeral root.
- `BUNDLE_TEST_HANDOFF_ROOT` in `ops/lib/scripts/bundle.sh` is active only when explicitly set; no operator workflow sets or depends on it.
- No git worktree or alternate repo creation.
- `--mode=certify-critical` must not run the full analyst/architect/ATS/meta matrix.

Worker Constraints (SSOT injected):
## Section 1: Universal Template Rules
- Template source uses `tpl` files with optional YAML frontmatter
- Canon frontmatter keys: `template_type` `template_id` `template_version` `requires_slots` `includes`
- Renderer strips frontmatter before output write
- Slot token form `\{\{TOKEN\}\}` with uppercase alphanumeric underscore
- Include forms `\{\{@include:path\}\}` and `\{\{@include:path#section\}\}`
- Include resolution is strict: missing file fail missing section fail circular graph fail
- Strict mode default: every required slot value present and no unresolved token
- Non strict mode allowed only for lint and normalization workflows
- Worker facing generated surfaces remain pointer first and exclude disposable artifacts

Target Files allowlist (hard gate):
- storage/dp/active/allowlist.txt

## 3.4 Execution Plan (A-E)

### 3.4.1 State
- `tools/test/bundle.sh` is a ~2000-line shell monolith that mutates live repo surfaces (`TASK.md`, `storage/handoff/PLAN.md`, `storage/handoff/RESULTS.md`, `storage/handoff/CLOSING.md`) using backup/restore machinery (`ARCHITECT_PLAN_BACKUP`, `AUDIT_TASK_BACKUP`, `AUDIT_RESULTS_BACKUP`, `AUDIT_CLOSING_BACKUP`) and a cleanup path array.
- Current slices: `closeout-sanity`, `audit-coherence`, `route-contract`, `rerun-lineage`; none scoped to bundle-kernel truth alone.
- `--mode=certify-critical` exists but runs bounded closeout-safe slices on every invocation without inspecting which files changed.
- `ops/lib/scripts/bundle.sh` already reads `smoke_handoff_root=var/tmp/_smoke/handoff` and `smoke_dump_root=var/tmp/_smoke/dumps` from `BUNDLE.md` for output routing, but has no mechanism to redirect its handoff input surface reads away from `$REPO_ROOT/storage/handoff/`.
- `docs/ops/specs/tools/test/bundle.md` and `docs/ops/registry/test.md` describe the current four-slice structure including `closeout-sanity` and `audit-coherence`.

### 3.4.2 Request
1. Add `BUNDLE_TEST_HANDOFF_ROOT` env var to `ops/lib/scripts/bundle.sh`: after `REPO_ROOT` is set, if `BUNDLE_TEST_HANDOFF_ROOT` is non-empty, use it as the base path for all handoff input surface reads (TOPIC.md, PLAN.md, RESULTS.md, CLOSING.md) and TASK resolution. Output routing (`smoke_handoff_root`, `smoke_dump_root`) is unchanged. No other bundle behavior changes.
2. Rewrite `tools/test/bundle.sh` as a thin runner plus one small helper library:
   - At start: generate a unique ephemeral run root at `var/tmp/_smoke/<run-id>/`; export `BUNDLE_TEST_HANDOFF_ROOT` pointing to that root; export `BUNDLE_SMOKE_HANDOFF_ROOT` and `BUNDLE_SMOKE_DUMP_ROOT` to paths within that root.
   - Create only the minimal synthetic surfaces needed inside the ephemeral root for each slice (synthetic TOPIC.md, synthetic PLAN.md with Summary/Key Changes/Test Plan/Assumptions sections, synthetic RESULTS.md, synthetic CLOSING.md, and a synthetic TASK surface as needed for audit/rerun slices).
   - Cleanup removes only `var/tmp/_smoke/<run-id>/`; no live surface backup or restore machinery.
3. Implement four public slices against structured sources (bundle manifest JSON, dump manifest, package listing, exit status):
   - `route-contract`: analyst, architect, alias, and project/foreman route behavior; assert profile routing, artifact naming prefix, dump manifest persistence profile; do not assert stance wording or shipping-spine prose.
   - `package-contract`: required/forbidden package membership per profile; assert analyst package includes TOPIC.md and excludes PLAN.md; assert architect package includes PLAN.md only; assert audit package includes resolved RESULTS and CLOSING files; do not assert human-facing prose.
   - `fail-closed`: missing TOPIC.md fails analyst; missing PLAN.md fails architect; missing RESULTS.md or CLOSING.md fails audit; invalid profile and invalid rerun intent fail closed.
   - `rerun-lineage`: initial audit stays `AUDIT-*`; repeat submission without `--rerun` stays `AUDIT-*` even when prior artifact exists; explicit `--rerun` becomes `AUDIT-R1-*`; rerun manifest records `submission.kind: audit_resubmission`, `submission.resubmission_index: 1`, and `submission.supersedes_bundle_path`.
4. Implement `--mode=certify-critical` smart gate: inspect `git diff --name-only` and `git ls-files --others --exclude-standard` at test entry; if no watched path (`ops/bin/bundle`, `ops/lib/scripts/bundle.sh`, `ops/lib/manifests/BUNDLE.md`, `tools/test/bundle.sh`) appears in the combined set, print one skip line and exit 0; otherwise run all four slices. Explicit `--slice=...` always forces execution regardless of mode.
5. Update `docs/ops/specs/tools/test/bundle.md`: replace Purpose, Invocation, Slice Contract, Invariants and Failure Modes, and Cleanup Contract sections to reflect the four new slices, kernel-only scope, ephemeral isolation contract, `--mode=certify-critical` watched-path behavior, and watched path list.
6. Update `docs/ops/registry/test.md` `TEST-02` row Notes field to reflect kernel-only scope and new slice surface.

### 3.4.3 Changelog
- UPDATE storage/dp/active/allowlist.txt
- UPDATE ops/lib/scripts/bundle.sh
- UPDATE tools/test/bundle.sh
- UPDATE docs/ops/specs/tools/test/bundle.md
- UPDATE docs/ops/registry/test.md

### 3.4.4 Patch / Diff
1. In `storage/dp/active/allowlist.txt`, add `storage/handoff/CLOSING-DP-OPS-0217.md` and `archives/surfaces/TASK-DP-OPS-0217-*.md`; add any additional files touched by this packet not already present.
2. In `ops/lib/scripts/bundle.sh`, after `REPO_ROOT` is resolved: add a `BUNDLE_TEST_HANDOFF_ROOT` block — if the env var is non-empty, set `BUNDLE_HANDOFF_BASE="${BUNDLE_TEST_HANDOFF_ROOT}"`, else `BUNDLE_HANDOFF_BASE="${REPO_ROOT}/storage/handoff"`. Replace all direct reads of `${REPO_ROOT}/storage/handoff/<surface>` for input path resolution with `${BUNDLE_HANDOFF_BASE}/<surface>`. Apply the same redirection to TASK resolution: if `BUNDLE_TEST_HANDOFF_ROOT` is set, resolve TASK from `${BUNDLE_TEST_HANDOFF_ROOT}/TASK.md`, else from `${REPO_ROOT}/TASK.md`. Output routing vars (`BUNDLE_SMOKE_HANDOFF_ROOT`, `BUNDLE_SMOKE_DUMP_ROOT`) remain derived from `BUNDLE.md` and are unchanged.
3. Rewrite `tools/test/bundle.sh`: implement runner, ephemeral root generation (`var/tmp/_smoke/<run-id>/`), `BUNDLE_TEST_HANDOFF_ROOT` export, `BUNDLE_SMOKE_HANDOFF_ROOT` and `BUNDLE_SMOKE_DUMP_ROOT` export to paths within the ephemeral root, synthetic surface creation per slice, four slice functions (`route-contract`, `package-contract`, `fail-closed`, `rerun-lineage`), `--mode=certify-critical` watched-path gate, and cleanup trap confined to `var/tmp/_smoke/<run-id>/`. Remove all backup/restore machinery, cleanup path arrays, and `closeout-sanity`/`audit-coherence` slice logic.
4. In `docs/ops/specs/tools/test/bundle.md`, rewrite the Purpose, Invocation, Slice Contract, Invariants and Failure Modes, and Cleanup Contract sections to match the new kernel-only test: four slices, ephemeral isolation, `--mode=certify-critical` watched-path behavior, and watched path list.
5. In `docs/ops/registry/test.md`, update the `TEST-02` row Notes field to: `Spec: docs/ops/specs/tools/test/bundle.md. Verifies bundle transport-kernel truth: profile routing (route-contract), package membership (package-contract), fail-closed behavior (fail-closed), and audit rerun lineage identity (rerun-lineage). Uses an isolated ephemeral runtime root under var/tmp/_smoke/; --mode=certify-critical skips when no watched bundle-kernel file changed.`

### 3.4.5 Receipt (Proofs to collect) - MUST RUN

**Mandatory receipt commands (always run; do not omit):**
- bash tools/lint/dp.sh TASK.md
- bash tools/lint/task.sh
- bash tools/lint/integrity.sh
- bash tools/lint/style.sh
- ./ops/bin/llms
- bash tools/lint/integrity.sh
- git diff --name-only
- git diff --stat
- comm -23 <(git diff --name-only | sort) <(sort storage/dp/active/allowlist.txt) || true
- comm -23 <(git ls-files --others --exclude-standard | sort) <(sort storage/dp/active/allowlist.txt) || true
- ./ops/bin/open

**DP-specific receipt commands (scope-specific; author below):**
Note: Command substitution forms (e.g., $(pwd), $(git ...)) are rejected by certify replay. Use literal values only. dp.sh enforces at lint time.
- bash tools/lint/dp.sh --test
- bash tools/lint/integrity.sh
- bash tools/test/bundle.sh --slice=route-contract
- bash tools/test/bundle.sh --slice=package-contract
- bash tools/test/bundle.sh --slice=fail-closed
- bash tools/test/bundle.sh --slice=rerun-lineage
- bash tools/test/bundle.sh
- bash tools/test/bundle.sh --mode=certify-critical
- git status --porcelain
- ! grep -rn "closeout-sanity\|audit-coherence\|ARCHITECT_PLAN_BACKUP\|AUDIT_TASK_BACKUP" tools/test/bundle.sh

## 3.5 Closeout (Mandatory Routing)
- Execute docs/MANUAL.md Closeout Cycle in order (Verify, Harvest, Refresh, Log, Prune).
- Update SoP.md and PoW.md with DP entries, including objective summary and current proof / receipt summary.
- Protocol order for closeout: Verify -> Generate Results -> COMMIT (Operator Only) -> Prune.
- Run prune hygiene: ./ops/bin/prune --scrub.
- Refresh llms artifacts: `./ops/bin/llms`
- Regenerate session artifacts: `./ops/bin/open --out=auto`
- Ensure the next session begins with refreshed session artifacts and matching receipts.
- Note: audit dump generation is owned by `./ops/bin/bundle --profile=audit --out=auto` and is separate from operator session refresh. Do not stamp an additional `ops/bin/dump --scope=core` into closeout unless the DP explicitly authorizes a standalone state capture.
- Refresh side-effect: `ops/bin/llms` regenerates `ops/lib/manifests/OPS.md` as a compile event; if `OPS.md` is not in the allowlist, restore it before running `integrity.sh`. See `docs/MANUAL.md` Refresh side-effect notice for the full procedure.

### 3.5.1 Mandatory Closing Sidecar
Closing sidecar content is generated and maintained at `storage/handoff/CLOSING.md` and is validated by `ops/bin/certify` as a hard gate at certification time. Do not author, predict, populate, or approximate any sidecar-derived closeout output outside this sidecar.
Certify separately collects contractor-authored narrative for the RESULTS Contractor Execution Narrative section at certify time via interactive editor prompt; this narrative input is independent from closing sidecar content.

Before running certify, confirm `storage/handoff/CLOSING.md` has been maintained throughout execution and reflects observable reality only.

Closing sidecar fields required at certify time:
- Commit Message
- Create Pull Request (Title)
- Create Pull Request (Description)
- Confirm Merge (Commit Message)
- Confirm Merge (Extended Description)
- Confirm Merge (Add a Comment)
