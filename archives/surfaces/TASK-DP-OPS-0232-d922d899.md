---
trace_id: stela-20260325T130442Z-0cc33cc3
packet_id: DP-OPS-0232
created_at: 2026-03-25T13:24:47Z
previous: archives/surfaces/TASK-DP-OPS-0231-091a79ba.md
---
# STELA TASK DASHBOARD
Status: ACTIVE
Owner: Integrator
Last Updated: 2026-03-19

## 1. Session State
Pointer: Session context output (generated by ops/bin/open)
Context Manifest: ops/lib/manifests/CONTEXT.md (checked by tools/lint/context.sh)

Session State contract:
- Session State is pointer-first. Do not write inline branch or hash state here.
- OPEN is the session state snapshot. OPEN-PORCELAIN is conditional dirty-state detail.
- RESULTS is the audit record for the DP.
- TASK is the stable routing surface.

## 2. Logic Pointers
Primary Constraint: PoT.md (Policy of Truth).
Surface Contract Spec: docs/ops/specs/surfaces/task.md.

### 2.1 Governance Pointers
Jurisdiction: PoT.md Section 3.
Git Authority: PoT.md Section 4.1.
Behavioral Standard: PoT.md Section 4.2.
Staffing Protocol: PoT.md Section 4.1.

### 2.2 Enforcement and Gates
Style: tools/lint/style.sh
Context: tools/lint/context.sh
Truth: tools/lint/truth.sh
DP lint: tools/lint/dp.sh (enforces DP transaction rules in Section 3; does not enforce TASK container schema)
TASK lint: tools/lint/task.sh (sole TASK surface/container schema enforcer)
Repo verify: tools/verify.sh
llms parity: tools/lint/llms.sh

## 3. Current Dispatch Packet (DP)

Section-by-section authoring protocol:
- Generate DP structure from `ops/src/surfaces/dp.md.tpl` using `ops/bin/draft`.
- Do not hand-edit DP structural boilerplate in `TASK.md` after generation.
- Manual edits are limited to slot content only (objective, scope, safety, plan body, receipt body).
- Freeze each completed subsection in order (3.1 -> 3.2 -> 3.3 -> 3.4.x).
- Do not revise earlier sections to justify later work. If scope changes, end the thread and draft a new DP.

Freeze Rule:
- If a completed DP subsection changes later, append an AMENDMENT block in RESULTS; otherwise DP is FAIL.

AMENDMENT: minimum fields
- Reopened section:
- Reason:
- Impact:
- Allowlist change (none or exact paths):

Input discipline (hard rule):
- Worker receives only the DP text. Worker does not require OPEN or DUMP attachments.
- DP writer must not attach or cite disposable artifacts (rough notes, chat logs, scratch drafts, throwaway excerpts).
- DP writer must not include pasted bundles (OPEN, DUMP payloads, manifests) inside the DP body.
- Canon pointers only. Evidence belongs in storage artifacts and RESULTS.



### DP-OPS-0232: MANUAL and Operator-Surface Wayfinding Cleanup

## 3.1 Freshness Gate (Must Pass Before Work)
Base Branch: main
Work Branch: work/dp-ops-0232-manual-wayfinding-2026-03-25
Base HEAD: d922d899
Freshness Stamp: 2026-03-25
Note: FRESHNESS_STAMP must be YYYY-MM-DD format only. No trace tokens, no timestamps, no other text. Certify rejects all other forms. dp.sh enforces at lint time.

Required local re-check (worker runs before any edits; paste verbatim outputs under `## Contractor Execution Narrative` -> `### Preflight State` in RESULTS):
- git rev-parse --abbrev-ref HEAD
- git rev-parse --short HEAD
- git status --porcelain

These three commands are freshness proof only. Do not treat §3.4.5 receipt replay as a substitute for their pre-edit outputs.

Preconditions:
- No commits on main.
- Working tree must be clean before execution begins.
- If Base HEAD changes, regenerate session artifacts from canonical tools before proceeding.

STOP conditions:
- STOP if any mismatch (branch, Base HEAD, missing work branch).
- STOP if working tree is dirty before execution begins.
- STOP if told to create or switch branches.

## 3.1.1 DP Preflight Gate (Run Before Any Edits)
Purpose:
- Catch malformed DP or TASK schema before work begins.

Worker runs (paste outcome in RESULTS):
- bash tools/lint/dp.sh --test
- bash tools/lint/dp.sh TASK.md
- bash tools/lint/task.sh

STOP if any preflight check fails.

### CbC Design Discipline Preflight (TASK.md §3.1.1)
Not applicable. This DP makes prose, wording, and wayfinding alignment changes only. No linter, script guard, or validation binary is added, modified, or replaced. Changes to `ops/bin/help` and `ops/lib/scripts/bundle.sh` are limited to UI heredoc text and a variable string assignment; no logic, exit-code, or guard behavior changes.

## 3.2 Required Context Load (Read Before Doing Anything)

### 3.2.1 Canon load order (always)
Worker must confirm loaded before edits begin:
1. PoT.md
2. SoP.md
3. PoW.md
4. TASK.md
5. docs/MAP.md
6. docs/MANUAL.md
7. ops/lib/manifests/CONTEXT.md

Notes:
- Worker does not read OPEN. OPEN is for Integrator state refresh and for receipts.
- Disposable artifacts must not be referenced or included.
- Worker input is the DP text only.
- DP writer must not attach or cite disposable artifacts.
- DP writer must not embed pasted bundles.

## Rules

1 Temp path rule uses `var/tmp` only
2 RESULTS generation rule uses `ops/bin/certify` only
3 Receipt rule runs every DP mandated command and treats any skip as STOP
4 Disposable artifact rule treats OPEN dump bundles and manifests as non canonical session artifacts
5 Use repo-relative paths only in all output. Never print absolute filesystem paths. Do not emit clickable file links.

### 3.2.2 DP-scoped load order (per DP)
- docs/MANUAL.md (primary target; read Section 0 Mechanical Workflow in full)
- ops/bin/help (primary target; read show_menu and show_curriculum functions in full)
- docs/ops/specs/binaries/help.md (inspect for accuracy after help changes)
- ops/src/surfaces/task.md.tpl (primary target; read Sections 2.2 and 3 in full)
- docs/ops/specs/surfaces/task.md (primary target; read DP Generation Contract and Enforcement sections)
- ops/lib/scripts/bundle.sh (target: read bundle_render_draft_authoring_scaffold function)
- ops/src/stances/draft.md.tpl (target: read Work Branch instruction in CONTENT REQUIREMENTS)

## 3.3 Scope and Safety
Objective:
Align operator-facing prose in `docs/MANUAL.md`, `ops/bin/help`, `ops/src/surfaces/task.md.tpl`, `docs/ops/specs/surfaces/task.md`, `ops/lib/scripts/bundle.sh`, and `ops/src/stances/draft.md.tpl` with the live post-0231 system: bundle-first worker-ready draft path, current results/llms validation truth, and correct OPEN/bundle authority boundaries. This is a prose and wayfinding alignment pass; runtime mechanics, constitutional branch doctrine, and OPEN authority are already landed and remain unchanged.

In scope:
- `docs/MANUAL.md`: Replace Execution Cycle Step 2 (`./ops/bin/draft` as standard draft step) with bundle-first path; add explicit non-bundle repo-sharing path block in Mechanical Workflow.
- `ops/bin/help`: Fix `show_curriculum` closeout commands (replace dead `bash tools/lint/llms.sh` and wrong `bash tools/lint/dp.sh storage/handoff/RESULTS.md` with `bash tools/lint/results.sh storage/handoff/RESULTS.md`); update `show_menu` Quick Start to include bundle-first draft step and update `draft` command description to name bundle as the standard operator path.
- `docs/ops/specs/binaries/help.md`: Read and confirm accuracy after help changes; make no content change unless the abstract description becomes factually wrong.
- `ops/src/surfaces/task.md.tpl`: Remove dead `tools/lint/llms.sh` reference in `### 2.2 Enforcement and Gates`; update DP generation description in Section 3 to reflect bundle-first path; remove dead `bash tools/lint/llms.sh` from the `### 3.4.5 Receipt` command list.
- `docs/ops/specs/surfaces/task.md`: Remove false claim that `tools/lint/dp.sh` validates RESULTS artifacts; replace stale `./ops/bin/open then ./ops/bin/draft` standard generation flow with bundle-first description.
- `ops/lib/scripts/bundle.sh`: Change `suggested_work_branch` in `bundle_render_draft_authoring_scaffold` from a pre-filled packet-id/date literal to the canonical PoT.md §6.2.1 pattern `work/<DP-ID>-YYYY-MM-DD`, removing rote branch-name literal prefill.
- `ops/src/stances/draft.md.tpl`: Update Work Branch instruction in CONTENT REQUIREMENTS to remove the "preserve packet-id/date framing already present" guidance that assumed bundle pre-fill, replacing it with an instruction to author the branch following PoT.md §6.2.1 form.

Out of scope:
- Runtime mechanics of `ops/bin/draft`, `ops/bin/bundle`, `ops/bin/certify`, or any binary other than the specific prose changes listed above.
- Constitutional branch doctrine (`PoT.md` §6.2.x) — already landed and unchanged.
- OPEN authority mechanics — already landed; this pass only aligns prose descriptions.
- `docs/ops/specs/binaries/draft.md`, `docs/ops/specs/binaries/bundle.md`, or any spec not listed in scope.
- `ops/src/surfaces/dp.md.tpl` — no changes; this DP does not modify the DP template or require a dp.sh hash update.
- `tools/lint/dp.sh` — no changes.
- Any project files, `opt/_factory/`, `projects/`, or surfaces outside the explicit allowlist.
- Worker-facing DP text, certify behavior, or RESULTS schema.

Safety and invariants:
- No manual generation of RESULTS, CLOSING, or audit bundle output.
- No structural edits to TASK.md or dp.md.tpl.
- All edits are prose/text replacements within existing file structure; no new sections, no file deletions.
- Allowlist hard gate: only files in `storage/dp/active/allowlist.txt` may be modified.
- Do not edit `PoT.md`, `SoP.md`, `PoW.md`, `TASK.md`, or any `archives/` leaf.
- Do not change any binary exit-code logic, validation semantics, or guard conditions.
- Changes to `ops/lib/scripts/bundle.sh` are limited to the single variable assignment `suggested_work_branch=...` inside `bundle_render_draft_authoring_scaffold`; all surrounding logic is no-touch.
- Changes to `ops/src/stances/draft.md.tpl` are limited to the Work Branch bullet in the CONTENT REQUIREMENTS step; all surrounding stance contract text, shared includes, and output rules are no-touch.

Target Files allowlist (hard gate):
- storage/dp/active/allowlist.txt

## 3.4 Execution Plan (A-E)

### 3.4.1 State
Branch `main` at HEAD `d922d899` with clean working tree. Bundle generated at `2026-03-25T12:22:16Z` with resolved profile `draft`. PLAN.md present and lint-status PASS. Active packet id is DP-OPS-0232.

Direct inspection of attached artifacts confirms the following defects in scope:

- `docs/MANUAL.md` Execution Cycle Step 2 still presents `./ops/bin/draft` as the standard draft step; the `### Draft Workflow` section (also in `docs/MANUAL.md`) already describes the live bundle-first path correctly, creating two competing stories in the same file.
- `ops/bin/help` `show_curriculum` Closeout Sequence references the dead script `bash tools/lint/llms.sh` and uses the wrong command `bash tools/lint/dp.sh storage/handoff/RESULTS.md` instead of `bash tools/lint/results.sh storage/handoff/RESULTS.md`. `show_menu` Quick Start has no bundle-first draft step and the `draft` command description presents it as a standalone tool without noting that bundle is the standard operator path.
- `ops/src/surfaces/task.md.tpl` Section 2.2 references dead `tools/lint/llms.sh`; Section 3 authoring protocol presents `ops/bin/draft` as the generation tool without noting the bundle-first standard; the `### 3.4.5 Receipt` command list contains `bash tools/lint/llms.sh`.
- `docs/ops/specs/surfaces/task.md` Enforcement section claims `tools/lint/dp.sh` validates RESULTS artifacts (false; `tools/lint/results.sh` validates RESULTS); DP Generation Contract section states `Standard generation flow is ./ops/bin/open then ./ops/bin/draft` (stale).
- `ops/lib/scripts/bundle.sh` `bundle_render_draft_authoring_scaffold` pre-fills `suggested_work_branch` with a specific packet-id/date literal (`work/${packet_id_branch_fragment}-<replace-branch-slug>-${freshness_stamp}`), handing Architect a rote branch-name literal to copy mechanically.
- `ops/src/stances/draft.md.tpl` Work Branch instruction says "preserve packet-id/date framing already present in the scaffold when present," which is residue from the pre-fill pattern and will be wrong after bundle.sh is fixed.
- `docs/MANUAL.md` Mechanical Workflow section does not include an explicit non-bundle repo-sharing path.

`tools/lint/llms.sh` does not exist in the repo (confirmed: no `FILE BEGIN: tools/lint/llms` entry in full dump index). `tools/lint/results.sh` exists at `tools/lint/results.sh`. `docs/ops/specs/binaries/help.md` abstract description ("With no argument it prints the command index and quick-start menu. In `curriculum` mode it prints startup and closeout command sequences.") remains accurate after the planned help changes and needs no content edit.

### 3.4.2 Request
1. Replace Execution Cycle Step 2 in `docs/MANUAL.md` so it describes the bundle-first draft path (`./ops/bin/bundle --profile=draft --out=auto`, Architect scaffold authoring, save to `storage/dp/intake/DP.md`, lint before dispatch) instead of `./ops/bin/draft`.
2. Add a `**Non-Bundle Repo-Sharing Path:**` block to `docs/MANUAL.md` Mechanical Workflow immediately after the OPEN and OPEN-PORCELAIN Contract block, documenting: `./ops/bin/open --out=auto` then `./ops/bin/dump --scope=core --format=chatgpt --out=auto`, with `--scope=platform` called out as opt-in for factory content, and a clear note that this path is separate from bundle-mediated transport.
3. In `ops/bin/help` `show_curriculum`: (a) replace `bash tools/lint/dp.sh storage/handoff/RESULTS.md` with `bash tools/lint/results.sh storage/handoff/RESULTS.md`; (b) remove the dead `bash tools/lint/llms.sh` line.
4. In `ops/bin/help` `show_menu`: (a) add a step `3) Draft:` between the existing Open step (step 2) and the Preflight Gates step, renumbering subsequent steps; the new step describes `./ops/bin/bundle --profile=draft --out=auto` with a brief note to complete the scaffold, save to `storage/dp/intake/DP.md`, and lint before dispatch; (b) update the `draft` Command Index entry description to name bundle as the standard operator path and describe `draft` as the local generator.
5. In `ops/src/surfaces/task.md.tpl` Section 2.2: remove the `llms parity: tools/lint/llms.sh` line entirely (dead script, no replacement enforcement gate applicable).
6. In `ops/src/surfaces/task.md.tpl` Section 3 authoring protocol: replace the sentence that presents `ops/bin/draft` as the generation tool with a sentence describing bundle-first as the standard operator path and `ops/bin/draft` as the local alternative.
7. In `ops/src/surfaces/task.md.tpl` `### 3.4.5 Receipt` command list: remove the `bash tools/lint/llms.sh` line.
8. In `docs/ops/specs/surfaces/task.md` Enforcement section: fix `tools/lint/dp.sh` description to remove the false claim it "validates RESULTS artifacts"; change to "Enforces DP transaction rules in Section 3."
9. In `docs/ops/specs/surfaces/task.md` DP Generation Contract section: replace the `Standard generation flow is ./ops/bin/open then ./ops/bin/draft` sentence with a description of the bundle-first standard operator path.
10. In `ops/lib/scripts/bundle.sh` `bundle_render_draft_authoring_scaffold`: replace the pre-filled literal assignment `suggested_work_branch="work/${packet_id_branch_fragment}-<replace-branch-slug>-${freshness_stamp}"` with the canonical PoT.md §6.2.1 pattern `suggested_work_branch="work/<DP-ID>-YYYY-MM-DD"`.
11. In `ops/src/stances/draft.md.tpl` CONTENT REQUIREMENTS: replace the Work Branch bullet so it instructs Architect to author the branch following PoT.md §6.2.1 form `work/<DP-ID>-YYYY-MM-DD`, filling in the current packet id and date, instead of "replace only the branch slug marker; preserve packet-id/date framing already present in the scaffold when present."
12. Read `docs/ops/specs/binaries/help.md` and confirm the abstract description remains accurate after the help changes; make no content edit unless a factual inaccuracy is found.
13. Update `storage/dp/active/allowlist.txt` to add all changed files before running receipt commands.

### 3.4.3 Changelog
- UPDATE docs/MANUAL.md
- UPDATE ops/bin/help
- UPDATE ops/src/surfaces/task.md.tpl
- UPDATE docs/ops/specs/surfaces/task.md
- UPDATE ops/lib/scripts/bundle.sh
- UPDATE ops/src/stances/draft.md.tpl
- UPDATE storage/dp/active/allowlist.txt

### 3.4.4 Patch / Diff

**1. Update `docs/MANUAL.md` — Execution Cycle Step 2.**

Read `docs/MANUAL.md`. Find the exact line:
```
2.  **Draft:** `./ops/bin/draft` (generates canonical DP block, refreshes `storage/dp/intake/DP.md`, and updates `TASK.md`).
```
Replace with:
```
2.  **Draft:** `./ops/bin/bundle --profile=draft --out=auto` (generates draft bundle with embedded DP AUTHORING SCAFFOLD; deliver `DRAFT-*.txt` to Architect; Architect returns one fenced worker-ready DP body; save full output to `storage/dp/intake/DP.md`; lint immediately: `bash tools/lint/dp.sh storage/dp/intake/DP.md`; confirm PASS before dispatch).
```

**2. Update `docs/MANUAL.md` — Add non-bundle repo-sharing path.**

In `docs/MANUAL.md`, find the exact block ending with:
```
- `ops/bin/bundle` consumes a real current OPEN artifact for the active branch and HEAD. If none exists, or the latest OPEN is stale for the current branch/HEAD, bundle refreshes one through `./ops/bin/open --out=auto` before continuing. Bundle metadata may mirror OPEN fields, but it is not a substitute for the artifact.
- `OPEN-PORCELAIN` (`storage/handoff/OPEN-PORCELAIN-*.txt`) is conditionally emitted: it is written only when the working tree is dirty. It is not a universal shipping requirement; clean sessions suppress it by design.
```
After that block (before the next `**Execution-Decision Handling` block), insert:
```

**Non-Bundle Repo-Sharing Path (plain session):**
For sessions where no bundle profile is in use, share repo state using:
1. `./ops/bin/open --out=auto` — freshness gate and trace anchor.
2. `./ops/bin/dump --scope=core --format=chatgpt --out=auto` — core context snapshot (excludes `projects/` and `opt/_factory/`).
- Use `--scope=platform` only when `opt/_factory/` content is intentionally in scope for the session.
This path is separate from bundle-mediated planning, draft, and audit transport; it does not replace bundle-based shipping spine stages.
```

**3. Update `ops/bin/help` — Fix `show_curriculum` closeout commands.**

Read `ops/bin/help`. In the `show_curriculum` function, find the exact lines:
```
   ${green}bash tools/lint/dp.sh storage/handoff/RESULTS.md${reset}
   ${green}bash tools/lint/llms.sh${reset}
```
Replace with:
```
   ${green}bash tools/lint/results.sh storage/handoff/RESULTS.md${reset}
```
(Two lines replaced by one: the wrong RESULTS lint is corrected; the dead llms.sh line is removed.)

**4. Update `ops/bin/help` — Update `show_menu` Quick Start and `draft` Command Index entry.**

In the `show_menu` function, find the exact block:
```
${bold}Command Index${reset}
  ${cyan}open${reset}       Session freshness and trace anchor (STELA_TRACE_ID required by certify).
  ${cyan}draft${reset}      Generate canonical DP from template; saves to storage/dp/intake/ and TASK.md.
```
Replace with:
```
${bold}Command Index${reset}
  ${cyan}open${reset}       Session freshness and trace anchor (STELA_TRACE_ID required by certify).
  ${cyan}draft${reset}      Local DP generator (template render); standard operator draft path is ${cyan}bundle --profile=draft${reset}.
```

Then find the exact Quick Start block:
```
${bold}Quick Start (Main Spine)${reset}
  1) Read constitution and active contract: ${green}PoT.md${reset}, ${green}TASK.md${reset}
  2) Open session: ${green}./ops/bin/open --out=auto${reset}
  3) Run preflight gates: ${green}bash tools/lint/dp.sh --test${reset}, ${green}bash tools/lint/dp.sh TASK.md${reset}, ${green}bash tools/lint/task.sh${reset}
  4) Execute DP. Certify: ${green}./ops/bin/certify --dp=DP-OPS-XXXX --out=auto${reset}
  5) Audit: ${green}./ops/bin/bundle --profile=audit --out=auto${reset}
  6) Operator: commit, PR, merge per CLOSING sidecar.
```
Replace with:
```
${bold}Quick Start (Main Spine)${reset}
  1) Read constitution and active contract: ${green}PoT.md${reset}, ${green}TASK.md${reset}
  2) Open session: ${green}./ops/bin/open --out=auto${reset}
  3) Draft: ${green}./ops/bin/bundle --profile=draft --out=auto${reset} (Architect completes scaffold; save DP to ${green}storage/dp/intake/DP.md${reset}; lint before dispatch)
  4) Run preflight gates: ${green}bash tools/lint/dp.sh --test${reset}, ${green}bash tools/lint/dp.sh TASK.md${reset}, ${green}bash tools/lint/task.sh${reset}
  5) Execute DP. Certify: ${green}./ops/bin/certify --dp=DP-OPS-XXXX --out=auto${reset}
  6) Audit: ${green}./ops/bin/bundle --profile=audit --out=auto${reset}
  7) Operator: commit, PR, merge per CLOSING sidecar.
```

**5. Update `ops/src/surfaces/task.md.tpl` — Remove dead llms.sh from Section 2.2.**

Read `ops/src/surfaces/task.md.tpl`. In the `### 2.2 Enforcement and Gates` section, find the exact line:
```
llms parity: tools/lint/llms.sh
```
Remove this line entirely (dead script; no live replacement enforcement gate applicable in this section).

**6. Update `ops/src/surfaces/task.md.tpl` — Update Section 3 DP generation description.**

In the `## 3. Current Dispatch Packet (DP)` section authoring protocol, find the exact line:
```
- Generate DP structure from `ops/src/surfaces/dp.md.tpl` using `ops/bin/draft`.
```
Replace with:
```
- Generate DP structure via `./ops/bin/bundle --profile=draft --out=auto` (standard operator path: Architect completes the embedded DP AUTHORING SCAFFOLD; save the returned DP body to `storage/dp/intake/DP.md`) or locally via `ops/bin/draft` (template render only).
```

**7. Update `ops/src/surfaces/task.md.tpl` — Remove dead llms.sh from Section 3.4.5 Receipt.**

In the `### 3.4.5 Receipt (Proofs to collect) - MUST RUN` section, find the exact line:
```
- bash tools/lint/llms.sh
```
Remove this line entirely.

**8. Update `docs/ops/specs/surfaces/task.md` — Fix dp.sh enforcement description.**

Read `docs/ops/specs/surfaces/task.md`. In the `## Enforcement` section, find the exact line:
```
- `tools/lint/dp.sh`: Enforces DP transaction rules in Section 3 and validates RESULTS artifacts.
```
Replace with:
```
- `tools/lint/dp.sh`: Enforces DP transaction rules in Section 3.
- `tools/lint/results.sh`: Validates RESULTS artifacts.
```

**9. Update `docs/ops/specs/surfaces/task.md` — Fix DP Generation Contract standard flow.**

In the `## DP Generation Contract` section, find the exact line:
```
- Standard generation flow is `./ops/bin/open` then `./ops/bin/draft`.
```
Replace with:
```
- Standard operator generation flow is bundle-first: `./ops/bin/bundle --profile=draft --out=auto`; Architect completes the embedded DP AUTHORING SCAFFOLD and returns a fenced worker-ready DP body; operator saves it to `storage/dp/intake/DP.md` and lints with `bash tools/lint/dp.sh storage/dp/intake/DP.md`. Local generation via `./ops/bin/draft` remains available as a template-render alternative.
```

**10. Update `ops/lib/scripts/bundle.sh` — Remove pre-filled branch-name literal.**

Read `ops/lib/scripts/bundle.sh`. In the `bundle_render_draft_authoring_scaffold` function, find the exact line:
```
  suggested_work_branch="work/${packet_id_branch_fragment}-<replace-branch-slug>-${freshness_stamp}"
```
Replace with:
```
  suggested_work_branch="work/<DP-ID>-YYYY-MM-DD"
```
All other logic in this function is no-touch.

**11. Update `ops/src/stances/draft.md.tpl` — Fix Work Branch instruction.**

Read `ops/src/stances/draft.md.tpl`. In the CONTENT REQUIREMENTS step (step 4), find the exact line:
```
   * `Work Branch`: replace only the branch slug marker; preserve packet-id/date framing already present in the scaffold when present.
```
Replace with:
```
   * `Work Branch`: author the work branch following PoT.md §6.2.1 form `work/<DP-ID>-YYYY-MM-DD`; fill in the current packet id and freshness stamp date.
```

**12. Confirm `docs/ops/specs/binaries/help.md` accuracy.**

Read `docs/ops/specs/binaries/help.md`. Confirm that the Mechanics and Sequencing section accurately describes the live binary behavior after the above help changes: "With no argument it prints the command index and quick-start menu. In `curriculum` mode it prints startup and closeout command sequences." This description is abstract and remains accurate; make no content edit unless a factual inaccuracy is found. Record result in execution narrative.

**13. Update `storage/dp/active/allowlist.txt`.**

Add the following entries to `storage/dp/active/allowlist.txt` before running receipt commands:
```
docs/MANUAL.md
ops/bin/help
ops/src/surfaces/task.md.tpl
docs/ops/specs/surfaces/task.md
ops/lib/scripts/bundle.sh
ops/src/stances/draft.md.tpl
```

**14. Verify.**

Run:
```
git diff --name-only
```
Confirm only the seven files in §3.4.3 Changelog were modified (plus `storage/dp/active/allowlist.txt`). STOP if any unintended change is found and revert before proceeding.

### 3.4.5 Receipt (Proofs to collect) - MUST RUN

**Mandatory receipt commands (always run; do not omit):**
- bash tools/lint/dp.sh TASK.md
- bash tools/lint/task.sh
- bash tools/lint/integrity.sh
- bash tools/lint/style.sh
- ./ops/bin/llms
- bash tools/lint/integrity.sh
- git diff --name-only
- git diff --stat
- comm -23 <(git diff --name-only | sort) <(sort storage/dp/active/allowlist.txt) || true
- comm -23 <(git ls-files --others --exclude-standard | sort) <(sort storage/dp/active/allowlist.txt) || true
- ./ops/bin/open

**DP-specific receipt commands (scope-specific; author below):**
Note: Command substitution forms (e.g., $(pwd), $(git ...)) are rejected by certify replay. Use literal values only. Repo-local binaries must be authored as literal `./ops/bin/...` or `ops/bin/...` commands. Unsupported receipt-command forms are rejected by dp.sh and certify.
- bash tools/lint/plan.sh storage/handoff/PLAN.md
- grep -n "bundle --profile=draft" docs/MANUAL.md
- grep -n "Non-Bundle Repo-Sharing Path" docs/MANUAL.md
- bash -lc '! grep -nF "generates canonical DP block, refreshes" docs/MANUAL.md'
- bash -lc '! grep -n "bash tools/lint/llms\.sh" ops/bin/help'
- grep -n "tools/lint/results.sh" ops/bin/help
- grep -n "bundle --profile=draft" ops/bin/help
- bash -lc '! grep -n "tools/lint/llms\.sh" ops/src/surfaces/task.md.tpl'
- grep -n "bundle --profile=draft" ops/src/surfaces/task.md.tpl
- bash -lc '! grep -n "validates RESULTS artifacts" docs/ops/specs/surfaces/task.md'
- grep -n "tools/lint/results.sh" docs/ops/specs/surfaces/task.md
- bash -lc '! grep -nF "Standard generation flow is " docs/ops/specs/surfaces/task.md'
- grep -n "bundle-first" docs/ops/specs/surfaces/task.md
- bash -lc '! grep -n "replace-branch-slug" ops/lib/scripts/bundle.sh'
- grep -n "suggested_work_branch" ops/lib/scripts/bundle.sh
- grep -n "Work Branch.*PoT\|DP-ID.*YYYY-MM-DD" ops/src/stances/draft.md.tpl

## 3.5 Closeout (Mandatory Routing)
- Execute docs/MANUAL.md Closeout Cycle in order (Verify, Harvest, Refresh, Log, Prune).
- Update SoP.md and PoW.md with DP entries, including objective summary and current proof / receipt summary.
- Protocol order for closeout: Verify -> Generate Results -> COMMIT (Operator Only) -> Prune.
- Run prune hygiene: ./ops/bin/prune --scrub.
- Refresh llms artifacts: `./ops/bin/llms`
- Regenerate session artifacts: `./ops/bin/open --out=auto`
- Ensure the next session begins with refreshed session artifacts and matching receipts.
- Note: audit dump generation is owned by `./ops/bin/bundle --profile=audit --out=auto` and is separate from operator session refresh. Do not stamp an additional `ops/bin/dump --scope=core` into closeout unless the DP explicitly authorizes a standalone state capture.
- Refresh side-effect: `ops/bin/llms` regenerates `ops/lib/manifests/OPS.md` as a compile event; if `OPS.md` is not in the allowlist, restore it before running `integrity.sh`. See `docs/MANUAL.md` Refresh side-effect notice for the full procedure.

### 3.5.1 Mandatory Closing Sidecar
Closing sidecar content is generated and maintained at `storage/handoff/CLOSING.md` and is validated by `ops/bin/certify` as a hard gate at certification time. Do not author, predict, populate, or approximate any sidecar-derived closeout output outside this sidecar.
Certify separately collects contractor-authored narrative for the RESULTS Contractor Execution Narrative section at certify time via interactive editor prompt; this narrative input is independent from closing sidecar content.

Before running certify, confirm `storage/handoff/CLOSING.md` has been maintained throughout execution and reflects observable reality only.

Closing sidecar fields required at certify time:
- Commit Message
- Create Pull Request (Title)
- Create Pull Request (Description)
- Confirm Merge (Commit Message)
- Confirm Merge (Extended Description)
- Confirm Merge (Add a Comment)
