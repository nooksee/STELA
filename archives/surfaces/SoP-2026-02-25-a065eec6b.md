---
trace_id: stela-20260225T125245Z-3e1b6966
packet_id: DP-OPS-0112
created_at: 2026-02-25T13:02:12Z
previous: archives/surfaces/SoP-2026-02-25-a065eec6b.md
---
## 2026-02-25 00:41:02 UTC — DP-OPS-0111 Sidecar Schema Alignment and RESULTS Lint Schema Recovery
Objective summary:
Updated `tools/lint/style.sh` closing-block checks to recognize both legacy and v2 closing sidecar label schemas accepted by `ops/bin/certify`, documented certify as the schema authority in the style lint spec and MANUAL closeout guidance, and then completed an operator-authorized in-flight scope expansion to align `tools/lint/results.sh` with certify's legacy and v2 Mandatory Closing Block schemas after certify failed on a generated v2 RESULTS receipt. The recovery also re-ran closeout sequencing after resolving intake fallback residue and a sidecar manifest-prefix rejection.

Verification commands:
- git rev-parse --abbrev-ref HEAD
- git rev-parse --short HEAD
- git status --porcelain
- bash tools/lint/dp.sh --test
- bash tools/lint/dp.sh TASK.md
- bash tools/lint/task.sh
- ./tools/verify.sh
- ./tools/lint/truth.sh
- ./tools/lint/style.sh
- ./tools/lint/ff.sh
- ./tools/lint/context.sh
- ./tools/lint/agent.sh
- bash tools/lint/factory.sh
- bash tools/lint/integrity.sh
- git diff --name-only
- git diff --stat
- comm -23 <(git diff --name-only | sort) <(sort storage/dp/active/allowlist.txt) || true
- comm -23 <(git ls-files --others --exclude-standard | sort) <(sort storage/dp/active/allowlist.txt) || true
- grep -c "Key Files Touched" tools/lint/style.sh
- grep -c "Scope Summary" tools/lint/style.sh
- grep -c "is_v2_schema" tools/lint/style.sh
- ./ops/bin/open
- ./ops/bin/map
- ./ops/bin/llms --out-dir=/home/nos4r2/dev/nukece
- git restore --source=HEAD --staged --worktree -- ops/lib/manifests/OPS.md
- ./ops/lib/scripts/agent.sh harvest-check
- ops/bin/certify --dp="DP-OPS-0111" --out=auto
- bash tools/lint/results.sh storage/handoff/DP-OPS-0111-RESULTS.md
- grep -c "required_closing_labels_v2" tools/lint/results.sh
- grep -c "Key Files Touched" tools/lint/results.sh
- grep -c "closing_block_is_v2" tools/lint/results.sh
- ./ops/bin/prune --scrub
- ./ops/bin/open --out=auto
- ./ops/bin/dump --scope=platform --format=chatgpt --out=auto

Functional receipt summary:
Freshness gate and DP preflight passed on `work/DP-OPS-0111-sidecar-schema-2026-02-24` at `a4b52a3bd` with a clean starting tree. The scoped `tools/lint/style.sh` edits shipped as requested and `bash tools/lint/style.sh` passed with a v2 sidecar. Closeout then exposed three sequential seams: first certify intake fallback was blocked by leftover `storage/dp/intake/DP-OPS-0110.md`; after operator removal, certify rejected the v2 sidecar `Key Files Touched` field because root-level `llms*.txt` entries violate the current prefix-constrained manifest validator; after sidecar correction, certify generated a v2 RESULTS closing block and `tools/lint/results.sh` failed because it still enforced legacy-only closing labels. Operator authorized expansion of DP-OPS-0111 to align `tools/lint/results.sh` and document the RESULTS schema authority contract. The patched results linter passed directly on the generated `storage/handoff/DP-OPS-0111-RESULTS.md` before certify rerun. Refresh again regenerated out-of-scope `ops/lib/manifests/OPS.md`; it was restored and integrity reran clean. A recovery attempt to move the live DP-OPS-0111 packet into the TASK head leaf caused `tools/lint/task.sh` failures because the TASK surface linter still enforces an older TASK closeout schema for new packet IDs; the TASK pointer head was therefore returned to the grandfathered leaf body, TASK leaf metadata was normalized to packet `DP-OPS-0111`, and the operator-authorized scope expansion was recorded in this SoP entry, the PoW notes, and the v2 closing sidecar for auditor continuity.

Receipt pointers:
- RESULTS: storage/handoff/DP-OPS-0111-RESULTS.md
- OPEN: storage/handoff/OPEN-*.txt
- DUMP: storage/dumps/dump-platform-*.txt
