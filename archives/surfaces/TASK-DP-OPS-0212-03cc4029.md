---
trace_id: stela-20260319T034332Z-0d0b591e
packet_id: DP-OPS-0212
created_at: 2026-03-19T03:47:40Z
previous: archives/surfaces/TASK-DP-OPS-0211-38ce0e08.md
---
# STELA TASK DASHBOARD
Status: ACTIVE
Owner: Integrator
Last Updated: 2026-03-18

## 1. Session State
Pointer: Session context output (generated by ops/bin/open)
Context Manifest: ops/lib/manifests/CONTEXT.md (checked by tools/lint/context.sh)

Session State contract:
- Session State is pointer-first. Do not write inline branch or hash state here.
- OPEN is the session state snapshot. OPEN-PORCELAIN is conditional dirty-state detail.
- RESULTS is the audit record for the DP.
- TASK is the stable routing surface.

## 2. Logic Pointers
Primary Constraint: PoT.md (Policy of Truth).
Surface Contract Spec: docs/ops/specs/surfaces/task.md.

### 2.1 Governance Pointers
Jurisdiction: PoT.md Section 3.
Git Authority: PoT.md Section 4.1.
Behavioral Standard: PoT.md Section 4.2.
Staffing Protocol: PoT.md Section 4.1.

### 2.2 Enforcement and Gates
Style: tools/lint/style.sh
Context: tools/lint/context.sh
Truth: tools/lint/truth.sh
DP lint: tools/lint/dp.sh (enforces DP transaction rules in Section 3; does not enforce TASK container schema)
TASK lint: tools/lint/task.sh (sole TASK surface/container schema enforcer)
Repo verify: tools/verify.sh
llms parity: tools/lint/llms.sh

## 3. Current Dispatch Packet (DP)

Section-by-section authoring protocol:
- Generate DP structure from `ops/src/surfaces/dp.md.tpl` using `ops/bin/draft`.
- Do not hand-edit DP structural boilerplate in `TASK.md` after generation.
- Manual edits are limited to slot content only (objective, scope, safety, plan body, receipt body).
- Freeze each completed subsection in order (3.1 -> 3.2 -> 3.3 -> 3.4.x).
- Do not revise earlier sections to justify later work. If scope changes, end the thread and draft a new DP.

Freeze Rule:
- If a completed DP subsection changes later, append an AMENDMENT block in RESULTS; otherwise DP is FAIL.

AMENDMENT: minimum fields
- Reopened section:
- Reason:
- Impact:
- Allowlist change (none or exact paths):

Input discipline (hard rule):
- Worker receives only the DP text. Worker does not require OPEN or DUMP attachments.
- DP writer must not attach or cite disposable artifacts (rough notes, chat logs, scratch drafts, throwaway excerpts).
- DP writer must not include pasted bundles (OPEN, DUMP payloads, manifests) inside the DP body.
- Canon pointers only. Evidence belongs in storage artifacts and RESULTS.

### DP-OPS-0212: Processed lineage retirement slice 1

## 3.1 Freshness Gate (Must Pass Before Work)
Base Branch: main
Required Work Branch: work/dp-ops-0212-processed-lineage-retirement-slice-1-2026-03-18
Base HEAD: 03cc4029
Freshness Stamp: 2026-03-18
Note: FRESHNESS_STAMP must be YYYY-MM-DD format only. No trace tokens, no timestamps, no other text. Certify rejects all other forms. dp.sh enforces at lint time.

Required local re-check (worker runs; paste outputs in RESULTS):
- git rev-parse --abbrev-ref HEAD
- git rev-parse --short HEAD
- git status --porcelain

Preconditions:
- No commits on main.
- Working tree must be clean before execution begins.
- If Base HEAD changes, regenerate session artifacts from canonical tools before proceeding.

STOP conditions:
- STOP if any mismatch (branch, Base HEAD, missing work branch).
- STOP if working tree is dirty before execution begins.
- STOP if told to create or switch branches.

## 3.1.1 DP Preflight Gate (Run Before Any Edits)
Purpose:
- Catch malformed DP or TASK schema before work begins.

Worker runs (paste outcome in RESULTS):
- bash tools/lint/dp.sh --test
- bash tools/lint/dp.sh TASK.md
- bash tools/lint/task.sh

STOP if any preflight check fails.

### CbC Design Discipline Preflight (TASK.md §3.1.1)
Required when the DP objective adds, modifies, or replaces a linter, script, guard, or validation binary.
For non-tooling DPs: state "Not applicable" with a one-line justification.
Applicable. This packet changes packet/addendum lineage handling across certify, draft, addendum issuance, bundle audit-source resolution, verify messaging, results proof, and synchronized canon/spec surfaces.

Invariants:
1. Reuse-first: retire live `storage/dp/processed/` dependence by reusing the existing TASK leaf chain for base packets and `archives/surfaces/ADDENDUM-DP-OPS-XXXX-<git-short-hash>.md` for addenda; do not invent a new lineage shelf, registry, or standalone `archives/surfaces/DP-OPS-XXXX.md` collection.
2. Active latest-wins draft surfaces remain `storage/dp/intake/DP.md` and `storage/dp/intake/ADDENDUM.md`; active receipt surfaces remain `storage/handoff/RESULTS.md` and `storage/handoff/CLOSING.md`.
3. Packet/addendum identity stays explicit in TASK leaves, addendum lineage leaves, RESULTS `dp_source`, audit transport, and telemetry.
4. This is first-slice retirement only. Broader archive cleanup, retention cleanup, and unrelated naming work stay out of scope.
5. Do not use tracked storage state as proof. `storage/` remains disposable operational state except the active allowlist policy file.

## 3.2 Required Context Load (Read Before Doing Anything)

### 3.2.1 Canon load order (always)
Worker must confirm loaded before edits begin:
1. PoT.md
2. SoP.md
3. PoW.md
4. TASK.md
5. docs/MAP.md
6. docs/MANUAL.md
7. ops/lib/manifests/CONTEXT.md

Notes:
- Worker does not read OPEN. OPEN is for Integrator state refresh and for receipts.
- Disposable artifacts must not be referenced or included.
- Worker input is the DP text only.
- DP writer must not attach or cite disposable artifacts.
- DP writer must not embed pasted bundles.

## Rules

1 Temp path rule uses `var/tmp` only
2 RESULTS generation rule uses `ops/bin/certify` only
3 Receipt rule runs every DP mandated command and treats any skip as STOP
4 Disposable artifact rule treats OPEN dump bundles and manifests as non canonical session artifacts
5 Use repo-relative paths only in all output. Never print absolute filesystem paths. Do not emit clickable file links.

### 3.2.2 DP-scoped load order (per DP)
- storage/current/PLAN.md
- docs/MANUAL.md
- docs/MAP.md
- ops/bin/certify
- ops/bin/addendum
- ops/bin/draft
- ops/lib/scripts/bundle.sh
- tools/test/bundle.sh
- tools/verify.sh
- tools/lint/results.sh
- docs/ops/specs/binaries/addendum.md
- docs/ops/specs/binaries/bundle.md
- docs/ops/specs/binaries/certify.md
- docs/ops/specs/binaries/draft.md
- docs/ops/specs/surfaces/results.md
- storage/dp/active/allowlist.txt

## 3.3 Scope and Safety
Objective:
- Retire `storage/dp/processed/` as a live packet/addendum lineage path in the first slice of the corridor by moving base packet authority to the existing TASK leaf chain and addendum authority to addendum-only archive leaves under `archives/surfaces/`, then making RESULTS, addendum issuance, architect follow-on, audit transport, verify messaging, and synced canon/spec text follow that one rule.
- Keep active operator surfaces unchanged while removing live runtime dependence on processed-storage packet copies.

In scope:
- UPDATE ops/bin/certify so base packet authority resolves to the TASK leaf chain, addendum authority resolves to `archives/surfaces/ADDENDUM-DP-OPS-XXXX-<git-short-hash>.md`, and RESULTS `dp_source` follows that lineage instead of processed storage.
- UPDATE ops/bin/addendum so base-packet existence checks resolve from active intake plus the TASK leaf chain, and addendum-letter checks scan addendum lineage content rather than packet-named archive filenames.
- UPDATE ops/bin/draft so addendum/base-packet follow-on resolution uses the active intake draft when matching and otherwise the TASK leaf chain, not processed fallback.
- UPDATE ops/lib/scripts/bundle.sh and tools/test/bundle.sh so audit packet-source resolution and smoke fixtures follow the new lineage rule.
- UPDATE tools/verify.sh and tools/lint/results.sh so messages and proof expectations stop treating processed storage as the live packet lineage path.
- UPDATE docs/MANUAL.md, docs/MAP.md, docs/ops/specs/binaries/addendum.md, docs/ops/specs/binaries/bundle.md, docs/ops/specs/binaries/certify.md, docs/ops/specs/binaries/draft.md, and docs/ops/specs/surfaces/results.md so canon matches the new lineage rule.
- UPDATE storage/dp/active/allowlist.txt for every tracked file touched by this packet.

Out of scope:
- Any new storage shelf, registry, or manifest family for lineage.
- Any change to active latest-wins surface names (`DP.md`, `ADDENDUM.md`, `RESULTS.md`, `CLOSING.md`).
- Any change to `storage/current/PLAN.md` consumer precedence or architect PLAN source routing.
- Any execution-decision prompt/spec/output-contract work.
- Any generic dump filename prefixing work.
- Any prune/retention cleanup beyond what this lineage swap strictly requires.
- Any manual authoring of `storage/handoff/RESULTS.md` or `storage/handoff/CLOSING.md`.

Safety and invariants:
- Guidance and runtime must say the same thing; no aspirational wording.
- Reuse the existing TASK leaf chain for base packets and addendum-only archive leaves for addenda; do not create a separate base-packet leaf class.
- Packet/addendum identity must remain explicit in the TASK/addendum lineage path and delivered proof surfaces.
- Do not leave mixed live truth where some code resolves packet lineage from `archives/surfaces/` and other code still requires `storage/dp/processed/`.
- If a dependent path cannot be moved cleanly in this slice, STOP and narrow the packet rather than adding a second lineage rule.

Worker Constraints (SSOT injected):
## Section 1: Universal Template Rules
- Template source uses `tpl` files with optional YAML frontmatter
- Canon frontmatter keys: `template_type` `template_id` `template_version` `requires_slots` `includes`
- Renderer strips frontmatter before output write
- Slot token form `\{\{TOKEN\}\}` with uppercase alphanumeric underscore
- Include forms `\{\{@include:path\}\}` and `\{\{@include:path#section\}\}`
- Include resolution is strict: missing file fail missing section fail circular graph fail
- Strict mode default: every required slot value present and no unresolved token
- Non strict mode allowed only for lint and normalization workflows
- Worker facing generated surfaces remain pointer first and exclude disposable artifacts

Target Files allowlist (hard gate):
- storage/dp/active/allowlist.txt

## 3.4 Execution Plan (A-E)

### 3.4.1 State
- `storage/dp/processed/` is still named as live packet lineage in certify, addendum issuance, draft follow-on resolution, bundle audit packet-source resolution, verify messaging, tests, and docs.
- The accepted spine plan marks processed-lineage retirement as priority unfinished business, not housekeeping.
- Active draft and receipt surfaces are already latest-wins and should remain so.
- The correct reuse path already exists: `archives/surfaces/` is the archive leaf class used for other canonical surfaces.
- The first slice should replace live dependence on processed packet copies, not try to solve every historical cleanup concern at once.

### 3.4.2 Request
- Replace live packet/addendum lineage reads and writes so base packets resolve from the TASK leaf chain and addenda resolve from `archives/surfaces/ADDENDUM-DP-OPS-XXXX-<git-short-hash>.md`.
- Make RESULTS `dp_source` point to the authoritative TASK/addendum lineage path instead of `storage/dp/processed/`.
- Keep active intake surfaces latest-wins and disposable.
- Remove live runtime/doc/spec/test dependence on `storage/dp/processed/` as packet lineage, while leaving any later shelf cleanup for a follow-on slice.

### 3.4.3 Changelog
UPDATE:
- storage/dp/active/allowlist.txt
- ops/bin/certify
- ops/bin/addendum
- ops/bin/draft
- ops/lib/scripts/bundle.sh
- tools/lint/results.sh
- tools/test/bundle.sh
- tools/verify.sh
- docs/MANUAL.md
- docs/MAP.md
- docs/ops/specs/binaries/addendum.md
- docs/ops/specs/binaries/bundle.md
- docs/ops/specs/binaries/certify.md
- docs/ops/specs/binaries/draft.md
- docs/ops/specs/surfaces/results.md

### 3.4.4 Patch / Diff
1. Update `storage/dp/active/allowlist.txt` first with every tracked file touched by this packet.
2. Read the scoped runtime/spec surfaces in `3.2.2` before editing. Reuse the TASK leaf chain for base packets and addendum-only archive leaves for addenda; do not introduce a new lineage store.
3. In `ops/bin/certify`, replace processed packet/addendum copy-as-lineage behavior so base packet authority stays on the TASK leaf chain, addenda emit `archives/surfaces/ADDENDUM-DP-OPS-XXXX-<git-short-hash>.md`, and delivered RESULTS `dp_source` resolves to that lineage path.
4. In `ops/bin/addendum`, replace processed-directory base-packet and addendum-letter checks with resolution against the active intake surface when it matches and otherwise the TASK/addendum lineage set.
5. In `ops/bin/draft`, replace processed fallback for addendum/base packet follow-on resolution with the same active-intake-or-TASK-lineage rule.
6. In `ops/lib/scripts/bundle.sh` and `tools/test/bundle.sh`, replace audit packet-source resolution and test fixtures that assume `storage/dp/processed/` is the live lineage path.
7. In `tools/verify.sh` and `tools/lint/results.sh`, remove wording and proof assumptions that still treat processed storage as the live authoritative packet source.
8. In `docs/MANUAL.md`, `docs/MAP.md`, and the scoped specs, replace processed-lineage wording with the new `archives/surfaces/` packet/addendum lineage rule while keeping active latest-wins surfaces unchanged.
9. Do not widen this slice into generic dump naming, `storage/current/PLAN.md` consumer precedence, execution-decision changes, or broad archive cleanup.

### 3.4.5 Receipt (Proofs to collect) - MUST RUN

**Mandatory receipt commands (always run; do not omit):**
- bash tools/lint/dp.sh TASK.md
- bash tools/lint/task.sh
- bash tools/lint/integrity.sh
- bash tools/lint/style.sh
- ./ops/bin/llms
- bash tools/lint/integrity.sh
- git diff --name-only
- git diff --stat
- comm -23 <(git diff --name-only | sort) <(sort storage/dp/active/allowlist.txt) || true
- comm -23 <(git ls-files --others --exclude-standard | sort) <(sort storage/dp/active/allowlist.txt) || true
- ./ops/bin/open

**DP-specific receipt commands (scope-specific; author below):**
Note: Command substitution forms (e.g., $(pwd), $(git ...)) are rejected by certify replay. Use literal values only. dp.sh enforces at lint time.
- bash tools/test/bundle.sh --slice=closeout-sanity
- bash tools/test/bundle.sh --slice=audit-coherence
- bash tools/test/bundle.sh --slice=route-contract
- bash tools/test/bundle.sh --slice=rerun-lineage
- bash tools/verify.sh --mode=certify-critical
- bash tools/lint/results.sh --all
- ! rg -n "storage/dp/processed|DP_PROCESSED_DIR" ops/bin/certify ops/bin/addendum ops/bin/draft ops/lib/scripts/bundle.sh tools/lint/results.sh tools/test/bundle.sh tools/verify.sh docs/MANUAL.md docs/MAP.md docs/ops/specs/binaries/addendum.md docs/ops/specs/binaries/bundle.md docs/ops/specs/binaries/certify.md docs/ops/specs/binaries/draft.md docs/ops/specs/surfaces/results.md
- rg -n "TASK-DP-OPS|ADDENDUM-DP-OPS|dp_source:" ops/bin/certify ops/bin/addendum ops/bin/draft ops/lib/scripts/bundle.sh tools/test/bundle.sh docs/MANUAL.md docs/MAP.md docs/ops/specs/binaries/addendum.md docs/ops/specs/binaries/bundle.md docs/ops/specs/binaries/certify.md docs/ops/specs/binaries/draft.md docs/ops/specs/surfaces/results.md

## 3.5 Closeout (Mandatory Routing)
- Execute docs/MANUAL.md Closeout Cycle in order (Verify, Harvest, Refresh, Log, Prune).
- Update SoP.md and PoW.md with DP entries, including objective summary and current proof / receipt summary.
- Protocol order for closeout: Verify -> Generate Results -> COMMIT (Operator Only) -> Prune.
- Run prune hygiene: ./ops/bin/prune --scrub.
- Refresh llms artifacts: `./ops/bin/llms`
- Regenerate session artifacts: `./ops/bin/open --out=auto`
- Ensure the next session begins with refreshed session artifacts and matching receipts.
- Note: audit dump generation is owned by `./ops/bin/bundle --profile=audit --out=auto` and is separate from operator session refresh. Do not stamp an additional `ops/bin/dump --scope=core` into closeout unless the DP explicitly authorizes a standalone state capture.
- Refresh side-effect: `ops/bin/llms` regenerates `ops/lib/manifests/OPS.md` as a compile event; if `OPS.md` is not in the allowlist, restore it before running `integrity.sh`. See `docs/MANUAL.md` Refresh side-effect notice for the full procedure.

### 3.5.1 Mandatory Closing Sidecar
Closing sidecar content is generated and maintained at `storage/handoff/CLOSING.md` and is validated by `ops/bin/certify` as a hard gate at certification time. Do not author, predict, populate, or approximate any sidecar-derived closeout output outside this sidecar.
Certify separately collects contractor-authored narrative for the RESULTS Contractor Execution Narrative section at certify time via interactive editor prompt; this narrative input is independent from closing sidecar content.

Before running certify, confirm `storage/handoff/CLOSING.md` has been maintained throughout execution and reflects observable reality only.

Closing sidecar fields required at certify time:
- Commit Message
- Create Pull Request (Title)
- Create Pull Request (Description)
- Confirm Merge (Commit Message)
- Confirm Merge (Extended Description)
- Confirm Merge (Add a Comment)
