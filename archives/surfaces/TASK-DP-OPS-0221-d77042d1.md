---
trace_id: stela-20260322T201236Z-1c731d4f
packet_id: DP-OPS-0221
created_at: 2026-03-22T20:26:41Z
previous: archives/surfaces/TASK-DP-OPS-0220-d7f51e6a.md
---
# STELA TASK DASHBOARD
Status: ACTIVE
Owner: Integrator
Last Updated: 2026-03-19

## 1. Session State
Pointer: Session context output (generated by ops/bin/open)
Context Manifest: ops/lib/manifests/CONTEXT.md (checked by tools/lint/context.sh)

Session State contract:
- Session State is pointer-first. Do not write inline branch or hash state here.
- OPEN is the session state snapshot. OPEN-PORCELAIN is conditional dirty-state detail.
- RESULTS is the audit record for the DP.
- TASK is the stable routing surface.

## 2. Logic Pointers
Primary Constraint: PoT.md (Policy of Truth).
Surface Contract Spec: docs/ops/specs/surfaces/task.md.

### 2.1 Governance Pointers
Jurisdiction: PoT.md Section 3.
Git Authority: PoT.md Section 4.1.
Behavioral Standard: PoT.md Section 4.2.
Staffing Protocol: PoT.md Section 4.1.

### 2.2 Enforcement and Gates
Style: tools/lint/style.sh
Context: tools/lint/context.sh
Truth: tools/lint/truth.sh
DP lint: tools/lint/dp.sh (enforces DP transaction rules in Section 3; does not enforce TASK container schema)
TASK lint: tools/lint/task.sh (sole TASK surface/container schema enforcer)
Repo verify: tools/verify.sh
llms parity: tools/lint/llms.sh

## 3. Current Dispatch Packet (DP)

Section-by-section authoring protocol:
- Generate DP structure from `ops/src/surfaces/dp.md.tpl` using `ops/bin/draft`.
- Do not hand-edit DP structural boilerplate in `TASK.md` after generation.
- Manual edits are limited to slot content only (objective, scope, safety, plan body, receipt body).
- Freeze each completed subsection in order (3.1 -> 3.2 -> 3.3 -> 3.4.x).
- Do not revise earlier sections to justify later work. If scope changes, end the thread and draft a new DP.

Freeze Rule:
- If a completed DP subsection changes later, append an AMENDMENT block in RESULTS; otherwise DP is FAIL.

AMENDMENT: minimum fields
- Reopened section:
- Reason:
- Impact:
- Allowlist change (none or exact paths):

Input discipline (hard rule):
- Worker receives only the DP text. Worker does not require OPEN or DUMP attachments.
- DP writer must not attach or cite disposable artifacts (rough notes, chat logs, scratch drafts, throwaway excerpts).
- DP writer must not include pasted bundles (OPEN, DUMP payloads, manifests) inside the DP body.
- Canon pointers only. Evidence belongs in storage artifacts and RESULTS.



### DP-OPS-0221: Execution-Decision Lane Reset

## 3.1 Freshness Gate (Must Pass Before Work)
Base Branch: main
Required Work Branch: work/dp-ops-0221-execution-decision-lane-reset-2026-03-22
Base HEAD: d7f51e6a
Freshness Stamp: 2026-03-22
Note: FRESHNESS_STAMP must be YYYY-MM-DD format only. No trace tokens, no timestamps, no other text. Certify rejects all other forms. dp.sh enforces at lint time.

Required local re-check (worker runs; paste outputs in RESULTS):
- git rev-parse --abbrev-ref HEAD
- git rev-parse --short HEAD
- git status --porcelain

Preconditions:
- No commits on main.
- Working tree must be clean before execution begins.
- If Base HEAD changes, regenerate session artifacts from canonical tools before proceeding.

STOP conditions:
- STOP if any mismatch (branch, Base HEAD, missing work branch).
- STOP if working tree is dirty before execution begins.
- STOP if told to create or switch branches.

## 3.1.1 DP Preflight Gate (Run Before Any Edits)
Purpose:
- Catch malformed DP or TASK schema before work begins.

Worker runs (paste outcome in RESULTS):
- bash tools/lint/dp.sh --test
- bash tools/lint/dp.sh TASK.md
- bash tools/lint/task.sh

STOP if any preflight check fails.

### CbC Design Discipline Preflight (TASK.md §3.1.1)
Required when the DP objective adds, modifies, or replaces a linter, script, guard, or validation binary.
For non-tooling DPs: state "Not applicable" with a one-line justification.
Applicable. This packet modifies tools/lint/response.sh (a validation binary).

Invariants:
1. check_execution_decision_schema must accept a body that contains all required constraint labels and at least one complete step block.
2. check_execution_decision_schema must reject a body missing required constraint labels.
3. check_execution_decision_schema must reject a body missing step block labels.
4. All existing response.sh mode checks must remain unaffected.

## 3.2 Required Context Load (Read Before Doing Anything)

### 3.2.1 Canon load order (always)
Worker must confirm loaded before edits begin:
1. PoT.md
2. SoP.md
3. PoW.md
4. TASK.md
5. docs/MAP.md
6. docs/MANUAL.md
7. ops/lib/manifests/CONTEXT.md

Notes:
- Worker does not read OPEN. OPEN is for Integrator state refresh and for receipts.
- Disposable artifacts must not be referenced or included.
- Worker input is the DP text only.
- DP writer must not attach or cite disposable artifacts.
- DP writer must not embed pasted bundles.

## Rules

1 Temp path rule uses `var/tmp` only
2 RESULTS generation rule uses `ops/bin/certify` only
3 Receipt rule runs every DP mandated command and treats any skip as STOP
4 Disposable artifact rule treats OPEN dump bundles and manifests as non canonical session artifacts
5 Use repo-relative paths only in all output. Never print absolute filesystem paths. Do not emit clickable file links.

### 3.2.2 DP-scoped load order (per DP)
8. tools/lint/response.sh (lines 570-621: lint_response_file mode dispatch; end of file: smoke block)
9. ops/src/runbook/execution-decision.yaml (full file — validation block lines 49-55)
10. docs/ops/specs/runbook/execution-decision.md (full file — label list and validation claim)
11. docs/MANUAL.md (lines 44-49: execution-decision handling section)

## 3.3 Scope and Safety
Objective:
Reset the execution-decision lane to a coherent, non-contradictory state: remove the fake validation claim that references a response.sh mode that hard-fails at runtime; implement a real structural schema check for the execution-decision format; align the YAML validation block, spec, and MANUAL to describe the same working model.

In scope:
- tools/lint/response.sh: add execution-decision as a valid mode with a structural schema check function (check_execution_decision_schema); check for required constraint-section labels and at least one step block with required step labels; reject existing out-of-scope markers; add smoke fixtures (pass and fail)
- ops/src/runbook/execution-decision.yaml: replace the validation block with a reference to the real lint command and pass condition
- docs/ops/specs/runbook/execution-decision.md: correct the constraint-section label list (spec lists four labels that differ from the YAML prompt template — align to YAML); update validation description (step 4) to reference the real command; remove the "active testing policy" scoring claim (step 5) or replace with accurate description
- docs/MANUAL.md: update lines 44-49 execution-decision section to reference the real lint command
- storage/dp/active/allowlist.txt: add TASK archive entry for this DP

Out of scope:
- ops/src/runbook/execution-decision.yaml prompt_template: no change to the prompt body itself
- Any response.sh validation mode other than execution-decision
- Bundle profile for execution-decision: not in scope; placement remains manual/disposable
- TASK.md, SoP.md, PoW.md: no structural edits

Safety and invariants:
- All existing response.sh modes (dp, audit, architect, analyst, foreman, conformist) must continue to pass their existing checks unchanged
- The execution-decision schema check must not cause valid final-plan or conversational analyst output to fail
- response.sh --test must pass after adding the new mode and fixtures
- The new schema check must reject missing required labels and accept a well-formed execution-decision body

Worker Constraints (SSOT injected):
## Section 1: Universal Template Rules
- Template source uses `tpl` files with optional YAML frontmatter
- Canon frontmatter keys: `template_type` `template_id` `template_version` `requires_slots` `includes`
- Renderer strips frontmatter before output write
- Slot token form `\{\{TOKEN\}\}` with uppercase alphanumeric underscore
- Include forms `\{\{@include:path\}\}` and `\{\{@include:path#section\}\}`
- Include resolution is strict: missing file fail missing section fail circular graph fail
- Strict mode default: every required slot value present and no unresolved token
- Non strict mode allowed only for lint and normalization workflows
- Worker facing generated surfaces remain pointer first and exclude disposable artifacts

Target Files allowlist (hard gate):
- storage/dp/active/allowlist.txt

## 3.4 Execution Plan (A-E)

### 3.4.1 State
tools/lint/response.sh: execution-decision is not in the valid-mode switch (lines 1256-1263); attempting --mode=execution-decision produces immediate hard failure with "invalid mode". No check_execution_decision_schema function exists. No smoke fixtures exist.

ops/src/runbook/execution-decision.yaml lines 49-55: validation block claims lint_command "bash tools/lint/response.sh --mode=execution-decision" works with a defined pass_condition and three failure_modes. This is false; the command hard-fails at runtime.

docs/ops/specs/runbook/execution-decision.md: step 4 repeats the false validation claim. Step 5 describes "log-claim-drift" and "log-schema-drift" scoring under "active testing policy" — no such scoring infrastructure exists. Constraint-section labels in the spec (Active operational constraints understood / Constraint source visibility / Constraint confidence / Constraint gaps or unknowns) do not match the YAML prompt template labels (Active task constraints used / Observable evidence used / Evidence gaps or unknowns / Confidence level).

docs/MANUAL.md lines 44-49: repeats the false validation claim at line 49.

### 3.4.2 Request
1. In tools/lint/response.sh, add execution-decision to the valid-mode switch (lines 1256-1263). Add a check_execution_decision_schema function that:
   - Verifies the required constraint-section labels are present (matching YAML prompt template): "Active task constraints used:", "Observable evidence used:", "Evidence gaps or unknowns:", "Confidence level:"
   - Verifies at least one complete step block is present with all required step labels: "Trigger:", "Decision:", "Rationale:", "Tools considered or used:", "Actions taken:", "Actions not taken:", "Key evidence:"
   - Fails with a specific message for each missing label class
   Add the check_execution_decision_schema call to the lint_response_file dispatch for execution-decision mode. Add smoke fixtures: one valid pass fixture and at least two fail fixtures (missing constraint label, missing step label).

2. In ops/src/runbook/execution-decision.yaml, replace the validation block:
   Old block (lines 49-55):
     validation:
       mode: "execution-decision"
       lint_command: "bash tools/lint/response.sh --mode=execution-decision"
       pass_condition: "OK: response lint passed (mode=execution-decision)"
       failure_modes: ...
   New block: keep mode and lint_command pointing to the real working command; keep pass_condition matching the real response.sh output; remove the three failure_modes entries (they described behavior that was never implemented).

3. In docs/ops/specs/runbook/execution-decision.md:
   - Replace the four constraint-section label list items with the four labels from the YAML prompt template.
   - Replace step 4 text to reference the real working lint command.
   - Remove or replace step 5 "active testing policy" scoring claim with an accurate statement.

4. In docs/MANUAL.md lines 44-49, update the validation claim to reference the real working lint command.

5. In storage/dp/active/allowlist.txt, append:
   archives/surfaces/TASK-DP-OPS-0221-*.md

6. Run receipt commands and paste outputs in RESULTS.

### 3.4.3 Changelog
UPDATE tools/lint/response.sh — add execution-decision mode to valid-mode switch; add check_execution_decision_schema; add smoke fixtures
UPDATE ops/src/runbook/execution-decision.yaml — replace validation block with accurate working values; remove false failure_modes
UPDATE docs/ops/specs/runbook/execution-decision.md — align constraint labels to YAML; fix validation claim; remove false scoring claim
UPDATE docs/MANUAL.md — fix execution-decision validation reference
UPDATE storage/dp/active/allowlist.txt — add TASK archive entry for DP-OPS-0221

### 3.4.4 Patch / Diff
A. Read tools/lint/response.sh lines 570-621 (mode dispatch) and end of file (smoke block) in full.

B. Add execution-decision to the valid-mode switch and add check_execution_decision_schema as described in Request item 1.

C. Add smoke fixtures to the analyst smoke block or a dedicated execution-decision block. Run bash tools/lint/response.sh --test. STOP if test fails.

D. Read ops/src/runbook/execution-decision.yaml in full.

E. Apply validation block replacement as described in Request item 2.

F. Read docs/ops/specs/runbook/execution-decision.md in full.

G. Apply label alignment and validation claim fixes as described in Request item 3.

H. Read docs/MANUAL.md lines 40-55 in context.

I. Apply MANUAL.md fix as described in Request item 4.

J. In storage/dp/active/allowlist.txt, append archives/surfaces/TASK-DP-OPS-0221-*.md.

K. Run receipt commands and paste outputs in RESULTS.

### 3.4.5 Receipt (Proofs to collect) - MUST RUN

**Mandatory receipt commands (always run; do not omit):**
- bash tools/lint/dp.sh TASK.md
- bash tools/lint/task.sh
- bash tools/lint/integrity.sh
- bash tools/lint/style.sh
- ./ops/bin/llms
- bash tools/lint/integrity.sh
- git diff --name-only
- git diff --stat
- comm -23 <(git diff --name-only | sort) <(sort storage/dp/active/allowlist.txt) || true
- comm -23 <(git ls-files --others --exclude-standard | sort) <(sort storage/dp/active/allowlist.txt) || true
- ./ops/bin/open

**DP-specific receipt commands (scope-specific; author below):**
Note: Command substitution forms (e.g., $(pwd), $(git ...)) are rejected by certify replay. Use literal values only. dp.sh enforces at lint time.
- bash tools/lint/response.sh --test

## 3.5 Closeout (Mandatory Routing)
- Execute docs/MANUAL.md Closeout Cycle in order (Verify, Harvest, Refresh, Log, Prune).
- Update SoP.md and PoW.md with DP entries, including objective summary and current proof / receipt summary.
- Protocol order for closeout: Verify -> Generate Results -> COMMIT (Operator Only) -> Prune.
- Run prune hygiene: ./ops/bin/prune --scrub.
- Refresh llms artifacts: `./ops/bin/llms`
- Regenerate session artifacts: `./ops/bin/open --out=auto`
- Ensure the next session begins with refreshed session artifacts and matching receipts.
- Note: audit dump generation is owned by `./ops/bin/bundle --profile=audit --out=auto` and is separate from operator session refresh. Do not stamp an additional `ops/bin/dump --scope=core` into closeout unless the DP explicitly authorizes a standalone state capture.
- Refresh side-effect: `ops/bin/llms` regenerates `ops/lib/manifests/OPS.md` as a compile event; if `OPS.md` is not in the allowlist, restore it before running `integrity.sh`. See `docs/MANUAL.md` Refresh side-effect notice for the full procedure.

### 3.5.1 Mandatory Closing Sidecar
Closing sidecar content is generated and maintained at `storage/handoff/CLOSING.md` and is validated by `ops/bin/certify` as a hard gate at certification time. Do not author, predict, populate, or approximate any sidecar-derived closeout output outside this sidecar.
Certify separately collects contractor-authored narrative for the RESULTS Contractor Execution Narrative section at certify time via interactive editor prompt; this narrative input is independent from closing sidecar content.

Before running certify, confirm `storage/handoff/CLOSING.md` has been maintained throughout execution and reflects observable reality only.

Closing sidecar fields required at certify time:
- Commit Message
- Create Pull Request (Title)
- Create Pull Request (Description)
- Confirm Merge (Commit Message)
- Confirm Merge (Extended Description)
- Confirm Merge (Add a Comment)
