---
trace_id: stela-20260226T020946Z-0a96760e
packet_id: DP-OPS-0114
created_at: 2026-02-26T02:13:46Z
previous: (none)
---
# STELA TASK DASHBOARD
Status: ACTIVE
Owner: Integrator
Last Updated: 2026-02-18

## 1. Session State
Pointer: Session context output (generated by ops/bin/open)
Context Manifest: ops/lib/manifests/CONTEXT.md (checked by tools/lint/context.sh)

Session State contract:
- Session State is pointer-first. Do not write inline branch or hash state here.
- OPEN and OPEN-PORCELAIN are the session state snapshot.
- OPEN-PORCELAIN is emitted on every OPEN run. Clean sessions produce a zero-line artifact.
- RESULTS is the audit record for the DP.
- TASK is the stable routing surface.
- Operator note (2026-02-17): Do not edit `SoP.md` or `PoW.md` unless explicitly instructed.

## 2. Logic Pointers
Primary Constraint: PoT.md (Policy of Truth).
Surface Contract Spec: docs/ops/specs/surfaces/task.md.

### 2.1 Governance Pointers
Jurisdiction: PoT.md Section 3.
Git Authority: PoT.md Section 4.1.
Behavioral Standard: PoT.md Section 4.2.
Staffing Protocol: PoT.md Section 4.1.

### 2.2 Enforcement and Gates
Style: tools/lint/style.sh
Context: tools/lint/context.sh
Truth: tools/lint/truth.sh
DP lint: tools/lint/dp.sh (enforces DP transaction rules in Section 3; does not enforce TASK container schema)
TASK lint: tools/lint/task.sh (sole TASK surface/container schema enforcer)
Repo verify: tools/verify.sh
llms parity: tools/lint/llms.sh

## 3. Current Dispatch Packet (DP)

Section-by-section authoring protocol:
- Generate DP structure from `ops/src/surfaces/dp.md.tpl` using `ops/bin/draft`.
- Do not hand-edit DP structural boilerplate in `TASK.md` after generation.
- Manual edits are limited to slot content only (objective, scope, safety, plan body, receipt body).
- Freeze each completed subsection in order (3.1 -> 3.2 -> 3.3 -> 3.4.x).
- Do not revise earlier sections to justify later work. If scope changes, end the thread and draft a new DP.

Freeze Rule:
- If a completed DP subsection changes later, append an AMENDMENT block in RESULTS; otherwise DP is FAIL.

AMENDMENT: minimum fields
- Reopened section:
- Reason:
- Impact:
- Allowlist change (none or exact paths):

Input discipline (hard rule):
- Worker receives only the DP text. Worker does not require OPEN or DUMP attachments.
- DP writer must not attach or cite disposable artifacts (rough notes, chat logs, scratch drafts, throwaway excerpts).
- DP writer must not include pasted bundles (OPEN, DUMP payloads, manifests) inside the DP body.
- Canon pointers only. Evidence belongs in storage artifacts and RESULTS.

### DP-OPS-0114: `ops/bin/llms` Refresh Side-Effect Documentation

## 3.1 Freshness Gate (Must Pass Before Work)
Base Branch: main
Required Work Branch: work/DP-OPS-0114-llms-refresh-docs-2026-02-25
Base HEAD: 10e5cc3a2
Freshness Stamp: 2026-02-25
Note: FRESHNESS_STAMP must be YYYY-MM-DD format only. No trace tokens, no timestamps, no other text. Certify rejects all other forms. dp.sh enforces at lint time.

Required local re-check (worker runs; paste outputs in RESULTS):
- git rev-parse --abbrev-ref HEAD
- git rev-parse --short HEAD
- git status --porcelain

Preconditions:
- No commits on main.
- Working tree must be clean before execution begins.
- If Base HEAD changes, regenerate session artifacts from canonical tools before proceeding.

STOP conditions:
- STOP if any mismatch (branch, Base HEAD, missing work branch).
- STOP if working tree is dirty before execution begins.
- STOP if told to create or switch branches.

## 3.1.1 DP Preflight Gate (Run Before Any Edits)
Purpose:
- Catch malformed DP or TASK schema before work begins.

Worker runs (paste outcome in RESULTS):
- bash tools/lint/dp.sh --test
- bash tools/lint/dp.sh TASK.md
- bash tools/lint/task.sh

STOP if any preflight check fails.

### CbC Design Discipline Preflight (TASK.md §3.1.1)
Required when the DP objective adds, modifies, or replaces a linter, script, guard, or validation binary.
For non-tooling DPs: state "Not applicable" with a one-line justification.
Not applicable. This DP adds no linter, script, guard, or validation binary. Both target changes are documentation edits only.

## 3.2 Required Context Load (Read Before Doing Anything)

### 3.2.1 Canon load order (always)
Worker must confirm loaded before edits begin:
1. PoT.md
2. SoP.md
3. PoW.md
4. TASK.md
5. docs/MAP.md
6. docs/MANUAL.md
7. ops/lib/manifests/CONTEXT.md

Notes:
- Worker does not read OPEN. OPEN is for Integrator state refresh and for receipts.
- Disposable artifacts must not be referenced or included.
- Worker input is the DP text only.
- DP writer must not attach or cite disposable artifacts.
- DP writer must not embed pasted bundles.

<!-- CCD: ff_target="machine-dense" ff_band="10-20" -->
# Contractor Brief

1 Temp path rule uses `var/tmp` only
2 RESULTS generation rule uses `ops/bin/certify` only
3 Receipt rule runs every DP mandated command and treats any skip as STOP
4 Disposable artifact rule treats OPEN dump bundles and manifests as non canonical session artifacts
5 Path rule uses existing repo paths or explicit `NEW` changelog markers and forbids globs and brace expansion

### 3.2.2 DP-scoped load order (per DP)
Read in this order before any edits begin:
1. `docs/MANUAL.md` — target file; read the full Refresh phase guidance (Step 4 of the Closeout Cycle) before editing; locate the exact insertion anchor.
2. `docs/ops/specs/binaries/llms.md` — read to confirm the `ops/lib/manifests/OPS.md` side-effect description before authoring the MANUAL notice.
3. `ops/src/surfaces/dp.md.tpl` — target template file; read §3.5 Closeout in full before editing; locate the exact insertion anchor.

## 3.3 Scope and Safety
Objective:
Document the expected side effect of `ops/bin/llms`: the binary regenerates `ops/lib/manifests/OPS.md` during Refresh in addition to the `llms*.txt` bundle outputs. Contractors must know this is expected compile-event behavior, that the pre-commit hook protects only the commit boundary and does not prevent the working-tree modification, and that `git restore` plus a re-run of `tools/lint/integrity.sh` is required if `OPS.md` is not in the active DP allowlist. Add a companion reminder in the DP closeout instructions template referencing this notice.

In scope:
- `docs/MANUAL.md` — UPDATE: add an explicit `ops/bin/llms` Refresh side-effect notice inside the existing Refresh step (Step 4 of the Closeout Cycle). The notice must cover the following five points in order:
  1. `ops/bin/llms` is a compile event. It regenerates `ops/lib/manifests/OPS.md` in addition to `llms*.txt` bundle outputs. This is expected behavior.
  2. The pre-commit hook (`.github/hooks/llms`) protects against committing `OPS.md` out-of-scope. It does not prevent the working-tree modification.
  3. If `OPS.md` is not in the active DP allowlist, restore it after Refresh: `git restore --source=HEAD --staged --worktree -- ops/lib/manifests/OPS.md`, then re-run `bash tools/lint/integrity.sh` to confirm clean state before proceeding.
  4. If `OPS.md` content genuinely needs updating, add it to the allowlist and authorize the change explicitly before proceeding.
  5. OPEN/DUMP Refresh can legitimately increase porcelain entries even when the DP is docs-only; account for this before running `integrity.sh`.
- `ops/src/surfaces/dp.md.tpl` — UPDATE: add a single Refresh side-effect reminder line in §3.5 Closeout, immediately after the existing `- Ensure the next session begins with refreshed session artifacts and matching receipts.` line, referencing the MANUAL notice.

Out of scope:
- `ops/bin/llms` implementation — no changes.
- `.github/hooks/llms` pre-commit hook — no changes.
- `ops/lib/manifests/OPS.md` content — not changed.
- `docs/ops/specs/binaries/llms.md` — no changes; the existing spec already describes the side effect; this DP adds operator-facing guidance only.
- Any code change of any kind.
- Any closing-block schema, label, or toolchain topic — that is post-0116+A baseline; do not introduce closing-block changes here.

Safety and invariants:
- No manual edits to generated outputs (`ops/lib/manifests/OPS.md`, `llms*.txt`, `archives/*`).
- No structural edits to TASK.md or the DP block.
- Allowlist hard gate: every file changed must appear in `storage/dp/active/allowlist.txt` before `tools/lint/integrity.sh` runs.
- Docs-only DP: no code changes are authorized in any file.
- Closing-block schema assumption: current six-label schema only (post-0116+A baseline). No legacy or compatibility paths introduced.

Worker Constraints (SSOT injected):
## Section 1: Universal Template Rules
- Template source uses `tpl` files with optional YAML frontmatter
- Canon frontmatter keys: `template_type` `template_id` `template_version` `requires_slots` `includes`
- Renderer strips frontmatter before output write
- Slot token form `\{\{TOKEN\}\}` with uppercase alphanumeric underscore
- Include forms `\{\{@include:path\}\}` and `\{\{@include:path#section\}\}`
- Include resolution is strict: missing file fail missing section fail circular graph fail
- Strict mode default: every required slot value present and no unresolved token
- Non strict mode allowed only for lint and normalization workflows
- Worker facing generated surfaces remain pointer first and exclude disposable artifacts

Target Files allowlist (hard gate):
- storage/dp/active/allowlist.txt

## 3.4 Execution Plan (A-E)

### 3.4.1 State
Repository is at HEAD `10e5cc3a2` on branch `main`. Working tree is clean. DP-OPS-0113 is closed; its archive leaf `archives/surfaces/TASK-DP-OPS-0113-298978a48.md` is present in the repository. Closing-block toolchain is current-schema-only (single six-label schema, post-0116+A baseline). `docs/MANUAL.md` Refresh phase guidance (Closeout Cycle Step 4) does not contain an explicit notice of the `ops/bin/llms` `OPS.md` working-tree side effect or the required restore procedure. `ops/src/surfaces/dp.md.tpl` §3.5 Closeout does not contain a Refresh side-effect reminder line.

### 3.4.2 Request
1. Add an explicit, findable `ops/bin/llms` Refresh side-effect notice to `docs/MANUAL.md` under Closeout Cycle Step 4 (Refresh), covering all five documented points: the compile-event nature of the binary, the hook's commit-boundary-only scope, the `git restore` recovery command and integrity re-check, the allowlist authorization path for intentional `OPS.md` changes, and the porcelain-entry note.
2. Add a single companion reminder line to `ops/src/surfaces/dp.md.tpl` §3.5 Closeout that references the MANUAL notice for the Refresh side effect.
3. Update `storage/dp/active/allowlist.txt` to replace the DP-OPS-0113 entries with the DP-OPS-0114 scope entries and standard closeout paths.
4. Run all required preflight and receipt gates. No code changes.

### 3.4.3 Changelog
- `docs/MANUAL.md` — UPDATE: add `ops/bin/llms` Refresh side-effect notice block under Closeout Cycle Step 4 (Refresh).
- `ops/src/surfaces/dp.md.tpl` — UPDATE: add Refresh side-effect reminder line in §3.5 Closeout.
- `storage/dp/active/allowlist.txt` — UPDATE: replace DP-OPS-0113 scope entries with DP-OPS-0114 scope entries and standard closeout paths.

### 3.4.4 Patch / Diff

**Step 1 — Update `storage/dp/active/allowlist.txt`**

Replace the full contents of `storage/dp/active/allowlist.txt` with the following entries:
```
TASK.md
PoW.md
SoP.md
docs/MANUAL.md
ops/src/surfaces/dp.md.tpl
storage/dp/active/allowlist.txt
storage/handoff/CLOSING-DP-OPS-0114.md
llms.txt
llms-core.txt
llms-full.txt
archives/manifests/compile-*.md
```

Note: certify-generated `archives/surfaces/` leaf paths for SoP, PoW, and TASK (`archives/surfaces/SoP-*.md`, `archives/surfaces/PoW-*.md`, `archives/surfaces/TASK-DP-OPS-0114-*.md`) are not known at draft time because the filename includes the git short hash at certification time. Per `docs/MANUAL.md` §Generate Results pre-certify allowlist declaration: before invoking `ops/bin/certify`, add the three specific `archives/surfaces/` leaf paths to `storage/dp/active/allowlist.txt` once their filenames are determined from `git diff --name-only` output during the pre-certify state snapshot.

**Step 2 — Edit `docs/MANUAL.md`: add Refresh side-effect notice**

Locate the Refresh step block in the Closeout Cycle section. The anchor line is:
```
./ops/bin/llms --out-dir="$(pwd)"
```

Immediately after that line and before the `Compile snapshot policy:` line, insert the following block:
```
`ops/bin/llms` Refresh Side-Effect Notice:
- `ops/bin/llms` is a compile event. It regenerates `ops/lib/manifests/OPS.md` in addition to `llms*.txt` bundle outputs. This is expected behavior, not a defect.
- The pre-commit hook (`.github/hooks/llms`) protects against committing `OPS.md` out-of-scope at the commit boundary. It does not prevent the working-tree modification from occurring during Refresh.
- If `OPS.md` is not in the active DP allowlist, restore it after Refresh before running `tools/lint/integrity.sh`:
  `git restore --source=HEAD --staged --worktree -- ops/lib/manifests/OPS.md`
  Then re-run `bash tools/lint/integrity.sh` to confirm clean state before proceeding.
- If `OPS.md` content genuinely needs updating, add it to the allowlist and authorize the change explicitly before proceeding.
- OPEN/DUMP Refresh can legitimately produce porcelain entries (e.g., modified `OPS.md`, new `archives/manifests/compile-*.md` leaves) even when the DP is docs-only. Account for these before running `integrity.sh` or interpreting `git status --porcelain` output.
```

**Step 3 — Edit `ops/src/surfaces/dp.md.tpl`: add Refresh reminder in §3.5**

Locate §3.5 Closeout in `ops/src/surfaces/dp.md.tpl`. The anchor line is:
```
- Ensure the next session begins with refreshed session artifacts and matching receipts.
```

Immediately after that line, insert the following line:
```
- Refresh side-effect: `ops/bin/llms` regenerates `ops/lib/manifests/OPS.md` as a compile event; if `OPS.md` is not in the allowlist, restore it before running `integrity.sh`. See `docs/MANUAL.md` Refresh side-effect notice for the full procedure.
```

**Step 4 — Maintain closing sidecar**

Create and populate `storage/handoff/CLOSING-DP-OPS-0114.md` using `ops/src/surfaces/closing.md.tpl` as the schema. Maintain this file throughout execution. It must reflect observable reality only before certify runs.

### 3.4.5 Receipt (Proofs to collect) - MUST RUN

**Mandatory receipt commands (always run; do not omit):**
- bash tools/lint/dp.sh TASK.md
- bash tools/lint/task.sh
- bash tools/lint/integrity.sh
- bash tools/lint/style.sh
- git diff --name-only
- git diff --stat
- comm -23 <(git diff --name-only | sort) <(sort storage/dp/active/allowlist.txt) || true
- comm -23 <(git ls-files --others --exclude-standard | sort) <(sort storage/dp/active/allowlist.txt) || true
- ./ops/bin/open

**DP-specific receipt commands (scope-specific; author below):**
Note: Command substitution forms (e.g., $(pwd), $(git ...)) are rejected by certify replay. Use literal values only. dp.sh enforces at lint time.
- bash tools/lint/truth.sh
- grep -n "ops/lib/manifests/OPS.md" docs/MANUAL.md
- grep -n "Refresh side-effect" ops/src/surfaces/dp.md.tpl

## 3.5 Closeout (Mandatory Routing)
- Execute docs/MANUAL.md Closeout Cycle in order (Verify, Harvest, Refresh, Log, Prune).
- Update SoP.md and PoW.md with DP entries, including objective summary and verification commands run.
- Protocol order for closeout: Verify -> Generate Results -> COMMIT (Operator Only) -> Prune.
- Run prune hygiene: ./ops/bin/prune --scrub.
- Regenerate session artifacts: `./ops/bin/open --out=auto`
- Capture updated platform state: `./ops/bin/dump --scope=platform --format=chatgpt --out=auto`
- Ensure the next session begins with refreshed session artifacts and matching receipts.
- Refresh side-effect: `ops/bin/llms` regenerates `ops/lib/manifests/OPS.md` as a compile event; if `OPS.md` is not in the allowlist, restore it before running `integrity.sh`. See `docs/MANUAL.md` Refresh side-effect notice for the full procedure.

### 3.5.1 Mandatory Closing Block
Closing block content is generated exclusively by `ops/bin/certify` from the closing sidecar at `storage/handoff/CLOSING-DP-OPS-0114.md` at certification time. Do not author, predict, populate, or approximate any closing block field at draft time or during execution. The closing sidecar is the only human-authored narrative input certify ingests. Certify generates all of the following from observable repository state: Primary Commit Header, Pull Request Title, Pull Request Description, Final Squash Stub, Extended Technical Manifest, and Review Conversation Starter.

Before running certify, confirm `storage/handoff/CLOSING-DP-OPS-0114.md` has been maintained throughout execution and reflects observable reality only.

Fields certify generates from the closing sidecar and repository state:
- Primary Commit Header
- Pull Request Title
- Pull Request Description
- Final Squash Stub
- Extended Technical Manifest
- Review Conversation Starter
