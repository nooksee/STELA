---
trace_id: stela-20260402T194515Z-77586cdb
packet_id: DP-OPS-0244
created_at: 2026-04-02T19:47:55Z
previous: (none)
---
# STELA TASK DASHBOARD
Status: ACTIVE
Owner: Integrator
Last Updated: 2026-02-14

## 1. Session State
Pointer: Session context output (generated by ops/bin/open)
Context Manifest: ops/lib/manifests/CONTEXT.md (checked by tools/lint/context.sh)

Session State contract:
- Session State is pointer-first. Do not write inline branch or hash state here.
- OPEN (and OPEN-PORCELAIN when dirty) is the session state snapshot.
- RESULTS is the audit record for the DP.
- TASK is the stable routing surface.

## 2. Logic Pointers
Primary Constraint: PoT.md (Policy of Truth).
Surface Contract Spec: docs/ops/specs/surfaces/task.md.

### 2.1 Governance Pointers
Jurisdiction: PoT.md Section 3.
Git Authority: PoT.md Section 4.1.
Behavioral Standard: PoT.md Section 4.2.
Staffing Protocol: PoT.md Section 4.1.

### 2.2 Enforcement and Gates
Style: tools/lint/style.sh
Context: tools/lint/context.sh
Truth: tools/lint/truth.sh
DP lint: tools/lint/dp.sh (enforces DP transaction rules in Section 3; does not enforce TASK container schema)
TASK lint: tools/lint/task.sh (sole TASK surface/container schema enforcer)
Repo verify: tools/verify.sh

## 3. Current Dispatch Packet (DP)

Section-by-section authoring protocol:
- Generate DP structure via `./ops/bin/bundle --profile=draft --out=auto` (standard operator path: Architect completes the embedded DP AUTHORING SCAFFOLD; save the returned DP body to `storage/dp/intake/DP.md`) or locally via `ops/bin/draft` (template render only).
- Do not hand-edit DP structural boilerplate in `TASK.md` after generation.
- Manual edits are limited to slot content only (objective, scope, safety, plan body, receipt body).
- Freeze each completed subsection in order (3.1 -> 3.2 -> 3.3 -> 3.4.x).
- Do not revise earlier sections to justify later work. If scope changes, end the thread and draft a new DP.

Freeze Rule:
- If a completed DP subsection changes later, append an AMENDMENT block in RESULTS; otherwise DP is FAIL.

AMENDMENT: minimum fields
- Reopened section:
- Reason:
- Impact:
- Allowlist change (none or exact paths):

Input discipline (hard rule):
- Worker receives only the DP text. Worker does not require OPEN or DUMP attachments.
- DP writer must not attach or cite disposable artifacts (rough notes, chat logs, scratch drafts, throwaway excerpts).
- DP writer must not include pasted bundles (OPEN, DUMP payloads, manifests) inside the DP body.
- Canon pointers only. Evidence belongs in storage artifacts and RESULTS.


### DP-OPS-0244: Decouple core DP role language from task and surface names

## 3.1 Freshness Gate (Must Pass Before Work)
Base Branch: main
Work Branch: work/dp-ops-0244-2026-04-02
Base HEAD: 8affdafe
Freshness Stamp: 2026-04-02
Note: FRESHNESS_STAMP must be YYYY-MM-DD format only. No trace tokens, no timestamps, no other text. Certify rejects all other forms. dp.sh enforces at lint time.

Required local re-check (worker runs before any edits; paste verbatim outputs under `## Worker Execution Narrative` -> `### Preflight State` in RESULTS):
- git rev-parse --abbrev-ref HEAD
- git rev-parse --short HEAD
- git status --porcelain

These three commands are freshness proof only. Do not treat §3.4.5 receipt replay as a substitute for their pre-edit outputs.

Preconditions:
- No commits on main.
- Working tree must be clean before execution begins.
- If Base HEAD changes, regenerate session artifacts from canonical tools before proceeding.

STOP conditions:
- STOP if any mismatch (branch, Base HEAD, missing work branch).
- STOP if working tree is dirty before execution begins.
- STOP if told to create or switch branches.

## 3.1.1 DP Preflight Gate (Run Before Any Edits)
Purpose:
- Catch malformed DP or TASK schema before work begins.

Worker runs (paste outcome in RESULTS):
- bash tools/lint/dp.sh --test
- bash tools/lint/dp.sh TASK.md
- bash tools/lint/task.sh

STOP if any preflight check fails.

### CbC Design Discipline Preflight (TASK.md §3.1.1)
Required when the DP objective adds, modifies, or replaces a linter, script, guard, or validation binary.
For non-tooling DPs: state "Not applicable" with a one-line justification.
Tooling-applicable. The packet updates active render/runtime binaries (`ops/bin/manifest`, `ops/bin/template`, `ops/bin/addendum`, `ops/bin/draft`, `ops/bin/certify`, `ops/bin/help`, `ops/bin/open`), active lint/scripts (`tools/lint/results.sh`, `tools/lint/dp.sh`, `tools/lint/response.sh`, `tools/lint/style.sh`, `ops/lib/scripts/editor.sh`), and the coupled specs/registries/templates needed to keep the core DP spine truthful after the narrative rename, role-wording cleanup, alias removal, and surface-template moves.

## 3.2 Required Context Load (Read Before Doing Anything)

### 3.2.1 Canon load order (always)
Worker must confirm loaded before edits begin:
1. PoT.md
2. SoP.md
3. PoW.md
4. TASK.md
5. docs/MAP.md
6. docs/MANUAL.md
7. ops/lib/manifests/CONTEXT.md

Notes:
- Worker does not read OPEN. OPEN is for Integrator state refresh and for receipts.
- Disposable artifacts must not be referenced or included.
- Worker input is the DP text only.
- DP writer must not attach or cite disposable artifacts.
- DP writer must not embed pasted bundles.

## Rules

1 Temp path rule uses `var/tmp` only
2 RESULTS generation rule uses `ops/bin/certify` only
3 Receipt rule runs every DP mandated command and treats any skip as STOP
4 Disposable artifact rule treats OPEN dump bundles and manifests as non canonical session artifacts
5 Use repo-relative paths only in all output. Never print absolute filesystem paths. Do not emit clickable file links.

### 3.2.2 DP-scoped load order (per DP)
- storage/dp/active/allowlist.txt
- docs/MANUAL.md
- ops/src/surfaces/dp.md.tpl
- ops/src/surfaces/results.md.tpl
- docs/ops/specs/surfaces/results.md
- tools/lint/results.sh
- docs/ops/specs/tools/lint/results.md
- ops/bin/certify
- docs/ops/specs/binaries/certify.md
- ops/lib/scripts/editor.sh
- docs/ops/specs/scripts/editor.md
- tools/lint/response.sh
- docs/ops/specs/tools/lint/response.md
- tools/lint/style.sh
- docs/ops/registry/lint.md
- tools/lint/dp.sh
- docs/ops/specs/tools/lint/dp.md
- ops/bin/manifest
- docs/ops/specs/binaries/manifest.md
- ops/bin/template
- docs/ops/specs/binaries/template.md
- ops/bin/addendum
- ops/bin/draft
- ops/bin/help
- ops/bin/open
- ops/src/stances/planning.md.tpl
- docs/ops/specs/stances/planning.md
- ops/src/stances/draft.md.tpl
- docs/ops/specs/stances/draft.md
- ops/src/stances/audit.md.tpl
- ops/src/stances/addenda.md.tpl
- docs/ops/specs/stances/addenda.md
- ops/src/shared/stances.json
- ops/src/stances/plan.md.tpl
- ops/src/stances/addendum.md.tpl
- docs/ops/specs/stances/plan.md
- docs/ops/specs/surfaces/pow.md
- docs/ops/specs/binaries/addendum.md
- docs/ops/registry/binaries.md
- docs/ops/registry/templates.md
- ops/lib/manifests/OPS.md

## 3.3 Scope and Safety
Objective:
- Decouple the core DP spine from mixed role/task naming by renaming the RESULTS execution narrative to `Worker Execution Narrative`, normalizing the active addenda and MANUAL lane vocabulary to current played-role terms, and removing role-alias render keys from the live manifest/template path.
- Separate surface templates from stance templates by moving the `plan` and `addendum` surfaces out of `ops/src/stances/` and updating the coupled specs and registries to match.
- Stop before factory-coupled agent identity cleanup; the current `opt/_factory/agents/` leaves still declare `foreman` and `conformist`, so narrowing `docs/ops/specs/definitions/agents.md` or `tools/lint/agent.sh` in this packet would create a knowingly false contract.

In scope:
- `ops/src/surfaces/dp.md.tpl`
  - Rename the RESULTS narrative reference from `Contractor Execution Narrative` to `Worker Execution Narrative` and update the surrounding wording to match the current worker role.
- `ops/src/surfaces/results.md.tpl`
  - Rename the execution narrative section heading to `## Worker Execution Narrative` and rename the slot key if needed so the surface contract no longer uses contractor-only naming.
- `docs/ops/specs/surfaces/results.md`
  - Mirror the narrative rename and any worker-facing wording changes exactly.
- `ops/bin/certify`
  - Rename the execution-narrative capture/render path, required heading checks, and slot handoff so certify emits `Worker Execution Narrative` consistently.
- `docs/ops/specs/binaries/certify.md`
  - Mirror the certify narrative rename and slot/heading wording exactly.
- `ops/lib/scripts/editor.sh`
  - Rename the narrative scaffold labels and helper wording to `Worker Execution Narrative`.
- `docs/ops/specs/scripts/editor.md`
  - Mirror the editor scaffold rename exactly.
- `tools/lint/results.sh`
  - Update schema/lint enforcement to validate `Worker Execution Narrative` instead of the retired contractor heading.
- `docs/ops/specs/tools/lint/results.md`
  - Mirror the results-lint rename and enforcement text exactly.
- `tools/lint/response.sh`
- `docs/ops/specs/tools/lint/response.md`
- `tools/lint/style.sh`
- `docs/ops/registry/lint.md`
- `ops/src/shared/stances.json`
- `ops/src/stances/draft.md.tpl`
- `docs/ops/specs/stances/draft.md`
- `ops/src/stances/audit.md.tpl`
  - Update narrative-heading guardrails and wording so planning/draft/audit enforcement tracks `Worker Execution Narrative`, not the retired contractor heading.
- `docs/MANUAL.md`
  - Update only the active core spine and addenda lane wording needed for current played roles: `architect`, `worker`, `supervisor`, `auditor`, `operator`.
  - Correct `integrator refresh` to `architect refresh` where the repo is describing OPEN's role in draft/dispatch flow.
- `docs/ops/specs/binaries/addendum.md`
  - Replace `foreman` / `Contractor` / stale mixed-role prose with the current supervisor/worker terminology while preserving the addendum issuance chain.
- `ops/bin/addendum`
- `ops/bin/draft`
- `tools/lint/dp.sh`
- `docs/ops/specs/tools/lint/dp.md`
  - Move the canonical addendum template path from `ops/src/stances/addendum.md.tpl` to `ops/src/surfaces/addendum.md.tpl` and keep runtime readers and lint/spec constants in sync.
- `ops/bin/manifest`
  - Remove the live role-alias render keys `stance-analyst` and `stance-authority`.
  - Keep canonical stance render keys process-named only.
  - Update surface template paths if `plan` and `addendum` move under `ops/src/surfaces/`.
- `docs/ops/specs/binaries/manifest.md`
  - Remove the role-alias render-key contract and mirror the new canonical render set exactly.
- `ops/bin/template`
  - Remove the role-alias shim routing that still blesses `stance-analyst` and `stance-authority`.
- `docs/ops/specs/binaries/template.md`
  - Mirror the template-router alias removal exactly.
- `ops/src/stances/plan.md.tpl`
- `ops/src/stances/addendum.md.tpl`
  - Move these surface templates out of `ops/src/stances/` into `ops/src/surfaces/`.
- `docs/ops/specs/stances/plan.md`
  - Move or replace this spec so the `PLAN` surface is documented under the surface-spec path rather than the stance-spec path.
- `docs/ops/specs/surfaces/addendum.md`
  - Add the missing addendum surface spec if needed so addendum surface behavior is no longer described only through the binary spec.
- `docs/ops/specs/stances/planning.md`
- `docs/ops/specs/stances/addenda.md`
  - Remove legacy alias statements and keep only the canonical stance render path.
- `docs/ops/registry/binaries.md`
  - Remove registry prose that still advertises the role-alias shim.
- `ops/bin/help`
- `ops/bin/open`
  - Update the visible active-lane wording to current `supervisor` / `addendum` terminology where these binaries advertise secondary lanes.
- `docs/ops/registry/templates.md`
  - Update template registry paths if `plan` and `addendum` templates move.
- `docs/ops/specs/surfaces/pow.md`
  - Mirror the narrative-heading rename where PoW points operators back to RESULTS.
- `storage/dp/active/allowlist.txt`
  - Update only for the exact moved/new/deleted paths in this packet.
- `ops/lib/manifests/OPS.md`
- `llms.txt`
- `llms-core.txt`
- `llms-full.txt`
  - Accept only the direct `./ops/bin/llms` compile fallout tied to the path and wording changes above.

Out of scope:
- `docs/ops/specs/definitions/agents.md`
- `tools/lint/agent.sh`
- `opt/_factory/agents/`
- `opt/_factory/AGENTS.md`
- `ops/lib/scripts/agent.sh`
- `docs/ops/registry/agents.md`
- `docs/ops/registry/factory.md`
- `docs/ops/specs/tools/lint/factory.md`
- hook consolidation (`.github/hooks/*`)
- `RoR` audit-carriage follow-up
- planner behavior, popup transport, or plan/draft semantics
- bundle profile changes outside the manifest/template alias removal required by this packet

Safety and invariants:
- Do not widen this packet into factory identity migration. If a proposed change would require editing `docs/ops/specs/definitions/agents.md`, `tools/lint/agent.sh`, or any `opt/_factory/agents/*` leaf to stay truthful, STOP and report that seam instead of forcing it into `0244`.
- Keep the role cleanup anchored to the current actor model already settled in canon: `operator`, `integrator`, and `contractor` remain base actors; `architect`, `supervisor`, and `worker` are played roles.
- Preserve the addendum intervention chain and addendum authority boundaries. This packet may rename stale role terms, but it must not change who authorizes or executes addenda.
- Remove only the live role-alias render keys (`stance-analyst`, `stance-authority`). Do not invent new compatibility aliases to replace them.
- If `plan` and `addendum` templates move into `ops/src/surfaces/`, update every active runtime/spec/registry pointer in the same packet so no active path still points to the old stance directory.
- `ops/lib/manifests/OPS.md` and root `llms*.txt` may change only as direct `./ops/bin/llms` side effects. If unrelated compile churn appears, STOP and restore packet focus before certify.

Worker Constraints (SSOT injected):
## Section 1: Universal Template Rules
- Template source uses `tpl` files with optional YAML frontmatter
- Canon frontmatter keys: `template_type` `template_id` `template_version` `requires_slots` `includes`
- Renderer strips frontmatter before output write
- Slot token form `\{\{TOKEN\}\}` with uppercase alphanumeric underscore
- Include forms `\{\{@include:path\}\}` and `\{\{@include:path#section\}\}`
- Include resolution is strict: missing file fail missing section fail circular graph fail
- Strict mode default: every required slot value present and no unresolved token
- Non strict mode allowed only for lint and normalization workflows
- Worker facing generated surfaces remain pointer first and exclude disposable artifacts

Target Files allowlist (hard gate):
- storage/dp/active/allowlist.txt

## 3.4 Execution Plan (A-E)

### 3.4.1 State
- `DP-OPS-0241` through `DP-OPS-0243` removed the active conform lane, aligned deletion semantics across `integrity.sh`, `dp.sh`, and `certify`, and removed the orphan contractor stance/spec pair. The next decoupling seam is now naming rather than delete hygiene.
- The RESULTS path still hard-codes `Contractor Execution Narrative` in the DP surface template, RESULTS surface template/spec, and results lint/spec.
- The active narrative path also still hard-codes contractor wording in `ops/bin/certify`, `ops/lib/scripts/editor.sh`, `tools/lint/response.sh`, `tools/lint/style.sh`, `ops/src/shared/stances.json`, `ops/src/stances/draft.md.tpl`, `ops/src/stances/audit.md.tpl`, `docs/ops/specs/stances/draft.md`, `docs/ops/specs/surfaces/pow.md`, and the coupled certify/editor/response/registry specs.
- The active MANUAL/addenda path still mixes retired role nouns such as `Contractor`, `foreman`, and `Integrator` in the shipping spine wording, and line 37 in `docs/MANUAL.md` still says OPEN is for `integrator refresh`.
- `ops/bin/manifest` and `ops/bin/template` still advertise role-alias render keys `stance-analyst` and `stance-authority`.
- `ops/src/stances/plan.md.tpl` and `ops/src/stances/addendum.md.tpl` are surface templates living under the stance directory; `tools/lint/dp.sh`, `ops/bin/addendum`, and `ops/bin/draft` still read the old addendum template path directly; `docs/ops/specs/stances/plan.md` is explicitly a surface spec while addendum surface behavior is still described through the binary spec.
- The factory-agent identity layer is not ready for cleanup in this packet: current `opt/_factory/agents/*` leaves still declare `runtime_role: foreman|conformist` and `stance_id: addenda|audit|conformist`, so tightening `docs/ops/specs/definitions/agents.md` or `tools/lint/agent.sh` here would knowingly diverge from visible repo truth.

### 3.4.2 Request
- Rename `Contractor Execution Narrative` to `Worker Execution Narrative` across the active DP/RESULTS contract, including the RESULTS surface template, its spec, and results lint/spec.
- Normalize `docs/MANUAL.md` and `docs/ops/specs/binaries/addendum.md` to the current played-role terms `architect`, `worker`, `supervisor`, `auditor`, and `operator`, including changing `integrator refresh` to `architect refresh`.
- Remove `stance-analyst` and `stance-authority` from the live manifest/template render path and from the coupled specs/registry prose that still blesses them.
- Move the `plan` and `addendum` surface templates out of `ops/src/stances/` into `ops/src/surfaces/`, and update the active specs/registries so surface templates and stance templates are no longer mixed in the same directory contract.
- Do not edit the factory agent definitions or agent lint in this packet.

### 3.4.3 Changelog
- UPDATE `ops/src/surfaces/dp.md.tpl`
- UPDATE `ops/src/surfaces/results.md.tpl`
- UPDATE `docs/ops/specs/surfaces/results.md`
- UPDATE `ops/bin/certify`
- UPDATE `docs/ops/specs/binaries/certify.md`
- UPDATE `ops/lib/scripts/editor.sh`
- UPDATE `docs/ops/specs/scripts/editor.md`
- UPDATE `tools/lint/results.sh`
- UPDATE `docs/ops/specs/tools/lint/results.md`
- UPDATE `tools/lint/response.sh`
- UPDATE `docs/ops/specs/tools/lint/response.md`
- UPDATE `tools/lint/style.sh`
- UPDATE `docs/ops/registry/lint.md`
- UPDATE `docs/MANUAL.md`
- UPDATE `ops/src/shared/stances.json`
- UPDATE `ops/src/stances/draft.md.tpl`
- UPDATE `docs/ops/specs/stances/draft.md`
- UPDATE `ops/src/stances/audit.md.tpl`
- UPDATE `docs/ops/specs/binaries/addendum.md`
- UPDATE `ops/bin/addendum`
- UPDATE `ops/bin/draft`
- UPDATE `tools/lint/dp.sh`
- UPDATE `docs/ops/specs/tools/lint/dp.md`
- UPDATE `ops/bin/manifest`
- UPDATE `docs/ops/specs/binaries/manifest.md`
- UPDATE `ops/bin/template`
- UPDATE `docs/ops/specs/binaries/template.md`
- UPDATE `docs/ops/specs/stances/planning.md`
- UPDATE `docs/ops/specs/stances/addenda.md`
- MOVE `ops/src/stances/plan.md.tpl` -> `ops/src/surfaces/plan.md.tpl`
- MOVE `ops/src/stances/addendum.md.tpl` -> `ops/src/surfaces/addendum.md.tpl`
- MOVE `docs/ops/specs/stances/plan.md` -> `docs/ops/specs/surfaces/plan.md`
- NEW `docs/ops/specs/surfaces/addendum.md` (if needed to make addendum surface behavior first-class)
- UPDATE `docs/ops/registry/binaries.md`
- UPDATE `ops/bin/help`
- UPDATE `ops/bin/open`
- UPDATE `docs/ops/specs/surfaces/pow.md`
- UPDATE `docs/ops/registry/templates.md`
- UPDATE `storage/dp/active/allowlist.txt`
- UPDATE `ops/lib/manifests/OPS.md`
- UPDATE `llms.txt`
- UPDATE `llms-core.txt`
- UPDATE `llms-full.txt`

### 3.4.4 Patch / Diff
1. Rename the RESULTS execution narrative heading from `Contractor Execution Narrative` to `Worker Execution Narrative` across the full active capture/render/lint path: DP and RESULTS templates, certify/editor helpers, RESULTS/spec/lint/spec, draft/audit/response/style guardrails, and the shared stance JSON.
2. Update `docs/MANUAL.md`, `docs/ops/specs/binaries/addendum.md`, `ops/bin/help`, `ops/bin/open`, and the coupled registry/spec prose to the current played-role vocabulary: `architect`, `worker`, `supervisor`, `auditor`, and `operator`. Keep the addendum authority chain intact while removing stale `Contractor`, `foreman`, and `Integrator` residue in the active path.
3. Remove `stance-analyst` and `stance-authority` from `ops/bin/manifest`, `ops/bin/template`, and the coupled manifest/template specs and registry prose so active render keys are process/surface named rather than role-alias named.
4. Move `plan` and `addendum` surface templates into `ops/src/surfaces/`; update `tools/lint/dp.sh`, `ops/bin/addendum`, `ops/bin/draft`, and the coupled spec/registry paths so surface templates/specs no longer live under the stance contract by accident.
5. Update `storage/dp/active/allowlist.txt` for the exact moved/new/deleted paths, then run `./ops/bin/llms` and keep only the expected generated manifest fallout in `ops/lib/manifests/OPS.md` and root `llms*.txt`.
6. Verify there are no remaining live references to `Contractor Execution Narrative`, `stance-analyst`, `stance-authority`, `integrator refresh`, `foreman`, or the old `ops/src/stances/{plan,addendum}.md.tpl` paths in the active runtime/spec/registry/lint surfaces before closeout.

### 3.4.5 Receipt (Proofs to collect) - MUST RUN

**Mandatory receipt commands (always run; do not omit):**
- bash tools/lint/dp.sh TASK.md
- bash tools/lint/task.sh
- bash tools/lint/integrity.sh
- bash tools/lint/style.sh
- ./ops/bin/llms
- bash tools/lint/integrity.sh
- git diff --name-only
- git diff --stat
- comm -23 <(git diff --name-only | sort) <(sort storage/dp/active/allowlist.txt) || true
- comm -23 <(git ls-files --others --exclude-standard | sort) <(sort storage/dp/active/allowlist.txt) || true
- ./ops/bin/open

**DP-specific receipt commands (scope-specific; author below):**
Note: Command substitution forms (e.g., $(pwd), $(git ...)) are rejected by certify replay. Use literal values only. Repo-local binaries must be authored as literal `./ops/bin/...` or `ops/bin/...` commands. Unsupported receipt-command forms are rejected by dp.sh and certify.
- bash tools/lint/truth.sh
- ! rg -n "Contractor Execution Narrative|CONTRACTOR_NARRATIVE|contractor-authored narrative|contractor execution narrative" ops/src/surfaces/results.md.tpl ops/src/surfaces/dp.md.tpl ops/bin/certify ops/lib/scripts/editor.sh docs/ops/specs/binaries/certify.md docs/ops/specs/surfaces/results.md docs/ops/specs/surfaces/pow.md tools/lint/results.sh docs/ops/specs/tools/lint/results.md tools/lint/response.sh docs/ops/specs/tools/lint/response.md docs/MANUAL.md ops/src/shared/stances.json ops/src/stances/draft.md.tpl ops/src/stances/audit.md.tpl docs/ops/specs/stances/draft.md docs/ops/registry/lint.md docs/ops/specs/scripts/editor.md
- ! rg -n "stance-analyst|stance-authority" ops/bin/manifest ops/bin/template docs/ops/specs/binaries/manifest.md docs/ops/specs/binaries/template.md docs/ops/specs/stances/planning.md docs/ops/specs/stances/addenda.md docs/ops/registry/binaries.md
- ! rg -n "integrator refresh|foreman|Contractor does not invoke|Integrator/foreman|operator/foreman|foreman/addendum" docs/MANUAL.md docs/ops/specs/binaries/addendum.md docs/ops/registry/binaries.md ops/bin/help ops/bin/open docs/ops/specs/stances/addenda.md
- test -e ops/src/surfaces/plan.md.tpl
- test -e ops/src/surfaces/addendum.md.tpl
- ! test -e ops/src/stances/plan.md.tpl
- ! test -e ops/src/stances/addendum.md.tpl
- test -e docs/ops/specs/surfaces/plan.md
- test -e docs/ops/specs/surfaces/addendum.md

## 3.5 Closeout (Mandatory Routing)
- Execute docs/MANUAL.md Closeout Cycle in order (Verify, Harvest, Refresh, Log, Prune).
- Update SoP.md and PoW.md with DP entries, including objective summary and current proof / receipt summary.
- Protocol order for closeout: Verify -> Generate Results -> COMMIT (Operator Only) -> Prune.
- Run prune hygiene: ./ops/bin/prune --scrub.
- Refresh llms artifacts: `./ops/bin/llms`
- Regenerate session artifacts: `./ops/bin/open --out=auto`
- Ensure the next session begins with refreshed session artifacts and matching receipts.
- Note: audit dump generation is owned by `./ops/bin/bundle --profile=audit --out=auto` and is separate from operator session refresh. Do not stamp an additional `ops/bin/dump --scope=core` into closeout unless the DP explicitly authorizes a standalone state capture.
- Refresh side-effect: `ops/bin/llms` regenerates `ops/lib/manifests/OPS.md` as a compile event; if `OPS.md` is not in the allowlist, restore it before running `integrity.sh`. See `docs/MANUAL.md` Refresh side-effect notice for the full procedure.

### 3.5.1 Mandatory Closing Sidecar
Closing sidecar content is generated and maintained at `storage/handoff/CLOSING.md` and is validated by `ops/bin/certify` as a hard gate at certification time. Do not author, predict, populate, or approximate any sidecar-derived closeout output outside this sidecar.
Certify separately collects worker-authored narrative for the RESULTS Worker Execution Narrative section at certify time via interactive editor prompt; this narrative input is independent from closing sidecar content.

Before running certify, confirm `storage/handoff/CLOSING.md` has been maintained throughout execution and reflects observable reality only.

Closing sidecar fields required at certify time:
- Commit Message
- Create Pull Request (Title)
- Create Pull Request (Description)
- Confirm Merge (Commit Message)
- Confirm Merge (Extended Description)
- Confirm Merge (Add a Comment)
