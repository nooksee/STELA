---
trace_id: stela-20260219T232930Z-1b8d1d35
packet_id: DP-OPS-0076
created_at: 2026-02-20T01:17:58Z
previous: archives/surfaces/TASK-DP-OPS-0076-fe8df16e.md
---
# STELA TASK DASHBOARD
Status: ACTIVE
Owner: Integrator
Last Updated: 2026-02-18

## 1. Session State
Pointer: Session context output (generated by ops/bin/open)
Context Manifest: ops/lib/manifests/CONTEXT.md (checked by tools/lint/context.sh)

Session State contract:
- Session State is pointer-first. Do not write inline branch or hash state here.
- OPEN and OPEN-PORCELAIN are the session state snapshot.
- OPEN-PORCELAIN is emitted on every OPEN run. Clean sessions produce a zero-line artifact.
- RESULTS is the audit record for the DP.
- TASK is the stable routing surface.
- Operator note (2026-02-17): Do not edit `SoP.md` or `PoW.md` unless explicitly instructed.

## 2. Logic Pointers
Primary Constraint: PoT.md (Policy of Truth).
Surface Contract Spec: docs/ops/specs/surfaces/task.md.

### 2.1 Governance Pointers
Jurisdiction: PoT.md Section 3.
Git Authority: PoT.md Section 4.1.
Behavioral Standard: PoT.md Section 4.2.
Staffing Protocol: PoT.md Section 4.1.

### 2.2 Enforcement and Gates
Style: tools/lint/style.sh
Context: tools/lint/context.sh
Truth: tools/lint/truth.sh
DP lint: tools/lint/dp.sh (enforces DP transaction rules in Section 3; does not enforce TASK container schema)
TASK lint: tools/lint/task.sh (sole TASK surface/container schema enforcer)
Repo verify: tools/verify.sh
llms parity: tools/lint/llms.sh

## 3. Current Dispatch Packet (DP)

Section-by-section authoring protocol:
- Generate DP structure from `ops/src/surfaces/dp.md.tpl` using `ops/bin/draft`.
- Do not hand-edit DP structural boilerplate in `TASK.md` after generation.
- Manual edits are limited to slot content only (objective, scope, safety, plan body, receipt body).
- Freeze each completed subsection in order (3.1 -> 3.2 -> 3.3 -> 3.4.x).
- Do not revise earlier sections to justify later work. If scope changes, end the thread and draft a new DP.

Freeze Rule:
- If a completed DP subsection changes later, append an AMENDMENT block in RESULTS; otherwise DP is FAIL.

AMENDMENT: minimum fields
- Reopened section:
- Reason:
- Impact:
- Allowlist change (none or exact paths):

Input discipline (hard rule):
- Worker receives only the DP text. Worker does not require OPEN or DUMP attachments.
- DP writer must not attach or cite disposable artifacts (rough notes, chat logs, scratch drafts, throwaway excerpts).
- DP writer must not include pasted bundles (OPEN, DUMP payloads, manifests) inside the DP body.
- Canon pointers only. Evidence belongs in storage artifacts and RESULTS.

### DP-OPS-0075: Administrative Closeout and Prune Hardening

## 3.1 Freshness Gate (Must Pass Before Work)
Base Branch: main
Required Work Branch: work/dp-ops-0075-2026-02-19
Base HEAD: e4acd466
Freshness Stamp: 2026-02-19

Required local re-check (worker runs; paste outputs in RESULTS):
- git rev-parse --abbrev-ref HEAD
- git rev-parse --short HEAD
- git status --porcelain

Preconditions:
- No commits on main.
- Working tree must be clean before execution begins.
- If Base HEAD changes, regenerate session artifacts from canonical tools before proceeding.

STOP conditions:
- STOP if any mismatch (branch, Base HEAD, missing work branch).
- STOP if working tree is dirty before execution begins.
- STOP if told to create or switch branches.

## 3.1.1 DP Preflight Gate (Run Before Any Edits)
Purpose:
- Catch malformed DP or TASK schema before work begins.

Worker runs (paste outcome in RESULTS):
- bash tools/lint/dp.sh --test
- bash tools/lint/dp.sh TASK.md
- bash tools/lint/task.sh

STOP if any preflight check fails.

## 3.2 Required Context Load (Read Before Doing Anything)

### 3.2.1 Canon load order (always)
Worker must confirm loaded before edits begin:
1. PoT.md
2. SoP.md
3. PoW.md
4. TASK.md
5. docs/MAP.md
6. docs/MANUAL.md
7. ops/lib/manifests/CONTEXT.md

Notes:
- Worker does not read OPEN. OPEN is for Integrator state refresh and for receipts.
- Disposable artifacts must not be referenced or included.
- Worker input is the DP text only.
- DP writer must not attach or cite disposable artifacts.
- DP writer must not embed pasted bundles.

### 3.2.2 DP-scoped load order (per DP)
1. docs/ops/registry/BINARIES.md
2. docs/ops/registry/LINT.md
3. ops/bin/prune
4. docs/ops/specs/binaries/prune.md
5. tools/lint/integrity.sh
6. docs/ops/specs/tools/lint/integrity.md
7. storage/dp/active/allowlist.txt

## 3.3 Scope and Safety
Objective:
Administrative context: DP-OPS-0074 executed a premature prune invocation before `ops/bin/certify` generated the RESULTS receipt. This deleted `storage/handoff/DP-OPS-0074-RESULTS.md` and `storage/handoff/CLOSING-DP-OPS-0074.md` from the working tree before they were ever committed. Both paths remain declared in `storage/dp/active/allowlist.txt`. The working tree is currently clean at `e4acd466`. The integrity gate is not failing because `tools/lint/integrity.sh` only evaluates observed mutations against the allowlist, and the working tree has none. The dead allowlist entries are harmless at rest but must be formally retired before any future mutation surfaces them as a gate violation.

Formally administer the close of DP-OPS-0074, retire two dead allowlist entries, patch `ops/bin/prune` to prevent premature deletion of uncommitted handoff artifacts, and restore ledger equilibrium by updating `SoP.md` and `PoW.md`.

In scope:
- Edit `storage/dp/active/allowlist.txt`: remove the two dead entries (`storage/handoff/DP-OPS-0074-RESULTS.md` and `storage/handoff/CLOSING-DP-OPS-0074.md`) and append new archive surface entries as they are generated during the DP run.
- Edit `ops/bin/prune`: extend `ensure_results_artifacts_committed_and_clean` to also enumerate and guard `CLOSING-DP-OPS-*.md` files in addition to `*-RESULTS.md` files. Add a hard pre-deletion check in `cleanup_dp_target_artifacts` that fails with a SAFETY VIOLATION if any matching handoff file exists on disk but is not committed, regardless of whether the file was seeded by a prior run or not.
- Edit `SoP.md`: append one entry for DP-OPS-0074 (administrative close; RESULTS artifact lost to premature prune; no rework required) and one entry for DP-OPS-0075 (this DP).
- Edit `PoW.md`: append one entry for DP-OPS-0075.
- Update `TASK.md` pointer to the new archive surface generated by `ops/bin/open`.
- Add generated archive surfaces (SoP, PoW, TASK) to the allowlist as they are produced.

Out of scope:
- Any edits to `tools/lint/integrity.sh`. The current gate logic is not broken and does not require modification.
- Any edits to `ops/bin/certify`, `tools/lint/results.sh`, or any other certification tooling.
- Any changes under `projects/`, `docs/`, `opt/`, or `llms` bundles.
- Reconstruction or fabrication of the lost DP-OPS-0074 RESULTS artifact. The administrative close entry in SoP.md is the authoritative record of the loss.
- `ops/bin/prune` execution of any kind during this DP until the Operator commits the Closing Block.

Safety and invariants:
- No manual edits to generated outputs.
- No structural edits to TASK or DP container contracts.
- Allowlist hard gate: every tracked mutation must be in `storage/dp/active/allowlist.txt` before execution.
- Contractor does not commit, merge, rebase, cherry-pick, push, or switch branches.
- `ops/bin/prune` is forbidden during this DP until the Operator has committed the RESULTS artifact and the Mandatory Closing Block is confirmed populated.

Worker Constraints (SSOT injected):
## Section 1: Universal Template Rules
- Templates are authored as `.tpl` files and may begin with YAML frontmatter.
- Frontmatter keys are canonical and machine-read:
  - `template_type`
  - `template_id`
  - `template_version`
  - `requires_slots`
  - `includes`
- Rendered output must strip YAML frontmatter before writing output.
- Slot tokens are `\{\{TOKEN\}\}` where `TOKEN` is uppercase alphanumeric with underscores.
- Include directives are supported in template body content:
  - `\{\{@include:path\}\}`
  - `\{\{@include:path#section\}\}`
- Include resolution is deterministic and strict:
  - Missing files fail render.
  - Missing section anchors fail render.
  - Circular include graphs fail render.
- Strict mode is default for worker-facing output:
  - Every `requires_slots` token must be provided.
  - Unresolved `\{\{TOKEN\}\}` placeholders fail render.
- Non-strict mode is allowed only for linting and normalization workflows.
- Generated worker-facing surfaces must remain pointer-first and must not embed disposable artifacts.

Target Files allowlist (hard gate):
- storage/dp/active/allowlist.txt

## 3.4 Execution Plan (A-E)

### 3.4.1 State
- Active branch: `work/dp-ops-0075-2026-02-19`.
- Base HEAD: `e4acd466`.
- Working tree is clean at session open.

### 3.4.2 Request
- Retire the two dead allowlist entries left by the premature prune during DP-OPS-0074.
- Harden `ops/bin/prune` against the specific failure mode that caused the loss: deletion of uncommitted handoff artifacts (both RESULTS and CLOSING) before certification was complete.
- Restore ledger equilibrium: SoP.md and PoW.md must reflect the administrative close of DP-OPS-0074 and the execution of DP-OPS-0075.

### 3.4.3 Changelog

UPDATE:
- storage/dp/active/allowlist.txt
- ops/bin/prune
- SoP.md
- PoW.md
- TASK.md

NEW (generated; add to allowlist as produced):
- archives/surfaces/SoP-2026-02-19-{hash}.md
- archives/surfaces/PoW-2026-02-19-{hash}.md
- archives/surfaces/TASK-DP-OPS-0075-{hash}.md

### 3.4.4 Patch / Diff

A) Allowlist retirement.
   Remove exactly these two lines from `storage/dp/active/allowlist.txt`:
   - `storage/handoff/DP-OPS-0074-RESULTS.md`
   - `storage/handoff/CLOSING-DP-OPS-0074.md`
   Do not remove any other entries. Do not reorder remaining entries.

B) Prune hardening.
   In `ops/bin/prune`, make two targeted edits:

   Edit 1 — `ensure_results_artifacts_committed_and_clean`: The function currently enumerates only `*-RESULTS.md` files in `storage/handoff/`. Extend the candidate enumeration to also include `CLOSING-DP-OPS-*.md` files in `storage/handoff/`. If `--dp` is set, add `storage/handoff/CLOSING-${DP_TARGET}.md` to the candidates list alongside the RESULTS candidate. The committed-and-clean guard logic that follows already applies correctly to any candidate in the list; no changes to that logic are required.

   Edit 2 — `cleanup_dp_target_artifacts`: Before the `find` loop that deletes matching files, add a pre-deletion safety check. For each file in `storage/handoff/` that would be matched by the `*${DP_TARGET}*` pattern, if the file physically exists on disk and is not recorded as committed in git (`git ls-files --error-unmatch` returns non-zero), emit the RESULTS_GUARD_FATAL message and exit 1. This closes the gap where an uncommitted file passes the guard silently because the existing check in `ensure_results_artifacts_committed_and_clean` exits early when the file is absent.

C) SoP.md update.
   Prepend two new entries to SoP.md following the established ledger format.

   Entry 1 — DP-OPS-0074 administrative close:
   - Packet ID: DP-OPS-0074
   - Timestamp: 2026-02-19
   - Work Branch: work/dp-ops-0074-2026-02-18 (merged at e4acd466)
   - Base HEAD: (see PoW entry)
   - Scope: Certification infrastructure deployment (ops/bin/certify, tools/lint/integrity.sh, tools/lint/results.sh).
   - Outcome: ADMINISTRATIVE CLOSE. RESULTS artifact was deleted by a premature prune invocation before `ops/bin/certify` completed. Artifact was never committed. No rework is required; the deployed toolchain is confirmed functional at e4acd466. Loss documented in SoP. Prune hardening executed in DP-OPS-0075.
   - Notes: Equilibrium restored by DP-OPS-0075.

   Entry 2 — DP-OPS-0075:
   - Packet ID: DP-OPS-0075
   - Timestamp: 2026-02-19
   - Work Branch: work/dp-ops-0075-2026-02-19
   - Base HEAD: e4acd466
   - Scope: Administrative closeout of DP-OPS-0074; retirement of two dead allowlist entries; hardening of ops/bin/prune RESULTS and CLOSING guard; SoP and PoW equilibrium restoration.
   - Notes: No payload changes. Toolchain hardening only.

D) PoW.md update.
   Append one new entry to PoW.md for DP-OPS-0075 following established ledger format. Include pointers to the RESULTS artifact, the OPEN receipt, and the DUMP artifact generated by the closeout sequence.

### 3.4.5 Receipt (Proofs to collect) - MUST RUN
git rev-parse --abbrev-ref HEAD
git rev-parse --short HEAD
git status --porcelain
bash tools/lint/dp.sh --test
bash tools/lint/dp.sh TASK.md
bash tools/lint/task.sh
./ops/bin/open --out=auto --dp="DP-OPS-0075"
./ops/bin/dump --scope=platform --format=chatgpt --out=auto --bundle
./tools/verify.sh
./tools/lint/truth.sh
./tools/lint/style.sh
./tools/lint/context.sh
./tools/lint/integrity.sh
./tools/lint/llms.sh
git diff --name-only
git diff --stat
comm -23 <(git diff --name-only | sort) <(sort storage/dp/active/allowlist.txt) || true
comm -23 <(git ls-files --others --exclude-standard | sort) <(sort storage/dp/active/allowlist.txt) || true
Verify Section 3.5 Closing Block is populated in RESULTS.
Required pasted outputs: receipts, verification outcomes, and diff output.
Mandatory Closing Block required in RESULTS.

## 3.5 Closeout (Mandatory Routing)
- Execute docs/MANUAL.md Closeout Cycle in order (Verify, Harvest, Refresh, Log, Prune).
- Update SoP.md and PoW.md with DP entries, including objective summary and verification commands run.
- Protocol order for closeout: Verify -> Generate Results -> COMMIT (Operator Only) -> Prune.
- Run prune hygiene: ./ops/bin/prune --scrub.
- Ensure the next session begins with refreshed session artifacts and matching receipts.

### 3.5.1 Mandatory Closing Block
Primary Commit Header (plaintext)

Pull Request Title (plaintext)

Pull Request Description (markdown)

Final Squash Stub (plaintext) (Must differ from #1)

Extended Technical Manifest (plaintext)

Review Conversation Starter (markdown)
