---
trace_id: stela-20260322T173417Z-2ff34e53
packet_id: DP-OPS-0219
created_at: 2026-03-22T17:58:47Z
previous: archives/surfaces/TASK-DP-OPS-0218-41a1677b.md
---
# STELA TASK DASHBOARD
Status: ACTIVE
Owner: Integrator
Last Updated: 2026-03-19

## 1. Session State
Pointer: Session context output (generated by ops/bin/open)
Context Manifest: ops/lib/manifests/CONTEXT.md (checked by tools/lint/context.sh)

Session State contract:
- Session State is pointer-first. Do not write inline branch or hash state here.
- OPEN is the session state snapshot. OPEN-PORCELAIN is conditional dirty-state detail.
- RESULTS is the audit record for the DP.
- TASK is the stable routing surface.

## 2. Logic Pointers
Primary Constraint: PoT.md (Policy of Truth).
Surface Contract Spec: docs/ops/specs/surfaces/task.md.

### 2.1 Governance Pointers
Jurisdiction: PoT.md Section 3.
Git Authority: PoT.md Section 4.1.
Behavioral Standard: PoT.md Section 4.2.
Staffing Protocol: PoT.md Section 4.1.

### 2.2 Enforcement and Gates
Style: tools/lint/style.sh
Context: tools/lint/context.sh
Truth: tools/lint/truth.sh
DP lint: tools/lint/dp.sh (enforces DP transaction rules in Section 3; does not enforce TASK container schema)
TASK lint: tools/lint/task.sh (sole TASK surface/container schema enforcer)
Repo verify: tools/verify.sh
llms parity: tools/lint/llms.sh

## 3. Current Dispatch Packet (DP)

Section-by-section authoring protocol:
- Generate DP structure from `ops/src/surfaces/dp.md.tpl` using `ops/bin/draft`.
- Do not hand-edit DP structural boilerplate in `TASK.md` after generation.
- Manual edits are limited to slot content only (objective, scope, safety, plan body, receipt body).
- Freeze each completed subsection in order (3.1 -> 3.2 -> 3.3 -> 3.4.x).
- Do not revise earlier sections to justify later work. If scope changes, end the thread and draft a new DP.

Freeze Rule:
- If a completed DP subsection changes later, append an AMENDMENT block in RESULTS; otherwise DP is FAIL.

AMENDMENT: minimum fields
- Reopened section:
- Reason:
- Impact:
- Allowlist change (none or exact paths):

Input discipline (hard rule):
- Worker receives only the DP text. Worker does not require OPEN or DUMP attachments.
- DP writer must not attach or cite disposable artifacts (rough notes, chat logs, scratch drafts, throwaway excerpts).
- DP writer must not include pasted bundles (OPEN, DUMP payloads, manifests) inside the DP body.
- Canon pointers only. Evidence belongs in storage artifacts and RESULTS.



### DP-OPS-0219: Guided Analyst Planning Upgrade

## 3.1 Freshness Gate (Must Pass Before Work)
Base Branch: main
Required Work Branch: work/dp-ops-0219-analyst-planning-upgrade-2026-03-22
Base HEAD: 1649d4e0
Freshness Stamp: 2026-03-22
Note: FRESHNESS_STAMP must be YYYY-MM-DD format only. No trace tokens, no timestamps, no other text. Certify rejects all other forms. dp.sh enforces at lint time.

Required local re-check (worker runs; paste outputs in RESULTS):
- git rev-parse --abbrev-ref HEAD
- git rev-parse --short HEAD
- git status --porcelain

Preconditions:
- No commits on main.
- Working tree must be clean before execution begins.
- If Base HEAD changes, regenerate session artifacts from canonical tools before proceeding.

STOP conditions:
- STOP if any mismatch (branch, Base HEAD, missing work branch).
- STOP if working tree is dirty before execution begins.
- STOP if told to create or switch branches.

## 3.1.1 DP Preflight Gate (Run Before Any Edits)
Purpose:
- Catch malformed DP or TASK schema before work begins.

Worker runs (paste outcome in RESULTS):
- bash tools/lint/dp.sh --test
- bash tools/lint/dp.sh TASK.md
- bash tools/lint/task.sh

STOP if any preflight check fails.

### CbC Design Discipline Preflight (TASK.md §3.1.1)
Required when the DP objective adds, modifies, or replaces a linter, script, guard, or validation binary.
For non-tooling DPs: state "Not applicable" with a one-line justification.
Applicable. This packet modifies `tools/lint/response.sh` (a validation binary) and `tools/lint/style.sh` (a validation binary).

Invariants:
1. `check_analyst_body_scope` must continue to reject audit markers, contractor narrative, policy overcompensation prose, and decision fields after edits.
2. `check_analyst_mode_contract` must continue to freeze the analyst stance contract; freeze strings are replaced to match new stance text, not removed.
3. Analyst stance must remain within the existing `TOPIC.md -> PLAN.md` workflow loop; no new state surfaces are introduced.

## 3.2 Required Context Load (Read Before Doing Anything)

### 3.2.1 Canon load order (always)
Worker must confirm loaded before edits begin:
1. PoT.md
2. SoP.md
3. PoW.md
4. TASK.md
5. docs/MAP.md
6. docs/MANUAL.md
7. ops/lib/manifests/CONTEXT.md

Notes:
- Worker does not read OPEN. OPEN is for Integrator state refresh and for receipts.
- Disposable artifacts must not be referenced or included.
- Worker input is the DP text only.
- DP writer must not attach or cite disposable artifacts.
- DP writer must not embed pasted bundles.

## Rules

1 Temp path rule uses `var/tmp` only
2 RESULTS generation rule uses `ops/bin/certify` only
3 Receipt rule runs every DP mandated command and treats any skip as STOP
4 Disposable artifact rule treats OPEN dump bundles and manifests as non canonical session artifacts
5 Use repo-relative paths only in all output. Never print absolute filesystem paths. Do not emit clickable file links.

### 3.2.2 DP-scoped load order (per DP)
Worker must confirm loaded before edits begin:
8. ops/src/stances/analyst.md.tpl
9. ops/src/shared/stances.json
10. docs/ops/specs/stances/analyst.md
11. tools/lint/response.sh (lines 209–280 and 827–900 minimum)
12. tools/lint/style.sh (lines 345–419 minimum: check_analyst_mode_contract)

## 3.3 Scope and Safety
Objective:
Upgrade the analyst stance to a guided planning routine: add an explicit explore-first step, a material-ambiguity threshold that absorbs low-impact gaps as assumptions, and a structured optioned question mode (max 3 questions, exactly 3 options each, exactly one `(Recommended)` per question). Update `tools/lint/response.sh` and `tools/lint/style.sh` to validate and freeze the new contract; update the analyst spec to describe it.

In scope:
- ops/src/stances/analyst.md.tpl: add explore-first rule, material-ambiguity threshold, and structured question-mode contract with canonical format skeleton and lint-pattern line
- docs/ops/specs/stances/analyst.md: update Outputs and Invariants sections to reflect explore-first, material-ambiguity threshold, and structured question-mode
- tools/lint/response.sh: upgrade check_analyst_body_scope to validate structured question-mode when Q1./Q2./Q3. questions are present (max 3, exactly 3 options per question, exactly one `(Recommended)` per question); update existing valid conversational smoke fixture to use new structured format; add failing smoke fixtures for over-count questions, under-count options, and missing Recommended
- tools/lint/style.sh: replace check_analyst_mode_contract freeze strings to match new analyst.md.tpl text; full replacement job — old literals will not exist after the stance edit
- docs/MANUAL.md: locate analyst-facing guidance sections; update any wording that contradicts the new guided-planning contract; note findings in RESULTS
- storage/dp/active/allowlist.txt: add CLOSING and TASK archive entries for this DP

Out of scope:
- ops/src/shared/stances.json shared rule blocks: do not modify
- ops/src/surfaces/dp.md.tpl: no changes to DP template structure
- Any stance templates other than analyst.md.tpl (architect, auditor, foreman, conformist)
- tools/lint/plan.sh: confirm-only; plan.sh is called only for non-conversational analyst bodies in response.sh and requires no code change
- Bundle scripts, certify, or other binaries not named in scope
- TASK.md, SoP.md, PoW.md: no structural edits
- No new analyst state surfaces; improvement stays inside the existing TOPIC.md -> PLAN.md loop

Safety and invariants:
- Do not weaken existing scope-boundary checks in check_analyst_body_scope; audit markers, contractor narrative, policy overcompensation prose, and decision fields must continue to fail lint
- Do not remove the `Questions / Conversation:` footer requirement; it remains required in question mode
- New structured question-mode validation must not cause valid final-plan analyst output to fail
- Allowlist is the hard gate: edits must remain within listed paths
- No new analyst state surfaces are introduced

Worker Constraints (SSOT injected):
## Section 1: Universal Template Rules
- Template source uses `tpl` files with optional YAML frontmatter
- Canon frontmatter keys: `template_type` `template_id` `template_version` `requires_slots` `includes`
- Renderer strips frontmatter before output write
- Slot token form `\{\{TOKEN\}\}` with uppercase alphanumeric underscore
- Include forms `\{\{@include:path\}\}` and `\{\{@include:path#section\}\}`
- Include resolution is strict: missing file fail missing section fail circular graph fail
- Strict mode default: every required slot value present and no unresolved token
- Non strict mode allowed only for lint and normalization workflows
- Worker facing generated surfaces remain pointer first and exclude disposable artifacts

Target Files allowlist (hard gate):
- storage/dp/active/allowlist.txt

## 3.4 Execution Plan (A-E)

### 3.4.1 State
ops/src/stances/analyst.md.tpl currently has four steps: (0) PRECONDITIONS, (1) read topic against bundle context only, (2) decide whether intent is settled — if material ambiguity remains stay conversational; if settled emit PLAN.md, (3) conversational mode: `1. Analysis and Discussion` first line, `Questions / Conversation:` footer, (4) final plan mode. No explore-first rule. Step 2 mentions "material ambiguity" but provides no threshold for distinguishing material from immaterial gaps. Step 3 specifies no question count limit, no option count, and no Recommended marker. The inline rules block contains conversational-mode lines that will require replacement when the stance text changes.

tools/lint/response.sh check_analyst_body_scope (lines 209–280): conversational mode detected by `1. Analysis and Discussion` first line. In conversational mode, only `^Questions / Conversation:` footer is validated (line 269); no question-count, option-count, or Recommended-marker checks exist. Analyst smoke block (lines 827–885): one valid plan fixture, one valid conversational fixture using an unstructured bullet question, one audit-marker fail, one contractor-narrative fail, one policy-overcompensation fail. No structured question-mode smoke fixtures.

tools/lint/style.sh check_analyst_mode_contract (lines 345–419): 13 literal grep-Fq freeze checks against current analyst.md.tpl text including `required_discussion_default`, `required_questions`, `required_weak_topic`, `required_plan_direct`, and others. All will become stale after the stance edit; this is a full replacement job.

docs/ops/specs/stances/analyst.md Outputs section: describes conversational and final-plan modes but does not mention explore-first, material-ambiguity threshold, or structured question format. Invariants section: three existing entries; no question-structure invariant.

### 3.4.2 Request
1. In ops/src/stances/analyst.md.tpl, Steps section — three edits:

   Edit 1a — rewrite Step 1 for explore-first:
     Old: "1. Read the attached topic against attached bundle context only."
     New: "1. Read the attached topic and all directly attached bundle evidence before reaching any conclusion or asking any question."

   Edit 1b — rewrite Step 2 for material-ambiguity threshold:
     Old:
       "2. Decide whether intent is settled enough for a final plan:
          * If material ambiguity remains, stay conversational and ask concise questions inside one fenced markdown block.
          * If intent is settled, output only a complete `PLAN.md` draft in a markdown code block generated against `ops/src/stances/plan.md.tpl`."
     New:
       "2. Apply the material-ambiguity threshold:
          * If a gap is low-impact, choose the smallest justified assumption and proceed without asking.
          * If a gap would materially change the plan, stay in question mode.
          * If intent is fully settled, emit only a complete `PLAN.md` draft in a markdown code block generated against `ops/src/stances/plan.md.tpl`."

   Edit 1c — rewrite Step 3 for structured question-mode:
     Old:
       "3. In conversational planning mode, output one fenced markdown block:
          * First non-empty line: `1. Analysis and Discussion`
          * End with `Questions / Conversation:` when clarification, tradeoff choice, or confirmation would help."
     New:
       "3. In conversational planning mode, output one fenced markdown block:
          * First non-empty line: `1. Analysis and Discussion`
          * Add a `2. Decision Questions` section when questions are needed.
          * Allow at most 3 questions. Each question must present exactly 3 meaningful, mutually exclusive options. Mark exactly one option `(Recommended)`. If 3 real options do not exist, make the smallest justified assumption instead of asking.
          * Include a concise operator response format after the options such as `Q1:A, Q2:C` or `Use recommended options`.
          * End with `Questions / Conversation:`."

2. In ops/src/stances/analyst.md.tpl, after the Steps section and before the inline rules block, insert the canonical question-mode format block and lint-pattern line exactly as they appear in the governing plan (Key Changes §1, beginning at "Canonical question-mode format...").

3. In ops/src/stances/analyst.md.tpl, inline rules block — locate the line:
     `For conversational planning mode: end with \`Questions / Conversation:\` and short operator-facing prompts when clarification, tradeoff choice, or confirmation would help.`
   Replace it with a line that encodes the structured question-mode requirements consistent with the updated Step 3 (structured `2. Decision Questions` section, max 3 questions, 3 options, one Recommended, operator response format, `Questions / Conversation:` footer). Worker chooses exact wording; note the specific replacement in RESULTS so it can be used as the updated style.sh freeze string.

4. In tools/lint/response.sh, check_analyst_body_scope conversational mode block (lines 268–274): after the existing `Questions / Conversation:` footer check and before `return 0`, add three structured validation checks that fire when Q1./Q2./Q3. questions are present:
   - Count lines matching `^Q[1-3]\.[[:space:]]` in the body; if count > 3: fail "analyst conversational body must contain at most 3 questions".
   - If count > 0: for each question number N (1, 2, 3) found, identify its option lines (`^- [ABC]\.[[:space:]]` lines belonging to that question's block, up to the next `^Q` line or `^Questions / Conversation:`); if the count of option lines for any question is not exactly 3: fail "each analyst question must have exactly 3 options".
   - For each question's 3 option lines, count occurrences of the literal string `(Recommended)`; if the count is not exactly 1: fail "each analyst question must have exactly one (Recommended) option".

5. In tools/lint/response.sh, analyst smoke block: update the existing valid conversational fixture (EOF_ANALYST_CONVERSATION) to use the new structured question-mode format: add a `2. Decision Questions` section header, one Q1. question with three A./B./C. options including one `(Recommended)`, an operator response format line, and the `Questions / Conversation:` footer. Confirm it still passes lint. Then append four new fixtures after the existing analyst fixtures:
   - Fixture A — PASS: structured question-mode with 2 questions (each 3 options, one Recommended), `Questions / Conversation:` footer.
   - Fixture B — FAIL: a question with only 2 options (no Recommended). Expected to fail lint.
   - Fixture C — FAIL: a question with 3 options but no `(Recommended)` marker. Expected to fail lint.
   - Fixture D — FAIL: 4 questions. Expected to fail lint.
   Run bash tools/lint/response.sh --test after adding fixtures; paste output in RESULTS.

6. In tools/lint/style.sh, check_analyst_mode_contract (lines 345–419): read the full function. For each freeze variable, compare its current string value verbatim against the updated analyst.md.tpl. Replace every variable value that no longer exists verbatim in the file with the exact matching new line from the updated stance. Worker reads the updated analyst.md.tpl first, then makes replacements. This is a full replacement job; no freeze check is removed.

7. In docs/ops/specs/stances/analyst.md, Outputs section: after the existing conversational-mode lines, add:
   - "- Analyst reads topic and all directly attached bundle evidence before asking anything (explore-first)."
   - "- Material-ambiguity threshold: low-impact gaps are absorbed as smallest justified assumptions; only gaps that would materially change the plan trigger question mode."
   - "- Structured question mode: at most 3 questions per run; each question presents exactly 3 meaningful, mutually exclusive options; exactly one option per question is marked `(Recommended)`."
   In Invariants section, add after existing entries:
   - "- Analyst does not emit structureless questions; when question mode is used, the Q1./Q2./Q3. format with A./B./C. options and one `(Recommended)` per question is required."

8. In docs/MANUAL.md: read the file. Locate any sections describing analyst behavior or the TOPIC.md -> PLAN.md loop. Update any wording that contradicts explore-first, material-ambiguity threshold, or structured question mode. If no analyst guidance sections are found, note "no analyst guidance sections found in MANUAL.md, no change" in RESULTS.

9. Confirm tools/lint/plan.sh: read response.sh around line 276 to confirm plan.sh is only called for non-conversational analyst bodies. Note "plan.sh: confirm-only, no code change" in RESULTS.

10. In storage/dp/active/allowlist.txt, append at end of file:
    storage/handoff/CLOSING-DP-OPS-0219.md
    archives/surfaces/TASK-DP-OPS-0219-*.md

11. Run receipt commands and paste outputs in RESULTS.

### 3.4.3 Changelog
UPDATE ops/src/stances/analyst.md.tpl — rewrite Steps 1–3 for explore-first, material-ambiguity threshold, and structured question-mode; add canonical format block and lint-pattern line; replace stale conversational inline rule
UPDATE tools/lint/response.sh — add structured question-mode validation to check_analyst_body_scope; update existing valid conversational smoke fixture to structured format; add four new smoke fixtures (1 pass, 3 fail)
UPDATE tools/lint/style.sh — replace all stale check_analyst_mode_contract freeze strings to match updated analyst.md.tpl text
UPDATE docs/ops/specs/stances/analyst.md — add explore-first, material-ambiguity threshold, and structured question-mode to Outputs; add question-structure invariant to Invariants
CHECK docs/MANUAL.md — locate analyst guidance; update if contradicts new contract; note findings in RESULTS
UPDATE storage/dp/active/allowlist.txt — add CLOSING and TASK archive entries for DP-OPS-0219

### 3.4.4 Patch / Diff
A. Read ops/src/stances/analyst.md.tpl in full.

B. Apply edits 1a, 1b, 1c from §3.4.2 Request to the Steps section.

C. After the Steps section and before the inline rules block, insert the canonical question-mode format block and lint-pattern line as specified in §3.4.2 Request item 2.

D. In the inline rules block, apply the replacement described in §3.4.2 Request item 3. Record the exact new line text in RESULTS for use as the updated style.sh freeze string.

E. Read the full updated analyst.md.tpl and note which existing inline rule lines remain unchanged (these will still need freeze-string matches in style.sh).

F. Read tools/lint/response.sh lines 209–280 in full.

G. Apply the structured question-mode validation additions to check_analyst_body_scope as described in §3.4.2 Request item 4.

H. Read tools/lint/response.sh lines 827–900 (current end of analyst smoke block) in full.

I. Update the existing valid conversational fixture (EOF_ANALYST_CONVERSATION) and append the four new fixtures as described in §3.4.2 Request item 5.

J. Run bash tools/lint/response.sh --test. Paste output in RESULTS. STOP if test fails.

K. Read tools/lint/style.sh lines 345–419 in full.

L. Replace stale freeze variable values in check_analyst_mode_contract as described in §3.4.2 Request item 6. Run bash tools/lint/style.sh after replacements to confirm no new failures.

M. Read docs/ops/specs/stances/analyst.md in full.

N. Apply Outputs and Invariants additions described in §3.4.2 Request item 7.

O. Read docs/MANUAL.md. Apply updates as described in §3.4.2 Request item 8.

P. Confirm tools/lint/plan.sh call scope per §3.4.2 Request item 9. Note in RESULTS.

Q. In storage/dp/active/allowlist.txt, append:
   storage/handoff/CLOSING-DP-OPS-0219.md
   archives/surfaces/TASK-DP-OPS-0219-*.md

R. Run receipt commands and paste outputs in RESULTS.

### 3.4.5 Receipt (Proofs to collect) - MUST RUN

**Mandatory receipt commands (always run; do not omit):**
- bash tools/lint/dp.sh TASK.md
- bash tools/lint/task.sh
- bash tools/lint/integrity.sh
- bash tools/lint/style.sh
- ./ops/bin/llms
- bash tools/lint/integrity.sh
- git diff --name-only
- git diff --stat
- comm -23 <(git diff --name-only | sort) <(sort storage/dp/active/allowlist.txt) || true
- comm -23 <(git ls-files --others --exclude-standard | sort) <(sort storage/dp/active/allowlist.txt) || true
- ./ops/bin/open

**DP-specific receipt commands (scope-specific; author below):**
Note: Command substitution forms (e.g., $(pwd), $(git ...)) are rejected by certify replay. Use literal values only. dp.sh enforces at lint time.
- bash tools/lint/response.sh --test

## 3.5 Closeout (Mandatory Routing)
- Execute docs/MANUAL.md Closeout Cycle in order (Verify, Harvest, Refresh, Log, Prune).
- Update SoP.md and PoW.md with DP entries, including objective summary and current proof / receipt summary.
- Protocol order for closeout: Verify -> Generate Results -> COMMIT (Operator Only) -> Prune.
- Run prune hygiene: ./ops/bin/prune --scrub.
- Refresh llms artifacts: `./ops/bin/llms`
- Regenerate session artifacts: `./ops/bin/open --out=auto`
- Ensure the next session begins with refreshed session artifacts and matching receipts.
- Note: audit dump generation is owned by `./ops/bin/bundle --profile=audit --out=auto` and is separate from operator session refresh. Do not stamp an additional `ops/bin/dump --scope=core` into closeout unless the DP explicitly authorizes a standalone state capture.
- Refresh side-effect: `ops/bin/llms` regenerates `ops/lib/manifests/OPS.md` as a compile event; if `OPS.md` is not in the allowlist, restore it before running `integrity.sh`. See `docs/MANUAL.md` Refresh side-effect notice for the full procedure.

### 3.5.1 Mandatory Closing Sidecar
Closing sidecar content is generated and maintained at `storage/handoff/CLOSING.md` and is validated by `ops/bin/certify` as a hard gate at certification time. Do not author, predict, populate, or approximate any sidecar-derived closeout output outside this sidecar.
Certify separately collects contractor-authored narrative for the RESULTS Contractor Execution Narrative section at certify time via interactive editor prompt; this narrative input is independent from closing sidecar content.

Before running certify, confirm `storage/handoff/CLOSING.md` has been maintained throughout execution and reflects observable reality only.

Closing sidecar fields required at certify time:
- Commit Message
- Create Pull Request (Title)
- Create Pull Request (Description)
- Confirm Merge (Commit Message)
- Confirm Merge (Extended Description)
- Confirm Merge (Add a Comment)
