---
trace_id: stela-20260223T154153Z-2aa11788
packet_id: DP-OPS-0105
created_at: 2026-02-23T16:35:08Z
previous: (none)
---
# STELA TASK DASHBOARD
Status: ACTIVE
Owner: Integrator
Last Updated: 2026-02-18

## 1. Session State
Pointer: Session context output (generated by ops/bin/open)
Context Manifest: ops/lib/manifests/CONTEXT.md (checked by tools/lint/context.sh)

Session State contract:
- Session State is pointer-first. Do not write inline branch or hash state here.
- OPEN and OPEN-PORCELAIN are the session state snapshot.
- OPEN-PORCELAIN is emitted on every OPEN run. Clean sessions produce a zero-line artifact.
- RESULTS is the audit record for the DP.
- TASK is the stable routing surface.
- Operator note (2026-02-17): Do not edit `SoP.md` or `PoW.md` unless explicitly instructed.

## 2. Logic Pointers
Primary Constraint: PoT.md (Policy of Truth).
Surface Contract Spec: docs/ops/specs/surfaces/task.md.

### 2.1 Governance Pointers
Jurisdiction: PoT.md Section 3.
Git Authority: PoT.md Section 4.1.
Behavioral Standard: PoT.md Section 4.2.
Staffing Protocol: PoT.md Section 4.1.

### 2.2 Enforcement and Gates
Style: tools/lint/style.sh
Context: tools/lint/context.sh
Truth: tools/lint/truth.sh
DP lint: tools/lint/dp.sh (enforces DP transaction rules in Section 3; does not enforce TASK container schema)
TASK lint: tools/lint/task.sh (sole TASK surface/container schema enforcer)
Repo verify: tools/verify.sh
llms parity: tools/lint/llms.sh

## 3. Current Dispatch Packet (DP)

Section-by-section authoring protocol:
- Generate DP structure from `ops/src/surfaces/dp.md.tpl` using `ops/bin/draft`.
- Do not hand-edit DP structural boilerplate in `TASK.md` after generation.
- Manual edits are limited to slot content only (objective, scope, safety, plan body, receipt body).
- Freeze each completed subsection in order (3.1 -> 3.2 -> 3.3 -> 3.4.x).
- Do not revise earlier sections to justify later work. If scope changes, end the thread and draft a new DP.

Freeze Rule:
- If a completed DP subsection changes later, append an AMENDMENT block in RESULTS; otherwise DP is FAIL.

AMENDMENT: minimum fields
- Reopened section:
- Reason:
- Impact:
- Allowlist change (none or exact paths):

Input discipline (hard rule):
- Worker receives only the DP text. Worker does not require OPEN or DUMP attachments.
- DP writer must not attach or cite disposable artifacts (rough notes, chat logs, scratch drafts, throwaway excerpts).
- DP writer must not include pasted bundles (OPEN, DUMP payloads, manifests) inside the DP body.
- Canon pointers only. Evidence belongs in storage artifacts and RESULTS.

### DP-OPS-0105: Addendum Binary, Certify Compatibility Alignment, and Closing Block Format Canonization

## 3.1 Freshness Gate (Must Pass Before Work)
Base Branch: main
Required Work Branch: work/addendum-binary-certify-compat-2026-02-23
Base HEAD: 6f54f8789
Freshness Stamp: 2026-02-23
Note: FRESHNESS_STAMP must be YYYY-MM-DD format only. No trace tokens, no timestamps, no other text. Certify rejects all other forms. dp.sh enforces at lint time.

Required local re-check (worker runs; paste outputs in RESULTS):
- git rev-parse --abbrev-ref HEAD
- git rev-parse --short HEAD
- git status --porcelain

Preconditions:
- No commits on main.
- Working tree must be clean before execution begins.
- If Base HEAD changes, regenerate session artifacts from canonical tools before proceeding.

STOP conditions:
- STOP if any mismatch (branch, Base HEAD, missing work branch).
- STOP if working tree is dirty before execution begins.
- STOP if told to create or switch branches.

## 3.1.1 DP Preflight Gate (Run Before Any Edits)
Purpose:
- Catch malformed DP or TASK schema before work begins.

Worker runs (paste outcome in RESULTS):
- bash tools/lint/dp.sh --test
- bash tools/lint/dp.sh TASK.md
- bash tools/lint/task.sh

STOP if any preflight check fails.

### CbC Design Discipline Preflight (TASK.md §3.1.1)
Required when the DP objective adds, modifies, or replaces a linter, script, guard, or validation binary.
For non-tooling DPs: state "Not applicable" with a one-line justification.
Required when the DP objective adds, modifies, or replaces a linter, script, guard, or validation binary.
For non-tooling DPs: state "Not applicable" with a one-line justification.

This DP adds one generation binary (`ops/bin/addendum`) and two detection checks in `tools/lint/dp.sh`. Two separate CbC entries are required.

**Entry 1: `ops/bin/addendum` (generation binary)**

1. What problem does this tool detect or prevent? Without it, the Integrator/auditor must hand-author addendum artifacts with no template scaffolding. There is no structural guarantee that a hand-authored addendum matches `ops/src/surfaces/addendum.md.tpl`. The inverted-role problem (Contractors authoring addendum content) has no enforcement point.

2. Can the problem be prevented structurally instead? Yes. The binary IS the structural enforcement. Addendum content is generated from the canonical template via `ops/bin/template render addendum`; manual composition is bypassed entirely.

3. Deletion Test score: C. Without the binary, addenda are hand-authored without template scaffolding, which perpetuates the inverted-role process. Deletion would regress to the pre-Phase-A state for addendum production. The binary cannot score A (it solves a real problem) or D (it eliminates manual composition).

4. Complexity Budget (cost-to-payoff ratio): Approximately 60-80 lines. Payoff: role clarity enforcement, template integrity on every addendum, and a clean auditor authority chain. Ratio: highly favorable.

5. Structural prevention mechanism: Template-based generation. The binary resolves the addendum template path, validates required slot population before writing, and writes output to `storage/dp/intake/` only. No manual content path exists after the binary is in place.

**Entry 2: `tools/lint/dp.sh` additions — Freshness Stamp format check and receipt command substitution check**

1. What problem does this tool detect or prevent? Three consecutive DPs (0101, 0102, 0103) required mid-closeout certify compatibility remediations for the same root cause: DP writers used trace-style Freshness Stamps and command substitution forms in receipt commands. Neither constraint was enforced at authoring time. The problem surfaces only when certify rejects the intake packet during closeout.

2. Can the problem be prevented structurally instead? No. A DP writer can always type a trace token or `$(pwd)` into the DP body. The template comment additions (see §3.4.4) make the constraints visible, but only detection at lint time can halt a non-compliant packet before it reaches certify.

3. Deletion Test score: C. Without the checks, certify compatibility errors surface only at closeout replay, requiring operator-authorized in-session remediation as has happened repeatedly. Detection at lint time is the correct and proportionate response.

4. Complexity Budget: Approximately 25 lines total for both checks. Payoff: elimination of a recurring certify compatibility stop that has required remediation in three consecutive DPs. Ratio: highly favorable.

5. Detection scope: Narrow and explicit. `check_freshness_stamp_format` validates the extracted Freshness Stamp value against `^[0-9]{4}-[0-9]{2}-[0-9]{2}$`. `check_receipt_command_substitution` scans Section 3.4.5 receipt commands for `$(` tokens and fails on any match.


## 3.2 Required Context Load (Read Before Doing Anything)

### 3.2.1 Canon load order (always)
Worker must confirm loaded before edits begin:
1. PoT.md
2. SoP.md
3. PoW.md
4. TASK.md
5. docs/MAP.md
6. docs/MANUAL.md
7. ops/lib/manifests/CONTEXT.md

Notes:
- Worker does not read OPEN. OPEN is for Integrator state refresh and for receipts.
- Disposable artifacts must not be referenced or included.
- Worker input is the DP text only.
- DP writer must not attach or cite disposable artifacts.
- DP writer must not embed pasted bundles.

<!-- CCD: ff_target="machine-dense" ff_band="10-20" -->
# Contractor Brief

1 Temp path rule uses `var/tmp` only
2 RESULTS generation rule uses `ops/bin/certify` only
3 Receipt rule runs every DP mandated command and treats any skip as STOP
4 Disposable artifact rule treats OPEN dump bundles and manifests as non canonical session artifacts
5 Path rule uses existing repo paths or explicit `NEW` changelog markers and forbids globs and brace expansion

### 3.2.2 DP-scoped load order (per DP)
- ops/src/surfaces/addendum.md.tpl (full file; addendum binary implementation target)
- ops/src/surfaces/dp.md.tpl (full file; certify-compat comment targets and §3.5.1 format update)
- tools/lint/dp.sh (full file; new checks must integrate with existing lint_payload dispatch)
- ops/bin/draft (full file; addendum binary must follow this implementation pattern)
- docs/MANUAL.md (DP Mechanics section; certify-compat authoring rules insertion point)
- docs/ops/specs/tools/lint/dp.md (spec update target for new checks)
- docs/ops/registry/binaries.md (registry entry target)

## 3.3 Scope and Safety
Objective:
Ship `ops/bin/addendum` (generation binary for Integrator-issued addendum artifacts), add Freshness Stamp format and receipt command substitution detection checks to `tools/lint/dp.sh`, add certify-compatibility annotations to `ops/src/surfaces/dp.md.tpl` (FRESHNESS_STAMP slot, RECEIPT_EXTRA slot, and §3.5.1 format canonization), and update all affected documentation and registry surfaces.

In scope:
- ops/bin/addendum (NEW binary; follows ops/bin/draft pattern; reads slots file; renders addendum via ops/bin/template; writes to storage/dp/intake/)
- docs/ops/specs/binaries/addendum.md (NEW spec for ops/bin/addendum)
- docs/ops/registry/binaries.md (UPDATE; add OPS-BIN-17 entry for ops/bin/addendum)
- ops/src/surfaces/dp.md.tpl (UPDATE; three changes: FRESHNESS_STAMP certify-compat annotation, RECEIPT_EXTRA certify-compat annotation, §3.5.1 Mandatory Closing Block list-format canonization per operator directive)
- tools/lint/dp.sh (UPDATE; add check_freshness_stamp_format and check_receipt_command_substitution functions; wire both into lint_payload; update CANONICAL_DP_TEMPLATE_SHA256 constant to match post-edit dp.md.tpl hash)
- docs/ops/specs/tools/lint/dp.md (UPDATE; document the two new checks)
- docs/MANUAL.md (UPDATE; add certify-compatibility authoring rules to DP Mechanics section)
- storage/dp/active/allowlist.txt (UPDATE; DP-OPS-0105 target set)
- storage/handoff/CLOSING-DP-OPS-0105.md (maintained during execution; sidecar)
- Closeout-generated surfaces (llms bundles, compile manifest, SoP leaf, PoW leaf, TASK archive leaf)

Out of scope:
- ops/bin/certify (no changes to the replay engine; only the authoring surface changes)
- ops/src/surfaces/results.md.tpl (closing block parsing logic in dp.sh validators is unchanged; only the dp.md.tpl instruction format changes)
- ops/src/surfaces/closing.md.tpl (sidecar template format unchanged; parenthetical labels remain required in sidecar output)
- tools/lint/style.sh (no changes to closing block label pattern validation)
- Any file not listed in scope above
- Phase C dump discipline documentation (DP-OPS-0106 target)
- Phase D and Phase E items

Safety and invariants:
- No manual edits to generated outputs (RESULTS, llms bundles, compile manifests).
- No structural edits to TASK.md or DP boilerplate.
- Allowlist hard gate: every changed or created file must appear in storage/dp/active/allowlist.txt before any edits begin.
- CANONICAL_DP_TEMPLATE_SHA256 in tools/lint/dp.sh must be computed from the actual edited dp.md.tpl after all dp.md.tpl edits are complete. Do not pre-fill or estimate. This is a hard STOP if skipped.
- ops/bin/addendum must not write to any path outside storage/dp/intake/.
- The two new dp.sh checks must be added to lint_payload() dispatch in the same commit as the function definitions; partial activation is a system failure state.
- §3.5.1 format change in dp.md.tpl changes the normalized structure hash. The CANONICAL_DP_TEMPLATE_SHA256 update must account for all three dp.md.tpl changes in one computation, not incremental updates.

Worker Constraints (SSOT injected):
## Section 1: Universal Template Rules
- Template source uses `tpl` files with optional YAML frontmatter
- Canon frontmatter keys: `template_type` `template_id` `template_version` `requires_slots` `includes`
- Renderer strips frontmatter before output write
- Slot token form `\{\{TOKEN\}\}` with uppercase alphanumeric underscore
- Include forms `\{\{@include:path\}\}` and `\{\{@include:path#section\}\}`
- Include resolution is strict: missing file fail missing section fail circular graph fail
- Strict mode default: every required slot value present and no unresolved token
- Non strict mode allowed only for lint and normalization workflows
- Worker facing generated surfaces remain pointer first and exclude disposable artifacts

Target Files allowlist (hard gate):
- storage/dp/active/allowlist.txt

## 3.4 Execution Plan (A-E)

### 3.4.1 State
HEAD at session open: 6f54f8789. Branch: main. Working tree: clean. DP-OPS-0104 is closed and merged; decision architecture, auditor authority protocol, inaugural decision record, and MANUAL Post-Work Audit step are committed. Phase A schema is in place at `archives/decisions/`. `ops/src/surfaces/addendum.md.tpl` exists and is validated by `dp.sh` at hash `42cb7586c6ed103e995730f1a8c34a1c7e0676b717c27dfb987950feeac7ec9e`. `ops/bin/draft` is the reference implementation pattern for `ops/bin/addendum`. `tools/lint/dp.sh` has no Freshness Stamp format check and no receipt command substitution check. `ops/src/surfaces/dp.md.tpl` §3.5.1 uses a bare-label format with parenthetical type annotations; operator directive requires list-format canonization. `docs/ops/registry/binaries.md` has OPS-BIN-01 through OPS-BIN-16; next entry is OPS-BIN-17. `docs/ops/specs/binaries/` has no `addendum.md`.

### 3.4.2 Request
The worker must produce the following:

1. `ops/bin/addendum` — new binary that renders an addendum artifact from `ops/src/surfaces/addendum.md.tpl`. Must follow the `ops/bin/draft` implementation pattern exactly: read a slots file from the argument `--slots-file=<path>`, resolve the addendum template path via `ops/bin/template`, validate that required slots (BASE_DP_ID, ADDENDUM_ID, OPERATOR_AUTHORIZATION, SCOPE_DELTA, ADDENDUM_OBJECTIVE, ADDENDUM_RECEIPT) are populated and non-placeholder, assign the next available addendum letter by scanning `storage/dp/intake/` for existing addenda for the base DP, emit a preview to stdout, and write the final artifact to `storage/dp/intake/<BASE_DP_ID>-ADDENDUM-<LETTER>.md`. Accept `--base-dp=<DP_ID>` as the base DP identifier argument. Must be executable (`chmod +x`). Must source `ops/lib/scripts/common.sh`.

2. `docs/ops/specs/binaries/addendum.md` — new spec file following the pattern of `docs/ops/specs/binaries/draft.md`. Must include: purpose, invocation syntax, arguments (`--base-dp`, `--slots-file`), output path schema, required slots list, slot validation rules, next-letter auto-assignment logic, error conditions, and relationship to addendum issuance protocol from `docs/MANUAL.md`.

3. `docs/ops/registry/binaries.md` — add OPS-BIN-17 row: `| OPS-BIN-17 | Addendum Generator | ops/bin/addendum | Spec: \`docs/ops/specs/binaries/addendum.md\`. Renders fully-formed addendum artifacts from \`ops/src/surfaces/addendum.md.tpl\` using a slots file; assigns next addendum letter automatically; validates required slot population before writing to \`storage/dp/intake/\`. Issued by the Integrator/auditor; Contractor does not invoke this binary. |`

4. `ops/src/surfaces/dp.md.tpl` — three targeted changes:

   **Change A — FRESHNESS_STAMP annotation.** After the line `Freshness Stamp: {{FRESHNESS_STAMP}}`, insert the following note line verbatim:
```
   Note: FRESHNESS_STAMP must be YYYY-MM-DD format only. No trace tokens, no timestamps, no other text. Certify rejects all other forms. dp.sh enforces at lint time.
```

   **Change B — RECEIPT_EXTRA annotation.** Replace the comment line above `- sha256sum ops/src/surfaces/dp.md.tpl
- grep CANONICAL_DP_TEMPLATE_SHA256= tools/lint/dp.sh
- bash -c 'grep -n "check_freshness_stamp_format\|check_receipt_command_substitution" tools/lint/dp.sh'
- bash -c 'grep -n "check_freshness_stamp_format\|check_receipt_command_substitution" tools/lint/dp.sh | grep -c "check_"'
- bash tools/lint/dp.sh --test
- bash -n ops/bin/addendum
- bash -c 'head -5 ops/bin/addendum'
- bash -c 'ls -la ops/bin/addendum'
- bash -c 'grep -c "OPS-BIN-17" docs/ops/registry/binaries.md'
- bash -c 'grep -c "Certify Compatibility Authoring Rules" docs/MANUAL.md'
- ./ops/bin/dump --selection=dp+allowlist --from-dp=auto --format=chatgpt --out=auto` (currently `**DP-specific receipt commands (scope-specific; author below):**`) with the following two-line block verbatim:
```
   **DP-specific receipt commands (scope-specific; author below):**
   Note: Command substitution forms (e.g., $(pwd), $(git ...)) are rejected by certify replay. Use literal values only. dp.sh enforces at lint time.
```

   **Change C — §3.5.1 Mandatory Closing Block list format.** Replace the entire §3.5.1 body (the six bare-label lines including blank separators) with the following list verbatim:
```
   ### 3.5.1 Mandatory Closing Block
   - Primary Commit Header
   - Pull Request Title
   - Pull Request Description
   - Final Squash Stub
   - Extended Technical Manifest
   - Review Conversation Starter
```
   Note: This is an instruction-format change only. The parenthetical type labels (plaintext, markdown) remain required in the actual `storage/handoff/CLOSING-DP-OPS-XXXX.md` sidecar and RESULTS artifact; the style.sh and dp.sh validators are unchanged.

5. `tools/lint/dp.sh` — four targeted changes:

   **Change A — add `check_freshness_stamp_format` function.** Insert the following function definition immediately after the closing brace of `check_required_fields()`:
```bash
   check_freshness_stamp_format() {
     local path="$1"
     local freshness_stamp
     freshness_stamp="$(trim "$(strip_backticks "$(extract_field_value "Freshness Stamp" "$path")")")"
     if [[ -z "$freshness_stamp" ]]; then
       return 0
     fi
     if contains_placeholder "$freshness_stamp"; then
       return 0
     fi
     if [[ ! "$freshness_stamp" =~ ^[0-9]{4}-[0-9]{2}-[0-9]{2}$ ]]; then
       fail "Freshness Stamp must be YYYY-MM-DD format only; got: ${freshness_stamp}"
     fi
   }
```

   **Change B — add `check_receipt_command_substitution` function.** Insert the following function definition immediately after the closing brace of `check_freshness_stamp_format()`:
```bash
   check_receipt_command_substitution() {
     local path="$1"
     local receipt_block
     local line
     receipt_block="$(extract_block "$path" '^### 3[.]4[.]5' '^## 3[.]5([.]|[[:space:]])')"
     if [[ -z "$receipt_block" ]]; then
       return 0
     fi
     while IFS= read -r line || [[ -n "$line" ]]; do
       [[ -z "$line" ]] && continue
       if [[ "$line" == *'$('* ]]; then
         fail "Section 3.4.5 receipt command contains command substitution (rejected by certify replay): ${line}"
       fi
     done <<< "$receipt_block"
   }
```

   **Change C — wire both new checks into `lint_payload()`.** In the `lint_payload()` function body, after the line `check_dump_selection_scope "$path"`, add:
```bash
   check_freshness_stamp_format "$path"
   check_receipt_command_substitution "$path"
```

   **Change D — update `CANONICAL_DP_TEMPLATE_SHA256`.** After all dp.md.tpl edits are complete, recompute the SHA256:
```bash
   sha256sum ops/src/surfaces/dp.md.tpl
```
   Replace the existing `CANONICAL_DP_TEMPLATE_SHA256` constant value with the computed hash. This step is a hard STOP if skipped or pre-filled.

6. `docs/ops/specs/tools/lint/dp.md` — add a new subsection documenting the two new checks: Freshness Stamp format enforcement (pattern `^[0-9]{4}-[0-9]{2}-[0-9]{2}$`, failure on non-date forms) and receipt command substitution detection (`$(` token scan across Section 3.4.5 receipt block, failure on any match). Document that both checks are wired in `lint_payload()` after `check_dump_selection_scope`.

7. `docs/MANUAL.md` — in the DP Mechanics section, add a subsection titled "Certify Compatibility Authoring Rules" with the following content verbatim:
```
   ### Certify Compatibility Authoring Rules

   Two DP authoring constraints are enforced by both dp.sh at lint time and certify at replay time.

   Freshness Stamp format: The Freshness Stamp field value must be YYYY-MM-DD and nothing else.
   No trace tokens, no timestamps, no other text. Examples of invalid forms: stela-20260223T151354Z-16f27113,
   2026-02-23T15:13Z, today. Example of valid form: 2026-02-23. Certify rejects all other forms and dp.sh
   fails at preflight if the format is invalid.

   Receipt command substitution: Section 3.4.5 receipt commands must use literal values only.
   Command substitution forms (expressions beginning with $() ) are rejected by certify's literal replay
   engine. Example of invalid form: ./ops/bin/llms --out-dir="$(pwd)". Example of valid form:
   ./ops/bin/llms --out-dir=. . Use literal relative or absolute paths in all receipt commands.
   dp.sh fails at preflight if any command substitution token is found in Section 3.4.5.
```

### 3.4.3 Changelog
- ops/bin/addendum — NEW
- docs/ops/specs/binaries/addendum.md — NEW
- docs/ops/registry/binaries.md — UPDATE (add OPS-BIN-17)
- ops/src/surfaces/dp.md.tpl — UPDATE (FRESHNESS_STAMP annotation, RECEIPT_EXTRA annotation, §3.5.1 list format)
- tools/lint/dp.sh — UPDATE (check_freshness_stamp_format, check_receipt_command_substitution, lint_payload wiring, CANONICAL_DP_TEMPLATE_SHA256 update)
- docs/ops/specs/tools/lint/dp.md — UPDATE (new checks documentation)
- docs/MANUAL.md — UPDATE (Certify Compatibility Authoring Rules subsection)
- storage/dp/active/allowlist.txt — UPDATE (DP-OPS-0105 target set)
- storage/handoff/CLOSING-DP-OPS-0105.md — MAINTAINED during execution

### 3.4.4 Patch / Diff

**Step 1 — Update allowlist.**
Write the following exact paths to `storage/dp/active/allowlist.txt` (one per line, no bullets):
```
ops/bin/addendum
docs/ops/specs/binaries/addendum.md
docs/ops/registry/binaries.md
ops/src/surfaces/dp.md.tpl
tools/lint/dp.sh
docs/ops/specs/tools/lint/dp.md
docs/MANUAL.md
storage/dp/active/allowlist.txt
storage/handoff/CLOSING-DP-OPS-0105.md
archives/surfaces/SoP-2026-02-23-*.md
archives/surfaces/PoW-2026-02-23-*.md
archives/surfaces/TASK-DP-OPS-0105-*.md
archives/manifests/compile-*.md
llms.txt
llms-core.txt
llms-full.txt
```

**Step 2 — Create `ops/bin/addendum`.**
Create the file `ops/bin/addendum` following the `ops/bin/draft` implementation pattern exactly. The binary must:
- Begin with `#!/usr/bin/env bash` and `set -euo pipefail`.
- Source `ops/lib/scripts/common.sh`.
- Parse `--base-dp=<DP_ID>` and `--slots-file=<path>` arguments.
- Validate that `--base-dp` matches `^DP-OPS-[0-9]{4}$`.
- Validate that `--slots-file` exists and is readable.
- Validate that the base DP exists in `storage/dp/processed/` (at least one file matching `<BASE_DP_ID>*.md`).
- Determine the next available addendum letter by scanning `storage/dp/intake/` for files matching `<BASE_DP_ID>-ADDENDUM-[A-Z].md`; start at A.
- Call `ops/bin/template render addendum` with the slots file to render the artifact.
- Validate required slots (BASE_DP_ID, ADDENDUM_ID, OPERATOR_AUTHORIZATION, SCOPE_DELTA, ADDENDUM_OBJECTIVE, ADDENDUM_RECEIPT) are populated and non-placeholder in the rendered output.
- Emit the rendered artifact to stdout as a preview.
- Write the final artifact to `storage/dp/intake/<BASE_DP_ID>-ADDENDUM-<LETTER>.md`.
- Emit a confirmation message to stdout stating the output path.
- Make the file executable: `chmod +x ops/bin/addendum`.

**Step 3 — Create `docs/ops/specs/binaries/addendum.md`.**
Create a spec file following the structure of `docs/ops/specs/binaries/draft.md`. Include: purpose, invocation syntax (`./ops/bin/addendum --base-dp=DP-OPS-XXXX --slots-file=<path>`), argument definitions, output path schema (`storage/dp/intake/<BASE_DP_ID>-ADDENDUM-<LETTER>.md`), required slots list, slot validation rules, next-letter auto-assignment logic, error conditions (base DP not found in processed, slots file missing, required slot unpopulated, command substitution in output), role restriction (issued by Integrator/auditor only; Contractor does not invoke), and pointer to `docs/MANUAL.md` addendum issuance procedure.

**Step 4 — Update `docs/ops/registry/binaries.md`.**
Append the following row to the registry table:
```
| OPS-BIN-17 | Addendum Generator | ops/bin/addendum | Spec: `docs/ops/specs/binaries/addendum.md`. Renders fully-formed addendum artifacts from `ops/src/surfaces/addendum.md.tpl` using a slots file; assigns next addendum letter automatically; validates required slot population before writing to `storage/dp/intake/`. Issued by the Integrator/auditor; Contractor does not invoke this binary. |
```

**Step 5 — Edit `ops/src/surfaces/dp.md.tpl`.**
Apply Changes A, B, and C as specified in §3.4.2 Request item 4. All three changes must be applied in a single edit pass. Do not apply partial changes and re-run lint.

**Step 6 — Recompute `CANONICAL_DP_TEMPLATE_SHA256`.**
After Step 5 is complete and the dp.md.tpl file is saved, run:
```bash
sha256sum ops/src/surfaces/dp.md.tpl
```
Record the output hash. In `tools/lint/dp.sh`, replace the existing `CANONICAL_DP_TEMPLATE_SHA256` constant value with this computed hash. Do not proceed until this step is complete.

**Step 7 — Edit `tools/lint/dp.sh`.**
Apply Changes A, B, C, and D as specified in §3.4.2 Request item 5. Change D (hash constant update) must be the result of Step 6, not a pre-filled value.

**Step 8 — Create/update `docs/ops/specs/tools/lint/dp.md`.**
Add the new checks subsection as specified in §3.4.2 Request item 6.

**Step 9 — Edit `docs/MANUAL.md`.**
Add the Certify Compatibility Authoring Rules subsection as specified in §3.4.2 Request item 7.

**Step 10 — Verify implementation.**
Run the mandatory receipt commands in §3.4.5 and confirm all pass before maintaining the closing sidecar.

### 3.4.5 Receipt (Proofs to collect) - MUST RUN

**Mandatory receipt commands (always run; do not omit):**
- bash tools/lint/dp.sh TASK.md
- bash tools/lint/task.sh
- bash tools/lint/integrity.sh
- bash tools/lint/style.sh
- git diff --name-only
- git diff --stat
- comm -23 <(git diff --name-only | sort) <(sort storage/dp/active/allowlist.txt) || true
- comm -23 <(git ls-files --others --exclude-standard | sort) <(sort storage/dp/active/allowlist.txt) || true
- ./ops/bin/open

**DP-specific receipt commands (scope-specific; author below):**
Note: Command substitution forms (e.g., $(pwd), $(git ...)) are rejected by certify replay. Use literal values only. dp.sh enforces at lint time.
- sha256sum ops/src/surfaces/dp.md.tpl
- grep CANONICAL_DP_TEMPLATE_SHA256= tools/lint/dp.sh
- bash -c 'grep -n "check_freshness_stamp_format\|check_receipt_command_substitution" tools/lint/dp.sh'
- bash -c 'grep -n "check_freshness_stamp_format\|check_receipt_command_substitution" tools/lint/dp.sh | grep -c "check_"'
- bash tools/lint/dp.sh --test
- bash -n ops/bin/addendum
- bash -c 'head -5 ops/bin/addendum'
- bash -c 'ls -la ops/bin/addendum'
- bash -c 'grep -c "OPS-BIN-17" docs/ops/registry/binaries.md'
- bash -c 'grep -c "Certify Compatibility Authoring Rules" docs/MANUAL.md'
- ./ops/bin/dump --selection=dp+allowlist --from-dp=auto --format=chatgpt --out=auto

## 3.5 Closeout (Mandatory Routing)
- Execute docs/MANUAL.md Closeout Cycle in order (Verify, Harvest, Refresh, Log, Prune).
- Update SoP.md and PoW.md with DP entries, including objective summary and verification commands run.
- Protocol order for closeout: Verify -> Generate Results -> COMMIT (Operator Only) -> Prune.
- Run prune hygiene: ./ops/bin/prune --scrub.
- Regenerate session artifacts: `./ops/bin/open --out=auto`
- Capture updated platform state: `./ops/bin/dump --scope=platform --format=chatgpt --out=auto`
- Ensure the next session begins with refreshed session artifacts and matching receipts.

### 3.5.1 Mandatory Closing Block
- Primary Commit Header
- Pull Request Title
- Pull Request Description
- Final Squash Stub
- Extended Technical Manifest
- Review Conversation Starter
