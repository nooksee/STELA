---
trace_id: stela-20260325T024818Z-18a254cf
packet_id: DP-OPS-0231
created_at: 2026-03-25T02:57:54Z
previous: (none)
---
# STELA TASK DASHBOARD
Status: ACTIVE
Owner: Integrator
Last Updated: 2026-03-19

## 1. Session State
Pointer: Session context output (generated by ops/bin/open)
Context Manifest: ops/lib/manifests/CONTEXT.md (checked by tools/lint/context.sh)

Session State contract:
- Session State is pointer-first. Do not write inline branch or hash state here.
- OPEN is the session state snapshot. OPEN-PORCELAIN is conditional dirty-state detail.
- RESULTS is the audit record for the DP.
- TASK is the stable routing surface.

## 2. Logic Pointers
Primary Constraint: PoT.md (Policy of Truth).
Surface Contract Spec: docs/ops/specs/surfaces/task.md.

### 2.1 Governance Pointers
Jurisdiction: PoT.md Section 3.
Git Authority: PoT.md Section 4.1.
Behavioral Standard: PoT.md Section 4.2.
Staffing Protocol: PoT.md Section 4.1.

### 2.2 Enforcement and Gates
Style: tools/lint/style.sh
Context: tools/lint/context.sh
Truth: tools/lint/truth.sh
DP lint: tools/lint/dp.sh (enforces DP transaction rules in Section 3; does not enforce TASK container schema)
TASK lint: tools/lint/task.sh (sole TASK surface/container schema enforcer)
Repo verify: tools/verify.sh
llms parity: tools/lint/llms.sh

## 3. Current Dispatch Packet (DP)

Section-by-section authoring protocol:
- Generate DP structure from `ops/src/surfaces/dp.md.tpl` using `ops/bin/draft`.
- Do not hand-edit DP structural boilerplate in `TASK.md` after generation.
- Manual edits are limited to slot content only (objective, scope, safety, plan body, receipt body).
- Freeze each completed subsection in order (3.1 -> 3.2 -> 3.3 -> 3.4.x).
- Do not revise earlier sections to justify later work. If scope changes, end the thread and draft a new DP.

Freeze Rule:
- If a completed DP subsection changes later, append an AMENDMENT block in RESULTS; otherwise DP is FAIL.

AMENDMENT: minimum fields
- Reopened section:
- Reason:
- Impact:
- Allowlist change (none or exact paths):

Input discipline (hard rule):
- Worker receives only the DP text. Worker does not require OPEN or DUMP attachments.
- DP writer must not attach or cite disposable artifacts (rough notes, chat logs, scratch drafts, throwaway excerpts).
- DP writer must not include pasted bundles (OPEN, DUMP payloads, manifests) inside the DP body.
- Canon pointers only. Evidence belongs in storage artifacts and RESULTS.



### DP-OPS-0231: PoT and OPEN Prose Retooling — Branch Naming, Provisional Handling, and Draft-Surface Alignment

## 3.1 Freshness Gate (Must Pass Before Work)
Base Branch: main
Work Branch: work/dp-ops-0231-pot-open-retool-2026-03-25
Base HEAD: 091a79ba
Freshness Stamp: 2026-03-25
Note: FRESHNESS_STAMP must be YYYY-MM-DD format only. No trace tokens, no timestamps, no other text. Certify rejects all other forms. dp.sh enforces at lint time.

Required local re-check (worker runs before any edits; paste verbatim outputs under `## Contractor Execution Narrative` -> `### Preflight State` in RESULTS):
- git rev-parse --abbrev-ref HEAD
- git rev-parse --short HEAD
- git status --porcelain

These three commands are freshness proof only. Do not treat §3.4.5 receipt replay as a substitute for their pre-edit outputs.

Preconditions:
- No commits on main.
- Working tree must be clean before execution begins.
- If Base HEAD changes, regenerate session artifacts from canonical tools before proceeding.

STOP conditions:
- STOP if any mismatch (branch, Base HEAD, missing work branch).
- STOP if working tree is dirty before execution begins.
- STOP if told to create or switch branches.

## 3.1.1 DP Preflight Gate (Run Before Any Edits)
Purpose:
- Catch malformed DP or TASK schema before work begins.

Worker runs (paste outcome in RESULTS):
- bash tools/lint/dp.sh --test
- bash tools/lint/dp.sh TASK.md
- bash tools/lint/task.sh

STOP if any preflight check fails.

### CbC Design Discipline Preflight (TASK.md §3.1.1)
Required when the DP objective adds, modifies, or replaces a linter, script, guard, or validation binary.
For non-tooling DPs: state "Not applicable" with a one-line justification.
This DP modifies `tools/lint/dp.sh` and is therefore tooling-applicable. The changes are limited to field-label string literal substitutions (`Required Work Branch` → `Work Branch`), error-message text updates (`work/<topic>-YYYY-MM-DD` → `work/<DP-ID>-YYYY-MM-DD`), the awk normalization block that matches the old label, and test fixtures that reference the old label strings. The validation regex `^work/[^[:space:]]+-[0-9]{4}-[0-9]{2}-[0-9]{2}$` is not changed; no allow-set constants or behavioral guard logic changes. Template hash constants in `tools/lint/dp.sh` must also be updated after `ops/src/surfaces/dp.md.tpl` is edited; this is a synchronized constant update required by the template-hash contract, not a behavioral change.

## 3.2 Required Context Load (Read Before Doing Anything)

### 3.2.1 Canon load order (always)
Worker must confirm loaded before edits begin:
1. PoT.md
2. SoP.md
3. PoW.md
4. TASK.md
5. docs/MAP.md
6. docs/MANUAL.md
7. ops/lib/manifests/CONTEXT.md

Notes:
- Worker does not read OPEN. OPEN is for Integrator state refresh and for receipts.
- Disposable artifacts must not be referenced or included.
- Worker input is the DP text only.
- DP writer must not attach or cite disposable artifacts.
- DP writer must not embed pasted bundles.

## Rules

1 Temp path rule uses `var/tmp` only
2 RESULTS generation rule uses `ops/bin/certify` only
3 Receipt rule runs every DP mandated command and treats any skip as STOP
4 Disposable artifact rule treats OPEN dump bundles and manifests as non canonical session artifacts
5 Use repo-relative paths only in all output. Never print absolute filesystem paths. Do not emit clickable file links.

### 3.2.2 DP-scoped load order (per DP)
- PoT.md (constitutional lead; branch-naming and provisional-value doctrine being modified)
- docs/ops/specs/binaries/open.md (OPEN semantics spec; will be narrowed)
- tools/lint/dp.sh (branch-form and field-label enforcement; will be updated)
- docs/ops/specs/tools/lint/dp.md (dp.sh spec; will be updated)
- ops/bin/certify (TASK branch-field parser; literal field-label update only)
- docs/ops/specs/binaries/certify.md (certify branch-field spec; literal field-label update only)
- ops/bin/open (emitted OPEN prompt binary; draft-lane text will be updated)
- ops/src/surfaces/dp.md.tpl (DP template; field label changes)
- ops/src/surfaces/task.md.tpl (TASK template; field label changes)
- ops/src/stances/draft.md.tpl (draft stance; content-requirement label reference changes)
- docs/MANUAL.md (PoT section-number citation updates if Branching Doctrine is renumbered)
- docs/ops/specs/binaries/hooks.md (PoT section-number citation updates if Branching Doctrine is renumbered)
- .github/hooks/pre-commit (PoT section-number citation updates if Branching Doctrine is renumbered)
- .github/hooks/pre-push (PoT section-number citation updates if Branching Doctrine is renumbered)

## 3.3 Scope and Safety
Objective:
Retool `PoT.md` to adopt `work/<DP-ID>-YYYY-MM-DD` branch naming, move the provisional-value execution rule into the main §6.2.2 Immutable Dispatch Packet Workflow path, add an OPEN freshness-anchor reference to the workflow prose, restructure §6 so Branching Doctrine appears before the immutable packet workflow details, and remove §6.3 after absorbing its surviving content. Simplify the `Required Work Branch` field label to `Work Branch` across all coupled enforcement, spec, parser, prompt, and template surfaces. Tighten the emitted OPEN prompt to reference the bundle-embedded DP authoring scaffold instead of TASK headings, narrow `docs/ops/specs/binaries/open.md` to OPEN-as-trace-anchor semantics with an explicit `main` authority boundary, and update the small set of repo citations that would otherwise continue pointing at the old `§6.2.3` branch-doctrine location after renumbering.

In scope:
- `PoT.md` — §6.2 and §6.2.3 branch-naming text; §6.2.2 provisional-value bullet and OPEN reference; §6 restructure to place Branching Doctrine at §6.2.1 before §6.2.2; §6.3 removal after content absorption evaluation.
- `docs/ops/specs/binaries/open.md` — add `main` context-refresh vs. packet-execution authority distinction; replace any workflow doctrine with dependency language back to PoT.md.
- `ops/bin/open` — replace stale `follow TASK headings/order` draft cue with scaffold-first guidance; tighten active-draft-surface packet-identity wording in [SHIPPING SPINE].
- `ops/src/surfaces/dp.md.tpl` — rename field label from `Required Work Branch:` to `Work Branch:` on the template line; update the awk normalization block in `tools/lint/dp.sh` that matches this label (see dp.sh entry).
- `ops/src/surfaces/task.md.tpl` — rename field label from `Required Work Branch:` to `Work Branch:` on the placeholder line.
- `ops/src/stances/draft.md.tpl` — update CONTENT REQUIREMENTS Step 4 label from `` `Required Work Branch` `` to `` `Work Branch` ``.
- `tools/lint/dp.sh` — all `Required Work Branch` string literals → `Work Branch`; branch-form error message → `work/<DP-ID>-YYYY-MM-DD`; awk normalization block label; test fixtures; `CANONICAL_DP_TEMPLATE_SHA256` constant updated to reflect the new dp.md.tpl after template edit.
- `docs/ops/specs/tools/lint/dp.md` — all `Required Work Branch` → `Work Branch`; all `work/<topic>-YYYY-MM-DD` → `work/<DP-ID>-YYYY-MM-DD`.
- `ops/bin/certify` — update the literal TASK field-label parser from `Required Work Branch:` to `Work Branch:` only; do not alter branch equality logic.
- `docs/ops/specs/binaries/certify.md` — update the branch-field label in the postflight packet-consistency description only.
- `docs/MANUAL.md`, `docs/ops/specs/binaries/hooks.md`, `.github/hooks/pre-commit`, `.github/hooks/pre-push` — if Branching Doctrine is renumbered from `§6.2.3` to `§6.2.1`, update those literal PoT citations and nothing else.

Out of scope:
- `docs/MAP.md` (PLAN §Assumptions: follow-on alignment pass only; do not touch).
- Hook enforcement behavior (`not main` / `work/*` is sufficient; no `<DP-ID>` pattern enforcement required per PLAN).
- Certify branch-equality logic (keep the equality check exactly as-is; only the literal field-label parser may change).
- Any surface not in the minimum coupled set identified by PLAN.
- Any PoT.md section outside §6 Workflow.
- `tools/lint/task.sh` template hash — if task.sh enforces a hash on task.md.tpl and fails after the label rename, STOP and request an allowlist addendum before proceeding.

Safety and invariants:
- No manual-generated structural output; DP structural boilerplate is preserved from canonical template.
- Allowlist hard gate: every file in §3.4.3 Changelog must appear in `storage/dp/active/allowlist.txt` before any edits begin. STOP if any target file is absent.
- `PROPOSED_WORK_BRANCH` in dp.md.tpl is a slot token (schema identifier form); the drafting-marker scan strips it. Do not treat it as a prohibited drafting marker.
- Do not alter `stance_id: draft`, `profile=draft`, `stance-draft`, `stance-audit`, or any schema identifier that contains `draft` or `audit` as an identifier substring.
- Do not change the validation regex `^work/[^[:space:]]+-[0-9]{4}-[0-9]{2}-[0-9]{2}$` in dp.sh.
- Do not change certify branch-equality logic.
- Do not change hook enforcement.
- Do not add PoT workflow doctrine to `docs/ops/specs/binaries/open.md`; use dependency language pointing to PoT.md only.
- Do not pre-populate §3.5.1 Mandatory Closing Sidecar fields.

Worker Constraints (SSOT injected):
## Section 1: Universal Template Rules
- Template source uses `tpl` files with optional YAML frontmatter
- Canon frontmatter keys: `template_type` `template_id` `template_version` `requires_slots` `includes`
- Renderer strips frontmatter before output write
- Slot token form `\{\{TOKEN\}\}` with uppercase alphanumeric underscore
- Include forms `\{\{@include:path\}\}` and `\{\{@include:path#section\}\}`
- Include resolution is strict: missing file fail missing section fail circular graph fail
- Strict mode default: every required slot value present and no unresolved token
- Non strict mode allowed only for lint and normalization workflows
- Worker facing generated surfaces remain pointer first and exclude disposable artifacts

Target Files allowlist (hard gate):
- storage/dp/active/allowlist.txt

## 3.4 Execution Plan (A-E)

### 3.4.1 State
Branch `main`, HEAD `091a79ba`, working tree clean (0 porcelain entries), as confirmed by bundle metadata. Last commit: `091a79ba Main history now uses role nouns consistently in remaining planning prose`.

Current defects visible from dump inspection at `091a79ba`:
- `PoT.md` §6.2 uses `work/<topic>-YYYY-MM-DD` in the Standard Workflow branch-naming line.
- `PoT.md` §6.2.3 Branching Doctrine Naming Schema uses `work/<topic>-YYYY-MM-DD`.
- `PoT.md` §6.2.2 Immutable Dispatch Packet Workflow contains no provisional-value execution rule and no OPEN freshness-anchor reference.
- `PoT.md` §6.2.3 Branching Doctrine is positioned after §6.2.2 Immutable Dispatch Packet Workflow; PLAN requires it to appear before.
- `PoT.md` §6.3 Drafting Proposal Protocol holds the provisional-marking rule as a standalone subsection, separate from the main workflow path.
- `ops/bin/open` [PRIMARY WORK SURFACE] heredoc emits: `- When drafting a DP: follow TASK headings/order; output only the DP; ask if required fields are missing.`
- `ops/bin/open` [SHIPPING SPINE] heredoc emits: `- Active draft surface: storage/dp/intake/DP.md (latest-wins); packet identity remains DP-OPS-XXXX.`
- `ops/src/surfaces/dp.md.tpl` field line: `Required Work Branch: {{PROPOSED_WORK_BRANCH}}`
- `ops/src/surfaces/task.md.tpl` field line: `Required Work Branch: work/dp-ops-XXXX-YYYY-MM-DD`
- `ops/src/stances/draft.md.tpl` Step 4: `` * `Required Work Branch`: replace only the branch slug marker... ``
- `tools/lint/dp.sh`: `extract_field_value "Required Work Branch"` (multiple occurrences); `fail "Required Work Branch must follow 'work/<topic>-YYYY-MM-DD' form (PoT.md §6.2.3)"`; awk normalization block matches `Required Work Branch`; test fixtures inject `Required Work Branch`.
- `docs/ops/specs/tools/lint/dp.md`: references `Required Work Branch` in coherence-check and fixture-requirement prose; references `work/<topic>-YYYY-MM-DD` form in spec body and fixture descriptions.
- `docs/ops/specs/binaries/open.md`: does not explicitly distinguish between valid OPEN runs on `main` for context refresh and forbidden packet execution on `main`.
- `ops/bin/certify` still parses the TASK field name literally as `Required Work Branch:`.
- `docs/ops/specs/binaries/certify.md` still documents the field label as `Required Work Branch:`.
- `docs/MANUAL.md`, `docs/ops/specs/binaries/hooks.md`, `.github/hooks/pre-commit`, and `.github/hooks/pre-push` all cite `PoT §6.2.3`; if Branching Doctrine moves to `§6.2.1`, those citations become stale unless updated in the same packet.

### 3.4.2 Request
1. Read each target file directly from the repo before editing it. Do not use dump payload text as the edit source.
2. In `PoT.md` §6.2 Standard Workflow: change the branch-naming line from `work/<topic>-YYYY-MM-DD` to `work/<DP-ID>-YYYY-MM-DD`.
3. In `PoT.md` §6.2.3 Branching Doctrine, Naming Schema line: change `work/<topic>-YYYY-MM-DD` to `work/<DP-ID>-YYYY-MM-DD`.
4. In `PoT.md` §6.2.2 Immutable Dispatch Packet Workflow: add `- All provisional values in the DP must be resolved before worker execution begins.` as a new bullet. Add `- OPEN is the spine-grade freshness and trace anchor; establish it with ./ops/bin/open before packet execution.` as a new bullet near the freshness-state step. Do not alter any existing bullets.
5. In `PoT.md` §6: move the Branching Doctrine section (currently §6.2.3) to become §6.2.1, inserted between §6.2 Standard Workflow and §6.2.2 Immutable Dispatch Packet Workflow. Renumber the heading from `### 6.2.3` to `### 6.2.1`. Confirm no cross-references to `§6.2.3` exist elsewhere in the file before renaming; if any exist, update them.
6. In `PoT.md` §6.3 Drafting Proposal Protocol: the provisional-marking rule moves to §6.2.2 (Step 4 above). Evaluate the remaining two rules — Integrator-proposal and Operator-authority — against §4.1 Staffing Protocol. If those rules are substantively covered by §4.1, remove §6.3 in its entirety. If any content is unhoused, migrate it to the appropriate §4 or §6 location first, then remove §6.3. Record the evaluation outcome in the execution narrative before removing.
7. In `docs/ops/specs/binaries/open.md`: add a new section or paragraph — placed after the existing **Mechanics and Sequencing** section — with the heading `## OPEN Authority Boundary` containing: `OPEN may be run on \`main\` for context refresh or intake routing. OPEN does not authorize packet execution or mutation on \`main\`; that authority is governed by PoT.md §6.`
8. In `ops/bin/open`: in the first heredoc (cat <<EOF), find and replace the [PRIMARY WORK SURFACE] draft cue line:
   Find: `- When drafting a DP: follow TASK headings/order; output only the DP; ask if required fields are missing.`
   Replace: `- When drafting a DP: use the DP AUTHORING SCAFFOLD embedded in the draft bundle; output only the DP; ask if required fields are missing.`
9. In `ops/bin/open`: in the second heredoc (cat <<EOF), find and replace the [SHIPPING SPINE] active-draft-surface line:
   Find: `- Active draft surface: storage/dp/intake/DP.md (latest-wins); packet identity remains DP-OPS-XXXX.`
   Replace: `- Active draft surface: storage/dp/intake/DP.md (latest-wins); packet identity is bundle/request-mediated.`
10. In `ops/src/surfaces/dp.md.tpl`: find `Required Work Branch: {{PROPOSED_WORK_BRANCH}}` and replace with `Work Branch: {{PROPOSED_WORK_BRANCH}}`.
11. After Step 10, run `bash tools/lint/dp.sh --test` to obtain the new `CANONICAL_DP_TEMPLATE_SHA256` value emitted by the test run. Update the hard-coded `CANONICAL_DP_TEMPLATE_SHA256` constant in `tools/lint/dp.sh` to this new value before proceeding.
12. In `ops/src/surfaces/task.md.tpl`: find `Required Work Branch: work/dp-ops-XXXX-YYYY-MM-DD` and replace with `Work Branch: work/dp-ops-XXXX-YYYY-MM-DD`.
13. In `ops/src/stances/draft.md.tpl` Step 4: find `` * `Required Work Branch`: replace only the branch slug marker; preserve packet-id/date framing already present in the scaffold when present. `` and replace with `` * `Work Branch`: replace only the branch slug marker; preserve packet-id/date framing already present in the scaffold when present. ``
14. In `tools/lint/dp.sh`: apply the following changes globally:
    a. Replace every string literal occurrence of `Required Work Branch` with `Work Branch` — this covers `extract_field_value` call arguments, `fail` message strings, the awk normalization block pattern and print statement, and `sed` fixture injection commands.
    b. Find and replace the branch-form error message:
       Find: `fail "Required Work Branch must follow 'work/<topic>-YYYY-MM-DD' form (PoT.md §6.2.3): ${work_branch}"`
       Replace: `fail "Work Branch must follow 'work/<DP-ID>-YYYY-MM-DD' form (PoT.md §6.2.3): ${work_branch}"`
    c. Update `CANONICAL_DP_TEMPLATE_SHA256` constant with the new hash value obtained in Step 11.
    d. Do not change the validation regex `^work/[^[:space:]]+-[0-9]{4}-[0-9]{2}-[0-9]{2}$` on any adjacent line.
15. In `docs/ops/specs/tools/lint/dp.md`: replace all occurrences of `Required Work Branch` with `Work Branch`; replace all occurrences of `work/<topic>-YYYY-MM-DD` with `work/<DP-ID>-YYYY-MM-DD`.
16. In `ops/bin/certify`: update the literal TASK field parser from `Required Work Branch:` to `Work Branch:` only. Do not alter the equality comparison against the current branch.
17. In `docs/ops/specs/binaries/certify.md`: update the postflight packet-consistency bullet from `Required Work Branch:` to `Work Branch:`.
18. If Step 5 renumbers Branching Doctrine from `§6.2.3` to `§6.2.1`, update every repo citation found by `rg -n "PoT §6\\.2\\.3|§6\\.2\\.3"` in:
   - `docs/MANUAL.md`
   - `docs/ops/specs/binaries/hooks.md`
   - `.github/hooks/pre-commit`
   - `.github/hooks/pre-push`
   No other wording changes are permitted in those files.
19. Run `git diff` and confirm: only the files listed in §3.4.3 Changelog were modified; no `stance_id`, profile key, schema identifier, or unintended content was altered. STOP if any such change is found and revert it.

### 3.4.3 Changelog
- UPDATE `PoT.md` — branch naming in §6.2 and §6.2.1 (formerly §6.2.3); provisional-value rule and OPEN reference added to §6.2.2; §6.2.3 Branching Doctrine moved to §6.2.1; §6.3 removed after content evaluation
- UPDATE `docs/ops/specs/binaries/open.md` — new OPEN Authority Boundary section distinguishing `main` context refresh from packet execution
- UPDATE `ops/bin/open` — scaffold-first draft cue; tightened active-draft-surface packet-identity wording
- UPDATE `ops/src/surfaces/dp.md.tpl` — `Required Work Branch:` renamed to `Work Branch:`
- UPDATE `ops/src/surfaces/task.md.tpl` — `Required Work Branch:` renamed to `Work Branch:`
- UPDATE `ops/src/stances/draft.md.tpl` — content-requirement label updated to `Work Branch`
- UPDATE `tools/lint/dp.sh` — field-label strings, branch-form error message, awk normalization block, test fixtures, `CANONICAL_DP_TEMPLATE_SHA256` constant
- UPDATE `docs/ops/specs/tools/lint/dp.md` — field-label and branch-form spec references
- UPDATE `ops/bin/certify` — literal TASK field parser updated to `Work Branch:`
- UPDATE `docs/ops/specs/binaries/certify.md` — postflight field-label description updated to `Work Branch:`
- UPDATE `docs/MANUAL.md` — `PoT §6.2.3` branch-doctrine citation updated to `§6.2.1` if Branching Doctrine is renumbered
- UPDATE `docs/ops/specs/binaries/hooks.md` — `PoT §6.2.3` branch-doctrine citation updated to `§6.2.1` if Branching Doctrine is renumbered
- UPDATE `.github/hooks/pre-commit` — `PoT §6.2.3` branch-doctrine citation updated to `§6.2.1` if Branching Doctrine is renumbered
- UPDATE `.github/hooks/pre-push` — `PoT §6.2.3` branch-doctrine citation updated to `§6.2.1` if Branching Doctrine is renumbered

### 3.4.4 Patch / Diff

1. **Verify allowlist coverage.** Confirm every file in §3.4.3 Changelog appears in `storage/dp/active/allowlist.txt`. STOP if any target file is absent.

2. **Step 1 — Update `PoT.md`.** Read the file, then apply:

   a. In §6.2 Standard Workflow body, find:
```
   - Create a branch named `work/<topic>-YYYY-MM-DD`.
```
   Replace with:
```
   - Create a branch named `work/<DP-ID>-YYYY-MM-DD`.
```

   b. In §6.2.3 Branching Doctrine, Naming Schema line, find:
```
   - Naming Schema: work/<topic>-YYYY-MM-DD.
```
   Replace with:
```
   - Naming Schema: work/<DP-ID>-YYYY-MM-DD.
```

   c. In §6.2.2 Immutable Dispatch Packet Workflow, after the existing bullet `- Establish freshness state.` add:
```
   - OPEN is the spine-grade freshness and trace anchor; establish it with ./ops/bin/open before packet execution.
```
   After the existing bullet `- Maintain closing-sidecar input during execution.` add:
```
   - All provisional values in the DP must be resolved before worker execution begins.
```

   d. Move the §6.2.3 Branching Doctrine section to become §6.2.1, positioned immediately after the §6.2 Standard Workflow body and before §6.2.2 Immutable Dispatch Packet Workflow. Change the heading from `### 6.2.3 Branching Doctrine (SSoT)` to `### 6.2.1 Branching Doctrine (SSoT)`. Scan the rest of PoT.md for any cross-references to `§6.2.3`; update them to `§6.2.1` if found.

   e. Evaluate §6.3 Drafting Proposal Protocol for removal. The provisional-marking rule is now in §6.2.2. The Integrator-proposal and Operator-authority rules (`Integrator proposes branch name and Base HEAD`; `Operator creates branches; Contractors do not create or switch branches`) are enforced at DP runtime by the STOP conditions in the DP template and by §4.1 Staffing Protocol. Remove §6.3 in its entirety. Record this evaluation in the execution narrative.

3. **Step 2 — Update `docs/ops/specs/binaries/open.md`.** Read the file, then append a new section after the **Mechanics and Sequencing** section:
```
   ## OPEN Authority Boundary
   OPEN may be run on `main` for context refresh or intake routing. OPEN does not authorize packet execution or mutation on `main`; that authority is governed by PoT.md §6.
```

4. **Step 3 — Update `ops/bin/open`.** Read the file, then apply two targeted replacements in the heredoc blocks:

   a. In the first `cat <<EOF` block, find the exact line:
```
   - When drafting a DP: follow TASK headings/order; output only the DP; ask if required fields are missing.
```
   Replace with:
```
   - When drafting a DP: use the DP AUTHORING SCAFFOLD embedded in the draft bundle; output only the DP; ask if required fields are missing.
```

   b. In the second `cat <<EOF` block, find the exact line:
```
   - Active draft surface: storage/dp/intake/DP.md (latest-wins); packet identity remains DP-OPS-XXXX.
```
   Replace with:
```
   - Active draft surface: storage/dp/intake/DP.md (latest-wins); packet identity is bundle/request-mediated.
```

5. **Step 4 — Update `ops/src/surfaces/dp.md.tpl`.** Read the file, then find:
```
   Required Work Branch: {{PROPOSED_WORK_BRANCH}}
```
   Replace with:
```
   Work Branch: {{PROPOSED_WORK_BRANCH}}
```

6. **Step 4.1 — Capture new dp.md.tpl hash and update dp.sh constant.** Immediately after Step 4, run:
```
   bash tools/lint/dp.sh --test
```
   The output will emit the new `CANONICAL_DP_TEMPLATE_SHA256` value. Record this value. Proceed to Step 7 (dp.sh edit) and update the constant there. Do not run further dp.sh lint commands before the constant is updated.

7. **Step 5 — Update `ops/src/surfaces/task.md.tpl`.** Read the file, then find:
```
   Required Work Branch: work/dp-ops-XXXX-YYYY-MM-DD
```
   Replace with:
```
   Work Branch: work/dp-ops-XXXX-YYYY-MM-DD
```

8. **Step 6 — Update `ops/src/stances/draft.md.tpl`.** Read the file, then find:
```
   * `Required Work Branch`: replace only the branch slug marker; preserve packet-id/date framing already present in the scaffold when present.
```
   Replace with:
```
   * `Work Branch`: replace only the branch slug marker; preserve packet-id/date framing already present in the scaffold when present.
```

9. **Step 7 — Update `tools/lint/dp.sh`.** Read the file, then apply all of the following:

   a. Replace every string literal `Required Work Branch` with `Work Branch` — this includes the `extract_field_value` call arguments (two occurrences), the `fail` message strings referencing the field name, the awk normalization pattern that matches the old field label, the awk print statement that emits the work-branch slot token, and the `sed` test-fixture injection commands for the invalid branch-form and provisional-marker branch fixtures.

   b. Find and update the branch-form error message:
```
   fail "Required Work Branch must follow 'work/<topic>-YYYY-MM-DD' form (PoT.md §6.2.3): ${work_branch}"
```
   Replace with:
```
   fail "Work Branch must follow 'work/<DP-ID>-YYYY-MM-DD' form (PoT.md §6.2.3): ${work_branch}"
```

   c. Update the `CANONICAL_DP_TEMPLATE_SHA256` constant to the new hash value captured in Step 4.1.

   d. Do not change the validation regex `^work/[^[:space:]]+-[0-9]{4}-[0-9]{2}-[0-9]{2}$` or any surrounding guard logic.

10. **Step 8 — Update `docs/ops/specs/tools/lint/dp.md`.** Read the file, then:
    a. Replace every occurrence of `Required Work Branch` with `Work Branch`.
    b. Replace every occurrence of `work/<topic>-YYYY-MM-DD` with `work/<DP-ID>-YYYY-MM-DD`.

11. **Step 9 — Verify.** Run:
```
    git diff
```
    Confirm only the eight files in §3.4.3 Changelog were modified. Confirm no `stance_id`, profile key, schema identifier, or content outside the specified changes was altered. STOP if any unintended change is found and revert it before proceeding.

### 3.4.5 Receipt (Proofs to collect) - MUST RUN

**Mandatory receipt commands (always run; do not omit):**
- bash tools/lint/dp.sh TASK.md
- bash tools/lint/task.sh
- bash tools/lint/integrity.sh
- bash tools/lint/style.sh
- ./ops/bin/llms
- bash tools/lint/integrity.sh
- git diff --name-only
- git diff --stat
- comm -23 <(git diff --name-only | sort) <(sort storage/dp/active/allowlist.txt) || true
- comm -23 <(git ls-files --others --exclude-standard | sort) <(sort storage/dp/active/allowlist.txt) || true
- ./ops/bin/open

**DP-specific receipt commands (scope-specific; author below):**
Note: Command substitution forms (e.g., $(pwd), $(git ...)) are rejected by certify replay. Use literal values only. Repo-local binaries must be authored as literal `./ops/bin/...` or `ops/bin/...` commands. Unsupported receipt-command forms are rejected by dp.sh and certify.
- bash tools/lint/dp.sh --test
- bash tools/lint/plan.sh storage/handoff/PLAN.md
- grep -n "DP-ID" PoT.md
- grep -n "Work Branch" ops/src/surfaces/dp.md.tpl ops/src/surfaces/task.md.tpl ops/src/stances/draft.md.tpl tools/lint/dp.sh docs/ops/specs/tools/lint/dp.md
- bash -lc '! grep -n "Required Work Branch" ops/src/surfaces/dp.md.tpl ops/src/surfaces/task.md.tpl ops/src/stances/draft.md.tpl'
- bash -lc '! grep -n "follow TASK headings" ops/bin/open'
- bash -lc '! grep -n "packet identity remains" ops/bin/open'
- bash -lc '! grep -rn "work/<topic>" PoT.md tools/lint/dp.sh docs/ops/specs/tools/lint/dp.md'
- grep -n "OPEN Authority Boundary\|context refresh\|packet execution" docs/ops/specs/binaries/open.md

## 3.5 Closeout (Mandatory Routing)
- Execute docs/MANUAL.md Closeout Cycle in order (Verify, Harvest, Refresh, Log, Prune).
- Update SoP.md and PoW.md with DP entries, including objective summary and current proof / receipt summary.
- Protocol order for closeout: Verify -> Generate Results -> COMMIT (Operator Only) -> Prune.
- Run prune hygiene: ./ops/bin/prune --scrub.
- Refresh llms artifacts: `./ops/bin/llms`
- Regenerate session artifacts: `./ops/bin/open --out=auto`
- Ensure the next session begins with refreshed session artifacts and matching receipts.
- Note: audit dump generation is owned by `./ops/bin/bundle --profile=audit --out=auto` and is separate from operator session refresh. Do not stamp an additional `ops/bin/dump --scope=core` into closeout unless the DP explicitly authorizes a standalone state capture.
- Refresh side-effect: `ops/bin/llms` regenerates `ops/lib/manifests/OPS.md` as a compile event; if `OPS.md` is not in the allowlist, restore it before running `integrity.sh`. See `docs/MANUAL.md` Refresh side-effect notice for the full procedure.

### 3.5.1 Mandatory Closing Sidecar
Closing sidecar content is generated and maintained at `storage/handoff/CLOSING.md` and is validated by `ops/bin/certify` as a hard gate at certification time. Do not author, predict, populate, or approximate any sidecar-derived closeout output outside this sidecar.
Certify separately collects contractor-authored narrative for the RESULTS Contractor Execution Narrative section at certify time via interactive editor prompt; this narrative input is independent from closing sidecar content.

Before running certify, confirm `storage/handoff/CLOSING.md` has been maintained throughout execution and reflects observable reality only.

Closing sidecar fields required at certify time:
- Commit Message
- Create Pull Request (Title)
- Create Pull Request (Description)
- Confirm Merge (Commit Message)
- Confirm Merge (Extended Description)
- Confirm Merge (Add a Comment)
