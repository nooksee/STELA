---
trace_id: stela-20260225T193227Z-521f6355
packet_id: DP-OPS-0116
created_at: 2026-02-25T20:25:43Z
previous: archives/surfaces/TASK-DP-OPS-0116-f134c69c2.md
---
# STELA TASK DASHBOARD
Status: ACTIVE
Owner: Integrator
Last Updated: 2026-02-18

## 1. Session State
Pointer: Session context output (generated by ops/bin/open)
Context Manifest: ops/lib/manifests/CONTEXT.md (checked by tools/lint/context.sh)

Session State contract:
- Session State is pointer-first. Do not write inline branch or hash state here.
- OPEN and OPEN-PORCELAIN are the session state snapshot.
- OPEN-PORCELAIN is emitted on every OPEN run. Clean sessions produce a zero-line artifact.
- RESULTS is the audit record for the DP.
- TASK is the stable routing surface.
- Operator note (2026-02-17): Do not edit `SoP.md` or `PoW.md` unless explicitly instructed.

## 2. Logic Pointers
Primary Constraint: PoT.md (Policy of Truth).
Surface Contract Spec: docs/ops/specs/surfaces/task.md.

### 2.1 Governance Pointers
Jurisdiction: PoT.md Section 3.
Git Authority: PoT.md Section 4.1.
Behavioral Standard: PoT.md Section 4.2.
Staffing Protocol: PoT.md Section 4.1.

### 2.2 Enforcement and Gates
Style: tools/lint/style.sh
Context: tools/lint/context.sh
Truth: tools/lint/truth.sh
DP lint: tools/lint/dp.sh (enforces DP transaction rules in Section 3; does not enforce TASK container schema)
TASK lint: tools/lint/task.sh (sole TASK surface/container schema enforcer)
Repo verify: tools/verify.sh
llms parity: tools/lint/llms.sh

## 3. Current Dispatch Packet (DP)

Section-by-section authoring protocol:
- Generate DP structure from `ops/src/surfaces/dp.md.tpl` using `ops/bin/draft`.
- Do not hand-edit DP structural boilerplate in `TASK.md` after generation.
- Manual edits are limited to slot content only (objective, scope, safety, plan body, receipt body).
- Freeze each completed subsection in order (3.1 -> 3.2 -> 3.3 -> 3.4.x).
- Do not revise earlier sections to justify later work. If scope changes, end the thread and draft a new DP.

Freeze Rule:
- If a completed DP subsection changes later, append an AMENDMENT block in RESULTS; otherwise DP is FAIL.

AMENDMENT: minimum fields
- Reopened section:
- Reason:
- Impact:
- Allowlist change (none or exact paths):

Input discipline (hard rule):
- Worker receives only the DP text. Worker does not require OPEN or DUMP attachments.
- DP writer must not attach or cite disposable artifacts (rough notes, chat logs, scratch drafts, throwaway excerpts).
- DP writer must not include pasted bundles (OPEN, DUMP payloads, manifests) inside the DP body.
- Canon pointers only. Evidence belongs in storage artifacts and RESULTS.

### DP-OPS-0112: Dispatch-Finalize Gate (`dp.sh` PROPOSED Token Scan)

## 3.1 Freshness Gate (Must Pass Before Work)
Base Branch: main
Required Work Branch: work/DP-OPS-0112-dispatch-gate-2026-02-25
Base HEAD: a065eec6b
Freshness Stamp: 2026-02-25
Note: FRESHNESS_STAMP must be YYYY-MM-DD format only. No trace tokens, no timestamps, no other text. Certify rejects all other forms. dp.sh enforces at lint time.

Required local re-check (worker runs; paste outputs in RESULTS):
- git rev-parse --abbrev-ref HEAD
- git rev-parse --short HEAD
- git status --porcelain

Preconditions:
- No commits on main.
- Working tree must be clean before execution begins.
- If Base HEAD changes, regenerate session artifacts from canonical tools before proceeding.

STOP conditions:
- STOP if any mismatch (branch, Base HEAD, missing work branch).
- STOP if working tree is dirty before execution begins.
- STOP if told to create or switch branches.

## 3.1.1 DP Preflight Gate (Run Before Any Edits)
Purpose:
- Catch malformed DP or TASK schema before work begins.

Worker runs (paste outcome in RESULTS):
- bash tools/lint/dp.sh --test
- bash tools/lint/dp.sh TASK.md
- bash tools/lint/task.sh

STOP if any preflight check fails.

### CbC Design Discipline Preflight (TASK.md §3.1.1)
Required when the DP objective adds, modifies, or replaces a linter, script, guard, or validation binary.
For non-tooling DPs: state "Not applicable" with a one-line justification.
Required when the DP objective adds, modifies, or replaces a linter, script, guard, or validation binary.
For non-tooling DPs: state "Not applicable" with a one-line justification.
This DP adds and modifies linter behavior in tools/lint/dp.sh. Prevention: the PROPOSED-token scan is implemented as a guard inside lint_payload() that fires before any structural validation, making it structurally impossible to dispatch a DP with unfinalised provisional markers regardless of downstream tooling state. Detection is not the primary mechanism.

## 3.2 Required Context Load (Read Before Doing Anything)

### 3.2.1 Canon load order (always)
Worker must confirm loaded before edits begin:
1. PoT.md
2. SoP.md
3. PoW.md
4. TASK.md
5. docs/MAP.md
6. docs/MANUAL.md
7. ops/lib/manifests/CONTEXT.md

Notes:
- Worker does not read OPEN. OPEN is for Integrator state refresh and for receipts.
- Disposable artifacts must not be referenced or included.
- Worker input is the DP text only.
- DP writer must not attach or cite disposable artifacts.
- DP writer must not embed pasted bundles.

<!-- CCD: ff_target="machine-dense" ff_band="10-20" -->
# Contractor Brief

1 Temp path rule uses `var/tmp` only
2 RESULTS generation rule uses `ops/bin/certify` only
3 Receipt rule runs every DP mandated command and treats any skip as STOP
4 Disposable artifact rule treats OPEN dump bundles and manifests as non canonical session artifacts
5 Path rule uses existing repo paths or explicit `NEW` changelog markers and forbids globs and brace expansion

### 3.2.2 DP-scoped load order (per DP)
- tools/lint/dp.sh — primary implementation target; worker must read the full file before editing, paying particular attention to: the `lint_payload()` function and its existing check call order, the `check_receipt_command_substitution()` and `check_freshness_stamp_format()` functions as structural analogues for the new check, and the `run_test()` harness and `render_fixture_from_template()` function to understand how --test coverage is organized.
- docs/ops/specs/tools/lint/dp.md — spec surface for dp.sh; worker must read the full file to identify the check-list section to update.
- docs/MANUAL.md — operator manual; worker must read the pre-dispatch checklist section to identify the exact entry to update.

## 3.3 Scope and Safety
Objective:
Harden `tools/lint/dp.sh` with a PROPOSED token scan that fires as the first check in `lint_payload()`, before structural validation, so that any intake packet containing an unresolved placeholder emits an early, unambiguous FAIL before the operator hands off to a contractor. Update `docs/MANUAL.md` pre-dispatch guidance and `docs/ops/specs/tools/lint/dp.md` to document the check.

In scope:
The scope is the addition of a PROPOSED-token scan to `tools/lint/dp.sh`. Specifically: a new function `check_proposed_token_scan()` inserted at line 1038 that performs a case-sensitive literal scan for the string `PROPOSED`, prints matching line numbers and content to stderr, and exits with the message `PROPOSED token found — finalize intake packet before dispatch`. That function is called as the first check inside `lint_payload()` at line 1058, before template and structure validation. The `--test` flag coverage was extended at lines 1217 and 1326 with a negative fixture asserting PROPOSED-token scan failure. Documentation was updated in `docs/ops/specs/tools/lint/dp.md` (lines 11 and 17) and operator guidance was updated in `docs/MANUAL.md` (line 181). The allowlist at `storage/dp/active/allowlist.txt` was rewritten to cover the four changed files plus the planned closing sidecar and archive surfaces keyed to hash `a065eec6b`. Total diff: 4 files, 48 insertions, 28 deletions.

Out of scope:
- `ops/bin/certify` — no changes; fallback logic is unchanged.
- `tools/lint/dp.sh` structural validation logic — no changes beyond the PROPOSED scan addition and the --test harness extension.
- Any new binary (`ops/bin/finalize` or similar) — explicitly excluded; the dp.sh check is sufficient per OQ-5.
- `CONTRACTOR-NOTES.md` surface formalization — moved to DP-OPS-0117; no action here.
- Any other lint gate — no changes.
- `ops/src/surfaces/dp.md.tpl` — no changes; `CANONICAL_DP_TEMPLATE_SHA256` in dp.sh is unchanged.

Safety and invariants:
- No manual edits to generated outputs.
- No structural edits to TASK or the DP boilerplate.
- The allowlist hard gate enforces the exact file set; no unlisted file may be modified.
- The PROPOSED scan must be strictly additive: it must not alter any existing check behavior or check ordering beyond inserting itself first in `lint_payload()`.
- The CANONICAL_DP_TEMPLATE_SHA256 constant in dp.sh must not be changed; dp.md.tpl is not in scope.
- The --test harness must continue to emit `OK: --test passed` on a valid fixture with no PROPOSED tokens.
- All existing lint gates must continue to pass after edits.

Worker Constraints (SSOT injected):
## Section 1: Universal Template Rules
- Template source uses `tpl` files with optional YAML frontmatter
- Canon frontmatter keys: `template_type` `template_id` `template_version` `requires_slots` `includes`
- Renderer strips frontmatter before output write
- Slot token form `\{\{TOKEN\}\}` with uppercase alphanumeric underscore
- Include forms `\{\{@include:path\}\}` and `\{\{@include:path#section\}\}`
- Include resolution is strict: missing file fail missing section fail circular graph fail
- Strict mode default: every required slot value present and no unresolved token
- Non strict mode allowed only for lint and normalization workflows
- Worker facing generated surfaces remain pointer first and exclude disposable artifacts

Target Files allowlist (hard gate):
- storage/dp/active/allowlist.txt

## 3.4 Execution Plan (A-E)

### 3.4.1 State
Branch `main` is at HEAD `a065eec6b`, the merge commit for DP-OPS-0111 (Sidecar Schema Alignment). Working tree is clean with zero porcelain entries. No active DP is in progress. `tools/lint/dp.sh` currently validates intake packets through template hash, structure hash, required field, allowlist pointer integrity, dump selection scope, freshness stamp format, and receipt command substitution checks — in that order inside `lint_payload()`. No PROPOSED token scan exists. `docs/ops/specs/tools/lint/dp.md` exists and documents the dp.sh check list. `docs/MANUAL.md` exists and contains a pre-dispatch checklist section referencing `dp.sh`.

### 3.4.2 Request
Add a PROPOSED token scan to `tools/lint/dp.sh` as the first check in `lint_payload()`. The scan must: iterate over the lines of the target packet file; for each line containing the literal string `PROPOSED` (case-sensitive), print the line number and line content to stderr; after the scan, if any matching lines were found, call `fail()` with the message "PROPOSED token found — finalize intake packet before dispatch" and return. Extend `run_test()` to assert the new scan fails on a PROPOSED-containing fixture. Update `docs/MANUAL.md` pre-dispatch checklist to remove the separate finalize step and state that `bash tools/lint/dp.sh` PASS is sufficient. Update `docs/ops/specs/tools/lint/dp.md` check list to document the PROPOSED token scan.

### 3.4.3 Changelog
- UPDATE: `tools/lint/dp.sh` — add `check_proposed_token_scan()` function and insert call as first check in `lint_payload()`; extend `run_test()` with PROPOSED-token negative fixture.
- UPDATE: `docs/MANUAL.md` — update pre-dispatch checklist entry to reflect that `bash tools/lint/dp.sh` PASS is the single required step; no separate finalize step.
- UPDATE: `docs/ops/specs/tools/lint/dp.md` — add PROPOSED token scan to the documented check list.
- UPDATE: `storage/dp/active/allowlist.txt` — include all three files above plus certify-generated ledger and TASK archive surfaces.

### 3.4.4 Patch / Diff
1. Read `tools/lint/dp.sh` in full before making any edits. Identify the exact line where `lint_payload()` begins and the line where `check_template_hash_preflight` is called (currently the first statement in `lint_payload()`). Identify the `run_test()` function boundaries and the existing negative fixture pattern for later extension.

2. Read `docs/ops/specs/tools/lint/dp.md` in full. Identify the section that lists the checks dp.sh performs.

3. Read `docs/MANUAL.md` in full. Identify the pre-dispatch checklist section and the specific bullet or step that currently instructs the operator to run `bash tools/lint/dp.sh` (and any separate finalize step referenced there).

4. In `tools/lint/dp.sh`, immediately before the definition of `lint_payload()`, add the following new function:
```
   check_proposed_token_scan() {
     local path="$1"
     local found=0
     local lineno=0
     while IFS= read -r line || [[ -n "$line" ]]; do
       lineno=$(( lineno + 1 ))
       if [[ "$line" == *PROPOSED* ]]; then
         echo "PROPOSED token at line ${lineno}: ${line}" >&2
         found=1
       fi
     done < "$path"
     if (( found )); then
       fail "PROPOSED token found — finalize intake packet before dispatch"
     fi
   }
```

5. In `tools/lint/dp.sh`, inside `lint_payload()`, insert a call to `check_proposed_token_scan "$path"` as the very first statement, before the existing `check_template_hash_preflight` call. The resulting call order inside `lint_payload()` must be:
```
   check_proposed_token_scan "$path"
   check_template_hash_preflight
   check_structure_hash "$path"
   check_required_fields "$path"
   check_allowlist_pointer_integrity "$path"
   check_dump_selection_scope "$path"
   check_freshness_stamp_format "$path"
   check_receipt_command_substitution "$path"
```

6. In `tools/lint/dp.sh`, inside `run_test()`, after the existing negative fixture for `tmp_results_invalid` (and before the final `rm -f` cleanup line), add a PROPOSED-token negative fixture: copy `$tmp_valid` to a new temporary file, insert a line containing the literal string `PROPOSED` into the copy using `sed` or `echo`, assert that `DP_ALLOWLIST_POINTER_OVERRIDE="$tmp_allowlist_valid" lint_path` on the modified copy fails (exits non-zero), and emit `FAIL: --test expected PROPOSED token scan failure` to stderr and `exit 1` if it unexpectedly passes. Add the new temporary file to the `rm -f` cleanup line.

7. In `docs/ops/specs/tools/lint/dp.md`, in the section documenting the checks dp.sh performs, add an entry for the PROPOSED token scan. State: the scan fires as the first check, before template hash verification and structural validation; it reports each matching line number and line content on stderr; it emits FAIL and exits non-zero if any line in the packet contains the literal string `PROPOSED`.

8. In `docs/MANUAL.md`, in the pre-dispatch checklist section, update the entry for `bash tools/lint/dp.sh` to: run `bash tools/lint/dp.sh` on the intake packet path and confirm PASS. A PASS result confirms that no `PROPOSED` tokens remain in the packet and that the packet is structurally well-formed. No separate finalize step is required. Remove any reference to a separate finalize step or binary from this checklist entry if one exists.

9. Update `storage/dp/active/allowlist.txt` to include exactly the following persistent repo-state files for this DP:
   - `tools/lint/dp.sh`
   - `docs/MANUAL.md`
   - `docs/ops/specs/tools/lint/dp.md`
   - `storage/dp/active/allowlist.txt`
   - `storage/handoff/CLOSING-DP-OPS-0112.md`
   - `archives/surfaces/SoP-2026-02-25-a065eec6b.md`
   - `archives/surfaces/PoW-2026-02-25-a065eec6b.md`
   - `archives/surfaces/TASK-DP-OPS-0112-a065eec6b.md`

   Note: The three `archives/surfaces/` entries are the certify-generated ledger and TASK archive surfaces. Their exact filenames are determined by certify at certification time and must be confirmed against certify output before closeout. Update the allowlist entries to match certify's actual output if the hash suffix differs from `a065eec6b`.

### 3.4.5 Receipt (Proofs to collect) - MUST RUN

**Mandatory receipt commands (always run; do not omit):**
- bash tools/lint/dp.sh TASK.md
- bash tools/lint/task.sh
- bash tools/lint/integrity.sh
- bash tools/lint/style.sh
- git diff --name-only
- git diff --stat
- comm -23 <(git diff --name-only | sort) <(sort storage/dp/active/allowlist.txt) || true
- comm -23 <(git ls-files --others --exclude-standard | sort) <(sort storage/dp/active/allowlist.txt) || true
- ./ops/bin/open

**DP-specific receipt commands (scope-specific; author below):**
Note: Command substitution forms (e.g., $(pwd), $(git ...)) are rejected by certify replay. Use literal values only. dp.sh enforces at lint time.
Note: Command substitution forms (e.g., $(pwd), $(git ...)) are rejected by certify replay. Use literal values only. dp.sh enforces at lint time.
- bash tools/lint/dp.sh --test
- grep -n "check_proposed_token_scan" tools/lint/dp.sh
- grep -n "PROPOSED token found" tools/lint/dp.sh
- grep -n "PROPOSED" docs/MANUAL.md
- grep -n "PROPOSED" docs/ops/specs/tools/lint/dp.md

## 3.5 Closeout (Mandatory Routing)
- Execute docs/MANUAL.md Closeout Cycle in order (Verify, Harvest, Refresh, Log, Prune).
- Update SoP.md and PoW.md with DP entries, including objective summary and verification commands run.
- Protocol order for closeout: Verify -> Generate Results -> COMMIT (Operator Only) -> Prune.
- Run prune hygiene: ./ops/bin/prune --scrub.
- Regenerate session artifacts: `./ops/bin/open --out=auto`
- Capture updated platform state: `./ops/bin/dump --scope=platform --format=chatgpt --out=auto`
- Ensure the next session begins with refreshed session artifacts and matching receipts.

### 3.5.1 Mandatory Closing Block
Closing block content is generated exclusively by `ops/bin/certify` from the closing sidecar at `storage/handoff/CLOSING-DP-OPS-0112.md` at certification time. Do not author, predict, populate, or approximate any closing block field at draft time or during execution. The closing sidecar is the only human-authored narrative input certify ingests. Certify generates all of the following from observable repository state: Primary Commit Header, Pull Request Title, Pull Request Description, Final Squash Stub, Extended Technical Manifest, and Review Conversation Starter.

Before running certify, confirm `storage/handoff/CLOSING-DP-OPS-0112.md` has been maintained throughout execution and reflects observable reality only.

Fields certify generates from the closing sidecar and repository state:
- Primary Commit Header
- Pull Request Title
- Pull Request Description
- Final Squash Stub
- Extended Technical Manifest
- Review Conversation Starter
