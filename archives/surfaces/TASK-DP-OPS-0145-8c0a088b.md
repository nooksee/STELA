---
trace_id: stela-20260303T060933Z-3b0d65fe
packet_id: DP-OPS-0145
created_at: 2026-03-03T12:18:44Z
previous: archives/surfaces/TASK-DP-OPS-0144-2d8df85d.md
---
# STELA TASK DASHBOARD
Status: ACTIVE
Owner: Integrator
Last Updated: 2026-03-02

## 1. Session State
Pointer: Session context output (generated by ops/bin/open)
Context Manifest: ops/lib/manifests/CONTEXT.md (checked by tools/lint/context.sh)

Session State contract:
- Session State is pointer-first. Do not write inline branch or hash state here.
- OPEN and OPEN-PORCELAIN are the session state snapshot.
- OPEN-PORCELAIN is emitted on every OPEN run. Clean sessions produce a zero-line artifact.
- RESULTS is the audit record for the DP.
- TASK is the stable routing surface.
- Operator note (2026-02-17): Do not edit `SoP.md` or `PoW.md` unless explicitly instructed.

## 2. Logic Pointers
Primary Constraint: PoT.md (Policy of Truth).
Surface Contract Spec: docs/ops/specs/surfaces/task.md.

### 2.1 Governance Pointers
Jurisdiction: PoT.md Section 3.
Git Authority: PoT.md Section 4.1.
Behavioral Standard: PoT.md Section 4.2.
Staffing Protocol: PoT.md Section 4.1.

### 2.2 Enforcement and Gates
Style: tools/lint/style.sh
Context: tools/lint/context.sh
Truth: tools/lint/truth.sh
DP lint: tools/lint/dp.sh (enforces DP transaction rules in Section 3; does not enforce TASK container schema)
TASK lint: tools/lint/task.sh (sole TASK surface/container schema enforcer)
Repo verify: tools/verify.sh
llms parity: tools/lint/llms.sh

## 3. Current Dispatch Packet (DP)

Section-by-section authoring protocol:
- Generate DP structure from `ops/src/surfaces/dp.md.tpl` using `ops/bin/draft`.
- Do not hand-edit DP structural boilerplate in `TASK.md` after generation.
- Manual edits are limited to slot content only (objective, scope, safety, plan body, receipt body).
- Freeze each completed subsection in order (3.1 -> 3.2 -> 3.3 -> 3.4.x).
- Do not revise earlier sections to justify later work. If scope changes, end the thread and draft a new DP.

Freeze Rule:
- If a completed DP subsection changes later, append an AMENDMENT block in RESULTS; otherwise DP is FAIL.

AMENDMENT: minimum fields
- Reopened section:
- Reason:
- Impact:
- Allowlist change (none or exact paths):

Input discipline (hard rule):
- Worker receives only the DP text. Worker does not require OPEN or DUMP attachments.
- DP writer must not attach or cite disposable artifacts (rough notes, chat logs, scratch drafts, throwaway excerpts).
- DP writer must not include pasted bundles (OPEN, DUMP payloads, manifests) inside the DP body.
- Canon pointers only. Evidence belongs in storage artifacts and RESULTS.


### DP-OPS-0144: Remove Mandatory Closing Block From RESULTS While Keeping Closing Sidecar Enforcement

## 3.1 Freshness Gate (Must Pass Before Work)
Base Branch: main
Required Work Branch: work/results-closing-sidecar-2026-03-02
Base HEAD: 2d8df85d
Freshness Stamp: 2026-03-02
Note: FRESHNESS_STAMP must be YYYY-MM-DD format only. No trace tokens, no timestamps, no other text. Certify rejects all other forms. dp.sh enforces at lint time.

Required local re-check (worker runs; paste outputs in RESULTS):
- git rev-parse --abbrev-ref HEAD
- git rev-parse --short HEAD
- git status --porcelain

Preconditions:
- No commits on main.
- Working tree must be clean before execution begins.
- If Base HEAD changes, regenerate session artifacts from canonical tools before proceeding.

STOP conditions:
- STOP if any mismatch (branch, Base HEAD, missing work branch).
- STOP if working tree is dirty before execution begins.
- STOP if told to create or switch branches.

## 3.1.1 DP Preflight Gate (Run Before Any Edits)
Purpose:
- Catch malformed DP or TASK schema before work begins.

Worker runs (paste outcome in RESULTS):
- bash tools/lint/dp.sh --test
- bash tools/lint/dp.sh TASK.md
- bash tools/lint/task.sh

STOP if any preflight check fails.

### CbC Design Discipline Preflight (TASK.md §3.1.1)
This DP modifies closeout enforcement tooling (`ops/bin/certify`, `tools/lint/results.sh`, `tools/lint/task.sh`, `tools/lint/dp.sh`) and template surfaces to remove the RESULTS Mandatory Closing Block while preserving strict closing sidecar validation as a hard gate.
Structural prevention is viable and proportionate here because the RESULTS schema change is enforced by construction via the generator (`ops/bin/certify` + `ops/src/surfaces/results.md.tpl`) and validated deterministically by lints, eliminating RESULTS-side embedding and drift risk without relaxing sidecar enforcement.


## 3.2 Required Context Load (Read Before Doing Anything)

### 3.2.1 Canon load order (always)
Worker must confirm loaded before edits begin:
1. PoT.md
2. SoP.md
3. PoW.md
4. TASK.md
5. docs/MAP.md
6. docs/MANUAL.md
7. ops/lib/manifests/CONTEXT.md

Notes:
- Worker does not read OPEN. OPEN is for Integrator state refresh and for receipts.
- Disposable artifacts must not be referenced or included.
- Worker input is the DP text only.
- DP writer must not attach or cite disposable artifacts.
- DP writer must not embed pasted bundles.

<!-- CCD: ff_target="machine-dense" ff_band="30-40" -->
# Contractor Brief

1 Temp path rule uses `var/tmp` only
2 RESULTS generation rule uses `ops/bin/certify` only
3 Receipt rule runs every DP mandated command and treats any skip as STOP
4 Disposable artifact rule treats OPEN dump bundles and manifests as non canonical session artifacts
5 Use repo-relative paths only in all output. Never print absolute filesystem paths. Do not emit clickable file links.

### 3.2.2 DP-scoped load order (per DP)
1. ops/src/surfaces/results.md.tpl
2. ops/bin/certify
3. tools/lint/results.sh
4. tools/lint/task.sh
5. tools/lint/dp.sh
6. ops/src/surfaces/dp.md.tpl
7. ops/src/surfaces/task.md.tpl
8. ops/lib/manifests/CLOSING.md
9. docs/ops/specs/surfaces/results.md
10. docs/ops/specs/binaries/certify.md
11. docs/ops/specs/tools/lint/results.md
12. docs/ops/specs/surfaces/task.md
13. docs/ops/specs/tools/lint/task.md
14. docs/ops/specs/tools/lint/dp.md
15. docs/ops/registry/lint.md
16. docs/ops/prompts/e-prompt-01.md
17. docs/ops/prompts/e-prompt-03.md
18. docs/DESIGN.md
19. docs/MANUAL.md

## 3.3 Scope and Safety
Objective:
Remove the Mandatory Closing Block from newly generated RESULTS receipts while preserving strict closing sidecar enforcement in certify and lint flows.
Rename §3.5.1 terminology in DP and TASK surfaces to Mandatory Closing Sidecar and enforce the new heading in tools/lint/task.sh with same-packet TASK regeneration.

In scope:
- ops/src/surfaces/results.md.tpl (UPDATE)
- ops/bin/certify (UPDATE)
- tools/lint/results.sh (UPDATE)
- tools/lint/task.sh (UPDATE)
- ops/src/surfaces/dp.md.tpl (UPDATE)
- ops/src/surfaces/task.md.tpl (UPDATE)
- tools/lint/dp.sh (UPDATE)
- docs/ops/specs/surfaces/results.md (UPDATE)
- docs/ops/specs/binaries/certify.md (UPDATE)
- docs/ops/specs/tools/lint/results.md (UPDATE)
- docs/ops/specs/surfaces/task.md (UPDATE)
- docs/ops/specs/tools/lint/task.md (UPDATE)
- docs/ops/specs/tools/lint/dp.md (UPDATE)
- docs/MANUAL.md (UPDATE)
- docs/ops/prompts/e-prompt-01.md (UPDATE)
- docs/ops/prompts/e-prompt-03.md (UPDATE)
- docs/ops/registry/lint.md (UPDATE)
- docs/DESIGN.md (UPDATE)
- storage/dp/active/allowlist.txt (UPDATE)
- SoP.md (UPDATE)
- PoW.md (UPDATE)
- TASK.md (UPDATE)
- llms.txt (UPDATE)
- llms-core.txt (UPDATE)
- llms-full.txt (UPDATE)
- ops/lib/manifests/OPS.md (UPDATE)

Out of scope:
- Any historical RESULTS migration or rewrite.
- Any change to the six-label SSOT in ops/lib/manifests/CLOSING.md.
- Any relaxation of closing sidecar validation strictness or failure semantics.
- Any unrelated refactors outside the listed files.

Safety and invariants:
- No manual edits to generated outputs (RESULTS, OPEN, DUMP, archived leaves). Generated artifacts must only be produced by canonical tools.
- No structural edits to TASK.md or DP boilerplate. Structure changes occur through template updates plus ops/bin/draft regeneration.
- Allowlist is a hard gate: all tracked modifications must be covered by storage/dp/active/allowlist.txt.
- Forward-only schema change: do not retrofit historical receipts.
- Preserve strict closing sidecar validation in ops/bin/certify as a hard stop gate after RESULTS embedding removal.

Worker Constraints (SSOT injected):
## Section 1: Universal Template Rules
- Template source uses `tpl` files with optional YAML frontmatter
- Canon frontmatter keys: `template_type` `template_id` `template_version` `requires_slots` `includes`
- Renderer strips frontmatter before output write
- Slot token form `\{\{TOKEN\}\}` with uppercase alphanumeric underscore
- Include forms `\{\{@include:path\}\}` and `\{\{@include:path#section\}\}`
- Include resolution is strict: missing file fail missing section fail circular graph fail
- Strict mode default: every required slot value present and no unresolved token
- Non strict mode allowed only for lint and normalization workflows
- Worker facing generated surfaces remain pointer first and exclude disposable artifacts

Target Files allowlist (hard gate):
- storage/dp/active/allowlist.txt

## 3.4 Execution Plan (A-E)

### 3.4.1 State
- RESULTS template currently includes a Mandatory Closing Block section and requires a CLOSING_BLOCK slot.
- Certify currently validates sidecar content and also embeds full sidecar text into RESULTS with parity checks.
- results.sh currently enforces Mandatory Closing Block heading and six-label populated content.
- task.sh and DP/TASK templates currently use Mandatory Closing Block terminology in §3.5.1.
- dp.sh --test currently uses RESULTS fixtures that include a Mandatory Closing Block section.

### 3.4.2 Request
- Update RESULTS schema to end at Contractor Execution Narrative and render no closing-sidecar text.
- Keep strict closing sidecar validation as certify-internal hard gating while removing RESULTS embedding/parity checks and closing validation text from RESULTS output.
- Update results.sh to enforce the new schema and tolerate legacy receipts with extra trailing closing-block sections.
- Rename §3.5.1 to Mandatory Closing Sidecar across templates/specs/lints and enforce new heading in task.sh.
- Align dp.sh fixtures and docs/prompts/registry text with the new contract.

### 3.4.3 Changelog
NEW:
- storage/handoff/CLOSING-DP-OPS-0144.md

UPDATE:
- ops/src/surfaces/results.md.tpl
- ops/bin/certify
- tools/lint/results.sh
- tools/lint/task.sh
- ops/src/surfaces/dp.md.tpl
- ops/src/surfaces/task.md.tpl
- tools/lint/dp.sh
- docs/ops/specs/surfaces/results.md
- docs/ops/specs/binaries/certify.md
- docs/ops/specs/tools/lint/results.md
- docs/ops/specs/surfaces/task.md
- docs/ops/specs/tools/lint/task.md
- docs/ops/specs/tools/lint/dp.md
- docs/MANUAL.md
- docs/ops/prompts/e-prompt-01.md
- docs/ops/prompts/e-prompt-03.md
- docs/ops/registry/lint.md
- docs/DESIGN.md
- storage/dp/active/allowlist.txt
- SoP.md
- PoW.md
- TASK.md
- llms.txt
- llms-core.txt
- llms-full.txt
- ops/lib/manifests/OPS.md

### 3.4.4 Patch / Diff
1. Update ops/src/surfaces/results.md.tpl to remove CLOSING_BLOCK slot and remove the Mandatory Closing Block section.
2. Update ops/bin/certify to keep strict sidecar validation hard gates while removing all RESULTS sidecar embedding, parity checks, and sidecar-validation text from RESULTS output.
3. Update tools/lint/results.sh required headings and narrative extraction to match the new schema and remove closing-block parsing/enforcement.
4. Update tools/lint/task.sh to enforce the §3.5.1 heading text Mandatory Closing Sidecar.
5. Update ops/src/surfaces/dp.md.tpl and ops/src/surfaces/task.md.tpl terminology and closeout routing text to sidecar language.
6. Update tools/lint/dp.sh --test fixtures so valid RESULTS omit closing block section and invalid RESULTS fail via missing Decision Leaf line.
7. Update canonical specs/docs/prompts/registry text to remove closing-block-in-RESULTS claims and align terminology.
8. Regenerate TASK.md in the same packet via ops/bin/draft with this slots file.
9. Update storage/dp/active/allowlist.txt to include all changed files.

### 3.4.5 Receipt (Proofs to collect) - MUST RUN

**Mandatory receipt commands (always run; do not omit):**
- bash tools/lint/dp.sh TASK.md
- bash tools/lint/task.sh
- bash tools/lint/integrity.sh
- bash tools/lint/style.sh
- git diff --name-only
- git diff --stat
- comm -23 <(git diff --name-only | sort) <(sort storage/dp/active/allowlist.txt) || true
- comm -23 <(git ls-files --others --exclude-standard | sort) <(sort storage/dp/active/allowlist.txt) || true
- ./ops/bin/open

**DP-specific receipt commands (scope-specific; author below):**
Note: Command substitution forms (e.g., $(pwd), $(git ...)) are rejected by certify replay. Use literal values only. dp.sh enforces at lint time.
- bash -lc 'grep -n "^## Contractor Execution Narrative$" storage/handoff/DP-OPS-0144-RESULTS.md'
- bash -lc '! rg -q "^## Mandatory Closing Block$" storage/handoff/DP-OPS-0144-RESULTS.md'
- bash -lc '! rg -q "^CLOSING Manifest Validation:" storage/handoff/DP-OPS-0144-RESULTS.md'

## 3.5 Closeout (Mandatory Routing)
- Execute docs/MANUAL.md Closeout Cycle in order (Verify, Harvest, Refresh, Log, Prune).
- Update SoP.md and PoW.md with DP entries, including objective summary and verification commands run.
- Protocol order for closeout: Verify -> Generate Results -> COMMIT (Operator Only) -> Prune.
- Run prune hygiene: ./ops/bin/prune --scrub.
- Regenerate session artifacts: `./ops/bin/open --out=auto`
- Capture updated Ops state: `./ops/bin/dump --scope=core --format=chatgpt --out=auto`
- Ensure the next session begins with refreshed session artifacts and matching receipts.
- Refresh side-effect: `ops/bin/llms` regenerates `ops/lib/manifests/OPS.md` as a compile event; if `OPS.md` is not in the allowlist, restore it before running `integrity.sh`. See `docs/MANUAL.md` Refresh side-effect notice for the full procedure.

### 3.5.1 Mandatory Closing Sidecar
Closing sidecar content is generated and maintained at `storage/handoff/CLOSING-DP-OPS-0144.md` and is validated by `ops/bin/certify` as a hard gate at certification time. Do not author, predict, populate, or approximate any sidecar-derived closeout output outside this sidecar.
Certify separately collects contractor-authored narrative for the RESULTS Contractor Execution Narrative section at certify time via interactive editor prompt; this narrative input is independent from closing sidecar content.

Before running certify, confirm `storage/handoff/CLOSING-DP-OPS-0144.md` has been maintained throughout execution and reflects observable reality only.

Closing sidecar fields required at certify time:
- Commit Message
- Create Pull Request (Title)
- Create Pull Request (Description)
- Confirm Merge (Commit Message)
- Confirm Merge (Extended Description)
- Confirm Merge (Add a Comment)
