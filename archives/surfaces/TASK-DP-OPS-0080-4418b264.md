---
trace_id: stela-20260220T141519Z-3c7827b1
packet_id: DP-OPS-0080
created_at: 2026-02-20T14:16:58Z
previous: (none)
---
# STELA TASK DASHBOARD
Status: ACTIVE
Owner: Integrator
Last Updated: 2026-02-18

## 1. Session State
Pointer: Session context output (generated by ops/bin/open)
Context Manifest: ops/lib/manifests/CONTEXT.md (checked by tools/lint/context.sh)

Session State contract:
- Session State is pointer-first. Do not write inline branch or hash state here.
- OPEN and OPEN-PORCELAIN are the session state snapshot.
- OPEN-PORCELAIN is emitted on every OPEN run. Clean sessions produce a zero-line artifact.
- RESULTS is the audit record for the DP.
- TASK is the stable routing surface.
- Operator note (2026-02-17): Do not edit `SoP.md` or `PoW.md` unless explicitly instructed.

## 2. Logic Pointers
Primary Constraint: PoT.md (Policy of Truth).
Surface Contract Spec: docs/ops/specs/surfaces/task.md.

### 2.1 Governance Pointers
Jurisdiction: PoT.md Section 3.
Git Authority: PoT.md Section 4.1.
Behavioral Standard: PoT.md Section 4.2.
Staffing Protocol: PoT.md Section 4.1.

### 2.2 Enforcement and Gates
Style: tools/lint/style.sh
Context: tools/lint/context.sh
Truth: tools/lint/truth.sh
DP lint: tools/lint/dp.sh (enforces DP transaction rules in Section 3; does not enforce TASK container schema)
TASK lint: tools/lint/task.sh (sole TASK surface/container schema enforcer)
Repo verify: tools/verify.sh
llms parity: tools/lint/llms.sh

## 3. Current Dispatch Packet (DP)

Section-by-section authoring protocol:
- Generate DP structure from `ops/src/surfaces/dp.md.tpl` using `ops/bin/draft`.
- Do not hand-edit DP structural boilerplate in `TASK.md` after generation.
- Manual edits are limited to slot content only (objective, scope, safety, plan body, receipt body).
- Freeze each completed subsection in order (3.1 -> 3.2 -> 3.3 -> 3.4.x).
- Do not revise earlier sections to justify later work. If scope changes, end the thread and draft a new DP.

Freeze Rule:
- If a completed DP subsection changes later, append an AMENDMENT block in RESULTS; otherwise DP is FAIL.

AMENDMENT: minimum fields
- Reopened section:
- Reason:
- Impact:
- Allowlist change (none or exact paths):

Input discipline (hard rule):
- Worker receives only the DP text. Worker does not require OPEN or DUMP attachments.
- DP writer must not attach or cite disposable artifacts (rough notes, chat logs, scratch drafts, throwaway excerpts).
- DP writer must not include pasted bundles (OPEN, DUMP payloads, manifests) inside the DP body.
- Canon pointers only. Evidence belongs in storage artifacts and RESULTS.

### DP-OPS-0080: DP Workflow Upgrade — Option A Bundle

## 3.1 Freshness Gate (Must Pass Before Work)
Base Branch: main
Required Work Branch: work/dp-ops-0080
Base HEAD: 4418b264
Freshness Stamp: 2026-02-20

Required local re-check (worker runs; paste outputs in RESULTS):
- git rev-parse --abbrev-ref HEAD
- git rev-parse --short HEAD
- git status --porcelain

Preconditions:
- No commits on main.
- Working tree must be clean before execution begins.
- If Base HEAD changes, regenerate session artifacts from canonical tools before proceeding.

STOP conditions:
- STOP if any mismatch (branch, Base HEAD, missing work branch).
- STOP if working tree is dirty before execution begins.
- STOP if told to create or switch branches.

## 3.1.1 DP Preflight Gate (Run Before Any Edits)
Purpose:
- Catch malformed DP or TASK schema before work begins.

Worker runs (paste outcome in RESULTS):
- bash tools/lint/dp.sh --test
- bash tools/lint/dp.sh TASK.md
- bash tools/lint/task.sh

STOP if any preflight check fails.

## 3.2 Required Context Load (Read Before Doing Anything)

### 3.2.1 Canon load order (always)
Worker must confirm loaded before edits begin:
1. PoT.md
2. SoP.md
3. PoW.md
4. TASK.md
5. docs/MAP.md
6. docs/MANUAL.md
7. ops/lib/manifests/CONTEXT.md

Notes:
- Worker does not read OPEN. OPEN is for Integrator state refresh and for receipts.
- Disposable artifacts must not be referenced or included.
- Worker input is the DP text only.
- DP writer must not attach or cite disposable artifacts.
- DP writer must not embed pasted bundles.

# Contractor Brief

**1. Temporary file paths.** Use `var/tmp/` for all temporary files within this repository.
The path `/tmp` is not valid within the Stela repo. `PoT.md §1.1` defines Resume storage;
all temporary writes must target that repository-defined location.

**2. RESULTS receipt generation.** `ops/bin/certify` is the only permitted generator of
RESULTS receipts. Manual assembly of receipt content is prohibited. `PoT.md §4.2` governs.

**3. Receipt commands are mandatory.** When the DP lists mandatory receipt commands (whether
in a `RECEIPT_COMMANDS` slot or in the hardened mandatory stub block), every command in that
block must be executed. No listed command is optional. Skipping any listed command is a STOP
condition.

**4. Disposable artifact prohibition.** OPEN files, dump bundles, and manifest files are
session artifacts. They must not be cited in DP content, committed to the repository, or
referenced in RESULTS as canonical sources.

**5. No invented paths.** All file paths referenced in execution steps must either exist in
the repo dump or be explicitly marked `NEW` in the DP changelog. Pattern paths, globs, and
brace expansions are prohibited.

### 3.2.2 DP-scoped load order (per DP)
- ops/bin/dump
- ops/src/surfaces/dp.md.tpl
- ops/lib/manifests/CONSTRAINTS.md
- tools/lint/style.sh
- docs/ops/prompts/E-PROMPT-01.md
- docs/ops/prompts/E-PROMPT-02.md
- docs/ops/prompts/E-PROMPT-03.md
- docs/ops/prompts/E-PROMPT-04.md
- docs/ops/specs/binaries/certify.md
- docs/ops/specs/surfaces/results.md

## 3.3 Scope and Safety
Objective:
Eliminate three DP workflow failure categories by delivering four additive updates: a dump
orientation preamble, a Contractor manifest wired into DP context, hardened mandatory receipt
stubs with a scoped `RECEIPT_EXTRA` slot, and closing-block authoring guidance with style lint
lead-verb enforcement across the four E-PROMPT operator surfaces.

In scope:
- storage/dp/active/allowlist.txt
- ops/bin/dump
- ops/bin/certify
- ops/lib/manifests/CONTRACTOR.md
- ops/src/surfaces/dp.md.tpl
- SoP.md
- tools/lint/style.sh
- docs/ops/prompts/E-PROMPT-01.md
- docs/ops/prompts/E-PROMPT-02.md
- docs/ops/prompts/E-PROMPT-03.md
- docs/ops/prompts/E-PROMPT-04.md
- ops/bin/draft
- tools/lint/dp.sh
- tools/lint/task.sh

Out of scope:
- ops/lib/manifests/CONSTRAINTS.md
- tools/lint/integrity.sh
- tools/lint/results.sh
- opt/_factory/
- archives/ and generated manifests except closeout-generated leaves

Safety and invariants:
- No commits on main; all work remains on `work/dp-ops-0080`.
- No manual edits to generated outputs (RESULTS, OPEN, dumps, manifests, llms bundles).
- No manual structural edits to TASK DP boilerplate; use canonical render/draft flow.
- Allowlist hard gate: every persistent mutated path is present in `storage/dp/active/allowlist.txt`.
- Runtime artifact prefixes (`storage/handoff/`, `storage/dumps/`, intake/processed paths) are forbidden in allowlist entries.
- STOP on any command failure.

Worker Constraints (SSOT injected):
## Section 1: Universal Template Rules
- Templates are authored as `.tpl` files and may begin with YAML frontmatter.
- Frontmatter keys are canonical and machine-read:
  - `template_type`
  - `template_id`
  - `template_version`
  - `requires_slots`
  - `includes`
- Rendered output must strip YAML frontmatter before writing output.
- Slot tokens are `\{\{TOKEN\}\}` where `TOKEN` is uppercase alphanumeric with underscores.
- Include directives are supported in template body content:
  - `\{\{@include:path\}\}`
  - `\{\{@include:path#section\}\}`
- Include resolution is deterministic and strict:
  - Missing files fail render.
  - Missing section anchors fail render.
  - Circular include graphs fail render.
- Strict mode is default for worker-facing output:
  - Every `requires_slots` token must be provided.
  - Unresolved `\{\{TOKEN\}\}` placeholders fail render.
- Non-strict mode is allowed only for linting and normalization workflows.
- Generated worker-facing surfaces must remain pointer-first and must not embed disposable artifacts.

Target Files allowlist (hard gate):
- storage/dp/active/allowlist.txt

## 3.4 Execution Plan (A-E)

### 3.4.1 State
- Branch: `work/dp-ops-0080` at base HEAD `4418b264`.
- Prior closed packet pointer is active in TASK head; this DP reinstates an active DP-OPS-0080 block.
- Worker scope bundles Option A workflow upgrades plus lint/hash compatibility needed by template v2.

### 3.4.2 Request
- Apply Step A through Step D in order with per-step verification gates.
- Keep mandatory receipt command stubs template-owned and place only scope-specific checks in `RECEIPT_EXTRA`.
- Produce closeout sidecar content, then generate RESULTS with `ops/bin/certify`.

### 3.4.3 Changelog
- UPDATE `storage/dp/active/allowlist.txt`
- UPDATE `ops/bin/dump`
- NEW `ops/lib/manifests/CONTRACTOR.md`
- UPDATE `ops/src/surfaces/dp.md.tpl`
- UPDATE `SoP.md`
- UPDATE `tools/lint/style.sh`
- UPDATE `docs/ops/prompts/E-PROMPT-01.md`
- UPDATE `docs/ops/prompts/E-PROMPT-02.md`
- UPDATE `docs/ops/prompts/E-PROMPT-03.md`
- UPDATE `docs/ops/prompts/E-PROMPT-04.md`
- UPDATE `ops/bin/draft` (support template v2 receipt slot and pointer-head TASK drafting)
- UPDATE `ops/bin/certify` (ignore Section 3.4.5 receipt heading labels while extracting executable commands)
- UPDATE `tools/lint/dp.sh` (template hash and receipt-slot normalization for template v2)
- UPDATE `tools/lint/task.sh` (receipt-contract checks aligned to template-injected mandatory stubs)

### 3.4.4 Patch / Diff
1. Update allowlist entries for all persistent DP-OPS-0080 targets before mutating those files.
2. Insert the fixed orientation preamble in `emit_dump()` between header metadata and payload begin markers.
3. Create `ops/lib/manifests/CONTRACTOR.md` and include it in DP template frontmatter and Section 3.2.
4. Harden DP template Section 3.4.5 with mandatory receipt stubs and `RECEIPT_EXTRA`; set template version to 2.
5. Append `Closing Block Style Guide` to `SoP.md` and add closing lead-word repetition enforcement to `tools/lint/style.sh`.
6. Refresh all four E-PROMPT files to reference Contractor constraints and receipt-stub workflow.
7. Update `ops/bin/draft` for template v2 slot naming and pointer-head TASK drafting compatibility.
8. Align `ops/bin/certify`, `tools/lint/dp.sh`, and `tools/lint/task.sh` with template v2 so command extraction and lint contracts remain deterministic.
9. Run mandatory receipt commands, generate RESULTS via `ops/bin/certify --dp="DP-OPS-0080" --out=auto`, then run prune scrub.

### 3.4.5 Receipt (Proofs to collect) - MUST RUN

**Mandatory receipt commands (always run; do not omit):**
- bash tools/lint/dp.sh TASK.md
- bash tools/lint/task.sh
- bash tools/lint/integrity.sh
- bash tools/lint/style.sh
- git diff --name-only
- git diff --stat
- comm -23 <(git diff --name-only | sort) <(sort storage/dp/active/allowlist.txt) || true
- comm -23 <(git ls-files --others --exclude-standard | sort) <(sort storage/dp/active/allowlist.txt) || true
- ./ops/bin/open

**DP-specific receipt commands (scope-specific; author below):**
- bash -n ops/bin/dump
- ./ops/bin/dump --scope=platform --format=chatgpt | grep -c "DUMP ORIENTATION BEGIN"
- cat ops/lib/manifests/CONTRACTOR.md | grep -c "var/tmp"
- cat ops/lib/manifests/CONTRACTOR.md | grep -c "certify"
- grep -c "CONTRACTOR.md" ops/src/surfaces/dp.md.tpl
- grep -c "RECEIPT_EXTRA" ops/src/surfaces/dp.md.tpl
- grep "RECEIPT_COMMANDS" ops/src/surfaces/dp.md.tpl || echo "CLEAN"
- bash -n tools/lint/style.sh
- grep -c "CONTRACTOR.md" docs/ops/prompts/E-PROMPT-01.md
- grep -c "CONTRACTOR.md" docs/ops/prompts/E-PROMPT-02.md
- grep -c "CONTRACTOR.md" docs/ops/prompts/E-PROMPT-03.md
- grep -c "CONTRACTOR.md" docs/ops/prompts/E-PROMPT-04.md
- grep -c "RECEIPT_EXTRA" docs/ops/prompts/E-PROMPT-03.md

## 3.5 Closeout (Mandatory Routing)
- Execute docs/MANUAL.md Closeout Cycle in order (Verify, Harvest, Refresh, Log, Prune).
- Update SoP.md and PoW.md with DP entries, including objective summary and verification commands run.
- Protocol order for closeout: Verify -> Generate Results -> COMMIT (Operator Only) -> Prune.
- Run prune hygiene: ./ops/bin/prune --scrub.
- Ensure the next session begins with refreshed session artifacts and matching receipts.

### 3.5.1 Mandatory Closing Block
Primary Commit Header (plaintext)

Pull Request Title (plaintext)

Pull Request Description (markdown)

Final Squash Stub (plaintext) (Must differ from #1)

Extended Technical Manifest (plaintext)

Review Conversation Starter (markdown)
