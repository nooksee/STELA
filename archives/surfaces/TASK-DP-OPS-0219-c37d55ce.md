---
trace_id: stela-20260323T150525Z-3ef27b01
packet_id: DP-OPS-0219
created_at: 2026-03-23T15:51:39Z
previous: (none)
---
# STELA TASK DASHBOARD
Status: ACTIVE
Owner: Integrator
Last Updated: 2026-03-19

## 1. Session State
Pointer: Session context output (generated by ops/bin/open)
Context Manifest: ops/lib/manifests/CONTEXT.md (checked by tools/lint/context.sh)

Session State contract:
- Session State is pointer-first. Do not write inline branch or hash state here.
- OPEN is the session state snapshot. OPEN-PORCELAIN is conditional dirty-state detail.
- RESULTS is the audit record for the DP.
- TASK is the stable routing surface.

## 2. Logic Pointers
Primary Constraint: PoT.md (Policy of Truth).
Surface Contract Spec: docs/ops/specs/surfaces/task.md.

### 2.1 Governance Pointers
Jurisdiction: PoT.md Section 3.
Git Authority: PoT.md Section 4.1.
Behavioral Standard: PoT.md Section 4.2.
Staffing Protocol: PoT.md Section 4.1.

### 2.2 Enforcement and Gates
Style: tools/lint/style.sh
Context: tools/lint/context.sh
Truth: tools/lint/truth.sh
DP lint: tools/lint/dp.sh (enforces DP transaction rules in Section 3; does not enforce TASK container schema)
TASK lint: tools/lint/task.sh (sole TASK surface/container schema enforcer)
Repo verify: tools/verify.sh
llms parity: tools/lint/llms.sh

## 3. Current Dispatch Packet (DP)

Section-by-section authoring protocol:
- Generate DP structure from `ops/src/surfaces/dp.md.tpl` using `ops/bin/draft`.
- Do not hand-edit DP structural boilerplate in `TASK.md` after generation.
- Manual edits are limited to slot content only (objective, scope, safety, plan body, receipt body).
- Freeze each completed subsection in order (3.1 -> 3.2 -> 3.3 -> 3.4.x).
- Do not revise earlier sections to justify later work. If scope changes, end the thread and draft a new DP.

Freeze Rule:
- If a completed DP subsection changes later, append an AMENDMENT block in RESULTS; otherwise DP is FAIL.

AMENDMENT: minimum fields
- Reopened section:
- Reason:
- Impact:
- Allowlist change (none or exact paths):

Input discipline (hard rule):
- Worker receives only the DP text. Worker does not require OPEN or DUMP attachments.
- DP writer must not attach or cite disposable artifacts (rough notes, chat logs, scratch drafts, throwaway excerpts).
- DP writer must not include pasted bundles (OPEN, DUMP payloads, manifests) inside the DP body.
- Canon pointers only. Evidence belongs in storage artifacts and RESULTS.



### DP-OPS-0219: Analyst to Planning rename

## 3.1 Freshness Gate (Must Pass Before Work)
Base Branch: main
Required Work Branch: work/dp-ops-0219-analyst-planning-upgrade-2026-03-22
Base HEAD: c37d55ce
Freshness Stamp: 2026-03-23
Note: FRESHNESS_STAMP must be YYYY-MM-DD format only. No trace tokens, no timestamps, no other text. Certify rejects all other forms. dp.sh enforces at lint time.

Required local re-check (worker runs; paste outputs in RESULTS):
- git rev-parse --abbrev-ref HEAD
- git rev-parse --short HEAD
- git status --porcelain

Preconditions:
- No commits on main.
- Working tree must be clean before execution begins.
- If Base HEAD changes, regenerate session artifacts from canonical tools before proceeding.

STOP conditions:
- STOP if any mismatch (branch, Base HEAD, missing work branch).
- STOP if working tree is dirty before execution begins.
- STOP if told to create or switch branches.

## 3.1.1 DP Preflight Gate (Run Before Any Edits)
Purpose:
- Catch malformed DP or TASK schema before work begins.

Worker runs (paste outcome in RESULTS):
- bash tools/lint/dp.sh --test
- bash tools/lint/dp.sh TASK.md
- bash tools/lint/task.sh

STOP if any preflight check fails.

### CbC Design Discipline Preflight (TASK.md §3.1.1)
Required when the DP objective adds, modifies, or replaces a linter, script, guard, or validation binary.
For non-tooling DPs: state "Not applicable" with a one-line justification.
Applicable — DP modifies linter files (response.sh, style.sh). All changes are mechanical renames only. No new logic, no architectural decisions, no cross-cutting behavior changes. Existing contracts are preserved exactly; only identifiers change.

## 3.2 Required Context Load (Read Before Doing Anything)

### 3.2.1 Canon load order (always)
Worker must confirm loaded before edits begin:
1. PoT.md
2. SoP.md
3. PoW.md
4. TASK.md
5. docs/MAP.md
6. docs/MANUAL.md
7. ops/lib/manifests/CONTEXT.md

Notes:
- Worker does not read OPEN. OPEN is for Integrator state refresh and for receipts.
- Disposable artifacts must not be referenced or included.
- Worker input is the DP text only.
- DP writer must not attach or cite disposable artifacts.
- DP writer must not embed pasted bundles.

## Rules

1 Temp path rule uses `var/tmp` only
2 RESULTS generation rule uses `ops/bin/certify` only
3 Receipt rule runs every DP mandated command and treats any skip as STOP
4 Disposable artifact rule treats OPEN dump bundles and manifests as non canonical session artifacts
5 Use repo-relative paths only in all output. Never print absolute filesystem paths. Do not emit clickable file links.

### 3.2.2 DP-scoped load order (per DP)
- storage/handoff/PLAN-FACTORY.md (Analyst to Planning inventory)
- ops/src/stances/analyst.md.tpl
- ops/lib/manifests/BUNDLE.md
- ops/lib/scripts/bundle.sh
- tools/lint/response.sh
- tools/lint/style.sh
- ops/bin/manifest
- ops/bin/template
- tools/test/bundle.sh
- ops/etc/verification.manifest

## 3.3 Scope and Safety
Objective:
Rename all system artifacts that identify the analyst task to "planning": rename ops/src/stances/analyst.md.tpl → planning.md.tpl and docs/ops/specs/stances/analyst.md → planning.md; update all identifiers (profiles, prefixes, function names, mode strings, variable names, manifest keys, test fixtures) that use "analyst" as a task name; update the cross-role strings "direct architect handoff" and "direct architect drafting" → "direct draft handoff" and "direct draft drafting" in planning.md.tpl and the matching style.sh required strings in lockstep.

In scope:
- ops/src/stances/analyst.md.tpl → rename to planning.md.tpl; update template_id: analyst → planning; --profile=analyst → --profile=planning; analyst input source → planning input source; active analyst query → active planning query; Default analyst behavior → Default planning behavior; direct architect handoff → direct draft handoff; direct architect drafting → direct draft drafting (lines 18, 37, and 73); all four For machine-ingest analyst mode: → For machine-ingest planning mode:
- docs/ops/specs/stances/analyst.md → rename to docs/ops/specs/stances/planning.md
- ops/lib/manifests/BUNDLE.md — supported_profiles, auto_default_profile, stance_template_analyst, stance_template_project, artifact_prefix_analyst, dump_scope_analyst, all prose analyst → planning, ANALYST → PLANNING
- ops/lib/scripts/bundle.sh — usage line, analyst) case, all analyst comparisons and ANALYST prefix strings → planning, PLANNING
- tools/lint/response.sh — --mode=analyst, check_analyst_body_scope, all response_analyst_* vars, all EOF_ANALYST_* heredoc tags, response_mode="analyst" in test block, mode validation list
- tools/lint/style.sh — check_analyst_mode_contract (function def and call), stance_analyst local var, all $stance_analyst refs, all analyst.md.tpl missing... error strings, required_final_plan and required_plan_direct strings updated to match new tpl text exactly
- ops/bin/manifest — stance-analyst key, dispatch map entry, file path → stance-planning, planning.md.tpl
- ops/bin/template — stance-analyst → stance-planning
- tools/test/bundle.sh — --profile=analyst, next_out analyst, all resolved_profile='analyst' assertions, all ANALYST prefix checks → planning, PLANNING
- ops/etc/verification.manifest — analyst reference
- storage/dp/active/allowlist.txt — append ops/src/stances/planning.md.tpl and docs/ops/specs/stances/planning.md
- docs/MANUAL.md, docs/MAP.md, docs/ops/specs/stances/analyst.md (via rename), docs/ops/registry/* — prose analyst → planning where referring to the task/profile; preserve "Analyst" as role name

Out of scope:
- All other role renames (architect, auditor, foreman, conformist, contractor — DPs 2e–2i)
- Any stance contract changes (Item 3)
- Factory extraction (Item 4)
- ops/src/shared/stances.json — shared rules unchanged

Safety and invariants:
- bash tools/lint/response.sh --test must pass after all changes
- bash tools/lint/style.sh must pass after all changes
- ops/bin/bundle --profile=planning must resolve correctly
- ops/bin/manifest stance-planning must return ops/src/stances/planning.md.tpl
- --profile=analyst must not be a valid profile after this DP
- No changes to certify, bundle binary, draft, open, or other binaries

Worker Constraints (SSOT injected):
## Section 1: Universal Template Rules
- Template source uses `tpl` files with optional YAML frontmatter
- Canon frontmatter keys: `template_type` `template_id` `template_version` `requires_slots` `includes`
- Renderer strips frontmatter before output write
- Slot token form `\{\{TOKEN\}\}` with uppercase alphanumeric underscore
- Include forms `\{\{@include:path\}\}` and `\{\{@include:path#section\}\}`
- Include resolution is strict: missing file fail missing section fail circular graph fail
- Strict mode default: every required slot value present and no unresolved token
- Non strict mode allowed only for lint and normalization workflows
- Worker facing generated surfaces remain pointer first and exclude disposable artifacts

Target Files allowlist (hard gate):
- storage/dp/active/allowlist.txt

## 3.4 Execution Plan (A-E)

### 3.4.1 State
DP-OPS-0223 complete: line-anchor hardening, --intake-only flag, and --rerun guard landed. Role-task decoupling planned in storage/handoff/PLAN-FACTORY.md; DP-OPS-0219 pre-reserved and branch pre-created at c37d55ce. analyst is the first role identifier to be decoupled — all system artifacts that name the analyst task become planning.

### 3.4.2 Request
- a. In ops/src/stances/analyst.md.tpl: make all identifier changes listed in scope; rename file to planning.md.tpl
- b. Rename docs/ops/specs/stances/analyst.md → docs/ops/specs/stances/planning.md
- c. In ops/lib/manifests/BUNDLE.md: update all analyst keys, values, and prose to planning/PLANNING
- d. In ops/lib/scripts/bundle.sh: update usage line, case branch, comparisons, prefix strings
- e. In tools/lint/response.sh: rename function, rename variables, rename heredoc tags, update mode list
- f. In tools/lint/style.sh: rename function, rename local var, update error strings, update required_final_plan and required_plan_direct to match updated tpl strings exactly
- g. In ops/bin/manifest: rename stance-analyst key and file path reference to stance-planning and planning.md.tpl
- h. In ops/bin/template: rename stance-analyst to stance-planning
- i. In tools/test/bundle.sh: update profile, assertions, and prefix checks
- j. In ops/etc/verification.manifest: update analyst reference
- k. In storage/dp/active/allowlist.txt: append new file paths for renamed files
- l. Docs prose sweep: MANUAL.md, MAP.md, renamed spec, registry files — task/profile references analyst → planning; preserve "Analyst" as role name

### 3.4.3 Changelog
- RENAME: ops/src/stances/analyst.md.tpl → ops/src/stances/planning.md.tpl
- RENAME: docs/ops/specs/stances/analyst.md → docs/ops/specs/stances/planning.md
- UPDATE: ops/lib/manifests/BUNDLE.md
- UPDATE: ops/lib/scripts/bundle.sh
- UPDATE: tools/lint/response.sh
- UPDATE: tools/lint/style.sh
- UPDATE: ops/bin/manifest
- UPDATE: ops/bin/template
- UPDATE: tools/test/bundle.sh
- UPDATE: ops/etc/verification.manifest
- UPDATE: storage/dp/active/allowlist.txt
- UPDATE: docs/MANUAL.md, docs/MAP.md, docs/ops/registry/* (prose sweep)

### 3.4.4 Patch / Diff
1. ops/src/stances/analyst.md.tpl → planning.md.tpl: template_id: analyst → planning; --profile=analyst → --profile=planning; analyst input source → planning input source; active analyst query → active planning query; Default analyst behavior → Default planning behavior; direct architect handoff → direct draft handoff (line 18); direct architect drafting → direct draft drafting (lines 37 and 73); all four For machine-ingest analyst mode: → For machine-ingest planning mode: (lines 62–65)
2. docs/ops/specs/stances/analyst.md → planning.md: file rename; update any internal self-references
3. ops/lib/manifests/BUNDLE.md: replace all analyst keys/values/prose with planning, ANALYST → PLANNING
4. ops/lib/scripts/bundle.sh: analyst) → planning); all analyst comparisons → planning; ANALYST → PLANNING
5. tools/lint/response.sh: check_analyst_body_scope → check_planning_body_scope; response_analyst_* → response_planning_*; EOF_ANALYST_* → EOF_PLANNING_*; mode list and test block
6. tools/lint/style.sh: check_analyst_mode_contract → check_planning_mode_contract; stance_analyst → stance_planning; file path; error strings; required_final_plan and required_plan_direct updated to match new tpl lines exactly
7. ops/bin/manifest: stance-analyst → stance-planning; analyst.md.tpl → planning.md.tpl in dispatch map
8. ops/bin/template: stance-analyst → stance-planning
9. tools/test/bundle.sh: --profile=analyst → --profile=planning; resolved_profile='analyst' → 'planning'; ANALYST → PLANNING
10. ops/etc/verification.manifest: analyst entry → planning
11. storage/dp/active/allowlist.txt: append ops/src/stances/planning.md.tpl and docs/ops/specs/stances/planning.md
12. Docs: grep-and-replace task/profile references analyst → planning; preserve "Analyst" as role name in prose

### 3.4.5 Receipt (Proofs to collect) - MUST RUN

**Mandatory receipt commands (always run; do not omit):**
- bash tools/lint/dp.sh TASK.md
- bash tools/lint/task.sh
- bash tools/lint/integrity.sh
- bash tools/lint/style.sh
- ./ops/bin/llms
- bash tools/lint/integrity.sh
- git diff --name-only
- git diff --stat
- comm -23 <(git diff --name-only | sort) <(sort storage/dp/active/allowlist.txt) || true
- comm -23 <(git ls-files --others --exclude-standard | sort) <(sort storage/dp/active/allowlist.txt) || true
- ./ops/bin/open

**DP-specific receipt commands (scope-specific; author below):**
Note: Command substitution forms (e.g., $(pwd), $(git ...)) are rejected by certify replay. Use literal values only. dp.sh enforces at lint time.
- bash tools/lint/response.sh --test
- ops/bin/manifest stance-planning

## 3.5 Closeout (Mandatory Routing)
- Execute docs/MANUAL.md Closeout Cycle in order (Verify, Harvest, Refresh, Log, Prune).
- Update SoP.md and PoW.md with DP entries, including objective summary and current proof / receipt summary.
- Protocol order for closeout: Verify -> Generate Results -> COMMIT (Operator Only) -> Prune.
- Run prune hygiene: ./ops/bin/prune --scrub.
- Refresh llms artifacts: `./ops/bin/llms`
- Regenerate session artifacts: `./ops/bin/open --out=auto`
- Ensure the next session begins with refreshed session artifacts and matching receipts.
- Note: audit dump generation is owned by `./ops/bin/bundle --profile=audit --out=auto` and is separate from operator session refresh. Do not stamp an additional `ops/bin/dump --scope=core` into closeout unless the DP explicitly authorizes a standalone state capture.
- Refresh side-effect: `ops/bin/llms` regenerates `ops/lib/manifests/OPS.md` as a compile event; if `OPS.md` is not in the allowlist, restore it before running `integrity.sh`. See `docs/MANUAL.md` Refresh side-effect notice for the full procedure.

### 3.5.1 Mandatory Closing Sidecar
Closing sidecar content is generated and maintained at `storage/handoff/CLOSING.md` and is validated by `ops/bin/certify` as a hard gate at certification time. Do not author, predict, populate, or approximate any sidecar-derived closeout output outside this sidecar.
Certify separately collects contractor-authored narrative for the RESULTS Contractor Execution Narrative section at certify time via interactive editor prompt; this narrative input is independent from closing sidecar content.

Before running certify, confirm `storage/handoff/CLOSING.md` has been maintained throughout execution and reflects observable reality only.

Closing sidecar fields required at certify time:
- Commit Message
- Create Pull Request (Title)
- Create Pull Request (Description)
- Confirm Merge (Commit Message)
- Confirm Merge (Extended Description)
- Confirm Merge (Add a Comment)
