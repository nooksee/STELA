---
trace_id: stela-20260403T171857Z-69a5558d
packet_id: DP-OPS-0249
created_at: 2026-04-03T18:06:09Z
previous: archives/surfaces/TASK-DP-OPS-0248-9d3c74cd.md
---
# STELA TASK DASHBOARD
Status: ACTIVE
Owner: Integrator
Last Updated: 2026-02-14

## 1. Session State
Pointer: Session context output (generated by ops/bin/open)
Context Manifest: ops/lib/manifests/CONTEXT.md (checked by tools/lint/context.sh)

Session State contract:
- Session State is pointer-first. Do not write inline branch or hash state here.
- OPEN (and OPEN-PORCELAIN when dirty) is the session state snapshot.
- RESULTS is the audit record for the DP.
- TASK is the stable routing surface.

## 2. Logic Pointers
Primary Constraint: PoT.md (Policy of Truth).
Surface Contract Spec: docs/ops/specs/surfaces/task.md.

### 2.1 Governance Pointers
Jurisdiction: PoT.md Section 3.
Git Authority: PoT.md Section 4.1.
Behavioral Standard: PoT.md Section 4.2.
Staffing Protocol: PoT.md Section 4.1.

### 2.2 Enforcement and Gates
Style: tools/lint/style.sh
Context: tools/lint/context.sh
Truth: tools/lint/truth.sh
DP lint: tools/lint/dp.sh (enforces DP transaction rules in Section 3; does not enforce TASK container schema)
TASK lint: tools/lint/task.sh (sole TASK surface/container schema enforcer)
Repo verify: tools/verify.sh

## 3. Current Dispatch Packet (DP)

Section-by-section authoring protocol:
- Generate DP structure via `./ops/bin/bundle --profile=draft --out=auto` (standard operator path: Architect completes the embedded DP AUTHORING SCAFFOLD; save the returned DP body to `storage/dp/intake/DP.md`) or locally via `ops/bin/draft` (template render only).
- Do not hand-edit DP structural boilerplate in `TASK.md` after generation.
- Manual edits are limited to slot content only (objective, scope, safety, plan body, receipt body).
- Freeze each completed subsection in order (3.1 -> 3.2 -> 3.3 -> 3.4.x).
- Do not revise earlier sections to justify later work. If scope changes, end the thread and draft a new DP.

Freeze Rule:
- If a completed DP subsection changes later, append an AMENDMENT block in RESULTS; otherwise DP is FAIL.

AMENDMENT: minimum fields
- Reopened section:
- Reason:
- Impact:
- Allowlist change (none or exact paths):

Input discipline (hard rule):
- Worker receives only the DP text. Worker does not require OPEN or DUMP attachments.
- DP writer must not attach or cite disposable artifacts (rough notes, chat logs, scratch drafts, throwaway excerpts).
- DP writer must not include pasted bundles (OPEN, DUMP payloads, manifests) inside the DP body.
- Canon pointers only. Evidence belongs in storage artifacts and RESULTS.


### DP-OPS-0249: Rename the execution manifest to neutral function naming and clean coupled role prose

## 3.1 Freshness Gate (Must Pass Before Work)
Base Branch: main
Work Branch: work/dp-ops-0249-2026-04-03
Base HEAD: 0547c7b1
Freshness Stamp: 2026-04-03
Note: FRESHNESS_STAMP must be YYYY-MM-DD format only. No trace tokens, no timestamps, no other text. Certify rejects all other forms. dp.sh enforces at lint time.

Required local re-check (worker runs before any edits; paste verbatim outputs under `## Worker Execution Narrative` -> `### Preflight State` in RESULTS):
- git rev-parse --abbrev-ref HEAD
- git rev-parse --short HEAD
- git status --porcelain

These three commands are freshness proof only. Do not treat §3.4.5 receipt replay as a substitute for their pre-edit outputs.

Preconditions:
- No commits on main.
- Working tree must be clean before execution begins.
- If Base HEAD changes, regenerate session artifacts from canonical tools before proceeding.

STOP conditions:
- STOP if any mismatch (branch, Base HEAD, missing work branch).
- STOP if working tree is dirty before execution begins.
- STOP if told to create or switch branches.

## 3.1.1 DP Preflight Gate (Run Before Any Edits)
Purpose:
- Catch malformed DP or TASK schema before work begins.

Worker runs (paste outcome in RESULTS):
- bash tools/lint/dp.sh --test
- bash tools/lint/dp.sh TASK.md
- bash tools/lint/task.sh

STOP if any preflight check fails.

### CbC Design Discipline Preflight (TASK.md §3.1.1)
Tooling-applicable. This packet renames a shared manifest injected into active surface templates and touched by lint gates. Structural prevention requires neutral function naming in core canon now so future actor/role mapping can move into definitions without reopening the active spine.

## 3.2 Required Context Load (Read Before Doing Anything)

### 3.2.1 Canon load order (always)
Worker must confirm loaded before edits begin:
1. PoT.md
2. SoP.md
3. PoW.md
4. TASK.md
5. docs/MAP.md
6. docs/MANUAL.md
7. ops/lib/manifests/CONTEXT.md

Notes:
- Worker does not read OPEN. OPEN is for Integrator state refresh and for receipts.
- Disposable artifacts must not be referenced or included.
- Worker input is the DP text only.
- DP writer must not attach or cite disposable artifacts.
- DP writer must not embed pasted bundles.

## Rules

1 Temp path rule uses `var/tmp` only
2 RESULTS generation rule uses `ops/bin/certify` only
3 Receipt rule runs every DP mandated command and treats any skip as STOP
4 Disposable artifact rule treats OPEN dump bundles and manifests as non canonical session artifacts
5 Use repo-relative paths only in all output. Never print absolute filesystem paths. Do not emit clickable file links.

### 3.2.2 DP-scoped load order (per DP)
- ops/lib/manifests/EXECUTION.md
- ops/src/shared/stances.json
- ops/src/surfaces/dp.md.tpl
- ops/src/surfaces/addendum.md.tpl
- ops/src/stances/audit.md.tpl
- ops/bin/certify
- tools/lint/dp.sh
- tools/lint/ff.sh
- storage/dp/active/allowlist.txt
- docs/MANUAL.md
- docs/INDEX.md
- docs/ops/specs/tools/lint/dp.md
- docs/ops/specs/tools/lint/task.md
- docs/ops/specs/tools/lint/context.md
- docs/ops/specs/tools/lint/llms.md
- docs/ops/specs/tools/lint/ff.md

## 3.3 Scope and Safety
Objective:
- Decouple the shared execution contract from actor/role naming by renaming `ops/lib/manifests/CONTRACTOR.md` to a neutral function name and updating directly coupled active-spine references.
- Replace `contractor` with `worker` only where the prose is really execution-role behavior or baseline naming; keep PoT actor truth and other real-entity statements intact.

In scope:
- MOVE `ops/lib/manifests/CONTRACTOR.md` -> `ops/lib/manifests/EXECUTION.md` and retitle the manifest accordingly while preserving the current rules block content.
- UPDATE `ops/src/shared/stances.json` to reference the neutral execution manifest and neutral shared wording.
- UPDATE `ops/src/surfaces/dp.md.tpl` and `ops/src/surfaces/addendum.md.tpl` to include the renamed execution manifest.
- UPDATE `ops/src/stances/audit.md.tpl` so audit instructions stop calling the shared execution include a contractor brief and stop calling narrative hygiene contractor output rules.
- UPDATE `ops/bin/certify` only if closeout exposes a command-log fence-boundary defect in RESULTS generation; keep the repair strictly structural.
- UPDATE `tools/lint/dp.sh` for the canonical `dp.md.tpl` hash change introduced by the template path rename.
- UPDATE `tools/lint/ff.sh` for the manifest path rename.
- UPDATE `storage/dp/active/allowlist.txt` for the manifest path rename.
- UPDATE directly coupled active-spine docs/specs where `contractor` is currently standing in for execution-role behavior or worker baseline semantics:
  - `docs/MANUAL.md`
  - `docs/INDEX.md`
  - `docs/ops/specs/tools/lint/dp.md`
  - `docs/ops/specs/tools/lint/task.md`
  - `docs/ops/specs/tools/lint/context.md`
  - `docs/ops/specs/tools/lint/llms.md`
- UPDATE `ops/lib/manifests/OPS.md`, `llms-core.txt`, `llms-full.txt`, and `llms.txt` only if closeout refresh emits the expected compile fallout.

Out of scope:
- `PoT.md` staffing protocol and triad wording.
- `ops/bin/open` base-actor lines.
- Factory definitions, factory registry, agent leaves, and `tools/lint/agent.sh`.
- Any change to active task/stance ids (`planning`, `draft`, `audit`, `addenda`).
- Any change to `Worker Execution Narrative`, audit step naming, or results lint behavior.
- Any hook work, `llms` stdout sanitation, or runbook transport work.

Safety and invariants:
- Keep real-entity truth explicit: `operator`, `integrator`, and `contractor` remain canon actor names in PoT and actor-bearing surfaces.
- Do not flatten actor truth into role truth by blind search/replace; change `contractor` to `worker` only where the line is really describing execution-role behavior or baseline semantics.
- The renamed manifest must remain a shared execution/function contract, not a new actor or task surface.
- Preserve the rules block content in the execution manifest unless a directly necessary wording adjustment is part of the neutralization itself.
- Do not widen into factory role/schema migration in this packet.
- If closeout exposes a certify-generated RESULTS structure defect, repair only the command-log fence rendering needed to keep the proof frame valid; do not widen into results-lint redesign or other receipt-surface changes.
- Prove `tools/lint/ff.sh` changes with syntax and path-level evidence only; do not claim a full-repo FF pass from this packet while unrelated CCD debt remains outside scope.
- If `./ops/bin/llms` emits `ops/lib/manifests/OPS.md` or root `llms*.txt`, accept only that expected compile fallout and restore any unrelated generated paths before the post-`llms` integrity check.

Worker Constraints (SSOT injected):
## Section 1: Universal Template Rules
- Template source uses `tpl` files with optional YAML frontmatter
- Canon frontmatter keys: `template_type` `template_id` `template_version` `requires_slots` `includes`
- Renderer strips frontmatter before output write
- Slot token form `\{\{TOKEN\}\}` with uppercase alphanumeric underscore
- Include forms `\{\{@include:path\}\}` and `\{\{@include:path#section\}\}`
- Include resolution is strict: missing file fail missing section fail circular graph fail
- Strict mode default: every required slot value present and no unresolved token
- Non strict mode allowed only for lint and normalization workflows
- Worker facing generated surfaces remain pointer first and exclude disposable artifacts

Target Files allowlist (hard gate):
- storage/dp/active/allowlist.txt

## 3.4 Execution Plan (A-E)

### 3.4.1 State
- `storage/handoff/PLAN.md` marks `0240` through `0248` as landed and explicitly queues execution-contract decoupling as the next active spine seam.
- The fresh draft bundle assigns `packet_id: DP-OPS-0249`, `Base HEAD: 0547c7b1`, and `Freshness Stamp: 2026-04-03`.
- `ops/lib/manifests/CONTRACTOR.md` currently contains a tiny shared rules block, but active templates and stance guidance still import and describe it with actor-bound wording.
- Direct coupled active-spine references are visible in `ops/src/shared/stances.json`, `ops/src/surfaces/dp.md.tpl`, `ops/src/surfaces/addendum.md.tpl`, `ops/src/stances/audit.md.tpl`, `tools/lint/dp.sh`, `tools/lint/ff.sh`, `storage/dp/active/allowlist.txt`, `docs/MANUAL.md`, `docs/INDEX.md`, and the listed lint specs; closeout also exposed one narrow `ops/bin/certify` receipt-frame coupling.

### 3.4.2 Request
- Rename the shared manifest to neutral execution/function naming and wire the active spine to the new path.
- Clean the directly coupled internal prose so actor truth stays in PoT while execution-role wording becomes `worker` or neutral execution language where appropriate.
- Keep the packet strictly in the active spine; do not use this rename as a back door into factory definitions or global actor-role schema changes.
- Mirror all touched code/template changes in their coupled specs and references so the rename does not leave stale path or terminology residue.

### 3.4.3 Changelog
- NEW ops/lib/manifests/EXECUTION.md
- DELETE ops/lib/manifests/CONTRACTOR.md
- UPDATE ops/src/shared/stances.json
- UPDATE ops/src/surfaces/dp.md.tpl
- UPDATE ops/src/surfaces/addendum.md.tpl
- UPDATE ops/src/stances/audit.md.tpl
- UPDATE ops/bin/certify
- UPDATE tools/lint/dp.sh
- UPDATE tools/lint/ff.sh
- UPDATE storage/dp/active/allowlist.txt
- UPDATE docs/MANUAL.md
- UPDATE docs/INDEX.md
- UPDATE docs/ops/specs/tools/lint/dp.md
- UPDATE docs/ops/specs/tools/lint/task.md
- UPDATE docs/ops/specs/tools/lint/context.md
- UPDATE docs/ops/specs/tools/lint/llms.md
- UPDATE ops/lib/manifests/OPS.md (generated only if emitted)
- UPDATE llms-core.txt (generated only if emitted)
- UPDATE llms-full.txt (generated only if emitted)
- UPDATE llms.txt (generated only if emitted)

### 3.4.4 Patch / Diff
1. Move `ops/lib/manifests/CONTRACTOR.md` to `ops/lib/manifests/EXECUTION.md` and retitle the manifest so the file names the function, not the actor.
2. Update active template/shared-stance consumers to the new execution manifest path and neutral wording: `ops/src/shared/stances.json`, `ops/src/surfaces/dp.md.tpl`, `ops/src/surfaces/addendum.md.tpl`, and `ops/src/stances/audit.md.tpl`.
3. Update the directly coupled docs/lint specs, `tools/lint/dp.sh`, `tools/lint/ff.sh`, and `storage/dp/active/allowlist.txt` so the manifest rename and the execution-role wording changes stay consistent across the active spine.
4. If certify emits a malformed RESULTS command-log fence during closeout, patch `ops/bin/certify` narrowly so appended command headings always begin on a fresh line.
5. Keep PoT and factory definitions untouched; if a line is genuinely actor truth, leave it alone. Only convert `contractor` -> `worker` where the line is clearly about execution-role behavior or worker baseline semantics.
6. Run the bounded proofs, then close out with the expected compile fallout only if emitted.

### 3.4.5 Receipt (Proofs to collect) - MUST RUN

**Mandatory receipt commands (always run; do not omit):**
- bash tools/lint/dp.sh TASK.md
- bash tools/lint/task.sh
- bash tools/lint/integrity.sh
- bash tools/lint/style.sh
- ./ops/bin/llms
- bash tools/lint/integrity.sh
- git diff --name-only
- git diff --stat
- comm -23 <(git diff --name-only | sort) <(sort storage/dp/active/allowlist.txt) || true
- comm -23 <(git ls-files --others --exclude-standard | sort) <(sort storage/dp/active/allowlist.txt) || true
- ./ops/bin/open

**DP-specific receipt commands (scope-specific; author below):**
Note: Command substitution forms (e.g., $(pwd), $(git ...)) are rejected by certify replay. Use literal values only. Repo-local binaries must be authored as literal `./ops/bin/...` or `ops/bin/...` commands. Unsupported receipt-command forms are rejected by dp.sh and certify.
- bash tools/lint/truth.sh
- bash -n tools/lint/ff.sh
- git diff --exit-code -- PoT.md docs/ops/specs/definitions/agents.md docs/ops/registry/agents.md tools/lint/agent.sh ops/bin/open
- rg -n 'ops/lib/manifests/CONTRACTOR.md|Contractor Brief|Contractor constraints|contractor output rules|contractor baseline' docs/MANUAL.md docs/INDEX.md docs/ops/specs/tools/lint/dp.md docs/ops/specs/tools/lint/task.md docs/ops/specs/tools/lint/context.md docs/ops/specs/tools/lint/llms.md ops/src/shared/stances.json ops/src/surfaces/dp.md.tpl ops/src/surfaces/addendum.md.tpl ops/src/stances/audit.md.tpl tools/lint/ff.sh storage/dp/active/allowlist.txt || true
- rg -n 'ops/lib/manifests/EXECUTION.md' ops/src/shared/stances.json ops/src/surfaces/dp.md.tpl ops/src/surfaces/addendum.md.tpl tools/lint/ff.sh storage/dp/active/allowlist.txt
- rg -n 'worker baseline|worker sessions|worker hallucination' docs/MANUAL.md docs/INDEX.md docs/ops/specs/tools/lint/dp.md docs/ops/specs/tools/lint/task.md docs/ops/specs/tools/lint/context.md docs/ops/specs/tools/lint/llms.md
- ./ops/bin/manifest render stance-audit --out=- >/dev/null
- git diff --check

## 3.5 Closeout (Mandatory Routing)
- Execute docs/MANUAL.md Closeout Cycle in order (Verify, Harvest, Refresh, Log, Prune).
- Update SoP.md and PoW.md with DP entries, including objective summary and current proof / receipt summary.
- Protocol order for closeout: Verify -> Generate Results -> COMMIT (Operator Only) -> Prune.
- Run prune hygiene: ./ops/bin/prune --scrub.
- Refresh llms artifacts: `./ops/bin/llms`
- Regenerate session artifacts: `./ops/bin/open --out=auto`
- Ensure the next session begins with refreshed session artifacts and matching receipts.
- Note: audit dump generation is owned by `./ops/bin/bundle --profile=audit --out=auto` and is separate from operator session refresh. Do not stamp an additional `ops/bin/dump --scope=core` into closeout unless the DP explicitly authorizes a standalone state capture.
- Refresh side-effect: `ops/bin/llms` regenerates `ops/lib/manifests/OPS.md` as a compile event; if `OPS.md` is not in the allowlist, restore it before running `integrity.sh`. See `docs/MANUAL.md` Refresh side-effect notice for the full procedure.

### 3.5.1 Mandatory Closing Sidecar
Closing sidecar content is generated and maintained at `storage/handoff/CLOSING.md` and is validated by `ops/bin/certify` as a hard gate at certification time. Do not author, predict, populate, or approximate any sidecar-derived closeout output outside this sidecar.
Certify separately collects worker-authored narrative for the RESULTS Worker Execution Narrative section at certify time via interactive editor prompt; this narrative input is independent from closing sidecar content.

Before running certify, confirm `storage/handoff/CLOSING.md` has been maintained throughout execution and reflects observable reality only.

Closing sidecar fields required at certify time:
- Commit Message
- Create Pull Request (Title)
- Create Pull Request (Description)
- Confirm Merge (Commit Message)
- Confirm Merge (Extended Description)
- Confirm Merge (Add a Comment)
