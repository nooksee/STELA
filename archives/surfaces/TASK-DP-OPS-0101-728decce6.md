---
trace_id: stela-20260222T225450Z-56ca1d7e
packet_id: DP-OPS-0101
created_at: 2026-02-22T23:03:38Z
previous: archives/surfaces/TASK-DP-OPS-0101-728decce6.md
---
# STELA TASK DASHBOARD
Status: ACTIVE
Owner: Integrator
Last Updated: 2026-02-18

## 1. Session State
Pointer: Session context output (generated by ops/bin/open)
Context Manifest: ops/lib/manifests/CONTEXT.md (checked by tools/lint/context.sh)

Session State contract:
- Session State is pointer-first. Do not write inline branch or hash state here.
- OPEN and OPEN-PORCELAIN are the session state snapshot.
- OPEN-PORCELAIN is emitted on every OPEN run. Clean sessions produce a zero-line artifact.
- RESULTS is the audit record for the DP.
- TASK is the stable routing surface.
- Operator note (2026-02-17): Do not edit `SoP.md` or `PoW.md` unless explicitly instructed.

## 2. Logic Pointers
Primary Constraint: PoT.md (Policy of Truth).
Surface Contract Spec: docs/ops/specs/surfaces/task.md.

### 2.1 Governance Pointers
Jurisdiction: PoT.md Section 3.
Git Authority: PoT.md Section 4.1.
Behavioral Standard: PoT.md Section 4.2.
Staffing Protocol: PoT.md Section 4.1.

### 2.2 Enforcement and Gates
Style: tools/lint/style.sh
Context: tools/lint/context.sh
Truth: tools/lint/truth.sh
DP lint: tools/lint/dp.sh (enforces DP transaction rules in Section 3; does not enforce TASK container schema)
TASK lint: tools/lint/task.sh (sole TASK surface/container schema enforcer)
Repo verify: tools/verify.sh
llms parity: tools/lint/llms.sh

## 3. Current Dispatch Packet (DP)

Section-by-section authoring protocol:
- Generate DP structure from `ops/src/surfaces/dp.md.tpl` using `ops/bin/draft`.
- Do not hand-edit DP structural boilerplate in `TASK.md` after generation.
- Manual edits are limited to slot content only (objective, scope, safety, plan body, receipt body).
- Freeze each completed subsection in order (3.1 -> 3.2 -> 3.3 -> 3.4.x).
- Do not revise earlier sections to justify later work. If scope changes, end the thread and draft a new DP.

Freeze Rule:
- If a completed DP subsection changes later, append an AMENDMENT block in RESULTS; otherwise DP is FAIL.

AMENDMENT: minimum fields
- Reopened section:
- Reason:
- Impact:
- Allowlist change (none or exact paths):

Input discipline (hard rule):
- Worker receives only the DP text. Worker does not require OPEN or DUMP attachments.
- DP writer must not attach or cite disposable artifacts (rough notes, chat logs, scratch drafts, throwaway excerpts).
- DP writer must not include pasted bundles (OPEN, DUMP payloads, manifests) inside the DP body.
- Canon pointers only. Evidence belongs in storage artifacts and RESULTS.

### DP-OPS-0101: CbC Phase 1 Canonization

## 3.1 Freshness Gate (Must Pass Before Work)
Base Branch: main
Required Work Branch: work/cbc-phase1-canonization-2026-02-22
Base HEAD: 728decce6
Freshness Stamp: 2026-02-22

Required local re-check (worker runs; paste outputs in RESULTS):
- git rev-parse --abbrev-ref HEAD
- git rev-parse --short HEAD
- git status --porcelain

Preconditions:
- No commits on main.
- Working tree must be clean before execution begins.
- If Base HEAD changes, regenerate session artifacts from canonical tools before proceeding.

STOP conditions:
- STOP if any mismatch (branch, Base HEAD, missing work branch).
- STOP if working tree is dirty before execution begins.
- STOP if told to create or switch branches.

## 3.1.1 DP Preflight Gate (Run Before Any Edits)
Purpose:
- Catch malformed DP or TASK schema before work begins.

Worker runs (paste outcome in RESULTS):
- bash tools/lint/dp.sh --test
- bash tools/lint/dp.sh TASK.md
- bash tools/lint/task.sh

STOP if any preflight check fails.

### CbC Design Discipline Preflight (TASK.md §3.1.1)
Required when the DP objective adds, modifies, or replaces a linter, script, guard, or validation binary.
For non-tooling DPs: state "Not applicable" with a one-line justification.
This DP modifies `tools/lint/dp.sh` (a linter) and `ops/src/surfaces/dp.md.tpl` (a template whose hash is validated by that linter). Both surfaces qualify as enforcement tooling under the CbC definition.

Q1 — Well-formed assertion or behavior-fighter?
The CbC preflight slot asserts that a documented procedure was exercised before DP dispatch. The slot check in dp.sh is a yes/no assertion against observable slot content (non-empty, non-placeholder). It does not fight behavior. The template slot enforces discipline structurally: ops/bin/draft cannot produce a valid DP without populating the slot.

Q2 — Structural prevention available?
Yes. The dp.md.tpl slot IS the primary structural prevention mechanism. Once the slot exists in the template, ops/bin/draft cannot produce a complete DP without populating it. The dp.sh non-empty check (added by this DP) is a narrow safety net for renderer edge cases where a slot is present but emitted empty.

Q3 — Why is detection needed alongside structure?
A misconfigured slot renderer could emit an empty token value without failing template generation. The non-empty check in dp.sh catches that edge case. The linter complexity added is approximately 15 lines across two locations in dp.sh. This is proportionate: one `block_has_content` call in `check_required_fields` and one awk mode block in `normalize_dp_structure`.

Q4 — Deletion Test score: C.
Without the slot and check, the CbC discipline has no structural activation point in the DP workflow. DPs under pressure skip it. The discipline remains advisory with no enforcement consequence.

Q5 — Complexity Budget:
Net addition to dp.md.tpl: approximately 10 lines (frontmatter slot + subsection heading + slot token + two documentation lines).
Net addition to dp.sh: approximately 20 lines (normalizer mode block + check_required_fields extension + SHA256 constant update).
Total: approximately 30 lines.
Payoff: every tooling DP is structurally required to exercise the CbC procedure before reaching valid draft state. Cost-to-payoff ratio is well below 1×. No redesign triggered.

Decision Registry entry: C — Keep. Structural slot is the primary prevention; detection is a proportionate safety net.

## 3.2 Required Context Load (Read Before Doing Anything)

### 3.2.1 Canon load order (always)
Worker must confirm loaded before edits begin:
1. PoT.md
2. SoP.md
3. PoW.md
4. TASK.md
5. docs/MAP.md
6. docs/MANUAL.md
7. ops/lib/manifests/CONTEXT.md

Notes:
- Worker does not read OPEN. OPEN is for Integrator state refresh and for receipts.
- Disposable artifacts must not be referenced or included.
- Worker input is the DP text only.
- DP writer must not attach or cite disposable artifacts.
- DP writer must not embed pasted bundles.

<!-- CCD: ff_target="machine-dense" ff_band="10-20" -->
# Contractor Brief

1 Temp path rule uses `var/tmp` only
2 RESULTS generation rule uses `ops/bin/certify` only
3 Receipt rule runs every DP mandated command and treats any skip as STOP
4 Disposable artifact rule treats OPEN dump bundles and manifests as non canonical session artifacts
5 Path rule uses existing repo paths or explicit `NEW` changelog markers and forbids globs and brace expansion

### 3.2.2 DP-scoped load order (per DP)
1. `PoT.md` §4.2 — locate the Operational Directives bullet list; this is the amendment target
2. `ops/src/surfaces/dp.md.tpl` — read the full file; this is the template modification target for the CbC_PREFLIGHT slot addition
3. `tools/lint/dp.sh` — read the full file; identify `CANONICAL_DP_TEMPLATE_SHA256` constant, `normalize_dp_structure` awk function, and `check_required_fields` function; all three are modification targets
4. `docs/MANUAL.md` — locate the Closeout Cycle section; the tooling DP addendum is appended there
5. `docs/INDEX.md` — locate the section appropriate for a design discipline document; one new entry is added

## 3.3 Scope and Safety
Objective:
Canonize the Correct-by-Construction design discipline. Create `docs/DESIGN.md` as the canonical SSOT for the CbC procedure, the Phase 1 retrospective audit findings, and the Decision Registry. Amend `PoT.md` §4.2 to give the discipline constitutional weight. Activate the discipline structurally by adding the `CbC_PREFLIGHT` slot to `ops/src/surfaces/dp.md.tpl` and the corresponding validation to `tools/lint/dp.sh`. Add a tooling DP closeout addendum to `docs/MANUAL.md`. Add a discovery entry in `docs/INDEX.md`.

In scope:
- `docs/DESIGN.md` — new file, CbC SSOT
- `PoT.md` — one bullet addition to §4.2 Operational Directives
- `ops/src/surfaces/dp.md.tpl` — add `CbC_PREFLIGHT` to `requires_slots` frontmatter; add CbC preflight subsection and slot token to §3.1.1
- `tools/lint/dp.sh` — update `CANONICAL_DP_TEMPLATE_SHA256` constant; add `CbC_PREFLIGHT` mode to `normalize_dp_structure`; add `cbc_preflight_block` non-empty check to `check_required_fields`
- `docs/MANUAL.md` — add tooling DP closeout addendum to Closeout Cycle section
- `docs/INDEX.md` — add `docs/DESIGN.md` discovery entry
- `llms.txt`, `llms-core.txt`, `llms-full.txt` — closeout bundle refresh (generated)
- `docs/MAP.md` — map regeneration (generated)
- `storage/dp/active/allowlist.txt` — update to reflect this DP's target file set
- `storage/handoff/CLOSING-DP-OPS-0101.md` — closing sidecar (maintained during execution)
- `archives/manifests/compile-*.md` — compile archive leaves (generated during closeout)

Out of scope:
- Phase 2 through Phase 4 items from the CbC plan (retrospective audit work, B-scored structural redesigns, ongoing governance tooling) — each is a separate DP
- Any modification to `ops/src/surfaces/task.md.tpl` — the DP preflight gate lives in dp.md.tpl, not task.md.tpl
- Any modification to `tools/lint/results.sh` — no RESULTS schema changes required
- Any modification to `ops/bin/draft` — draft consumes the updated dp.md.tpl with no binary changes needed
- Any modification to `ops/lib/manifests/CONTEXT.md` — DESIGN.md is a docs surface, not a context member
- `opt/` contents — factory isolation boundary
- `projects/` contents — payload boundary

Safety and invariants:
- No manual edits to generated outputs (`llms.txt`, `llms-core.txt`, `llms-full.txt`, `docs/MAP.md`); regenerate with `ops/bin/llms` and `ops/bin/map` only.
- No structural edits to TASK.md or the DP block itself after preflight passes.
- Allowlist hard gate: every modified or created file must appear in `storage/dp/active/allowlist.txt` before any edits begin.
- The `CANONICAL_DP_TEMPLATE_SHA256` update in `tools/lint/dp.sh` must be computed from the actual file after dp.md.tpl edits are complete; do not pre-fill or estimate the hash value.
- No contractions in any authored surface per PoT §4.2 Linguistic Precision.
- After updating dp.md.tpl, run `bash tools/lint/dp.sh --test` and `bash tools/lint/dp.sh TASK.md` before proceeding; STOP if either fails.

Worker Constraints (SSOT injected):
## Section 1: Universal Template Rules
- Template source uses `tpl` files with optional YAML frontmatter
- Canon frontmatter keys: `template_type` `template_id` `template_version` `requires_slots` `includes`
- Renderer strips frontmatter before output write
- Slot token form `\{\{TOKEN\}\}` with uppercase alphanumeric underscore
- Include forms `\{\{@include:path\}\}` and `\{\{@include:path#section\}\}`
- Include resolution is strict: missing file fail missing section fail circular graph fail
- Strict mode default: every required slot value present and no unresolved token
- Non strict mode allowed only for lint and normalization workflows
- Worker facing generated surfaces remain pointer first and exclude disposable artifacts

Target Files allowlist (hard gate):
- storage/dp/active/allowlist.txt

## 3.4 Execution Plan (A-E)

### 3.4.1 State
Branch: `work/cbc-phase1-canonization-2026-02-22`. HEAD: `728decce6`. Working tree was clean before execution began. The active TASK surface initially pointed to a historical packet and did not contain `DP-OPS-0101`, which required canonical draft regeneration before the post-template DP lint gate could pass.

### 3.4.2 Request
1. Update `storage/dp/active/allowlist.txt` to the DP-OPS-0101 target file set before touching any other file.
2. Create `docs/DESIGN.md` with the CbC SSOT content.
3. Amend `PoT.md` §4.2 with the Design Discipline Operational Directives bullet.
4. Edit `ops/src/surfaces/dp.md.tpl` to add `CbC_PREFLIGHT` to `requires_slots` and add the CbC preflight subsection in §3.1.1.
5. Edit `tools/lint/dp.sh` to normalize and require the CbC preflight block and update `CANONICAL_DP_TEMPLATE_SHA256` from the actual edited template hash.
6. Edit `docs/MANUAL.md` to add the tooling DP closeout addendum under Closeout Cycle -> Verify.
7. Edit `docs/INDEX.md` to add the `docs/DESIGN.md` discovery entry.
8. Run the required receipt command suite in full.
9. Maintain `storage/handoff/CLOSING-DP-OPS-0101.md` throughout execution and complete it before running certify.

### 3.4.3 Changelog
- UPDATE `storage/dp/active/allowlist.txt` — replace with DP-OPS-0101 target file set
- NEW `docs/DESIGN.md` — CbC canonical SSOT: procedure, retrospective audit, decision registry
- UPDATE `PoT.md` — §4.2 Operational Directives: one new bullet
- UPDATE `ops/src/surfaces/dp.md.tpl` — `requires_slots` frontmatter + §3.1.1 CbC preflight subsection
- UPDATE `tools/lint/dp.sh` — SHA256 constant; `normalize_dp_structure` awk mode; `check_required_fields` extension
- UPDATE `docs/MANUAL.md` — Closeout Cycle section: tooling DP addendum
- UPDATE `docs/INDEX.md` — add `docs/DESIGN.md` entry

### 3.4.4 Patch / Diff
This regenerated draft exists to reactivate the TASK surface with `DP-OPS-0101` after the template and lint changes added a required CbC preflight block. The authoritative patch instructions and exact diff content remain the operator-provided DP packet for this session. The worker must continue using the operator packet for execution order, exact edits, and receipt command requirements.

### 3.4.5 Receipt (Proofs to collect) - MUST RUN

**Mandatory receipt commands (always run; do not omit):**
- bash tools/lint/dp.sh TASK.md
- bash tools/lint/task.sh
- bash tools/lint/integrity.sh
- bash tools/lint/style.sh
- git diff --name-only
- git diff --stat
- comm -23 <(git diff --name-only | sort) <(sort storage/dp/active/allowlist.txt) || true
- comm -23 <(git ls-files --others --exclude-standard | sort) <(sort storage/dp/active/allowlist.txt) || true
- ./ops/bin/open

**DP-specific receipt commands (scope-specific; author below):**
- bash tools/lint/dp.sh --test
- bash tools/lint/style.sh docs/DESIGN.md PoT.md docs/MANUAL.md docs/INDEX.md
- bash tools/lint/truth.sh
- bash tools/lint/ff.sh
- bash -n tools/lint/dp.sh
- sha256sum ops/src/surfaces/dp.md.tpl
- grep 'CANONICAL_DP_TEMPLATE_SHA256=' tools/lint/dp.sh
- grep -n '^  - CbC_PREFLIGHT$' ops/src/surfaces/dp.md.tpl
- grep -n 'CbC Design Discipline Preflight' ops/src/surfaces/dp.md.tpl tools/lint/dp.sh docs/MANUAL.md
- grep -n 'Design Discipline' PoT.md
- grep -n 'DESIGN.md' docs/INDEX.md PoT.md
- wc -l tools/lint/dp.sh ops/src/surfaces/dp.md.tpl docs/DESIGN.md docs/MANUAL.md PoT.md docs/INDEX.md
- ./tools/verify.sh

## 3.5 Closeout (Mandatory Routing)
- Execute docs/MANUAL.md Closeout Cycle in order (Verify, Harvest, Refresh, Log, Prune).
- Update SoP.md and PoW.md with DP entries, including objective summary and verification commands run.
- Protocol order for closeout: Verify -> Generate Results -> COMMIT (Operator Only) -> Prune.
- Run prune hygiene: ./ops/bin/prune --scrub.
- Regenerate session artifacts: `./ops/bin/open --out=auto`
- Capture updated platform state: `./ops/bin/dump --scope=platform --format=chatgpt --out=auto`
- Ensure the next session begins with refreshed session artifacts and matching receipts.

### 3.5.1 Mandatory Closing Block
Primary Commit Header (plaintext)

Pull Request Title (plaintext)

Pull Request Description (markdown)

Final Squash Stub (plaintext) (Must differ from #1)

Extended Technical Manifest (plaintext)

Review Conversation Starter (markdown)
