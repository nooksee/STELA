---
trace_id: stela-20260221T222812Z-405615e7
packet_id: DP-OPS-0090
created_at: 2026-02-21T22:28:49Z
previous: archives/surfaces/TASK-DP-OPS-0090-8e7e0b48.md
---
# STELA TASK DASHBOARD
Status: ACTIVE
Owner: Integrator
Last Updated: 2026-02-18

## 1. Session State
Pointer: Session context output (generated by ops/bin/open)
Context Manifest: ops/lib/manifests/CONTEXT.md (checked by tools/lint/context.sh)

Session State contract:
- Session State is pointer-first. Do not write inline branch or hash state here.
- OPEN and OPEN-PORCELAIN are the session state snapshot.
- OPEN-PORCELAIN is emitted on every OPEN run. Clean sessions produce a zero-line artifact.
- RESULTS is the audit record for the DP.
- TASK is the stable routing surface.
- Operator note (2026-02-17): Do not edit `SoP.md` or `PoW.md` unless explicitly instructed.

## 2. Logic Pointers
Primary Constraint: PoT.md (Policy of Truth).
Surface Contract Spec: docs/ops/specs/surfaces/task.md.

### 2.1 Governance Pointers
Jurisdiction: PoT.md Section 3.
Git Authority: PoT.md Section 4.1.
Behavioral Standard: PoT.md Section 4.2.
Staffing Protocol: PoT.md Section 4.1.

### 2.2 Enforcement and Gates
Style: tools/lint/style.sh
Context: tools/lint/context.sh
Truth: tools/lint/truth.sh
DP lint: tools/lint/dp.sh (enforces DP transaction rules in Section 3; does not enforce TASK container schema)
TASK lint: tools/lint/task.sh (sole TASK surface/container schema enforcer)
Repo verify: tools/verify.sh
llms parity: tools/lint/llms.sh

## 3. Current Dispatch Packet (DP)

Section-by-section authoring protocol:
- Generate DP structure from `ops/src/surfaces/dp.md.tpl` using `ops/bin/draft`.
- Do not hand-edit DP structural boilerplate in `TASK.md` after generation.
- Manual edits are limited to slot content only (objective, scope, safety, plan body, receipt body).
- Freeze each completed subsection in order (3.1 -> 3.2 -> 3.3 -> 3.4.x).
- Do not revise earlier sections to justify later work. If scope changes, end the thread and draft a new DP.

Freeze Rule:
- If a completed DP subsection changes later, append an AMENDMENT block in RESULTS; otherwise DP is FAIL.

AMENDMENT: minimum fields
- Reopened section:
- Reason:
- Impact:
- Allowlist change (none or exact paths):

Input discipline (hard rule):
- Worker receives only the DP text. Worker does not require OPEN or DUMP attachments.
- DP writer must not attach or cite disposable artifacts (rough notes, chat logs, scratch drafts, throwaway excerpts).
- DP writer must not include pasted bundles (OPEN, DUMP payloads, manifests) inside the DP body.
- Canon pointers only. Evidence belongs in storage artifacts and RESULTS.

### DP-OPS-0088: CCD Phase 1 Wave 1 Header Addition Authoring Rewrites and ff.sh Escalation

## 3.1 Freshness Gate (Must Pass Before Work)
Base Branch: main
Required Work Branch: work/ccd-wave1-headers-2026-02-21
Base HEAD: 29dc0be7
Freshness Stamp: 2026-02-21

Required local re-check (worker runs; paste outputs in RESULTS):
- git rev-parse --abbrev-ref HEAD
- git rev-parse --short HEAD
- git status --porcelain

Preconditions:
- No commits on main.
- Working tree must be clean before execution begins.
- If Base HEAD changes, regenerate session artifacts from canonical tools before proceeding.

STOP conditions:
- STOP if any mismatch (branch, Base HEAD, missing work branch).
- STOP if working tree is dirty before execution begins.
- STOP if told to create or switch branches.

## 3.1.1 DP Preflight Gate (Run Before Any Edits)
Purpose:
- Catch malformed DP or TASK schema before work begins.

Worker runs (paste outcome in RESULTS):
- bash tools/lint/dp.sh --test
- bash tools/lint/dp.sh TASK.md
- bash tools/lint/task.sh

STOP if any preflight check fails.

## 3.2 Required Context Load (Read Before Doing Anything)

### 3.2.1 Canon load order (always)
Worker must confirm loaded before edits begin:
1. PoT.md
2. SoP.md
3. PoW.md
4. TASK.md
5. docs/MAP.md
6. docs/MANUAL.md
7. ops/lib/manifests/CONTEXT.md

Notes:
- Worker does not read OPEN. OPEN is for Integrator state refresh and for receipts.
- Disposable artifacts must not be referenced or included.
- Worker input is the DP text only.
- DP writer must not attach or cite disposable artifacts.
- DP writer must not embed pasted bundles.

<!-- CCD: ff_target="machine-dense" ff_band="10-20" -->
# Contractor Brief

1 Temp path rule uses `var/tmp` only
2 RESULTS generation rule uses `ops/bin/certify` only
3 Receipt rule runs every DP mandated command and treats any skip as STOP
4 Disposable artifact rule treats OPEN dump bundles and manifests as non canonical session artifacts
5 Path rule uses existing repo paths or explicit `NEW` changelog markers and forbids globs and brace expansion

### 3.2.2 DP-scoped load order (per DP)
1. `docs/CCD.md` - tier definitions header format scoring rationale
2. `docs/ops/specs/tools/lint/ff.md` - ff header parsing rules and scoring algorithm
3. `docs/ops/registry/lint.md` - verify ff lint registry entry
4. `PoT.md` Sections 4.2 and 4.3 - list duplication candidates
5. `docs/MANUAL.md` Closeout Cycle block - restatement candidates

## 3.3 Scope and Safety
Objective:
Add CCD headers to Wave 1 files, compress authoring to the declared density bands, and escalate ff lint to fail when any Wave 1 file is missing a CCD header.

In scope:
- `storage/dp/active/allowlist.txt`
- `PoT.md`
- `docs/MAP.md`
- `docs/MANUAL.md`
- `ops/lib/manifests/CONTEXT.md`
- `ops/lib/manifests/CONSTRAINTS.md`
- `ops/lib/manifests/CONTRACTOR.md`
- `ops/lib/manifests/CORE.md`
- `ops/lib/manifests/OPS.md`
- `ops/lib/manifests/DISCOVERY.md`
- `README.md`
- `tools/lint/ff.sh`
- `storage/handoff/CLOSING-DP-OPS-0088.md`
- `TASK.md`
- `SoP.md`
- `PoW.md`
- `llms.txt`
- `llms-core.txt`
- `llms-full.txt`

Out of scope:
- Wave 2 governance and registry surfaces outside this packet
- Wave 3 specification tree updates
- Template field additions for `ff_target` and `ff_band`
- Workflow wiring changes in `.github/workflows/gates.yml`
- Any file not listed in scope

Safety and invariants:
- No manual edits to generated outputs under `archives/` and no manual edits to `llms.txt`, `llms-core.txt`, or `llms-full.txt` before regeneration
- CCD headers must use `<!-- CCD: ff_target="..." ff_band="..." -->` on the first non blank content line after front matter
- No contractions in authored markdown
- `tools/lint/ff.sh` edits are limited to `is_wave1_hardened()` plus the no header branch escalation
- Context manifest edits retain assertions constraints and pointers while removing rationale sentences only
- Allowlist hard gate remains active for every touched path
- Authoring rewrites preserve operational meaning

Worker Constraints (SSOT injected):
## Section 1: Universal Template Rules
- Template source uses `tpl` files with optional YAML frontmatter
- Canon frontmatter keys: `template_type` `template_id` `template_version` `requires_slots` `includes`
- Renderer strips frontmatter before output write
- Slot token form `\{\{TOKEN\}\}` with uppercase alphanumeric underscore
- Include forms `\{\{@include:path\}\}` and `\{\{@include:path#section\}\}`
- Include resolution is strict: missing file fail missing section fail circular graph fail
- Strict mode default: every required slot value present and no unresolved token
- Non strict mode allowed only for lint and normalization workflows
- Worker facing generated surfaces remain pointer first and exclude disposable artifacts

Target Files allowlist (hard gate):
- storage/dp/active/allowlist.txt

## 3.4 Execution Plan (A-E)

### 3.4.1 State
Branch is `work/ccd-wave1-headers-2026-02-21` at `29dc0be7` with Phase 0 CCD foundations already present from DP OPS 0087. Wave 1 files required CCD headers and ff lint required escalation from warning to failure for missing Wave 1 headers.

### 3.4.2 Request
1. Replace `storage/dp/active/allowlist.txt` with the Wave 1 scope entries
2. Add the Tier 3 header in `PoT.md` and compress Section 4 duplicates using canonical pointers
3. Add the Tier 2 header in `docs/MAP.md` and rewrite Section 0 to pointer driven routing
4. Add the Tier 2 header in `docs/MANUAL.md` and remove closeout restatement prose
5. Add the Tier 1 header in `ops/lib/manifests/CONTEXT.md` and retain assertion only content
6. Add Tier 1 headers in `ops/lib/manifests/CONSTRAINTS.md`, `ops/lib/manifests/CONTRACTOR.md`, `ops/lib/manifests/CORE.md`, `ops/lib/manifests/OPS.md`, and `ops/lib/manifests/DISCOVERY.md` with assertion dense wording
7. Add the Tier 4 header in `README.md` and keep pointer accurate routing
8. Add `is_wave1_hardened()` to `tools/lint/ff.sh` and escalate no header handling to failure for Wave 1 paths only
9. Run mandatory receipt commands plus ff and targeted style checks
10. Run closeout cycle and generate RESULTS with `ops/bin/certify --dp="DP-OPS-0088" --out=auto`

### 3.4.3 Changelog
UPDATE storage/dp/active/allowlist.txt
UPDATE PoT.md
UPDATE docs/MAP.md
UPDATE docs/MANUAL.md
UPDATE ops/lib/manifests/CONTEXT.md
UPDATE ops/lib/manifests/CONSTRAINTS.md
UPDATE ops/lib/manifests/CONTRACTOR.md
UPDATE ops/lib/manifests/CORE.md
UPDATE ops/lib/manifests/OPS.md
UPDATE ops/lib/manifests/DISCOVERY.md
UPDATE README.md
UPDATE tools/lint/ff.sh
UPDATE TASK.md
UPDATE SoP.md
UPDATE PoW.md
UPDATE llms.txt
UPDATE llms-core.txt
UPDATE llms-full.txt
NEW storage/handoff/CLOSING-DP-OPS-0088.md
NEW archives/manifests/compile-<timestamp>-<hash>.md
NEW archives/surfaces/PoW-2026-02-21-<hash>.md
NEW archives/surfaces/SoP-2026-02-21-<hash>.md
NEW archives/surfaces/TASK-DP-OPS-0088-<hash>.md

### 3.4.4 Patch / Diff
- Replace active allowlist contents with Wave 1 scope entries and closing sidecar path
- Insert Wave 1 CCD headers with declared targets and bands on each in scope file
- Rewrite targeted prose in `PoT.md`, `docs/MAP.md`, `docs/MANUAL.md`, `README.md`, `ops/lib/manifests/CONSTRAINTS.md`, and `ops/lib/manifests/CONTRACTOR.md` to match tier density expectations without changing operational meaning
- Add `is_wave1_hardened()` in `tools/lint/ff.sh` before markdown enumeration and switch the no header branch to Wave 1 fail logic

### 3.4.5 Receipt (Proofs to collect) - MUST RUN

**Mandatory receipt commands (always run; do not omit):**
- bash tools/lint/dp.sh TASK.md
- bash tools/lint/task.sh
- bash tools/lint/integrity.sh
- bash tools/lint/style.sh
- git diff --name-only
- git diff --stat
- comm -23 <(git diff --name-only | sort) <(sort storage/dp/active/allowlist.txt) || true
- comm -23 <(git ls-files --others --exclude-standard | sort) <(sort storage/dp/active/allowlist.txt) || true
- ./ops/bin/open

**DP-specific receipt commands (scope-specific; author below):**
- bash tools/lint/ff.sh
- bash -n tools/lint/ff.sh
- bash tools/lint/style.sh PoT.md docs/MAP.md docs/MANUAL.md ops/lib/manifests/CONTEXT.md ops/lib/manifests/CONSTRAINTS.md ops/lib/manifests/CONTRACTOR.md ops/lib/manifests/CORE.md ops/lib/manifests/OPS.md ops/lib/manifests/DISCOVERY.md README.md
- ./ops/bin/llms --out-dir=/home/nos4r2/dev/nukece
- ./ops/bin/dump --scope=platform --exclude-dir=opt/_factory --format=chatgpt --out=auto

## 3.5 Closeout (Mandatory Routing)
- Execute docs/MANUAL.md Closeout Cycle in order (Verify, Harvest, Refresh, Log, Prune).
- Update SoP.md and PoW.md with DP entries, including objective summary and verification commands run.
- Protocol order for closeout: Verify -> Generate Results -> COMMIT (Operator Only) -> Prune.
- Run prune hygiene: ./ops/bin/prune --scrub.
- Regenerate session artifacts: `./ops/bin/open --out=auto`
- Capture updated platform state: `./ops/bin/dump --scope=platform --format=chatgpt --out=auto`
- Ensure the next session begins with refreshed session artifacts and matching receipts.

### 3.5.1 Mandatory Closing Block
Commit Message (plaintext)

Create Pull Request (Title) (plaintext)

Create Pull Request (Description) (markdown)

Confirm Merge (Commit Message) (plaintext) (Must differ from #1)

Confirm Merge (Extended Description) (plaintext)

Confirm Merge (Add a Comment) (markdown)
