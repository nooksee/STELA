---
trace_id: stela-20260323T174840Z-1240155b
packet_id: DP-OPS-0224
created_at: 2026-03-23T17:56:25Z
previous: archives/surfaces/TASK-DP-OPS-0219-c37d55ce.md
---
# STELA TASK DASHBOARD
Status: ACTIVE
Owner: Integrator
Last Updated: 2026-03-19

## 1. Session State
Pointer: Session context output (generated by ops/bin/open)
Context Manifest: ops/lib/manifests/CONTEXT.md (checked by tools/lint/context.sh)

Session State contract:
- Session State is pointer-first. Do not write inline branch or hash state here.
- OPEN is the session state snapshot. OPEN-PORCELAIN is conditional dirty-state detail.
- RESULTS is the audit record for the DP.
- TASK is the stable routing surface.

## 2. Logic Pointers
Primary Constraint: PoT.md (Policy of Truth).
Surface Contract Spec: docs/ops/specs/surfaces/task.md.

### 2.1 Governance Pointers
Jurisdiction: PoT.md Section 3.
Git Authority: PoT.md Section 4.1.
Behavioral Standard: PoT.md Section 4.2.
Staffing Protocol: PoT.md Section 4.1.

### 2.2 Enforcement and Gates
Style: tools/lint/style.sh
Context: tools/lint/context.sh
Truth: tools/lint/truth.sh
DP lint: tools/lint/dp.sh (enforces DP transaction rules in Section 3; does not enforce TASK container schema)
TASK lint: tools/lint/task.sh (sole TASK surface/container schema enforcer)
Repo verify: tools/verify.sh
llms parity: tools/lint/llms.sh

## 3. Current Dispatch Packet (DP)

Section-by-section authoring protocol:
- Generate DP structure from `ops/src/surfaces/dp.md.tpl` using `ops/bin/draft`.
- Do not hand-edit DP structural boilerplate in `TASK.md` after generation.
- Manual edits are limited to slot content only (objective, scope, safety, plan body, receipt body).
- Freeze each completed subsection in order (3.1 -> 3.2 -> 3.3 -> 3.4.x).
- Do not revise earlier sections to justify later work. If scope changes, end the thread and draft a new DP.

Freeze Rule:
- If a completed DP subsection changes later, append an AMENDMENT block in RESULTS; otherwise DP is FAIL.

AMENDMENT: minimum fields
- Reopened section:
- Reason:
- Impact:
- Allowlist change (none or exact paths):

Input discipline (hard rule):
- Worker receives only the DP text. Worker does not require OPEN or DUMP attachments.
- DP writer must not attach or cite disposable artifacts (rough notes, chat logs, scratch drafts, throwaway excerpts).
- DP writer must not include pasted bundles (OPEN, DUMP payloads, manifests) inside the DP body.
- Canon pointers only. Evidence belongs in storage artifacts and RESULTS.



### DP-OPS-0224: Analyst-to-Planning rename closeout from inherited current branch

## 3.1 Freshness Gate (Must Pass Before Work)
Base Branch: main
Required Work Branch: work/dp-ops-0219-analyst-planning-upgrade-2026-03-22
Base HEAD: c37d55ce
Freshness Stamp: 2026-03-23
Note: FRESHNESS_STAMP must be YYYY-MM-DD format only. No trace tokens, no timestamps, no other text. Certify rejects all other forms. dp.sh enforces at lint time.

Required local re-check (worker runs; paste outputs in RESULTS):
- git rev-parse --abbrev-ref HEAD
- git rev-parse --short HEAD
- git status --porcelain

Preconditions:
- Stay on the current inherited work branch.
- This packet continues from the current inherited in-progress branch state rather than claiming a clean-start baseline.
- Do not create, switch, reset, rebase, or cherry-pick branches as part of this packet.
- Current branch state may be dirty or clean, but every touched tracked path must remain within this packet's authoritative scope or certify-owned generated surfaces.

STOP conditions:
- STOP if the active branch is not `work/dp-ops-0219-analyst-planning-upgrade-2026-03-22`.
- STOP if Base HEAD is not `c37d55ce`.
- STOP if `git status --porcelain` shows tracked paths outside this packet's authoritative scope or certify-owned generated surfaces.
- STOP if this packet would require baseline reconstruction, branch surgery, or retroactive clean-start claims.
- STOP if verification finds substantive rename drift outside the file set listed below.

## 3.1.1 DP Preflight Gate (Run Before Any Edits)
Purpose:
- Catch malformed DP or TASK schema before closeout work begins.

Worker runs (paste outcome in RESULTS):
- bash tools/lint/dp.sh --test
- bash tools/lint/dp.sh TASK.md
- bash tools/lint/task.sh

STOP if any preflight check fails.

### CbC Design Discipline Preflight (TASK.md §3.1.1)
Applicable — this packet does not authorize new architecture or new tooling behavior. It authorizes only:
- a quick verification sweep over the already-landed analyst-to-planning rename,
- the smallest repair needed if verification finds a missed rename/proof blocker inside the existing file set,
- closeout, certify, and audit bundle generation.

No new lanes, no new policy, no redesign.

## 3.2 Required Context Load (Read Before Doing Anything)

### 3.2.1 Canon load order (always)
Worker must confirm loaded before edits begin:
1. PoT.md
2. SoP.md
3. PoW.md
4. TASK.md
5. docs/MAP.md
6. docs/MANUAL.md
7. ops/lib/manifests/CONTEXT.md

Notes:
- Worker does not read OPEN. OPEN is for Integrator state refresh and receipts.
- Disposable artifacts must not be referenced or included.
- Worker input is the DP text only.
- DP writer must not attach or cite disposable artifacts.
- DP writer must not embed pasted bundles.

## Rules

1. Temp path rule uses `var/tmp` only
2. RESULTS generation rule uses `ops/bin/certify` only
3. Receipt rule runs every DP-mandated command and treats any skip as STOP
4. Disposable artifact rule treats OPEN dump bundles and manifests as non-canonical session artifacts
5. Use repo-relative paths only in all output. Never print absolute filesystem paths. Do not emit clickable file links.
6. This packet must describe the inherited current branch state truthfully. Do not narrate a clean start that did not happen.
7. Do not claim any proof from prior attempts unless it is rerun now and logged now.

### 3.2.2 DP-scoped load order (per DP)
- ops/src/stances/planning.md.tpl
- docs/ops/specs/stances/planning.md
- ops/lib/manifests/BUNDLE.md
- ops/lib/scripts/bundle.sh
- ops/bin/manifest
- ops/bin/template
- ops/bin/help
- ops/etc/persistence.manifest
- ops/etc/retention.manifest
- ops/etc/verification.manifest
- tools/lint/response.sh
- tools/lint/style.sh
- tools/test/bundle.sh
- tools/test/factory.sh
- docs/ops/specs/binaries/bundle.md
- docs/ops/specs/tools/test/bundle.md
- docs/ops/specs/tools/test/factory.md
- docs/ops/specs/tools/lint/response.md
- docs/ops/specs/tools/lint/style.md
- docs/ops/specs/tools/lint/plan.md
- docs/ops/specs/stances/plan.md
- docs/ops/specs/stances/architect.md
- docs/ops/registry/agents.md
- docs/ops/registry/binaries.md
- docs/ops/registry/factory.md
- docs/ops/registry/lint.md
- docs/MANUAL.md
- docs/MAP.md
- storage/dp/active/allowlist.txt
- storage/handoff/CLOSING.md

## 3.3 Scope and Safety
Objective:
Use the current inherited work branch, pick up where execution left off, perform a quick but real verification that the analyst-to-planning rename already landed across the intended runtime/lint/test/spec surfaces, make only the smallest in-scope fix if verification finds a missed item, then complete closeout, certify, and the audit bundle truthfully.

In scope:
- Verification of the already-landed analyst-to-planning rename across the authoritative file set below
- Minimal repair of any missed rename/proof blocker found during this verification sweep, limited to the same file set
- Allowlist sync for the actual tracked file set and closeout-generated tracked artifacts
- Current-run proof collection only
- SoP / PoW updates
- Closing sidecar completion
- llms refresh outputs and `ops/lib/manifests/OPS.md`
- certify and audit bundle generation

Out of scope:
- Fresh reissue framing
- Addendum framing
- Branch creation, switching, reset, rebase, cherry-pick, or baseline reconstruction
- Any attempt to retroactively prove a clean start
- Broad doc churn beyond the already-implemented rename and closeout-required truthfulness fixes
- New role renames, contract redesign, or certify behavior changes
- Trust-me narratives or references to proofs not rerun in this packet

Safety and invariants:
- Preserve `Analyst` as the staffing role name.
- `planning` is the canonical task/profile/process name for this path.
- If the quick verification sweep shows the implementation is already correct, do not touch implementation files just to make noise.
- Do not cite `storage/handoff/PLAN-FACTORY.md` or any disposable artifact anywhere in the packet, request, patch plan, or narrative.
- Do not hand-author generated outputs; use canonical tools only for RESULTS, llms outputs, archive leaves, dump artifacts, and audit artifacts.
- Do not claim any command, test, or proof unless its output is present in the current certify log or directly visible in current canonical artifacts.
- `ops/bin/manifest stance-planning` must resolve to `ops/src/stances/planning.md.tpl`.
- No valid runtime/task/profile route may remain that still requires `--profile=analyst` for this rename path.
- Allowlist hard gate applies to every tracked path touched by current execution or closeout.

Worker Constraints (SSOT injected):
## Section 1: Universal Template Rules
- Template source uses `tpl` files with optional YAML frontmatter
- Canon frontmatter keys: `template_type` `template_id` `template_version` `requires_slots` `includes`
- Renderer strips frontmatter before output write
- Slot token form `\{\{TOKEN\}\}` with uppercase alphanumeric underscore
- Include forms `\{\{@include:path\}\}` and `\{\{@include:path#section\}\}`
- Include resolution is strict: missing file fail missing section fail circular graph fail
- Strict mode default: every required slot value present and no unresolved token
- Non-strict mode allowed only for lint and normalization workflows
- Worker-facing generated surfaces remain pointer-first and exclude disposable artifacts

Target Files allowlist (hard gate):
- storage/dp/active/allowlist.txt

## 3.4 Execution Plan (A-E)

### 3.4.1 State
- The current branch already contains the substantive analyst-to-planning rename work from the prior effort.
- The remaining need is not a broad implementation pass. It is a truthful continuation packet:
  - confirm the rename actually landed where intended,
  - repair only any small missed item that blocks proof,
  - close out and certify from the current branch state.
- Prior audit failure centered on packet truthfulness, scope truth, disposable-artifact citation, and unsupported proof claims.
- This packet does not attempt to erase or relabel that history. It simply completes the existing branch truthfully from its present state.

### 3.4.2 Request
- a. Stay on `work/dp-ops-0219-analyst-planning-upgrade-2026-03-22` and continue from the current inherited branch state
- b. Perform a quick verification sweep across the authoritative file set to confirm the analyst-to-planning rename is already present and coherent
- c. Preserve `Analyst` as the staffing role name while confirming `planning` is the canonical task/profile/process name
- d. If the sweep finds a missed rename, stale route, stale spec reference, or proof blocker inside the authorized file set, make the smallest fix necessary and no broader rewrite
- e. Update `storage/dp/active/allowlist.txt` to match the actual tracked file set touched by the current tree and closeout-generated artifacts
- f. Refresh canonical generated outputs with approved tools only: `llms.txt`, `llms-core.txt`, `llms-full.txt`, and `ops/lib/manifests/OPS.md`
- g. Complete SoP, PoW, CLOSING, certify, and audit bundle generation using current-run proof only
- h. Keep the certify narrative strictly limited to what was observed and rerun in this packet

### 3.4.3 Changelog
UPDATE:
- ops/src/stances/planning.md.tpl
- docs/ops/specs/stances/planning.md
- ops/lib/manifests/BUNDLE.md
- ops/lib/scripts/bundle.sh
- ops/bin/manifest
- ops/bin/template
- ops/bin/help
- ops/etc/persistence.manifest
- ops/etc/retention.manifest
- ops/etc/verification.manifest
- tools/lint/response.sh
- tools/lint/style.sh
- tools/test/bundle.sh
- tools/test/factory.sh
- docs/MANUAL.md
- docs/MAP.md
- docs/ops/registry/agents.md
- docs/ops/registry/binaries.md
- docs/ops/registry/factory.md
- docs/ops/registry/lint.md
- docs/ops/specs/binaries/bundle.md
- docs/ops/specs/stances/architect.md
- docs/ops/specs/stances/plan.md
- docs/ops/specs/tools/lint/plan.md
- docs/ops/specs/tools/lint/response.md
- docs/ops/specs/tools/lint/style.md
- docs/ops/specs/tools/test/bundle.md
- docs/ops/specs/tools/test/factory.md
- storage/dp/active/allowlist.txt
- SoP.md
- PoW.md
- storage/handoff/CLOSING.md
- storage/handoff/RESULTS.md

GENERATE:
- llms.txt
- llms-core.txt
- llms-full.txt
- ops/lib/manifests/OPS.md
- archives/surfaces/TASK-DP-OPS-0224-*.md
- archives/surfaces/SoP-2026-03-23-*.md
- archives/surfaces/PoW-2026-03-23-*.md
- storage/handoff/AUDIT-work-dp-ops-0219-analyst-planning-upgrade-2026-03-22-*.txt
- storage/handoff/AUDIT-work-dp-ops-0219-analyst-planning-upgrade-2026-03-22-*.manifest.json
- storage/handoff/AUDIT-work-dp-ops-0219-analyst-planning-upgrade-2026-03-22-*.tar

### 3.4.4 Patch / Diff
1. Capture the actual starting branch, HEAD, and working-tree state exactly as found.
2. Run a focused verification sweep over the authoritative scope:
   - confirm `ops/bin/manifest stance-planning --out=-` resolves correctly
   - confirm the planning stance/spec files exist and are wired
   - confirm stale route strings such as `--profile=analyst`, `stance-analyst`, and old analyst stance file references do not remain in the active runtime/spec path
3. If the sweep shows the implementation is already correct, leave implementation files untouched and proceed directly to closeout.
4. If the sweep finds a missed rename/proof blocker inside the authoritative scope, make the smallest fix necessary and rerun the affected checks.
5. Update `storage/dp/active/allowlist.txt` so it covers every tracked file actually touched in this packet, including refresh/closeout-generated tracked artifacts.
6. Run the required verification matrix and keep reruns honest and logged.
7. Refresh generated canon with `./ops/bin/llms`, then run `./ops/bin/allowlist`, then rerun integrity.
8. Maintain `storage/handoff/CLOSING.md` with observable reality only.
9. Run `./ops/bin/open --out=auto`, then run `./ops/bin/certify --dp=DP-OPS-0224 --allow-intake-fallback --out=auto`.
10. After certify succeeds, generate the audit delivery artifacts with `./ops/bin/bundle --profile=audit --out=auto`.
11. Refresh session artifacts and complete prune hygiene per MANUAL closeout order.

### 3.4.5 Receipt (Proofs to collect) - MUST RUN

**Mandatory receipt commands (always run; do not omit):**
- bash tools/lint/dp.sh TASK.md
- bash tools/lint/task.sh
- bash tools/lint/integrity.sh
- bash tools/lint/style.sh
- ./ops/bin/llms
- bash tools/lint/integrity.sh
- git diff --name-only
- git diff --stat
- comm -23 <(git diff --name-only | sort) <(sort storage/dp/active/allowlist.txt) || true
- comm -23 <(git ls-files --others --exclude-standard | sort) <(sort storage/dp/active/allowlist.txt) || true
- ./ops/bin/open

**DP-specific receipt commands (scope-specific; author below):**
Note: Command substitution forms (e.g., $(pwd), $(git ...)) are rejected by certify replay. Use literal values only. dp.sh enforces at lint time.
- ops/bin/manifest stance-planning --out=-
- rg -n -- '--profile=analyst|stance-analyst|ops/src/stances/analyst\.md\.tpl|docs/ops/specs/stances/analyst\.md' ops docs tools || true
- bash tools/lint/response.sh --test
- bash tools/test/factory.sh
- bash tools/test/bundle.sh --mode=certify-critical
- bash tools/test/bundle.sh --slice=route-contract
- bash tools/test/bundle.sh --slice=package-contract
- bash tools/verify.sh --mode=full

Notes:
- Do not claim historical proofs. Rerun what you need and log it now.
- If any verification command fails, STOP, fix only within scope, and rerun.

## 3.5 Closeout (Mandatory Routing)
- Execute `docs/MANUAL.md` Closeout Cycle in order: Verify -> Harvest -> Refresh -> Log -> Prune.
- Update SoP.md and PoW.md with DP-OPS-0224 entries that truthfully describe:
  - this packet continued from the inherited current branch state,
  - the implementation was verified in place,
  - only current-run proof is asserted.
- Protocol order for closeout: Verify -> Generate Results -> COMMIT (Operator Only) -> Prune.
- Run `./ops/bin/open --out=auto` before certify so the current run has a valid `STELA_TRACE_ID`.
- Refresh llms artifacts with `./ops/bin/llms`.
- Regenerate session artifacts with `./ops/bin/open --out=auto`.
- Run prune hygiene with `./ops/bin/prune --scrub`.
- Audit bundle generation is owned by `./ops/bin/bundle --profile=audit --out=auto`.
- Do not emit any extra non-canonical dump or bundle artifacts unless explicitly authorized by this packet.

### 3.5.1 Mandatory Closing Sidecar
Closing sidecar content is generated and maintained at `storage/handoff/CLOSING.md` and is validated by `ops/bin/certify` as a hard gate at certification time. Do not author, predict, populate, or approximate any sidecar-derived closeout output outside this sidecar.

Certify separately collects contractor-authored narrative for the RESULTS Contractor Execution Narrative section at certify time via interactive editor prompt; this narrative input is independent from closing sidecar content.

Before running certify, confirm `storage/handoff/CLOSING.md` reflects observable current reality only.

Closing sidecar fields required at certify time:
- Commit Message
- Create Pull Request (Title)
- Create Pull Request (Description)
- Confirm Merge (Commit Message)
- Confirm Merge (Extended Description)
- Confirm Merge (Add a Comment)
