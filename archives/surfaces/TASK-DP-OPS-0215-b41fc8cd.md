---
trace_id: stela-20260319T195345Z-65ba4ac8
packet_id: DP-OPS-0215
created_at: 2026-03-19T19:58:14Z
previous: (none)
---
# STELA TASK DASHBOARD
Status: ACTIVE
Owner: Integrator
Last Updated: 2026-03-18

## 1. Session State
Pointer: Session context output (generated by ops/bin/open)
Context Manifest: ops/lib/manifests/CONTEXT.md (checked by tools/lint/context.sh)

Session State contract:
- Session State is pointer-first. Do not write inline branch or hash state here.
- OPEN is the session state snapshot. OPEN-PORCELAIN is conditional dirty-state detail.
- RESULTS is the audit record for the DP.
- TASK is the stable routing surface.

## 2. Logic Pointers
Primary Constraint: PoT.md (Policy of Truth).
Surface Contract Spec: docs/ops/specs/surfaces/task.md.

### 2.1 Governance Pointers
Jurisdiction: PoT.md Section 3.
Git Authority: PoT.md Section 4.1.
Behavioral Standard: PoT.md Section 4.2.
Staffing Protocol: PoT.md Section 4.1.

### 2.2 Enforcement and Gates
Style: tools/lint/style.sh
Context: tools/lint/context.sh
Truth: tools/lint/truth.sh
DP lint: tools/lint/dp.sh (enforces DP transaction rules in Section 3; does not enforce TASK container schema)
TASK lint: tools/lint/task.sh (sole TASK surface/container schema enforcer)
Repo verify: tools/verify.sh
llms parity: tools/lint/llms.sh

## 3. Current Dispatch Packet (DP)

Section-by-section authoring protocol:
- Generate DP structure from `ops/src/surfaces/dp.md.tpl` using `ops/bin/draft`.
- Do not hand-edit DP structural boilerplate in `TASK.md` after generation.
- Manual edits are limited to slot content only (objective, scope, safety, plan body, receipt body).
- Freeze each completed subsection in order (3.1 -> 3.2 -> 3.3 -> 3.4.x).
- Do not revise earlier sections to justify later work. If scope changes, end the thread and draft a new DP.

Freeze Rule:
- If a completed DP subsection changes later, append an AMENDMENT block in RESULTS; otherwise DP is FAIL.

AMENDMENT: minimum fields
- Reopened section:
- Reason:
- Impact:
- Allowlist change (none or exact paths):

Input discipline (hard rule):
- Worker receives only the DP text. Worker does not require OPEN or DUMP attachments.
- DP writer must not attach or cite disposable artifacts (rough notes, chat logs, scratch drafts, throwaway excerpts).
- DP writer must not include pasted bundles (OPEN, DUMP payloads, manifests) inside the DP body.
- Canon pointers only. Evidence belongs in storage artifacts and RESULTS.


### DP-OPS-0215: Dispatch stance behavior recenter slice 2

## 3.1 Freshness Gate (Must Pass Before Work)
Base Branch: main
Required Work Branch: work/dp-ops-0215-dispatch-stance-behavior-recenter-slice-2-2026-03-19
Base HEAD: b41fc8cd
Freshness Stamp: 2026-03-19
Note: FRESHNESS_STAMP must be YYYY-MM-DD format only. No trace tokens, no timestamps, no other text. Certify rejects all other forms. dp.sh enforces at lint time.

Required local re-check (worker runs; paste outputs in RESULTS):
- git rev-parse --abbrev-ref HEAD
- git rev-parse --short HEAD
- git status --porcelain

Preconditions:
- No commits on main.
- Working tree must be clean before execution begins.
- If Base HEAD changes, regenerate session artifacts from canonical tools before proceeding.

STOP conditions:
- STOP if any mismatch (branch, Base HEAD, missing work branch).
- STOP if working tree is dirty before execution begins.
- STOP if told to create or switch branches.

## 3.1.1 DP Preflight Gate (Run Before Any Edits)
Purpose:
- Catch malformed DP or TASK schema before work begins.

Worker runs (paste outcome in RESULTS):
- bash tools/lint/dp.sh --test
- bash tools/lint/dp.sh TASK.md
- bash tools/lint/task.sh

STOP if any preflight check fails.

### CbC Design Discipline Preflight (TASK.md §3.1.1)
Required when the DP objective adds, modifies, or replaces a linter, script, guard, or validation binary.
For non-tooling DPs: state "Not applicable" with a one-line justification.
Applicable. This packet changes machine-ingest dispatch stance contracts and their shared contract layer. The goal is behavioral effect, not local wording churn.

Invariants:
1. Hard truth/evidence discipline stays hard; the packet must not authorize fabricated inspection or free-form invention.
2. Dispatch lanes may make bounded continuity decisions needed to turn visible plan intent into usable output, but they must not expand option, slice, or authority.
3. Stance changes must be evaluated as a family, not as isolated local prose patches; adjacent dispatch stances must stay coherent with each other.
4. Output contracts remain machine-ingest safe: single fenced body, no role drift, no audit/receipt contamination in non-audit lanes.

Structural prevention assessment:
- Partial. Shared stance rules can structurally carry more of the common truth/output/continuity contract so local stance files stop reintroducing dominant prohibition stacks. Detection remains necessary for behavior that is still frozen in lane-local wording or specs.

Complexity note:
- Moderate. The packet is narrow in file count but high impact because small stance lines strongly shape downstream drafting behavior.

Decision Record Trigger:
- If execution discovers that materially useful recentering requires transport/runtime changes outside stance/spec/shared-contract surfaces, set `Decision Required: Yes` and author a decision leaf under `archives/decisions/`.
- Otherwise set `Decision Required: No` and `Decision Leaf: None`.

## 3.2 Required Context Load (Read Before Doing Anything)

### 3.2.1 Canon load order (always)
Worker must confirm loaded before edits begin:
1. PoT.md
2. SoP.md
3. PoW.md
4. TASK.md
5. docs/MAP.md
6. docs/MANUAL.md
7. ops/lib/manifests/CONTEXT.md

Notes:
- Worker does not read OPEN. OPEN is for Integrator state refresh and for receipts.
- Disposable artifacts must not be referenced or included.
- Worker input is the DP text only.
- DP writer must not attach or cite disposable artifacts.
- DP writer must not embed pasted bundles.

## Rules

1 Temp path rule uses `var/tmp` only
2 RESULTS generation rule uses `ops/bin/certify` only
3 Receipt rule runs every DP mandated command and treats any skip as STOP
4 Disposable artifact rule treats OPEN dump bundles and manifests as non canonical session artifacts
5 Use repo-relative paths only in all output. Never print absolute filesystem paths. Do not emit clickable file links.

### 3.2.2 DP-scoped load order (per DP)
- ops/src/shared/stances.json
- ops/src/stances/analyst.md.tpl
- ops/src/stances/architect.md.tpl
- ops/src/stances/conformist.md.tpl
- docs/ops/specs/stances/analyst.md
- docs/ops/specs/stances/architect.md
- docs/ops/specs/stances/conformist.md
- tools/lint/response.sh
- docs/ops/specs/tools/lint/response.md

## 3.3 Scope and Safety
Objective:
- Recenter dispatch stance behavior so architect, analyst, and conformist stop collapsing toward safe read-only or fail-closed output when visible evidence already settles bounded continuity inside the selected scope.
- Move the dominant behavior-shaping lines from prohibition-first to truth-first plus bounded continuity, without weakening machine-ingest safety.

In scope:
- Shared stance contract rules that set cross-lane truth, output, and continuity behavior.
- Architect stance lines that currently overweight handoff-only, no-correction, no-bridge-decision, and immediate-emission behavior.
- Analyst stance lines that still push plan output toward generic flattening even when the needed repo surfaces are attached.
- Conformist stance lines that treat missing continuity details as an automatic STOP instead of a bounded normalization task.
- Matching stance specs and response-lint spec text so the documented contract matches the intended behavioral effect.

Out of scope:
- Bundle/runtime transport changes, request assembly, or plan folder split implementation.
- Smoke isolation, proof economy, or route-contract runtime reduction.
- Auditor, foreman, addendum, or execution-decision redesign.
- Current packet promotion into `TASK.md` or other closeout-lineage work.
- New options, phases, slices, or authority expansion beyond bounded continuity inside the selected dispatch scope.

Safety and invariants:
- No manual generated-output fabrication.
- No structural `TASK.md`, `SoP.md`, `PoW.md`, `RESULTS.md`, or `CLOSING.md` edits.
- Keep machine-ingest safety hard: one fenced body, role-drift rejection, and hard truth/evidence lines remain intact.
- Do not turn bounded continuity into free invention; any allowed inference must stay within visible intent and existing repo patterns.
- Compare adjacent dispatch stances before finalizing a local stance edit so the family moves coherently.
- Allowlist hard gate applies before implementation edits.

Worker Constraints (SSOT injected):
## Section 1: Universal Template Rules
- Template source uses `tpl` files with optional YAML frontmatter
- Canon frontmatter keys: `template_type` `template_id` `template_version` `requires_slots` `includes`
- Renderer strips frontmatter before output write
- Slot token form `\{\{TOKEN\}\}` with uppercase alphanumeric underscore
- Include forms `\{\{@include:path\}\}` and `\{\{@include:path#section\}\}`
- Include resolution is strict: missing file fail missing section fail circular graph fail
- Strict mode default: every required slot value present and no unresolved token
- Non strict mode allowed only for lint and normalization workflows
- Worker facing generated surfaces remain pointer first and exclude disposable artifacts

Target Files allowlist (hard gate):
- storage/dp/active/allowlist.txt

## 3.4 Execution Plan (A-E)

### 3.4.1 State
- `0214` closed cleanly and audit passed, but the intended behavioral effect did not land: architect still reads primarily as prohibition-first, analyst remains too flattening in plan mode, and conformist still defaults to STOP on continuity gaps.
- The current plan calls for finishing the first stance loosen/tighten pass by materially changing downstream behavior instead of adding softer guidance around the existing dominant lines.
- Shared stance rules now carry basic truth/output guidance, but the shared layer is still too thin to keep dispatch lanes from drifting into individually patched behavior.
- This packet is intentionally limited to stance/spec/shared-contract surfaces; transport/runtime and smoke debt stay for later slices.

### 3.4.2 Request
- Recenter the shared dispatch contract so truth discipline and bounded continuity are the dominant design, not lane-local prohibition stacks.
- Update architect so selected slices still govern scope, but the lane is plainly authorized to make small bridge decisions and correct stale or self-contradictory request details inside that scope when attached evidence is sufficient.
- Update analyst so explicit plan-output mode can produce a serviceable handoff from attached repo surfaces without artificially flattening everything to generic language.
- Update conformist so normalization remains bounded and structural, but no longer deadlocks on ordinary continuity gaps that are resolvable from visible intent and repo-local patterns.
- Keep all three lanes machine-ingest safe and mutually coherent: same truth floor, same role-drift boundaries, same bounded-continuity idea expressed through one family design instead of patched local exceptions.

### 3.4.3 Changelog
- UPDATE ops/src/shared/stances.json
- UPDATE ops/src/stances/analyst.md.tpl
- UPDATE ops/src/stances/architect.md.tpl
- UPDATE ops/src/stances/conformist.md.tpl
- UPDATE docs/ops/specs/stances/analyst.md
- UPDATE docs/ops/specs/stances/architect.md
- UPDATE docs/ops/specs/stances/conformist.md
- UPDATE docs/ops/specs/tools/lint/response.md

### 3.4.4 Patch / Diff
1. Verify that `storage/dp/active/allowlist.txt` already covers every tracked file in this packet before editing any stance or spec surface.
2. Expand `ops/src/shared/stances.json` so the common dispatch stance layer carries the reusable truth/continuity/output rules that should not keep being re-patched lane by lane.
3. Rewrite the dominant contract lines in `ops/src/stances/architect.md.tpl` so architect remains slice-bounded and machine-ingest safe, but is no longer defined primarily by handoff-only / no-bridge / immediate-full-DP prohibition language.
4. Update `docs/ops/specs/stances/architect.md` to match the new intended behavioral effect and make the changed dominant lines explicit in the spec.
5. Update `ops/src/stances/analyst.md.tpl` so explicit plan-output mode may use attached repo surfaces to produce a concrete handoff and make bounded continuity decisions when they do not change option, slice, or authority.
6. Update `docs/ops/specs/stances/analyst.md` so the spec matches that tighter, more useful plan-output contract.
7. Update `ops/src/stances/conformist.md.tpl` so conformist remains a bounded normalizer but no longer treats ordinary continuity gaps as an automatic STOP when visible intent and repo patterns settle the repair.
8. Update `docs/ops/specs/stances/conformist.md` and `docs/ops/specs/tools/lint/response.md` so the documented lane family and ingress contract reflect the same machine-ingest-safe but less deadlocking design.
9. Run the targeted stance/render/lint proof stack and confirm the packet changed behavior at the contract level without adding transport/runtime scope.

### 3.4.5 Receipt (Proofs to collect) - MUST RUN

**Mandatory receipt commands (always run; do not omit):**
- bash tools/lint/dp.sh TASK.md
- bash tools/lint/task.sh
- bash tools/lint/integrity.sh
- bash tools/lint/style.sh
- ./ops/bin/llms
- bash tools/lint/integrity.sh
- git diff --name-only
- git diff --stat
- comm -23 <(git diff --name-only | sort) <(sort storage/dp/active/allowlist.txt) || true
- comm -23 <(git ls-files --others --exclude-standard | sort) <(sort storage/dp/active/allowlist.txt) || true
- ./ops/bin/open

**DP-specific receipt commands (scope-specific; author below):**
Note: Command substitution forms (e.g., $(pwd), $(git ...)) are rejected by certify replay. Use literal values only. dp.sh enforces at lint time.
- bash tools/lint/dp.sh --test
- bash tools/lint/response.sh --test
- ./ops/bin/manifest render stance-architect --out=- | rg -n "bridge decisions|bounded continuity|stale or self-contradictory request details|selected slices still govern scope"
- ./ops/bin/manifest render stance-analyst --out=- | rg -n "attached repo surfaces|bounded continuity|option, slice, or authority"
- ./ops/bin/manifest render stance-conformist --out=- | rg -n "bounded normalizer|continuity gaps|visible intent|repo patterns"
- rg -n "bounded continuity|bridge decisions|continuity gaps|machine-ingest safe" ops/src/shared/stances.json ops/src/stances/analyst.md.tpl ops/src/stances/architect.md.tpl ops/src/stances/conformist.md.tpl docs/ops/specs/stances/analyst.md docs/ops/specs/stances/architect.md docs/ops/specs/stances/conformist.md docs/ops/specs/tools/lint/response.md

## 3.5 Closeout (Mandatory Routing)
- Execute docs/MANUAL.md Closeout Cycle in order (Verify, Harvest, Refresh, Log, Prune).
- Update SoP.md and PoW.md with DP entries, including objective summary and current proof / receipt summary.
- Protocol order for closeout: Verify -> Generate Results -> COMMIT (Operator Only) -> Prune.
- Run prune hygiene: ./ops/bin/prune --scrub.
- Refresh llms artifacts: `./ops/bin/llms`
- Regenerate session artifacts: `./ops/bin/open --out=auto`
- Ensure the next session begins with refreshed session artifacts and matching receipts.
- Note: audit dump generation is owned by `./ops/bin/bundle --profile=audit --out=auto` and is separate from operator session refresh. Do not stamp an additional `ops/bin/dump --scope=core` into closeout unless the DP explicitly authorizes a standalone state capture.
- Refresh side-effect: `ops/bin/llms` regenerates `ops/lib/manifests/OPS.md` as a compile event; if `OPS.md` is not in the allowlist, restore it before running `integrity.sh`. See `docs/MANUAL.md` Refresh side-effect notice for the full procedure.

### 3.5.1 Mandatory Closing Sidecar
Closing sidecar content is generated and maintained at `storage/handoff/CLOSING.md` and is validated by `ops/bin/certify` as a hard gate at certification time. Do not author, predict, populate, or approximate any sidecar-derived closeout output outside this sidecar.
Certify separately collects contractor-authored narrative for the RESULTS Contractor Execution Narrative section at certify time via interactive editor prompt; this narrative input is independent from closing sidecar content.

Before running certify, confirm `storage/handoff/CLOSING.md` has been maintained throughout execution and reflects observable reality only.

Closing sidecar fields required at certify time:
- Commit Message
- Create Pull Request (Title)
- Create Pull Request (Description)
- Confirm Merge (Commit Message)
- Confirm Merge (Extended Description)
- Confirm Merge (Add a Comment)
