---
trace_id: stela-20260221T014712Z-447b618f
packet_id: DP-OPS-0086
created_at: 2026-02-21T01:49:41Z
previous: archives/surfaces/TASK-DP-OPS-0085-4d576f63.md
---
# STELA TASK DASHBOARD
Status: ACTIVE
Owner: Integrator
Last Updated: 2026-02-18

## 1. Session State
Pointer: Session context output (generated by ops/bin/open)
Context Manifest: ops/lib/manifests/CONTEXT.md (checked by tools/lint/context.sh)

Session State contract:
- Session State is pointer-first. Do not write inline branch or hash state here.
- OPEN and OPEN-PORCELAIN are the session state snapshot.
- OPEN-PORCELAIN is emitted on every OPEN run. Clean sessions produce a zero-line artifact.
- RESULTS is the audit record for the DP.
- TASK is the stable routing surface.
- Operator note (2026-02-17): Do not edit `SoP.md` or `PoW.md` unless explicitly instructed.

## 2. Logic Pointers
Primary Constraint: PoT.md (Policy of Truth).
Surface Contract Spec: docs/ops/specs/surfaces/task.md.

### 2.1 Governance Pointers
Jurisdiction: PoT.md Section 3.
Git Authority: PoT.md Section 4.1.
Behavioral Standard: PoT.md Section 4.2.
Staffing Protocol: PoT.md Section 4.1.

### 2.2 Enforcement and Gates
Style: tools/lint/style.sh
Context: tools/lint/context.sh
Truth: tools/lint/truth.sh
DP lint: tools/lint/dp.sh (enforces DP transaction rules in Section 3; does not enforce TASK container schema)
TASK lint: tools/lint/task.sh (sole TASK surface/container schema enforcer)
Repo verify: tools/verify.sh
llms parity: tools/lint/llms.sh

## 3. Current Dispatch Packet (DP)

Section-by-section authoring protocol:
- Generate DP structure from `ops/src/surfaces/dp.md.tpl` using `ops/bin/draft`.
- Do not hand-edit DP structural boilerplate in `TASK.md` after generation.
- Manual edits are limited to slot content only (objective, scope, safety, plan body, receipt body).
- Freeze each completed subsection in order (3.1 -> 3.2 -> 3.3 -> 3.4.x).
- Do not revise earlier sections to justify later work. If scope changes, end the thread and draft a new DP.

Freeze Rule:
- If a completed DP subsection changes later, append an AMENDMENT block in RESULTS; otherwise DP is FAIL.

AMENDMENT: minimum fields
- Reopened section:
- Reason:
- Impact:
- Allowlist change (none or exact paths):

Input discipline (hard rule):
- Worker receives only the DP text. Worker does not require OPEN or DUMP attachments.
- DP writer must not attach or cite disposable artifacts (rough notes, chat logs, scratch drafts, throwaway excerpts).
- DP writer must not include pasted bundles (OPEN, DUMP payloads, manifests) inside the DP body.
- Canon pointers only. Evidence belongs in storage artifacts and RESULTS.

### DP-OPS-0084: Binary Specification Explainability Refactor

## 3.1 Freshness Gate (Must Pass Before Work)
Base Branch: main
Required Work Branch: work/dp-ops-0084-2026-02-20
Base HEAD: 1577d65b
Freshness Stamp: 2026-02-20

Required local re-check (worker runs; paste outputs in RESULTS):
- git rev-parse --abbrev-ref HEAD
- git rev-parse --short HEAD
- git status --porcelain

Preconditions:
- No commits on main.
- Working tree must be clean before execution begins.
- If Base HEAD changes, regenerate session artifacts from canonical tools before proceeding.

STOP conditions:
- STOP if any mismatch (branch, Base HEAD, missing work branch).
- STOP if working tree is dirty before execution begins.
- STOP if told to create or switch branches.

## 3.1.1 DP Preflight Gate (Run Before Any Edits)
Purpose:
- Catch malformed DP or TASK schema before work begins.

Worker runs (paste outcome in RESULTS):
- bash tools/lint/dp.sh --test
- bash tools/lint/dp.sh TASK.md
- bash tools/lint/task.sh

STOP if any preflight check fails.

## 3.2 Required Context Load (Read Before Doing Anything)

### 3.2.1 Canon load order (always)
Worker must confirm loaded before edits begin:
1. PoT.md
2. SoP.md
3. PoW.md
4. TASK.md
5. docs/MAP.md
6. docs/MANUAL.md
7. ops/lib/manifests/CONTEXT.md

Notes:
- Worker does not read OPEN. OPEN is for Integrator state refresh and for receipts.
- Disposable artifacts must not be referenced or included.
- Worker input is the DP text only.
- DP writer must not attach or cite disposable artifacts.
- DP writer must not embed pasted bundles.

# Contractor Brief

**1. Temporary file paths.** Use `var/tmp/` for all temporary files within this repository.
The path `/tmp` is not valid within the Stela repo. `PoT.md §1.1` defines Resume storage;
all temporary writes must target that repository-defined location.

**2. RESULTS receipt generation.** `ops/bin/certify` is the only permitted generator of
RESULTS receipts. Manual assembly of receipt content is prohibited. `PoT.md §4.2` governs.

**3. Receipt commands are mandatory.** When the DP lists mandatory receipt commands (whether
in a `RECEIPT_COMMANDS` slot or in the hardened mandatory stub block), every command in that
block must be executed. No listed command is optional. Skipping any listed command is a STOP
condition.

**4. Disposable artifact prohibition.** OPEN files, dump bundles, and manifest files are
session artifacts. They must not be cited in DP content, committed to the repository, or
referenced in RESULTS as canonical sources.

**5. No invented paths.** All file paths referenced in execution steps must either exist in
the repo dump or be explicitly marked `NEW` in the DP changelog. Pattern paths, globs, and
brace expansions are prohibited.

### 3.2.2 DP-scoped load order (per DP)
Worker must load these resources before authoring any spec content:

1. `ops/src/docs/spec.md.tpl` — canonical four-slot structure that every spec file must conform to after this DP.
2. `tools/lint/style.sh` — to understand which jargon tokens and structural failures will cause CI rejection.
3. `docs/ops/specs/binaries/certify.md` — read existing content before authoring replacement.
4. `docs/ops/specs/binaries/compile.md` — read existing content before authoring replacement.
5. `docs/ops/specs/binaries/context.md` — read existing content before authoring replacement.
6. `docs/ops/specs/binaries/draft.md` — read stub content before authoring replacement.
7. `docs/ops/specs/binaries/dump.md` — read existing content before authoring replacement.
8. `docs/ops/specs/binaries/factory.md` — read stub content before authoring replacement.
9. `docs/ops/specs/binaries/help.md` — read existing content before authoring replacement.
10. `docs/ops/specs/binaries/llms.md` — read existing content before authoring replacement.
11. `docs/ops/specs/binaries/manifest.md` — read stub content before authoring replacement.
12. `docs/ops/specs/binaries/map.md` — read existing content before authoring replacement.
13. `docs/ops/specs/binaries/meta.md` — read stub content before authoring replacement.
14. `docs/ops/specs/binaries/open.md` — read existing content before authoring replacement.
15. `docs/ops/specs/binaries/project.md` — read existing content before authoring replacement.
16. `docs/ops/specs/binaries/prune.md` — read existing content before authoring replacement.
17. `docs/ops/specs/binaries/scaffold.md` — read stub content before authoring replacement.
18. `docs/ops/specs/binaries/template.md` — read existing content before authoring replacement.
19. `ops/bin/certify` — source executable; read mechanics before authoring First Principles Rationale and Mechanics and Sequencing slots.
20. `ops/bin/compile` — source executable; read mechanics before authoring slots.
21. `ops/bin/context` — source executable; read mechanics before authoring slots.
22. `ops/bin/draft` — source executable; read mechanics before authoring slots.
23. `ops/bin/dump` — source executable; read mechanics before authoring slots.
24. `ops/bin/factory` — source executable; read mechanics before authoring slots.
25. `ops/bin/help` — source executable; read mechanics before authoring slots.
26. `ops/bin/llms` — source executable; read mechanics before authoring slots.
27. `ops/bin/manifest` — source executable; read mechanics before authoring slots.
28. `ops/bin/map` — source executable; read mechanics before authoring slots.
29. `ops/bin/meta` — source executable; read mechanics before authoring slots.
30. `ops/bin/open` — source executable; read mechanics before authoring slots.
31. `ops/bin/project` — source executable; read mechanics before authoring slots.
32. `ops/bin/prune` — source executable; read mechanics before authoring slots.
33. `ops/bin/scaffold` — source executable; read mechanics before authoring slots.
34. `ops/bin/template` — source executable; read mechanics before authoring slots.

## 3.3 Scope and Safety
Objective:
Author and finalize philosophy-compliant content for all sixteen binary specification files in `docs/ops/specs/binaries/`. Apply Anti-Jargon Protocol, First-Principles Architecture, Anecdotal Anchoring, and the Integrity Filter to every spec to achieve the mandated twenty-five percent increase in functional explainability. All deliverable spec surfaces must contain finalized prose with zero `PROPOSED:` markers at certification time.

In scope:
- `docs/ops/specs/binaries/certify.md` — refactor into four-slot spec.md.tpl structure with philosophy-compliant content.
- `docs/ops/specs/binaries/compile.md` — refactor into four-slot spec.md.tpl structure with philosophy-compliant content.
- `docs/ops/specs/binaries/context.md` — refactor into four-slot spec.md.tpl structure with philosophy-compliant content.
- `docs/ops/specs/binaries/draft.md` — deepen stub slots with finalized, philosophy-compliant content.
- `docs/ops/specs/binaries/dump.md` — refactor into four-slot spec.md.tpl structure with philosophy-compliant content.
- `docs/ops/specs/binaries/factory.md` — deepen stub slots with finalized, philosophy-compliant content.
- `docs/ops/specs/binaries/help.md` — refactor into four-slot spec.md.tpl structure with philosophy-compliant content.
- `docs/ops/specs/binaries/llms.md` — refactor into four-slot spec.md.tpl structure with philosophy-compliant content.
- `docs/ops/specs/binaries/manifest.md` — deepen stub slots with finalized, philosophy-compliant content.
- `docs/ops/specs/binaries/map.md` — refactor into four-slot spec.md.tpl structure with philosophy-compliant content.
- `docs/ops/specs/binaries/meta.md` — deepen stub slots with finalized, philosophy-compliant content.
- `docs/ops/specs/binaries/open.md` — refactor into four-slot spec.md.tpl structure with philosophy-compliant content.
- `docs/ops/specs/binaries/project.md` — refactor into four-slot spec.md.tpl structure with philosophy-compliant content.
- `docs/ops/specs/binaries/prune.md` — refactor into four-slot spec.md.tpl structure with philosophy-compliant content.
- `docs/ops/specs/binaries/scaffold.md` — deepen stub slots with finalized, philosophy-compliant content.
- `docs/ops/specs/binaries/template.md` — refactor into four-slot spec.md.tpl structure with philosophy-compliant content.
- `storage/dp/active/allowlist.txt` — update to reflect DP-OPS-0084 target files.
- `TASK.md` — standard DP surface update.
- `SoP.md` — standard closeout ledger update.
- `PoW.md` — standard proof ledger update.
- `storage/handoff/CLOSING-DP-OPS-0084.md` (NEW) — human-authored closing sidecar maintained during execution.

Out of scope:
- `docs/ops/specs/scripts/` — reserved for DP-OPS-0085; no edits permitted in this packet.
- `docs/ops/specs/tools/` — reserved for DP-OPS-0086; no edits permitted in this packet.
- `docs/ops/specs/surfaces/` — not part of the explainability refactor sequence; no edits permitted.
- `docs/ops/specs/definitions/` — not part of the explainability refactor sequence; no edits permitted.
- `ops/src/docs/spec.md.tpl` — schema is locked from DP-OPS-0082; no structural edits permitted.
- `tools/lint/style.sh` — linter is locked from DP-OPS-0082; no edits permitted.
- `ops/bin/` executables — source binaries are read-only reference material; no edits permitted.
- `docs/ops/registry/` — registry edits are out of scope unless a factual path-pointer defect blocking certification is discovered and operator scope expansion is granted.
- Any file not listed in the allowlist.

Safety and invariants:
- No manual edits to generated outputs. All spec content is hand-authored directly into spec files; `ops/bin/template render spec` must not be re-invoked in this packet, as DP-OPS-0083 already performed stub generation.
- No structural edits to TASK or DP. Section numbering, heading text, and template boilerplate are immutable.
- Allowlist hard gate. Any file modified that is absent from `storage/dp/active/allowlist.txt` is a scope violation requiring immediate STOP and operator authorization before proceeding.
- No `PROPOSED:` markers in deliverable spec surfaces. Every slot value must be finalized prose before closeout.
- No new stub generation. Stub creation belongs to DP-OPS-0083. This packet performs content maturation only.
- No cross-domain edits. Scripts and linter specs are strictly forbidden in this packet per cross-packet hard rules.
- SoP.md update is mandatory for equilibrium maintenance; the task is not complete until the ledger reflects this packet.

Worker Constraints (SSOT injected):
## Section 1: Universal Template Rules
- Templates are authored as `.tpl` files and may begin with YAML frontmatter.
- Frontmatter keys are canonical and machine-read:
  - `template_type`
  - `template_id`
  - `template_version`
  - `requires_slots`
  - `includes`
- Rendered output must strip YAML frontmatter before writing output.
- Slot tokens are `\{\{TOKEN\}\}` where `TOKEN` is uppercase alphanumeric with underscores.
- Include directives are supported in template body content:
  - `\{\{@include:path\}\}`
  - `\{\{@include:path#section\}\}`
- Include resolution is deterministic and strict:
  - Missing files fail render.
  - Missing section anchors fail render.
  - Circular include graphs fail render.
- Strict mode is default for worker-facing output:
  - Every `requires_slots` token must be provided.
  - Unresolved `\{\{TOKEN\}\}` placeholders fail render.
- Non-strict mode is allowed only for linting and normalization workflows.
- Generated worker-facing surfaces must remain pointer-first and must not embed disposable artifacts.

Target Files allowlist (hard gate):
- storage/dp/active/allowlist.txt

## 3.4 Execution Plan (A-E)

### 3.4.1 State
Repository is at HEAD `1577d65b` on `main` with a clean working tree. DP-OPS-0083 merged successfully, establishing the following state:

- All sixteen binary executables in `ops/bin/` have a corresponding specification file in `docs/ops/specs/binaries/`.
- Five files (draft.md, factory.md, manifest.md, meta.md, scaffold.md) were generated as stubs by `ops/bin/template render spec` during DP-OPS-0083 and carry the four-slot `spec.md.tpl` structure. These stubs contain initial prose without `PROPOSED:` markers, but the content requires deepening to satisfy the Anti-Jargon, First-Principles, Anecdotal Anchoring, and Integrity Filter requirements.
- Eleven files (certify.md, compile.md, context.md, dump.md, help.md, llms.md, map.md, open.md, project.md, prune.md, template.md) contain pre-existing specification content authored before the `spec.md.tpl` schema was established by DP-OPS-0082. These files use varied legacy heading structures and do not conform to the canonical four-slot format.
- `ops/src/docs/spec.md.tpl` is locked and defines the required slot structure: `First Principles Rationale`, `Mechanics and Sequencing`, `Anecdotal Anchor`, `Integrity Filter Warnings`.
- `tools/lint/style.sh` is hardened from DP-OPS-0082 to reject spec files that do not conform to the four-slot structure and to enforce the Anti-Jargon jargon blacklist.
- SoP.md records the DP-OPS-0083 functional receipt summary. PoW.md carries the corresponding proof pointer. Both are pointer-first and will be updated by `ops/bin/certify` during closeout.
- `storage/dp/active/allowlist.txt` carries entries from DP-OPS-0083 and must be replaced with the DP-OPS-0084 target file list before work begins.

### 3.4.2 Request
Author and finalize philosophy-compliant specification content for all sixteen files in `docs/ops/specs/binaries/`. The following requirements apply to every file in this packet:

**Structural requirement.** Every spec file must conform to the rendered output of `ops/src/docs/spec.md.tpl`. The canonical rendered structure is:
```
<!-- SPEC-SURFACE:REQUIRED -->
# Technical Specification

## First Principles Rationale
<slot content>

## Mechanics and Sequencing
<slot content>

## Anecdotal Anchor
<slot content>

## Integrity Filter Warnings
<slot content>
```
Files that currently use non-conforming legacy heading structures must be fully refactored to this structure. The `<!-- SPEC-SURFACE:REQUIRED -->` comment must be preserved as the first line.

**Anti-Jargon requirement.** Eliminate generalized verbs such as "handles," "processes," "manages," and "updates." Replace each with an explicit, mechanistic description of what the tool does at the control-flow level. Do not use prestige language or undefined acronyms without inline definition.

**First-Principles requirement.** The `First Principles Rationale` slot must answer why this binary exists as a system necessity by referencing the specific failure mode it prevents, the invariant it protects, or the throughput guarantee it provides. The justification must trace back to a constitutional axiom in `PoT.md`.

**Anecdotal Anchoring requirement.** The `Anecdotal Anchor` slot must ground at least one statement in historical operational evidence drawn from `SoP.md` or `PoW.md` where such evidence exists. If no specific historical incident is available for a tool, the anchor must reference the class of operational failure that motivated the tool's design.

**Integrity Filter requirement.** The `Integrity Filter Warnings` slot must explicitly document known failure boundaries, edge conditions, and unhandled states that can produce incorrect outputs or blocked execution. Known architectural limitations or legacy assumptions must be declared openly rather than omitted.

**Provisional content prohibition.** No `PROPOSED:` marker is permitted in any deliverable spec file at certification time. Any provisional text must be replaced with finalized prose before the acceptance gate is run.

**Acceptance gate.** Before proceeding to closeout, the worker must run the following command and confirm zero matches:
```
grep -rn "PROPOSED:" docs/ops/specs/binaries/certify.md docs/ops/specs/binaries/compile.md docs/ops/specs/binaries/context.md docs/ops/specs/binaries/draft.md docs/ops/specs/binaries/dump.md docs/ops/specs/binaries/factory.md docs/ops/specs/binaries/help.md docs/ops/specs/binaries/llms.md docs/ops/specs/binaries/manifest.md docs/ops/specs/binaries/map.md docs/ops/specs/binaries/meta.md docs/ops/specs/binaries/open.md docs/ops/specs/binaries/project.md docs/ops/specs/binaries/prune.md docs/ops/specs/binaries/scaffold.md docs/ops/specs/binaries/template.md || true
```
A non-empty result is a STOP condition. No certification may proceed until the check returns empty.

### 3.4.3 Changelog
All files listed below exist in the repository at HEAD `1577d65b` unless marked `NEW`.

UPDATE  `storage/dp/active/allowlist.txt`
UPDATE  `docs/ops/specs/binaries/certify.md`
UPDATE  `docs/ops/specs/binaries/compile.md`
UPDATE  `docs/ops/specs/binaries/context.md`
UPDATE  `docs/ops/specs/binaries/draft.md`
UPDATE  `docs/ops/specs/binaries/dump.md`
UPDATE  `docs/ops/specs/binaries/factory.md`
UPDATE  `docs/ops/specs/binaries/help.md`
UPDATE  `docs/ops/specs/binaries/llms.md`
UPDATE  `docs/ops/specs/binaries/manifest.md`
UPDATE  `docs/ops/specs/binaries/map.md`
UPDATE  `docs/ops/specs/binaries/meta.md`
UPDATE  `docs/ops/specs/binaries/open.md`
UPDATE  `docs/ops/specs/binaries/project.md`
UPDATE  `docs/ops/specs/binaries/prune.md`
UPDATE  `docs/ops/specs/binaries/scaffold.md`
UPDATE  `docs/ops/specs/binaries/template.md`
UPDATE  `TASK.md`
UPDATE  `SoP.md`
UPDATE  `PoW.md`
NEW     `storage/handoff/CLOSING-DP-OPS-0084.md`

### 3.4.4 Patch / Diff

**Step 1 — Update allowlist.**
Replace the contents of `storage/dp/active/allowlist.txt` with the following file list, one entry per line:
```
storage/dp/active/allowlist.txt
TASK.md
SoP.md
PoW.md
storage/handoff/CLOSING-DP-OPS-0084.md
docs/ops/specs/binaries/certify.md
docs/ops/specs/binaries/compile.md
docs/ops/specs/binaries/context.md
docs/ops/specs/binaries/draft.md
docs/ops/specs/binaries/dump.md
docs/ops/specs/binaries/factory.md
docs/ops/specs/binaries/help.md
docs/ops/specs/binaries/llms.md
docs/ops/specs/binaries/manifest.md
docs/ops/specs/binaries/map.md
docs/ops/specs/binaries/meta.md
docs/ops/specs/binaries/open.md
docs/ops/specs/binaries/project.md
docs/ops/specs/binaries/prune.md
docs/ops/specs/binaries/scaffold.md
docs/ops/specs/binaries/template.md
```

Run `bash tools/lint/integrity.sh` after updating the allowlist and confirm it passes before proceeding to spec authoring.

**Step 2 — Initialize closing sidecar.**
Create `storage/handoff/CLOSING-DP-OPS-0084.md` (NEW). This sidecar must be maintained throughout execution and must be non-empty before `ops/bin/certify` is invoked. Populate it progressively as each spec file is authored and confirmed passing through `tools/lint/style.sh`.

**Step 3 — Author spec content for the five stub files.**
For each of the following files, read the current stub content alongside the corresponding source executable to produce finalized prose that satisfies all four authoring requirements (Anti-Jargon, First-Principles, Anecdotal Anchoring, Integrity Filter). The four-slot structure is already present; only the slot values require replacement or deepening. Do not alter the heading text or the `<!-- SPEC-SURFACE:REQUIRED -->` comment.

- `docs/ops/specs/binaries/draft.md` — read `ops/bin/draft` for mechanics; anchor to the historical operational observation that hand-authored DP structures generated structural variance that caused task lint failures prior to draft's introduction (referenced in SoP entries for DP-OPS-0065 and the immutable workflow cutover).
- `docs/ops/specs/binaries/factory.md` — read `ops/bin/factory` for mechanics; anchor to the definition promotion workflows that incurred formatting drift before factory centralized rendering, as recorded in the DP-OPS-0077 fission history.
- `docs/ops/specs/binaries/manifest.md` — read `ops/bin/manifest` for mechanics; anchor to the reliability improvement observed after template-driven generation replaced manual packet and receipt assembly (DP-OPS-0065 immutable workflow cutover).
- `docs/ops/specs/binaries/meta.md` — read `ops/bin/meta` for mechanics; anchor to the class of evidence gap that occurred when manual project context capture missed one of the OPEN or DUMP artifacts.
- `docs/ops/specs/binaries/scaffold.md` — read `ops/bin/scaffold` for mechanics; anchor to the class of failure where ad hoc project bootstrapping produced missing or misnamed folders prior to scaffold's introduction (DP-OPS-0078 project binary fission history).

**Step 4 — Refactor the eleven legacy-format spec files.**
For each of the following files, read the existing content alongside the corresponding source executable, then fully replace the file body with the canonical four-slot rendered structure. Preserve all mechanistic detail from the existing content; translate it into the appropriate slot rather than discarding it. Do not retain legacy heading structures (such as `## Constitutional Anchor`, `## Operator Contract`, `## Technical Specifications`, `## Requirements`, `## Usage`, `## Forensic Insight`) after the refactor; all content must be redistributed into the four canonical slots.

- `docs/ops/specs/binaries/certify.md` — read `ops/bin/certify`; place constitutional justification (immutable RESULTS requirement, Generation Mandate) in First Principles Rationale; place the fourteen-step execution sequence in Mechanics and Sequencing; anchor to the DP-OPS-0074 enforcement-model gap where no-argument receipt scanning versus strict hash parity required explicit operator authorization (referenced in SoP and the plan's Integrity Filter section); document hard gates in Integrity Filter Warnings.
- `docs/ops/specs/binaries/compile.md` — read `ops/bin/compile`; anchor to the manifest state drift that made context and llms generation unreliable before compile's atomic integration contract was established.
- `docs/ops/specs/binaries/context.md` — read `ops/bin/context`; anchor to the class of session preparation failure where outdated manifests produced stale OPS-layer context.
- `docs/ops/specs/binaries/dump.md` — read `ops/bin/dump`; anchor to the class of failure where hidden or untracked artifacts reshaping the governance narrative was identified as a risk during the immutable workflow adoption.
- `docs/ops/specs/binaries/help.md` — read `ops/bin/help`; anchor to the operational observation that operator wayfinding failures during dispatch required a deterministic CLI reference surface.
- `docs/ops/specs/binaries/llms.md` — read `ops/bin/llms`; anchor to the SSOT drift risk where AI discovery entry points fell out of sync with runtime manifest state.
- `docs/ops/specs/binaries/map.md` — read `ops/bin/map`; anchor to the class of drift where `docs/MAP.md` auto-generated blocks were accidentally overwritten or desynchronized from the repository topology.
- `docs/ops/specs/binaries/open.md` — read `ops/bin/open`; anchor to the DP-OPS-0065 freshness gate establishment, where the absence of a formal session checkpoint allowed work to proceed against stale git state.
- `docs/ops/specs/binaries/project.md` — read `ops/bin/project`; anchor to the DP-OPS-0078 fission history where project's scope was split into scaffold and meta.
- `docs/ops/specs/binaries/prune.md` — read `ops/bin/prune`; anchor to the DP-OPS-0074 incident where a premature prune execution deleted a RESULTS artifact before certification completed, as referenced in the plan's Anecdotal Anchoring section.
- `docs/ops/specs/binaries/template.md` — read `ops/bin/template`; anchor to the DP-OPS-0067 template-system cutover where canonical metadata and frontmatter stripping were first enforced.

**Step 5 — Run acceptance gate.**
After all sixteen spec files have been authored and saved, execute the provisional content check:
```
grep -rn "PROPOSED:" docs/ops/specs/binaries/certify.md docs/ops/specs/binaries/compile.md docs/ops/specs/binaries/context.md docs/ops/specs/binaries/draft.md docs/ops/specs/binaries/dump.md docs/ops/specs/binaries/factory.md docs/ops/specs/binaries/help.md docs/ops/specs/binaries/llms.md docs/ops/specs/binaries/manifest.md docs/ops/specs/binaries/map.md docs/ops/specs/binaries/meta.md docs/ops/specs/binaries/open.md docs/ops/specs/binaries/project.md docs/ops/specs/binaries/prune.md docs/ops/specs/binaries/scaffold.md docs/ops/specs/binaries/template.md || true
```
The output must be empty. A non-empty result is a STOP condition.

Then run the full linter suite to confirm all sixteen spec files pass `tools/lint/style.sh` before proceeding to closeout.

### 3.4.5 Receipt (Proofs to collect) - MUST RUN

**Mandatory receipt commands (always run; do not omit):**
- bash tools/lint/dp.sh TASK.md
- bash tools/lint/task.sh
- bash tools/lint/integrity.sh
- bash tools/lint/style.sh
- git diff --name-only
- git diff --stat
- comm -23 <(git diff --name-only | sort) <(sort storage/dp/active/allowlist.txt) || true
- comm -23 <(git ls-files --others --exclude-standard | sort) <(sort storage/dp/active/allowlist.txt) || true
- ./ops/bin/open

**DP-specific receipt commands (scope-specific; author below):**
- bash tools/lint/truth.sh
- bash tools/lint/context.sh
- grep -rn "PROPOSED:" docs/ops/specs/binaries/certify.md docs/ops/specs/binaries/compile.md docs/ops/specs/binaries/context.md docs/ops/specs/binaries/draft.md docs/ops/specs/binaries/dump.md docs/ops/specs/binaries/factory.md docs/ops/specs/binaries/help.md docs/ops/specs/binaries/llms.md docs/ops/specs/binaries/manifest.md docs/ops/specs/binaries/map.md docs/ops/specs/binaries/meta.md docs/ops/specs/binaries/open.md docs/ops/specs/binaries/project.md docs/ops/specs/binaries/prune.md docs/ops/specs/binaries/scaffold.md docs/ops/specs/binaries/template.md || true
- ./ops/bin/certify --dp="DP-OPS-0084" --out=auto

## 3.5 Closeout (Mandatory Routing)
- Execute docs/MANUAL.md Closeout Cycle in order (Verify, Harvest, Refresh, Log, Prune).
- Update SoP.md and PoW.md with DP entries, including objective summary and verification commands run.
- Protocol order for closeout: Verify -> Generate Results -> COMMIT (Operator Only) -> Prune.
- Run prune hygiene: ./ops/bin/prune --scrub.
- Ensure the next session begins with refreshed session artifacts and matching receipts.

### 3.5.1 Mandatory Closing Block
Commit Message (plaintext)

Create Pull Request (Title) (plaintext)

Create Pull Request (Description) (markdown)

Confirm Merge (Commit Message) (plaintext) (Must differ from #1)

Confirm Merge (Extended Description) (plaintext)

Confirm Merge (Add a Comment) (markdown)
