---
trace_id: stela-20260303T210843Z-2ea3137d
packet_id: DP-OPS-0146
created_at: 2026-03-03T21:34:06Z
previous: (none)
---
# STELA TASK DASHBOARD
Status: ACTIVE
Owner: Integrator
Last Updated: 2026-03-02

## 1. Session State
Pointer: Session context output (generated by ops/bin/open)
Context Manifest: ops/lib/manifests/CONTEXT.md (checked by tools/lint/context.sh)

Session State contract:
- Session State is pointer-first. Do not write inline branch or hash state here.
- OPEN and OPEN-PORCELAIN are the session state snapshot.
- OPEN-PORCELAIN is emitted on every OPEN run. Clean sessions produce a zero-line artifact.
- RESULTS is the audit record for the DP.
- TASK is the stable routing surface.
- Operator note (2026-02-17): Do not edit `SoP.md` or `PoW.md` unless explicitly instructed.

## 2. Logic Pointers
Primary Constraint: PoT.md (Policy of Truth).
Surface Contract Spec: docs/ops/specs/surfaces/task.md.

### 2.1 Governance Pointers
Jurisdiction: PoT.md Section 3.
Git Authority: PoT.md Section 4.1.
Behavioral Standard: PoT.md Section 4.2.
Staffing Protocol: PoT.md Section 4.1.

### 2.2 Enforcement and Gates
Style: tools/lint/style.sh
Context: tools/lint/context.sh
Truth: tools/lint/truth.sh
DP lint: tools/lint/dp.sh (enforces DP transaction rules in Section 3; does not enforce TASK container schema)
TASK lint: tools/lint/task.sh (sole TASK surface/container schema enforcer)
Repo verify: tools/verify.sh
llms parity: tools/lint/llms.sh

## 3. Current Dispatch Packet (DP)

Section-by-section authoring protocol:
- Generate DP structure from `ops/src/surfaces/dp.md.tpl` using `ops/bin/draft`.
- Do not hand-edit DP structural boilerplate in `TASK.md` after generation.
- Manual edits are limited to slot content only (objective, scope, safety, plan body, receipt body).
- Freeze each completed subsection in order (3.1 -> 3.2 -> 3.3 -> 3.4.x).
- Do not revise earlier sections to justify later work. If scope changes, end the thread and draft a new DP.

Freeze Rule:
- If a completed DP subsection changes later, append an AMENDMENT block in RESULTS; otherwise DP is FAIL.

AMENDMENT: minimum fields
- Reopened section:
- Reason:
- Impact:
- Allowlist change (none or exact paths):

Input discipline (hard rule):
- Worker receives only the DP text. Worker does not require OPEN or DUMP attachments.
- DP writer must not attach or cite disposable artifacts (rough notes, chat logs, scratch drafts, throwaway excerpts).
- DP writer must not include pasted bundles (OPEN, DUMP payloads, manifests) inside the DP body.
- Canon pointers only. Evidence belongs in storage artifacts and RESULTS.


### DP-OPS-0146: Bundle Conformance and Intake-Contract Hardening

## 3.1 Freshness Gate (Must Pass Before Work)
Base Branch: main
Required Work Branch: work/dp-ops-0146-bundle-conformance-2026-03-03
Base HEAD: 9f0fdc6a
Freshness Stamp: 2026-03-03
Note: FRESHNESS_STAMP must be YYYY-MM-DD format only. No trace tokens, no timestamps, no other text. Certify rejects all other forms. dp.sh enforces at lint time.

Required local re-check (worker runs; paste outputs in RESULTS):
- git rev-parse --abbrev-ref HEAD
- git rev-parse --short HEAD
- git status --porcelain

Preconditions:
- No commits on main.
- Working tree must be clean before execution begins.
- If Base HEAD changes, regenerate session artifacts from canonical tools before proceeding.

STOP conditions:
- STOP if any mismatch (branch, Base HEAD, missing work branch).
- STOP if working tree is dirty before execution begins.
- STOP if told to create or switch branches.

## 3.1.1 DP Preflight Gate (Run Before Any Edits)
Purpose:
- Catch malformed DP or TASK schema before work begins.

Worker runs (paste outcome in RESULTS):
- bash tools/lint/dp.sh --test
- bash tools/lint/dp.sh TASK.md
- bash tools/lint/task.sh

STOP if any preflight check fails.

### CbC Design Discipline Preflight (TASK.md §3.1.1)
Required when the DP objective adds, modifies, or replaces a linter, script, guard, or validation binary.
For non-tooling DPs: state "Not applicable" with a one-line justification.
This DP adds and modifies tooling surfaces (`ops/bin/bundle`, `ops/lib/scripts/bundle.sh`, `tools/lint/plan.sh`) and adds a new template surface (`ops/src/surfaces/plan.md.tpl`) plus associated specs.
Structural prevention is applied where feasible via template-backed required sections and deterministic routing contracts.
Runtime validation remains required for operator-provided intent content (`--intent`) and decision-leaf presence checks in auditor profile flows.
Detection minimization is enforced by keeping PLAN lint checks deterministic and limited to structural presence checks only.

## 3.2 Required Context Load (Read Before Doing Anything)

### 3.2.1 Canon load order (always)
Worker must confirm loaded before edits begin:
1. PoT.md
2. SoP.md
3. PoW.md
4. TASK.md
5. docs/MAP.md
6. docs/MANUAL.md
7. ops/lib/manifests/CONTEXT.md

Notes:
- Worker does not read OPEN. OPEN is for Integrator state refresh and for receipts.
- Disposable artifacts must not be referenced or included.
- Worker input is the DP text only.
- DP writer must not attach or cite disposable artifacts.
- DP writer must not embed pasted bundles.

<!-- CCD: ff_target="machine-dense" ff_band="30-40" -->
# Contractor Brief

1 Temp path rule uses `var/tmp` only
2 RESULTS generation rule uses `ops/bin/certify` only
3 Receipt rule runs every DP mandated command and treats any skip as STOP
4 Disposable artifact rule treats OPEN dump bundles and manifests as non canonical session artifacts
5 Use repo-relative paths only in all output. Never print absolute filesystem paths. Do not emit clickable file links.

### 3.2.2 DP-scoped load order (per DP)
1. docs/DESIGN.md
2. storage/.gitignore
3. docs/ops/prompts/README.md
4. docs/ops/prompts/e-prompt-01.md
5. docs/ops/prompts/e-prompt-02.md
6. docs/ops/prompts/e-prompt-03.md
7. docs/ops/prompts/e-prompt-04.md
8. docs/ops/prompts/e-prompt-05.md
9. docs/ops/prompts/e-prompt-06.md
10. docs/ops/registry/prompts.md
11. docs/ops/registry/templates.md
12. docs/ops/specs/binaries/bundle.md
13. docs/ops/specs/scripts/bundle.md
14. docs/ops/specs/tools/lint/plan.md
15. docs/ops/specs/surfaces/plan.md
16. ops/bin/bundle
17. ops/lib/scripts/bundle.sh
18. tools/lint/plan.sh
19. ops/src/surfaces/plan.md.tpl
20. ops/bin/open
21. ops/bin/dump

## 3.3 Scope and Safety
Objective:
Harden bundle intake and the operator-facing contract by implementing bundle v2 behavior, adding `hygiene` and `auditor` profiles, adding auditor intent gating with decision-leaf validation, embedding OPEN data directly in bundle artifacts, and emitting a deterministic bundle tar sidecar.
Stabilize architect drafting behavior by enforcing Architect Handoff execution from `PLAN.md` selections only, and add a canonical PLAN template plus deterministic PLAN lint checks so architect-mode DP drafting does not stall on missing slot semantics.

In scope:
- ops/bin/bundle (UPDATE)
- ops/lib/scripts/bundle.sh (UPDATE)
- tools/lint/plan.sh (UPDATE)
- ops/src/surfaces/plan.md.tpl (NEW)
- docs/ops/specs/binaries/bundle.md (UPDATE)
- docs/ops/specs/scripts/bundle.md (UPDATE)
- docs/ops/specs/tools/lint/plan.md (UPDATE)
- docs/ops/specs/surfaces/plan.md (NEW)
- docs/ops/prompts/README.md (UPDATE)
- docs/ops/prompts/e-prompt-01.md (UPDATE)
- docs/ops/prompts/e-prompt-02.md (UPDATE)
- docs/ops/prompts/e-prompt-03.md (UPDATE)
- docs/ops/prompts/e-prompt-04.md (UPDATE)
- docs/ops/prompts/e-prompt-05.md (UPDATE)
- docs/ops/prompts/e-prompt-06.md (UPDATE)
- docs/ops/registry/prompts.md (UPDATE)
- docs/ops/registry/templates.md (UPDATE)
- storage/dp/active/allowlist.txt (UPDATE)
- TASK.md (UPDATE)
- SoP.md (UPDATE)
- PoW.md (UPDATE)
- llms.txt (UPDATE)
- llms-core.txt (UPDATE)
- llms-full.txt (UPDATE)

Out of scope:
- Any PoT authority change or terminology rewrite in canonical governance documents.
- Any change to `ops/bin/certify` behavior or RESULTS schema.
- Any standalone `ops/bin/open` CLI behavior change.
- Any standalone `ops/bin/dump` selection semantics change.
- Any global lint gating expansion beyond explicit route-gate and receipt contexts for PLAN lint.
- Any factory scope dump changes or platform/factory dump mode redesign.

Safety and invariants:
- No manual edits to generated artifacts (OPEN, dump payloads, dump manifests, bundle artifacts, RESULTS).
- Bundle runtime remains deterministic and does not parse factory markdown definitions at runtime.
- Bundle output remains pointer-first for dump content and does not inline dump payload bodies in the bundle text.
- `--profile=auto` routes to architect only when `storage/handoff/PLAN.md` exists and `tools/lint/plan.sh` passes; otherwise route to analyst.
- Auditor mode hard-gates on `--intent` format and decision-leaf presence in dump payload.
- DP Section 3.4.5 receipt commands remain certify-compatible: no command substitution and no brace/glob/bracket pattern tokens.
- Maintain `storage/dp/active/allowlist.txt` as a hard gate for all tracked file modifications.

Worker Constraints (SSOT injected):
## Section 1: Universal Template Rules
- Template source uses `tpl` files with optional YAML frontmatter
- Canon frontmatter keys: `template_type` `template_id` `template_version` `requires_slots` `includes`
- Renderer strips frontmatter before output write
- Slot token form `\{\{TOKEN\}\}` with uppercase alphanumeric underscore
- Include forms `\{\{@include:path\}\}` and `\{\{@include:path#section\}\}`
- Include resolution is strict: missing file fail missing section fail circular graph fail
- Strict mode default: every required slot value present and no unresolved token
- Non strict mode allowed only for lint and normalization workflows
- Worker facing generated surfaces remain pointer first and exclude disposable artifacts

Target Files allowlist (hard gate):
- storage/dp/active/allowlist.txt

## 3.4 Execution Plan (A-E)

### 3.4.1 State
- Bundle currently emits v1 artifacts and still creates standalone `OPEN-bundle-*` artifacts.
- Bundle currently supports `auto|analyst|architect|audit|project` and does not provide `hygiene|auditor` profiles.
- Bundle currently emits `.txt` and `.manifest.json` and does not emit a bundle `.tar` sidecar.
- Architect prompt behavior has shown menu/selection loops in web model runs and requires a stronger attach-only execution contract.
- Analyst plan output currently lacks a canonical template-backed slot map surface for architect DP-slot binding.

### 3.4.2 Request
- Implement PLAN-selected path: Selected Option `RECOMMENDED`, Slice Mode `single`, Selected Slices `S1`.
- Add bundle profile surface `auto|analyst|architect|audit|project|hygiene|auditor`.
- Add `--intent` support and auditor intent hard-gating with decision-leaf validation.
- Remove internal `ops/bin/open` invocation from bundle runtime and embed OPEN data directly in bundle text/manifest.
- Emit bundle `.tar` sidecar every run and upgrade manifest schema to `bundle_version: "2"` with v2 required fields.
- Add canonical PLAN template and deterministic PLAN lint checks for required Architect Handoff and DP Slot Source Map fields.
- Update architect prompt rules so architect drafts immediately from attached PLAN selections and does not propose new options/phases/slices.
- Add hard mode split guardrails in prompt contracts so audit verdict flows require `--profile=audit` and addendum authorization flows require `--profile=auditor`.

### 3.4.3 Changelog
UPDATE:
- ops/bin/bundle
- ops/lib/scripts/bundle.sh
- tools/lint/plan.sh
- docs/ops/prompts/README.md
- docs/ops/prompts/e-prompt-01.md
- docs/ops/prompts/e-prompt-02.md
- docs/ops/prompts/e-prompt-03.md
- docs/ops/prompts/e-prompt-04.md
- docs/ops/prompts/e-prompt-05.md
- docs/ops/prompts/e-prompt-06.md
- docs/ops/registry/prompts.md
- docs/ops/registry/templates.md
- docs/ops/specs/binaries/bundle.md
- docs/ops/specs/scripts/bundle.md
- docs/ops/specs/tools/lint/plan.md
- storage/dp/active/allowlist.txt
- TASK.md
- SoP.md
- PoW.md
- llms.txt
- llms-core.txt
- llms-full.txt
NEW:
- ops/src/surfaces/plan.md.tpl
- docs/ops/specs/surfaces/plan.md

### 3.4.4 Patch / Diff
1. Update `storage/dp/active/allowlist.txt` with the full tracked file set in this DP before editing.
2. Update `ops/lib/scripts/bundle.sh` and `ops/bin/bundle` profile parsing/routing to include `hygiene` and `auditor` while preserving current `project` slug validation.
3. Keep `--profile=auto` routing contract unchanged: architect only when PLAN exists and `tools/lint/plan.sh` passes, else analyst.
4. Remove internal `ops/bin/open` invocation from bundle runtime; construct OPEN block inline from deterministic runtime data.
5. Upgrade bundle output contract to emit `.txt`, `.manifest.json`, and `.tar` sidecar from bundle orchestration.
6. Upgrade bundle manifest to `bundle_version: "2"` and include required v2 fields including `open.embedded`, `open.intent`, `addendum`, and `package` blocks.
7. Implement auditor gating in bundle runtime: require `--intent`, validate intent format, parse decision ID, verify decision ID exists in dump payload, and hard-fail on absence.
8. Update dump invocation inside bundle to explicit `.txt` output under `storage/dumps/` so bundle orchestration does not produce `dump-*.tar.xz` side artifacts.
9. Add `ops/src/surfaces/plan.md.tpl` with required sections `Architect Handoff` and `DP Slot Source Map`.
10. Update `tools/lint/plan.sh` and `docs/ops/specs/tools/lint/plan.md` for deterministic architect-targeted slot-map checks only, with no style enforcement.
11. Add `docs/ops/specs/surfaces/plan.md` and register the template in `docs/ops/registry/templates.md`.
12. Update prompts and prompt docs (`e-prompt-01`, `02`, `03`, `04`, `05`, `06`, README, registry) to enforce attach-only bundle behavior, explicit audit versus auditor mode separation, and architect execution from explicit handoff selections only.
13. Run full verification and receipt sequence, then close out via `ops/bin/certify` after sidecar completion.

### 3.4.5 Receipt (Proofs to collect) - MUST RUN

**Mandatory receipt commands (always run; do not omit):**
- bash tools/lint/dp.sh TASK.md
- bash tools/lint/task.sh
- bash tools/lint/integrity.sh
- bash tools/lint/style.sh
- git diff --name-only
- git diff --stat
- comm -23 <(git diff --name-only | sort) <(sort storage/dp/active/allowlist.txt) || true
- comm -23 <(git ls-files --others --exclude-standard | sort) <(sort storage/dp/active/allowlist.txt) || true
- ./ops/bin/open

**DP-specific receipt commands (scope-specific; author below):**
Note: Command substitution forms (e.g., $(pwd), $(git ...)) are rejected by certify replay. Use literal values only. dp.sh enforces at lint time.
- bash -n ops/bin/bundle
- bash -n ops/lib/scripts/bundle.sh
- bash -n tools/lint/plan.sh
- bash tools/lint/plan.sh --test
- test -f ops/src/surfaces/plan.md.tpl
- test -f docs/ops/specs/surfaces/plan.md
- ./ops/bin/bundle --profile=analyst --out=auto
- ./ops/bin/bundle --profile=architect --out=auto
- ./ops/bin/bundle --profile=audit --out=auto
- ./ops/bin/bundle --profile=hygiene --out=auto
- ./ops/bin/bundle --profile=auto --out=auto
- sh -c '! ./ops/bin/bundle --profile=auditor --out=auto'
- sh -c '! ./ops/bin/bundle --profile=auditor --intent="bad format" --out=auto'
- ./ops/bin/bundle --profile=auditor --intent="ADDENDUM REQUIRED: RoR-2026-03-01-025 - bundle auditor proof" --out=auto
- grep -n '"bundle_version": "2"' storage/handoff/BUNDLE-analyst-work-dp-ops-0146-bundle-conformance-2026-03-03-9f0fdc6a.manifest.json
- grep -n '"embedded": true' storage/handoff/BUNDLE-analyst-work-dp-ops-0146-bundle-conformance-2026-03-03-9f0fdc6a.manifest.json
- test -f storage/handoff/BUNDLE-analyst-work-dp-ops-0146-bundle-conformance-2026-03-03-9f0fdc6a.tar
- test ! -f storage/handoff/OPEN-bundle-analyst-work-dp-ops-0146-bundle-conformance-2026-03-03-9f0fdc6a.txt
- sh -c 'tar -tf storage/handoff/BUNDLE-analyst-work-dp-ops-0146-bundle-conformance-2026-03-03-9f0fdc6a.tar | sed -n "1,40p"'
- grep -n 'resolved_profile=architect' docs/ops/prompts/e-prompt-03.md
- grep -n 'resolved_profile=audit' docs/ops/prompts/e-prompt-01.md
- grep -n 'auditor' docs/ops/prompts/e-prompt-01.md
- grep -n 'Architect Handoff' docs/ops/prompts/e-prompt-03.md
- grep -n 'Selected Option:' docs/ops/prompts/e-prompt-04.md
- grep -n 'Slice Mode:' docs/ops/prompts/e-prompt-04.md
- grep -n 'Selected Slices:' docs/ops/prompts/e-prompt-04.md
- grep -n 'Execution Order:' docs/ops/prompts/e-prompt-04.md
- grep -n 'resolved_profile=hygiene' docs/ops/prompts/e-prompt-02.md
- grep -n 'resolved_profile=auditor' docs/ops/prompts/e-prompt-05.md
- grep -n 'ADDENDUM REQUIRED:' docs/ops/prompts/e-prompt-05.md
- grep -n 'DP Slot Source Map' ops/src/surfaces/plan.md.tpl
- grep -n 'DP_ID' ops/src/surfaces/plan.md.tpl
- grep -n 'plan.md.tpl' docs/ops/registry/templates.md

## 3.5 Closeout (Mandatory Routing)
- Execute docs/MANUAL.md Closeout Cycle in order (Verify, Harvest, Refresh, Log, Prune).
- Update SoP.md and PoW.md with DP entries, including objective summary and verification commands run.
- Protocol order for closeout: Verify -> Generate Results -> COMMIT (Operator Only) -> Prune.
- Run prune hygiene: ./ops/bin/prune --scrub.
- Regenerate session artifacts: `./ops/bin/open --out=auto`
- Capture updated Ops state: `./ops/bin/dump --scope=core --format=chatgpt --out=auto`
- Ensure the next session begins with refreshed session artifacts and matching receipts.
- Refresh side-effect: `ops/bin/llms` regenerates `ops/lib/manifests/OPS.md` as a compile event; if `OPS.md` is not in the allowlist, restore it before running `integrity.sh`. See `docs/MANUAL.md` Refresh side-effect notice for the full procedure.

### 3.5.1 Mandatory Closing Sidecar
Closing sidecar content is generated and maintained at `storage/handoff/CLOSING-DP-OPS-0146.md` and is validated by `ops/bin/certify` as a hard gate at certification time. Do not author, predict, populate, or approximate any sidecar-derived closeout output outside this sidecar.
Certify separately collects contractor-authored narrative for the RESULTS Contractor Execution Narrative section at certify time via interactive editor prompt; this narrative input is independent from closing sidecar content.

Before running certify, confirm `storage/handoff/CLOSING-DP-OPS-0146.md` has been maintained throughout execution and reflects observable reality only.

Closing sidecar fields required at certify time:
- Commit Message
- Create Pull Request (Title)
- Create Pull Request (Description)
- Confirm Merge (Commit Message)
- Confirm Merge (Extended Description)
- Confirm Merge (Add a Comment)
