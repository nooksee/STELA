---
trace_id: stela-20260315T175329Z-0e7f20f7
packet_id: DP-OPS-0198
created_at: /home/nos4r2/dev/stela/var/tmp/certify.DP-OPS-0198.Baeosb/dp-block.md
previous: archives/surfaces/TASK-DP-OPS-0197-87f23f56.md
---
# STELA TASK DASHBOARD
Status: ACTIVE
Owner: Integrator
Last Updated: 2026-03-02

## 1. Session State
Pointer: Session context output (generated by ops/bin/open)
Context Manifest: ops/lib/manifests/CONTEXT.md (checked by tools/lint/context.sh)

Session State contract:
- Session State is pointer-first. Do not write inline branch or hash state here.
- OPEN and OPEN-PORCELAIN are the session state snapshot.
- OPEN-PORCELAIN is emitted on every OPEN run. Clean sessions produce a zero-line artifact.
- RESULTS is the audit record for the DP.
- TASK is the stable routing surface.
- Operator note (2026-02-17): Do not edit `SoP.md` or `PoW.md` unless explicitly instructed.

## 2. Logic Pointers
Primary Constraint: PoT.md (Policy of Truth).
Surface Contract Spec: docs/ops/specs/surfaces/task.md.

### 2.1 Governance Pointers
Jurisdiction: PoT.md Section 3.
Git Authority: PoT.md Section 4.1.
Behavioral Standard: PoT.md Section 4.2.
Staffing Protocol: PoT.md Section 4.1.

### 2.2 Enforcement and Gates
Style: tools/lint/style.sh
Context: tools/lint/context.sh
Truth: tools/lint/truth.sh
DP lint: tools/lint/dp.sh (enforces DP transaction rules in Section 3; does not enforce TASK container schema)
TASK lint: tools/lint/task.sh (sole TASK surface/container schema enforcer)
Repo verify: tools/verify.sh
llms parity: tools/lint/llms.sh

## 3. Current Dispatch Packet (DP)

Section-by-section authoring protocol:
- Generate DP structure from `ops/src/surfaces/dp.md.tpl` using `ops/bin/draft`.
- Do not hand-edit DP structural boilerplate in `TASK.md` after generation.
- Manual edits are limited to slot content only (objective, scope, safety, plan body, receipt body).
- Freeze each completed subsection in order (3.1 -> 3.2 -> 3.3 -> 3.4.x).
- Do not revise earlier sections to justify later work. If scope changes, end the thread and draft a new DP.

Freeze Rule:
- If a completed DP subsection changes later, append an AMENDMENT block in RESULTS; otherwise DP is FAIL.

AMENDMENT: minimum fields
- Reopened section:
- Reason:
- Impact:
- Allowlist change (none or exact paths):

Input discipline (hard rule):
- Worker receives only the DP text. Worker does not require OPEN or DUMP attachments.
- DP writer must not attach or cite disposable artifacts (rough notes, chat logs, scratch drafts, throwaway excerpts).
- DP writer must not include pasted bundles (OPEN, DUMP payloads, manifests) inside the DP body.
- Canon pointers only. Evidence belongs in storage artifacts and RESULTS.


### DP-OPS-0198: Closeout Auto-Guard, Verify Harmony, and Dump-Visible Prune

## 3.1 Freshness Gate (Must Pass Before Work)
Base Branch: main
Required Work Branch: work/dp-ops-0198-closeout-autoguard-storage-prune-2026-03-15
Base HEAD: 87f23f56
Freshness Stamp: 2026-03-15
Note: FRESHNESS_STAMP must be YYYY-MM-DD format only. No trace tokens, no timestamps, no other text. Certify rejects all other forms. dp.sh enforces at lint time.

Required local re-check (worker runs; paste outputs in RESULTS):
- git rev-parse --abbrev-ref HEAD
- git rev-parse --short HEAD
- git status --porcelain

Preconditions:
- No commits on main.
- Working tree must be clean before execution begins.
- If Base HEAD changes, regenerate session artifacts from canonical tools before proceeding.

STOP conditions:
- STOP if any mismatch (branch, Base HEAD, missing work branch).
- STOP if working tree is dirty before execution begins.
- STOP if told to create or switch branches.

## 3.1.1 DP Preflight Gate (Run Before Any Edits)
Purpose:
- Catch malformed DP or TASK schema before work begins.

Worker runs (paste outcome in RESULTS):
- bash tools/lint/dp.sh --test
- bash tools/lint/dp.sh TASK.md
- bash tools/lint/task.sh

STOP if any preflight check fails.

### CbC Design Discipline Preflight (TASK.md §3.1.1)
Required when the DP objective adds, modifies, or replaces a linter, script, guard, or validation binary.
For non-tooling DPs: state "Not applicable" with a one-line justification.
Applicable. This packet hardens closeout, verify orchestration, and prune as system behaviors rather than recurring operator-memory chores. Structural prevention target is to remove packet-specific allowlist rediscovery for certify-generated surfaces, keep active TASK generation packet-consistent, tighten verify and certify lane ordering so cheap failures land early, and make prune speak to the surfaces that actually inflate full-dump context. Detection remains necessary because generated-surface routing, TASK materialization, lane composition, dump-visible pressure ranking, and prune safety boundaries can still drift unless the new contracts are enforced in code and tests.

## 3.2 Required Context Load (Read Before Doing Anything)

### 3.2.1 Canon load order (always)
Worker must confirm loaded before edits begin:
1. PoT.md
2. SoP.md
3. PoW.md
4. TASK.md
5. docs/MAP.md
6. docs/MANUAL.md
7. ops/lib/manifests/CONTEXT.md

Notes:
- Worker does not read OPEN. OPEN is for Integrator state refresh and for receipts.
- Disposable artifacts must not be referenced or included.
- Worker input is the DP text only.
- DP writer must not attach or cite disposable artifacts.
- DP writer must not embed pasted bundles.

## Rules

1 Temp path rule uses `var/tmp` only
2 RESULTS generation rule uses `ops/bin/certify` only
3 Receipt rule runs every DP mandated command and treats any skip as STOP
4 Disposable artifact rule treats OPEN dump bundles and manifests as non canonical session artifacts
5 Use repo-relative paths only in all output. Never print absolute filesystem paths. Do not emit clickable file links.

### 3.2.2 DP-scoped load order (per DP)
- ops/bin/certify
- ops/bin/prune
- tools/verify.sh
- tools/test/bundle.sh
- tools/lint/integrity.sh
- ops/lib/scripts/task.sh
- docs/ops/specs/binaries/certify.md
- docs/ops/specs/binaries/prune.md
- docs/ops/specs/scripts/task.md
- docs/ops/specs/tools/verify.md
- docs/ops/specs/tools/test/bundle.md
- docs/ops/specs/tools/lint/integrity.md
- ops/lib/manifests/PRUNE.md
- docs/MANUAL.md
- storage/dp/active/allowlist.txt
- ops/bin/trace

## 3.3 Scope and Safety
Objective:
- Eliminate the recurring closeout failure where certify-generated surfaces require manual per-packet allowlist patching.
- Fix the TASK generation and closeout path so the active archived TASK leaf body always matches the active packet id and work branch, with no stale prior-packet body carryover.
- Tighten the harmony between lint, verify, and certify so cheap deterministic failures land early and the repo-gate path stops paying avoidable replay cost.
- Make prune measure whole-repo pressure, but require this packet's automatic action to stay focused on the surfaces that full dump actually serializes and on dump-visible disposable artifacts.

In scope:
- UPDATE `ops/bin/certify` so certify-generated surfaces are recognized structurally at preflight/replay time and no longer depend on packet-specific allowlist additions for each new DP leaf.
- UPDATE the TASK materialization or closeout path that archives the active TASK leaf so packet id, branch, and body content remain packet-consistent at certify time.
- UPDATE `tools/verify.sh` and any directly coupled verify-path helpers so certify-critical and repo-gate paths fail fast, stay mode-local, and stop paying avoidable duplicate cost.
- UPDATE the relevant integrity/allowlist enforcement path so certify-owned generated surfaces are validated against an internal generated-surface policy instead of recurring packet-specific memory work.
- UPDATE docs/specs/manual so generated closeout artifacts, TASK packet-consistency, verify lane composition, and their policy boundaries are explicit.
- UPDATE `ops/bin/prune` and `ops/lib/manifests/PRUNE.md` so dump-visible pressure is reported by exact path class and top contributor, with safe apply available only for dump-visible disposable artifacts.
- ALLOW prune report output to include broader whole-repo pressure context, but keep non-dump retention action operator-directed or explicitly contractor-directed rather than automatic under this packet.
- KEEP canonical dump-visible bulk reportable even when not prune-eligible. Current measured dominant contributor is `archives/`, especially `archives/surfaces/` leaves.
- KEEP prune apply denylist-protected and current-proof-safe:
  - current RESULTS
  - current CLOSING
  - active OPEN / OPEN-PORCELAIN
  - active bundle artifacts from the current run
  - current head pointers and live canonical heads
  - any dump-visible surface still required to certify or audit the current active packet
- UPDATE tests/specs/manual to prove:
  - certify no longer fails on missing per-DP generated leaf patterns,
  - active TASK head and archived TASK leaf point at and describe the same packet,
  - verify/certify/lint lane ordering is coherent and cheap failures land before expensive bundle replay,
  - prune report identifies the dominant full-dump contributors by path,
  - prune apply removes only eligible dump-visible disposable artifacts,
  - current proof surfaces survive intact,
  - report output still explains what happened.
- UPDATE `storage/dp/active/allowlist.txt` only for tracked files touched by this packet; do not keep expanding it with new per-DP generated leaf patterns if the mechanism is correctly fixed.

Out of scope:
- Changes to analyst, architect, contractor, or audit stance behavior.
- Factory staffing / definition routing.
- New external services or background daemons.
- Broad repository retention redesign beyond dump-visible pressure reporting and safe prune of disposable dump-visible artifacts.
- Automatic whole-repo prune action outside dump-visible disposable artifacts.
- Renaming ingest surfaces.
- Reworking bundle profile contracts outside what is directly required for TASK packet-consistency, verify harmony, dump-visible prune behavior, or certify generated-surface ownership.

Safety and invariants:
- Certify-generated surfaces must remain deterministic and auditable.
- Active TASK head and archived TASK leaf content must remain packet-consistent; frontmatter and body must not describe different packets.
- Generated-surface auto-handling must not become a loophole that masks unrelated changed files outside allowlist.
- Prune must distinguish between canonical dump-visible bulk and disposable dump-visible artifacts; report-only and apply paths must not blur that boundary.
- Whole-repo pressure may be observed and reported, but automatic action under this packet must stay bounded to dump-visible disposable artifacts unless the operator or a later packet explicitly broadens scope.
- Current active proof surfaces must never become prune candidates.
- Report output must remain available before and after apply so the operator can see what prune would remove and what it did remove.
- The fix must reduce recurring closeout labor; if a new DP still needs manual `TASK-DP-OPS-<id>-*` allowlist edits after this slice, the packet is incomplete.
- The fix must improve repo-gate practicality; if certify-critical or default verify still pays unrelated expensive paths before cheap gate failures are known, the packet is incomplete.

Worker Constraints (SSOT injected):
## Section 1: Universal Template Rules
- Template source uses `tpl` files with optional YAML frontmatter
- Canon frontmatter keys: `template_type` `template_id` `template_version` `requires_slots` `includes`
- Renderer strips frontmatter before output write
- Slot token form `\{\{TOKEN\}\}` with uppercase alphanumeric underscore
- Include forms `\{\{@include:path\}\}` and `\{\{@include:path#section\}\}`
- Include resolution is strict: missing file fail missing section fail circular graph fail
- Strict mode default: every required slot value present and no unresolved token
- Non strict mode allowed only for lint and normalization workflows
- Worker facing generated surfaces remain pointer first and exclude disposable artifacts

Target Files allowlist (hard gate):
- storage/dp/active/allowlist.txt

## 3.4 Execution Plan (A-E)

### 3.4.1 State
- Closeout still repeatedly fails on missing per-DP allowlist coverage for certify-generated TASK archive leaves.
- The system already knows those paths are certify-owned and predictable, but it still treats them like packet-specific manual surprises.
- Recent audit evidence also exposed a stronger correctness defect: the archived active TASK leaf can carry the prior packet body even when frontmatter has advanced to the new packet id.
- Verify, certify, and repo gates are still too loosely orchestrated: recent certified runs put `bash tools/test/bundle.sh` in the `78s` to `82s` range and `verify --mode=full` at roughly `109s`, which is enough to push GitHub gates toward the five-minute mark when replay and other lanes stack on top.
- Direct working-tree measurement shows current full-dump bulk is not `storage`; it is `archives/`, with `archives/surfaces/` as the dominant contributor. Current top-level dump-visible sizes are approximately: `archives 8.1M`, `ops 764K`, `docs 604K`, `tools 380K`, `opt 144K`.
- The largest current tracked files likely in full dump are overwhelmingly `archives/surfaces/PoW-*`, `archives/surfaces/SoP-*`, and archived TASK leaves.
- This means generic storage pruning does not answer the operator question. The dump-visible pressure report and any safe apply path must target what the full dump actually serializes.

### 3.4.2 Request
- Remove the recurring generated-leaf allowlist failure as a class.
- Make certify-generated surfaces structurally owned by certify rather than by per-packet memory or commentary.
- Make active TASK generation packet-consistent so a re-audit sees the right packet body, not a stale predecessor.
- Make lint, verify, and certify act like one system: cheap deterministic checks first, mode-local expensive checks only where they add value, and no unrelated architect or analyst bleed into the active packet lane.
- Make prune answer the dump question directly: what is inflating full-dump context, what part is canonical and report-only, and what part is disposable and safe to reduce.
- Let prune also surface broader whole-repo pressure for operator awareness, while keeping this packet's automatic remediation bounded to the dump-visible disposable side.
- Keep the mechanism explainable: operator should be able to see what certify auto-owned, how verify lanes were composed, what full dump is carrying, and what prune reported or removed.
- If a proposed auto-ownership or prune rule is too broad and could hide unrelated out-of-scope changes, STOP and narrow it.

### 3.4.3 Changelog
UPDATE:
- ops/bin/certify
- ops/bin/prune
- tools/verify.sh
- tools/test/bundle.sh
- tools/lint/integrity.sh
- ops/lib/scripts/task.sh
- ops/lib/manifests/PRUNE.md
- docs/ops/specs/binaries/certify.md
- docs/ops/specs/binaries/prune.md
- docs/ops/specs/scripts/task.md
- docs/ops/specs/tools/verify.md
- docs/ops/specs/tools/test/bundle.md
- docs/ops/specs/tools/lint/integrity.md
- docs/MANUAL.md
- storage/dp/active/allowlist.txt

### 3.4.4 Patch / Diff
1. Update `storage/dp/active/allowlist.txt` first with every tracked file touched by this packet and the active closing-sidecar path for `DP-OPS-0198`.
2. Refactor `ops/bin/certify` so expected certify-generated surfaces are computed structurally from packet/runtime identity and validated as certify-owned outputs instead of requiring per-packet allowlist pattern edits.
3. Fix the TASK materialization or closeout path so the archived active TASK leaf body is rendered from the active packet and stays consistent with packet frontmatter and work-branch identity.
4. Tighten the allowlist and integrity path so only exact certify-generated classes are auto-owned; unrelated changed files must still fail hard.
5. Refactor `tools/verify.sh` and any directly coupled bundle smoke path so cheap deterministic lanes fail first and certify-critical mode avoids unrelated expensive replay where policy does not require it.
6. Add explicit preflight and lane reporting that names the generated-surface policy and the verify/certify lane order in effect so the operator can see what is allowed to emit and what is paying time.
7. Extend `ops/bin/prune` and `ops/lib/manifests/PRUNE.md` with a pressure report that can show both whole-repo and dump-visible contributors by exact path class, while distinguishing canonical report-only bulk from disposable prune-eligible artifacts.
8. Add a safe apply path for dump-visible disposable artifacts only; do not make canonical archive surfaces or broader whole-repo pressure candidates for automatic prune action under this packet.
9. Protect current proof surfaces with exact denylist and retention rules so current RESULTS, CLOSING, OPEN, current bundle artifacts, and active canonical heads survive prune apply.
10. Update manual/specs so the generated-surface ownership boundary, TASK packet-consistency rule, verify/certify harmony, and dump-visible prune contract are explicit.
11. Run targeted gates proving:
   - certify-generated surfaces no longer require manual `TASK-DP-OPS-<id>-*` allowlist edits,
   - active TASK head and archived TASK leaf point at and describe the same packet,
   - verify/certify/lint lane order is coherent and cheap failures appear before expensive bundle smoke,
   - prune can report broader whole-repo pressure without widening automatic action,
   - prune report identifies the dominant full-dump contributors by path,
   - prune apply removes only eligible dump-visible disposable artifacts,
   - current proof surfaces remain intact,
   - report output remains accurate.

### 3.4.5 Receipt (Proofs to collect) - MUST RUN

**Mandatory receipt commands (always run; do not omit):**
- bash tools/lint/dp.sh TASK.md
- bash tools/lint/task.sh
- bash tools/lint/integrity.sh
- bash tools/lint/style.sh
- bash tools/test/bundle.sh --mode=certify-critical
- ./ops/bin/llms
- bash tools/lint/integrity.sh
- git diff --name-only
- git diff --stat
- comm -23 <(git diff --name-only | sort) <(sort storage/dp/active/allowlist.txt) || true
- comm -23 <(git ls-files --others --exclude-standard | sort) <(sort storage/dp/active/allowlist.txt) || true
- ./ops/bin/open

**DP-specific receipt commands (scope-specific; author below):**
Note: Command substitution forms (e.g., $(pwd), $(git ...)) are rejected by certify replay. Use literal values only. dp.sh enforces at lint time.
- bash tools/verify.sh --mode=certify-critical
- bash tools/verify.sh --mode=full
- bash tools/lint/task.sh TASK.md
- ./ops/bin/prune --target=dump --phase=report --dry-run
- ./ops/bin/prune --target=dump --phase=apply --dry-run
- grep -n "generated surface\|generated path\|allowlist\|TASK-DP-OPS" ops/bin/certify docs/ops/specs/binaries/certify.md docs/MANUAL.md
- grep -n "packet_id\|TASK-DP-OPS\|Current Dispatch Packet\|work/dp-ops-0198" TASK.md ops/lib/scripts/task.sh docs/ops/specs/scripts/task.md
- grep -n "mode=certify-critical\|bundle.sh\|lane\|phase\|fail fast\|fail-fast" tools/verify.sh tools/test/bundle.sh docs/ops/specs/tools/verify.md docs/ops/specs/tools/test/bundle.md docs/MANUAL.md
- grep -n "target=dump\|dump-visible\|canonical\|report-only\|apply" ops/bin/prune ops/lib/manifests/PRUNE.md docs/ops/specs/binaries/prune.md docs/MANUAL.md
- grep -n "allowlist\|generated\|certify" tools/lint/integrity.sh docs/ops/specs/tools/lint/integrity.md

## 3.5 Closeout (Mandatory Routing)
- Execute docs/MANUAL.md Closeout Cycle in order (Verify, Harvest, Refresh, Log, Prune).
- Update SoP.md and PoW.md with DP entries, including objective summary and verification commands run.
- Protocol order for closeout: Verify -> Generate Results -> COMMIT (Operator Only) -> Prune.
- Run prune hygiene: ./ops/bin/prune --scrub.
- Refresh llms artifacts: `./ops/bin/llms`
- Regenerate session artifacts: `./ops/bin/open --out=auto`
- Capture updated Ops state: `./ops/bin/dump --scope=core --format=chatgpt --out=auto`
- Ensure the next session begins with refreshed session artifacts and matching receipts.
- Refresh side-effect: `ops/bin/llms` regenerates `ops/lib/manifests/OPS.md` as a compile event; if `OPS.md` is not in the allowlist, restore it before running `integrity.sh`. See `docs/MANUAL.md` Refresh side-effect notice for the full procedure.

### 3.5.1 Mandatory Closing Sidecar
Closing sidecar content is generated and maintained at `storage/handoff/CLOSING-DP-OPS-0198.md` and is validated by `ops/bin/certify` as a hard gate at certification time. Do not author, predict, populate, or approximate any sidecar-derived closeout output outside this sidecar.
Certify separately collects contractor-authored narrative for the RESULTS Contractor Execution Narrative section at certify time via interactive editor prompt; this narrative input is independent from closing sidecar content.

Before running certify, confirm `storage/handoff/CLOSING-DP-OPS-0198.md` has been maintained throughout execution and reflects observable reality only.

Closing sidecar fields required at certify time:
- Commit Message
- Create Pull Request (Title)
- Create Pull Request (Description)
- Confirm Merge (Commit Message)
- Confirm Merge (Extended Description)
- Confirm Merge (Add a Comment)
