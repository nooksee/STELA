---
trace_id: stela-20260331T015458Z-24983a2d
packet_id: DP-OPS-0235
created_at: 2026-03-31T01:57:02Z
previous: archives/surfaces/TASK-DP-OPS-0234-6170e576.md
---
# STELA TASK DASHBOARD
Status: ACTIVE
Owner: Integrator
Last Updated: 2026-02-14

## 1. Session State
Pointer: Session context output (generated by ops/bin/open)
Context Manifest: ops/lib/manifests/CONTEXT.md (checked by tools/lint/context.sh)

Session State contract:
- Session State is pointer-first. Do not write inline branch or hash state here.
- OPEN (and OPEN-PORCELAIN when dirty) is the session state snapshot.
- RESULTS is the audit record for the DP.
- TASK is the stable routing surface.

## 2. Logic Pointers
Primary Constraint: PoT.md (Policy of Truth).
Surface Contract Spec: docs/ops/specs/surfaces/task.md.

### 2.1 Governance Pointers
Jurisdiction: PoT.md Section 3.
Git Authority: PoT.md Section 4.1.
Behavioral Standard: PoT.md Section 4.2.
Staffing Protocol: PoT.md Section 4.1.

### 2.2 Enforcement and Gates
Style: tools/lint/style.sh
Context: tools/lint/context.sh
Truth: tools/lint/truth.sh
DP lint: tools/lint/dp.sh (enforces DP transaction rules in Section 3; does not enforce TASK container schema)
TASK lint: tools/lint/task.sh (sole TASK surface/container schema enforcer)
Repo verify: tools/verify.sh

## 3. Current Dispatch Packet (DP)

Section-by-section authoring protocol:
- Generate DP structure via `./ops/bin/bundle --profile=draft --out=auto` (standard operator path: Architect completes the embedded DP AUTHORING SCAFFOLD; save the returned DP body to `storage/dp/intake/DP.md`) or locally via `ops/bin/draft` (template render only).
- Do not hand-edit DP structural boilerplate in `TASK.md` after generation.
- Manual edits are limited to slot content only (objective, scope, safety, plan body, receipt body).
- Freeze each completed subsection in order (3.1 -> 3.2 -> 3.3 -> 3.4.x).
- Do not revise earlier sections to justify later work. If scope changes, end the thread and draft a new DP.

Freeze Rule:
- If a completed DP subsection changes later, append an AMENDMENT block in RESULTS; otherwise DP is FAIL.

AMENDMENT: minimum fields
- Reopened section:
- Reason:
- Impact:
- Allowlist change (none or exact paths):

Input discipline (hard rule):
- Worker receives only the DP text. Worker does not require OPEN or DUMP attachments.
- DP writer must not attach or cite disposable artifacts (rough notes, chat logs, scratch drafts, throwaway excerpts).
- DP writer must not include pasted bundles (OPEN, DUMP payloads, manifests) inside the DP body.
- Canon pointers only. Evidence belongs in storage artifacts and RESULTS.

### DP-OPS-0235: Relax planning question-mode transport

## 3.1 Freshness Gate (Must Pass Before Work)
Base Branch: main
Work Branch: work/dp-ops-0235-2026-03-31
Base HEAD: 62a11168
Freshness Stamp: 2026-03-31
Note: FRESHNESS_STAMP must be YYYY-MM-DD format only. No trace tokens, no timestamps, no other text. Certify rejects all other forms. dp.sh enforces at lint time.

Required local re-check (worker runs before any edits; paste verbatim outputs under `## Contractor Execution Narrative` -> `### Preflight State` in RESULTS):
- git rev-parse --abbrev-ref HEAD
- git rev-parse --short HEAD
- git status --porcelain

These three commands are freshness proof only. Do not treat §3.4.5 receipt replay as a substitute for their pre-edit outputs.

Preconditions:
- No commits on main.
- Working tree must be clean before execution begins.
- If Base HEAD changes, regenerate session artifacts from canonical tools before proceeding.

STOP conditions:
- STOP if any mismatch (branch, Base HEAD, missing work branch).
- STOP if working tree is dirty before execution begins.
- STOP if told to create or switch branches.

## 3.1.1 DP Preflight Gate (Run Before Any Edits)
Purpose:
- Catch malformed DP or TASK schema before work begins.

Worker runs (paste outcome in RESULTS):
- bash tools/lint/dp.sh --test
- bash tools/lint/dp.sh TASK.md
- bash tools/lint/task.sh

STOP if any preflight check fails.

### CbC Design Discipline Preflight (TASK.md §3.1.1)
This packet is tooling-applicable: it changes the planning stance contract and the planning response-envelope lint/spec so question mode can ask a short natural boundary question without the stale conversational wrapper. Safe behavior must be re-proven under `bash tools/lint/response.sh --test` before dispatch.

## 3.2 Required Context Load (Read Before Doing Anything)

### 3.2.1 Canon load order (always)
Worker must confirm loaded before edits begin:
1. PoT.md
2. SoP.md
3. PoW.md
4. TASK.md
5. docs/MAP.md
6. docs/MANUAL.md
7. ops/lib/manifests/CONTEXT.md

Notes:
- Worker does not read OPEN. OPEN is for Integrator state refresh and for receipts.
- Disposable artifacts must not be referenced or included.
- Worker input is the DP text only.
- DP writer must not attach or cite disposable artifacts.
- DP writer must not embed pasted bundles.

## Rules

1 Temp path rule uses `var/tmp` only
2 RESULTS generation rule uses `ops/bin/certify` only
3 Receipt rule runs every DP mandated command and treats any skip as STOP
4 Disposable artifact rule treats OPEN dump bundles and manifests as non canonical session artifacts
5 Use repo-relative paths only in all output. Never print absolute filesystem paths. Do not emit clickable file links.

### 3.2.2 DP-scoped load order (per DP)
1. storage/handoff/PLAN.md
2. ops/src/stances/planning.md.tpl
3. docs/ops/specs/stances/planning.md
4. tools/lint/response.sh
5. docs/ops/specs/tools/lint/response.md
6. tools/lint/style.sh
7. docs/ops/specs/tools/lint/style.md

## 3.3 Scope and Safety
Objective:
Keep the final-plan-first planning behavior from DP-OPS-0234, but remove the stale question-mode transport that still forces `Analysis and Discussion`, an invented third option, and `Questions / Conversation:`. The worker should leave planning question mode as one short packet-boundary question with the smallest truthful option set and no host-UI promises.

In scope:
- `ops/src/stances/planning.md.tpl`: remove the stale question-mode wrapper requirements; keep clarification as a narrow exception and preserve final-plan-first behavior.
- `docs/ops/specs/stances/planning.md`: mirror the updated question-mode transport contract exactly.
- `tools/lint/response.sh`: update planning-mode envelope/fixture logic so clarification mode no longer requires the old conversational wrapper shape and no longer forces exactly three options.
- `docs/ops/specs/tools/lint/response.md`: mirror the updated planning response-envelope contract and self-test expectations.
- `tools/lint/style.sh`: stop requiring the shared single-fence include for planning mode and instead enforce the planning-specific final-plan/question-mode split.
- `docs/ops/specs/tools/lint/style.md`: mirror the revised planning style guardrails exactly.

Out of scope:
- `ops/src/stances/plan.md.tpl`
- `ops/lib/scripts/bundle.sh`
- `ops/src/surfaces/dp.md.tpl`
- `docs/MANUAL.md`, `docs/CLOSEOUT.md`, `docs/MAP.md`, `docs/INDEX.md`
- docs-template activation
- README alignment
- popup/UI guarantees for any host product

Safety and invariants:
- Final plan mode must remain unchanged: final planning output is still one fenced markdown code block in canonical `PLAN.md` shape.
- Clarification must remain a narrow exception rather than the default mode.
- One real packet-boundary question remains preferred when clarification is necessary.
- Question mode must prefer the smallest truthful option set: use 2 options for a real binary choice and 3 only when the third branch is genuinely distinct and evidence-grounded.
- Do not invent extra branches solely to satisfy formatting.
- Do not reintroduce `Analysis and Discussion` or `Questions / Conversation:` as required planning question-mode transport.
- Do not encode promises about native popup UI; only shape model output.
- Leave `storage/dp/active/allowlist.txt` unchanged unless a preflight proof shows missing coverage.

Worker Constraints (SSOT injected):
## Section 1: Universal Template Rules
- Template source uses `tpl` files with optional YAML frontmatter
- Canon frontmatter keys: `template_type` `template_id` `template_version` `requires_slots` `includes`
- Renderer strips frontmatter before output write
- Slot token form `\{\{TOKEN\}\}` with uppercase alphanumeric underscore
- Include forms `\{\{@include:path\}\}` and `\{\{@include:path#section\}\}`
- Include resolution is strict: missing file fail missing section fail circular graph fail
- Strict mode default: every required slot value present and no unresolved token
- Non strict mode allowed only for lint and normalization workflows
- Worker facing generated surfaces remain pointer first and exclude disposable artifacts

Target Files allowlist (hard gate):
- storage/dp/active/allowlist.txt

## 3.4 Execution Plan (A-E)

### 3.4.1 State
- DP-OPS-0234 already landed the final-plan-first planning contract in `ops/src/stances/planning.md.tpl`, `docs/ops/specs/stances/planning.md`, `tools/lint/style.sh`, and `docs/ops/specs/tools/lint/style.md`.
- The remaining residue is question-mode transport only:
- `ops/src/stances/planning.md.tpl` still requires `1. Analysis and Discussion`, exactly 3 options, and `Questions / Conversation:`.
- `docs/ops/specs/stances/planning.md` still documents the same conversational question wrapper.
- `tools/lint/response.sh` still accepts/enforces that old wrapper shape and its `--test` fixtures are built around it.
- `ops/src/stances/planning.md.tpl` also still inherits the shared single-fence include, so rendered planning output remains internally contradictory until the planning-specific style contract no longer requires that shared fence line.
- Recent planning-bundle testing showed both GPT and Claude converged on the same good boundary-question behavior once the wrapper was loosened, but neither host produced guaranteed native picker UI. The repo change should therefore target response shape only, not host-product UI behavior.

### 3.4.2 Request
- Update the planning stance/spec so clarification mode asks the packet-boundary question first instead of requiring an analysis preamble.
- Update the planning response lint/spec so clarification mode accepts the smallest truthful bounded choice set:
  - 2 options for a real binary boundary choice
  - 3 options only when the third branch is genuinely distinct
- Remove the mandatory `Questions / Conversation:` footer requirement from planning clarification mode.
- Update the planning style lint/spec so planning no longer inherits the shared always-fenced contract; final-plan mode stays fenced while clarification mode remains plain text.
- Preserve the final-plan-first default and the one-fenced-block final-plan output contract unchanged.
- Preserve the rule that subordinate choices are settled directly when they do not change the packet boundary.

### 3.4.3 Changelog
- UPDATE ops/src/stances/planning.md.tpl
- UPDATE docs/ops/specs/stances/planning.md
- UPDATE tools/lint/response.sh
- UPDATE docs/ops/specs/tools/lint/response.md
- UPDATE tools/lint/style.sh
- UPDATE docs/ops/specs/tools/lint/style.md

### 3.4.4 Patch / Diff
1. In `ops/src/stances/planning.md.tpl`, rewrite question-mode exception instructions so they:
   - ask the question first
   - allow 2-3 meaningful, mutually exclusive options
   - prefer the smallest truthful set
   - remove the required `1. Analysis and Discussion` preamble
   - remove the required `Questions / Conversation:` footer
   - keep final-plan mode fenced and unchanged
2. In `docs/ops/specs/stances/planning.md`, mirror the updated question-mode transport and remove statements that still define conversational planning as the required question wrapper.
3. In `tools/lint/response.sh`, update planning-mode validation so it accepts two planning shapes:
   - final-plan mode: one fenced markdown code block that passes `bash tools/lint/plan.sh`
   - clarification mode: a short planning question output that does not require the old conversational wrapper and allows 2-3 options with at most one `(Recommended)` marker when justified
4. In `tools/lint/response.sh`, update the planning `--test` fixtures accordingly:
   - valid binary question mode
   - valid three-option question mode
   - invalid four-option question mode
   - invalid invented-wrapper regressions as needed
5. In `docs/ops/specs/tools/lint/response.md`, mirror the revised planning-mode mechanics and fixture expectations exactly.
6. In `tools/lint/style.sh` and `docs/ops/specs/tools/lint/style.md`, remove the planning-only requirement for the shared single-fence include and replace it with planning-specific guards:
   - final-plan mode stays fenced and emits no text outside the block
   - question mode asks first and remains unfenced
   - the planning contract no longer depends on the shared always-fenced include
7. Keep the packet narrow:
   - no bundle changes
   - no plan template changes
   - no popup-specific host-UI claims

### 3.4.5 Receipt (Proofs to collect) - MUST RUN

**Mandatory receipt commands (always run; do not omit):**
- bash tools/lint/dp.sh TASK.md
- bash tools/lint/task.sh
- bash tools/lint/integrity.sh
- bash tools/lint/style.sh
- ./ops/bin/llms
- bash tools/lint/integrity.sh
- git diff --name-only
- git diff --stat
- comm -23 <(git diff --name-only | sort) <(sort storage/dp/active/allowlist.txt) || true
- comm -23 <(git ls-files --others --exclude-standard | sort) <(sort storage/dp/active/allowlist.txt) || true
- ./ops/bin/open

**DP-specific receipt commands (scope-specific; author below):**
Note: Command substitution forms (e.g., $(pwd), $(git ...)) are rejected by certify replay. Use literal values only. Repo-local binaries must be authored as literal `./ops/bin/...` or `ops/bin/...` commands. Unsupported receipt-command forms are rejected by dp.sh and certify.
- bash tools/lint/response.sh --test
- bash tools/lint/dp.sh storage/dp/intake/DP.md
- grep -n "ask the question first" ops/src/stances/planning.md.tpl docs/ops/specs/stances/planning.md
- grep -n "2-3 meaningful" ops/src/stances/planning.md.tpl docs/ops/specs/stances/planning.md docs/ops/specs/tools/lint/response.md
- grep -n "smallest truthful" ops/src/stances/planning.md.tpl docs/ops/specs/stances/planning.md docs/ops/specs/tools/lint/response.md
- ! grep -Fq "Analysis and Discussion" ops/src/stances/planning.md.tpl
- ! grep -Fq "Questions / Conversation:" ops/src/stances/planning.md.tpl

## 3.5 Closeout (Mandatory Routing)
- Execute docs/MANUAL.md Closeout Cycle in order (Verify, Harvest, Refresh, Log, Prune).
- Update SoP.md and PoW.md with DP entries, including objective summary and current proof / receipt summary.
- Protocol order for closeout: Verify -> Generate Results -> COMMIT (Operator Only) -> Prune.
- Run prune hygiene: ./ops/bin/prune --scrub.
- Refresh llms artifacts: `./ops/bin/llms`
- Regenerate session artifacts: `./ops/bin/open --out=auto`
- Ensure the next session begins with refreshed session artifacts and matching receipts.
- Note: audit dump generation is owned by `./ops/bin/bundle --profile=audit --out=auto` and is separate from operator session refresh. Do not stamp an additional `ops/bin/dump --scope=core` into closeout unless the DP explicitly authorizes a standalone state capture.
- Refresh side-effect: `ops/bin/llms` regenerates `ops/lib/manifests/OPS.md` as a compile event; if `OPS.md` is not in the allowlist, restore it before running `integrity.sh`. See `docs/MANUAL.md` Refresh side-effect notice for the full procedure.

### 3.5.1 Mandatory Closing Sidecar
Closing sidecar content is generated and maintained at `storage/handoff/CLOSING.md` and is validated by `ops/bin/certify` as a hard gate at certification time. Do not author, predict, populate, or approximate any sidecar-derived closeout output outside this sidecar.
Certify separately collects contractor-authored narrative for the RESULTS Contractor Execution Narrative section at certify time via interactive editor prompt; this narrative input is independent from closing sidecar content.

Before running certify, confirm `storage/handoff/CLOSING.md` has been maintained throughout execution and reflects observable reality only.

Closing sidecar fields required at certify time:
- Commit Message
- Create Pull Request (Title)
- Create Pull Request (Description)
- Confirm Merge (Commit Message)
- Confirm Merge (Extended Description)
- Confirm Merge (Add a Comment)
