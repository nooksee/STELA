---
trace_id: stela-20260325T174252Z-26c52f54
packet_id: DP-OPS-0233
created_at: 2026-03-25T17:43:47Z
previous: (none)
---
# STELA TASK DASHBOARD
Status: ACTIVE
Owner: Integrator
Last Updated: 2026-02-14

## 1. Session State
Pointer: Session context output (generated by ops/bin/open)
Context Manifest: ops/lib/manifests/CONTEXT.md (checked by tools/lint/context.sh)

Session State contract:
- Session State is pointer-first. Do not write inline branch or hash state here.
- OPEN (and OPEN-PORCELAIN when dirty) is the session state snapshot.
- RESULTS is the audit record for the DP.
- TASK is the stable routing surface.

## 2. Logic Pointers
Primary Constraint: PoT.md (Policy of Truth).
Surface Contract Spec: docs/ops/specs/surfaces/task.md.

### 2.1 Governance Pointers
Jurisdiction: PoT.md Section 3.
Git Authority: PoT.md Section 4.1.
Behavioral Standard: PoT.md Section 4.2.
Staffing Protocol: PoT.md Section 4.1.

### 2.2 Enforcement and Gates
Style: tools/lint/style.sh
Context: tools/lint/context.sh
Truth: tools/lint/truth.sh
DP lint: tools/lint/dp.sh (enforces DP transaction rules in Section 3; does not enforce TASK container schema)
TASK lint: tools/lint/task.sh (sole TASK surface/container schema enforcer)
Repo verify: tools/verify.sh

## 3. Current Dispatch Packet (DP)

Section-by-section authoring protocol:
- Generate DP structure via `./ops/bin/bundle --profile=draft --out=auto` (standard operator path: Architect completes the embedded DP AUTHORING SCAFFOLD; save the returned DP body to `storage/dp/intake/DP.md`) or locally via `ops/bin/draft` (template render only).
- Do not hand-edit DP structural boilerplate in `TASK.md` after generation.
- Manual edits are limited to slot content only (objective, scope, safety, plan body, receipt body).
- Freeze each completed subsection in order (3.1 -> 3.2 -> 3.3 -> 3.4.x).
- Do not revise earlier sections to justify later work. If scope changes, end the thread and draft a new DP.

Freeze Rule:
- If a completed DP subsection changes later, append an AMENDMENT block in RESULTS; otherwise DP is FAIL.

AMENDMENT: minimum fields
- Reopened section:
- Reason:
- Impact:
- Allowlist change (none or exact paths):

Input discipline (hard rule):
- Worker receives only the DP text. Worker does not require OPEN or DUMP attachments.
- DP writer must not attach or cite disposable artifacts (rough notes, chat logs, scratch drafts, throwaway excerpts).
- DP writer must not include pasted bundles (OPEN, DUMP payloads, manifests) inside the DP body.
- Canon pointers only. Evidence belongs in storage artifacts and RESULTS.

### DP-OPS-0233: Complete addenda decoupling

## 3.1 Freshness Gate (Must Pass Before Work)
Base Branch: main
Work Branch: work/dp-ops-0233-2026-03-25
Base HEAD: cfa39a27
Freshness Stamp: 2026-03-25
Note: FRESHNESS_STAMP must be YYYY-MM-DD format only. No trace tokens, no timestamps, no other text. Certify rejects all other forms. dp.sh enforces at lint time.

Required local re-check (worker runs before any edits; paste verbatim outputs under `## Contractor Execution Narrative` -> `### Preflight State` in RESULTS):
- git rev-parse --abbrev-ref HEAD
- git rev-parse --short HEAD
- git status --porcelain

These three commands are freshness proof only. Do not treat §3.4.5 receipt replay as a substitute for their pre-edit outputs.

Preconditions:
- No commits on main.
- Working tree must be clean before execution begins.
- If Base HEAD changes, regenerate session artifacts from canonical tools before proceeding.

STOP conditions:
- STOP if any mismatch (branch, Base HEAD, missing work branch).
- STOP if working tree is dirty before execution begins.
- STOP if told to create or switch branches.

## 3.1.1 DP Preflight Gate (Run Before Any Edits)
Purpose:
- Catch malformed DP or TASK schema before work begins.

Worker runs (paste outcome in RESULTS):
- bash tools/lint/dp.sh --test
- bash tools/lint/dp.sh TASK.md
- bash tools/lint/task.sh

STOP if any preflight check fails.

### CbC Design Discipline Preflight (TASK.md §3.1.1)
Applicable. This packet modifies live bundle/runtime policy, lint contracts, dispatch wiring, validation manifests, and removes wired or referenced stance surfaces. The work is a bounded contract-correction slice for the still-open role-task decoupling items after the DP-OPS-0232 baseline, not a broader factory-model rewrite.

## 3.2 Required Context Load (Read Before Doing Anything)

### 3.2.1 Canon load order (always)
Worker must confirm loaded before edits begin:
1. PoT.md
2. SoP.md
3. PoW.md
4. TASK.md
5. docs/MAP.md
6. docs/MANUAL.md
7. ops/lib/manifests/CONTEXT.md

Notes:
- Worker does not read OPEN. OPEN is for Integrator state refresh and for receipts.
- Disposable artifacts must not be referenced or included.
- Worker input is the DP text only.
- DP writer must not attach or cite disposable artifacts.
- DP writer must not embed pasted bundles.

## Rules

1 Temp path rule uses `var/tmp` only
2 RESULTS generation rule uses `ops/bin/certify` only
3 Receipt rule runs every DP mandated command and treats any skip as STOP
4 Disposable artifact rule treats OPEN dump bundles and manifests as non canonical session artifacts
5 Use repo-relative paths only in all output. Never print absolute filesystem paths. Do not emit clickable file links.

### 3.2.2 DP-scoped load order (per DP)
Worker must additionally load, before edits begin:
1. storage/handoff/PLAN.md
2. ops/lib/manifests/BUNDLE.md
3. ops/lib/scripts/bundle.sh
4. tools/lint/response.sh
5. tools/lint/style.sh
6. ops/bin/manifest
7. ops/bin/template
8. ops/etc/verification.manifest
9. docs/ops/registry/agents.md
10. docs/ops/registry/factory.md
11. docs/ops/specs/definitions/agents.md
12. docs/ops/specs/tools/lint/factory.md
13. tools/lint/factory.sh
14. tools/lint/agent.sh
15. docs/ops/specs/tools/lint/agent.md
16. ops/src/stances/addenda.md.tpl
17. docs/ops/specs/stances/addenda.md
18. ops/src/stances/audit.md.tpl
19. ops/src/stances/contractor.md.tpl
20. docs/ops/specs/stances/contractor.md
21. ops/etc/persistence.manifest
22. ops/etc/retention.manifest
23. opt/_factory/agents/r-agent-01.md
24. opt/_factory/agents/r-agent-04.md
25. opt/_factory/agents/r-agent-08.md
26. storage/dp/active/allowlist.txt

## 3.3 Scope and Safety
Objective:
Complete the still-open post-DP-OPS-0232 role-task decoupling slice by renaming the live `foreman` task-bearing surfaces to `addenda`.
Keep actor-bearing role nouns intact where the attached plan explicitly preserves them, preserve `ADDENDUM` as the document/artifact noun, and do not reopen broader OPEN / MANUAL / factory-reset work or later decoupling removals outside this packet.

In scope:
- `ops/lib/manifests/BUNDLE.md`
- `ops/lib/scripts/bundle.sh`
- `tools/lint/response.sh`
- `tools/lint/style.sh`
- `ops/bin/manifest`
- `ops/bin/template`
- `ops/etc/verification.manifest`
- `docs/ops/registry/agents.md`
- `docs/ops/registry/factory.md`
- `docs/ops/specs/definitions/agents.md`
- `docs/ops/specs/tools/lint/factory.md`
- `tools/lint/factory.sh`
- `tools/lint/agent.sh`
- `docs/ops/specs/tools/lint/agent.md`
- `ops/etc/persistence.manifest`
- `ops/etc/retention.manifest`
- `ops/src/stances/contractor.md.tpl`
- `docs/ops/specs/stances/contractor.md`
- `opt/_factory/agents/r-agent-01.md`
- `opt/_factory/agents/r-agent-04.md`
- `opt/_factory/agents/r-agent-08.md`
- `docs/MANUAL.md`
- `docs/MAP.md`
- directly implicated `docs/ops/specs/*` and `docs/ops/registry/*` prose surfaces named by the plan for foreman/addenda truth
- rename `ops/src/stances/foreman.md.tpl` -> `ops/src/stances/addenda.md.tpl`
- rename `docs/ops/specs/stances/foreman.md` -> `docs/ops/specs/stances/addenda.md`
- update `ops/src/stances/audit.md.tpl` audit/addenda guard text to match the addenda rename
- update `storage/dp/active/allowlist.txt` only as required by the addenda rename

Out of scope:
- Part 2 factory canon reset
- stance-family normalization beyond the exact addenda decoupling changes above
- broad OPEN prose cleanup, broad MANUAL polish, or unrelated bundle wording cleanup
- re-opening completed `planning`, `draft`, or `audit` decoupling work except where the attached plan names a direct cross-dependency
- replacing actor-bearing `runtime_role` / `Runtime Role` values with task nouns
- `conform`, `conformist`, `hygiene`, and `contractor` removal work
- inventing new role-model assignments beyond the smallest truth-preserving updates needed for the addenda rename

Safety and invariants:
- Preserve actor-bearing role nouns where the attached plan says they remain role-owned, especially `runtime_role`, `Runtime Role`, role-naming maps, and actor-subject prose.
- Use `addenda` only for task/profile/process/stance-bearing surfaces; preserve `addendum` where it is already the document noun.
- Keep the settled `auditor` runtime-role / `audit` task split intact.
- No manual-generated-output fabrication. All receipt commands must run.
- No structural edits to DP/TASK canon outside this packet body.
- Respect the hard allowlist gate in `storage/dp/active/allowlist.txt`; update it exactly for the addenda rename and nothing broader.
- If an actor-bearing registry row cannot be kept truthful from directly visible repo state under the addenda rename, STOP instead of inventing a replacement identity.

Worker Constraints (SSOT injected):
## Section 1: Universal Template Rules
- Template source uses `tpl` files with optional YAML frontmatter
- Canon frontmatter keys: `template_type` `template_id` `template_version` `requires_slots` `includes`
- Renderer strips frontmatter before output write
- Slot token form `\{\{TOKEN\}\}` with uppercase alphanumeric underscore
- Include forms `\{\{@include:path\}\}` and `\{\{@include:path#section\}\}`
- Include resolution is strict: missing file fail missing section fail circular graph fail
- Strict mode default: every required slot value present and no unresolved token
- Non strict mode allowed only for lint and normalization workflows
- Worker facing generated surfaces remain pointer first and exclude disposable artifacts

Target Files allowlist (hard gate):
- storage/dp/active/allowlist.txt

## 3.4 Execution Plan (A-E)

### 3.4.1 State
- Bundle metadata shows `resolved_profile=draft`, `packet_id=DP-OPS-0233`, `dp_draft_path=storage/dp/intake/DP.md`, `closing_sidecar=storage/handoff/CLOSING.md`, `scope=full`, and `plan lint_status=PASS`.
- OPEN shows branch `main`, HEAD `cfa39a27`, and a clean working tree baseline.
- The attached plan states the post-DP-OPS-0232 draft/bundle/branch-equilibrium is already landed and that the next open decoupling item is `2g. Foreman -> Addenda`.
- The full dump index confirms the live presence of `ops/src/stances/foreman.md.tpl`, its coupled stance spec, and the touched runtime/lint/registry surfaces named by the plan for the addenda rename.
- Continuity decision: this DP intentionally realizes `2g` only, leaving `2h` and `2i` for later packets instead of bundling the remaining decoupling items together.

### 3.4.2 Request
- Rename the live `foreman` task-bearing lane to `addenda` across bundle policy, runtime wiring, lint mode names, stance/template dispatch, and coupled docs/specs/registries.
- Preserve `ADDENDUM` as the artifact/document noun and preserve actor-bearing role nouns where the plan explicitly forbids blind task-noun replacement.
- Apply the named cross-dependency in `ops/src/stances/audit.md.tpl` and the coupled `tools/lint/style.sh` expectation so audit/addenda guard strings stay in lockstep.
- Update `docs/ops/registry/agents.md` and other directly implicated registry/spec surfaces so stance/task references remain truthful after the addenda rename, without inventing task-noun runtime roles.
- Keep the packet narrowly bounded to `2g`. Do not reopen `2h`, `2i`, low-priority OPEN / MANUAL residue, or broader factory-reset schema work.

### 3.4.3 Changelog
- UPDATE `ops/lib/manifests/BUNDLE.md`
- UPDATE `ops/lib/scripts/bundle.sh`
- UPDATE `tools/lint/response.sh`
- UPDATE `tools/lint/style.sh`
- UPDATE `ops/bin/manifest`
- UPDATE `ops/bin/template`
- UPDATE `ops/etc/verification.manifest`
- UPDATE `docs/MANUAL.md`
- UPDATE `docs/MAP.md`
- UPDATE `docs/ops/registry/agents.md`
- UPDATE `docs/ops/registry/factory.md`
- UPDATE `docs/ops/specs/definitions/agents.md`
- UPDATE `docs/ops/specs/tools/lint/factory.md`
- UPDATE `tools/lint/factory.sh`
- UPDATE `tools/lint/agent.sh`
- UPDATE `docs/ops/specs/tools/lint/agent.md`
- UPDATE `ops/src/stances/audit.md.tpl`
- UPDATE `ops/src/stances/contractor.md.tpl`
- UPDATE `docs/ops/specs/stances/contractor.md`
- UPDATE `ops/etc/persistence.manifest`
- UPDATE `ops/etc/retention.manifest`
- UPDATE `opt/_factory/agents/r-agent-01.md`
- UPDATE `opt/_factory/agents/r-agent-04.md`
- UPDATE `opt/_factory/agents/r-agent-08.md`
- UPDATE `storage/dp/active/allowlist.txt`
- UPDATE directly implicated `docs/ops/specs/*` and `docs/ops/registry/*` prose surfaces required by the attached plan
- NEW `ops/src/stances/addenda.md.tpl`
- NEW `docs/ops/specs/stances/addenda.md`
- DELETE `ops/src/stances/foreman.md.tpl`
- DELETE `docs/ops/specs/stances/foreman.md`

### 3.4.4 Patch / Diff
1. Rename the live foreman stance/spec pair to addenda and update their internal task-bearing strings:
   - `ops/src/stances/foreman.md.tpl` -> `ops/src/stances/addenda.md.tpl`
   - `docs/ops/specs/stances/foreman.md` -> `docs/ops/specs/stances/addenda.md`
   - inside the renamed stance template, update `template_id`, `resolved_profile`, bundle-correction strings, and every `For machine-ingest foreman mode:` line to the addenda form while preserving `addendum` as the document noun.
2. Update the audit/addenda coupling exactly where the plan names it:
   - in `ops/src/stances/audit.md.tpl`, replace the foreman audit-guard sentence with the addenda form
   - in `tools/lint/style.sh`, update the paired required-string checks and rename the addenda-related contract functions/locals exactly as the plan prescribes so the lint contract matches the edited stance text.
3. Update bundle policy and runtime wiring for foreman -> addenda only:
   - in `ops/lib/manifests/BUNDLE.md`, rename foreman profile keys to addenda, keep prefix `ADDENDUM`, and update prose accordingly
   - in `ops/lib/scripts/bundle.sh`, rename foreman variables/functions/cases to addenda and keep task-bearing strings aligned with the updated policy manifest.
4. Update lint/runtime dispatch surfaces to match the new addenda truth:
   - in `tools/lint/response.sh`, rename foreman mode handling to addenda
   - in `tools/lint/style.sh`, rename the foreman contract checks to addenda
   - in `ops/bin/manifest` and `ops/bin/template`, switch foreman stance dispatch to addenda and update `stance-authority` file-path target if needed.
5. Update verification and registry/spec truth:
   - update foreman references in `ops/etc/verification.manifest` to the addenda form where the plan names them
   - update `docs/ops/registry/agents.md`, the directly implicated registry/spec surfaces, the affected live factory agent leaves, and `tools/lint/agent.sh` plus its spec so task-bearing `stance_id` truth stays aligned after the addenda rename
   - preserve actor-bearing role nouns where the attached plan explicitly reserves them; do not rewrite `runtime_role` or `Runtime Role` to `addenda`.
6. Update directly implicated runtime/profile follow-through surfaces required by the rename:
   - in `ops/etc/persistence.manifest`, rename the addendum-authorization persistence profile from `foreman` to `addenda`
   - in `ops/etc/retention.manifest`, rename the bundle-report class from the foreman form to the addenda form
   - in `ops/src/stances/contractor.md.tpl` and `docs/ops/specs/stances/contractor.md`, replace addenda-handoff references to `--profile=foreman`, `resolved_profile=foreman`, and `ops/src/stances/foreman.md.tpl` with the addenda equivalents while preserving the actor-bearing role noun `foreman`
7. Repair the allowlist for the rename only:
   - update allowlist entries as needed for the foreman -> addenda rename.
8. Sweep the touched scope only for forbidden residue and stop on drift:
   - confirm no task-bearing `foreman` identifiers remain in profiles, stance files, variables, lint modes, or function names
   - confirm no actor-bearing role noun was flattened into a task noun outside the explicit plan allowances.

### 3.4.5 Receipt (Proofs to collect) - MUST RUN

**Mandatory receipt commands (always run; do not omit):**
- bash tools/lint/dp.sh --test
- bash tools/lint/dp.sh TASK.md
- bash tools/lint/task.sh
- bash tools/lint/integrity.sh
- bash tools/lint/style.sh
- ./ops/bin/llms
- bash tools/lint/integrity.sh
- git diff --name-only
- git diff --stat
- comm -23 <(git diff --name-only | sort) <(sort storage/dp/active/allowlist.txt) || true
- comm -23 <(git ls-files --others --exclude-standard | sort) <(sort storage/dp/active/allowlist.txt) || true
- ./ops/bin/open

**DP-specific receipt commands (scope-specific; author below):**
Note: Command substitution forms (e.g., $(pwd), $(git ...)) are rejected by certify replay. Use literal values only. Repo-local binaries must be authored as literal `./ops/bin/...` or `ops/bin/...` commands. Unsupported receipt-command forms are rejected by dp.sh and certify.
- bash tools/lint/factory.sh
- bash tools/lint/agent.sh opt/_factory/agents/r-agent-01.md
- bash tools/lint/agent.sh opt/_factory/agents/r-agent-02.md
- bash tools/lint/agent.sh opt/_factory/agents/r-agent-04.md
- bash tools/lint/agent.sh opt/_factory/agents/r-agent-06.md
- bash tools/lint/agent.sh opt/_factory/agents/r-agent-08.md
- bash tools/test/bundle.sh
- git ls-files --others --exclude-standard | sort
- bash tools/lint/dp.sh storage/dp/intake/DP.md
- grep -n "addenda\|ADDENDUM\|stance-addenda\|resolved_profile=addenda" ops/lib/manifests/BUNDLE.md ops/lib/scripts/bundle.sh tools/lint/response.sh tools/lint/style.sh ops/bin/manifest ops/bin/template ops/etc/persistence.manifest ops/etc/retention.manifest ops/src/stances/addenda.md.tpl ops/src/stances/contractor.md.tpl docs/ops/specs/stances/addenda.md docs/ops/specs/stances/contractor.md
- grep -n "runtime_role\|Runtime Role\|stance_id" docs/ops/registry/agents.md docs/ops/registry/factory.md docs/ops/specs/definitions/agents.md opt/_factory/agents/r-agent-01.md opt/_factory/agents/r-agent-04.md opt/_factory/agents/r-agent-08.md

## 3.5 Closeout (Mandatory Routing)
- Execute docs/MANUAL.md Closeout Cycle in order (Verify, Harvest, Refresh, Log, Prune).
- Update SoP.md and PoW.md with DP entries, including objective summary and current proof / receipt summary.
- Protocol order for closeout: Verify -> Generate Results -> COMMIT (Operator Only) -> Prune.
- Run prune hygiene: ./ops/bin/prune --scrub.
- Refresh llms artifacts: `./ops/bin/llms`
- Regenerate session artifacts: `./ops/bin/open --out=auto`
- Ensure the next session begins with refreshed session artifacts and matching receipts.
- Note: audit dump generation is owned by `./ops/bin/bundle --profile=audit --out=auto` and is separate from operator session refresh. Do not stamp an additional `ops/bin/dump --scope=core` into closeout unless the DP explicitly authorizes a standalone state capture.
- Refresh side-effect: `ops/bin/llms` regenerates `ops/lib/manifests/OPS.md` as a compile event; if `OPS.md` is not in the allowlist, restore it before running `integrity.sh`. See `docs/MANUAL.md` Refresh side-effect notice for the full procedure.

### 3.5.1 Mandatory Closing Sidecar
Closing sidecar content is generated and maintained at `storage/handoff/CLOSING.md` and is validated by `ops/bin/certify` as a hard gate at certification time. Do not author, predict, populate, or approximate any sidecar-derived closeout output outside this sidecar.
Certify separately collects contractor-authored narrative for the RESULTS Contractor Execution Narrative section at certify time via interactive editor prompt; this narrative input is independent from closing sidecar content.

Before running certify, confirm `storage/handoff/CLOSING.md` has been maintained throughout execution and reflects observable reality only.

Closing sidecar fields required at certify time:
- Commit Message
- Create Pull Request (Title)
- Create Pull Request (Description)
- Confirm Merge (Commit Message)
- Confirm Merge (Extended Description)
- Confirm Merge (Add a Comment)
