---
trace_id: stela-20260324T174225Z-56cd559f
packet_id: DP-OPS-0228
created_at: 2026-03-24T17:45:28Z
previous: archives/surfaces/TASK-DP-OPS-0227-7d1aed3d.md
---
# STELA TASK DASHBOARD
Status: ACTIVE
Owner: Integrator
Last Updated: 2026-03-19

## 1. Session State
Pointer: Session context output (generated by ops/bin/open)
Context Manifest: ops/lib/manifests/CONTEXT.md (checked by tools/lint/context.sh)

Session State contract:
- Session State is pointer-first. Do not write inline branch or hash state here.
- OPEN is the session state snapshot. OPEN-PORCELAIN is conditional dirty-state detail.
- RESULTS is the audit record for the DP.
- TASK is the stable routing surface.

## 2. Logic Pointers
Primary Constraint: PoT.md (Policy of Truth).
Surface Contract Spec: docs/ops/specs/surfaces/task.md.

### 2.1 Governance Pointers
Jurisdiction: PoT.md Section 3.
Git Authority: PoT.md Section 4.1.
Behavioral Standard: PoT.md Section 4.2.
Staffing Protocol: PoT.md Section 4.1.

### 2.2 Enforcement and Gates
Style: tools/lint/style.sh
Context: tools/lint/context.sh
Truth: tools/lint/truth.sh
DP lint: tools/lint/dp.sh (enforces DP transaction rules in Section 3; does not enforce TASK container schema)
TASK lint: tools/lint/task.sh (sole TASK surface/container schema enforcer)
Repo verify: tools/verify.sh
llms parity: tools/lint/llms.sh

## 3. Current Dispatch Packet (DP)

Section-by-section authoring protocol:
- Generate DP structure from `ops/src/surfaces/dp.md.tpl` using `ops/bin/draft`.
- Do not hand-edit DP structural boilerplate in `TASK.md` after generation.
- Manual edits are limited to slot content only (objective, scope, safety, plan body, receipt body).
- Freeze each completed subsection in order (3.1 -> 3.2 -> 3.3 -> 3.4.x).
- Do not revise earlier sections to justify later work. If scope changes, end the thread and draft a new DP.

Freeze Rule:
- If a completed DP subsection changes later, append an AMENDMENT block in RESULTS; otherwise DP is FAIL.

AMENDMENT: minimum fields
- Reopened section:
- Reason:
- Impact:
- Allowlist change (none or exact paths):

Input discipline (hard rule):
- Worker receives only the DP text. Worker does not require OPEN or DUMP attachments.
- DP writer must not attach or cite disposable artifacts (rough notes, chat logs, scratch drafts, throwaway excerpts).
- DP writer must not include pasted bundles (OPEN, DUMP payloads, manifests) inside the DP body.
- Canon pointers only. Evidence belongs in storage artifacts and RESULTS.



### DP-OPS-0228: Restore machine-generated worker-ready DP output

## 3.1 Freshness Gate (Must Pass Before Work)
Base Branch: main
Required Work Branch: work/dp-ops-0228-worker-ready-dp-generation-2026-03-24
Base HEAD: b00ba25a
Freshness Stamp: 2026-03-24
Note: FRESHNESS_STAMP must be YYYY-MM-DD format only. No trace tokens, no timestamps, no other text. Certify rejects all other forms. dp.sh enforces at lint time.

Required local re-check (worker runs before any edits; paste verbatim outputs under `## Contractor Execution Narrative` -> `### Preflight State` in RESULTS):
- git rev-parse --abbrev-ref HEAD
- git rev-parse --short HEAD
- git status --porcelain

These three commands are freshness proof only. Do not treat §3.4.5 receipt replay as a substitute for their pre-edit outputs.

Preconditions:
- No commits on main.
- Working tree must be clean before execution begins.
- If Base HEAD changes, regenerate session artifacts from canonical tools before proceeding.

STOP conditions:
- STOP if any mismatch (branch, Base HEAD, missing work branch).
- STOP if working tree is dirty before execution begins.
- STOP if told to create or switch branches.

## 3.1.1 DP Preflight Gate (Run Before Any Edits)
Purpose:
- Catch malformed DP or TASK schema before work begins.

Worker runs (paste outcome in RESULTS):
- bash tools/lint/dp.sh --test
- bash tools/lint/dp.sh TASK.md
- bash tools/lint/task.sh

STOP if any preflight check fails.

### CbC Design Discipline Preflight (TASK.md §3.1.1)
Required when the DP objective adds, modifies, or replaces a linter, script, guard, or validation binary.
For non-tooling DPs: state "Not applicable" with a one-line justification.
Applicable. This packet modifies bundle runtime transport behavior and the worker-facing draft stance contract. Keep the implementation bounded to the draft profile and coupled docs/tests only; do not widen into certify or unrelated lint branches.

## 3.2 Required Context Load (Read Before Doing Anything)

### 3.2.1 Canon load order (always)
Worker must confirm loaded before edits begin:
1. PoT.md
2. SoP.md
3. PoW.md
4. TASK.md
5. docs/MAP.md
6. docs/MANUAL.md
7. ops/lib/manifests/CONTEXT.md

Notes:
- Worker does not read OPEN. OPEN is for Integrator state refresh and for receipts.
- Disposable artifacts must not be referenced or included.
- Worker input is the DP text only.
- DP writer must not attach or cite disposable artifacts.
- DP writer must not embed pasted bundles.

## Rules

1 Temp path rule uses `var/tmp` only
2 RESULTS generation rule uses `ops/bin/certify` only
3 Receipt rule runs every DP mandated command and treats any skip as STOP
4 Disposable artifact rule treats OPEN dump bundles and manifests as non canonical session artifacts
5 Use repo-relative paths only in all output. Never print absolute filesystem paths. Do not emit clickable file links.

### 3.2.2 DP-scoped load order (per DP)
- `ops/src/stances/draft.md.tpl`
- `docs/ops/specs/stances/draft.md`
- `ops/lib/manifests/BUNDLE.md`
- `docs/ops/specs/binaries/bundle.md`
- `ops/lib/scripts/bundle.sh`
- `docs/MANUAL.md` §Draft Workflow
- `tools/test/bundle.sh`
- `ops/src/surfaces/dp.md.tpl`

## 3.3 Scope and Safety
Objective:
Restore machine-generated worker-ready DP output in the draft lane by having the draft path hand Architect a renderer-owned full DP authoring scaffold and require Architect to return a complete worker-ready DP body directly.

In scope:
- `ops/lib/scripts/bundle.sh`: for `--profile=draft`, generate a full DP authoring scaffold from the canonical DP template with current canon-owned text already expanded and with current packet metadata populated where runtime already knows it; include that scaffold in the emitted draft bundle as Architect-facing authoring material.
- `ops/lib/manifests/BUNDLE.md`: update the Draft Transport Contract and Shipping Spine Contract so the standard draft path is bundle-provided authoring scaffold -> Architect emits full worker-ready DP -> operator saves to `storage/dp/intake/DP.md` -> `bash tools/lint/dp.sh storage/dp/intake/DP.md`.
- `docs/ops/specs/binaries/bundle.md`: update active-surface and draft-profile contract text to describe worker-ready DP output backed by a bundle-provided authoring scaffold rather than an intermediate slots scaffold.
- `ops/src/stances/draft.md.tpl`: replace the scaffold-only output contract with a full-DP output contract grounded in the attached authoring scaffold. Architect must preserve canon-owned structure from the scaffold, replace unresolved placeholders and packet-specific slots, and emit one fenced worker-ready DP body beginning at `### DP-...`.
- `docs/ops/specs/stances/draft.md`: align Outputs, Invariants, and Shipping Spine Position with the worker-ready DP contract.
- `docs/MANUAL.md` §Draft Workflow: replace the post-Architect local render step with direct save of the full DP output to `storage/dp/intake/DP.md`, immediate `dp.sh` validation, and dispatch.
- `tools/test/bundle.sh`: add draft-bundle smoke coverage proving the draft bundle carries the full DP authoring scaffold and the draft request contract remains coherent.
- `storage/dp/active/allowlist.txt`: add all modified files.

Out of scope:
- `ops/bin/draft` and `ops/bin/template`: no new binary modes are added in this packet; the standard shipping-spine fix is bundle-driven.
- `tools/lint/dp.sh` and `docs/ops/specs/tools/lint/dp.md`: branch-name and provisional-marker overreach remain follow-on work unless a direct blocker appears during implementation.
- `tools/lint/style.sh` and `docs/ops/specs/tools/lint/style.md`: keep the existing draft-mode guard surface unless a directly-coupled correction is required by the new active draft contract.
- `ops/bin/open`, `ops/bin/certify`, audit bundle semantics, and OPEN trace-anchor behavior.
- Role-task cleanup outside the draft lane, including auditor/audit runtime-role residue and the remaining decoupling items in `storage/handoff/PLAN.md`.
- Any change to `TASK.md`, `SoP.md`, or `PoW.md` semantics.

Safety and invariants:
- Do not regress to freehand reconstruction of canon-owned DP boilerplate by the model. The bundle-provided authoring scaffold must remain renderer-owned.
- Do not reintroduce a post-Architect render step in the standard draft shipping spine.
- Do not change audit, certify, or OPEN-anchor semantics in this packet.
- Keep draft packet identity explicit as `DP-OPS-XXXX` in bundle request metadata, draft output, and downstream receipts.
- Keep allowlist hard-gate behavior unchanged; update `storage/dp/active/allowlist.txt` before closeout for every modified path.
- Do not widen packet scope into the branch/provisional lint overreach unless a direct implementation blocker appears and is recorded explicitly.

Worker Constraints (SSOT injected):
## Section 1: Universal Template Rules
- Template source uses `tpl` files with optional YAML frontmatter
- Canon frontmatter keys: `template_type` `template_id` `template_version` `requires_slots` `includes`
- Renderer strips frontmatter before output write
- Slot token form `\{\{TOKEN\}\}` with uppercase alphanumeric underscore
- Include forms `\{\{@include:path\}\}` and `\{\{@include:path#section\}\}`
- Include resolution is strict: missing file fail missing section fail circular graph fail
- Strict mode default: every required slot value present and no unresolved token
- Non strict mode allowed only for lint and normalization workflows
- Worker facing generated surfaces remain pointer first and exclude disposable artifacts

Target Files allowlist (hard gate):
- storage/dp/active/allowlist.txt

## 3.4 Execution Plan (A-E)

### 3.4.1 State
- Branch: main, HEAD `b00ba25a`. Machine-generated draft output currently lands as an intermediate slots scaffold, not as a worker-ready DP body.
- `ops/src/stances/draft.md.tpl` currently tells Architect to emit only populated scaffold blocks and explicitly forbids emitting the full rendered DP body.
- `ops/lib/manifests/BUNDLE.md`, `docs/MANUAL.md`, and `docs/ops/specs/stances/draft.md` currently describe the scaffold-only draft path.
- `ops/lib/scripts/bundle.sh` does not currently hand Architect a renderer-owned full DP authoring scaffold for the draft profile.
- The actual operator workflow still wants the machine output itself to be the final worker-ready DP, saved directly to `storage/dp/intake/DP.md` and linted immediately.

### 3.4.2 Request
1. Update `ops/lib/scripts/bundle.sh` so draft bundles carry an explicit full DP authoring scaffold generated from current canonical DP structure. The scaffold must preserve canon-owned headings, include-expanded SSOT blocks, and known packet metadata, while leaving packet-specific authored content for the Architect to fill.
2. Update `ops/src/stances/draft.md.tpl` so the Architect uses that attached authoring scaffold and returns one fenced full DP body, not an intermediate slots scaffold. The output contract must again start at `### DP-...`, but the model is filling a renderer-owned scaffold rather than reconstructing mutable boilerplate from instructions.
3. Update the coupled human-readable contracts in `docs/ops/specs/stances/draft.md`, `ops/lib/manifests/BUNDLE.md`, `docs/ops/specs/binaries/bundle.md`, and `docs/MANUAL.md` so the standard draft workflow is: generate draft bundle, deliver to Architect, save full DP output to `storage/dp/intake/DP.md`, lint, dispatch.
4. Add deterministic bundle smoke coverage in `tools/test/bundle.sh` proving the draft bundle contains the authoring scaffold and that draft request metadata still points at `storage/dp/intake/DP.md` as the active intake surface.
5. Remove the now-stale legacy compatibility-note framing from `ops/src/stances/draft.md.tpl` if it is no longer needed once the active contract is again full-DP output.

### 3.4.3 Changelog
- UPDATE `ops/lib/scripts/bundle.sh`
- UPDATE `ops/lib/manifests/BUNDLE.md`
- UPDATE `docs/ops/specs/binaries/bundle.md`
- UPDATE `ops/src/stances/draft.md.tpl`
- UPDATE `docs/ops/specs/stances/draft.md`
- UPDATE `docs/MANUAL.md`
- UPDATE `tools/test/bundle.sh`
- UPDATE `storage/dp/active/allowlist.txt`

### 3.4.4 Patch / Diff
1. `ops/lib/scripts/bundle.sh` — in draft-profile assembly, generate a full DP authoring scaffold from the canonical DP template and embed it in the draft bundle as Architect-facing authoring material; preserve current packet-id routing and `dp_draft_path=storage/dp/intake/DP.md`.
2. `ops/lib/manifests/BUNDLE.md` — rewrite the Draft Transport Contract and Shipping Spine Contract so draft mode produces worker-ready DP output from a bundle-provided authoring scaffold rather than from an intermediate slots scaffold.
3. `docs/ops/specs/binaries/bundle.md` — align the Draft Profile Surfaces and draft profile narrative with the new worker-ready authoring scaffold contract.
4. `ops/src/stances/draft.md.tpl` — replace scaffold-only instructions with a full-DP output contract: use the attached authoring scaffold, preserve canon-owned structure, fill packet-specific content, emit one fenced worker-ready DP body beginning at `### DP-...`, and do not emit audit-only sections.
5. `docs/ops/specs/stances/draft.md` — update Outputs, Invariants, and Shipping Spine Position to match the full worker-ready DP contract.
6. `docs/MANUAL.md` — update Draft Workflow steps so the operator saves the full DP output directly to `storage/dp/intake/DP.md`, runs `bash tools/lint/dp.sh storage/dp/intake/DP.md`, confirms PASS, and dispatches without a post-Architect render step.
7. `tools/test/bundle.sh` — add draft-bundle assertions for authoring-scaffold presence and draft request coherence.
8. `storage/dp/active/allowlist.txt` — add all touched files before closeout verification.

### 3.4.5 Receipt (Proofs to collect) - MUST RUN

**Mandatory receipt commands (always run; do not omit):**
- bash tools/lint/dp.sh TASK.md
- bash tools/lint/task.sh
- bash tools/lint/integrity.sh
- bash tools/lint/style.sh
- ./ops/bin/llms
- bash tools/lint/integrity.sh
- git diff --name-only
- git diff --stat
- comm -23 <(git diff --name-only | sort) <(sort storage/dp/active/allowlist.txt) || true
- comm -23 <(git ls-files --others --exclude-standard | sort) <(sort storage/dp/active/allowlist.txt) || true
- ./ops/bin/open

**DP-specific receipt commands (scope-specific; author below):**
Note: Command substitution forms (e.g., $(pwd), $(git ...)) are rejected by certify replay. Use literal values only. Repo-local binaries must be authored as literal `./ops/bin/...` or `ops/bin/...` commands. Unsupported receipt-command forms are rejected by dp.sh and certify.
- bash tools/lint/dp.sh storage/dp/intake/DP.md
- ops/bin/manifest render stance-draft --out=-
- bash tools/test/bundle.sh

## 3.5 Closeout (Mandatory Routing)
- Execute docs/MANUAL.md Closeout Cycle in order (Verify, Harvest, Refresh, Log, Prune).
- Update SoP.md and PoW.md with DP entries, including objective summary and current proof / receipt summary.
- Protocol order for closeout: Verify -> Generate Results -> COMMIT (Operator Only) -> Prune.
- Run prune hygiene: ./ops/bin/prune --scrub.
- Refresh llms artifacts: `./ops/bin/llms`
- Regenerate session artifacts: `./ops/bin/open --out=auto`
- Ensure the next session begins with refreshed session artifacts and matching receipts.
- Note: audit dump generation is owned by `./ops/bin/bundle --profile=audit --out=auto` and is separate from operator session refresh. Do not stamp an additional `ops/bin/dump --scope=core` into closeout unless the DP explicitly authorizes a standalone state capture.
- Refresh side-effect: `ops/bin/llms` regenerates `ops/lib/manifests/OPS.md` as a compile event; if `OPS.md` is not in the allowlist, restore it before running `integrity.sh`. See `docs/MANUAL.md` Refresh side-effect notice for the full procedure.

### 3.5.1 Mandatory Closing Sidecar
Closing sidecar content is generated and maintained at `storage/handoff/CLOSING.md` and is validated by `ops/bin/certify` as a hard gate at certification time. Do not author, predict, populate, or approximate any sidecar-derived closeout output outside this sidecar.
Certify separately collects contractor-authored narrative for the RESULTS Contractor Execution Narrative section at certify time via interactive editor prompt; this narrative input is independent from closing sidecar content.

Before running certify, confirm `storage/handoff/CLOSING.md` has been maintained throughout execution and reflects observable reality only.

Closing sidecar fields required at certify time:
- Commit Message
- Create Pull Request (Title)
- Create Pull Request (Description)
- Confirm Merge (Commit Message)
- Confirm Merge (Extended Description)
- Confirm Merge (Add a Comment)
