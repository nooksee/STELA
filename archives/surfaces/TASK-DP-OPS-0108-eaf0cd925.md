---
trace_id: stela-20260224T013825Z-0c0f36d0
packet_id: DP-OPS-0108
created_at: 2026-02-24T01:43:57Z
previous: archives/surfaces/TASK-DP-OPS-0107-7567d65ae.md
---
# STELA TASK DASHBOARD
Status: ACTIVE
Owner: Integrator
Last Updated: 2026-02-18

## 1. Session State
Pointer: Session context output (generated by ops/bin/open)
Context Manifest: ops/lib/manifests/CONTEXT.md (checked by tools/lint/context.sh)

Session State contract:
- Session State is pointer-first. Do not write inline branch or hash state here.
- OPEN and OPEN-PORCELAIN are the session state snapshot.
- OPEN-PORCELAIN is emitted on every OPEN run. Clean sessions produce a zero-line artifact.
- RESULTS is the audit record for the DP.
- TASK is the stable routing surface.
- Operator note (2026-02-17): Do not edit `SoP.md` or `PoW.md` unless explicitly instructed.

## 2. Logic Pointers
Primary Constraint: PoT.md (Policy of Truth).
Surface Contract Spec: docs/ops/specs/surfaces/task.md.

### 2.1 Governance Pointers
Jurisdiction: PoT.md Section 3.
Git Authority: PoT.md Section 4.1.
Behavioral Standard: PoT.md Section 4.2.
Staffing Protocol: PoT.md Section 4.1.

### 2.2 Enforcement and Gates
Style: tools/lint/style.sh
Context: tools/lint/context.sh
Truth: tools/lint/truth.sh
DP lint: tools/lint/dp.sh (enforces DP transaction rules in Section 3; does not enforce TASK container schema)
TASK lint: tools/lint/task.sh (sole TASK surface/container schema enforcer)
Repo verify: tools/verify.sh
llms parity: tools/lint/llms.sh

## 3. Current Dispatch Packet (DP)

Section-by-section authoring protocol:
- Generate DP structure from `ops/src/surfaces/dp.md.tpl` using `ops/bin/draft`.
- Do not hand-edit DP structural boilerplate in `TASK.md` after generation.
- Manual edits are limited to slot content only (objective, scope, safety, plan body, receipt body).
- Freeze each completed subsection in order (3.1 -> 3.2 -> 3.3 -> 3.4.x).
- Do not revise earlier sections to justify later work. If scope changes, end the thread and draft a new DP.

Freeze Rule:
- If a completed DP subsection changes later, append an AMENDMENT block in RESULTS; otherwise DP is FAIL.

AMENDMENT: minimum fields
- Reopened section:
- Reason:
- Impact:
- Allowlist change (none or exact paths):

Input discipline (hard rule):
- Worker receives only the DP text. Worker does not require OPEN or DUMP attachments.
- DP writer must not attach or cite disposable artifacts (rough notes, chat logs, scratch drafts, throwaway excerpts).
- DP writer must not include pasted bundles (OPEN, DUMP payloads, manifests) inside the DP body.
- Canon pointers only. Evidence belongs in storage artifacts and RESULTS.

### DP-OPS-0107: Temporal Paradox Remediation — DP Template Closing Block Surgery and Integrator Stance Hardening

## 3.1 Freshness Gate (Must Pass Before Work)
Base Branch: main
Required Work Branch: work/certify-state-generation-2026-02-23
Base HEAD: 7567d65ae
Freshness Stamp: 2026-02-23
Note: FRESHNESS_STAMP must be YYYY-MM-DD format only. No trace tokens, no timestamps, no other text. Certify rejects all other forms. dp.sh enforces at lint time.

Required local re-check (worker runs; paste outputs in RESULTS):
- git rev-parse --abbrev-ref HEAD
- git rev-parse --short HEAD
- git status --porcelain

Preconditions:
- No commits on main.
- Working tree must be clean before execution begins.
- If Base HEAD changes, regenerate session artifacts from canonical tools before proceeding.

STOP conditions:
- STOP if any mismatch (branch, Base HEAD, missing work branch).
- STOP if working tree is dirty before execution begins.
- STOP if told to create or switch branches.

## 3.1.1 DP Preflight Gate (Run Before Any Edits)
Purpose:
- Catch malformed DP or TASK schema before work begins.

Worker runs (paste outcome in RESULTS):
- bash tools/lint/dp.sh --test
- bash tools/lint/dp.sh TASK.md
- bash tools/lint/task.sh

STOP if any preflight check fails.

### CbC Design Discipline Preflight (TASK.md §3.1.1)
Required when the DP objective adds, modifies, or replaces a linter, script, guard, or validation binary.
For non-tooling DPs: state "Not applicable" with a one-line justification.
{{CbC_PREFLIGHT}}

## 3.2 Required Context Load (Read Before Doing Anything)

### 3.2.1 Canon load order (always)
Worker must confirm loaded before edits begin:
1. PoT.md
2. SoP.md
3. PoW.md
4. TASK.md
5. docs/MAP.md
6. docs/MANUAL.md
7. ops/lib/manifests/CONTEXT.md

Notes:
- Worker does not read OPEN. OPEN is for Integrator state refresh and for receipts.
- Disposable artifacts must not be referenced or included.
- Worker input is the DP text only.
- DP writer must not attach or cite disposable artifacts.
- DP writer must not embed pasted bundles.

<!-- CCD: ff_target="machine-dense" ff_band="10-20" -->
# Contractor Brief

1 Temp path rule uses `var/tmp` only
2 RESULTS generation rule uses `ops/bin/certify` only
3 Receipt rule runs every DP mandated command and treats any skip as STOP
4 Disposable artifact rule treats OPEN dump bundles and manifests as non canonical session artifacts
5 Path rule uses existing repo paths or explicit `NEW` changelog markers and forbids globs and brace expansion

### 3.2.2 DP-scoped load order (per DP)
1. `ops/src/surfaces/dp.md.tpl` — full file; this is the primary modification target
2. `docs/ops/prompts/e-prompt-03.md` — full file; this is the secondary modification target
3. `tools/lint/dp.sh` — full file; the `CANONICAL_DP_TEMPLATE_SHA256` constant must be updated after `dp.md.tpl` is modified
4. `docs/MANUAL.md` — Closeout Cycle section only; confirm closing sidecar routing is already documented before any changes

## 3.3 Scope and Safety
Objective:
Eliminate the structural invitation for AI Integrators and Contractors to author predictive closing block metadata by patching `ops/src/surfaces/dp.md.tpl` §3.5.1 to route closing block generation exclusively through `ops/bin/certify`, and by patching `docs/ops/prompts/e-prompt-03.md` to hard-ban predictive closing content, conditional execution paths, and citation of session artifacts as canonical sources.

In scope:
- `ops/src/surfaces/dp.md.tpl` — §3.5.1 Mandatory Closing Block: replace bare label list with explicit certify-routing directive
- `docs/ops/prompts/e-prompt-03.md` — CONSTRAINTS section: add hard bans on predictive closing metadata, conditional execution paths, and OPEN/dump citation as canonical
- `tools/lint/dp.sh` — `CANONICAL_DP_TEMPLATE_SHA256` constant: update to match the post-edit `dp.md.tpl` hash
- `storage/dp/active/allowlist.txt` — update to DP-OPS-0107 target set
- `storage/handoff/CLOSING-DP-OPS-0107.md` — maintained during execution
- Standard closeout surfaces: llms bundles, compile archives, SoP/PoW ledger leaves

Out of scope:
- No changes to `ops/bin/certify` or its sidecar ingestion logic
- No changes to closing sidecar template or format
- No changes to `tools/lint/task.sh`, `tools/lint/style.sh`, or any other linter
- No changes to any file not listed in the allowlist
- No investigation of `ops/lib/scripts/common.sh` or Phase D-1 scaffold work
- No changes to `ops/bin/draft` or any binary other than the hash constant in `tools/lint/dp.sh`

Safety and invariants:
- No manual edits to generated outputs.
- No structural edits to TASK.md DP container outside approved slot content.
- Allowlist hard gate: every modified file must appear in `storage/dp/active/allowlist.txt` before any edits begin.
- The `CANONICAL_DP_TEMPLATE_SHA256` constant in `tools/lint/dp.sh` must be recomputed from the actual post-edit `dp.md.tpl` file. Pre-filling or estimating the hash is a hard STOP.
- Worker must run `bash tools/lint/dp.sh --test` after updating the hash constant to confirm the gate passes before proceeding to receipt commands.
- Work only in `var/tmp/` for scratch. Paths outside the allowlist are off-limits.
- Do not create, switch, or push branches.
- Do not commit. Operator commits only.
- STOP and report any ambiguity, missing input, or scope boundary condition before proceeding.

Worker Constraints (SSOT injected):
## Section 1: Universal Template Rules
- Template source uses `tpl` files with optional YAML frontmatter
- Canon frontmatter keys: `template_type` `template_id` `template_version` `requires_slots` `includes`
- Renderer strips frontmatter before output write
- Slot token form `\{\{TOKEN\}\}` with uppercase alphanumeric underscore
- Include forms `\{\{@include:path\}\}` and `\{\{@include:path#section\}\}`
- Include resolution is strict: missing file fail missing section fail circular graph fail
- Strict mode default: every required slot value present and no unresolved token
- Non strict mode allowed only for lint and normalization workflows
- Worker facing generated surfaces remain pointer first and exclude disposable artifacts

Target Files allowlist (hard gate):
- storage/dp/active/allowlist.txt

## 3.4 Execution Plan (A-E)

### 3.4.1 State
HEAD is `7567d65ae` on `main`. Working tree is clean. DP-OPS-0106 is closed and merged; the Dump Discipline and CDD/APD visibility contract is committed. `ops/src/surfaces/dp.md.tpl` §3.5.1 currently contains a bare label list: Primary Commit Header, Pull Request Title, Pull Request Description, Final Squash Stub, Extended Technical Manifest, Review Conversation Starter. No certify-routing instruction exists in that section. `docs/ops/prompts/e-prompt-03.md` currently contains no prohibition on predictive closing metadata, conditional execution paths, or citation of OPEN/dump as canonical sources.

### 3.4.2 Request
Apply two targeted edits and one hash constant update:

1. `ops/src/surfaces/dp.md.tpl` §3.5.1: Replace the bare label list with a certify-routing directive as specified in §3.4.4 Step 2.

2. `docs/ops/prompts/e-prompt-03.md` CONSTRAINTS section: Add four hard-ban lines as specified in §3.4.4 Step 3.

3. `tools/lint/dp.sh` `CANONICAL_DP_TEMPLATE_SHA256` constant: Recompute from the post-edit `dp.md.tpl` and update the constant as specified in §3.4.4 Step 4. This step must not be pre-filled.

### 3.4.3 Changelog
- `ops/src/surfaces/dp.md.tpl` — UPDATE (§3.5.1 certify-routing directive replaces bare label list)
- `docs/ops/prompts/e-prompt-03.md` — UPDATE (CONSTRAINTS section: four hard-ban additions)
- `tools/lint/dp.sh` — UPDATE (`CANONICAL_DP_TEMPLATE_SHA256` constant recomputed from post-edit template)
- `storage/dp/active/allowlist.txt` — UPDATE (DP-OPS-0107 target set)
- `storage/handoff/CLOSING-DP-OPS-0107.md` — MAINTAINED during execution
- Standard closeout surfaces: llms bundles, SoP/PoW leaves, compile archives

### 3.4.4 Patch / Diff
**Step 1 — Update allowlist.**
Write the following exact paths to `storage/dp/active/allowlist.txt` (one per line, no bullets):
```
ops/src/surfaces/dp.md.tpl
docs/ops/prompts/e-prompt-03.md
tools/lint/dp.sh
storage/dp/active/allowlist.txt
storage/handoff/CLOSING-DP-OPS-0107.md
archives/surfaces/SoP-2026-02-23-*.md
archives/surfaces/PoW-2026-02-23-*.md
archives/surfaces/TASK-DP-OPS-0107-*.md
archives/manifests/compile-*.md
llms.txt
llms-core.txt
llms-full.txt
```

**Step 2 — Edit `ops/src/surfaces/dp.md.tpl` §3.5.1.**
Locate the section:
```
 ### 3.5.1 Mandatory Closing Block
- Primary Commit Header
- Pull Request Title
- Pull Request Description
- Final Squash Stub
- Extended Technical Manifest
- Review Conversation Starter
```
Replace it in its entirety with:
```
 ### 3.5.1 Mandatory Closing Block
Closing block content is generated exclusively by `ops/bin/certify` from the closing sidecar at `storage/handoff/CLOSING-DP-OPS-0107.md` at certification time. Do not author, predict, populate, or approximate any closing block field at draft time or during execution. The closing sidecar is the only human-authored narrative input certify ingests. Certify generates all of the following from observable repository state: Primary Commit Header, Pull Request Title, Pull Request Description, Final Squash Stub, Extended Technical Manifest, and Review Conversation Starter.

Before running certify, confirm `storage/handoff/CLOSING-DP-OPS-0107.md` has been maintained throughout execution and reflects observable reality only.
```
Do not modify any other section of `dp.md.tpl`.

**Step 3 — Edit `docs/ops/prompts/e-prompt-03.md` CONSTRAINTS section.**
Locate the existing CONSTRAINTS block:
```
4. **CONSTRAINTS**:
   * Stay in plan scope. Paths exist in dump or marked NEW.
   * No pattern-paths/globs/brace-expansions (be literal).
   * Use exact section numbering per template.
```
Append the following four items to that list, preserving existing items verbatim:
```
   * Do not author, predict, or populate any §3.5.1 Mandatory Closing Block field (Primary Commit Header, Pull Request Title, Pull Request Description, Final Squash Stub, Extended Technical Manifest, Review Conversation Starter) at draft time or in any DP slot. These fields are generated by `ops/bin/certify` from the closing sidecar at certification time only.
   * Do not include conditional execution paths ("if X is found, then Y", "if viable, proceed with") in §3.4.4 Patch steps. Every step must be linear and unconditional. A DP requiring conditional branching is a mixed packet and must be split before drafting.
   * Do not cite OPEN artifacts, dump bundles, or any session artifact as a canonical source in any DP slot. These are non-canonical session artifacts for Integrator state refresh only.
   * STOP if any required input is missing, unverifiable, or ambiguous. Do not infer, approximate, or proceed on assumption.
```
Do not modify any other content in `e-prompt-03.md`.

**Step 4 — Recompute `CANONICAL_DP_TEMPLATE_SHA256` in `tools/lint/dp.sh`.**
After Step 2 is complete and `dp.md.tpl` is saved, run:
```
sha256sum ops/src/surfaces/dp.md.tpl
```
Record the output hash. In `tools/lint/dp.sh`, locate the line beginning with `CANONICAL_DP_TEMPLATE_SHA256=` and replace the hash value with the computed result. Do not proceed until this step is complete. This value must not be pre-filled or estimated.

**Step 5 — Verify.**
Run `bash tools/lint/dp.sh --test` and confirm it passes before proceeding to receipt commands. STOP if it fails.

### 3.4.5 Receipt (Proofs to collect) - MUST RUN

**Mandatory receipt commands (always run; do not omit):**
- bash tools/lint/dp.sh TASK.md
- bash tools/lint/task.sh
- bash tools/lint/integrity.sh
- bash tools/lint/style.sh
- git diff --name-only
- git diff --stat
- comm -23 <(git diff --name-only | sort) <(sort storage/dp/active/allowlist.txt) || true
- comm -23 <(git ls-files --others --exclude-standard | sort) <(sort storage/dp/active/allowlist.txt) || true
- ./ops/bin/open

**DP-specific receipt commands (scope-specific; author below):**
Note: Command substitution forms (e.g., $(pwd), $(git ...)) are rejected by certify replay. Use literal values only. dp.sh enforces at lint time.
**DP-specific receipt commands (scope-specific):**
Note: Command substitution forms (e.g., $(pwd), $(git ...)) are rejected by certify replay. Use literal values only. dp.sh enforces at lint time.
- sha256sum ops/src/surfaces/dp.md.tpl
- grep CANONICAL_DP_TEMPLATE_SHA256= tools/lint/dp.sh
- bash tools/lint/dp.sh --test
- grep -c "generated exclusively by `ops/bin/certify`" ops/src/surfaces/dp.md.tpl
- grep -c "Do not author, predict, or populate" docs/ops/prompts/e-prompt-03.md
- grep -c "mixed packet" docs/ops/prompts/e-prompt-03.md
- grep -c "STOP if any required input" docs/ops/prompts/e-prompt-03.md

## 3.5 Closeout (Mandatory Routing)
- Execute docs/MANUAL.md Closeout Cycle in order (Verify, Harvest, Refresh, Log, Prune).
- Update SoP.md and PoW.md with DP entries, including objective summary and verification commands run.
- Protocol order for closeout: Verify -> Generate Results -> COMMIT (Operator Only) -> Prune.
- Run prune hygiene: ./ops/bin/prune --scrub.
- Regenerate session artifacts: `./ops/bin/open --out=auto`
- Capture updated platform state: `./ops/bin/dump --scope=platform --format=chatgpt --out=auto`
- Ensure the next session begins with refreshed session artifacts and matching receipts.

### 3.5.1 Mandatory Closing Block
Closing block content is generated exclusively by `ops/bin/certify` from the closing sidecar at `storage/handoff/CLOSING-DP-OPS-0107.md` at certification time. Do not author, predict, populate, or approximate any closing block field at draft time or during execution. The closing sidecar is the only human-authored narrative input certify ingests. Certify generates all of the following from observable repository state: Primary Commit Header, Pull Request Title, Pull Request Description, Final Squash Stub, Extended Technical Manifest, and Review Conversation Starter.

Before running certify, confirm `storage/handoff/CLOSING-DP-OPS-0107.md` has been maintained throughout execution and reflects observable reality only.
