# STELA TASK DASHBOARD
Status: ACTIVE
Owner: Integrator
Last Updated: 2026-02-14

## 1. Session State
Pointer: Session context output (generated by ops/bin/open)
Context Manifest: ops/lib/manifests/CONTEXT.md (checked by tools/lint/context.sh)

Session State contract:
- Session State is pointer-first. Do not write inline branch or hash state here.
- OPEN (and OPEN-PORCELAIN when dirty) is the session state snapshot.
- RESULTS is the audit record for the DP.
- TASK is the stable routing surface.

## 2. Logic Pointers
Primary Constraint: PoT.md (Policy of Truth).
Surface Contract Spec: docs/ops/specs/surfaces/task.md.

### 2.1 Governance Pointers
Jurisdiction: PoT.md Section 3.
Git Authority: PoT.md Section 4.1.
Behavioral Standard: PoT.md Section 4.2.
Staffing Protocol: PoT.md Section 4.1.

### 2.2 Enforcement and Gates
Style: tools/lint/style.sh
Context: tools/lint/context.sh
Truth: tools/lint/truth.sh
DP lint: tools/lint/dp.sh (enforces DP transaction rules in Section 3; does not enforce TASK container schema)
TASK lint: tools/lint/task.sh (sole TASK surface/container schema enforcer)
Repo verify: tools/verify.sh
llms parity: tools/lint/llms.sh

## 3. Current Dispatch Packet (DP)

Section-by-section authoring protocol:
- Generate DP structure from `ops/src/surfaces/DP.md.tpl` using `ops/bin/draft`.
- Do not hand-edit DP structural boilerplate in `TASK.md` after generation.
- Manual edits are limited to slot content only (objective, scope, safety, plan body, receipt body).
- Freeze each completed subsection in order (3.1 -> 3.2 -> 3.3 -> 3.4.x).
- Do not revise earlier sections to justify later work. If scope changes, end the thread and draft a new DP.

Freeze Rule:
- If a completed DP subsection changes later, append an AMENDMENT block in RESULTS; otherwise DP is FAIL.

AMENDMENT: minimum fields
- Reopened section:
- Reason:
- Impact:
- Allowlist change (none or exact paths):

Input discipline (hard rule):
- Worker receives only the DP text. Worker does not require OPEN or DUMP attachments.
- DP writer must not attach or cite disposable artifacts (rough notes, chat logs, scratch drafts, throwaway excerpts).
- DP writer must not include pasted bundles (OPEN, DUMP payloads, manifests) inside the DP body.
- Canon pointers only. Evidence belongs in storage artifacts and RESULTS.

### DP-OPS-0065: Immutable workflow adoption (draft tool, DP template hash, dp lint reduction)

## 3.1 Freshness Gate (Must Pass Before Work)
Base Branch: main
Required Work Branch: work/dp-ops-0065-2026-02-14
Base HEAD: d3801c3a
Freshness Stamp: 2026-02-14

Required local re-check (worker runs; paste outputs in RESULTS):
- git rev-parse --abbrev-ref HEAD
- git rev-parse --short HEAD
- git status --porcelain

Preconditions:
- No commits on main.
- Working tree must be clean before execution begins.
- If Base HEAD changes, regenerate session artifacts from canonical tools before proceeding.

STOP conditions:
- STOP if any mismatch (branch, Base HEAD, missing work branch).
- STOP if working tree is dirty before execution begins.
- STOP if told to create or switch branches.

## 3.1.1 DP Preflight Gate (Run Before Any Edits)
Purpose:
- Catch malformed DP or TASK schema before work begins.

Worker runs (paste outcome in RESULTS):
- bash tools/lint/dp.sh --test
- bash tools/lint/dp.sh TASK.md
- bash tools/lint/task.sh

STOP if any preflight check fails.

## 3.2 Required Context Load (Read Before Doing Anything)

### 3.2.1 Canon load order (always)
Worker must confirm loaded before edits begin:
1. PoT.md
2. SoP.md
3. TASK.md
4. docs/MAP.md
5. docs/MANUAL.md
6. ops/lib/manifests/CONTEXT.md

Notes:
- Worker does not read OPEN. OPEN is for Integrator state refresh and for receipts.
- Disposable artifacts must not be referenced or included.
- Worker input is the DP text only.
- DP writer must not attach or cite disposable artifacts.
- DP writer must not embed pasted bundles.

### 3.2.2 DP-scoped load order (per DP)
- tools/lint/dp.sh
- docs/ops/specs/tools/lint/dp.md
- docs/ops/specs/surfaces/task.md
- docs/MANUAL.md
- ops/bin/llms
- tools/lint/llms.sh
- ops/bin/open
- docs/ops/specs/binaries/open.md
- ops/bin/prune
- docs/ops/specs/binaries/prune.md

## 3.3 Scope and Safety
Objective:
- Implement Phase 3: Immutable Workflow Adoption (Weeks 5-6): ship `ops/bin/draft` and canonical DP template, reduce DP lint structural complexity via template-hash verification, and codify immutable workflow rules in `PoT.md`.

In scope:
- Add a canonical DP template file and a single source-of-truth template hash for structure verification.
- Add `ops/bin/draft` to generate a DP skeleton from the canonical template and perform template-hash preflight.
- Refactor `tools/lint/dp.sh` to replace brittle structure regex checks with template-hash-based structure verification plus required-field checks.
- Deprecate manual editing of TASK.md structural boilerplate by documenting and enforcing the generated-template workflow.
- Update `PoT.md` to codify immutable workflow and generated-output rules required by Phase 3.
- Regenerate and commit llms outputs when `PoT.md` or `TASK.md` changes (tool-based only).

Out of scope:
- Any changes to manifest selection policy, manifest templates, or context inclusion beyond what is strictly required by Phase 3.
- Any changes to `opt/_factory` contents, migration mechanics, or repo-wide refactors.
- Any changes to non-DP lints, CI, or prune/open/dump semantics except where explicitly required to support Phase 3 deliverables.

Safety and invariants:
- Do not hand-edit generated outputs (including `llms*.txt`). Regenerate via tooling only.
- Do not broaden the allowlist. Any new touched file must be explicitly justified by Phase 3 deliverables.
- Preserve existing DP execution semantics and closeout routing requirements.
- Keep changes minimal, reviewable, and deterministic.

Target Files allowlist (hard gate):
- storage/dp/active/allowlist.txt

## 3.4 Execution Plan (A-E)

### 3.4.1 State
- Base is `main` at `d3801c3a`.
- `ops/bin/draft` does not exist yet; DP authoring still depends on manual boilerplate edits in `TASK.md`.
- `tools/lint/dp.sh` remains large and regex-heavy for structure checks.
- `PoT.md` and `TASK.md` are CORE surfaces; changing either requires `llms` regeneration for parity.

### 3.4.2 Request
- Deliver Phase 3 by introducing a canonical DP template and hash mechanism.
- Ship `ops/bin/draft` to generate DPs and update `TASK.md` deterministically.
- Reduce DP lint structure enforcement to template-hash verification plus required-field checks.
- Codify immutable generated-output workflow rules in `PoT.md`.

### 3.4.3 Changelog
- UPDATE: `storage/dp/active/allowlist.txt`
- NEW: `ops/bin/draft`
- NEW: `ops/src/surfaces/DP.md.tpl`
- UPDATE: `tools/lint/dp.sh`
- UPDATE: `TASK.md`
- UPDATE: `PoT.md`
- UPDATE: `SoP.md`
- UPDATE: `PoW.md`
- UPDATE: `docs/MANUAL.md`
- UPDATE: `docs/ops/specs/tools/lint/dp.md`
- UPDATE: `docs/ops/specs/surfaces/task.md`
- UPDATE (generated): `ops/lib/manifests/OPS.md`
- UPDATE (generated): `llms.txt`
- UPDATE (generated): `llms-core.txt`
- UPDATE (generated): `llms-full.txt`

### 3.4.4 Patch / Diff
A. Canonical DP template:
1. Create `ops/src/surfaces/DP.md.tpl` as the single schema source.
2. Encode placeholders for variable slots while preserving fixed headings/labels.
3. Store a canonical template sha256 in `tools/lint/dp.sh` and hard-fail on mismatch.

B. Draft tool release (`ops/bin/draft`):
1. Add required args for DP id/title/work branch/base head and slot intake via flags or sidecar file.
2. Render DP output from the canonical template.
3. Preflight template hash against `tools/lint/dp.sh`.
4. Write to `storage/dp/intake/DP-OPS-<id>.md` and replace the active TASK DP block.
5. Enforce guardrails: clean tree, unique TASK markers, required non-empty slots.

C. Lint reduction (`tools/lint/dp.sh`):
1. Replace brittle structural regex checks with normalized structure hashing against template.
2. Keep required-field checks for variable slots.
3. Keep allowlist pointer integrity: pointer file exists, non-empty, and each path exists in repo.
4. Update `--test` fixtures to cover template mismatch, structure mismatch, pointer/file validation.

D. Deprecate manual TASK structural edits:
1. Update `TASK.md` Section 3 prose to route structure generation through `ops/bin/draft`.
2. Update `docs/MANUAL.md` for open -> draft -> work sequence and explicit llms no-hand-edit rule.

E. Final cutover rules:
1. Update `PoT.md` to codify generated-output immutability (`llms*.txt` tooling-only).
2. Encode template-hash enforcement and draft-tool routing for DP structure.

F. Allowlist sidecar:
1. Replace `storage/dp/active/allowlist.txt` with the exact Phase 3 file set.

G. Generated outputs:
1. Run `./ops/bin/compile` and `./ops/bin/llms` after core surface updates; never hand-edit generated artifacts (`ops/lib/manifests/OPS.md`, `llms*.txt`).

### 3.4.5 Receipt (Proofs to collect) - MUST RUN
- ./ops/bin/open --out=auto --dp="DP-OPS-0065"
- ./ops/bin/dump --scope=platform --format=chatgpt --out=auto --bundle
- ./ops/bin/compile
- ./ops/bin/llms
- bash tools/lint/truth.sh
- bash tools/lint/task.sh
- bash tools/lint/dp.sh
- bash tools/lint/llms.sh
- bash tools/lint/context.sh
- bash tools/lint/style.sh
- ./tools/verify.sh
- ./ops/bin/prune --dry-run
- ./ops/bin/prune --target=pow --dry-run
- bash tools/lint/dp.sh storage/handoff/DP-OPS-0065-RESULTS.md
- git diff --name-only
- git diff --stat
- Verify Section 3.5 Closing Block is populated in RESULTS.
- Required pasted outputs: receipts, verification outcomes, and diff output.
- Mandatory Closing Block required in RESULTS.

## 3.5 Closeout (Mandatory Routing)
- Execute docs/MANUAL.md Closeout Cycle in order (Verify, Harvest, Refresh, Log, Prune).
- Update SoP.md and PoW.md with DP entries, including objective summary and verification commands run.
- Protocol order for closeout: Verify -> Generate Results -> COMMIT (Operator Only) -> Prune.
- Run prune hygiene at the end of the closeout sequence: ./ops/bin/prune --dp=DP-OPS-0065 --scrub.
- Use ./ops/bin/prune --reset-task only for explicit TASK baseline reset after PoW entry exists for the active DP id.
- Ensure the next session begins with refreshed session artifacts and matching receipts.

### 3.5.1 Mandatory Closing Block
Primary Commit Header (plaintext)

Pull Request Title (plaintext)

Pull Request Description (markdown)

Final Squash Stub (plaintext) (Must differ from #1)

Extended Technical Manifest (plaintext)

Review Conversation Starter (markdown)
