<!-- CCD: ff_target="operator-technical" ff_band="25-40" -->
## **Contractor (OPEN + DUMP + ADDENDUM REQUIRED)**
operator authorizes generation of a fresh OPEN (carrying `--intent="ADDENDUM REQUIRED: ..."` in `[FRESHNESS GATE]`), dump
payload + dump manifest. draft a new decision leaf pre-dump. include your reasoning for addendum. OPEN `[FRESHNESS GATE]` carries `ADDENDUM REQUIRED` in the `Intent for today:` line and the referenced decision leaf is present in the dump payload.
* Use `--scope=core` for standard operator audits when factory content is not under review.

